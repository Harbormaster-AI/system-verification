
<%= form_with model: @dSP, local: true do |form| %>
 
  <% if @dSP.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@dSP.errors.count, "error") %> prohibited
        this dSP from being saved:
      </h2>
      <ul>
        <% @dSP.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="card">
    <div class="card-body">

  <p>
    <%= form.label :name, { class: 'form-label'} %><br>
	<%= form.text_field :name, { class: 'form-label'} %>
  </p>			
  <p>
    <%= form.label :website, { class: 'form-label'} %><br>
	<%= form.text_field :website, { class: 'form-label'} %>
  </p>			
  <p>
    <%= form.label :region, { class: 'form-label'} %><br>
	<%= form.text_field :region, { class: 'form-label'} %>
  </p>			
  <p>
  </p>			

      <%= form.submit %>
    
    </div>
  
  </div> 
<% end %>