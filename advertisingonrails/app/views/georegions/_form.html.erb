
<%= form_with model: @geoRegion, local: true do |form| %>
 
  <% if @geoRegion.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@geoRegion.errors.count, "error") %> prohibited
        this geoRegion from being saved:
      </h2>
      <ul>
        <% @geoRegion.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="card">
    <div class="card-body">

  <p>
    <%= form.label :code, { class: 'form-label'} %><br>
	<%= form.text_field :code, { class: 'form-label'} %>
  </p>			
  <p>
    <%= form.label :name, { class: 'form-label'} %><br>
	<%= form.text_field :name, { class: 'form-label'} %>
  </p>			
  <p>
  </p>			
  <p>
  </p>			
  <p>
   <%= form.label :RegionType, { class: 'form-label'} %><br>
   <%= form.select :RegionType, GeoRegion.RegionTypes.keys, { class: 'form-control'} %>
  </p>			

      <%= form.submit %>
    
    </div>
  
  </div> 
<% end %>