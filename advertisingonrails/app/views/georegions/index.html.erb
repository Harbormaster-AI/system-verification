<div class="title text-center">
  List of GeoRegions
</div>

<table class="table table-sm table-hover table-striped"">
	<thead class="table-dark">
		<tr>

			<th style="text-align:center">code</th>
			<th style="text-align:center">name</th>
			<th style="text-align:center">RegionType</th>
			<th style="text-align:center" colspan="2">Actions</th>
	</thead>
	<tbody>
 
  <% @geoRegions.each do |geoRegion| %>
		<tr>   
			<td style="text-align:center">
	  			<%= geoRegion.code %>
			</td>
			<td style="text-align:center">
	  			<%= geoRegion.name %>
			</td>
			<td style="text-align:center">
	  			<%= geoRegion.RegionType %>
			</td>
			<td style="vertical-align:middle;text-align:center">
				<%= button_to 'Change', edit_geoRegion_path(geoRegion), :method => :get, :class => "btn btn-secondary btn-sm"%>
			</td>
			<td style="vertical-align:middle;text-align:center">
				<%= button_to 'Delete', geoRegion_path(geoRegion), :method => :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-danger btn-sm"%>
			</td>
    	</tr>
  <% end %>
</table>
<%= button_to 'New GeoRegion', new_geoRegion_path, :method => :get, :class => "btn btn-success btn-sm" %>