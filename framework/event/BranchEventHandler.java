/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.globalbank.event;

import org.axonframework.config.ProcessingGroup;
import org.axonframework.eventhandling.EventHandler;
import org.springframework.stereotype.Component;

import java.util.*;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.stream.Collectors;

import com.globalbank.api.*;
import com.globalbank.entity.*;
import com.globalbank.repository.*;

/**
 * ============================================================================
 * BranchEventHandler
 * ============================================================================
 *
 * Event handler for Branch as outlined for the CQRS pattern.  All reads related to Branch are invoked here
 * and dispersed as an event to be handled elsewhere.
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/12/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Banking Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core banking, payments, lending, cards, KYC, and FX capabilities.</td></tr>
 *          <tr><td>industry</td><td>Banking</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * Services Used:
 *
 *      BranchRepository
 *
 * Events Handled
 *      CreatedBranchEvent
 *      UpdatedBranchEvent
 *      DeletedBranchEvent
 *
 * @author your_name_here
 * ============================================================================
 */
@Component("branch-handler")
@ProcessingGroup("branch-processing-group")
public class BranchEventHandler {
	
	// core constructor
	BranchEventHandler(EntityManager entityManager) {
        this.entityManager = entityManager;
    }	
	
    /*
     * @param	event CreateBranchEvent
     */
    @EventHandler
    public void handle( CreateBranchEvent event) {
        entityManager.persist(new Branch(event.getBranchId(), event.getName(), event.getBranchCode(), event.getAddress(), event.getPhone(), event.getOpeningHours(), event.getBank(), event.getAccounts(), event.getLoanAccounts(), event.getAtms()));
    }

    /*
     * @param	event UpdateBranchEvent
     */
    @EventHandler
    public Branch handle( UpdateBranchEvent event) {
    	entityManager.merge(new Branch(event.getBranchId(), event.getName(), event.getBranchCode(), event.getAddress(), event.getPhone(), event.getOpeningHours(), event.getBank(), event.getAccounts(), event.getLoanAccounts(), event.getAtms()));
    }
    
    /*
     * @param	event DeleteBranchEvent
     */
    @EventHandler
    public void on( DeleteBranchEvent event) {
    	Branch entity = new Branch();
   
    	// ------------------------------------------
    	// apply the BranchId
    	// ------------------------------------------
    	entity.setBranchId( event.getBranchId() );
    	
    	// ------------------------------------------
    	// add to an Iterable and delete
    	// ------------------------------------------
    	entityManager.remove( entity );
    }    
    
    // attributes
	private final BranchRepository branchEntityRepository;
    private static final Logger LOGGER 			= Logger.getLogger(BranchEventHandler.class.getName());

}
