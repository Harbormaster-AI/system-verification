/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.globalbank.event;

import org.axonframework.config.ProcessingGroup;
import org.axonframework.eventhandling.EventHandler;
import org.springframework.stereotype.Component;

import java.util.*;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.stream.Collectors;

import com.globalbank.api.*;
import com.globalbank.entity.*;
import com.globalbank.repository.*;

/**
 * ============================================================================
 * ATMEventHandler
 * ============================================================================
 *
 * Event handler for ATM as outlined for the CQRS pattern.  All reads related to ATM are invoked here
 * and dispersed as an event to be handled elsewhere.
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/12/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Banking Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core banking, payments, lending, cards, KYC, and FX capabilities.</td></tr>
 *          <tr><td>industry</td><td>Banking</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * Services Used:
 *
 *      ATMRepository
 *
 * Events Handled
 *      CreatedATMEvent
 *      UpdatedATMEvent
 *      DeletedATMEvent
 *
 * @author your_name_here
 * ============================================================================
 */
@Component("aTM-handler")
@ProcessingGroup("aTM-processing-group")
public class ATMEventHandler {
	
	// core constructor
	ATMEventHandler(EntityManager entityManager) {
        this.entityManager = entityManager;
    }	
	
    /*
     * @param	event CreateATMEvent
     */
    @EventHandler
    public void handle( CreateATMEvent event) {
        entityManager.persist(new ATM(event.getATMId(), event.getTerminalId(), event.getLocation(), event.getBranch(), event.getStatus()));
    }

    /*
     * @param	event UpdateATMEvent
     */
    @EventHandler
    public ATM handle( UpdateATMEvent event) {
    	entityManager.merge(new ATM(event.getATMId(), event.getTerminalId(), event.getLocation(), event.getBranch(), event.getStatus()));
    }
    
    /*
     * @param	event DeleteATMEvent
     */
    @EventHandler
    public void on( DeleteATMEvent event) {
    	ATM entity = new ATM();
   
    	// ------------------------------------------
    	// apply the ATMId
    	// ------------------------------------------
    	entity.setATMId( event.getATMId() );
    	
    	// ------------------------------------------
    	// add to an Iterable and delete
    	// ------------------------------------------
    	entityManager.remove( entity );
    }    
    
    // attributes
	private final ATMRepository aTMEntityRepository;
    private static final Logger LOGGER 			= Logger.getLogger(ATMEventHandler.class.getName());

}
