/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.harbormaster.event;

import org.axonframework.config.ProcessingGroup;
import org.axonframework.eventhandling.EventHandler;
import org.springframework.stereotype.Component;

import java.util.*;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.stream.Collectors;

import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.repository.*;

/**
 * ============================================================================
 * ApiKeyEventHandler
 * ============================================================================
 *
 * Event handler for ApiKey as outlined for the CQRS pattern.  All reads related to ApiKey are invoked here
 * and dispersed as an event to be handled elsewhere.
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * Services Used:
 *
 *      ApiKeyRepository
 *
 * Events Handled
 *      CreatedApiKeyEvent
 *      UpdatedApiKeyEvent
 *      DeletedApiKeyEvent
 *
 * @author Harbormaster Dev Team
 * ============================================================================
 */
@Component("apiKey-handler")
@ProcessingGroup("apiKey-processing-group")
public class ApiKeyEventHandler {
	
	// core constructor
	ApiKeyEventHandler(EntityManager entityManager) {
        this.entityManager = entityManager;
    }	
	
    /*
     * @param	event CreateApiKeyEvent
     */
    @EventHandler
    public void handle( CreateApiKeyEvent event) {
        entityManager.persist(new ApiKey(event.getApiKeyId(), event.getKeyId(), event.getHashedSecret(), event.getCreatedAt(), event.getLastUsedAt(), event.getAccessPolicy()));
    }

    /*
     * @param	event UpdateApiKeyEvent
     */
    @EventHandler
    public ApiKey handle( UpdateApiKeyEvent event) {
    	entityManager.merge(new ApiKey(event.getApiKeyId(), event.getKeyId(), event.getHashedSecret(), event.getCreatedAt(), event.getLastUsedAt(), event.getAccessPolicy()));
    }
    
    /*
     * @param	event DeleteApiKeyEvent
     */
    @EventHandler
    public void on( DeleteApiKeyEvent event) {
    	ApiKey entity = new ApiKey();
   
    	// ------------------------------------------
    	// apply the ApiKeyId
    	// ------------------------------------------
    	entity.setApiKeyId( event.getApiKeyId() );
    	
    	// ------------------------------------------
    	// add to an Iterable and delete
    	// ------------------------------------------
    	entityManager.remove( entity );
    }    
    
    // attributes
	private final ApiKeyRepository apiKeyEntityRepository;
    private static final Logger LOGGER 			= Logger.getLogger(ApiKeyEventHandler.class.getName());

}
