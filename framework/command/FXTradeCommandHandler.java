package com.globalbank.command;

import java.util.*;

import org.axonframework.modelling.command.Repository;

import org.axonframework.commandhandling.CommandHandler;
import org.springframework.stereotype.Component;

import com.globalbank.aggregate.*;
import com.globalbank.api.*;
import com.globalbank.exception.*;
import com.globalbank.repository.*;
import com.globalbank.validator.*;

/**
 * Command handler for FXTrade as outlined for the CQRS pattern.  All creates, updates, deletes and other writes 
 * related to FXTrade are handled here as external events are delegated to an associated Aggregate.
 *
 * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/12/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Banking Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core banking, payments, lending, cards, KYC, and FX capabilities.</td></tr>
 *          <tr><td>industry</td><td>Banking</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * Services Used
 *
 *  	Repository<FXTradeAggregate>
 *
 * Produces
 *
 *      application/json
 *
 * Commands Accepted
 *
 *      CreateFXTradeCommand
 *      UpdateFXTradeCommand
 *      DeleteFXTradeCommand
 *
 * @author your_name_here
 *
 */
@Component
class FXTradeCommandHandler {

    public FXTradeCommandHandler( Repository<FXTradeAggregate> repository ) {
    	super();
    	this.aggregateRepository = repository;	
    }
    
    @CommandHandler
    public void handle(CreateFXTradeCommand command) throws Exception {
    	
    	// --------------------------------------
    	// validate the create command
    	// --------------------------------------    	
		FXTradeValidator.getInstance().validate( command );
		
    	// --------------------------------------
    	// delegate to the aggregate
    	// -------------------------------------- 
    	aggregateRepository.newInstance(() -> new FXTradeAggregate(command));
    }


    @CommandHandler
    public void handle(UpdateFXTradeCommand command) throws Exception {
    	// --------------------------------------
    	// validate the update command
    	// --------------------------------------    	
		FXTradeValidator.getInstance().validate( command );

    	// --------------------------------------
    	// delegate to the aggregate
    	// --------------------------------------
    	aggregateRepository.newInstance(() -> new FXTradeAggregate(command));
    }

    
    @CommandHandler
    public void handle(DeleteFXTradeCommand command) throws Exception {
    	// --------------------------------------
    	// validate the delete command
    	// --------------------------------------    	
		FXTradeValidator.getInstance().validate( command );    
		
    	// --------------------------------------
    	// delegate to the aggregate
    	// --------------------------------------
    	aggregateRepository.newInstance(() -> new FXTradeAggregate(command));
    }
    
    // attributes
    private final Repository<FXTradeAggregate> aggregateRepository;
}