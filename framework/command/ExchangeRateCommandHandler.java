package com.globalbank.command;

import java.util.*;

import org.axonframework.modelling.command.Repository;

import org.axonframework.commandhandling.CommandHandler;
import org.springframework.stereotype.Component;

import com.globalbank.aggregate.*;
import com.globalbank.api.*;
import com.globalbank.exception.*;
import com.globalbank.repository.*;
import com.globalbank.validator.*;

/**
 * Command handler for ExchangeRate as outlined for the CQRS pattern.  All creates, updates, deletes and other writes 
 * related to ExchangeRate are handled here as external events are delegated to an associated Aggregate.
 *
 * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/12/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Banking Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core banking, payments, lending, cards, KYC, and FX capabilities.</td></tr>
 *          <tr><td>industry</td><td>Banking</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * Services Used
 *
 *  	Repository<ExchangeRateAggregate>
 *
 * Produces
 *
 *      application/json
 *
 * Commands Accepted
 *
 *      CreateExchangeRateCommand
 *      UpdateExchangeRateCommand
 *      DeleteExchangeRateCommand
 *
 * @author your_name_here
 *
 */
@Component
class ExchangeRateCommandHandler {

    public ExchangeRateCommandHandler( Repository<ExchangeRateAggregate> repository ) {
    	super();
    	this.aggregateRepository = repository;	
    }
    
    @CommandHandler
    public void handle(CreateExchangeRateCommand command) throws Exception {
    	
    	// --------------------------------------
    	// validate the create command
    	// --------------------------------------    	
		ExchangeRateValidator.getInstance().validate( command );
		
    	// --------------------------------------
    	// delegate to the aggregate
    	// -------------------------------------- 
    	aggregateRepository.newInstance(() -> new ExchangeRateAggregate(command));
    }


    @CommandHandler
    public void handle(UpdateExchangeRateCommand command) throws Exception {
    	// --------------------------------------
    	// validate the update command
    	// --------------------------------------    	
		ExchangeRateValidator.getInstance().validate( command );

    	// --------------------------------------
    	// delegate to the aggregate
    	// --------------------------------------
    	aggregateRepository.newInstance(() -> new ExchangeRateAggregate(command));
    }

    
    @CommandHandler
    public void handle(DeleteExchangeRateCommand command) throws Exception {
    	// --------------------------------------
    	// validate the delete command
    	// --------------------------------------    	
		ExchangeRateValidator.getInstance().validate( command );    
		
    	// --------------------------------------
    	// delegate to the aggregate
    	// --------------------------------------
    	aggregateRepository.newInstance(() -> new ExchangeRateAggregate(command));
    }
    
    // attributes
    private final Repository<ExchangeRateAggregate> aggregateRepository;
}