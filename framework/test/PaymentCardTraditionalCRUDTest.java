/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.globalbank.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;

import com.globalbank.service.*;
import com.globalbank.entity.*;
import com.globalbank.api.*;

/**
 * Test PaymentCard class.
 *
 * @author your_name_here
 */
public class PaymentCardTest{

        // ------------------------------------
		// default constructor
	    // ------------------------------------
		public PaymentCardTest() {
			LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
		}

		// test methods
		@Test
		/**
		 * Full Create-Read-Update-Delete of a PaymentCard, through a
		 * PaymentCardTest.
		 */
		public void testCRUD() throws Throwable {
			try {
				LOGGER.info("**********************************************************");
				LOGGER.info("Beginning full CRUD test on PaymentCard...");

				testCreate();
//				testRead();
//				testUpdate();
				testGetAll();
//				testDelete();

				LOGGER.info("Successfully ran a full test on PaymentCardTest...");
				LOGGER.info("**********************************************************");
				LOGGER.info("");
			} catch (Throwable e) {
				throw e;
			} finally {
				if (handler != null) {
					handler.flush();
					LOGGER.removeHandler(handler);
				}
			}
		}

		/** 
		 * Tests creating a new PaymentCard.
		 */
		public void testCreate() throws Throwable {
			LOGGER.info( "\n======== CREATE ======== ");
			LOGGER.info( "-- Attempting to create a PaymentCard");

			StringBuilder msg = new StringBuilder( "-- Failed to create a PaymentCard" );

			try {            
				PaymentCard entity = PaymentCardService.getPaymentCardInstance().createPaymentCard( generateNewEntity() );
				thePrimaryKey = entity.getPaymentCardId();
				LOGGER.info( "-- Successfully created a PaymentCard with primary key" + thePrimaryKey );
			}
			catch (Exception e) {
				LOGGER.warning( unexpectedErrorMsg );
				LOGGER.warning( msg.toString() + " : " + e );

				throw e;
			}
		}

		/** 
		 * Tests reading a PaymentCard. 
		 */
		public PaymentCard testRead() throws Throwable {
			LOGGER.info( "\n======== READ ======== ");
			LOGGER.info( "-- Reading a previously created PaymentCard" );

			PaymentCard entity = null;
			StringBuilder msg = new StringBuilder( "-- Failed to read PaymentCard with primary key" );
			msg.append( thePrimaryKey );
			
			PaymentCardFetchOneSummary fetchOneSummary = new PaymentCardFetchOneSummary( thePrimaryKey );

			try {
				entity = PaymentCardService.getPaymentCardInstance().getPaymentCard( fetchOneSummary );

				assertNotNull( entity,msg.toString() );

				LOGGER.info( "-- Successfully found PaymentCard " + entity.toString() );
			}
			catch ( Throwable e ) {
				LOGGER.warning( unexpectedErrorMsg );
				LOGGER.warning( msg.toString() + " : " + e );

				throw e;
			}
			
			return entity;
		}

		/** 
		 * Tests updating a PaymentCard.
		 */
		public void testUpdate() throws Throwable {
			LOGGER.info( "\n======== UPDATE ======== ");
			LOGGER.info( "-- Attempting to update a PaymentCard." );

			StringBuilder msg = new StringBuilder( "Failed to update a PaymentCard : " );        
			PaymentCard entity = null;

			try {            
				entity = testRead();

				assertNotNull( entity, msg.toString() );

				LOGGER.info( "-- Now updating the created PaymentCard." );

				// for use later on...
				thePrimaryKey = entity.getPaymentCardId();

				PaymentCardService proxy = PaymentCardService.getPaymentCardInstance();            
				proxy.updatePaymentCard( entity );   

				LOGGER.info( "-- Successfully saved PaymentCard - " + entity.toString() );
			}
			catch ( Throwable e ) {
				LOGGER.warning( unexpectedErrorMsg );
				LOGGER.warning( msg.toString() + " : primarykey = " + thePrimaryKey + " : entity-" +  entity + " : " + e );

				throw e;
			}
		}

		/** 
		 * Tests deleting a PaymentCard.
		 */
		public void testDelete() throws Throwable {
			LOGGER.info( "\n======== DELETE ======== ");
			LOGGER.info( "-- Deleting a previously created PaymentCard." );

			try{
			    PaymentCard entity = testRead(); 
				PaymentCardService.getPaymentCardInstance().delete( entity );

				LOGGER.info( "-- Successfully deleted PaymentCard with primary key " + thePrimaryKey );            
			}
			catch ( Throwable e ) {
				LOGGER.warning( unexpectedErrorMsg );
				LOGGER.warning( "-- Failed to delete PaymentCard with primary key " + thePrimaryKey );

				throw e;
			}
		}

		/**
		 * Tests getting all PaymentCards.
		 */
		public void testGetAll() throws Throwable {    
			LOGGER.info( "======== GETALL ======== ");
			LOGGER.info( "-- Retrieving Collection of PaymentCards:" );

			StringBuilder msg = new StringBuilder( "-- Failed to get all PaymentCard : " );        
			List<PaymentCard> collection  = null;

			try {
				// call the static get method on the PaymentCardService
				collection = PaymentCardService.getPaymentCardInstance().getAllPaymentCard();

				if ( collection == null || collection.size() == 0 ) {
					LOGGER.warning( unexpectedErrorMsg );
					LOGGER.warning( msg.toString() + " Empty collection returned."  );
				}
				else {
					// Now print out the values
					PaymentCard entity = null;            
					Iterator<PaymentCard> iter = collection.iterator();
					int index = 1;

					while( iter.hasNext() ) {
						// Retrieve the entity   
						entity = iter.next();

						assertNotNull( entity,"-- null entity in Collection." );
						assertNotNull( entity.getPaymentCardId(), "-- entity in Collection has a null primary key" );        

						LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
						index++;
					}
				}
			}
			catch ( Throwable e ) {
				LOGGER.warning( unexpectedErrorMsg );
				LOGGER.warning( msg.toString() + " : " + e );

				throw e;
			}
		}

		/**
		 * Assigns a common log handler for each test class in the suite
		 * @param		handler	Handler
		 * @return		PaymentCardTest
		 */
		public PaymentCardTest setHandler(Handler handler) {
			this.handler = handler;
			LOGGER.addHandler(handler); // assign so the LOGGER can only output results to the Handler
			return this;
		}

		/**
		 * Returns a new populated PaymentCard
		 * 
		 * @return PaymentCard
		 */
		protected PaymentCard generateNewEntity() {
			PaymentCard entity = new PaymentCard( null,  new CardPAN(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  0,  0,  null,  null,  null,  new HashSet<>(),  CardType.values()[0],  CardStatus.values()[0],  CardNetwork.values()[0] );
			
			return( entity );
		}

		//-----------------------------------------------------
		// attributes 
		//-----------------------------------------------------
		protected UUID thePrimaryKey 						= null;
		private final Logger LOGGER 						= Logger.getLogger(PaymentCardTest.class.getName());
		private Handler handler 							= null;
		private String unexpectedErrorMsg 					= ":::::::::::::: Unexpected Error :::::::::::::::::";
	}
