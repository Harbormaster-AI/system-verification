/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.globalbank.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;

import com.globalbank.service.*;
import com.globalbank.entity.*;
import com.globalbank.api.*;

/**
 * Test FeeCharge class.
 *
 * @author your_name_here
 */
public class FeeChargeTest{

        // ------------------------------------
		// default constructor
	    // ------------------------------------
		public FeeChargeTest() {
			LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
		}

		// test methods
		@Test
		/**
		 * Full Create-Read-Update-Delete of a FeeCharge, through a
		 * FeeChargeTest.
		 */
		public void testCRUD() throws Throwable {
			try {
				LOGGER.info("**********************************************************");
				LOGGER.info("Beginning full CRUD test on FeeCharge...");

				testCreate();
//				testRead();
//				testUpdate();
				testGetAll();
//				testDelete();

				LOGGER.info("Successfully ran a full test on FeeChargeTest...");
				LOGGER.info("**********************************************************");
				LOGGER.info("");
			} catch (Throwable e) {
				throw e;
			} finally {
				if (handler != null) {
					handler.flush();
					LOGGER.removeHandler(handler);
				}
			}
		}

		/** 
		 * Tests creating a new FeeCharge.
		 */
		public void testCreate() throws Throwable {
			LOGGER.info( "\n======== CREATE ======== ");
			LOGGER.info( "-- Attempting to create a FeeCharge");

			StringBuilder msg = new StringBuilder( "-- Failed to create a FeeCharge" );

			try {            
				FeeCharge entity = FeeChargeService.getFeeChargeInstance().createFeeCharge( generateNewEntity() );
				thePrimaryKey = entity.getFeeChargeId();
				LOGGER.info( "-- Successfully created a FeeCharge with primary key" + thePrimaryKey );
			}
			catch (Exception e) {
				LOGGER.warning( unexpectedErrorMsg );
				LOGGER.warning( msg.toString() + " : " + e );

				throw e;
			}
		}

		/** 
		 * Tests reading a FeeCharge. 
		 */
		public FeeCharge testRead() throws Throwable {
			LOGGER.info( "\n======== READ ======== ");
			LOGGER.info( "-- Reading a previously created FeeCharge" );

			FeeCharge entity = null;
			StringBuilder msg = new StringBuilder( "-- Failed to read FeeCharge with primary key" );
			msg.append( thePrimaryKey );
			
			FeeChargeFetchOneSummary fetchOneSummary = new FeeChargeFetchOneSummary( thePrimaryKey );

			try {
				entity = FeeChargeService.getFeeChargeInstance().getFeeCharge( fetchOneSummary );

				assertNotNull( entity,msg.toString() );

				LOGGER.info( "-- Successfully found FeeCharge " + entity.toString() );
			}
			catch ( Throwable e ) {
				LOGGER.warning( unexpectedErrorMsg );
				LOGGER.warning( msg.toString() + " : " + e );

				throw e;
			}
			
			return entity;
		}

		/** 
		 * Tests updating a FeeCharge.
		 */
		public void testUpdate() throws Throwable {
			LOGGER.info( "\n======== UPDATE ======== ");
			LOGGER.info( "-- Attempting to update a FeeCharge." );

			StringBuilder msg = new StringBuilder( "Failed to update a FeeCharge : " );        
			FeeCharge entity = null;

			try {            
				entity = testRead();

				assertNotNull( entity, msg.toString() );

				LOGGER.info( "-- Now updating the created FeeCharge." );

				// for use later on...
				thePrimaryKey = entity.getFeeChargeId();

				FeeChargeService proxy = FeeChargeService.getFeeChargeInstance();            
				proxy.updateFeeCharge( entity );   

				LOGGER.info( "-- Successfully saved FeeCharge - " + entity.toString() );
			}
			catch ( Throwable e ) {
				LOGGER.warning( unexpectedErrorMsg );
				LOGGER.warning( msg.toString() + " : primarykey = " + thePrimaryKey + " : entity-" +  entity + " : " + e );

				throw e;
			}
		}

		/** 
		 * Tests deleting a FeeCharge.
		 */
		public void testDelete() throws Throwable {
			LOGGER.info( "\n======== DELETE ======== ");
			LOGGER.info( "-- Deleting a previously created FeeCharge." );

			try{
			    FeeCharge entity = testRead(); 
				FeeChargeService.getFeeChargeInstance().delete( entity );

				LOGGER.info( "-- Successfully deleted FeeCharge with primary key " + thePrimaryKey );            
			}
			catch ( Throwable e ) {
				LOGGER.warning( unexpectedErrorMsg );
				LOGGER.warning( "-- Failed to delete FeeCharge with primary key " + thePrimaryKey );

				throw e;
			}
		}

		/**
		 * Tests getting all FeeCharges.
		 */
		public void testGetAll() throws Throwable {    
			LOGGER.info( "======== GETALL ======== ");
			LOGGER.info( "-- Retrieving Collection of FeeCharges:" );

			StringBuilder msg = new StringBuilder( "-- Failed to get all FeeCharge : " );        
			List<FeeCharge> collection  = null;

			try {
				// call the static get method on the FeeChargeService
				collection = FeeChargeService.getFeeChargeInstance().getAllFeeCharge();

				if ( collection == null || collection.size() == 0 ) {
					LOGGER.warning( unexpectedErrorMsg );
					LOGGER.warning( msg.toString() + " Empty collection returned."  );
				}
				else {
					// Now print out the values
					FeeCharge entity = null;            
					Iterator<FeeCharge> iter = collection.iterator();
					int index = 1;

					while( iter.hasNext() ) {
						// Retrieve the entity   
						entity = iter.next();

						assertNotNull( entity,"-- null entity in Collection." );
						assertNotNull( entity.getFeeChargeId(), "-- entity in Collection has a null primary key" );        

						LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
						index++;
					}
				}
			}
			catch ( Throwable e ) {
				LOGGER.warning( unexpectedErrorMsg );
				LOGGER.warning( msg.toString() + " : " + e );

				throw e;
			}
		}

		/**
		 * Assigns a common log handler for each test class in the suite
		 * @param		handler	Handler
		 * @return		FeeChargeTest
		 */
		public FeeChargeTest setHandler(Handler handler) {
			this.handler = handler;
			LOGGER.addHandler(handler); // assign so the LOGGER can only output results to the Handler
			return this;
		}

		/**
		 * Returns a new populated FeeCharge
		 * 
		 * @return FeeCharge
		 */
		protected FeeCharge generateNewEntity() {
			FeeCharge entity = new FeeCharge( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Money(),   new Date(),  null,  null,  FeeType.values()[0] );
			
			return( entity );
		}

		//-----------------------------------------------------
		// attributes 
		//-----------------------------------------------------
		protected UUID thePrimaryKey 						= null;
		private final Logger LOGGER 						= Logger.getLogger(FeeChargeTest.class.getName());
		private Handler handler 							= null;
		private String unexpectedErrorMsg 					= ":::::::::::::: Unexpected Error :::::::::::::::::";
	}
