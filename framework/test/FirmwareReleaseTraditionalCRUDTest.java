/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test FirmwareRelease class.
 *
 * @author Harbormaster Dev Team
 */
public class FirmwareReleaseTest{

        // ------------------------------------
		// default constructor
	    // ------------------------------------
		public FirmwareReleaseTest() {
			LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
		}

		// test methods
		@Test
		/**
		 * Full Create-Read-Update-Delete of a FirmwareRelease, through a
		 * FirmwareReleaseTest.
		 */
		public void testCRUD() throws Throwable {
			try {
				LOGGER.info("**********************************************************");
				LOGGER.info("Beginning full CRUD test on FirmwareRelease...");

				testCreate();
//				testRead();
//				testUpdate();
				testGetAll();
//				testDelete();

				LOGGER.info("Successfully ran a full test on FirmwareReleaseTest...");
				LOGGER.info("**********************************************************");
				LOGGER.info("");
			} catch (Throwable e) {
				throw e;
			} finally {
				if (handler != null) {
					handler.flush();
					LOGGER.removeHandler(handler);
				}
			}
		}

		/** 
		 * Tests creating a new FirmwareRelease.
		 */
		public void testCreate() throws Throwable {
			LOGGER.info( "\n======== CREATE ======== ");
			LOGGER.info( "-- Attempting to create a FirmwareRelease");

			StringBuilder msg = new StringBuilder( "-- Failed to create a FirmwareRelease" );

			try {            
				FirmwareRelease entity = FirmwareReleaseService.getFirmwareReleaseInstance().createFirmwareRelease( generateNewEntity() );
				thePrimaryKey = entity.getFirmwareReleaseId();
				LOGGER.info( "-- Successfully created a FirmwareRelease with primary key" + thePrimaryKey );
			}
			catch (Exception e) {
				LOGGER.warning( unexpectedErrorMsg );
				LOGGER.warning( msg.toString() + " : " + e );

				throw e;
			}
		}

		/** 
		 * Tests reading a FirmwareRelease. 
		 */
		public FirmwareRelease testRead() throws Throwable {
			LOGGER.info( "\n======== READ ======== ");
			LOGGER.info( "-- Reading a previously created FirmwareRelease" );

			FirmwareRelease entity = null;
			StringBuilder msg = new StringBuilder( "-- Failed to read FirmwareRelease with primary key" );
			msg.append( thePrimaryKey );
			
			FirmwareReleaseFetchOneSummary fetchOneSummary = new FirmwareReleaseFetchOneSummary( thePrimaryKey );

			try {
				entity = FirmwareReleaseService.getFirmwareReleaseInstance().getFirmwareRelease( fetchOneSummary );

				assertNotNull( entity,msg.toString() );

				LOGGER.info( "-- Successfully found FirmwareRelease " + entity.toString() );
			}
			catch ( Throwable e ) {
				LOGGER.warning( unexpectedErrorMsg );
				LOGGER.warning( msg.toString() + " : " + e );

				throw e;
			}
			
			return entity;
		}

		/** 
		 * Tests updating a FirmwareRelease.
		 */
		public void testUpdate() throws Throwable {
			LOGGER.info( "\n======== UPDATE ======== ");
			LOGGER.info( "-- Attempting to update a FirmwareRelease." );

			StringBuilder msg = new StringBuilder( "Failed to update a FirmwareRelease : " );        
			FirmwareRelease entity = null;

			try {            
				entity = testRead();

				assertNotNull( entity, msg.toString() );

				LOGGER.info( "-- Now updating the created FirmwareRelease." );

				// for use later on...
				thePrimaryKey = entity.getFirmwareReleaseId();

				FirmwareReleaseService proxy = FirmwareReleaseService.getFirmwareReleaseInstance();            
				proxy.updateFirmwareRelease( entity );   

				LOGGER.info( "-- Successfully saved FirmwareRelease - " + entity.toString() );
			}
			catch ( Throwable e ) {
				LOGGER.warning( unexpectedErrorMsg );
				LOGGER.warning( msg.toString() + " : primarykey = " + thePrimaryKey + " : entity-" +  entity + " : " + e );

				throw e;
			}
		}

		/** 
		 * Tests deleting a FirmwareRelease.
		 */
		public void testDelete() throws Throwable {
			LOGGER.info( "\n======== DELETE ======== ");
			LOGGER.info( "-- Deleting a previously created FirmwareRelease." );

			try{
			    FirmwareRelease entity = testRead(); 
				FirmwareReleaseService.getFirmwareReleaseInstance().delete( entity );

				LOGGER.info( "-- Successfully deleted FirmwareRelease with primary key " + thePrimaryKey );            
			}
			catch ( Throwable e ) {
				LOGGER.warning( unexpectedErrorMsg );
				LOGGER.warning( "-- Failed to delete FirmwareRelease with primary key " + thePrimaryKey );

				throw e;
			}
		}

		/**
		 * Tests getting all FirmwareReleases.
		 */
		public void testGetAll() throws Throwable {    
			LOGGER.info( "======== GETALL ======== ");
			LOGGER.info( "-- Retrieving Collection of FirmwareReleases:" );

			StringBuilder msg = new StringBuilder( "-- Failed to get all FirmwareRelease : " );        
			List<FirmwareRelease> collection  = null;

			try {
				// call the static get method on the FirmwareReleaseService
				collection = FirmwareReleaseService.getFirmwareReleaseInstance().getAllFirmwareRelease();

				if ( collection == null || collection.size() == 0 ) {
					LOGGER.warning( unexpectedErrorMsg );
					LOGGER.warning( msg.toString() + " Empty collection returned."  );
				}
				else {
					// Now print out the values
					FirmwareRelease entity = null;            
					Iterator<FirmwareRelease> iter = collection.iterator();
					int index = 1;

					while( iter.hasNext() ) {
						// Retrieve the entity   
						entity = iter.next();

						assertNotNull( entity,"-- null entity in Collection." );
						assertNotNull( entity.getFirmwareReleaseId(), "-- entity in Collection has a null primary key" );        

						LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
						index++;
					}
				}
			}
			catch ( Throwable e ) {
				LOGGER.warning( unexpectedErrorMsg );
				LOGGER.warning( msg.toString() + " : " + e );

				throw e;
			}
		}

		/**
		 * Assigns a common log handler for each test class in the suite
		 * @param		handler	Handler
		 * @return		FirmwareReleaseTest
		 */
		public FirmwareReleaseTest setHandler(Handler handler) {
			this.handler = handler;
			LOGGER.addHandler(handler); // assign so the LOGGER can only output results to the Handler
			return this;
		}

		/**
		 * Returns a new populated FirmwareRelease
		 * 
		 * @return FirmwareRelease
		 */
		protected FirmwareRelease generateNewEntity() {
			FirmwareRelease entity = new FirmwareRelease( null,  new FirmwareVersion(),   new Date(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Checksum(),  null );
			
			return( entity );
		}

		//-----------------------------------------------------
		// attributes 
		//-----------------------------------------------------
		protected UUID thePrimaryKey 						= null;
		private final Logger LOGGER 						= Logger.getLogger(FirmwareReleaseTest.class.getName());
		private Handler handler 							= null;
		private String unexpectedErrorMsg 					= ":::::::::::::: Unexpected Error :::::::::::::::::";
	}
