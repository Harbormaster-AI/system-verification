/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test AccessPolicy class.
 *
 * @author Harbormaster Dev Team
 */
public class AccessPolicyTest{

        // ------------------------------------
		// default constructor
	    // ------------------------------------
		public AccessPolicyTest() {
			LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
		}

		// test methods
		@Test
		/**
		 * Full Create-Read-Update-Delete of a AccessPolicy, through a
		 * AccessPolicyTest.
		 */
		public void testCRUD() throws Throwable {
			try {
				LOGGER.info("**********************************************************");
				LOGGER.info("Beginning full CRUD test on AccessPolicy...");

				testCreate();
//				testRead();
//				testUpdate();
				testGetAll();
//				testDelete();

				LOGGER.info("Successfully ran a full test on AccessPolicyTest...");
				LOGGER.info("**********************************************************");
				LOGGER.info("");
			} catch (Throwable e) {
				throw e;
			} finally {
				if (handler != null) {
					handler.flush();
					LOGGER.removeHandler(handler);
				}
			}
		}

		/** 
		 * Tests creating a new AccessPolicy.
		 */
		public void testCreate() throws Throwable {
			LOGGER.info( "\n======== CREATE ======== ");
			LOGGER.info( "-- Attempting to create a AccessPolicy");

			StringBuilder msg = new StringBuilder( "-- Failed to create a AccessPolicy" );

			try {            
				AccessPolicy entity = AccessPolicyService.getAccessPolicyInstance().createAccessPolicy( generateNewEntity() );
				thePrimaryKey = entity.getAccessPolicyId();
				LOGGER.info( "-- Successfully created a AccessPolicy with primary key" + thePrimaryKey );
			}
			catch (Exception e) {
				LOGGER.warning( unexpectedErrorMsg );
				LOGGER.warning( msg.toString() + " : " + e );

				throw e;
			}
		}

		/** 
		 * Tests reading a AccessPolicy. 
		 */
		public AccessPolicy testRead() throws Throwable {
			LOGGER.info( "\n======== READ ======== ");
			LOGGER.info( "-- Reading a previously created AccessPolicy" );

			AccessPolicy entity = null;
			StringBuilder msg = new StringBuilder( "-- Failed to read AccessPolicy with primary key" );
			msg.append( thePrimaryKey );
			
			AccessPolicyFetchOneSummary fetchOneSummary = new AccessPolicyFetchOneSummary( thePrimaryKey );

			try {
				entity = AccessPolicyService.getAccessPolicyInstance().getAccessPolicy( fetchOneSummary );

				assertNotNull( entity,msg.toString() );

				LOGGER.info( "-- Successfully found AccessPolicy " + entity.toString() );
			}
			catch ( Throwable e ) {
				LOGGER.warning( unexpectedErrorMsg );
				LOGGER.warning( msg.toString() + " : " + e );

				throw e;
			}
			
			return entity;
		}

		/** 
		 * Tests updating a AccessPolicy.
		 */
		public void testUpdate() throws Throwable {
			LOGGER.info( "\n======== UPDATE ======== ");
			LOGGER.info( "-- Attempting to update a AccessPolicy." );

			StringBuilder msg = new StringBuilder( "Failed to update a AccessPolicy : " );        
			AccessPolicy entity = null;

			try {            
				entity = testRead();

				assertNotNull( entity, msg.toString() );

				LOGGER.info( "-- Now updating the created AccessPolicy." );

				// for use later on...
				thePrimaryKey = entity.getAccessPolicyId();

				AccessPolicyService proxy = AccessPolicyService.getAccessPolicyInstance();            
				proxy.updateAccessPolicy( entity );   

				LOGGER.info( "-- Successfully saved AccessPolicy - " + entity.toString() );
			}
			catch ( Throwable e ) {
				LOGGER.warning( unexpectedErrorMsg );
				LOGGER.warning( msg.toString() + " : primarykey = " + thePrimaryKey + " : entity-" +  entity + " : " + e );

				throw e;
			}
		}

		/** 
		 * Tests deleting a AccessPolicy.
		 */
		public void testDelete() throws Throwable {
			LOGGER.info( "\n======== DELETE ======== ");
			LOGGER.info( "-- Deleting a previously created AccessPolicy." );

			try{
			    AccessPolicy entity = testRead(); 
				AccessPolicyService.getAccessPolicyInstance().delete( entity );

				LOGGER.info( "-- Successfully deleted AccessPolicy with primary key " + thePrimaryKey );            
			}
			catch ( Throwable e ) {
				LOGGER.warning( unexpectedErrorMsg );
				LOGGER.warning( "-- Failed to delete AccessPolicy with primary key " + thePrimaryKey );

				throw e;
			}
		}

		/**
		 * Tests getting all AccessPolicys.
		 */
		public void testGetAll() throws Throwable {    
			LOGGER.info( "======== GETALL ======== ");
			LOGGER.info( "-- Retrieving Collection of AccessPolicys:" );

			StringBuilder msg = new StringBuilder( "-- Failed to get all AccessPolicy : " );        
			List<AccessPolicy> collection  = null;

			try {
				// call the static get method on the AccessPolicyService
				collection = AccessPolicyService.getAccessPolicyInstance().getAllAccessPolicy();

				if ( collection == null || collection.size() == 0 ) {
					LOGGER.warning( unexpectedErrorMsg );
					LOGGER.warning( msg.toString() + " Empty collection returned."  );
				}
				else {
					// Now print out the values
					AccessPolicy entity = null;            
					Iterator<AccessPolicy> iter = collection.iterator();
					int index = 1;

					while( iter.hasNext() ) {
						// Retrieve the entity   
						entity = iter.next();

						assertNotNull( entity,"-- null entity in Collection." );
						assertNotNull( entity.getAccessPolicyId(), "-- entity in Collection has a null primary key" );        

						LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
						index++;
					}
				}
			}
			catch ( Throwable e ) {
				LOGGER.warning( unexpectedErrorMsg );
				LOGGER.warning( msg.toString() + " : " + e );

				throw e;
			}
		}

		/**
		 * Assigns a common log handler for each test class in the suite
		 * @param		handler	Handler
		 * @return		AccessPolicyTest
		 */
		public AccessPolicyTest setHandler(Handler handler) {
			this.handler = handler;
			LOGGER.addHandler(handler); // assign so the LOGGER can only output results to the Handler
			return this;
		}

		/**
		 * Returns a new populated AccessPolicy
		 * 
		 * @return AccessPolicy
		 */
		protected AccessPolicy generateNewEntity() {
			AccessPolicy entity = new AccessPolicy( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),   new Date(),  null,  new HashSet<>(),  new HashSet<>() );
			
			return( entity );
		}

		//-----------------------------------------------------
		// attributes 
		//-----------------------------------------------------
		protected UUID thePrimaryKey 						= null;
		private final Logger LOGGER 						= Logger.getLogger(AccessPolicyTest.class.getName());
		private Handler handler 							= null;
		private String unexpectedErrorMsg 					= ":::::::::::::: Unexpected Error :::::::::::::::::";
	}
