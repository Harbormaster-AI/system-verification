/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.globalbank.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;

import com.globalbank.service.*;
import com.globalbank.entity.*;
import com.globalbank.api.*;

/**
 * Test LoanPayment class.
 *
 * @author your_name_here
 */
public class LoanPaymentTest{

        // ------------------------------------
		// default constructor
	    // ------------------------------------
		public LoanPaymentTest() {
			LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
		}

		// test methods
		@Test
		/**
		 * Full Create-Read-Update-Delete of a LoanPayment, through a
		 * LoanPaymentTest.
		 */
		public void testCRUD() throws Throwable {
			try {
				LOGGER.info("**********************************************************");
				LOGGER.info("Beginning full CRUD test on LoanPayment...");

				testCreate();
//				testRead();
//				testUpdate();
				testGetAll();
//				testDelete();

				LOGGER.info("Successfully ran a full test on LoanPaymentTest...");
				LOGGER.info("**********************************************************");
				LOGGER.info("");
			} catch (Throwable e) {
				throw e;
			} finally {
				if (handler != null) {
					handler.flush();
					LOGGER.removeHandler(handler);
				}
			}
		}

		/** 
		 * Tests creating a new LoanPayment.
		 */
		public void testCreate() throws Throwable {
			LOGGER.info( "\n======== CREATE ======== ");
			LOGGER.info( "-- Attempting to create a LoanPayment");

			StringBuilder msg = new StringBuilder( "-- Failed to create a LoanPayment" );

			try {            
				LoanPayment entity = LoanPaymentService.getLoanPaymentInstance().createLoanPayment( generateNewEntity() );
				thePrimaryKey = entity.getLoanPaymentId();
				LOGGER.info( "-- Successfully created a LoanPayment with primary key" + thePrimaryKey );
			}
			catch (Exception e) {
				LOGGER.warning( unexpectedErrorMsg );
				LOGGER.warning( msg.toString() + " : " + e );

				throw e;
			}
		}

		/** 
		 * Tests reading a LoanPayment. 
		 */
		public LoanPayment testRead() throws Throwable {
			LOGGER.info( "\n======== READ ======== ");
			LOGGER.info( "-- Reading a previously created LoanPayment" );

			LoanPayment entity = null;
			StringBuilder msg = new StringBuilder( "-- Failed to read LoanPayment with primary key" );
			msg.append( thePrimaryKey );
			
			LoanPaymentFetchOneSummary fetchOneSummary = new LoanPaymentFetchOneSummary( thePrimaryKey );

			try {
				entity = LoanPaymentService.getLoanPaymentInstance().getLoanPayment( fetchOneSummary );

				assertNotNull( entity,msg.toString() );

				LOGGER.info( "-- Successfully found LoanPayment " + entity.toString() );
			}
			catch ( Throwable e ) {
				LOGGER.warning( unexpectedErrorMsg );
				LOGGER.warning( msg.toString() + " : " + e );

				throw e;
			}
			
			return entity;
		}

		/** 
		 * Tests updating a LoanPayment.
		 */
		public void testUpdate() throws Throwable {
			LOGGER.info( "\n======== UPDATE ======== ");
			LOGGER.info( "-- Attempting to update a LoanPayment." );

			StringBuilder msg = new StringBuilder( "Failed to update a LoanPayment : " );        
			LoanPayment entity = null;

			try {            
				entity = testRead();

				assertNotNull( entity, msg.toString() );

				LOGGER.info( "-- Now updating the created LoanPayment." );

				// for use later on...
				thePrimaryKey = entity.getLoanPaymentId();

				LoanPaymentService proxy = LoanPaymentService.getLoanPaymentInstance();            
				proxy.updateLoanPayment( entity );   

				LOGGER.info( "-- Successfully saved LoanPayment - " + entity.toString() );
			}
			catch ( Throwable e ) {
				LOGGER.warning( unexpectedErrorMsg );
				LOGGER.warning( msg.toString() + " : primarykey = " + thePrimaryKey + " : entity-" +  entity + " : " + e );

				throw e;
			}
		}

		/** 
		 * Tests deleting a LoanPayment.
		 */
		public void testDelete() throws Throwable {
			LOGGER.info( "\n======== DELETE ======== ");
			LOGGER.info( "-- Deleting a previously created LoanPayment." );

			try{
			    LoanPayment entity = testRead(); 
				LoanPaymentService.getLoanPaymentInstance().delete( entity );

				LOGGER.info( "-- Successfully deleted LoanPayment with primary key " + thePrimaryKey );            
			}
			catch ( Throwable e ) {
				LOGGER.warning( unexpectedErrorMsg );
				LOGGER.warning( "-- Failed to delete LoanPayment with primary key " + thePrimaryKey );

				throw e;
			}
		}

		/**
		 * Tests getting all LoanPayments.
		 */
		public void testGetAll() throws Throwable {    
			LOGGER.info( "======== GETALL ======== ");
			LOGGER.info( "-- Retrieving Collection of LoanPayments:" );

			StringBuilder msg = new StringBuilder( "-- Failed to get all LoanPayment : " );        
			List<LoanPayment> collection  = null;

			try {
				// call the static get method on the LoanPaymentService
				collection = LoanPaymentService.getLoanPaymentInstance().getAllLoanPayment();

				if ( collection == null || collection.size() == 0 ) {
					LOGGER.warning( unexpectedErrorMsg );
					LOGGER.warning( msg.toString() + " Empty collection returned."  );
				}
				else {
					// Now print out the values
					LoanPayment entity = null;            
					Iterator<LoanPayment> iter = collection.iterator();
					int index = 1;

					while( iter.hasNext() ) {
						// Retrieve the entity   
						entity = iter.next();

						assertNotNull( entity,"-- null entity in Collection." );
						assertNotNull( entity.getLoanPaymentId(), "-- entity in Collection has a null primary key" );        

						LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
						index++;
					}
				}
			}
			catch ( Throwable e ) {
				LOGGER.warning( unexpectedErrorMsg );
				LOGGER.warning( msg.toString() + " : " + e );

				throw e;
			}
		}

		/**
		 * Assigns a common log handler for each test class in the suite
		 * @param		handler	Handler
		 * @return		LoanPaymentTest
		 */
		public LoanPaymentTest setHandler(Handler handler) {
			this.handler = handler;
			LOGGER.addHandler(handler); // assign so the LOGGER can only output results to the Handler
			return this;
		}

		/**
		 * Returns a new populated LoanPayment
		 * 
		 * @return LoanPayment
		 */
		protected LoanPayment generateNewEntity() {
			LoanPayment entity = new LoanPayment( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Money(),   new Date(),  null,  null,  PaymentMethod.values()[0],  PaymentStatus.values()[0] );
			
			return( entity );
		}

		//-----------------------------------------------------
		// attributes 
		//-----------------------------------------------------
		protected UUID thePrimaryKey 						= null;
		private final Logger LOGGER 						= Logger.getLogger(LoanPaymentTest.class.getName());
		private Handler handler 							= null;
		private String unexpectedErrorMsg 					= ":::::::::::::: Unexpected Error :::::::::::::::::";
	}
