/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.globalbank.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;

import com.globalbank.service.*;
import com.globalbank.entity.*;
import com.globalbank.api.*;

/**
 * Test ExchangeRate class.
 *
 * @author your_name_here
 */
public class ExchangeRateTest{

        // ------------------------------------
		// default constructor
	    // ------------------------------------
		public ExchangeRateTest() {
			LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
		}

		// test methods
		@Test
		/**
		 * Full Create-Read-Update-Delete of a ExchangeRate, through a
		 * ExchangeRateTest.
		 */
		public void testCRUD() throws Throwable {
			try {
				LOGGER.info("**********************************************************");
				LOGGER.info("Beginning full CRUD test on ExchangeRate...");

				testCreate();
//				testRead();
//				testUpdate();
				testGetAll();
//				testDelete();

				LOGGER.info("Successfully ran a full test on ExchangeRateTest...");
				LOGGER.info("**********************************************************");
				LOGGER.info("");
			} catch (Throwable e) {
				throw e;
			} finally {
				if (handler != null) {
					handler.flush();
					LOGGER.removeHandler(handler);
				}
			}
		}

		/** 
		 * Tests creating a new ExchangeRate.
		 */
		public void testCreate() throws Throwable {
			LOGGER.info( "\n======== CREATE ======== ");
			LOGGER.info( "-- Attempting to create a ExchangeRate");

			StringBuilder msg = new StringBuilder( "-- Failed to create a ExchangeRate" );

			try {            
				ExchangeRate entity = ExchangeRateService.getExchangeRateInstance().createExchangeRate( generateNewEntity() );
				thePrimaryKey = entity.getExchangeRateId();
				LOGGER.info( "-- Successfully created a ExchangeRate with primary key" + thePrimaryKey );
			}
			catch (Exception e) {
				LOGGER.warning( unexpectedErrorMsg );
				LOGGER.warning( msg.toString() + " : " + e );

				throw e;
			}
		}

		/** 
		 * Tests reading a ExchangeRate. 
		 */
		public ExchangeRate testRead() throws Throwable {
			LOGGER.info( "\n======== READ ======== ");
			LOGGER.info( "-- Reading a previously created ExchangeRate" );

			ExchangeRate entity = null;
			StringBuilder msg = new StringBuilder( "-- Failed to read ExchangeRate with primary key" );
			msg.append( thePrimaryKey );
			
			ExchangeRateFetchOneSummary fetchOneSummary = new ExchangeRateFetchOneSummary( thePrimaryKey );

			try {
				entity = ExchangeRateService.getExchangeRateInstance().getExchangeRate( fetchOneSummary );

				assertNotNull( entity,msg.toString() );

				LOGGER.info( "-- Successfully found ExchangeRate " + entity.toString() );
			}
			catch ( Throwable e ) {
				LOGGER.warning( unexpectedErrorMsg );
				LOGGER.warning( msg.toString() + " : " + e );

				throw e;
			}
			
			return entity;
		}

		/** 
		 * Tests updating a ExchangeRate.
		 */
		public void testUpdate() throws Throwable {
			LOGGER.info( "\n======== UPDATE ======== ");
			LOGGER.info( "-- Attempting to update a ExchangeRate." );

			StringBuilder msg = new StringBuilder( "Failed to update a ExchangeRate : " );        
			ExchangeRate entity = null;

			try {            
				entity = testRead();

				assertNotNull( entity, msg.toString() );

				LOGGER.info( "-- Now updating the created ExchangeRate." );

				// for use later on...
				thePrimaryKey = entity.getExchangeRateId();

				ExchangeRateService proxy = ExchangeRateService.getExchangeRateInstance();            
				proxy.updateExchangeRate( entity );   

				LOGGER.info( "-- Successfully saved ExchangeRate - " + entity.toString() );
			}
			catch ( Throwable e ) {
				LOGGER.warning( unexpectedErrorMsg );
				LOGGER.warning( msg.toString() + " : primarykey = " + thePrimaryKey + " : entity-" +  entity + " : " + e );

				throw e;
			}
		}

		/** 
		 * Tests deleting a ExchangeRate.
		 */
		public void testDelete() throws Throwable {
			LOGGER.info( "\n======== DELETE ======== ");
			LOGGER.info( "-- Deleting a previously created ExchangeRate." );

			try{
			    ExchangeRate entity = testRead(); 
				ExchangeRateService.getExchangeRateInstance().delete( entity );

				LOGGER.info( "-- Successfully deleted ExchangeRate with primary key " + thePrimaryKey );            
			}
			catch ( Throwable e ) {
				LOGGER.warning( unexpectedErrorMsg );
				LOGGER.warning( "-- Failed to delete ExchangeRate with primary key " + thePrimaryKey );

				throw e;
			}
		}

		/**
		 * Tests getting all ExchangeRates.
		 */
		public void testGetAll() throws Throwable {    
			LOGGER.info( "======== GETALL ======== ");
			LOGGER.info( "-- Retrieving Collection of ExchangeRates:" );

			StringBuilder msg = new StringBuilder( "-- Failed to get all ExchangeRate : " );        
			List<ExchangeRate> collection  = null;

			try {
				// call the static get method on the ExchangeRateService
				collection = ExchangeRateService.getExchangeRateInstance().getAllExchangeRate();

				if ( collection == null || collection.size() == 0 ) {
					LOGGER.warning( unexpectedErrorMsg );
					LOGGER.warning( msg.toString() + " Empty collection returned."  );
				}
				else {
					// Now print out the values
					ExchangeRate entity = null;            
					Iterator<ExchangeRate> iter = collection.iterator();
					int index = 1;

					while( iter.hasNext() ) {
						// Retrieve the entity   
						entity = iter.next();

						assertNotNull( entity,"-- null entity in Collection." );
						assertNotNull( entity.getExchangeRateId(), "-- entity in Collection has a null primary key" );        

						LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
						index++;
					}
				}
			}
			catch ( Throwable e ) {
				LOGGER.warning( unexpectedErrorMsg );
				LOGGER.warning( msg.toString() + " : " + e );

				throw e;
			}
		}

		/**
		 * Assigns a common log handler for each test class in the suite
		 * @param		handler	Handler
		 * @return		ExchangeRateTest
		 */
		public ExchangeRateTest setHandler(Handler handler) {
			this.handler = handler;
			LOGGER.addHandler(handler); // assign so the LOGGER can only output results to the Handler
			return this;
		}

		/**
		 * Returns a new populated ExchangeRate
		 * 
		 * @return ExchangeRate
		 */
		protected ExchangeRate generateNewEntity() {
			ExchangeRate entity = new ExchangeRate( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  java.math.BigDecimal.ZERO,   new Date(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  null,  new HashSet<>() );
			
			return( entity );
		}

		//-----------------------------------------------------
		// attributes 
		//-----------------------------------------------------
		protected UUID thePrimaryKey 						= null;
		private final Logger LOGGER 						= Logger.getLogger(ExchangeRateTest.class.getName());
		private Handler handler 							= null;
		private String unexpectedErrorMsg 					= ":::::::::::::: Unexpected Error :::::::::::::::::";
	}
