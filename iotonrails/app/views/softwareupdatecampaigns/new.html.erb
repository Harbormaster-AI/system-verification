$generateAppStats.applyMetaDataTagWithCount( "Form View", "Display as a form to create a new entity." )
<div class="title text-center">
	New SoftwareUpdateCampaign
</div>
 
<%= render 'form' %>
 
<%= link_to 'Back', softwareUpdateCampaigns_path %>