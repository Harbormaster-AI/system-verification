$generateAppStats.applyMetaDataTagWithCount( "Form View", "Display entity data as a form." )
<%= form_with model: @dataRetentionPolicy, local: true do |form| %>
 
  <% if @dataRetentionPolicy.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@dataRetentionPolicy.errors.count, "error") %> prohibited
        this dataRetentionPolicy from being saved:
      </h2>
      <ul>
        <% @dataRetentionPolicy.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="card">
    <div class="card-body">

  <p>
    <%= form.label :name, { class: 'form-label'} %><br>
	<%= form.text_field :name, { class: 'form-label'} %>
  </p>			
  <p>
    <%= form.label :retentionDays, { class: 'form-label'} %><br>
	<%= form.text_field :retentionDays, { class: 'form-label'} %>
  </p>			
  <p>
  </p>			
  <p>
  </p>			

      <%= form.submit %>
    
    </div>
  
  </div> 
<% end %>