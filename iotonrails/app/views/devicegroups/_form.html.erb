<%= form_with model: @deviceGroup, local: true do |form| %>
 
  <% if @deviceGroup.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@deviceGroup.errors.count, "error") %> prohibited
        this deviceGroup from being saved:
      </h2>
      <ul>
        <% @deviceGroup.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="card">
    <div class="card-body">

  <p>
    <%= form.label :name, { class: 'form-label'} %><br>
	<%= form.text_field :name, { class: 'form-label'} %>
  </p>			
  <p>
    <%= form.label :criteria, { class: 'form-label'} %><br>
	<%= form.text_field :criteria, { class: 'form-label'} %>
  </p>			
  <p>
  </p>			
  <p>
  </p>			

      <%= form.submit %>
    
    </div>
  
  </div> 
<% end %>