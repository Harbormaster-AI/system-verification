version: "3.9"

services:
  db:
    image:    ports:
      - #DockerComposeDBPorts()
    volumes:
      - ./db:/docker-entrypoint-initdb.d

  platform:
    image: theharbormaster/banking-on-spring-boot-3-5:latest    ports:
      - "8080:8080
"
    depends_on:
      - db
    links:
      - db

