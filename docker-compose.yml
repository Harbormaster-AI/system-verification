version: "3.9"

services:
  db:
    image:    ports:
      - #DockerComposeDBPorts()
    volumes:
      - ./db:/docker-entrypoint-initdb.d

  platform:
    image: theharbormaster/iot-on-springboot:latest    ports:
      - "8080:8080
"
    depends_on:
      - db
    links:
      - db

