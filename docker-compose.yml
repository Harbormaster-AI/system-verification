version: "3.9"

services:
  db:
    image:unset-value    ports:
      - #DockerComposeDBPorts()
    volumes:
      - ./db:/docker-entrypoint-initdb.d

  platform:
    image: theharbormaster/iot-on-golang:latest    ports:
      - "4000:4000
"
    depends_on:
      - db
    links:
      - db

