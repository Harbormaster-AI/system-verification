/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.globalbank.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.springframework.util.Assert.state;

import com.globalbank.service.*;
import com.globalbank.entity.*;
import com.globalbank.api.*;
import com.globalbank.subscriber.*;

/**
 * <h2>DisputeTest</h2>
 * <p>
 * Junit test class for entity Dispute.  Handle the full lifecycle as it relates to persistence. Leverages
 * the DisputeService and a DisputeSubscriber to test everything under a MVC framework which includes the
 * DisputeAggregate and related commands, queries, and events.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/12/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Banking Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core banking, payments, lending, cards, KYC, and FX capabilities.</td></tr>
 *          <tr><td>industry</td><td>Banking</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 * 		DisputeService
 * 	    DisputeSubscriber
 *
 * <h3>Methods</h3>
 *      <ul>
 *      <li>read</li>
 *      <li>update</li>
 *      <li>delete</li>
 *      </ul>
 *
 * <p>
 * @author your_name_here
 * </p>
 */
public class DisputeTest {

    // ------------------------------------
	// default constructor
    // ------------------------------------
	public DisputeTest() {
		subscriber = new DisputeSubscriber();
	}

	// test methods
	@Test
	/*
	 * Initiate DisputeTest.
	 */
	public void startTest() throws Throwable {
		try {
			LOGGER.info("**********************************************************");
			LOGGER.info("Beginning test on Dispute...");
			LOGGER.info("**********************************************************\n");
			
			// ---------------------------------------------
			// jumpstart process
			// ---------------------------------------------
			jumpStart();
			
		} catch (Throwable e) {
			throw e;
		} finally {
		}
	}

	/** 
	 * jumpstart the process by instantiating2 Dispute
	 */
	protected void jumpStart() throws Throwable {
		LOGGER.info( "\n======== create instances to get the ball rolling  ======== ");

		disputeId = DisputeService.getDisputeInstance()
		.createDispute( generateNewCommand() )
		.get();

		// ---------------------------------------------
		// set up query subscriptions after the 1st create
		// ---------------------------------------------
		testingStep = "create";
		setUpQuerySubscriptions();

		DisputeService.getDisputeInstance()
				.createDispute( generateNewCommand() )
				.get();

	}

	/** 
	 * Set up query subscriptions
	 */
	protected void setUpQuerySubscriptions() throws Throwable {
		LOGGER.info( "\n======== Setting Up Query Subscriptions ======== ");
			
		try {            
			subscriber.disputeSubscribe().updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetAll update received for Dispute : " + successValue.getDisputeId());
							  if (successValue.getDisputeId().equals(disputeId)) {
								  if (testingStep.equals("create")) {
									  testingStep = "update";
									  update();
								  } else if (testingStep.equals("delete")) {
									  testingStep = "complete";
									  state( getAll().size() == sizeOfDisputeList - 1 , "value not deleted from list");
									  LOGGER.info("**********************************************************");
									  LOGGER.info("Dispute test completed successfully...");
									  LOGGER.info("**********************************************************\n");
								  }
							  }
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on dispute consumed")
					);
			subscriber.disputeSubscribe( disputeId ).updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetOne update received for Dispute : " + successValue.getDisputeId() + " in step " + testingStep);
							  testingStep = "delete";
							  sizeOfDisputeList = getAll().size();
							  delete();
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on dispute for disputeId consumed")

					);
			

			}
			catch (Exception e) {
				LOGGER.warning( e.getMessage() );
				throw e;
			}
		}
		
		/** 
	 * read a Dispute. 
	 */
	protected Dispute read() throws Throwable {
		LOGGER.info( "\n======== READ ======== ");
		LOGGER.info( "-- Reading a previously created Dispute" );

		Dispute entity = null;
		StringBuilder msg = new StringBuilder( "-- Failed to read Dispute with primary key" );
		msg.append( disputeId );
		
		DisputeFetchOneSummary fetchOneSummary = new DisputeFetchOneSummary( disputeId );

		try {
			entity = DisputeService.getDisputeInstance().getDispute( fetchOneSummary );

			assertNotNull( entity,msg.toString() );

			LOGGER.info( "-- Successfully found Dispute " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return entity;
	}

	/** 
	 * updating a Dispute.
	 */
	protected void update() throws Throwable {
		LOGGER.info( "\n======== UPDATE ======== ");
		LOGGER.info( "-- Attempting to update a Dispute." );

		StringBuilder msg = new StringBuilder( "Failed to update a Dispute : " );        
		Dispute entity = read();
		UpdateDisputeCommand command = generateUpdateCommand();
		command.setDisputeId(entity.getDisputeId());

		try {            
			assertNotNull( entity, msg.toString() );

			LOGGER.info( "-- Now updating the created Dispute." );

			// for use later on...
			disputeId = entity.getDisputeId();

			DisputeService proxy = DisputeService.getDisputeInstance();  

			proxy.updateDispute( command ).get();

			LOGGER.info( "-- Successfully saved Dispute - " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : primarykey = " + disputeId + " : command -" +  command + " : " + e );

			throw e;
		}
	}

	/** 
	 * delete a Dispute.
	 */
	protected void delete() throws Throwable {
		LOGGER.info( "\n======== DELETE ======== ");
		LOGGER.info( "-- Deleting a previously created Dispute." );

		Dispute entity = null;
		
		try{
		    entity = read(); 
			LOGGER.info( "-- Successfully read Dispute with id " + disputeId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to read Dispute with id " + disputeId );

			throw e;
		}

		try{
			DisputeService.getDisputeInstance().delete( new DeleteDisputeCommand( entity.getDisputeId() ) ).get();
			LOGGER.info( "-- Successfully deleted Dispute with id " + disputeId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to delete Dispute with id " + disputeId );

			throw e;
		}
	}

	/**
	 * get all Disputes.
	 */
	protected List<Dispute> getAll() throws Throwable {    
		LOGGER.info( "======== GETALL ======== ");
		LOGGER.info( "-- Retrieving Collection of Disputes:" );

		StringBuilder msg = new StringBuilder( "-- Failed to get all Dispute : " );        
		List<Dispute> collection  = new ArrayList<>();

		try {
			// call the static get method on the DisputeService
			collection = DisputeService.getDisputeInstance().getAllDispute();
			assertNotNull( collection, "An Empty collection of Dispute was incorrectly returned.");
			
			// Now print out the values
			Dispute entity = null;            
			Iterator<Dispute> iter = collection.iterator();
			int index = 1;

			while( iter.hasNext() ) {
				// Retrieve the entity   
				entity = iter.next();

				assertNotNull( entity,"-- null entity in Collection." );
				assertNotNull( entity.getDisputeId(), "-- entity in Collection has a null primary key" );        

				LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
				index++;
			}
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return collection;			
	}

	/**
	 * Assigns a common log handler for each test class in the suite 
	 * in the event log output needs to go elsewhere
	 * 
	 * @param		handler	Handler
	 * @return		DisputeTest
	 */
	protected DisputeTest setHandler(Handler handler) {
		if ( handler != null )
			LOGGER.addHandler(handler); 
		return this;
	}

	/**
	 * Returns a new populated Dispute
	 * 
	 * @return CreateDisputeCommand alias
	 */
	protected CreateDisputeCommand generateNewCommand() {
        CreateDisputeCommand command = new CreateDisputeCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),   new Date(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  DisputeStatus.values()[0] );
		
		return( command );
	}

		/**
		 * Returns a new populated Dispute
		 * 
		 * @return UpdateDisputeCommand alias
		 */
	protected UpdateDisputeCommand generateUpdateCommand() {
	        UpdateDisputeCommand command = new UpdateDisputeCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),   new Date(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  null,  null,  null,  null,  DisputeStatus.values()[0] );
			
			return( command );
		}
	//-----------------------------------------------------
	// attributes 
	//-----------------------------------------------------
	protected UUID disputeId = null;
	protected DisputeSubscriber subscriber = null;
	private final String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
	private final Logger LOGGER = Logger.getLogger(DisputeTest.class.getName());
	private String testingStep = "";
	private Integer sizeOfDisputeList = 0;
}
