/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.globalbank.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.springframework.util.Assert.state;

import com.globalbank.service.*;
import com.globalbank.entity.*;
import com.globalbank.api.*;
import com.globalbank.subscriber.*;

/**
 * <h2>ScreeningResultTest</h2>
 * <p>
 * Junit test class for entity ScreeningResult.  Handle the full lifecycle as it relates to persistence. Leverages
 * the ScreeningResultService and a ScreeningResultSubscriber to test everything under a MVC framework which includes the
 * ScreeningResultAggregate and related commands, queries, and events.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/12/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Banking Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core banking, payments, lending, cards, KYC, and FX capabilities.</td></tr>
 *          <tr><td>industry</td><td>Banking</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 * 		ScreeningResultService
 * 	    ScreeningResultSubscriber
 *
 * <h3>Methods</h3>
 *      <ul>
 *      <li>read</li>
 *      <li>update</li>
 *      <li>delete</li>
 *      </ul>
 *
 * <p>
 * @author your_name_here
 * </p>
 */
public class ScreeningResultTest {

    // ------------------------------------
	// default constructor
    // ------------------------------------
	public ScreeningResultTest() {
		subscriber = new ScreeningResultSubscriber();
	}

	// test methods
	@Test
	/*
	 * Initiate ScreeningResultTest.
	 */
	public void startTest() throws Throwable {
		try {
			LOGGER.info("**********************************************************");
			LOGGER.info("Beginning test on ScreeningResult...");
			LOGGER.info("**********************************************************\n");
			
			// ---------------------------------------------
			// jumpstart process
			// ---------------------------------------------
			jumpStart();
			
		} catch (Throwable e) {
			throw e;
		} finally {
		}
	}

	/** 
	 * jumpstart the process by instantiating2 ScreeningResult
	 */
	protected void jumpStart() throws Throwable {
		LOGGER.info( "\n======== create instances to get the ball rolling  ======== ");

		screeningResultId = ScreeningResultService.getScreeningResultInstance()
		.createScreeningResult( generateNewCommand() )
		.get();

		// ---------------------------------------------
		// set up query subscriptions after the 1st create
		// ---------------------------------------------
		testingStep = "create";
		setUpQuerySubscriptions();

		ScreeningResultService.getScreeningResultInstance()
				.createScreeningResult( generateNewCommand() )
				.get();

	}

	/** 
	 * Set up query subscriptions
	 */
	protected void setUpQuerySubscriptions() throws Throwable {
		LOGGER.info( "\n======== Setting Up Query Subscriptions ======== ");
			
		try {            
			subscriber.screeningResultSubscribe().updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetAll update received for ScreeningResult : " + successValue.getScreeningResultId());
							  if (successValue.getScreeningResultId().equals(screeningResultId)) {
								  if (testingStep.equals("create")) {
									  testingStep = "update";
									  update();
								  } else if (testingStep.equals("delete")) {
									  testingStep = "complete";
									  state( getAll().size() == sizeOfScreeningResultList - 1 , "value not deleted from list");
									  LOGGER.info("**********************************************************");
									  LOGGER.info("ScreeningResult test completed successfully...");
									  LOGGER.info("**********************************************************\n");
								  }
							  }
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on screeningResult consumed")
					);
			subscriber.screeningResultSubscribe( screeningResultId ).updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetOne update received for ScreeningResult : " + successValue.getScreeningResultId() + " in step " + testingStep);
							  testingStep = "delete";
							  sizeOfScreeningResultList = getAll().size();
							  delete();
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on screeningResult for screeningResultId consumed")

					);
			

			}
			catch (Exception e) {
				LOGGER.warning( e.getMessage() );
				throw e;
			}
		}
		
		/** 
	 * read a ScreeningResult. 
	 */
	protected ScreeningResult read() throws Throwable {
		LOGGER.info( "\n======== READ ======== ");
		LOGGER.info( "-- Reading a previously created ScreeningResult" );

		ScreeningResult entity = null;
		StringBuilder msg = new StringBuilder( "-- Failed to read ScreeningResult with primary key" );
		msg.append( screeningResultId );
		
		ScreeningResultFetchOneSummary fetchOneSummary = new ScreeningResultFetchOneSummary( screeningResultId );

		try {
			entity = ScreeningResultService.getScreeningResultInstance().getScreeningResult( fetchOneSummary );

			assertNotNull( entity,msg.toString() );

			LOGGER.info( "-- Successfully found ScreeningResult " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return entity;
	}

	/** 
	 * updating a ScreeningResult.
	 */
	protected void update() throws Throwable {
		LOGGER.info( "\n======== UPDATE ======== ");
		LOGGER.info( "-- Attempting to update a ScreeningResult." );

		StringBuilder msg = new StringBuilder( "Failed to update a ScreeningResult : " );        
		ScreeningResult entity = read();
		UpdateScreeningResultCommand command = generateUpdateCommand();
		command.setScreeningResultId(entity.getScreeningResultId());

		try {            
			assertNotNull( entity, msg.toString() );

			LOGGER.info( "-- Now updating the created ScreeningResult." );

			// for use later on...
			screeningResultId = entity.getScreeningResultId();

			ScreeningResultService proxy = ScreeningResultService.getScreeningResultInstance();  

			proxy.updateScreeningResult( command ).get();

			LOGGER.info( "-- Successfully saved ScreeningResult - " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : primarykey = " + screeningResultId + " : command -" +  command + " : " + e );

			throw e;
		}
	}

	/** 
	 * delete a ScreeningResult.
	 */
	protected void delete() throws Throwable {
		LOGGER.info( "\n======== DELETE ======== ");
		LOGGER.info( "-- Deleting a previously created ScreeningResult." );

		ScreeningResult entity = null;
		
		try{
		    entity = read(); 
			LOGGER.info( "-- Successfully read ScreeningResult with id " + screeningResultId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to read ScreeningResult with id " + screeningResultId );

			throw e;
		}

		try{
			ScreeningResultService.getScreeningResultInstance().delete( new DeleteScreeningResultCommand( entity.getScreeningResultId() ) ).get();
			LOGGER.info( "-- Successfully deleted ScreeningResult with id " + screeningResultId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to delete ScreeningResult with id " + screeningResultId );

			throw e;
		}
	}

	/**
	 * get all ScreeningResults.
	 */
	protected List<ScreeningResult> getAll() throws Throwable {    
		LOGGER.info( "======== GETALL ======== ");
		LOGGER.info( "-- Retrieving Collection of ScreeningResults:" );

		StringBuilder msg = new StringBuilder( "-- Failed to get all ScreeningResult : " );        
		List<ScreeningResult> collection  = new ArrayList<>();

		try {
			// call the static get method on the ScreeningResultService
			collection = ScreeningResultService.getScreeningResultInstance().getAllScreeningResult();
			assertNotNull( collection, "An Empty collection of ScreeningResult was incorrectly returned.");
			
			// Now print out the values
			ScreeningResult entity = null;            
			Iterator<ScreeningResult> iter = collection.iterator();
			int index = 1;

			while( iter.hasNext() ) {
				// Retrieve the entity   
				entity = iter.next();

				assertNotNull( entity,"-- null entity in Collection." );
				assertNotNull( entity.getScreeningResultId(), "-- entity in Collection has a null primary key" );        

				LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
				index++;
			}
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return collection;			
	}

	/**
	 * Assigns a common log handler for each test class in the suite 
	 * in the event log output needs to go elsewhere
	 * 
	 * @param		handler	Handler
	 * @return		ScreeningResultTest
	 */
	protected ScreeningResultTest setHandler(Handler handler) {
		if ( handler != null )
			LOGGER.addHandler(handler); 
		return this;
	}

	/**
	 * Returns a new populated ScreeningResult
	 * 
	 * @return CreateScreeningResultCommand alias
	 */
	protected CreateScreeningResultCommand generateNewCommand() {
        CreateScreeningResultCommand command = new CreateScreeningResultCommand( null,   new Date(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  ScreeningOutcome.values()[0] );
		
		return( command );
	}

		/**
		 * Returns a new populated ScreeningResult
		 * 
		 * @return UpdateScreeningResultCommand alias
		 */
	protected UpdateScreeningResultCommand generateUpdateCommand() {
	        UpdateScreeningResultCommand command = new UpdateScreeningResultCommand( null,   new Date(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  null,  ScreeningOutcome.values()[0] );
			
			return( command );
		}
	//-----------------------------------------------------
	// attributes 
	//-----------------------------------------------------
	protected UUID screeningResultId = null;
	protected ScreeningResultSubscriber subscriber = null;
	private final String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
	private final Logger LOGGER = Logger.getLogger(ScreeningResultTest.class.getName());
	private String testingStep = "";
	private Integer sizeOfScreeningResultList = 0;
}
