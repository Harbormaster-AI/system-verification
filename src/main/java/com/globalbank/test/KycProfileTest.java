/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.globalbank.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.springframework.util.Assert.state;

import com.globalbank.service.*;
import com.globalbank.entity.*;
import com.globalbank.api.*;
import com.globalbank.subscriber.*;

/**
 * <h2>KycProfileTest</h2>
 * <p>
 * Junit test class for entity KycProfile.  Handle the full lifecycle as it relates to persistence. Leverages
 * the KycProfileService and a KycProfileSubscriber to test everything under a MVC framework which includes the
 * KycProfileAggregate and related commands, queries, and events.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/12/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Banking Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core banking, payments, lending, cards, KYC, and FX capabilities.</td></tr>
 *          <tr><td>industry</td><td>Banking</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 * 		KycProfileService
 * 	    KycProfileSubscriber
 *
 * <h3>Methods</h3>
 *      <ul>
 *      <li>read</li>
 *      <li>update</li>
 *      <li>delete</li>
 *      </ul>
 *
 * <p>
 * @author your_name_here
 * </p>
 */
public class KycProfileTest {

    // ------------------------------------
	// default constructor
    // ------------------------------------
	public KycProfileTest() {
		subscriber = new KycProfileSubscriber();
	}

	// test methods
	@Test
	/*
	 * Initiate KycProfileTest.
	 */
	public void startTest() throws Throwable {
		try {
			LOGGER.info("**********************************************************");
			LOGGER.info("Beginning test on KycProfile...");
			LOGGER.info("**********************************************************\n");
			
			// ---------------------------------------------
			// jumpstart process
			// ---------------------------------------------
			jumpStart();
			
		} catch (Throwable e) {
			throw e;
		} finally {
		}
	}

	/** 
	 * jumpstart the process by instantiating2 KycProfile
	 */
	protected void jumpStart() throws Throwable {
		LOGGER.info( "\n======== create instances to get the ball rolling  ======== ");

		kycProfileId = KycProfileService.getKycProfileInstance()
		.createKycProfile( generateNewCommand() )
		.get();

		// ---------------------------------------------
		// set up query subscriptions after the 1st create
		// ---------------------------------------------
		testingStep = "create";
		setUpQuerySubscriptions();

		KycProfileService.getKycProfileInstance()
				.createKycProfile( generateNewCommand() )
				.get();

	}

	/** 
	 * Set up query subscriptions
	 */
	protected void setUpQuerySubscriptions() throws Throwable {
		LOGGER.info( "\n======== Setting Up Query Subscriptions ======== ");
			
		try {            
			subscriber.kycProfileSubscribe().updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetAll update received for KycProfile : " + successValue.getKycProfileId());
							  if (successValue.getKycProfileId().equals(kycProfileId)) {
								  if (testingStep.equals("create")) {
									  testingStep = "update";
									  update();
								  } else if (testingStep.equals("delete")) {
									  testingStep = "complete";
									  state( getAll().size() == sizeOfKycProfileList - 1 , "value not deleted from list");
									  LOGGER.info("**********************************************************");
									  LOGGER.info("KycProfile test completed successfully...");
									  LOGGER.info("**********************************************************\n");
								  }
							  }
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on kycProfile consumed")
					);
			subscriber.kycProfileSubscribe( kycProfileId ).updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetOne update received for KycProfile : " + successValue.getKycProfileId() + " in step " + testingStep);
							  testingStep = "delete";
							  sizeOfKycProfileList = getAll().size();
							  delete();
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on kycProfile for kycProfileId consumed")

					);
			

			}
			catch (Exception e) {
				LOGGER.warning( e.getMessage() );
				throw e;
			}
		}
		
		/** 
	 * read a KycProfile. 
	 */
	protected KycProfile read() throws Throwable {
		LOGGER.info( "\n======== READ ======== ");
		LOGGER.info( "-- Reading a previously created KycProfile" );

		KycProfile entity = null;
		StringBuilder msg = new StringBuilder( "-- Failed to read KycProfile with primary key" );
		msg.append( kycProfileId );
		
		KycProfileFetchOneSummary fetchOneSummary = new KycProfileFetchOneSummary( kycProfileId );

		try {
			entity = KycProfileService.getKycProfileInstance().getKycProfile( fetchOneSummary );

			assertNotNull( entity,msg.toString() );

			LOGGER.info( "-- Successfully found KycProfile " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return entity;
	}

	/** 
	 * updating a KycProfile.
	 */
	protected void update() throws Throwable {
		LOGGER.info( "\n======== UPDATE ======== ");
		LOGGER.info( "-- Attempting to update a KycProfile." );

		StringBuilder msg = new StringBuilder( "Failed to update a KycProfile : " );        
		KycProfile entity = read();
		UpdateKycProfileCommand command = generateUpdateCommand();
		command.setKycProfileId(entity.getKycProfileId());

		try {            
			assertNotNull( entity, msg.toString() );

			LOGGER.info( "-- Now updating the created KycProfile." );

			// for use later on...
			kycProfileId = entity.getKycProfileId();

			KycProfileService proxy = KycProfileService.getKycProfileInstance();  

			proxy.updateKycProfile( command ).get();

			LOGGER.info( "-- Successfully saved KycProfile - " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : primarykey = " + kycProfileId + " : command -" +  command + " : " + e );

			throw e;
		}
	}

	/** 
	 * delete a KycProfile.
	 */
	protected void delete() throws Throwable {
		LOGGER.info( "\n======== DELETE ======== ");
		LOGGER.info( "-- Deleting a previously created KycProfile." );

		KycProfile entity = null;
		
		try{
		    entity = read(); 
			LOGGER.info( "-- Successfully read KycProfile with id " + kycProfileId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to read KycProfile with id " + kycProfileId );

			throw e;
		}

		try{
			KycProfileService.getKycProfileInstance().delete( new DeleteKycProfileCommand( entity.getKycProfileId() ) ).get();
			LOGGER.info( "-- Successfully deleted KycProfile with id " + kycProfileId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to delete KycProfile with id " + kycProfileId );

			throw e;
		}
	}

	/**
	 * get all KycProfiles.
	 */
	protected List<KycProfile> getAll() throws Throwable {    
		LOGGER.info( "======== GETALL ======== ");
		LOGGER.info( "-- Retrieving Collection of KycProfiles:" );

		StringBuilder msg = new StringBuilder( "-- Failed to get all KycProfile : " );        
		List<KycProfile> collection  = new ArrayList<>();

		try {
			// call the static get method on the KycProfileService
			collection = KycProfileService.getKycProfileInstance().getAllKycProfile();
			assertNotNull( collection, "An Empty collection of KycProfile was incorrectly returned.");
			
			// Now print out the values
			KycProfile entity = null;            
			Iterator<KycProfile> iter = collection.iterator();
			int index = 1;

			while( iter.hasNext() ) {
				// Retrieve the entity   
				entity = iter.next();

				assertNotNull( entity,"-- null entity in Collection." );
				assertNotNull( entity.getKycProfileId(), "-- entity in Collection has a null primary key" );        

				LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
				index++;
			}
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return collection;			
	}

	/**
	 * Assigns a common log handler for each test class in the suite 
	 * in the event log output needs to go elsewhere
	 * 
	 * @param		handler	Handler
	 * @return		KycProfileTest
	 */
	protected KycProfileTest setHandler(Handler handler) {
		if ( handler != null )
			LOGGER.addHandler(handler); 
		return this;
	}

	/**
	 * Returns a new populated KycProfile
	 * 
	 * @return CreateKycProfileCommand alias
	 */
	protected CreateKycProfileCommand generateNewCommand() {
        CreateKycProfileCommand command = new CreateKycProfileCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),   new Date(),  KycStatus.values()[0] );
		
		return( command );
	}

		/**
		 * Returns a new populated KycProfile
		 * 
		 * @return UpdateKycProfileCommand alias
		 */
	protected UpdateKycProfileCommand generateUpdateCommand() {
	        UpdateKycProfileCommand command = new UpdateKycProfileCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),   new Date(),  null,  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  KycStatus.values()[0] );
			
			return( command );
		}
	//-----------------------------------------------------
	// attributes 
	//-----------------------------------------------------
	protected UUID kycProfileId = null;
	protected KycProfileSubscriber subscriber = null;
	private final String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
	private final Logger LOGGER = Logger.getLogger(KycProfileTest.class.getName());
	private String testingStep = "";
	private Integer sizeOfKycProfileList = 0;
}
