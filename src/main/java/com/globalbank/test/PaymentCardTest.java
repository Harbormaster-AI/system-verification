/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.globalbank.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.springframework.util.Assert.state;

import com.globalbank.service.*;
import com.globalbank.entity.*;
import com.globalbank.api.*;
import com.globalbank.subscriber.*;

/**
 * <h2>PaymentCardTest</h2>
 * <p>
 * Junit test class for entity PaymentCard.  Handle the full lifecycle as it relates to persistence. Leverages
 * the PaymentCardService and a PaymentCardSubscriber to test everything under a MVC framework which includes the
 * PaymentCardAggregate and related commands, queries, and events.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/12/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Banking Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core banking, payments, lending, cards, KYC, and FX capabilities.</td></tr>
 *          <tr><td>industry</td><td>Banking</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 * 		PaymentCardService
 * 	    PaymentCardSubscriber
 *
 * <h3>Methods</h3>
 *      <ul>
 *      <li>read</li>
 *      <li>update</li>
 *      <li>delete</li>
 *      </ul>
 *
 * <p>
 * @author your_name_here
 * </p>
 */
public class PaymentCardTest {

    // ------------------------------------
	// default constructor
    // ------------------------------------
	public PaymentCardTest() {
		subscriber = new PaymentCardSubscriber();
	}

	// test methods
	@Test
	/*
	 * Initiate PaymentCardTest.
	 */
	public void startTest() throws Throwable {
		try {
			LOGGER.info("**********************************************************");
			LOGGER.info("Beginning test on PaymentCard...");
			LOGGER.info("**********************************************************\n");
			
			// ---------------------------------------------
			// jumpstart process
			// ---------------------------------------------
			jumpStart();
			
		} catch (Throwable e) {
			throw e;
		} finally {
		}
	}

	/** 
	 * jumpstart the process by instantiating2 PaymentCard
	 */
	protected void jumpStart() throws Throwable {
		LOGGER.info( "\n======== create instances to get the ball rolling  ======== ");

		paymentCardId = PaymentCardService.getPaymentCardInstance()
		.createPaymentCard( generateNewCommand() )
		.get();

		// ---------------------------------------------
		// set up query subscriptions after the 1st create
		// ---------------------------------------------
		testingStep = "create";
		setUpQuerySubscriptions();

		PaymentCardService.getPaymentCardInstance()
				.createPaymentCard( generateNewCommand() )
				.get();

	}

	/** 
	 * Set up query subscriptions
	 */
	protected void setUpQuerySubscriptions() throws Throwable {
		LOGGER.info( "\n======== Setting Up Query Subscriptions ======== ");
			
		try {            
			subscriber.paymentCardSubscribe().updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetAll update received for PaymentCard : " + successValue.getPaymentCardId());
							  if (successValue.getPaymentCardId().equals(paymentCardId)) {
								  if (testingStep.equals("create")) {
									  testingStep = "update";
									  update();
								  } else if (testingStep.equals("delete")) {
									  testingStep = "complete";
									  state( getAll().size() == sizeOfPaymentCardList - 1 , "value not deleted from list");
									  LOGGER.info("**********************************************************");
									  LOGGER.info("PaymentCard test completed successfully...");
									  LOGGER.info("**********************************************************\n");
								  }
							  }
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on paymentCard consumed")
					);
			subscriber.paymentCardSubscribe( paymentCardId ).updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetOne update received for PaymentCard : " + successValue.getPaymentCardId() + " in step " + testingStep);
							  testingStep = "delete";
							  sizeOfPaymentCardList = getAll().size();
							  delete();
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on paymentCard for paymentCardId consumed")

					);
			

			}
			catch (Exception e) {
				LOGGER.warning( e.getMessage() );
				throw e;
			}
		}
		
		/** 
	 * read a PaymentCard. 
	 */
	protected PaymentCard read() throws Throwable {
		LOGGER.info( "\n======== READ ======== ");
		LOGGER.info( "-- Reading a previously created PaymentCard" );

		PaymentCard entity = null;
		StringBuilder msg = new StringBuilder( "-- Failed to read PaymentCard with primary key" );
		msg.append( paymentCardId );
		
		PaymentCardFetchOneSummary fetchOneSummary = new PaymentCardFetchOneSummary( paymentCardId );

		try {
			entity = PaymentCardService.getPaymentCardInstance().getPaymentCard( fetchOneSummary );

			assertNotNull( entity,msg.toString() );

			LOGGER.info( "-- Successfully found PaymentCard " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return entity;
	}

	/** 
	 * updating a PaymentCard.
	 */
	protected void update() throws Throwable {
		LOGGER.info( "\n======== UPDATE ======== ");
		LOGGER.info( "-- Attempting to update a PaymentCard." );

		StringBuilder msg = new StringBuilder( "Failed to update a PaymentCard : " );        
		PaymentCard entity = read();
		UpdatePaymentCardCommand command = generateUpdateCommand();
		command.setPaymentCardId(entity.getPaymentCardId());

		try {            
			assertNotNull( entity, msg.toString() );

			LOGGER.info( "-- Now updating the created PaymentCard." );

			// for use later on...
			paymentCardId = entity.getPaymentCardId();

			PaymentCardService proxy = PaymentCardService.getPaymentCardInstance();  

			proxy.updatePaymentCard( command ).get();

			LOGGER.info( "-- Successfully saved PaymentCard - " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : primarykey = " + paymentCardId + " : command -" +  command + " : " + e );

			throw e;
		}
	}

	/** 
	 * delete a PaymentCard.
	 */
	protected void delete() throws Throwable {
		LOGGER.info( "\n======== DELETE ======== ");
		LOGGER.info( "-- Deleting a previously created PaymentCard." );

		PaymentCard entity = null;
		
		try{
		    entity = read(); 
			LOGGER.info( "-- Successfully read PaymentCard with id " + paymentCardId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to read PaymentCard with id " + paymentCardId );

			throw e;
		}

		try{
			PaymentCardService.getPaymentCardInstance().delete( new DeletePaymentCardCommand( entity.getPaymentCardId() ) ).get();
			LOGGER.info( "-- Successfully deleted PaymentCard with id " + paymentCardId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to delete PaymentCard with id " + paymentCardId );

			throw e;
		}
	}

	/**
	 * get all PaymentCards.
	 */
	protected List<PaymentCard> getAll() throws Throwable {    
		LOGGER.info( "======== GETALL ======== ");
		LOGGER.info( "-- Retrieving Collection of PaymentCards:" );

		StringBuilder msg = new StringBuilder( "-- Failed to get all PaymentCard : " );        
		List<PaymentCard> collection  = new ArrayList<>();

		try {
			// call the static get method on the PaymentCardService
			collection = PaymentCardService.getPaymentCardInstance().getAllPaymentCard();
			assertNotNull( collection, "An Empty collection of PaymentCard was incorrectly returned.");
			
			// Now print out the values
			PaymentCard entity = null;            
			Iterator<PaymentCard> iter = collection.iterator();
			int index = 1;

			while( iter.hasNext() ) {
				// Retrieve the entity   
				entity = iter.next();

				assertNotNull( entity,"-- null entity in Collection." );
				assertNotNull( entity.getPaymentCardId(), "-- entity in Collection has a null primary key" );        

				LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
				index++;
			}
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return collection;			
	}

	/**
	 * Assigns a common log handler for each test class in the suite 
	 * in the event log output needs to go elsewhere
	 * 
	 * @param		handler	Handler
	 * @return		PaymentCardTest
	 */
	protected PaymentCardTest setHandler(Handler handler) {
		if ( handler != null )
			LOGGER.addHandler(handler); 
		return this;
	}

	/**
	 * Returns a new populated PaymentCard
	 * 
	 * @return CreatePaymentCardCommand alias
	 */
	protected CreatePaymentCardCommand generateNewCommand() {
        CreatePaymentCardCommand command = new CreatePaymentCardCommand( null,  new CardPAN(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  0,  0,  CardType.values()[0],  CardStatus.values()[0],  CardNetwork.values()[0] );
		
		return( command );
	}

		/**
		 * Returns a new populated PaymentCard
		 * 
		 * @return UpdatePaymentCardCommand alias
		 */
	protected UpdatePaymentCardCommand generateUpdateCommand() {
	        UpdatePaymentCardCommand command = new UpdatePaymentCardCommand( null,  new CardPAN(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  0,  0,  null,  null,  null,  new HashSet<>(),  CardType.values()[0],  CardStatus.values()[0],  CardNetwork.values()[0] );
			
			return( command );
		}
	//-----------------------------------------------------
	// attributes 
	//-----------------------------------------------------
	protected UUID paymentCardId = null;
	protected PaymentCardSubscriber subscriber = null;
	private final String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
	private final Logger LOGGER = Logger.getLogger(PaymentCardTest.class.getName());
	private String testingStep = "";
	private Integer sizeOfPaymentCardList = 0;
}
