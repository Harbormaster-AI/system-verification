/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.globalbank.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.springframework.util.Assert.state;

import com.globalbank.service.*;
import com.globalbank.entity.*;
import com.globalbank.api.*;
import com.globalbank.subscriber.*;

/**
 * <h2>TransactionTest</h2>
 * <p>
 * Junit test class for entity Transaction.  Handle the full lifecycle as it relates to persistence. Leverages
 * the TransactionService and a TransactionSubscriber to test everything under a MVC framework which includes the
 * TransactionAggregate and related commands, queries, and events.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/12/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Banking Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core banking, payments, lending, cards, KYC, and FX capabilities.</td></tr>
 *          <tr><td>industry</td><td>Banking</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 * 		TransactionService
 * 	    TransactionSubscriber
 *
 * <h3>Methods</h3>
 *      <ul>
 *      <li>read</li>
 *      <li>update</li>
 *      <li>delete</li>
 *      </ul>
 *
 * <p>
 * @author your_name_here
 * </p>
 */
public class TransactionTest {

    // ------------------------------------
	// default constructor
    // ------------------------------------
	public TransactionTest() {
		subscriber = new TransactionSubscriber();
	}

	// test methods
	@Test
	/*
	 * Initiate TransactionTest.
	 */
	public void startTest() throws Throwable {
		try {
			LOGGER.info("**********************************************************");
			LOGGER.info("Beginning test on Transaction...");
			LOGGER.info("**********************************************************\n");
			
			// ---------------------------------------------
			// jumpstart process
			// ---------------------------------------------
			jumpStart();
			
		} catch (Throwable e) {
			throw e;
		} finally {
		}
	}

	/** 
	 * jumpstart the process by instantiating2 Transaction
	 */
	protected void jumpStart() throws Throwable {
		LOGGER.info( "\n======== create instances to get the ball rolling  ======== ");

		transactionId = TransactionService.getTransactionInstance()
		.createTransaction( generateNewCommand() )
		.get();

		// ---------------------------------------------
		// set up query subscriptions after the 1st create
		// ---------------------------------------------
		testingStep = "create";
		setUpQuerySubscriptions();

		TransactionService.getTransactionInstance()
				.createTransaction( generateNewCommand() )
				.get();

	}

	/** 
	 * Set up query subscriptions
	 */
	protected void setUpQuerySubscriptions() throws Throwable {
		LOGGER.info( "\n======== Setting Up Query Subscriptions ======== ");
			
		try {            
			subscriber.transactionSubscribe().updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetAll update received for Transaction : " + successValue.getTransactionId());
							  if (successValue.getTransactionId().equals(transactionId)) {
								  if (testingStep.equals("create")) {
									  testingStep = "update";
									  update();
								  } else if (testingStep.equals("delete")) {
									  testingStep = "complete";
									  state( getAll().size() == sizeOfTransactionList - 1 , "value not deleted from list");
									  LOGGER.info("**********************************************************");
									  LOGGER.info("Transaction test completed successfully...");
									  LOGGER.info("**********************************************************\n");
								  }
							  }
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on transaction consumed")
					);
			subscriber.transactionSubscribe( transactionId ).updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetOne update received for Transaction : " + successValue.getTransactionId() + " in step " + testingStep);
							  testingStep = "delete";
							  sizeOfTransactionList = getAll().size();
							  delete();
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on transaction for transactionId consumed")

					);
			

			}
			catch (Exception e) {
				LOGGER.warning( e.getMessage() );
				throw e;
			}
		}
		
		/** 
	 * read a Transaction. 
	 */
	protected Transaction read() throws Throwable {
		LOGGER.info( "\n======== READ ======== ");
		LOGGER.info( "-- Reading a previously created Transaction" );

		Transaction entity = null;
		StringBuilder msg = new StringBuilder( "-- Failed to read Transaction with primary key" );
		msg.append( transactionId );
		
		TransactionFetchOneSummary fetchOneSummary = new TransactionFetchOneSummary( transactionId );

		try {
			entity = TransactionService.getTransactionInstance().getTransaction( fetchOneSummary );

			assertNotNull( entity,msg.toString() );

			LOGGER.info( "-- Successfully found Transaction " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return entity;
	}

	/** 
	 * updating a Transaction.
	 */
	protected void update() throws Throwable {
		LOGGER.info( "\n======== UPDATE ======== ");
		LOGGER.info( "-- Attempting to update a Transaction." );

		StringBuilder msg = new StringBuilder( "Failed to update a Transaction : " );        
		Transaction entity = read();
		UpdateTransactionCommand command = generateUpdateCommand();
		command.setTransactionId(entity.getTransactionId());

		try {            
			assertNotNull( entity, msg.toString() );

			LOGGER.info( "-- Now updating the created Transaction." );

			// for use later on...
			transactionId = entity.getTransactionId();

			TransactionService proxy = TransactionService.getTransactionInstance();  

			proxy.updateTransaction( command ).get();

			LOGGER.info( "-- Successfully saved Transaction - " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : primarykey = " + transactionId + " : command -" +  command + " : " + e );

			throw e;
		}
	}

	/** 
	 * delete a Transaction.
	 */
	protected void delete() throws Throwable {
		LOGGER.info( "\n======== DELETE ======== ");
		LOGGER.info( "-- Deleting a previously created Transaction." );

		Transaction entity = null;
		
		try{
		    entity = read(); 
			LOGGER.info( "-- Successfully read Transaction with id " + transactionId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to read Transaction with id " + transactionId );

			throw e;
		}

		try{
			TransactionService.getTransactionInstance().delete( new DeleteTransactionCommand( entity.getTransactionId() ) ).get();
			LOGGER.info( "-- Successfully deleted Transaction with id " + transactionId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to delete Transaction with id " + transactionId );

			throw e;
		}
	}

	/**
	 * get all Transactions.
	 */
	protected List<Transaction> getAll() throws Throwable {    
		LOGGER.info( "======== GETALL ======== ");
		LOGGER.info( "-- Retrieving Collection of Transactions:" );

		StringBuilder msg = new StringBuilder( "-- Failed to get all Transaction : " );        
		List<Transaction> collection  = new ArrayList<>();

		try {
			// call the static get method on the TransactionService
			collection = TransactionService.getTransactionInstance().getAllTransaction();
			assertNotNull( collection, "An Empty collection of Transaction was incorrectly returned.");
			
			// Now print out the values
			Transaction entity = null;            
			Iterator<Transaction> iter = collection.iterator();
			int index = 1;

			while( iter.hasNext() ) {
				// Retrieve the entity   
				entity = iter.next();

				assertNotNull( entity,"-- null entity in Collection." );
				assertNotNull( entity.getTransactionId(), "-- entity in Collection has a null primary key" );        

				LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
				index++;
			}
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return collection;			
	}

	/**
	 * Assigns a common log handler for each test class in the suite 
	 * in the event log output needs to go elsewhere
	 * 
	 * @param		handler	Handler
	 * @return		TransactionTest
	 */
	protected TransactionTest setHandler(Handler handler) {
		if ( handler != null )
			LOGGER.addHandler(handler); 
		return this;
	}

	/**
	 * Returns a new populated Transaction
	 * 
	 * @return CreateTransactionCommand alias
	 */
	protected CreateTransactionCommand generateNewCommand() {
        CreateTransactionCommand command = new CreateTransactionCommand( null,   new Date(),   new Date(),  new Money(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  TransactionDirection.values()[0],  TransactionType.values()[0],  TransactionStatus.values()[0],  ChannelType.values()[0] );
		
		return( command );
	}

		/**
		 * Returns a new populated Transaction
		 * 
		 * @return UpdateTransactionCommand alias
		 */
	protected UpdateTransactionCommand generateUpdateCommand() {
	        UpdateTransactionCommand command = new UpdateTransactionCommand( null,   new Date(),   new Date(),  new Money(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  null,  null,  null,  null,  null,  null,  TransactionDirection.values()[0],  TransactionType.values()[0],  TransactionStatus.values()[0],  ChannelType.values()[0] );
			
			return( command );
		}
	//-----------------------------------------------------
	// attributes 
	//-----------------------------------------------------
	protected UUID transactionId = null;
	protected TransactionSubscriber subscriber = null;
	private final String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
	private final Logger LOGGER = Logger.getLogger(TransactionTest.class.getName());
	private String testingStep = "";
	private Integer sizeOfTransactionList = 0;
}
