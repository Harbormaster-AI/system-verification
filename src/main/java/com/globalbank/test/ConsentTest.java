/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.globalbank.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.springframework.util.Assert.state;

import com.globalbank.service.*;
import com.globalbank.entity.*;
import com.globalbank.api.*;
import com.globalbank.subscriber.*;

/**
 * <h2>ConsentTest</h2>
 * <p>
 * Junit test class for entity Consent.  Handle the full lifecycle as it relates to persistence. Leverages
 * the ConsentService and a ConsentSubscriber to test everything under a MVC framework which includes the
 * ConsentAggregate and related commands, queries, and events.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/12/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Banking Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core banking, payments, lending, cards, KYC, and FX capabilities.</td></tr>
 *          <tr><td>industry</td><td>Banking</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 * 		ConsentService
 * 	    ConsentSubscriber
 *
 * <h3>Methods</h3>
 *      <ul>
 *      <li>read</li>
 *      <li>update</li>
 *      <li>delete</li>
 *      </ul>
 *
 * <p>
 * @author your_name_here
 * </p>
 */
public class ConsentTest {

    // ------------------------------------
	// default constructor
    // ------------------------------------
	public ConsentTest() {
		subscriber = new ConsentSubscriber();
	}

	// test methods
	@Test
	/*
	 * Initiate ConsentTest.
	 */
	public void startTest() throws Throwable {
		try {
			LOGGER.info("**********************************************************");
			LOGGER.info("Beginning test on Consent...");
			LOGGER.info("**********************************************************\n");
			
			// ---------------------------------------------
			// jumpstart process
			// ---------------------------------------------
			jumpStart();
			
		} catch (Throwable e) {
			throw e;
		} finally {
		}
	}

	/** 
	 * jumpstart the process by instantiating2 Consent
	 */
	protected void jumpStart() throws Throwable {
		LOGGER.info( "\n======== create instances to get the ball rolling  ======== ");

		consentId = ConsentService.getConsentInstance()
		.createConsent( generateNewCommand() )
		.get();

		// ---------------------------------------------
		// set up query subscriptions after the 1st create
		// ---------------------------------------------
		testingStep = "create";
		setUpQuerySubscriptions();

		ConsentService.getConsentInstance()
				.createConsent( generateNewCommand() )
				.get();

	}

	/** 
	 * Set up query subscriptions
	 */
	protected void setUpQuerySubscriptions() throws Throwable {
		LOGGER.info( "\n======== Setting Up Query Subscriptions ======== ");
			
		try {            
			subscriber.consentSubscribe().updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetAll update received for Consent : " + successValue.getConsentId());
							  if (successValue.getConsentId().equals(consentId)) {
								  if (testingStep.equals("create")) {
									  testingStep = "update";
									  update();
								  } else if (testingStep.equals("delete")) {
									  testingStep = "complete";
									  state( getAll().size() == sizeOfConsentList - 1 , "value not deleted from list");
									  LOGGER.info("**********************************************************");
									  LOGGER.info("Consent test completed successfully...");
									  LOGGER.info("**********************************************************\n");
								  }
							  }
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on consent consumed")
					);
			subscriber.consentSubscribe( consentId ).updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetOne update received for Consent : " + successValue.getConsentId() + " in step " + testingStep);
							  testingStep = "delete";
							  sizeOfConsentList = getAll().size();
							  delete();
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on consent for consentId consumed")

					);
			

			}
			catch (Exception e) {
				LOGGER.warning( e.getMessage() );
				throw e;
			}
		}
		
		/** 
	 * read a Consent. 
	 */
	protected Consent read() throws Throwable {
		LOGGER.info( "\n======== READ ======== ");
		LOGGER.info( "-- Reading a previously created Consent" );

		Consent entity = null;
		StringBuilder msg = new StringBuilder( "-- Failed to read Consent with primary key" );
		msg.append( consentId );
		
		ConsentFetchOneSummary fetchOneSummary = new ConsentFetchOneSummary( consentId );

		try {
			entity = ConsentService.getConsentInstance().getConsent( fetchOneSummary );

			assertNotNull( entity,msg.toString() );

			LOGGER.info( "-- Successfully found Consent " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return entity;
	}

	/** 
	 * updating a Consent.
	 */
	protected void update() throws Throwable {
		LOGGER.info( "\n======== UPDATE ======== ");
		LOGGER.info( "-- Attempting to update a Consent." );

		StringBuilder msg = new StringBuilder( "Failed to update a Consent : " );        
		Consent entity = read();
		UpdateConsentCommand command = generateUpdateCommand();
		command.setConsentId(entity.getConsentId());

		try {            
			assertNotNull( entity, msg.toString() );

			LOGGER.info( "-- Now updating the created Consent." );

			// for use later on...
			consentId = entity.getConsentId();

			ConsentService proxy = ConsentService.getConsentInstance();  

			proxy.updateConsent( command ).get();

			LOGGER.info( "-- Successfully saved Consent - " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : primarykey = " + consentId + " : command -" +  command + " : " + e );

			throw e;
		}
	}

	/** 
	 * delete a Consent.
	 */
	protected void delete() throws Throwable {
		LOGGER.info( "\n======== DELETE ======== ");
		LOGGER.info( "-- Deleting a previously created Consent." );

		Consent entity = null;
		
		try{
		    entity = read(); 
			LOGGER.info( "-- Successfully read Consent with id " + consentId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to read Consent with id " + consentId );

			throw e;
		}

		try{
			ConsentService.getConsentInstance().delete( new DeleteConsentCommand( entity.getConsentId() ) ).get();
			LOGGER.info( "-- Successfully deleted Consent with id " + consentId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to delete Consent with id " + consentId );

			throw e;
		}
	}

	/**
	 * get all Consents.
	 */
	protected List<Consent> getAll() throws Throwable {    
		LOGGER.info( "======== GETALL ======== ");
		LOGGER.info( "-- Retrieving Collection of Consents:" );

		StringBuilder msg = new StringBuilder( "-- Failed to get all Consent : " );        
		List<Consent> collection  = new ArrayList<>();

		try {
			// call the static get method on the ConsentService
			collection = ConsentService.getConsentInstance().getAllConsent();
			assertNotNull( collection, "An Empty collection of Consent was incorrectly returned.");
			
			// Now print out the values
			Consent entity = null;            
			Iterator<Consent> iter = collection.iterator();
			int index = 1;

			while( iter.hasNext() ) {
				// Retrieve the entity   
				entity = iter.next();

				assertNotNull( entity,"-- null entity in Collection." );
				assertNotNull( entity.getConsentId(), "-- entity in Collection has a null primary key" );        

				LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
				index++;
			}
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return collection;			
	}

	/**
	 * Assigns a common log handler for each test class in the suite 
	 * in the event log output needs to go elsewhere
	 * 
	 * @param		handler	Handler
	 * @return		ConsentTest
	 */
	protected ConsentTest setHandler(Handler handler) {
		if ( handler != null )
			LOGGER.addHandler(handler); 
		return this;
	}

	/**
	 * Returns a new populated Consent
	 * 
	 * @return CreateConsentCommand alias
	 */
	protected CreateConsentCommand generateNewCommand() {
        CreateConsentCommand command = new CreateConsentCommand( null,   new Date(),   new Date(),  ConsentType.values()[0],  ConsentStatus.values()[0] );
		
		return( command );
	}

		/**
		 * Returns a new populated Consent
		 * 
		 * @return UpdateConsentCommand alias
		 */
	protected UpdateConsentCommand generateUpdateCommand() {
	        UpdateConsentCommand command = new UpdateConsentCommand( null,   new Date(),   new Date(),  null,  null,  new HashSet<>(),  null,  ConsentType.values()[0],  ConsentStatus.values()[0] );
			
			return( command );
		}
	//-----------------------------------------------------
	// attributes 
	//-----------------------------------------------------
	protected UUID consentId = null;
	protected ConsentSubscriber subscriber = null;
	private final String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
	private final Logger LOGGER = Logger.getLogger(ConsentTest.class.getName());
	private String testingStep = "";
	private Integer sizeOfConsentList = 0;
}
