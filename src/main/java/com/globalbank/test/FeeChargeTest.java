/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.globalbank.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.springframework.util.Assert.state;

import com.globalbank.service.*;
import com.globalbank.entity.*;
import com.globalbank.api.*;
import com.globalbank.subscriber.*;

/**
 * <h2>FeeChargeTest</h2>
 * <p>
 * Junit test class for entity FeeCharge.  Handle the full lifecycle as it relates to persistence. Leverages
 * the FeeChargeService and a FeeChargeSubscriber to test everything under a MVC framework which includes the
 * FeeChargeAggregate and related commands, queries, and events.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/12/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Banking Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core banking, payments, lending, cards, KYC, and FX capabilities.</td></tr>
 *          <tr><td>industry</td><td>Banking</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 * 		FeeChargeService
 * 	    FeeChargeSubscriber
 *
 * <h3>Methods</h3>
 *      <ul>
 *      <li>read</li>
 *      <li>update</li>
 *      <li>delete</li>
 *      </ul>
 *
 * <p>
 * @author your_name_here
 * </p>
 */
public class FeeChargeTest {

    // ------------------------------------
	// default constructor
    // ------------------------------------
	public FeeChargeTest() {
		subscriber = new FeeChargeSubscriber();
	}

	// test methods
	@Test
	/*
	 * Initiate FeeChargeTest.
	 */
	public void startTest() throws Throwable {
		try {
			LOGGER.info("**********************************************************");
			LOGGER.info("Beginning test on FeeCharge...");
			LOGGER.info("**********************************************************\n");
			
			// ---------------------------------------------
			// jumpstart process
			// ---------------------------------------------
			jumpStart();
			
		} catch (Throwable e) {
			throw e;
		} finally {
		}
	}

	/** 
	 * jumpstart the process by instantiating2 FeeCharge
	 */
	protected void jumpStart() throws Throwable {
		LOGGER.info( "\n======== create instances to get the ball rolling  ======== ");

		feeChargeId = FeeChargeService.getFeeChargeInstance()
		.createFeeCharge( generateNewCommand() )
		.get();

		// ---------------------------------------------
		// set up query subscriptions after the 1st create
		// ---------------------------------------------
		testingStep = "create";
		setUpQuerySubscriptions();

		FeeChargeService.getFeeChargeInstance()
				.createFeeCharge( generateNewCommand() )
				.get();

	}

	/** 
	 * Set up query subscriptions
	 */
	protected void setUpQuerySubscriptions() throws Throwable {
		LOGGER.info( "\n======== Setting Up Query Subscriptions ======== ");
			
		try {            
			subscriber.feeChargeSubscribe().updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetAll update received for FeeCharge : " + successValue.getFeeChargeId());
							  if (successValue.getFeeChargeId().equals(feeChargeId)) {
								  if (testingStep.equals("create")) {
									  testingStep = "update";
									  update();
								  } else if (testingStep.equals("delete")) {
									  testingStep = "complete";
									  state( getAll().size() == sizeOfFeeChargeList - 1 , "value not deleted from list");
									  LOGGER.info("**********************************************************");
									  LOGGER.info("FeeCharge test completed successfully...");
									  LOGGER.info("**********************************************************\n");
								  }
							  }
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on feeCharge consumed")
					);
			subscriber.feeChargeSubscribe( feeChargeId ).updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetOne update received for FeeCharge : " + successValue.getFeeChargeId() + " in step " + testingStep);
							  testingStep = "delete";
							  sizeOfFeeChargeList = getAll().size();
							  delete();
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on feeCharge for feeChargeId consumed")

					);
			

			}
			catch (Exception e) {
				LOGGER.warning( e.getMessage() );
				throw e;
			}
		}
		
		/** 
	 * read a FeeCharge. 
	 */
	protected FeeCharge read() throws Throwable {
		LOGGER.info( "\n======== READ ======== ");
		LOGGER.info( "-- Reading a previously created FeeCharge" );

		FeeCharge entity = null;
		StringBuilder msg = new StringBuilder( "-- Failed to read FeeCharge with primary key" );
		msg.append( feeChargeId );
		
		FeeChargeFetchOneSummary fetchOneSummary = new FeeChargeFetchOneSummary( feeChargeId );

		try {
			entity = FeeChargeService.getFeeChargeInstance().getFeeCharge( fetchOneSummary );

			assertNotNull( entity,msg.toString() );

			LOGGER.info( "-- Successfully found FeeCharge " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return entity;
	}

	/** 
	 * updating a FeeCharge.
	 */
	protected void update() throws Throwable {
		LOGGER.info( "\n======== UPDATE ======== ");
		LOGGER.info( "-- Attempting to update a FeeCharge." );

		StringBuilder msg = new StringBuilder( "Failed to update a FeeCharge : " );        
		FeeCharge entity = read();
		UpdateFeeChargeCommand command = generateUpdateCommand();
		command.setFeeChargeId(entity.getFeeChargeId());

		try {            
			assertNotNull( entity, msg.toString() );

			LOGGER.info( "-- Now updating the created FeeCharge." );

			// for use later on...
			feeChargeId = entity.getFeeChargeId();

			FeeChargeService proxy = FeeChargeService.getFeeChargeInstance();  

			proxy.updateFeeCharge( command ).get();

			LOGGER.info( "-- Successfully saved FeeCharge - " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : primarykey = " + feeChargeId + " : command -" +  command + " : " + e );

			throw e;
		}
	}

	/** 
	 * delete a FeeCharge.
	 */
	protected void delete() throws Throwable {
		LOGGER.info( "\n======== DELETE ======== ");
		LOGGER.info( "-- Deleting a previously created FeeCharge." );

		FeeCharge entity = null;
		
		try{
		    entity = read(); 
			LOGGER.info( "-- Successfully read FeeCharge with id " + feeChargeId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to read FeeCharge with id " + feeChargeId );

			throw e;
		}

		try{
			FeeChargeService.getFeeChargeInstance().delete( new DeleteFeeChargeCommand( entity.getFeeChargeId() ) ).get();
			LOGGER.info( "-- Successfully deleted FeeCharge with id " + feeChargeId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to delete FeeCharge with id " + feeChargeId );

			throw e;
		}
	}

	/**
	 * get all FeeCharges.
	 */
	protected List<FeeCharge> getAll() throws Throwable {    
		LOGGER.info( "======== GETALL ======== ");
		LOGGER.info( "-- Retrieving Collection of FeeCharges:" );

		StringBuilder msg = new StringBuilder( "-- Failed to get all FeeCharge : " );        
		List<FeeCharge> collection  = new ArrayList<>();

		try {
			// call the static get method on the FeeChargeService
			collection = FeeChargeService.getFeeChargeInstance().getAllFeeCharge();
			assertNotNull( collection, "An Empty collection of FeeCharge was incorrectly returned.");
			
			// Now print out the values
			FeeCharge entity = null;            
			Iterator<FeeCharge> iter = collection.iterator();
			int index = 1;

			while( iter.hasNext() ) {
				// Retrieve the entity   
				entity = iter.next();

				assertNotNull( entity,"-- null entity in Collection." );
				assertNotNull( entity.getFeeChargeId(), "-- entity in Collection has a null primary key" );        

				LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
				index++;
			}
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return collection;			
	}

	/**
	 * Assigns a common log handler for each test class in the suite 
	 * in the event log output needs to go elsewhere
	 * 
	 * @param		handler	Handler
	 * @return		FeeChargeTest
	 */
	protected FeeChargeTest setHandler(Handler handler) {
		if ( handler != null )
			LOGGER.addHandler(handler); 
		return this;
	}

	/**
	 * Returns a new populated FeeCharge
	 * 
	 * @return CreateFeeChargeCommand alias
	 */
	protected CreateFeeChargeCommand generateNewCommand() {
        CreateFeeChargeCommand command = new CreateFeeChargeCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Money(),   new Date(),  FeeType.values()[0] );
		
		return( command );
	}

		/**
		 * Returns a new populated FeeCharge
		 * 
		 * @return UpdateFeeChargeCommand alias
		 */
	protected UpdateFeeChargeCommand generateUpdateCommand() {
	        UpdateFeeChargeCommand command = new UpdateFeeChargeCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Money(),   new Date(),  null,  null,  FeeType.values()[0] );
			
			return( command );
		}
	//-----------------------------------------------------
	// attributes 
	//-----------------------------------------------------
	protected UUID feeChargeId = null;
	protected FeeChargeSubscriber subscriber = null;
	private final String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
	private final Logger LOGGER = Logger.getLogger(FeeChargeTest.class.getName());
	private String testingStep = "";
	private Integer sizeOfFeeChargeList = 0;
}
