/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.globalbank.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.springframework.util.Assert.state;

import com.globalbank.service.*;
import com.globalbank.entity.*;
import com.globalbank.api.*;
import com.globalbank.subscriber.*;

/**
 * <h2>LoanAccountTest</h2>
 * <p>
 * Junit test class for entity LoanAccount.  Handle the full lifecycle as it relates to persistence. Leverages
 * the LoanAccountService and a LoanAccountSubscriber to test everything under a MVC framework which includes the
 * LoanAccountAggregate and related commands, queries, and events.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/12/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Banking Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core banking, payments, lending, cards, KYC, and FX capabilities.</td></tr>
 *          <tr><td>industry</td><td>Banking</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 * 		LoanAccountService
 * 	    LoanAccountSubscriber
 *
 * <h3>Methods</h3>
 *      <ul>
 *      <li>read</li>
 *      <li>update</li>
 *      <li>delete</li>
 *      </ul>
 *
 * <p>
 * @author your_name_here
 * </p>
 */
public class LoanAccountTest {

    // ------------------------------------
	// default constructor
    // ------------------------------------
	public LoanAccountTest() {
		subscriber = new LoanAccountSubscriber();
	}

	// test methods
	@Test
	/*
	 * Initiate LoanAccountTest.
	 */
	public void startTest() throws Throwable {
		try {
			LOGGER.info("**********************************************************");
			LOGGER.info("Beginning test on LoanAccount...");
			LOGGER.info("**********************************************************\n");
			
			// ---------------------------------------------
			// jumpstart process
			// ---------------------------------------------
			jumpStart();
			
		} catch (Throwable e) {
			throw e;
		} finally {
		}
	}

	/** 
	 * jumpstart the process by instantiating2 LoanAccount
	 */
	protected void jumpStart() throws Throwable {
		LOGGER.info( "\n======== create instances to get the ball rolling  ======== ");

		loanAccountId = LoanAccountService.getLoanAccountInstance()
		.createLoanAccount( generateNewCommand() )
		.get();

		// ---------------------------------------------
		// set up query subscriptions after the 1st create
		// ---------------------------------------------
		testingStep = "create";
		setUpQuerySubscriptions();

		LoanAccountService.getLoanAccountInstance()
				.createLoanAccount( generateNewCommand() )
				.get();

	}

	/** 
	 * Set up query subscriptions
	 */
	protected void setUpQuerySubscriptions() throws Throwable {
		LOGGER.info( "\n======== Setting Up Query Subscriptions ======== ");
			
		try {            
			subscriber.loanAccountSubscribe().updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetAll update received for LoanAccount : " + successValue.getLoanAccountId());
							  if (successValue.getLoanAccountId().equals(loanAccountId)) {
								  if (testingStep.equals("create")) {
									  testingStep = "update";
									  update();
								  } else if (testingStep.equals("delete")) {
									  testingStep = "complete";
									  state( getAll().size() == sizeOfLoanAccountList - 1 , "value not deleted from list");
									  LOGGER.info("**********************************************************");
									  LOGGER.info("LoanAccount test completed successfully...");
									  LOGGER.info("**********************************************************\n");
								  }
							  }
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on loanAccount consumed")
					);
			subscriber.loanAccountSubscribe( loanAccountId ).updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetOne update received for LoanAccount : " + successValue.getLoanAccountId() + " in step " + testingStep);
							  testingStep = "delete";
							  sizeOfLoanAccountList = getAll().size();
							  delete();
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on loanAccount for loanAccountId consumed")

					);
			

			}
			catch (Exception e) {
				LOGGER.warning( e.getMessage() );
				throw e;
			}
		}
		
		/** 
	 * read a LoanAccount. 
	 */
	protected LoanAccount read() throws Throwable {
		LOGGER.info( "\n======== READ ======== ");
		LOGGER.info( "-- Reading a previously created LoanAccount" );

		LoanAccount entity = null;
		StringBuilder msg = new StringBuilder( "-- Failed to read LoanAccount with primary key" );
		msg.append( loanAccountId );
		
		LoanAccountFetchOneSummary fetchOneSummary = new LoanAccountFetchOneSummary( loanAccountId );

		try {
			entity = LoanAccountService.getLoanAccountInstance().getLoanAccount( fetchOneSummary );

			assertNotNull( entity,msg.toString() );

			LOGGER.info( "-- Successfully found LoanAccount " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return entity;
	}

	/** 
	 * updating a LoanAccount.
	 */
	protected void update() throws Throwable {
		LOGGER.info( "\n======== UPDATE ======== ");
		LOGGER.info( "-- Attempting to update a LoanAccount." );

		StringBuilder msg = new StringBuilder( "Failed to update a LoanAccount : " );        
		LoanAccount entity = read();
		UpdateLoanAccountCommand command = generateUpdateCommand();
		command.setLoanAccountId(entity.getLoanAccountId());

		try {            
			assertNotNull( entity, msg.toString() );

			LOGGER.info( "-- Now updating the created LoanAccount." );

			// for use later on...
			loanAccountId = entity.getLoanAccountId();

			LoanAccountService proxy = LoanAccountService.getLoanAccountInstance();  

			proxy.updateLoanAccount( command ).get();

			LOGGER.info( "-- Successfully saved LoanAccount - " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : primarykey = " + loanAccountId + " : command -" +  command + " : " + e );

			throw e;
		}
	}

	/** 
	 * delete a LoanAccount.
	 */
	protected void delete() throws Throwable {
		LOGGER.info( "\n======== DELETE ======== ");
		LOGGER.info( "-- Deleting a previously created LoanAccount." );

		LoanAccount entity = null;
		
		try{
		    entity = read(); 
			LOGGER.info( "-- Successfully read LoanAccount with id " + loanAccountId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to read LoanAccount with id " + loanAccountId );

			throw e;
		}

		try{
			LoanAccountService.getLoanAccountInstance().delete( new DeleteLoanAccountCommand( entity.getLoanAccountId() ) ).get();
			LOGGER.info( "-- Successfully deleted LoanAccount with id " + loanAccountId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to delete LoanAccount with id " + loanAccountId );

			throw e;
		}
	}

	/**
	 * get all LoanAccounts.
	 */
	protected List<LoanAccount> getAll() throws Throwable {    
		LOGGER.info( "======== GETALL ======== ");
		LOGGER.info( "-- Retrieving Collection of LoanAccounts:" );

		StringBuilder msg = new StringBuilder( "-- Failed to get all LoanAccount : " );        
		List<LoanAccount> collection  = new ArrayList<>();

		try {
			// call the static get method on the LoanAccountService
			collection = LoanAccountService.getLoanAccountInstance().getAllLoanAccount();
			assertNotNull( collection, "An Empty collection of LoanAccount was incorrectly returned.");
			
			// Now print out the values
			LoanAccount entity = null;            
			Iterator<LoanAccount> iter = collection.iterator();
			int index = 1;

			while( iter.hasNext() ) {
				// Retrieve the entity   
				entity = iter.next();

				assertNotNull( entity,"-- null entity in Collection." );
				assertNotNull( entity.getLoanAccountId(), "-- entity in Collection has a null primary key" );        

				LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
				index++;
			}
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return collection;			
	}

	/**
	 * Assigns a common log handler for each test class in the suite 
	 * in the event log output needs to go elsewhere
	 * 
	 * @param		handler	Handler
	 * @return		LoanAccountTest
	 */
	protected LoanAccountTest setHandler(Handler handler) {
		if ( handler != null )
			LOGGER.addHandler(handler); 
		return this;
	}

	/**
	 * Returns a new populated LoanAccount
	 * 
	 * @return CreateLoanAccountCommand alias
	 */
	protected CreateLoanAccountCommand generateNewCommand() {
        CreateLoanAccountCommand command = new CreateLoanAccountCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Money(),  new Money(),  new Percentage(),   new Date(),   new Date(),  0,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  LoanType.values()[0],  RateType.values()[0],  InterestCompounding.values()[0],  LoanStatus.values()[0] );
		
		return( command );
	}

		/**
		 * Returns a new populated LoanAccount
		 * 
		 * @return UpdateLoanAccountCommand alias
		 */
	protected UpdateLoanAccountCommand generateUpdateCommand() {
	        UpdateLoanAccountCommand command = new UpdateLoanAccountCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Money(),  new Money(),  new Percentage(),   new Date(),   new Date(),  0,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  null,  null,  null,  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  LoanType.values()[0],  RateType.values()[0],  InterestCompounding.values()[0],  LoanStatus.values()[0] );
			
			return( command );
		}
	//-----------------------------------------------------
	// attributes 
	//-----------------------------------------------------
	protected UUID loanAccountId = null;
	protected LoanAccountSubscriber subscriber = null;
	private final String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
	private final Logger LOGGER = Logger.getLogger(LoanAccountTest.class.getName());
	private String testingStep = "";
	private Integer sizeOfLoanAccountList = 0;
}
