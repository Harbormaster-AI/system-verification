/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.globalbank.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.springframework.util.Assert.state;

import com.globalbank.service.*;
import com.globalbank.entity.*;
import com.globalbank.api.*;
import com.globalbank.subscriber.*;

/**
 * <h2>BankingProductTest</h2>
 * <p>
 * Junit test class for entity BankingProduct.  Handle the full lifecycle as it relates to persistence. Leverages
 * the BankingProductService and a BankingProductSubscriber to test everything under a MVC framework which includes the
 * BankingProductAggregate and related commands, queries, and events.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/12/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Banking Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core banking, payments, lending, cards, KYC, and FX capabilities.</td></tr>
 *          <tr><td>industry</td><td>Banking</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 * 		BankingProductService
 * 	    BankingProductSubscriber
 *
 * <h3>Methods</h3>
 *      <ul>
 *      <li>read</li>
 *      <li>update</li>
 *      <li>delete</li>
 *      </ul>
 *
 * <p>
 * @author your_name_here
 * </p>
 */
public class BankingProductTest {

    // ------------------------------------
	// default constructor
    // ------------------------------------
	public BankingProductTest() {
		subscriber = new BankingProductSubscriber();
	}

	// test methods
	@Test
	/*
	 * Initiate BankingProductTest.
	 */
	public void startTest() throws Throwable {
		try {
			LOGGER.info("**********************************************************");
			LOGGER.info("Beginning test on BankingProduct...");
			LOGGER.info("**********************************************************\n");
			
			// ---------------------------------------------
			// jumpstart process
			// ---------------------------------------------
			jumpStart();
			
		} catch (Throwable e) {
			throw e;
		} finally {
		}
	}

	/** 
	 * jumpstart the process by instantiating2 BankingProduct
	 */
	protected void jumpStart() throws Throwable {
		LOGGER.info( "\n======== create instances to get the ball rolling  ======== ");

		bankingProductId = BankingProductService.getBankingProductInstance()
		.createBankingProduct( generateNewCommand() )
		.get();

		// ---------------------------------------------
		// set up query subscriptions after the 1st create
		// ---------------------------------------------
		testingStep = "create";
		setUpQuerySubscriptions();

		BankingProductService.getBankingProductInstance()
				.createBankingProduct( generateNewCommand() )
				.get();

	}

	/** 
	 * Set up query subscriptions
	 */
	protected void setUpQuerySubscriptions() throws Throwable {
		LOGGER.info( "\n======== Setting Up Query Subscriptions ======== ");
			
		try {            
			subscriber.bankingProductSubscribe().updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetAll update received for BankingProduct : " + successValue.getBankingProductId());
							  if (successValue.getBankingProductId().equals(bankingProductId)) {
								  if (testingStep.equals("create")) {
									  testingStep = "update";
									  update();
								  } else if (testingStep.equals("delete")) {
									  testingStep = "complete";
									  state( getAll().size() == sizeOfBankingProductList - 1 , "value not deleted from list");
									  LOGGER.info("**********************************************************");
									  LOGGER.info("BankingProduct test completed successfully...");
									  LOGGER.info("**********************************************************\n");
								  }
							  }
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on bankingProduct consumed")
					);
			subscriber.bankingProductSubscribe( bankingProductId ).updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetOne update received for BankingProduct : " + successValue.getBankingProductId() + " in step " + testingStep);
							  testingStep = "delete";
							  sizeOfBankingProductList = getAll().size();
							  delete();
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on bankingProduct for bankingProductId consumed")

					);
			

			}
			catch (Exception e) {
				LOGGER.warning( e.getMessage() );
				throw e;
			}
		}
		
		/** 
	 * read a BankingProduct. 
	 */
	protected BankingProduct read() throws Throwable {
		LOGGER.info( "\n======== READ ======== ");
		LOGGER.info( "-- Reading a previously created BankingProduct" );

		BankingProduct entity = null;
		StringBuilder msg = new StringBuilder( "-- Failed to read BankingProduct with primary key" );
		msg.append( bankingProductId );
		
		BankingProductFetchOneSummary fetchOneSummary = new BankingProductFetchOneSummary( bankingProductId );

		try {
			entity = BankingProductService.getBankingProductInstance().getBankingProduct( fetchOneSummary );

			assertNotNull( entity,msg.toString() );

			LOGGER.info( "-- Successfully found BankingProduct " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return entity;
	}

	/** 
	 * updating a BankingProduct.
	 */
	protected void update() throws Throwable {
		LOGGER.info( "\n======== UPDATE ======== ");
		LOGGER.info( "-- Attempting to update a BankingProduct." );

		StringBuilder msg = new StringBuilder( "Failed to update a BankingProduct : " );        
		BankingProduct entity = read();
		UpdateBankingProductCommand command = generateUpdateCommand();
		command.setBankingProductId(entity.getBankingProductId());

		try {            
			assertNotNull( entity, msg.toString() );

			LOGGER.info( "-- Now updating the created BankingProduct." );

			// for use later on...
			bankingProductId = entity.getBankingProductId();

			BankingProductService proxy = BankingProductService.getBankingProductInstance();  

			proxy.updateBankingProduct( command ).get();

			LOGGER.info( "-- Successfully saved BankingProduct - " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : primarykey = " + bankingProductId + " : command -" +  command + " : " + e );

			throw e;
		}
	}

	/** 
	 * delete a BankingProduct.
	 */
	protected void delete() throws Throwable {
		LOGGER.info( "\n======== DELETE ======== ");
		LOGGER.info( "-- Deleting a previously created BankingProduct." );

		BankingProduct entity = null;
		
		try{
		    entity = read(); 
			LOGGER.info( "-- Successfully read BankingProduct with id " + bankingProductId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to read BankingProduct with id " + bankingProductId );

			throw e;
		}

		try{
			BankingProductService.getBankingProductInstance().delete( new DeleteBankingProductCommand( entity.getBankingProductId() ) ).get();
			LOGGER.info( "-- Successfully deleted BankingProduct with id " + bankingProductId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to delete BankingProduct with id " + bankingProductId );

			throw e;
		}
	}

	/**
	 * get all BankingProducts.
	 */
	protected List<BankingProduct> getAll() throws Throwable {    
		LOGGER.info( "======== GETALL ======== ");
		LOGGER.info( "-- Retrieving Collection of BankingProducts:" );

		StringBuilder msg = new StringBuilder( "-- Failed to get all BankingProduct : " );        
		List<BankingProduct> collection  = new ArrayList<>();

		try {
			// call the static get method on the BankingProductService
			collection = BankingProductService.getBankingProductInstance().getAllBankingProduct();
			assertNotNull( collection, "An Empty collection of BankingProduct was incorrectly returned.");
			
			// Now print out the values
			BankingProduct entity = null;            
			Iterator<BankingProduct> iter = collection.iterator();
			int index = 1;

			while( iter.hasNext() ) {
				// Retrieve the entity   
				entity = iter.next();

				assertNotNull( entity,"-- null entity in Collection." );
				assertNotNull( entity.getBankingProductId(), "-- entity in Collection has a null primary key" );        

				LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
				index++;
			}
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return collection;			
	}

	/**
	 * Assigns a common log handler for each test class in the suite 
	 * in the event log output needs to go elsewhere
	 * 
	 * @param		handler	Handler
	 * @return		BankingProductTest
	 */
	protected BankingProductTest setHandler(Handler handler) {
		if ( handler != null )
			LOGGER.addHandler(handler); 
		return this;
	}

	/**
	 * Returns a new populated BankingProduct
	 * 
	 * @return CreateBankingProductCommand alias
	 */
	protected CreateBankingProductCommand generateNewCommand() {
        CreateBankingProductCommand command = new CreateBankingProductCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  ProductCategory.values()[0] );
		
		return( command );
	}

		/**
		 * Returns a new populated BankingProduct
		 * 
		 * @return UpdateBankingProductCommand alias
		 */
	protected UpdateBankingProductCommand generateUpdateCommand() {
	        UpdateBankingProductCommand command = new UpdateBankingProductCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  null,  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  ProductCategory.values()[0] );
			
			return( command );
		}
	//-----------------------------------------------------
	// attributes 
	//-----------------------------------------------------
	protected UUID bankingProductId = null;
	protected BankingProductSubscriber subscriber = null;
	private final String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
	private final Logger LOGGER = Logger.getLogger(BankingProductTest.class.getName());
	private String testingStep = "";
	private Integer sizeOfBankingProductList = 0;
}
