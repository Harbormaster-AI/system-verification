/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.globalbank.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.springframework.util.Assert.state;

import com.globalbank.service.*;
import com.globalbank.entity.*;
import com.globalbank.api.*;
import com.globalbank.subscriber.*;

/**
 * <h2>ExchangeRateTest</h2>
 * <p>
 * Junit test class for entity ExchangeRate.  Handle the full lifecycle as it relates to persistence. Leverages
 * the ExchangeRateService and a ExchangeRateSubscriber to test everything under a MVC framework which includes the
 * ExchangeRateAggregate and related commands, queries, and events.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/12/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Banking Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core banking, payments, lending, cards, KYC, and FX capabilities.</td></tr>
 *          <tr><td>industry</td><td>Banking</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 * 		ExchangeRateService
 * 	    ExchangeRateSubscriber
 *
 * <h3>Methods</h3>
 *      <ul>
 *      <li>read</li>
 *      <li>update</li>
 *      <li>delete</li>
 *      </ul>
 *
 * <p>
 * @author your_name_here
 * </p>
 */
public class ExchangeRateTest {

    // ------------------------------------
	// default constructor
    // ------------------------------------
	public ExchangeRateTest() {
		subscriber = new ExchangeRateSubscriber();
	}

	// test methods
	@Test
	/*
	 * Initiate ExchangeRateTest.
	 */
	public void startTest() throws Throwable {
		try {
			LOGGER.info("**********************************************************");
			LOGGER.info("Beginning test on ExchangeRate...");
			LOGGER.info("**********************************************************\n");
			
			// ---------------------------------------------
			// jumpstart process
			// ---------------------------------------------
			jumpStart();
			
		} catch (Throwable e) {
			throw e;
		} finally {
		}
	}

	/** 
	 * jumpstart the process by instantiating2 ExchangeRate
	 */
	protected void jumpStart() throws Throwable {
		LOGGER.info( "\n======== create instances to get the ball rolling  ======== ");

		exchangeRateId = ExchangeRateService.getExchangeRateInstance()
		.createExchangeRate( generateNewCommand() )
		.get();

		// ---------------------------------------------
		// set up query subscriptions after the 1st create
		// ---------------------------------------------
		testingStep = "create";
		setUpQuerySubscriptions();

		ExchangeRateService.getExchangeRateInstance()
				.createExchangeRate( generateNewCommand() )
				.get();

	}

	/** 
	 * Set up query subscriptions
	 */
	protected void setUpQuerySubscriptions() throws Throwable {
		LOGGER.info( "\n======== Setting Up Query Subscriptions ======== ");
			
		try {            
			subscriber.exchangeRateSubscribe().updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetAll update received for ExchangeRate : " + successValue.getExchangeRateId());
							  if (successValue.getExchangeRateId().equals(exchangeRateId)) {
								  if (testingStep.equals("create")) {
									  testingStep = "update";
									  update();
								  } else if (testingStep.equals("delete")) {
									  testingStep = "complete";
									  state( getAll().size() == sizeOfExchangeRateList - 1 , "value not deleted from list");
									  LOGGER.info("**********************************************************");
									  LOGGER.info("ExchangeRate test completed successfully...");
									  LOGGER.info("**********************************************************\n");
								  }
							  }
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on exchangeRate consumed")
					);
			subscriber.exchangeRateSubscribe( exchangeRateId ).updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetOne update received for ExchangeRate : " + successValue.getExchangeRateId() + " in step " + testingStep);
							  testingStep = "delete";
							  sizeOfExchangeRateList = getAll().size();
							  delete();
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on exchangeRate for exchangeRateId consumed")

					);
			

			}
			catch (Exception e) {
				LOGGER.warning( e.getMessage() );
				throw e;
			}
		}
		
		/** 
	 * read a ExchangeRate. 
	 */
	protected ExchangeRate read() throws Throwable {
		LOGGER.info( "\n======== READ ======== ");
		LOGGER.info( "-- Reading a previously created ExchangeRate" );

		ExchangeRate entity = null;
		StringBuilder msg = new StringBuilder( "-- Failed to read ExchangeRate with primary key" );
		msg.append( exchangeRateId );
		
		ExchangeRateFetchOneSummary fetchOneSummary = new ExchangeRateFetchOneSummary( exchangeRateId );

		try {
			entity = ExchangeRateService.getExchangeRateInstance().getExchangeRate( fetchOneSummary );

			assertNotNull( entity,msg.toString() );

			LOGGER.info( "-- Successfully found ExchangeRate " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return entity;
	}

	/** 
	 * updating a ExchangeRate.
	 */
	protected void update() throws Throwable {
		LOGGER.info( "\n======== UPDATE ======== ");
		LOGGER.info( "-- Attempting to update a ExchangeRate." );

		StringBuilder msg = new StringBuilder( "Failed to update a ExchangeRate : " );        
		ExchangeRate entity = read();
		UpdateExchangeRateCommand command = generateUpdateCommand();
		command.setExchangeRateId(entity.getExchangeRateId());

		try {            
			assertNotNull( entity, msg.toString() );

			LOGGER.info( "-- Now updating the created ExchangeRate." );

			// for use later on...
			exchangeRateId = entity.getExchangeRateId();

			ExchangeRateService proxy = ExchangeRateService.getExchangeRateInstance();  

			proxy.updateExchangeRate( command ).get();

			LOGGER.info( "-- Successfully saved ExchangeRate - " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : primarykey = " + exchangeRateId + " : command -" +  command + " : " + e );

			throw e;
		}
	}

	/** 
	 * delete a ExchangeRate.
	 */
	protected void delete() throws Throwable {
		LOGGER.info( "\n======== DELETE ======== ");
		LOGGER.info( "-- Deleting a previously created ExchangeRate." );

		ExchangeRate entity = null;
		
		try{
		    entity = read(); 
			LOGGER.info( "-- Successfully read ExchangeRate with id " + exchangeRateId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to read ExchangeRate with id " + exchangeRateId );

			throw e;
		}

		try{
			ExchangeRateService.getExchangeRateInstance().delete( new DeleteExchangeRateCommand( entity.getExchangeRateId() ) ).get();
			LOGGER.info( "-- Successfully deleted ExchangeRate with id " + exchangeRateId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to delete ExchangeRate with id " + exchangeRateId );

			throw e;
		}
	}

	/**
	 * get all ExchangeRates.
	 */
	protected List<ExchangeRate> getAll() throws Throwable {    
		LOGGER.info( "======== GETALL ======== ");
		LOGGER.info( "-- Retrieving Collection of ExchangeRates:" );

		StringBuilder msg = new StringBuilder( "-- Failed to get all ExchangeRate : " );        
		List<ExchangeRate> collection  = new ArrayList<>();

		try {
			// call the static get method on the ExchangeRateService
			collection = ExchangeRateService.getExchangeRateInstance().getAllExchangeRate();
			assertNotNull( collection, "An Empty collection of ExchangeRate was incorrectly returned.");
			
			// Now print out the values
			ExchangeRate entity = null;            
			Iterator<ExchangeRate> iter = collection.iterator();
			int index = 1;

			while( iter.hasNext() ) {
				// Retrieve the entity   
				entity = iter.next();

				assertNotNull( entity,"-- null entity in Collection." );
				assertNotNull( entity.getExchangeRateId(), "-- entity in Collection has a null primary key" );        

				LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
				index++;
			}
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return collection;			
	}

	/**
	 * Assigns a common log handler for each test class in the suite 
	 * in the event log output needs to go elsewhere
	 * 
	 * @param		handler	Handler
	 * @return		ExchangeRateTest
	 */
	protected ExchangeRateTest setHandler(Handler handler) {
		if ( handler != null )
			LOGGER.addHandler(handler); 
		return this;
	}

	/**
	 * Returns a new populated ExchangeRate
	 * 
	 * @return CreateExchangeRateCommand alias
	 */
	protected CreateExchangeRateCommand generateNewCommand() {
        CreateExchangeRateCommand command = new CreateExchangeRateCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  java.math.BigDecimal.ZERO,   new Date(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16) );
		
		return( command );
	}

		/**
		 * Returns a new populated ExchangeRate
		 * 
		 * @return UpdateExchangeRateCommand alias
		 */
	protected UpdateExchangeRateCommand generateUpdateCommand() {
	        UpdateExchangeRateCommand command = new UpdateExchangeRateCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  java.math.BigDecimal.ZERO,   new Date(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  null,  new HashSet<>() );
			
			return( command );
		}
	//-----------------------------------------------------
	// attributes 
	//-----------------------------------------------------
	protected UUID exchangeRateId = null;
	protected ExchangeRateSubscriber subscriber = null;
	private final String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
	private final Logger LOGGER = Logger.getLogger(ExchangeRateTest.class.getName());
	private String testingStep = "";
	private Integer sizeOfExchangeRateList = 0;
}
