/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.globalbank.service;

import java.io.IOException;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.*;
import java.util.concurrent.*;

import org.axonframework.commandhandling.gateway.CommandGateway;
import org.axonframework.queryhandling.QueryGateway;
import org.axonframework.queryhandling.QueryUpdateEmitter;
import org.axonframework.messaging.responsetypes.ResponseTypes;
import org.springframework.stereotype.Service;
import org.springframework.beans.BeansException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;

import com.globalbank.api.*;
import com.globalbank.entity.*;
import com.globalbank.exception.*;
import com.globalbank.validator.*;

/**
 *
 * <h2>Customer business service</h2>
 *
 * <p>
 * This class implements the Service design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of Customer related services in the case of a Customer business related service failing.</li>
 * <li>Exposes a simpler, uniform Customer interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill Customer business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/12/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Banking Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core banking, payments, lending, cards, KYC, and FX capabilities.</td></tr>
 *          <tr><td>industry</td><td>Banking</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author your_name_here</p>
 *
 */
@Service
public class CustomerService 
extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
    /** 
     * Default Constructor 
     */
    public CustomerService()  {
	}

	@Override
	public void setApplicationContext(ApplicationContext ctx) throws BeansException {
		super.setApplicationContext(ctx);

		queryGateway 		= ctx.getBean(QueryGateway.class);
		commandGateway 		= ctx.getBean(CommandGateway.class);
		queryUpdateEmitter  = ctx.getBean(QueryUpdateEmitter.class);
		validator			= ctx.getBean(CustomerValidator.class);
	}


   /**
	* Customer Business Delegate Factory Method
	*
	* All methods are expected to be self-sufficient.
	*
	* @return 	CustomerService
	*/
	public static CustomerService getCustomerInstance() {
		return( new CustomerService() );
	}
 
   /**
    * Creates the provided command.
    * 
    * @param		command ${class.getCreateCommandAlias()}
    * @exception    ProcessingException
    * @exception	IllegalArgumentException
    * @return		CompletableFuture<UUID>
    */
	public CompletableFuture<UUID> createCustomer( CreateCustomerCommand command )
    throws ProcessingException, IllegalArgumentException {

		CompletableFuture<UUID> completableFuture = null;
				
		try {
			// --------------------------------------
        	// assign identity now if none
        	// -------------------------------------- 
			if ( command.getCustomerId() == null )
				command.setCustomerId( UUID.randomUUID() );
				
			// --------------------------------------
        	// validate the command
        	// --------------------------------------    	
        	validator.validate( command );    

    		// ---------------------------------------
    		// issue the CreateCustomerCommand - by convention the future return value for a create command
        	// that is handled by the constructor of an aggregate will return the UUID 
    		// ---------------------------------------
        	completableFuture = commandGateway.send( command );
        	
			LOGGER.log( Level.INFO, "return from Command Gateway for CreateCustomerCommand of Customer is " + command );
			
        }
        catch (Exception exc) {
            final String errMsg = "Unable to create Customer - " + exc;
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }        
        
        return completableFuture;
    }

   /**
    * Update the provided command.
    * @param		command UpdateCustomerCommand
    * @return		CompletableFuture<Void>
    * @exception    ProcessingException
    * @exception  	IllegalArgumentException
    */
    public CompletableFuture<Void> updateCustomer( UpdateCustomerCommand command ) 
    throws ProcessingException, IllegalArgumentException {
    	CompletableFuture<Void> completableFuture = null;
    	
    	try {       

			// --------------------------------------
        	// validate 
        	// --------------------------------------    	
        	validator.validate( command );    

        	// --------------------------------------
        	// issue the UpdateCustomerCommand and return right away
        	// --------------------------------------    	
        	completableFuture = commandGateway.send( command );
    	}
        catch (Exception exc) {
            final String errMsg = "Unable to save Customer - " + exc;
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        
    	return completableFuture;
    }
   
   /**
    * Deletes the associatied value object
    * @param		command DeleteCustomerCommand
    * @return		CompletableFuture<Void>
    * @exception 	ProcessingException
    */
    public CompletableFuture<Void> delete( DeleteCustomerCommand command ) 
    throws ProcessingException, IllegalArgumentException {	
    	
    	CompletableFuture<Void> completableFuture = null;
    	
        try {  
			// --------------------------------------
        	// validate the command
        	// --------------------------------------    	
        	validator.validate( command );    
        	
        	// --------------------------------------
        	// issue the DeleteCustomerCommand and return right away
        	// --------------------------------------    	
        	completableFuture = commandGateway.send( command );
        }
        catch (Exception exc) {
            final String errMsg = "Unable to delete Customer using Id = "  + command.getCustomerId();
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }
        
        return completableFuture;
    }

    /**
     * Method to retrieve the Customer via CustomerFetchOneSummary
     * @param 	summary CustomerFetchOneSummary 
     * @return 	CustomerFetchOneResponse
     * @exception ProcessingException - Thrown if processing any related problems
     * @exception IllegalArgumentException 
     */
    public Customer getCustomer( CustomerFetchOneSummary summary ) 
    throws ProcessingException, IllegalArgumentException {
    	
    	if( summary == null )
    		throw new IllegalArgumentException( "CustomerFetchOneSummary arg cannot be null" );
    	
    	Customer entity = null;
    	
        try {
        	// --------------------------------------
        	// validate the fetch one summary
        	// --------------------------------------    	
        	validator.validate( summary );    
        	
        	// --------------------------------------
        	// use queryGateway to send request to Find a Customer
        	// --------------------------------------
        	CompletableFuture<Customer> futureEntity = queryGateway.query(new FindCustomerQuery( new LoadCustomerFilter( summary.getCustomerId() ) ), ResponseTypes.instanceOf(Customer.class));
        	
        	entity = futureEntity.get();
        }
        catch( Exception exc ) {
            final String errMsg = "Unable to locate Customer with id " + summary.getCustomerId();
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }        
        
        return entity;
    }


    /**
     * Method to retrieve a collection of all Customers
     *
     * @return 	List<Customer> 
     * @exception ProcessingException Thrown if any problems
     */
    public List<Customer> getAllCustomer() 
    throws ProcessingException {
        List<Customer> list = null;

        try {
        	CompletableFuture<List<Customer>> futureList = queryGateway.query(new FindAllCustomerQuery(), ResponseTypes.multipleInstancesOf(Customer.class));
        	
        	list = futureList.get();
        }
        catch( Exception exc ) {
            String errMsg = "Failed to get all Customer";
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }        
        
        return list;
    }

    /**
     * assign Bank on Customer
     * @param		command AssignBankToCustomerCommand	
     * @exception	ProcessingException
     */     
	public void assignBank( AssignBankToCustomerCommand command ) throws ProcessingException {

		// --------------------------------------------
		// load the parent
		// --------------------------------------------
		load( command.getCustomerId() );
		
		BankService childDelegate 	= BankService.getBankInstance();
		CustomerService parentDelegate = CustomerService.getCustomerInstance();			
		UUID childId = command.getAssignment().getBankId();
		Bank child = null;
		
		try {
			// --------------------------------------
	    	// best to validate the command now
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );

		}
        catch( Throwable exc ) {
			final String msg = "Failed to get Bank using id " + childId;
			LOGGER.log( Level.WARNING,  msg );
			throw new ProcessingException( msg, exc );
        }
	}

    /**
     * unAssign Bank on Customer
     * @param		command UnAssignBankFromCustomerCommand
     * @exception	ProcessingException
     */     
	public void unAssignBank( UnAssignBankFromCustomerCommand command ) throws ProcessingException {

		try {
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    
	
	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );
		}
		catch( Exception exc ) {
			final String msg = "Failed to unassign Bank on Customer";
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}
	

    /**
     * add Account to Accounts 
     * @param		command AssignAccountsToCustomerCommand
     * @exception	ProcessingException
     */     
	public void addToAccounts( AssignAccountsToCustomerCommand command ) throws ProcessingException {
		
		
		// -------------------------------------------
		// load the parent
		// -------------------------------------------
		load( command.getCustomerId() );

		AccountService childDelegate 	= AccountService.getAccountInstance();
		CustomerService parentDelegate = CustomerService.getCustomerInstance();		
		UUID childId = command.getAddTo().getAccountId();
		
		try {		
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );			
		}
		catch( Exception exc ) {
			final String msg = "Failed to add a Account as Accounts to Customer" ; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}

	}

    /**
     * remove Account from Accounts
     * @param		command RemoveAccountsFromCustomerCommand
     * @exception	ProcessingException
     */     	
	public void removeFromAccounts( RemoveAccountsFromCustomerCommand command ) throws ProcessingException {		
		
		AccountService childDelegate 	= AccountService.getAccountInstance();
		UUID childId = command.getRemoveFrom().getAccountId();

		try {
			
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );

		}
		catch( Exception exc ) {
			final String msg = "Failed to remove child using Id " + childId; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}

    /**
     * add LoanAccount to LoanAccounts 
     * @param		command AssignLoanAccountsToCustomerCommand
     * @exception	ProcessingException
     */     
	public void addToLoanAccounts( AssignLoanAccountsToCustomerCommand command ) throws ProcessingException {
		
		
		// -------------------------------------------
		// load the parent
		// -------------------------------------------
		load( command.getCustomerId() );

		LoanAccountService childDelegate 	= LoanAccountService.getLoanAccountInstance();
		CustomerService parentDelegate = CustomerService.getCustomerInstance();		
		UUID childId = command.getAddTo().getLoanAccountId();
		
		try {		
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );			
		}
		catch( Exception exc ) {
			final String msg = "Failed to add a LoanAccount as LoanAccounts to Customer" ; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}

	}

    /**
     * remove LoanAccount from LoanAccounts
     * @param		command RemoveLoanAccountsFromCustomerCommand
     * @exception	ProcessingException
     */     	
	public void removeFromLoanAccounts( RemoveLoanAccountsFromCustomerCommand command ) throws ProcessingException {		
		
		LoanAccountService childDelegate 	= LoanAccountService.getLoanAccountInstance();
		UUID childId = command.getRemoveFrom().getLoanAccountId();

		try {
			
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );

		}
		catch( Exception exc ) {
			final String msg = "Failed to remove child using Id " + childId; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}

    /**
     * add PaymentCard to PaymentCards 
     * @param		command AssignPaymentCardsToCustomerCommand
     * @exception	ProcessingException
     */     
	public void addToPaymentCards( AssignPaymentCardsToCustomerCommand command ) throws ProcessingException {
		
		
		// -------------------------------------------
		// load the parent
		// -------------------------------------------
		load( command.getCustomerId() );

		PaymentCardService childDelegate 	= PaymentCardService.getPaymentCardInstance();
		CustomerService parentDelegate = CustomerService.getCustomerInstance();		
		UUID childId = command.getAddTo().getPaymentCardId();
		
		try {		
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );			
		}
		catch( Exception exc ) {
			final String msg = "Failed to add a PaymentCard as PaymentCards to Customer" ; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}

	}

    /**
     * remove PaymentCard from PaymentCards
     * @param		command RemovePaymentCardsFromCustomerCommand
     * @exception	ProcessingException
     */     	
	public void removeFromPaymentCards( RemovePaymentCardsFromCustomerCommand command ) throws ProcessingException {		
		
		PaymentCardService childDelegate 	= PaymentCardService.getPaymentCardInstance();
		UUID childId = command.getRemoveFrom().getPaymentCardId();

		try {
			
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );

		}
		catch( Exception exc ) {
			final String msg = "Failed to remove child using Id " + childId; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}

    /**
     * add ExternalAccount to ExternalAccounts 
     * @param		command AssignExternalAccountsToCustomerCommand
     * @exception	ProcessingException
     */     
	public void addToExternalAccounts( AssignExternalAccountsToCustomerCommand command ) throws ProcessingException {
		
		
		// -------------------------------------------
		// load the parent
		// -------------------------------------------
		load( command.getCustomerId() );

		ExternalAccountService childDelegate 	= ExternalAccountService.getExternalAccountInstance();
		CustomerService parentDelegate = CustomerService.getCustomerInstance();		
		UUID childId = command.getAddTo().getExternalAccountId();
		
		try {		
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );			
		}
		catch( Exception exc ) {
			final String msg = "Failed to add a ExternalAccount as ExternalAccounts to Customer" ; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}

	}

    /**
     * remove ExternalAccount from ExternalAccounts
     * @param		command RemoveExternalAccountsFromCustomerCommand
     * @exception	ProcessingException
     */     	
	public void removeFromExternalAccounts( RemoveExternalAccountsFromCustomerCommand command ) throws ProcessingException {		
		
		ExternalAccountService childDelegate 	= ExternalAccountService.getExternalAccountInstance();
		UUID childId = command.getRemoveFrom().getExternalAccountId();

		try {
			
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );

		}
		catch( Exception exc ) {
			final String msg = "Failed to remove child using Id " + childId; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}

    /**
     * add FundsTransfer to FundsTransfers 
     * @param		command AssignFundsTransfersToCustomerCommand
     * @exception	ProcessingException
     */     
	public void addToFundsTransfers( AssignFundsTransfersToCustomerCommand command ) throws ProcessingException {
		
		
		// -------------------------------------------
		// load the parent
		// -------------------------------------------
		load( command.getCustomerId() );

		FundsTransferService childDelegate 	= FundsTransferService.getFundsTransferInstance();
		CustomerService parentDelegate = CustomerService.getCustomerInstance();		
		UUID childId = command.getAddTo().getFundsTransferId();
		
		try {		
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );			
		}
		catch( Exception exc ) {
			final String msg = "Failed to add a FundsTransfer as FundsTransfers to Customer" ; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}

	}

    /**
     * remove FundsTransfer from FundsTransfers
     * @param		command RemoveFundsTransfersFromCustomerCommand
     * @exception	ProcessingException
     */     	
	public void removeFromFundsTransfers( RemoveFundsTransfersFromCustomerCommand command ) throws ProcessingException {		
		
		FundsTransferService childDelegate 	= FundsTransferService.getFundsTransferInstance();
		UUID childId = command.getRemoveFrom().getFundsTransferId();

		try {
			
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );

		}
		catch( Exception exc ) {
			final String msg = "Failed to remove child using Id " + childId; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}

    /**
     * add Dispute to Disputes 
     * @param		command AssignDisputesToCustomerCommand
     * @exception	ProcessingException
     */     
	public void addToDisputes( AssignDisputesToCustomerCommand command ) throws ProcessingException {
		
		
		// -------------------------------------------
		// load the parent
		// -------------------------------------------
		load( command.getCustomerId() );

		DisputeService childDelegate 	= DisputeService.getDisputeInstance();
		CustomerService parentDelegate = CustomerService.getCustomerInstance();		
		UUID childId = command.getAddTo().getDisputeId();
		
		try {		
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );			
		}
		catch( Exception exc ) {
			final String msg = "Failed to add a Dispute as Disputes to Customer" ; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}

	}

    /**
     * remove Dispute from Disputes
     * @param		command RemoveDisputesFromCustomerCommand
     * @exception	ProcessingException
     */     	
	public void removeFromDisputes( RemoveDisputesFromCustomerCommand command ) throws ProcessingException {		
		
		DisputeService childDelegate 	= DisputeService.getDisputeInstance();
		UUID childId = command.getRemoveFrom().getDisputeId();

		try {
			
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );

		}
		catch( Exception exc ) {
			final String msg = "Failed to remove child using Id " + childId; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}

    /**
     * add KycProfile to KycProfiles 
     * @param		command AssignKycProfilesToCustomerCommand
     * @exception	ProcessingException
     */     
	public void addToKycProfiles( AssignKycProfilesToCustomerCommand command ) throws ProcessingException {
		
		
		// -------------------------------------------
		// load the parent
		// -------------------------------------------
		load( command.getCustomerId() );

		KycProfileService childDelegate 	= KycProfileService.getKycProfileInstance();
		CustomerService parentDelegate = CustomerService.getCustomerInstance();		
		UUID childId = command.getAddTo().getKycProfileId();
		
		try {		
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );			
		}
		catch( Exception exc ) {
			final String msg = "Failed to add a KycProfile as KycProfiles to Customer" ; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}

	}

    /**
     * remove KycProfile from KycProfiles
     * @param		command RemoveKycProfilesFromCustomerCommand
     * @exception	ProcessingException
     */     	
	public void removeFromKycProfiles( RemoveKycProfilesFromCustomerCommand command ) throws ProcessingException {		
		
		KycProfileService childDelegate 	= KycProfileService.getKycProfileInstance();
		UUID childId = command.getRemoveFrom().getKycProfileId();

		try {
			
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );

		}
		catch( Exception exc ) {
			final String msg = "Failed to remove child using Id " + childId; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}

    /**
     * add Consent to Consents 
     * @param		command AssignConsentsToCustomerCommand
     * @exception	ProcessingException
     */     
	public void addToConsents( AssignConsentsToCustomerCommand command ) throws ProcessingException {
		
		
		// -------------------------------------------
		// load the parent
		// -------------------------------------------
		load( command.getCustomerId() );

		ConsentService childDelegate 	= ConsentService.getConsentInstance();
		CustomerService parentDelegate = CustomerService.getCustomerInstance();		
		UUID childId = command.getAddTo().getConsentId();
		
		try {		
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );			
		}
		catch( Exception exc ) {
			final String msg = "Failed to add a Consent as Consents to Customer" ; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}

	}

    /**
     * remove Consent from Consents
     * @param		command RemoveConsentsFromCustomerCommand
     * @exception	ProcessingException
     */     	
	public void removeFromConsents( RemoveConsentsFromCustomerCommand command ) throws ProcessingException {		
		
		ConsentService childDelegate 	= ConsentService.getConsentInstance();
		UUID childId = command.getRemoveFrom().getConsentId();

		try {
			
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );

		}
		catch( Exception exc ) {
			final String msg = "Failed to remove child using Id " + childId; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}



	/**
	 * Internal helper method to load the root 
	 * 
	 * @param		id	UUID
	 * @return		Customer
	 */
	protected Customer load( UUID id ) throws ProcessingException {
		customer = CustomerService.getCustomerInstance().getCustomer( new CustomerFetchOneSummary(id) );	
		return customer;
	}


//************************************************************************
// Attributes
//************************************************************************
	private QueryGateway queryGateway;
	private CommandGateway commandGateway;
	private QueryUpdateEmitter queryUpdateEmitter;
	private CustomerValidator validator;
	private Customer customer 	= null;
    private static final Logger LOGGER 			= Logger.getLogger(CustomerService.class.getName());
    
}
