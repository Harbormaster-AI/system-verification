/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.globalbank.service;

import java.io.IOException;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.*;
import java.util.concurrent.*;

import org.axonframework.commandhandling.gateway.CommandGateway;
import org.axonframework.queryhandling.QueryGateway;
import org.axonframework.queryhandling.QueryUpdateEmitter;
import org.axonframework.messaging.responsetypes.ResponseTypes;
import org.springframework.stereotype.Service;
import org.springframework.beans.BeansException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;

import com.globalbank.api.*;
import com.globalbank.entity.*;
import com.globalbank.exception.*;
import com.globalbank.validator.*;

/**
 *
 * <h2>Account business service</h2>
 *
 * <p>
 * This class implements the Service design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of Account related services in the case of a Account business related service failing.</li>
 * <li>Exposes a simpler, uniform Account interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill Account business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/12/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Banking Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core banking, payments, lending, cards, KYC, and FX capabilities.</td></tr>
 *          <tr><td>industry</td><td>Banking</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author your_name_here</p>
 *
 */
@Service
public class AccountService 
extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
    /** 
     * Default Constructor 
     */
    public AccountService()  {
	}

	@Override
	public void setApplicationContext(ApplicationContext ctx) throws BeansException {
		super.setApplicationContext(ctx);

		queryGateway 		= ctx.getBean(QueryGateway.class);
		commandGateway 		= ctx.getBean(CommandGateway.class);
		queryUpdateEmitter  = ctx.getBean(QueryUpdateEmitter.class);
		validator			= ctx.getBean(AccountValidator.class);
	}


   /**
	* Account Business Delegate Factory Method
	*
	* All methods are expected to be self-sufficient.
	*
	* @return 	AccountService
	*/
	public static AccountService getAccountInstance() {
		return( new AccountService() );
	}
 
   /**
    * Creates the provided command.
    * 
    * @param		command ${class.getCreateCommandAlias()}
    * @exception    ProcessingException
    * @exception	IllegalArgumentException
    * @return		CompletableFuture<UUID>
    */
	public CompletableFuture<UUID> createAccount( CreateAccountCommand command )
    throws ProcessingException, IllegalArgumentException {

		CompletableFuture<UUID> completableFuture = null;
				
		try {
			// --------------------------------------
        	// assign identity now if none
        	// -------------------------------------- 
			if ( command.getAccountId() == null )
				command.setAccountId( UUID.randomUUID() );
				
			// --------------------------------------
        	// validate the command
        	// --------------------------------------    	
        	validator.validate( command );    

    		// ---------------------------------------
    		// issue the CreateAccountCommand - by convention the future return value for a create command
        	// that is handled by the constructor of an aggregate will return the UUID 
    		// ---------------------------------------
        	completableFuture = commandGateway.send( command );
        	
			LOGGER.log( Level.INFO, "return from Command Gateway for CreateAccountCommand of Account is " + command );
			
        }
        catch (Exception exc) {
            final String errMsg = "Unable to create Account - " + exc;
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }        
        
        return completableFuture;
    }

   /**
    * Update the provided command.
    * @param		command UpdateAccountCommand
    * @return		CompletableFuture<Void>
    * @exception    ProcessingException
    * @exception  	IllegalArgumentException
    */
    public CompletableFuture<Void> updateAccount( UpdateAccountCommand command ) 
    throws ProcessingException, IllegalArgumentException {
    	CompletableFuture<Void> completableFuture = null;
    	
    	try {       

			// --------------------------------------
        	// validate 
        	// --------------------------------------    	
        	validator.validate( command );    

        	// --------------------------------------
        	// issue the UpdateAccountCommand and return right away
        	// --------------------------------------    	
        	completableFuture = commandGateway.send( command );
    	}
        catch (Exception exc) {
            final String errMsg = "Unable to save Account - " + exc;
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        
    	return completableFuture;
    }
   
   /**
    * Deletes the associatied value object
    * @param		command DeleteAccountCommand
    * @return		CompletableFuture<Void>
    * @exception 	ProcessingException
    */
    public CompletableFuture<Void> delete( DeleteAccountCommand command ) 
    throws ProcessingException, IllegalArgumentException {	
    	
    	CompletableFuture<Void> completableFuture = null;
    	
        try {  
			// --------------------------------------
        	// validate the command
        	// --------------------------------------    	
        	validator.validate( command );    
        	
        	// --------------------------------------
        	// issue the DeleteAccountCommand and return right away
        	// --------------------------------------    	
        	completableFuture = commandGateway.send( command );
        }
        catch (Exception exc) {
            final String errMsg = "Unable to delete Account using Id = "  + command.getAccountId();
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }
        
        return completableFuture;
    }

    /**
     * Method to retrieve the Account via AccountFetchOneSummary
     * @param 	summary AccountFetchOneSummary 
     * @return 	AccountFetchOneResponse
     * @exception ProcessingException - Thrown if processing any related problems
     * @exception IllegalArgumentException 
     */
    public Account getAccount( AccountFetchOneSummary summary ) 
    throws ProcessingException, IllegalArgumentException {
    	
    	if( summary == null )
    		throw new IllegalArgumentException( "AccountFetchOneSummary arg cannot be null" );
    	
    	Account entity = null;
    	
        try {
        	// --------------------------------------
        	// validate the fetch one summary
        	// --------------------------------------    	
        	validator.validate( summary );    
        	
        	// --------------------------------------
        	// use queryGateway to send request to Find a Account
        	// --------------------------------------
        	CompletableFuture<Account> futureEntity = queryGateway.query(new FindAccountQuery( new LoadAccountFilter( summary.getAccountId() ) ), ResponseTypes.instanceOf(Account.class));
        	
        	entity = futureEntity.get();
        }
        catch( Exception exc ) {
            final String errMsg = "Unable to locate Account with id " + summary.getAccountId();
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }        
        
        return entity;
    }


    /**
     * Method to retrieve a collection of all Accounts
     *
     * @return 	List<Account> 
     * @exception ProcessingException Thrown if any problems
     */
    public List<Account> getAllAccount() 
    throws ProcessingException {
        List<Account> list = null;

        try {
        	CompletableFuture<List<Account>> futureList = queryGateway.query(new FindAllAccountQuery(), ResponseTypes.multipleInstancesOf(Account.class));
        	
        	list = futureList.get();
        }
        catch( Exception exc ) {
            String errMsg = "Failed to get all Account";
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }        
        
        return list;
    }

    /**
     * assign Bank on Account
     * @param		command AssignBankToAccountCommand	
     * @exception	ProcessingException
     */     
	public void assignBank( AssignBankToAccountCommand command ) throws ProcessingException {

		// --------------------------------------------
		// load the parent
		// --------------------------------------------
		load( command.getAccountId() );
		
		BankService childDelegate 	= BankService.getBankInstance();
		AccountService parentDelegate = AccountService.getAccountInstance();			
		UUID childId = command.getAssignment().getBankId();
		Bank child = null;
		
		try {
			// --------------------------------------
	    	// best to validate the command now
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );

		}
        catch( Throwable exc ) {
			final String msg = "Failed to get Bank using id " + childId;
			LOGGER.log( Level.WARNING,  msg );
			throw new ProcessingException( msg, exc );
        }
	}

    /**
     * unAssign Bank on Account
     * @param		command UnAssignBankFromAccountCommand
     * @exception	ProcessingException
     */     
	public void unAssignBank( UnAssignBankFromAccountCommand command ) throws ProcessingException {

		try {
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    
	
	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );
		}
		catch( Exception exc ) {
			final String msg = "Failed to unassign Bank on Account";
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}
	
    /**
     * assign Branch on Account
     * @param		command AssignBranchToAccountCommand	
     * @exception	ProcessingException
     */     
	public void assignBranch( AssignBranchToAccountCommand command ) throws ProcessingException {

		// --------------------------------------------
		// load the parent
		// --------------------------------------------
		load( command.getAccountId() );
		
		BranchService childDelegate 	= BranchService.getBranchInstance();
		AccountService parentDelegate = AccountService.getAccountInstance();			
		UUID childId = command.getAssignment().getBranchId();
		Branch child = null;
		
		try {
			// --------------------------------------
	    	// best to validate the command now
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );

		}
        catch( Throwable exc ) {
			final String msg = "Failed to get Branch using id " + childId;
			LOGGER.log( Level.WARNING,  msg );
			throw new ProcessingException( msg, exc );
        }
	}

    /**
     * unAssign Branch on Account
     * @param		command UnAssignBranchFromAccountCommand
     * @exception	ProcessingException
     */     
	public void unAssignBranch( UnAssignBranchFromAccountCommand command ) throws ProcessingException {

		try {
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    
	
	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );
		}
		catch( Exception exc ) {
			final String msg = "Failed to unassign Branch on Account";
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}
	
    /**
     * assign Product on Account
     * @param		command AssignProductToAccountCommand	
     * @exception	ProcessingException
     */     
	public void assignProduct( AssignProductToAccountCommand command ) throws ProcessingException {

		// --------------------------------------------
		// load the parent
		// --------------------------------------------
		load( command.getAccountId() );
		
		BankingProductService childDelegate 	= BankingProductService.getBankingProductInstance();
		AccountService parentDelegate = AccountService.getAccountInstance();			
		UUID childId = command.getAssignment().getBankingProductId();
		BankingProduct child = null;
		
		try {
			// --------------------------------------
	    	// best to validate the command now
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );

		}
        catch( Throwable exc ) {
			final String msg = "Failed to get BankingProduct using id " + childId;
			LOGGER.log( Level.WARNING,  msg );
			throw new ProcessingException( msg, exc );
        }
	}

    /**
     * unAssign Product on Account
     * @param		command UnAssignProductFromAccountCommand
     * @exception	ProcessingException
     */     
	public void unAssignProduct( UnAssignProductFromAccountCommand command ) throws ProcessingException {

		try {
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    
	
	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );
		}
		catch( Exception exc ) {
			final String msg = "Failed to unassign Product on Account";
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}
	

    /**
     * add Customer to Owners 
     * @param		command AssignOwnersToAccountCommand
     * @exception	ProcessingException
     */     
	public void addToOwners( AssignOwnersToAccountCommand command ) throws ProcessingException {
		
		
		// -------------------------------------------
		// load the parent
		// -------------------------------------------
		load( command.getAccountId() );

		CustomerService childDelegate 	= CustomerService.getCustomerInstance();
		AccountService parentDelegate = AccountService.getAccountInstance();		
		UUID childId = command.getAddTo().getCustomerId();
		
		try {		
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );			
		}
		catch( Exception exc ) {
			final String msg = "Failed to add a Customer as Owners to Account" ; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}

	}

    /**
     * remove Customer from Owners
     * @param		command RemoveOwnersFromAccountCommand
     * @exception	ProcessingException
     */     	
	public void removeFromOwners( RemoveOwnersFromAccountCommand command ) throws ProcessingException {		
		
		CustomerService childDelegate 	= CustomerService.getCustomerInstance();
		UUID childId = command.getRemoveFrom().getCustomerId();

		try {
			
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );

		}
		catch( Exception exc ) {
			final String msg = "Failed to remove child using Id " + childId; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}

    /**
     * add Transaction to Transactions 
     * @param		command AssignTransactionsToAccountCommand
     * @exception	ProcessingException
     */     
	public void addToTransactions( AssignTransactionsToAccountCommand command ) throws ProcessingException {
		
		
		// -------------------------------------------
		// load the parent
		// -------------------------------------------
		load( command.getAccountId() );

		TransactionService childDelegate 	= TransactionService.getTransactionInstance();
		AccountService parentDelegate = AccountService.getAccountInstance();		
		UUID childId = command.getAddTo().getTransactionId();
		
		try {		
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );			
		}
		catch( Exception exc ) {
			final String msg = "Failed to add a Transaction as Transactions to Account" ; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}

	}

    /**
     * remove Transaction from Transactions
     * @param		command RemoveTransactionsFromAccountCommand
     * @exception	ProcessingException
     */     	
	public void removeFromTransactions( RemoveTransactionsFromAccountCommand command ) throws ProcessingException {		
		
		TransactionService childDelegate 	= TransactionService.getTransactionInstance();
		UUID childId = command.getRemoveFrom().getTransactionId();

		try {
			
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );

		}
		catch( Exception exc ) {
			final String msg = "Failed to remove child using Id " + childId; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}

    /**
     * add AccountStatement to Statements 
     * @param		command AssignStatementsToAccountCommand
     * @exception	ProcessingException
     */     
	public void addToStatements( AssignStatementsToAccountCommand command ) throws ProcessingException {
		
		
		// -------------------------------------------
		// load the parent
		// -------------------------------------------
		load( command.getAccountId() );

		AccountStatementService childDelegate 	= AccountStatementService.getAccountStatementInstance();
		AccountService parentDelegate = AccountService.getAccountInstance();		
		UUID childId = command.getAddTo().getAccountStatementId();
		
		try {		
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );			
		}
		catch( Exception exc ) {
			final String msg = "Failed to add a AccountStatement as Statements to Account" ; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}

	}

    /**
     * remove AccountStatement from Statements
     * @param		command RemoveStatementsFromAccountCommand
     * @exception	ProcessingException
     */     	
	public void removeFromStatements( RemoveStatementsFromAccountCommand command ) throws ProcessingException {		
		
		AccountStatementService childDelegate 	= AccountStatementService.getAccountStatementInstance();
		UUID childId = command.getRemoveFrom().getAccountStatementId();

		try {
			
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );

		}
		catch( Exception exc ) {
			final String msg = "Failed to remove child using Id " + childId; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}

    /**
     * add StandingInstruction to StandingInstructions 
     * @param		command AssignStandingInstructionsToAccountCommand
     * @exception	ProcessingException
     */     
	public void addToStandingInstructions( AssignStandingInstructionsToAccountCommand command ) throws ProcessingException {
		
		
		// -------------------------------------------
		// load the parent
		// -------------------------------------------
		load( command.getAccountId() );

		StandingInstructionService childDelegate 	= StandingInstructionService.getStandingInstructionInstance();
		AccountService parentDelegate = AccountService.getAccountInstance();		
		UUID childId = command.getAddTo().getStandingInstructionId();
		
		try {		
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );			
		}
		catch( Exception exc ) {
			final String msg = "Failed to add a StandingInstruction as StandingInstructions to Account" ; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}

	}

    /**
     * remove StandingInstruction from StandingInstructions
     * @param		command RemoveStandingInstructionsFromAccountCommand
     * @exception	ProcessingException
     */     	
	public void removeFromStandingInstructions( RemoveStandingInstructionsFromAccountCommand command ) throws ProcessingException {		
		
		StandingInstructionService childDelegate 	= StandingInstructionService.getStandingInstructionInstance();
		UUID childId = command.getRemoveFrom().getStandingInstructionId();

		try {
			
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );

		}
		catch( Exception exc ) {
			final String msg = "Failed to remove child using Id " + childId; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}

    /**
     * add FeeCharge to FeeCharges 
     * @param		command AssignFeeChargesToAccountCommand
     * @exception	ProcessingException
     */     
	public void addToFeeCharges( AssignFeeChargesToAccountCommand command ) throws ProcessingException {
		
		
		// -------------------------------------------
		// load the parent
		// -------------------------------------------
		load( command.getAccountId() );

		FeeChargeService childDelegate 	= FeeChargeService.getFeeChargeInstance();
		AccountService parentDelegate = AccountService.getAccountInstance();		
		UUID childId = command.getAddTo().getFeeChargeId();
		
		try {		
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );			
		}
		catch( Exception exc ) {
			final String msg = "Failed to add a FeeCharge as FeeCharges to Account" ; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}

	}

    /**
     * remove FeeCharge from FeeCharges
     * @param		command RemoveFeeChargesFromAccountCommand
     * @exception	ProcessingException
     */     	
	public void removeFromFeeCharges( RemoveFeeChargesFromAccountCommand command ) throws ProcessingException {		
		
		FeeChargeService childDelegate 	= FeeChargeService.getFeeChargeInstance();
		UUID childId = command.getRemoveFrom().getFeeChargeId();

		try {
			
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );

		}
		catch( Exception exc ) {
			final String msg = "Failed to remove child using Id " + childId; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}



	/**
	 * Internal helper method to load the root 
	 * 
	 * @param		id	UUID
	 * @return		Account
	 */
	protected Account load( UUID id ) throws ProcessingException {
		account = AccountService.getAccountInstance().getAccount( new AccountFetchOneSummary(id) );	
		return account;
	}


//************************************************************************
// Attributes
//************************************************************************
	private QueryGateway queryGateway;
	private CommandGateway commandGateway;
	private QueryUpdateEmitter queryUpdateEmitter;
	private AccountValidator validator;
	private Account account 	= null;
    private static final Logger LOGGER 			= Logger.getLogger(AccountService.class.getName());
    
}
