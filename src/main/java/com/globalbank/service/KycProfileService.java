/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.globalbank.service;

import java.io.IOException;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.*;
import java.util.concurrent.*;

import org.axonframework.commandhandling.gateway.CommandGateway;
import org.axonframework.queryhandling.QueryGateway;
import org.axonframework.queryhandling.QueryUpdateEmitter;
import org.axonframework.messaging.responsetypes.ResponseTypes;
import org.springframework.stereotype.Service;
import org.springframework.beans.BeansException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;

import com.globalbank.api.*;
import com.globalbank.entity.*;
import com.globalbank.exception.*;
import com.globalbank.validator.*;

/**
 *
 * <h2>KycProfile business service</h2>
 *
 * <p>
 * This class implements the Service design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of KycProfile related services in the case of a KycProfile business related service failing.</li>
 * <li>Exposes a simpler, uniform KycProfile interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill KycProfile business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/12/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Banking Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core banking, payments, lending, cards, KYC, and FX capabilities.</td></tr>
 *          <tr><td>industry</td><td>Banking</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author your_name_here</p>
 *
 */
@Service
public class KycProfileService 
extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
    /** 
     * Default Constructor 
     */
    public KycProfileService()  {
	}

	@Override
	public void setApplicationContext(ApplicationContext ctx) throws BeansException {
		super.setApplicationContext(ctx);

		queryGateway 		= ctx.getBean(QueryGateway.class);
		commandGateway 		= ctx.getBean(CommandGateway.class);
		queryUpdateEmitter  = ctx.getBean(QueryUpdateEmitter.class);
		validator			= ctx.getBean(KycProfileValidator.class);
	}


   /**
	* KycProfile Business Delegate Factory Method
	*
	* All methods are expected to be self-sufficient.
	*
	* @return 	KycProfileService
	*/
	public static KycProfileService getKycProfileInstance() {
		return( new KycProfileService() );
	}
 
   /**
    * Creates the provided command.
    * 
    * @param		command ${class.getCreateCommandAlias()}
    * @exception    ProcessingException
    * @exception	IllegalArgumentException
    * @return		CompletableFuture<UUID>
    */
	public CompletableFuture<UUID> createKycProfile( CreateKycProfileCommand command )
    throws ProcessingException, IllegalArgumentException {

		CompletableFuture<UUID> completableFuture = null;
				
		try {
			// --------------------------------------
        	// assign identity now if none
        	// -------------------------------------- 
			if ( command.getKycProfileId() == null )
				command.setKycProfileId( UUID.randomUUID() );
				
			// --------------------------------------
        	// validate the command
        	// --------------------------------------    	
        	validator.validate( command );    

    		// ---------------------------------------
    		// issue the CreateKycProfileCommand - by convention the future return value for a create command
        	// that is handled by the constructor of an aggregate will return the UUID 
    		// ---------------------------------------
        	completableFuture = commandGateway.send( command );
        	
			LOGGER.log( Level.INFO, "return from Command Gateway for CreateKycProfileCommand of KycProfile is " + command );
			
        }
        catch (Exception exc) {
            final String errMsg = "Unable to create KycProfile - " + exc;
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }        
        
        return completableFuture;
    }

   /**
    * Update the provided command.
    * @param		command UpdateKycProfileCommand
    * @return		CompletableFuture<Void>
    * @exception    ProcessingException
    * @exception  	IllegalArgumentException
    */
    public CompletableFuture<Void> updateKycProfile( UpdateKycProfileCommand command ) 
    throws ProcessingException, IllegalArgumentException {
    	CompletableFuture<Void> completableFuture = null;
    	
    	try {       

			// --------------------------------------
        	// validate 
        	// --------------------------------------    	
        	validator.validate( command );    

        	// --------------------------------------
        	// issue the UpdateKycProfileCommand and return right away
        	// --------------------------------------    	
        	completableFuture = commandGateway.send( command );
    	}
        catch (Exception exc) {
            final String errMsg = "Unable to save KycProfile - " + exc;
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        
    	return completableFuture;
    }
   
   /**
    * Deletes the associatied value object
    * @param		command DeleteKycProfileCommand
    * @return		CompletableFuture<Void>
    * @exception 	ProcessingException
    */
    public CompletableFuture<Void> delete( DeleteKycProfileCommand command ) 
    throws ProcessingException, IllegalArgumentException {	
    	
    	CompletableFuture<Void> completableFuture = null;
    	
        try {  
			// --------------------------------------
        	// validate the command
        	// --------------------------------------    	
        	validator.validate( command );    
        	
        	// --------------------------------------
        	// issue the DeleteKycProfileCommand and return right away
        	// --------------------------------------    	
        	completableFuture = commandGateway.send( command );
        }
        catch (Exception exc) {
            final String errMsg = "Unable to delete KycProfile using Id = "  + command.getKycProfileId();
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }
        
        return completableFuture;
    }

    /**
     * Method to retrieve the KycProfile via KycProfileFetchOneSummary
     * @param 	summary KycProfileFetchOneSummary 
     * @return 	KycProfileFetchOneResponse
     * @exception ProcessingException - Thrown if processing any related problems
     * @exception IllegalArgumentException 
     */
    public KycProfile getKycProfile( KycProfileFetchOneSummary summary ) 
    throws ProcessingException, IllegalArgumentException {
    	
    	if( summary == null )
    		throw new IllegalArgumentException( "KycProfileFetchOneSummary arg cannot be null" );
    	
    	KycProfile entity = null;
    	
        try {
        	// --------------------------------------
        	// validate the fetch one summary
        	// --------------------------------------    	
        	validator.validate( summary );    
        	
        	// --------------------------------------
        	// use queryGateway to send request to Find a KycProfile
        	// --------------------------------------
        	CompletableFuture<KycProfile> futureEntity = queryGateway.query(new FindKycProfileQuery( new LoadKycProfileFilter( summary.getKycProfileId() ) ), ResponseTypes.instanceOf(KycProfile.class));
        	
        	entity = futureEntity.get();
        }
        catch( Exception exc ) {
            final String errMsg = "Unable to locate KycProfile with id " + summary.getKycProfileId();
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }        
        
        return entity;
    }


    /**
     * Method to retrieve a collection of all KycProfiles
     *
     * @return 	List<KycProfile> 
     * @exception ProcessingException Thrown if any problems
     */
    public List<KycProfile> getAllKycProfile() 
    throws ProcessingException {
        List<KycProfile> list = null;

        try {
        	CompletableFuture<List<KycProfile>> futureList = queryGateway.query(new FindAllKycProfileQuery(), ResponseTypes.multipleInstancesOf(KycProfile.class));
        	
        	list = futureList.get();
        }
        catch( Exception exc ) {
            String errMsg = "Failed to get all KycProfile";
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }        
        
        return list;
    }

    /**
     * assign Customer on KycProfile
     * @param		command AssignCustomerToKycProfileCommand	
     * @exception	ProcessingException
     */     
	public void assignCustomer( AssignCustomerToKycProfileCommand command ) throws ProcessingException {

		// --------------------------------------------
		// load the parent
		// --------------------------------------------
		load( command.getKycProfileId() );
		
		CustomerService childDelegate 	= CustomerService.getCustomerInstance();
		KycProfileService parentDelegate = KycProfileService.getKycProfileInstance();			
		UUID childId = command.getAssignment().getCustomerId();
		Customer child = null;
		
		try {
			// --------------------------------------
	    	// best to validate the command now
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );

		}
        catch( Throwable exc ) {
			final String msg = "Failed to get Customer using id " + childId;
			LOGGER.log( Level.WARNING,  msg );
			throw new ProcessingException( msg, exc );
        }
	}

    /**
     * unAssign Customer on KycProfile
     * @param		command UnAssignCustomerFromKycProfileCommand
     * @exception	ProcessingException
     */     
	public void unAssignCustomer( UnAssignCustomerFromKycProfileCommand command ) throws ProcessingException {

		try {
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    
	
	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );
		}
		catch( Exception exc ) {
			final String msg = "Failed to unassign Customer on KycProfile";
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}
	

    /**
     * add IdentityDocument to IdentityDocuments 
     * @param		command AssignIdentityDocumentsToKycProfileCommand
     * @exception	ProcessingException
     */     
	public void addToIdentityDocuments( AssignIdentityDocumentsToKycProfileCommand command ) throws ProcessingException {
		
		
		// -------------------------------------------
		// load the parent
		// -------------------------------------------
		load( command.getKycProfileId() );

		IdentityDocumentService childDelegate 	= IdentityDocumentService.getIdentityDocumentInstance();
		KycProfileService parentDelegate = KycProfileService.getKycProfileInstance();		
		UUID childId = command.getAddTo().getIdentityDocumentId();
		
		try {		
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );			
		}
		catch( Exception exc ) {
			final String msg = "Failed to add a IdentityDocument as IdentityDocuments to KycProfile" ; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}

	}

    /**
     * remove IdentityDocument from IdentityDocuments
     * @param		command RemoveIdentityDocumentsFromKycProfileCommand
     * @exception	ProcessingException
     */     	
	public void removeFromIdentityDocuments( RemoveIdentityDocumentsFromKycProfileCommand command ) throws ProcessingException {		
		
		IdentityDocumentService childDelegate 	= IdentityDocumentService.getIdentityDocumentInstance();
		UUID childId = command.getRemoveFrom().getIdentityDocumentId();

		try {
			
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );

		}
		catch( Exception exc ) {
			final String msg = "Failed to remove child using Id " + childId; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}

    /**
     * add RiskAssessment to RiskAssessments 
     * @param		command AssignRiskAssessmentsToKycProfileCommand
     * @exception	ProcessingException
     */     
	public void addToRiskAssessments( AssignRiskAssessmentsToKycProfileCommand command ) throws ProcessingException {
		
		
		// -------------------------------------------
		// load the parent
		// -------------------------------------------
		load( command.getKycProfileId() );

		RiskAssessmentService childDelegate 	= RiskAssessmentService.getRiskAssessmentInstance();
		KycProfileService parentDelegate = KycProfileService.getKycProfileInstance();		
		UUID childId = command.getAddTo().getRiskAssessmentId();
		
		try {		
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );			
		}
		catch( Exception exc ) {
			final String msg = "Failed to add a RiskAssessment as RiskAssessments to KycProfile" ; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}

	}

    /**
     * remove RiskAssessment from RiskAssessments
     * @param		command RemoveRiskAssessmentsFromKycProfileCommand
     * @exception	ProcessingException
     */     	
	public void removeFromRiskAssessments( RemoveRiskAssessmentsFromKycProfileCommand command ) throws ProcessingException {		
		
		RiskAssessmentService childDelegate 	= RiskAssessmentService.getRiskAssessmentInstance();
		UUID childId = command.getRemoveFrom().getRiskAssessmentId();

		try {
			
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );

		}
		catch( Exception exc ) {
			final String msg = "Failed to remove child using Id " + childId; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}

    /**
     * add ScreeningResult to Screenings 
     * @param		command AssignScreeningsToKycProfileCommand
     * @exception	ProcessingException
     */     
	public void addToScreenings( AssignScreeningsToKycProfileCommand command ) throws ProcessingException {
		
		
		// -------------------------------------------
		// load the parent
		// -------------------------------------------
		load( command.getKycProfileId() );

		ScreeningResultService childDelegate 	= ScreeningResultService.getScreeningResultInstance();
		KycProfileService parentDelegate = KycProfileService.getKycProfileInstance();		
		UUID childId = command.getAddTo().getScreeningResultId();
		
		try {		
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );			
		}
		catch( Exception exc ) {
			final String msg = "Failed to add a ScreeningResult as Screenings to KycProfile" ; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}

	}

    /**
     * remove ScreeningResult from Screenings
     * @param		command RemoveScreeningsFromKycProfileCommand
     * @exception	ProcessingException
     */     	
	public void removeFromScreenings( RemoveScreeningsFromKycProfileCommand command ) throws ProcessingException {		
		
		ScreeningResultService childDelegate 	= ScreeningResultService.getScreeningResultInstance();
		UUID childId = command.getRemoveFrom().getScreeningResultId();

		try {
			
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );

		}
		catch( Exception exc ) {
			final String msg = "Failed to remove child using Id " + childId; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}



	/**
	 * Internal helper method to load the root 
	 * 
	 * @param		id	UUID
	 * @return		KycProfile
	 */
	protected KycProfile load( UUID id ) throws ProcessingException {
		kycProfile = KycProfileService.getKycProfileInstance().getKycProfile( new KycProfileFetchOneSummary(id) );	
		return kycProfile;
	}


//************************************************************************
// Attributes
//************************************************************************
	private QueryGateway queryGateway;
	private CommandGateway commandGateway;
	private QueryUpdateEmitter queryUpdateEmitter;
	private KycProfileValidator validator;
	private KycProfile kycProfile 	= null;
    private static final Logger LOGGER 			= Logger.getLogger(KycProfileService.class.getName());
    
}
