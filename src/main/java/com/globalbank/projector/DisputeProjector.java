/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.globalbank.projector;

import java.util.*;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.stream.Collectors;

import org.axonframework.config.ProcessingGroup;
import org.axonframework.eventhandling.EventHandler;
import org.axonframework.queryhandling.QueryHandler;
import org.axonframework.queryhandling.QueryUpdateEmitter;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.domain.Pageable;
import org.springframework.data.domain.PageRequest;
import org.springframework.stereotype.Component;

import com.globalbank.api.*;
import com.globalbank.entity.*;
import com.globalbank.exception.*;
import com.globalbank.repository.*;

/**
 * <h2>DisputeProjector</h2>
 * <p>
 * The Dispute Projector is responsible for maintaining the read model for
 * Dispute entities. It subscribes to domain events published by the Dispute
 * Aggregate and updates query-optimized projections used by REST APIs,
 * dashboards, and reporting services.
 * </p>
 * <p>
 * A Projector contains no business logic and never validates commands or
 * modifies the authoritative state of the domain.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/12/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Banking Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core banking, payments, lending, cards, KYC, and FX capabilities.</td></tr>
 *          <tr><td>industry</td><td>Banking</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services Used</h3>
 *
 *      <ul><li>QueryGateway</li></ul>
 *
 * <h3>Queries Handled</h3>
 *		<ul>
 *      <li>FindDisputeQuery</li>
 *      <li>FindAllDisputeQuery</li>
 *		</ul>
 *
 * <p>@author your_name_here</p>
 */
//@ProcessingGroup("dispute")
@Component("dispute-projector")
public class DisputeProjector extends DisputeEntityProjector {
		
	// core constructor
	public DisputeProjector(ProjectorRegistry registry,
									DisputeRepository repository,
									QueryUpdateEmitter queryUpdateEmitter ) {
        super(registry, repository);
        this.queryUpdateEmitter = queryUpdateEmitter;
    }	

	/*
     * @param	event CreateDisputeEvent
     */
    @EventHandler( payloadType=CreateDisputeEvent.class )
    public void handle( CreateDisputeEvent event) {
	    LOGGER.info("handling CreateDisputeEvent - " + event );
	    Dispute entity = new Dispute();
            entity.setDisputeId( event.getDisputeId() );
            entity.setDisputeReference( event.getDisputeReference() );
            entity.setRaisedOn( event.getRaisedOn() );
            entity.setReason( event.getReason() );
            entity.setStatus( event.getStatus() );
	    
    	// ------------------------------------------
    	// persist a new one
    	// ------------------------------------------ 
	    create(entity);
        
        // ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllDispute( entity );
    }

    /*
     * @param	event UpdateDisputeEvent
     */
    @EventHandler( payloadType=UpdateDisputeEvent.class )
    public void handle( UpdateDisputeEvent event) {
    	LOGGER.info("handling UpdateDisputeEvent - " + event );
    	
	    Dispute entity = new Dispute();
            entity.setDisputeId( event.getDisputeId() );
            entity.setDisputeReference( event.getDisputeReference() );
            entity.setRaisedOn( event.getRaisedOn() );
            entity.setReason( event.getReason() );
            entity.setTransaction( event.getTransaction() );
            entity.setCustomer( event.getCustomer() );
            entity.setAccount( event.getAccount() );
            entity.setPaymentCard( event.getPaymentCard() );
            entity.setStatus( event.getStatus() );
 
    	// ------------------------------------------
    	// save with an existing instance
    	// ------------------------------------------ 
		update(entity);

    	// ------------------------------------------
    	// emit to subscribers that find one
    	// ------------------------------------------    	
        emitFindDispute( entity );

    	// ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllDispute( entity );        
    }
    
    /*
     * @param	event DeleteDisputeEvent
     */
    @EventHandler( payloadType=DeleteDisputeEvent.class )
    public void handle( DeleteDisputeEvent event) {
    	LOGGER.info("handling DeleteDisputeEvent - " + event );
    	
    	// ------------------------------------------
    	// delete delegation
    	// ------------------------------------------
    	Dispute entity = delete( event.getDisputeId() );

    	// ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllDispute( entity );

    }    

    /*
     * @param	event AssignTransactionToDisputeEvent
     */
    @EventHandler( payloadType=AssignTransactionToDisputeEvent.class)
    public void handle( AssignTransactionToDisputeEvent event) {
	    LOGGER.info("handling AssignTransactionToDisputeEvent - " + event );

	    // ------------------------------------------
	    // delegate to assignTo
	    // ------------------------------------------
	    Dispute entity = assignTransaction( event.getDisputeId(), event.getAssignment() );

	    // ------------------------------------------
    	// emit to subscribers that find one
    	// ------------------------------------------    	
        emitFindDispute( entity );

    	// ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllDispute( entity );
    }
    

	/*
	 * @param	event UnAssignTransactionFromDisputeEvent
	 */
	@EventHandler( payloadType=UnAssignTransactionFromDisputeEvent.class)
	public void handle( UnAssignTransactionFromDisputeEvent event) {
	    LOGGER.info("handling UnAssignTransactionFromDisputeEvent - " + event );

	    // ------------------------------------------
	    // delegate to unAssignFrom
	    // ------------------------------------------
	    Dispute entity = unAssignTransaction( event.getDisputeId() );

		// ------------------------------------------
		// emit to subscribers that find one
		// ------------------------------------------    	
	    emitFindDispute( entity );
	
		// ------------------------------------------
		// emit to subscribers that find all
		// ------------------------------------------    	
	    emitFindAllDispute( entity );
	}

    /*
     * @param	event AssignCustomerToDisputeEvent
     */
    @EventHandler( payloadType=AssignCustomerToDisputeEvent.class)
    public void handle( AssignCustomerToDisputeEvent event) {
	    LOGGER.info("handling AssignCustomerToDisputeEvent - " + event );

	    // ------------------------------------------
	    // delegate to assignTo
	    // ------------------------------------------
	    Dispute entity = assignCustomer( event.getDisputeId(), event.getAssignment() );

	    // ------------------------------------------
    	// emit to subscribers that find one
    	// ------------------------------------------    	
        emitFindDispute( entity );

    	// ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllDispute( entity );
    }
    

	/*
	 * @param	event UnAssignCustomerFromDisputeEvent
	 */
	@EventHandler( payloadType=UnAssignCustomerFromDisputeEvent.class)
	public void handle( UnAssignCustomerFromDisputeEvent event) {
	    LOGGER.info("handling UnAssignCustomerFromDisputeEvent - " + event );

	    // ------------------------------------------
	    // delegate to unAssignFrom
	    // ------------------------------------------
	    Dispute entity = unAssignCustomer( event.getDisputeId() );

		// ------------------------------------------
		// emit to subscribers that find one
		// ------------------------------------------    	
	    emitFindDispute( entity );
	
		// ------------------------------------------
		// emit to subscribers that find all
		// ------------------------------------------    	
	    emitFindAllDispute( entity );
	}

    /*
     * @param	event AssignAccountToDisputeEvent
     */
    @EventHandler( payloadType=AssignAccountToDisputeEvent.class)
    public void handle( AssignAccountToDisputeEvent event) {
	    LOGGER.info("handling AssignAccountToDisputeEvent - " + event );

	    // ------------------------------------------
	    // delegate to assignTo
	    // ------------------------------------------
	    Dispute entity = assignAccount( event.getDisputeId(), event.getAssignment() );

	    // ------------------------------------------
    	// emit to subscribers that find one
    	// ------------------------------------------    	
        emitFindDispute( entity );

    	// ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllDispute( entity );
    }
    

	/*
	 * @param	event UnAssignAccountFromDisputeEvent
	 */
	@EventHandler( payloadType=UnAssignAccountFromDisputeEvent.class)
	public void handle( UnAssignAccountFromDisputeEvent event) {
	    LOGGER.info("handling UnAssignAccountFromDisputeEvent - " + event );

	    // ------------------------------------------
	    // delegate to unAssignFrom
	    // ------------------------------------------
	    Dispute entity = unAssignAccount( event.getDisputeId() );

		// ------------------------------------------
		// emit to subscribers that find one
		// ------------------------------------------    	
	    emitFindDispute( entity );
	
		// ------------------------------------------
		// emit to subscribers that find all
		// ------------------------------------------    	
	    emitFindAllDispute( entity );
	}

    /*
     * @param	event AssignPaymentCardToDisputeEvent
     */
    @EventHandler( payloadType=AssignPaymentCardToDisputeEvent.class)
    public void handle( AssignPaymentCardToDisputeEvent event) {
	    LOGGER.info("handling AssignPaymentCardToDisputeEvent - " + event );

	    // ------------------------------------------
	    // delegate to assignTo
	    // ------------------------------------------
	    Dispute entity = assignPaymentCard( event.getDisputeId(), event.getAssignment() );

	    // ------------------------------------------
    	// emit to subscribers that find one
    	// ------------------------------------------    	
        emitFindDispute( entity );

    	// ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllDispute( entity );
    }
    

	/*
	 * @param	event UnAssignPaymentCardFromDisputeEvent
	 */
	@EventHandler( payloadType=UnAssignPaymentCardFromDisputeEvent.class)
	public void handle( UnAssignPaymentCardFromDisputeEvent event) {
	    LOGGER.info("handling UnAssignPaymentCardFromDisputeEvent - " + event );

	    // ------------------------------------------
	    // delegate to unAssignFrom
	    // ------------------------------------------
	    Dispute entity = unAssignPaymentCard( event.getDisputeId() );

		// ------------------------------------------
		// emit to subscribers that find one
		// ------------------------------------------    	
	    emitFindDispute( entity );
	
		// ------------------------------------------
		// emit to subscribers that find all
		// ------------------------------------------    	
	    emitFindAllDispute( entity );
	}




    /**
     * Method to retrieve the Dispute via an DisputePrimaryKey.
     * @param 	id Long
     * @return 	Dispute
     * @exception ProcessingException - Thrown if processing any related problems
     * @exception IllegalArgumentException 
     */
    @SuppressWarnings("unused")
    @QueryHandler
    public Dispute handle( FindDisputeQuery query ) 
    throws ProcessingException, IllegalArgumentException {
    	return find( query.getFilter().getDisputeId() );
    }
    
    /**
     * Method to retrieve a collection of all Disputes
     *
     * @param	query	FindAllDisputeQuery 
     * @return 	List<Dispute> 
     * @exception ProcessingException Thrown if any problems
     */
    @SuppressWarnings("unused")
    @QueryHandler
    public List<Dispute> handle( FindAllDisputeQuery query ) throws ProcessingException {
    	return findAll( query );
    }



/**
 * emit to subscription queries of type FindDispute,
 * but only if the id matches
 *
 * @param		entity	Dispute
 */
	protected void emitFindDispute( Dispute entity ) {
		LOGGER.info("handling emitFindDispute" );
		
	    queryUpdateEmitter.emit(FindDisputeQuery.class,
	                            query -> query.getFilter().getDisputeId().equals(entity.getDisputeId()),
	                            entity);
	}
	
	/**
	 * unconditionally emit to subscription queries of type FindAllDispute
	 * 
	 * @param		entity	Dispute
	 */
	protected void emitFindAllDispute( Dispute entity ) {
		LOGGER.info("handling emitFindAllDispute" );
		
	    queryUpdateEmitter.emit(FindAllDisputeQuery.class,
	                            query -> true,
	                            entity);
	}

	//--------------------------------------------------
    // attributes
    // --------------------------------------------------
	@Autowired
	private final QueryUpdateEmitter queryUpdateEmitter;
    private static final Logger LOGGER 	= Logger.getLogger(DisputeProjector.class.getName());

}
