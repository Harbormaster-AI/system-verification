/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.globalbank.projector;

import java.util.*;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.stream.Collectors;

import org.axonframework.config.ProcessingGroup;
import org.axonframework.eventhandling.EventHandler;
import org.axonframework.queryhandling.QueryHandler;
import org.axonframework.queryhandling.QueryUpdateEmitter;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.domain.Pageable;
import org.springframework.data.domain.PageRequest;
import org.springframework.stereotype.Component;

import com.globalbank.api.*;
import com.globalbank.entity.*;
import com.globalbank.exception.*;
import com.globalbank.repository.*;

/**
 * <h2>FeeChargeProjector</h2>
 * <p>
 * The FeeCharge Projector is responsible for maintaining the read model for
 * FeeCharge entities. It subscribes to domain events published by the FeeCharge
 * Aggregate and updates query-optimized projections used by REST APIs,
 * dashboards, and reporting services.
 * </p>
 * <p>
 * A Projector contains no business logic and never validates commands or
 * modifies the authoritative state of the domain.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/12/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Banking Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core banking, payments, lending, cards, KYC, and FX capabilities.</td></tr>
 *          <tr><td>industry</td><td>Banking</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services Used</h3>
 *
 *      <ul><li>QueryGateway</li></ul>
 *
 * <h3>Queries Handled</h3>
 *		<ul>
 *      <li>FindFeeChargeQuery</li>
 *      <li>FindAllFeeChargeQuery</li>
 *		</ul>
 *
 * <p>@author your_name_here</p>
 */
//@ProcessingGroup("feeCharge")
@Component("feeCharge-projector")
public class FeeChargeProjector extends FeeChargeEntityProjector {
		
	// core constructor
	public FeeChargeProjector(ProjectorRegistry registry,
									FeeChargeRepository repository,
									QueryUpdateEmitter queryUpdateEmitter ) {
        super(registry, repository);
        this.queryUpdateEmitter = queryUpdateEmitter;
    }	

	/*
     * @param	event CreateFeeChargeEvent
     */
    @EventHandler( payloadType=CreateFeeChargeEvent.class )
    public void handle( CreateFeeChargeEvent event) {
	    LOGGER.info("handling CreateFeeChargeEvent - " + event );
	    FeeCharge entity = new FeeCharge();
            entity.setFeeChargeId( event.getFeeChargeId() );
            entity.setFeeCode( event.getFeeCode() );
            entity.setAmount( event.getAmount() );
            entity.setAppliedOn( event.getAppliedOn() );
            entity.setFeeType( event.getFeeType() );
	    
    	// ------------------------------------------
    	// persist a new one
    	// ------------------------------------------ 
	    create(entity);
        
        // ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllFeeCharge( entity );
    }

    /*
     * @param	event UpdateFeeChargeEvent
     */
    @EventHandler( payloadType=UpdateFeeChargeEvent.class )
    public void handle( UpdateFeeChargeEvent event) {
    	LOGGER.info("handling UpdateFeeChargeEvent - " + event );
    	
	    FeeCharge entity = new FeeCharge();
            entity.setFeeChargeId( event.getFeeChargeId() );
            entity.setFeeCode( event.getFeeCode() );
            entity.setAmount( event.getAmount() );
            entity.setAppliedOn( event.getAppliedOn() );
            entity.setAccount( event.getAccount() );
            entity.setLoanAccount( event.getLoanAccount() );
            entity.setFeeType( event.getFeeType() );
 
    	// ------------------------------------------
    	// save with an existing instance
    	// ------------------------------------------ 
		update(entity);

    	// ------------------------------------------
    	// emit to subscribers that find one
    	// ------------------------------------------    	
        emitFindFeeCharge( entity );

    	// ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllFeeCharge( entity );        
    }
    
    /*
     * @param	event DeleteFeeChargeEvent
     */
    @EventHandler( payloadType=DeleteFeeChargeEvent.class )
    public void handle( DeleteFeeChargeEvent event) {
    	LOGGER.info("handling DeleteFeeChargeEvent - " + event );
    	
    	// ------------------------------------------
    	// delete delegation
    	// ------------------------------------------
    	FeeCharge entity = delete( event.getFeeChargeId() );

    	// ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllFeeCharge( entity );

    }    

    /*
     * @param	event AssignAccountToFeeChargeEvent
     */
    @EventHandler( payloadType=AssignAccountToFeeChargeEvent.class)
    public void handle( AssignAccountToFeeChargeEvent event) {
	    LOGGER.info("handling AssignAccountToFeeChargeEvent - " + event );

	    // ------------------------------------------
	    // delegate to assignTo
	    // ------------------------------------------
	    FeeCharge entity = assignAccount( event.getFeeChargeId(), event.getAssignment() );

	    // ------------------------------------------
    	// emit to subscribers that find one
    	// ------------------------------------------    	
        emitFindFeeCharge( entity );

    	// ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllFeeCharge( entity );
    }
    

	/*
	 * @param	event UnAssignAccountFromFeeChargeEvent
	 */
	@EventHandler( payloadType=UnAssignAccountFromFeeChargeEvent.class)
	public void handle( UnAssignAccountFromFeeChargeEvent event) {
	    LOGGER.info("handling UnAssignAccountFromFeeChargeEvent - " + event );

	    // ------------------------------------------
	    // delegate to unAssignFrom
	    // ------------------------------------------
	    FeeCharge entity = unAssignAccount( event.getFeeChargeId() );

		// ------------------------------------------
		// emit to subscribers that find one
		// ------------------------------------------    	
	    emitFindFeeCharge( entity );
	
		// ------------------------------------------
		// emit to subscribers that find all
		// ------------------------------------------    	
	    emitFindAllFeeCharge( entity );
	}

    /*
     * @param	event AssignLoanAccountToFeeChargeEvent
     */
    @EventHandler( payloadType=AssignLoanAccountToFeeChargeEvent.class)
    public void handle( AssignLoanAccountToFeeChargeEvent event) {
	    LOGGER.info("handling AssignLoanAccountToFeeChargeEvent - " + event );

	    // ------------------------------------------
	    // delegate to assignTo
	    // ------------------------------------------
	    FeeCharge entity = assignLoanAccount( event.getFeeChargeId(), event.getAssignment() );

	    // ------------------------------------------
    	// emit to subscribers that find one
    	// ------------------------------------------    	
        emitFindFeeCharge( entity );

    	// ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllFeeCharge( entity );
    }
    

	/*
	 * @param	event UnAssignLoanAccountFromFeeChargeEvent
	 */
	@EventHandler( payloadType=UnAssignLoanAccountFromFeeChargeEvent.class)
	public void handle( UnAssignLoanAccountFromFeeChargeEvent event) {
	    LOGGER.info("handling UnAssignLoanAccountFromFeeChargeEvent - " + event );

	    // ------------------------------------------
	    // delegate to unAssignFrom
	    // ------------------------------------------
	    FeeCharge entity = unAssignLoanAccount( event.getFeeChargeId() );

		// ------------------------------------------
		// emit to subscribers that find one
		// ------------------------------------------    	
	    emitFindFeeCharge( entity );
	
		// ------------------------------------------
		// emit to subscribers that find all
		// ------------------------------------------    	
	    emitFindAllFeeCharge( entity );
	}




    /**
     * Method to retrieve the FeeCharge via an FeeChargePrimaryKey.
     * @param 	id Long
     * @return 	FeeCharge
     * @exception ProcessingException - Thrown if processing any related problems
     * @exception IllegalArgumentException 
     */
    @SuppressWarnings("unused")
    @QueryHandler
    public FeeCharge handle( FindFeeChargeQuery query ) 
    throws ProcessingException, IllegalArgumentException {
    	return find( query.getFilter().getFeeChargeId() );
    }
    
    /**
     * Method to retrieve a collection of all FeeCharges
     *
     * @param	query	FindAllFeeChargeQuery 
     * @return 	List<FeeCharge> 
     * @exception ProcessingException Thrown if any problems
     */
    @SuppressWarnings("unused")
    @QueryHandler
    public List<FeeCharge> handle( FindAllFeeChargeQuery query ) throws ProcessingException {
    	return findAll( query );
    }



/**
 * emit to subscription queries of type FindFeeCharge,
 * but only if the id matches
 *
 * @param		entity	FeeCharge
 */
	protected void emitFindFeeCharge( FeeCharge entity ) {
		LOGGER.info("handling emitFindFeeCharge" );
		
	    queryUpdateEmitter.emit(FindFeeChargeQuery.class,
	                            query -> query.getFilter().getFeeChargeId().equals(entity.getFeeChargeId()),
	                            entity);
	}
	
	/**
	 * unconditionally emit to subscription queries of type FindAllFeeCharge
	 * 
	 * @param		entity	FeeCharge
	 */
	protected void emitFindAllFeeCharge( FeeCharge entity ) {
		LOGGER.info("handling emitFindAllFeeCharge" );
		
	    queryUpdateEmitter.emit(FindAllFeeChargeQuery.class,
	                            query -> true,
	                            entity);
	}

	//--------------------------------------------------
    // attributes
    // --------------------------------------------------
	@Autowired
	private final QueryUpdateEmitter queryUpdateEmitter;
    private static final Logger LOGGER 	= Logger.getLogger(FeeChargeProjector.class.getName());

}
