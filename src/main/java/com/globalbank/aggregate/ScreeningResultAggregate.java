package com.globalbank.aggregate;

import com.globalbank.api.*;
import com.globalbank.entity.*;
import com.globalbank.exception.*;

import java.util.*;
import java.util.logging.Level;
import java.util.logging.Logger;

import org.axonframework.commandhandling.CommandHandler;
import org.axonframework.modelling.command.AggregateMember;
import org.axonframework.eventsourcing.EventSourcingHandler;
import org.axonframework.modelling.command.AggregateIdentifier;
import static org.axonframework.modelling.command.AggregateLifecycle.apply;
import org.axonframework.spring.stereotype.Aggregate;
import org.springframework.context.annotation.Profile;

/**
 * <h2>ScreeningResultAggregate</h2>
 * <p>
 * The ScreeningResult aggregate is the authoritative consistency boundary for all
 * branch-related operations. It validates incoming commands, enforces
 * business rules, maintains the integrity of the ScreeningResult domain, and emits
 * immutable domain events representing state changes.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/12/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Banking Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core banking, payments, lending, cards, KYC, and FX capabilities.</td></tr>
 *          <tr><td>industry</td><td>Banking</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Commands Accepted</h3>
 *      <ul>
 *      <li>CreateScreeningResultCommand</li>
 *      <li>UpdateScreeningResultCommand</li>
 *      <li>DeleteScreeningResultCommand</li>
 *      </ul>
 *
 * <h3>Associations</h3>
 * <ul>
 *       <li>AssignKycProfileToScreeningResultCommand</li>
 *       <li>UnAssignKycProfileFromScreeningResultCommand</li>
 *  </ul>
 *  <ul>
 *
 *
 * <h3>Events Emitted</h3>
 *      <ul>
 *      <li>CreateScreeningResultEvent</li>
 *      <li>UpdateScreeningResultEvent</li>
 *      <li>DeleteScreeningResultEvent</li>
   *       <li>AssignKycProfileToScreeningResultEvent</li>
 *       <li>UnAssignKycProfileFromScreeningResultEvent</li>
   *        </ul>
 *
 * <p>@author your_name_here</p>
 */
@Aggregate
public class ScreeningResultAggregate {  

	// -----------------------------------------
	// Axon requires an empty constructor
    // -----------------------------------------
    public ScreeningResultAggregate() {
    }

	// ----------------------------------------------
	// intrinsic command handlers
	// ----------------------------------------------
    @CommandHandler
    public ScreeningResultAggregate(CreateScreeningResultCommand command) throws Exception {
    	LOGGER.info( "Handling command CreateScreeningResultCommand" );
    	CreateScreeningResultEvent event = new CreateScreeningResultEvent(command.getScreeningResultId(), command.getScreeningDate(), command.getProvider(), command.getOutcome());
    	
        apply(event);
    }

    @CommandHandler
    public void handle(UpdateScreeningResultCommand command) throws Exception {
    	LOGGER.info( "handling command UpdateScreeningResultCommand" );
    	UpdateScreeningResultEvent event = new UpdateScreeningResultEvent(command.getScreeningResultId(), command.getScreeningDate(), command.getProvider(), command.getKycProfile(), command.getOutcome());        
    	
        apply(event);
    }

    @CommandHandler
    public void handle(DeleteScreeningResultCommand command) throws Exception {
    	LOGGER.info( "Handling command DeleteScreeningResultCommand" );
        apply(new DeleteScreeningResultEvent(command.getScreeningResultId()));
    }

	// ----------------------------------------------
	// association command handlers
	// ----------------------------------------------

    // single association commands
    @CommandHandler
    public void handle(AssignKycProfileToScreeningResultCommand command) throws Exception {
    	LOGGER.info( "Handling command AssignKycProfileToScreeningResultCommand" );
    	
    	if (  kycProfile != null && kycProfile.getKycProfileId() == command.getAssignment().getKycProfileId() )
    		throw new ProcessingException( "KycProfile already assigned with id " + command.getAssignment().getKycProfileId() );  
    		
        apply(new AssignKycProfileToScreeningResultEvent(command.getScreeningResultId(), command.getAssignment()));
    }

    @CommandHandler
    public void handle(UnAssignKycProfileFromScreeningResultCommand command) throws Exception {
    	LOGGER.info( "Handling command UnAssignKycProfileFromScreeningResultCommand" );

    	if (  kycProfile == null )
    		throw new ProcessingException( "KycProfile already has nothing assigned." );  

    	apply(new UnAssignKycProfileFromScreeningResultEvent(command.getScreeningResultId()));
    }

    // multiple association commands

	// ----------------------------------------------
	// intrinsic event source handlers
	// ----------------------------------------------
    @EventSourcingHandler
    void on(CreateScreeningResultEvent event) {	
    	LOGGER.info( "Event sourcing CreateScreeningResultEvent" );
    	this.screeningResultId = event.getScreeningResultId();
        this.screeningDate = event.getScreeningDate();
        this.provider = event.getProvider();
        this.outcome = event.getOutcome();
    }
    
    @EventSourcingHandler
    void on(UpdateScreeningResultEvent event) {
    	LOGGER.info( "Event sourcing classObject.getUpdateEventAlias()}" );
        this.screeningDate = event.getScreeningDate();
        this.provider = event.getProvider();
        this.kycProfile = event.getKycProfile();
        this.outcome = event.getOutcome();
    }   

	// ----------------------------------------------
	// association event source handlers
	// ----------------------------------------------
	// single associations
    @EventSourcingHandler
    void on(AssignKycProfileToScreeningResultEvent event ) {	
    	LOGGER.info( "Event sourcing AssignKycProfileToScreeningResultEvent" );
    	this.kycProfile = event.getAssignment();
    }

	@EventSourcingHandler
	void on(UnAssignKycProfileFromScreeningResultEvent event ) {	
		LOGGER.info( "Event sourcing UnAssignKycProfileFromScreeningResultEvent" );
		this.kycProfile = null;
	}


    // ------------------------------------------
    // attributes
    // ------------------------------------------
	
    @AggregateIdentifier
    private UUID screeningResultId;
    
    private  Date screeningDate;
    private String provider;
    private ScreeningOutcome outcome;
    private KycProfile kycProfile = null;

    private static final Logger LOGGER 	= Logger.getLogger(ScreeningResultAggregate.class.getName());
}
