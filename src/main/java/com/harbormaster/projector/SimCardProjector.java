/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.harbormaster.projector;

import java.util.*;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.stream.Collectors;

import org.axonframework.config.ProcessingGroup;
import org.axonframework.eventhandling.EventHandler;
import org.axonframework.queryhandling.QueryHandler;
import org.axonframework.queryhandling.QueryUpdateEmitter;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.domain.Pageable;
import org.springframework.data.domain.PageRequest;
import org.springframework.stereotype.Component;

import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.repository.*;

/**
 * <h2>SimCardProjector</h2>
 * <p>
 * The SimCard Projector is responsible for maintaining the read model for
 * SimCard entities. It subscribes to domain events published by the SimCard
 * Aggregate and updates query-optimized projections used by REST APIs,
 * dashboards, and reporting services.
 * </p>
 * <p>
 * A Projector contains no business logic and never validates commands or
 * modifies the authoritative state of the domain.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services Used</h3>
 *
 *      <ul><li>QueryGateway</li></ul>
 *
 * <h3>Queries Handled</h3>
 *		<ul>
 *      <li>FindSimCardQuery</li>
 *      <li>FindAllSimCardQuery</li>
 *		</ul>
 *
 * <p>@author Harbormaster Dev Team</p>
 */
//@ProcessingGroup("simCard")
@Component("simCard-projector")
public class SimCardProjector extends SimCardEntityProjector {
		
	// core constructor
	public SimCardProjector(ProjectorRegistry registry,
									SimCardRepository repository,
									QueryUpdateEmitter queryUpdateEmitter ) {
        super(registry, repository);
        this.queryUpdateEmitter = queryUpdateEmitter;
    }	

	/*
     * @param	event CreateSimCardEvent
     */
    @EventHandler( payloadType=CreateSimCardEvent.class )
    public void handle( CreateSimCardEvent event) {
	    LOGGER.info("handling CreateSimCardEvent - " + event );
	    SimCard entity = new SimCard();
            entity.setSimCardId( event.getSimCardId() );
            entity.setIccid( event.getIccid() );
            entity.setImsi( event.getImsi() );
            entity.setCarrier( event.getCarrier() );
            entity.setStatus( event.getStatus() );
	    
    	// ------------------------------------------
    	// persist a new one
    	// ------------------------------------------ 
	    create(entity);
        
        // ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllSimCard( entity );
    }

    /*
     * @param	event UpdateSimCardEvent
     */
    @EventHandler( payloadType=UpdateSimCardEvent.class )
    public void handle( UpdateSimCardEvent event) {
    	LOGGER.info("handling UpdateSimCardEvent - " + event );
    	
	    SimCard entity = new SimCard();
            entity.setSimCardId( event.getSimCardId() );
            entity.setIccid( event.getIccid() );
            entity.setImsi( event.getImsi() );
            entity.setCarrier( event.getCarrier() );
            entity.setNetworkProfiles( event.getNetworkProfiles() );
            entity.setTenant( event.getTenant() );
            entity.setConnectivityPlan( event.getConnectivityPlan() );
            entity.setStatus( event.getStatus() );
 
    	// ------------------------------------------
    	// save with an existing instance
    	// ------------------------------------------ 
		update(entity);

    	// ------------------------------------------
    	// emit to subscribers that find one
    	// ------------------------------------------    	
        emitFindSimCard( entity );

    	// ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllSimCard( entity );        
    }
    
    /*
     * @param	event DeleteSimCardEvent
     */
    @EventHandler( payloadType=DeleteSimCardEvent.class )
    public void handle( DeleteSimCardEvent event) {
    	LOGGER.info("handling DeleteSimCardEvent - " + event );
    	
    	// ------------------------------------------
    	// delete delegation
    	// ------------------------------------------
    	SimCard entity = delete( event.getSimCardId() );

    	// ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllSimCard( entity );

    }    

    /*
     * @param	event AssignTenantToSimCardEvent
     */
    @EventHandler( payloadType=AssignTenantToSimCardEvent.class)
    public void handle( AssignTenantToSimCardEvent event) {
	    LOGGER.info("handling AssignTenantToSimCardEvent - " + event );

	    // ------------------------------------------
	    // delegate to assignTo
	    // ------------------------------------------
	    SimCard entity = assignTenant( event.getChildId(), event.getAssignment() );

	    // ------------------------------------------
    	// emit to subscribers that find one
    	// ------------------------------------------    	
        emitFindSimCard( entity );

    	// ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllSimCard( entity );
    }
    

	/*
	 * @param	event UnAssignTenantFromSimCardEvent
	 */
	@EventHandler( payloadType=UnAssignTenantFromSimCardEvent.class)
	public void handle( UnAssignTenantFromSimCardEvent event) {
	    LOGGER.info("handling UnAssignTenantFromSimCardEvent - " + event );

	    // ------------------------------------------
	    // delegate to unAssignFrom
	    // ------------------------------------------
	    SimCard entity = unAssignTenant( event.getChildId() );

		// ------------------------------------------
		// emit to subscribers that find one
		// ------------------------------------------    	
	    emitFindSimCard( entity );
	
		// ------------------------------------------
		// emit to subscribers that find all
		// ------------------------------------------    	
	    emitFindAllSimCard( entity );
	}

    /*
     * @param	event AssignConnectivityPlanToSimCardEvent
     */
    @EventHandler( payloadType=AssignConnectivityPlanToSimCardEvent.class)
    public void handle( AssignConnectivityPlanToSimCardEvent event) {
	    LOGGER.info("handling AssignConnectivityPlanToSimCardEvent - " + event );

	    // ------------------------------------------
	    // delegate to assignTo
	    // ------------------------------------------
	    SimCard entity = assignConnectivityPlan( event.getChildId(), event.getAssignment() );

	    // ------------------------------------------
    	// emit to subscribers that find one
    	// ------------------------------------------    	
        emitFindSimCard( entity );

    	// ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllSimCard( entity );
    }
    

	/*
	 * @param	event UnAssignConnectivityPlanFromSimCardEvent
	 */
	@EventHandler( payloadType=UnAssignConnectivityPlanFromSimCardEvent.class)
	public void handle( UnAssignConnectivityPlanFromSimCardEvent event) {
	    LOGGER.info("handling UnAssignConnectivityPlanFromSimCardEvent - " + event );

	    // ------------------------------------------
	    // delegate to unAssignFrom
	    // ------------------------------------------
	    SimCard entity = unAssignConnectivityPlan( event.getChildId() );

		// ------------------------------------------
		// emit to subscribers that find one
		// ------------------------------------------    	
	    emitFindSimCard( entity );
	
		// ------------------------------------------
		// emit to subscribers that find all
		// ------------------------------------------    	
	    emitFindAllSimCard( entity );
	}


    /*
     * @param	event AssignNetworkProfilesToSimCardEvent
     */
    @EventHandler( payloadType=AssignNetworkProfilesToSimCardEvent.class)
    public void handle( AssignNetworkProfilesToSimCardEvent event) {
	    LOGGER.info("handling AssignNetworkProfilesToSimCardEvent - " + event );
	    
	    // ------------------------------------------
    	// delegate to addTo 
    	// ------------------------------------------ 
	    SimCard entity = addToNetworkProfiles(event.getParentId(), event.getChildIds() );
        
    	// ------------------------------------------
    	// emit to subscribers that find one
    	// ------------------------------------------    	
        emitFindSimCard( entity );

    	// ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllSimCard( entity );
    }
    

/*
 * @param	event RemoveNetworkProfilesFromSimCardEvent
 */
@EventHandler( payloadType=RemoveNetworkProfilesFromSimCardEvent.class)
public void handle( RemoveNetworkProfilesFromSimCardEvent event) {
    LOGGER.info("handling RemoveNetworkProfilesFromSimCardEvent - " + event );

    SimCard entity = removeFromNetworkProfiles(event.getParentId(), event.getChildIds() );
    
	// ------------------------------------------
	// emit to subscribers that find one
	// ------------------------------------------    	
    emitFindSimCard( entity );

	// ------------------------------------------
	// emit to subscribers that find all
	// ------------------------------------------    	
    emitFindAllSimCard( entity );
}



    /**
     * Method to retrieve the SimCard via an SimCardPrimaryKey.
     * @param 	id Long
     * @return 	SimCard
     * @exception ProcessingException - Thrown if processing any related problems
     * @exception IllegalArgumentException 
     */
    @SuppressWarnings("unused")
    @QueryHandler
    public SimCard handle( FindSimCardQuery query ) 
    throws ProcessingException, IllegalArgumentException {
    	return find( query.getFilter().getSimCardId() );
    }
    
    /**
     * Method to retrieve a collection of all SimCards
     *
     * @param	query	FindAllSimCardQuery 
     * @return 	List<SimCard> 
     * @exception ProcessingException Thrown if any problems
     */
    @SuppressWarnings("unused")
    @QueryHandler
    public List<SimCard> handle( FindAllSimCardQuery query ) throws ProcessingException {
    	return findAll( query );
    }



/**
 * emit to subscription queries of type FindSimCard,
 * but only if the id matches
 *
 * @param		entity	SimCard
 */
	protected void emitFindSimCard( SimCard entity ) {
		LOGGER.info("handling emitFindSimCard" );
		
	    queryUpdateEmitter.emit(FindSimCardQuery.class,
	                            query -> query.getFilter().getSimCardId().equals(entity.getSimCardId()),
	                            entity);
	}
	
	/**
	 * unconditionally emit to subscription queries of type FindAllSimCard
	 * 
	 * @param		entity	SimCard
	 */
	protected void emitFindAllSimCard( SimCard entity ) {
		LOGGER.info("handling emitFindAllSimCard" );
		
	    queryUpdateEmitter.emit(FindAllSimCardQuery.class,
	                            query -> true,
	                            entity);
	}

	//--------------------------------------------------
    // attributes
    // --------------------------------------------------
	@Autowired
	private final QueryUpdateEmitter queryUpdateEmitter;
    private static final Logger LOGGER 	= Logger.getLogger(SimCardProjector.class.getName());

}
