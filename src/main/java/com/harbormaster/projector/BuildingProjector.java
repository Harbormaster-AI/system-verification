/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.harbormaster.projector;

import java.util.*;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.stream.Collectors;

import org.axonframework.config.ProcessingGroup;
import org.axonframework.eventhandling.EventHandler;
import org.axonframework.queryhandling.QueryHandler;
import org.axonframework.queryhandling.QueryUpdateEmitter;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.domain.Pageable;
import org.springframework.data.domain.PageRequest;
import org.springframework.stereotype.Component;

import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.repository.*;

/**
 * <h2>BuildingProjector</h2>
 * <p>
 * The Building Projector is responsible for maintaining the read model for
 * Building entities. It subscribes to domain events published by the Building
 * Aggregate and updates query-optimized projections used by REST APIs,
 * dashboards, and reporting services.
 * </p>
 * <p>
 * A Projector contains no business logic and never validates commands or
 * modifies the authoritative state of the domain.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services Used</h3>
 *
 *      <ul><li>QueryGateway</li></ul>
 *
 * <h3>Queries Handled</h3>
 *		<ul>
 *      <li>FindBuildingQuery</li>
 *      <li>FindAllBuildingQuery</li>
 *		</ul>
 *
 * <p>@author Harbormaster Dev Team</p>
 */
//@ProcessingGroup("building")
@Component("building-projector")
public class BuildingProjector extends BuildingEntityProjector {
		
	// core constructor
	public BuildingProjector(ProjectorRegistry registry,
									BuildingRepository repository,
									QueryUpdateEmitter queryUpdateEmitter ) {
        super(registry, repository);
        this.queryUpdateEmitter = queryUpdateEmitter;
    }	

	/*
     * @param	event CreateBuildingEvent
     */
    @EventHandler( payloadType=CreateBuildingEvent.class )
    public void handle( CreateBuildingEvent event) {
	    LOGGER.info("handling CreateBuildingEvent - " + event );
	    Building entity = new Building();
            entity.setBuildingId( event.getBuildingId() );
            entity.setName( event.getName() );
	    
    	// ------------------------------------------
    	// persist a new one
    	// ------------------------------------------ 
	    create(entity);
        
        // ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllBuilding( entity );
    }

    /*
     * @param	event UpdateBuildingEvent
     */
    @EventHandler( payloadType=UpdateBuildingEvent.class )
    public void handle( UpdateBuildingEvent event) {
    	LOGGER.info("handling UpdateBuildingEvent - " + event );
    	
	    Building entity = new Building();
            entity.setBuildingId( event.getBuildingId() );
            entity.setName( event.getName() );
            entity.setSite( event.getSite() );
            entity.setFloors( event.getFloors() );
 
    	// ------------------------------------------
    	// save with an existing instance
    	// ------------------------------------------ 
		update(entity);

    	// ------------------------------------------
    	// emit to subscribers that find one
    	// ------------------------------------------    	
        emitFindBuilding( entity );

    	// ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllBuilding( entity );        
    }
    
    /*
     * @param	event DeleteBuildingEvent
     */
    @EventHandler( payloadType=DeleteBuildingEvent.class )
    public void handle( DeleteBuildingEvent event) {
    	LOGGER.info("handling DeleteBuildingEvent - " + event );
    	
    	// ------------------------------------------
    	// delete delegation
    	// ------------------------------------------
    	Building entity = delete( event.getBuildingId() );

    	// ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllBuilding( entity );

    }    

    /*
     * @param	event AssignSiteToBuildingEvent
     */
    @EventHandler( payloadType=AssignSiteToBuildingEvent.class)
    public void handle( AssignSiteToBuildingEvent event) {
	    LOGGER.info("handling AssignSiteToBuildingEvent - " + event );

	    // ------------------------------------------
	    // delegate to assignTo
	    // ------------------------------------------
	    Building entity = assignSite( event.getChildId(), event.getAssignment() );

	    // ------------------------------------------
    	// emit to subscribers that find one
    	// ------------------------------------------    	
        emitFindBuilding( entity );

    	// ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllBuilding( entity );
    }
    

	/*
	 * @param	event UnAssignSiteFromBuildingEvent
	 */
	@EventHandler( payloadType=UnAssignSiteFromBuildingEvent.class)
	public void handle( UnAssignSiteFromBuildingEvent event) {
	    LOGGER.info("handling UnAssignSiteFromBuildingEvent - " + event );

	    // ------------------------------------------
	    // delegate to unAssignFrom
	    // ------------------------------------------
	    Building entity = unAssignSite( event.getChildId() );

		// ------------------------------------------
		// emit to subscribers that find one
		// ------------------------------------------    	
	    emitFindBuilding( entity );
	
		// ------------------------------------------
		// emit to subscribers that find all
		// ------------------------------------------    	
	    emitFindAllBuilding( entity );
	}


    /*
     * @param	event AssignFloorsToBuildingEvent
     */
    @EventHandler( payloadType=AssignFloorsToBuildingEvent.class)
    public void handle( AssignFloorsToBuildingEvent event) {
	    LOGGER.info("handling AssignFloorsToBuildingEvent - " + event );
	    
	    // ------------------------------------------
    	// delegate to addTo 
    	// ------------------------------------------ 
	    Building entity = addToFloors(event.getParentId(), event.getChildIds() );
        
    	// ------------------------------------------
    	// emit to subscribers that find one
    	// ------------------------------------------    	
        emitFindBuilding( entity );

    	// ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllBuilding( entity );
    }
    

/*
 * @param	event RemoveFloorsFromBuildingEvent
 */
@EventHandler( payloadType=RemoveFloorsFromBuildingEvent.class)
public void handle( RemoveFloorsFromBuildingEvent event) {
    LOGGER.info("handling RemoveFloorsFromBuildingEvent - " + event );

    Building entity = removeFromFloors(event.getParentId(), event.getChildIds() );
    
	// ------------------------------------------
	// emit to subscribers that find one
	// ------------------------------------------    	
    emitFindBuilding( entity );

	// ------------------------------------------
	// emit to subscribers that find all
	// ------------------------------------------    	
    emitFindAllBuilding( entity );
}



    /**
     * Method to retrieve the Building via an BuildingPrimaryKey.
     * @param 	id Long
     * @return 	Building
     * @exception ProcessingException - Thrown if processing any related problems
     * @exception IllegalArgumentException 
     */
    @SuppressWarnings("unused")
    @QueryHandler
    public Building handle( FindBuildingQuery query ) 
    throws ProcessingException, IllegalArgumentException {
    	return find( query.getFilter().getBuildingId() );
    }
    
    /**
     * Method to retrieve a collection of all Buildings
     *
     * @param	query	FindAllBuildingQuery 
     * @return 	List<Building> 
     * @exception ProcessingException Thrown if any problems
     */
    @SuppressWarnings("unused")
    @QueryHandler
    public List<Building> handle( FindAllBuildingQuery query ) throws ProcessingException {
    	return findAll( query );
    }



/**
 * emit to subscription queries of type FindBuilding,
 * but only if the id matches
 *
 * @param		entity	Building
 */
	protected void emitFindBuilding( Building entity ) {
		LOGGER.info("handling emitFindBuilding" );
		
	    queryUpdateEmitter.emit(FindBuildingQuery.class,
	                            query -> query.getFilter().getBuildingId().equals(entity.getBuildingId()),
	                            entity);
	}
	
	/**
	 * unconditionally emit to subscription queries of type FindAllBuilding
	 * 
	 * @param		entity	Building
	 */
	protected void emitFindAllBuilding( Building entity ) {
		LOGGER.info("handling emitFindAllBuilding" );
		
	    queryUpdateEmitter.emit(FindAllBuildingQuery.class,
	                            query -> true,
	                            entity);
	}

	//--------------------------------------------------
    // attributes
    // --------------------------------------------------
	@Autowired
	private final QueryUpdateEmitter queryUpdateEmitter;
    private static final Logger LOGGER 	= Logger.getLogger(BuildingProjector.class.getName());

}
