/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.harbormaster.projector;

import java.util.*;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.stream.Collectors;

import org.axonframework.config.ProcessingGroup;
import org.axonframework.eventhandling.EventHandler;
import org.axonframework.queryhandling.QueryHandler;
import org.axonframework.queryhandling.QueryUpdateEmitter;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.domain.Pageable;
import org.springframework.data.domain.PageRequest;
import org.springframework.stereotype.Component;

import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.repository.*;

/**
 * <h2>TelemetryStreamProjector</h2>
 * <p>
 * The TelemetryStream Projector is responsible for maintaining the read model for
 * TelemetryStream entities. It subscribes to domain events published by the TelemetryStream
 * Aggregate and updates query-optimized projections used by REST APIs,
 * dashboards, and reporting services.
 * </p>
 * <p>
 * A Projector contains no business logic and never validates commands or
 * modifies the authoritative state of the domain.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services Used</h3>
 *
 *      <ul><li>QueryGateway</li></ul>
 *
 * <h3>Queries Handled</h3>
 *		<ul>
 *      <li>FindTelemetryStreamQuery</li>
 *      <li>FindAllTelemetryStreamQuery</li>
 *		</ul>
 *
 * <p>@author Harbormaster Dev Team</p>
 */
//@ProcessingGroup("telemetryStream")
@Component("telemetryStream-projector")
public class TelemetryStreamProjector extends TelemetryStreamEntityProjector {
		
	// core constructor
	public TelemetryStreamProjector(ProjectorRegistry registry,
									TelemetryStreamRepository repository,
									QueryUpdateEmitter queryUpdateEmitter ) {
        super(registry, repository);
        this.queryUpdateEmitter = queryUpdateEmitter;
    }	

	/*
     * @param	event CreateTelemetryStreamEvent
     */
    @EventHandler( payloadType=CreateTelemetryStreamEvent.class )
    public void handle( CreateTelemetryStreamEvent event) {
	    LOGGER.info("handling CreateTelemetryStreamEvent - " + event );
	    TelemetryStream entity = new TelemetryStream();
            entity.setTelemetryStreamId( event.getTelemetryStreamId() );
            entity.setStreamName( event.getStreamName() );
            entity.setRetentionDays( event.getRetentionDays() );
            entity.setQos( event.getQos() );
	    
    	// ------------------------------------------
    	// persist a new one
    	// ------------------------------------------ 
	    create(entity);
        
        // ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllTelemetryStream( entity );
    }

    /*
     * @param	event UpdateTelemetryStreamEvent
     */
    @EventHandler( payloadType=UpdateTelemetryStreamEvent.class )
    public void handle( UpdateTelemetryStreamEvent event) {
    	LOGGER.info("handling UpdateTelemetryStreamEvent - " + event );
    	
	    TelemetryStream entity = new TelemetryStream();
            entity.setTelemetryStreamId( event.getTelemetryStreamId() );
            entity.setStreamName( event.getStreamName() );
            entity.setRetentionDays( event.getRetentionDays() );
            entity.setDevice( event.getDevice() );
            entity.setSensor( event.getSensor() );
            entity.setSchema( event.getSchema() );
            entity.setMessagingEndpoint( event.getMessagingEndpoint() );
            entity.setRetentionPolicy( event.getRetentionPolicy() );
            entity.setQos( event.getQos() );
 
    	// ------------------------------------------
    	// save with an existing instance
    	// ------------------------------------------ 
		update(entity);

    	// ------------------------------------------
    	// emit to subscribers that find one
    	// ------------------------------------------    	
        emitFindTelemetryStream( entity );

    	// ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllTelemetryStream( entity );        
    }
    
    /*
     * @param	event DeleteTelemetryStreamEvent
     */
    @EventHandler( payloadType=DeleteTelemetryStreamEvent.class )
    public void handle( DeleteTelemetryStreamEvent event) {
    	LOGGER.info("handling DeleteTelemetryStreamEvent - " + event );
    	
    	// ------------------------------------------
    	// delete delegation
    	// ------------------------------------------
    	TelemetryStream entity = delete( event.getTelemetryStreamId() );

    	// ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllTelemetryStream( entity );

    }    

    /*
     * @param	event AssignDeviceToTelemetryStreamEvent
     */
    @EventHandler( payloadType=AssignDeviceToTelemetryStreamEvent.class)
    public void handle( AssignDeviceToTelemetryStreamEvent event) {
	    LOGGER.info("handling AssignDeviceToTelemetryStreamEvent - " + event );

	    // ------------------------------------------
	    // delegate to assignTo
	    // ------------------------------------------
	    TelemetryStream entity = assignDevice( event.getChildId(), event.getAssignment() );

	    // ------------------------------------------
    	// emit to subscribers that find one
    	// ------------------------------------------    	
        emitFindTelemetryStream( entity );

    	// ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllTelemetryStream( entity );
    }
    

	/*
	 * @param	event UnAssignDeviceFromTelemetryStreamEvent
	 */
	@EventHandler( payloadType=UnAssignDeviceFromTelemetryStreamEvent.class)
	public void handle( UnAssignDeviceFromTelemetryStreamEvent event) {
	    LOGGER.info("handling UnAssignDeviceFromTelemetryStreamEvent - " + event );

	    // ------------------------------------------
	    // delegate to unAssignFrom
	    // ------------------------------------------
	    TelemetryStream entity = unAssignDevice( event.getChildId() );

		// ------------------------------------------
		// emit to subscribers that find one
		// ------------------------------------------    	
	    emitFindTelemetryStream( entity );
	
		// ------------------------------------------
		// emit to subscribers that find all
		// ------------------------------------------    	
	    emitFindAllTelemetryStream( entity );
	}

    /*
     * @param	event AssignSensorToTelemetryStreamEvent
     */
    @EventHandler( payloadType=AssignSensorToTelemetryStreamEvent.class)
    public void handle( AssignSensorToTelemetryStreamEvent event) {
	    LOGGER.info("handling AssignSensorToTelemetryStreamEvent - " + event );

	    // ------------------------------------------
	    // delegate to assignTo
	    // ------------------------------------------
	    TelemetryStream entity = assignSensor( event.getChildId(), event.getAssignment() );

	    // ------------------------------------------
    	// emit to subscribers that find one
    	// ------------------------------------------    	
        emitFindTelemetryStream( entity );

    	// ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllTelemetryStream( entity );
    }
    

	/*
	 * @param	event UnAssignSensorFromTelemetryStreamEvent
	 */
	@EventHandler( payloadType=UnAssignSensorFromTelemetryStreamEvent.class)
	public void handle( UnAssignSensorFromTelemetryStreamEvent event) {
	    LOGGER.info("handling UnAssignSensorFromTelemetryStreamEvent - " + event );

	    // ------------------------------------------
	    // delegate to unAssignFrom
	    // ------------------------------------------
	    TelemetryStream entity = unAssignSensor( event.getChildId() );

		// ------------------------------------------
		// emit to subscribers that find one
		// ------------------------------------------    	
	    emitFindTelemetryStream( entity );
	
		// ------------------------------------------
		// emit to subscribers that find all
		// ------------------------------------------    	
	    emitFindAllTelemetryStream( entity );
	}

    /*
     * @param	event AssignSchemaToTelemetryStreamEvent
     */
    @EventHandler( payloadType=AssignSchemaToTelemetryStreamEvent.class)
    public void handle( AssignSchemaToTelemetryStreamEvent event) {
	    LOGGER.info("handling AssignSchemaToTelemetryStreamEvent - " + event );

	    // ------------------------------------------
	    // delegate to assignTo
	    // ------------------------------------------
	    TelemetryStream entity = assignSchema( event.getChildId(), event.getAssignment() );

	    // ------------------------------------------
    	// emit to subscribers that find one
    	// ------------------------------------------    	
        emitFindTelemetryStream( entity );

    	// ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllTelemetryStream( entity );
    }
    

	/*
	 * @param	event UnAssignSchemaFromTelemetryStreamEvent
	 */
	@EventHandler( payloadType=UnAssignSchemaFromTelemetryStreamEvent.class)
	public void handle( UnAssignSchemaFromTelemetryStreamEvent event) {
	    LOGGER.info("handling UnAssignSchemaFromTelemetryStreamEvent - " + event );

	    // ------------------------------------------
	    // delegate to unAssignFrom
	    // ------------------------------------------
	    TelemetryStream entity = unAssignSchema( event.getChildId() );

		// ------------------------------------------
		// emit to subscribers that find one
		// ------------------------------------------    	
	    emitFindTelemetryStream( entity );
	
		// ------------------------------------------
		// emit to subscribers that find all
		// ------------------------------------------    	
	    emitFindAllTelemetryStream( entity );
	}

    /*
     * @param	event AssignMessagingEndpointToTelemetryStreamEvent
     */
    @EventHandler( payloadType=AssignMessagingEndpointToTelemetryStreamEvent.class)
    public void handle( AssignMessagingEndpointToTelemetryStreamEvent event) {
	    LOGGER.info("handling AssignMessagingEndpointToTelemetryStreamEvent - " + event );

	    // ------------------------------------------
	    // delegate to assignTo
	    // ------------------------------------------
	    TelemetryStream entity = assignMessagingEndpoint( event.getChildId(), event.getAssignment() );

	    // ------------------------------------------
    	// emit to subscribers that find one
    	// ------------------------------------------    	
        emitFindTelemetryStream( entity );

    	// ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllTelemetryStream( entity );
    }
    

	/*
	 * @param	event UnAssignMessagingEndpointFromTelemetryStreamEvent
	 */
	@EventHandler( payloadType=UnAssignMessagingEndpointFromTelemetryStreamEvent.class)
	public void handle( UnAssignMessagingEndpointFromTelemetryStreamEvent event) {
	    LOGGER.info("handling UnAssignMessagingEndpointFromTelemetryStreamEvent - " + event );

	    // ------------------------------------------
	    // delegate to unAssignFrom
	    // ------------------------------------------
	    TelemetryStream entity = unAssignMessagingEndpoint( event.getChildId() );

		// ------------------------------------------
		// emit to subscribers that find one
		// ------------------------------------------    	
	    emitFindTelemetryStream( entity );
	
		// ------------------------------------------
		// emit to subscribers that find all
		// ------------------------------------------    	
	    emitFindAllTelemetryStream( entity );
	}

    /*
     * @param	event AssignRetentionPolicyToTelemetryStreamEvent
     */
    @EventHandler( payloadType=AssignRetentionPolicyToTelemetryStreamEvent.class)
    public void handle( AssignRetentionPolicyToTelemetryStreamEvent event) {
	    LOGGER.info("handling AssignRetentionPolicyToTelemetryStreamEvent - " + event );

	    // ------------------------------------------
	    // delegate to assignTo
	    // ------------------------------------------
	    TelemetryStream entity = assignRetentionPolicy( event.getChildId(), event.getAssignment() );

	    // ------------------------------------------
    	// emit to subscribers that find one
    	// ------------------------------------------    	
        emitFindTelemetryStream( entity );

    	// ------------------------------------------
    	// emit to subscribers that find all
    	// ------------------------------------------    	
        emitFindAllTelemetryStream( entity );
    }
    

	/*
	 * @param	event UnAssignRetentionPolicyFromTelemetryStreamEvent
	 */
	@EventHandler( payloadType=UnAssignRetentionPolicyFromTelemetryStreamEvent.class)
	public void handle( UnAssignRetentionPolicyFromTelemetryStreamEvent event) {
	    LOGGER.info("handling UnAssignRetentionPolicyFromTelemetryStreamEvent - " + event );

	    // ------------------------------------------
	    // delegate to unAssignFrom
	    // ------------------------------------------
	    TelemetryStream entity = unAssignRetentionPolicy( event.getChildId() );

		// ------------------------------------------
		// emit to subscribers that find one
		// ------------------------------------------    	
	    emitFindTelemetryStream( entity );
	
		// ------------------------------------------
		// emit to subscribers that find all
		// ------------------------------------------    	
	    emitFindAllTelemetryStream( entity );
	}




    /**
     * Method to retrieve the TelemetryStream via an TelemetryStreamPrimaryKey.
     * @param 	id Long
     * @return 	TelemetryStream
     * @exception ProcessingException - Thrown if processing any related problems
     * @exception IllegalArgumentException 
     */
    @SuppressWarnings("unused")
    @QueryHandler
    public TelemetryStream handle( FindTelemetryStreamQuery query ) 
    throws ProcessingException, IllegalArgumentException {
    	return find( query.getFilter().getTelemetryStreamId() );
    }
    
    /**
     * Method to retrieve a collection of all TelemetryStreams
     *
     * @param	query	FindAllTelemetryStreamQuery 
     * @return 	List<TelemetryStream> 
     * @exception ProcessingException Thrown if any problems
     */
    @SuppressWarnings("unused")
    @QueryHandler
    public List<TelemetryStream> handle( FindAllTelemetryStreamQuery query ) throws ProcessingException {
    	return findAll( query );
    }



/**
 * emit to subscription queries of type FindTelemetryStream,
 * but only if the id matches
 *
 * @param		entity	TelemetryStream
 */
	protected void emitFindTelemetryStream( TelemetryStream entity ) {
		LOGGER.info("handling emitFindTelemetryStream" );
		
	    queryUpdateEmitter.emit(FindTelemetryStreamQuery.class,
	                            query -> query.getFilter().getTelemetryStreamId().equals(entity.getTelemetryStreamId()),
	                            entity);
	}
	
	/**
	 * unconditionally emit to subscription queries of type FindAllTelemetryStream
	 * 
	 * @param		entity	TelemetryStream
	 */
	protected void emitFindAllTelemetryStream( TelemetryStream entity ) {
		LOGGER.info("handling emitFindAllTelemetryStream" );
		
	    queryUpdateEmitter.emit(FindAllTelemetryStreamQuery.class,
	                            query -> true,
	                            entity);
	}

	//--------------------------------------------------
    // attributes
    // --------------------------------------------------
	@Autowired
	private final QueryUpdateEmitter queryUpdateEmitter;
    private static final Logger LOGGER 	= Logger.getLogger(TelemetryStreamProjector.class.getName());

}
