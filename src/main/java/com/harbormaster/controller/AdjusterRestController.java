
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Insurance Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.controller;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;

import java.util.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import com.harbormaster.api.*;
import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;

/**
 * AdjusterRestController as REST API
 *
 * <p>Exposes REST endpoints for the Adjuster entity.</p>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Spring Boot 3.5</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>ServiceLayer</td></tr>
 *          <tr><td>architecture style</td><td>Layered</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Insurance Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core insurance product, underwriting, policy, billing, claims, and reinsurance capabilities.</td></tr>
 *          <tr><td>industry</td><td>Insurance</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services Used</h3>
 *
 *  	AdjusterService
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <h3>Commands Accepted</h3>
 *      <ul>
 *      <li>CreateAdjusterCommand</li>
 *      <li></li>UpdateAdjusterCommand</li>
 *      <liDeleteAdjusterCommand</li>
 *      </ul>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@CrossOrigin
@RestController
@RequestMapping("/Adjuster")
public class AdjusterRestController extends BaseSpringRestController {

	public AdjusterRestController( AdjusterService service ) {
		this.service = service;
	}
    /**
     * Handles create a Adjuster.  if not key provided, calls create, otherwise calls save
     * @param		Adjuster	adjuster
     * @return		Adjuster 
     */
	@PostMapping("/create")
    public Adjuster create( @RequestBody(required=true) CreateAdjusterCommand command ) {
    	Adjuster entity = null;

		entity = service.createAdjuster( command );
		LOGGER.info( "Successfully created Adjuster with Id " + entity.getAdjusterId()  );

		return entity;
    }

    /**
     * Handles updating a Adjuster.  if no key provided, calls create, otherwise calls save
     * @param		Adjuster adjuster
     * @return		Adjuster
     */
	@PutMapping("/update")
    public Adjuster update( @RequestBody(required=true) UpdateAdjusterCommand command ) {
		Adjuster entity = null;

		entity = service.updateAdjuster(command);
		LOGGER.info( "Successfully updated Adjuster with Id " + command.getAdjusterId()  );

		return entity;
	}
 
    /**
     * Handles deleting a Adjuster entity
     * @param		command ${class.getDeleteCommandAlias()}
     */
    @DeleteMapping("/delete")    
    public void delete( @RequestBody(required=true) DeleteAdjusterCommand command ) {                
		service.delete( command );
		LOGGER.info( "Successfully deleted Adjuster with Id " + command.getAdjusterId()  );
	}
	
    /**
     * Handles loading a Adjuster using a UUID
     * @param		UUID uuid 
     * @return		Adjuster
     */    
    @GetMapping("/get")
    public Adjuster get( @RequestParam(required=true) UUID uuid ) {
    	Adjuster entity = service.getAdjuster( new AdjusterFetchOneSummary( uuid ) );

		LOGGER.info( "Successfully loaded Adjuster with Id " + uuid   );

        return entity;
    }

    /**
     * Handles loading all Adjuster business objects
     * @return		List<Adjuster>
     */
    @GetMapping("/")
    public List<Adjuster> getAll() {
    	List<Adjuster> adjusterList = null;

		adjusterList = service.getAllAdjuster();
		LOGGER.info( "successfully loaded all Adjusters" );

        return adjusterList;
                            
    }


	/**
	 * Handles loading all Claims} business objects
	 * @return		List<Adjuster>

    @GetMapping("/Claims")
    public List<Adjuster> Claims(UUID parentId) {
		List<Adjuster> adjusterList = load(parentId).getClaims;
		return adjusterList;
	}

    /**
     * save Claims on Adjuster
     * @param		command AssignClaimsToAdjusterCommand
     */     
	@PutMapping("/addToClaims")
	public void addToClaims( @RequestBody(required=true) AssignClaimsToAdjusterCommand command ) {
		service.addToClaims( command );
		LOGGER.info( "Successfully added Claims with Id " + command.getAdjusterId()  );
	}

    /**
     * remove Claims on Adjuster
     * @param		command RemoveClaimsFromAdjusterCommand
     */     	
	@PutMapping("/removeFromClaims")
	public void removeFromClaims( 	@RequestBody(required=true) RemoveClaimsFromAdjusterCommand command )
	{		
		service.removeFromClaims( command );
		LOGGER.info( "Successfully removed Claims with Id " + command.getAdjusterId()  );
	}

	/**
	 * Handles loading all ServiceProviders} business objects
	 * @return		List<Adjuster>

    @GetMapping("/ServiceProviders")
    public List<Adjuster> ServiceProviders(UUID parentId) {
		List<Adjuster> adjusterList = load(parentId).getServiceProviders;
		return adjusterList;
	}

    /**
     * save ServiceProviders on Adjuster
     * @param		command AssignServiceProvidersToAdjusterCommand
     */     
	@PutMapping("/addToServiceProviders")
	public void addToServiceProviders( @RequestBody(required=true) AssignServiceProvidersToAdjusterCommand command ) {
		service.addToServiceProviders( command );
		LOGGER.info( "Successfully added ServiceProviders with Id " + command.getAdjusterId()  );
	}

    /**
     * remove ServiceProviders on Adjuster
     * @param		command RemoveServiceProvidersFromAdjusterCommand
     */     	
	@PutMapping("/removeFromServiceProviders")
	public void removeFromServiceProviders( 	@RequestBody(required=true) RemoveServiceProvidersFromAdjusterCommand command )
	{		
		service.removeFromServiceProviders( command );
		LOGGER.info( "Successfully removed ServiceProviders with Id " + command.getAdjusterId()  );
	}




//************************************************************************    
// Attributes
//************************************************************************
    protected Adjuster adjuster = null;
	protected AdjusterService service = null;
    private static final Logger LOGGER = LoggerFactory.getLogger(AdjusterRestController.class.getName());
    
}
