
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Insurance Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.controller;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;

import java.util.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import com.harbormaster.api.*;
import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;

/**
 * QuoteRestController as REST API
 *
 * <p>Exposes REST endpoints for the Quote entity.</p>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Spring Boot 3.5</td></tr>
 *          <tr><td>published</td><td>09/12/2026</td></tr>
 *          <tr><td>design pattern</td><td>ServiceLayer</td></tr>
 *          <tr><td>architecture style</td><td>Layered</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Insurance Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core insurance product, underwriting, policy, billing, claims, and reinsurance capabilities.</td></tr>
 *          <tr><td>industry</td><td>Insurance</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services Used</h3>
 *
 *  	QuoteService
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <h3>Commands Accepted</h3>
 *      <ul>
 *      <li>CreateQuoteCommand</li>
 *      <li></li>UpdateQuoteCommand</li>
 *      <liDeleteQuoteCommand</li>
 *      </ul>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@CrossOrigin
@RestController
@RequestMapping("/Quote")
public class QuoteRestController extends BaseSpringRestController {

	public QuoteRestController( QuoteService service ) {
		this.service = service;
	}
    /**
     * Handles create a Quote.  if not key provided, calls create, otherwise calls save
     * @param		Quote	quote
     * @return		Quote 
     */
	@PostMapping("/create")
    public Quote create( @RequestBody(required=true) CreateQuoteCommand command ) {
    	Quote entity = null;

		entity = service.createQuote( command );
		LOGGER.info( "Successfully created Quote with Id " + entity.getQuoteId()  );

		return entity;
    }

    /**
     * Handles updating a Quote.  if no key provided, calls create, otherwise calls save
     * @param		Quote quote
     * @return		Quote
     */
	@PutMapping("/update")
    public Quote update( @RequestBody(required=true) UpdateQuoteCommand command ) {
		Quote entity = null;

		entity = service.updateQuote(command);
		LOGGER.info( "Successfully updated Quote with Id " + command.getQuoteId()  );

		return entity;
	}
 
    /**
     * Handles deleting a Quote entity
     * @param		command ${class.getDeleteCommandAlias()}
     */
    @DeleteMapping("/delete")    
    public void delete( @RequestBody(required=true) DeleteQuoteCommand command ) {                
		service.delete( command );
		LOGGER.info( "Successfully deleted Quote with Id " + command.getQuoteId()  );
	}
	
    /**
     * Handles loading a Quote using a UUID
     * @param		UUID uuid 
     * @return		Quote
     */    
    @GetMapping("/get")
    public Quote get( @RequestParam(required=true) UUID uuid ) {
    	Quote entity = service.getQuote( new QuoteFetchOneSummary( uuid ) );

		LOGGER.info( "Successfully loaded Quote with Id " + uuid   );

        return entity;
    }

    /**
     * Handles loading all Quote business objects
     * @return		List<Quote>
     */
    @GetMapping("/")
    public List<Quote> getAll() {
    	List<Quote> quoteList = null;

		quoteList = service.getAllQuote();
		LOGGER.info( "successfully loaded all Quotes" );

        return quoteList;
                            
    }

	/**
	 * Handles loading all Application} business objects
	 * @return		List<Quote>

    @GetMapping("/Application")
    public List<Quote> Application(UUID parentId) {
		List<Quote> quoteList = load(parentId).getApplication;
		return quoteList;
	}
	 */
    /**
     * save Application on Quote
     * @param		command AssignApplicationToQuoteCommand
     */     
	@PutMapping("/assignApplication")
	public void assignApplication( @RequestBody AssignApplicationToQuoteCommand command ) {
		service.assignApplication( command );
		LOGGER.info( "Successfully assigned Application with Id " + command.getQuoteId()  );
	}

    /**
     * unassign Application on Quote
     * @param		 command UnAssignApplicationFromQuoteCommand
     */     
	@PutMapping("/unassignApplication")
	public void unAssignApplication( @RequestBody(required=true)  UnAssignApplicationFromQuoteCommand command ) {
		service.unAssignApplication( command );
		LOGGER.info( "Successfully unassigned Application with Id " + command.getQuoteId()  );
	}
	
	/**
	 * Handles loading all Policy} business objects
	 * @return		List<Quote>

    @GetMapping("/Policy")
    public List<Quote> Policy(UUID parentId) {
		List<Quote> quoteList = load(parentId).getPolicy;
		return quoteList;
	}
	 */
    /**
     * save Policy on Quote
     * @param		command AssignPolicyToQuoteCommand
     */     
	@PutMapping("/assignPolicy")
	public void assignPolicy( @RequestBody AssignPolicyToQuoteCommand command ) {
		service.assignPolicy( command );
		LOGGER.info( "Successfully assigned Policy with Id " + command.getQuoteId()  );
	}

    /**
     * unassign Policy on Quote
     * @param		 command UnAssignPolicyFromQuoteCommand
     */     
	@PutMapping("/unassignPolicy")
	public void unAssignPolicy( @RequestBody(required=true)  UnAssignPolicyFromQuoteCommand command ) {
		service.unAssignPolicy( command );
		LOGGER.info( "Successfully unassigned Policy with Id " + command.getQuoteId()  );
	}
	

	/**
	 * Handles loading all UnderwritingDecisions} business objects
	 * @return		List<Quote>

    @GetMapping("/UnderwritingDecisions")
    public List<Quote> UnderwritingDecisions(UUID parentId) {
		List<Quote> quoteList = load(parentId).getUnderwritingDecisions;
		return quoteList;
	}

    /**
     * save UnderwritingDecisions on Quote
     * @param		command AssignUnderwritingDecisionsToQuoteCommand
     */     
	@PutMapping("/addToUnderwritingDecisions")
	public void addToUnderwritingDecisions( @RequestBody(required=true) AssignUnderwritingDecisionsToQuoteCommand command ) {
		service.addToUnderwritingDecisions( command );
		LOGGER.info( "Successfully added UnderwritingDecisions with Id " + command.getQuoteId()  );
	}

    /**
     * remove UnderwritingDecisions on Quote
     * @param		command RemoveUnderwritingDecisionsFromQuoteCommand
     */     	
	@PutMapping("/removeFromUnderwritingDecisions")
	public void removeFromUnderwritingDecisions( 	@RequestBody(required=true) RemoveUnderwritingDecisionsFromQuoteCommand command )
	{		
		service.removeFromUnderwritingDecisions( command );
		LOGGER.info( "Successfully removed UnderwritingDecisions with Id " + command.getQuoteId()  );
	}




//************************************************************************    
// Attributes
//************************************************************************
    protected Quote quote = null;
	protected QuoteService service = null;
    private static final Logger LOGGER = LoggerFactory.getLogger(QuoteRestController.class.getName());
    
}
