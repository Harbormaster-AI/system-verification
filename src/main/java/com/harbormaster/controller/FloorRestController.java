
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.harbormaster.controller;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;

import java.util.*;
import java.util.concurrent.*;
import java.util.logging.Level;
import java.util.logging.Logger;

import com.harbormaster.api.*;
import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;

/**
 * FloorRestController as REST API
 *
 * <p>Exposes REST endpoints for the Floor entity.</p>
 *
 * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services Used</h3>
 *
 *  	FloorBusinessService
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <h3>Commands Accepted</h3>
 *      <ul>
 *      <li>CreateFloorCommand</li>
 *      <li></li>UpdateFloorCommand</li>
 *      <liDeleteFloorCommand</li>
 *      </ul>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@CrossOrigin
@RestController
@RequestMapping("/Floor")
public class FloorRestController extends BaseSpringRestController {

	public FloorRestController( FloorService service ) {
		this.service = service;
	}
	
    /**
     * Handles create a Floor.  if not key provided, calls create, otherwise calls save
     * @param		Floor	floor
     * @return		CompletableFuture<UUID> 
     */
	@PostMapping("/create")
    public CompletableFuture<UUID> create( @RequestBody(required=true) CreateFloorCommand command ) {
		CompletableFuture<UUID> completableFuture = null;
		try {       
        	
			completableFuture = service.createFloor( command );
        }
        catch( Throwable exc ) {
        	LOGGER.log( Level.WARNING, exc.getMessage(), exc );        	
        }
		
		return completableFuture;
    }

    /**
     * Handles updating a Floor.  if no key provided, calls create, otherwise calls save
     * @param		Floor floor
     * @return		CompletableFuture<Void>
     */
	@PutMapping("/update")
    public CompletableFuture<Void> update( @RequestBody(required=true) UpdateFloorCommand command ) {
		CompletableFuture<Void> completableFuture = null;
		try {                        	        
			// -----------------------------------------------
			// delegate the UpdateFloorCommand
			// -----------------------------------------------
			completableFuture = service.updateFloor(command);;
	    }
	    catch( Throwable exc ) {
	    	LOGGER.log( Level.WARNING, "FloorController:update() - successfully update Floor - " + exc.getMessage());        	
	    }		
		
		return completableFuture;
	}
 
    /**
     * Handles deleting a Floor entity
     * @param		command ${class.getDeleteCommandAlias()}
     * @return		CompletableFuture<Void>
     */
    @DeleteMapping("/delete")    
    public CompletableFuture<Void> delete( @RequestBody(required=true) DeleteFloorCommand command ) {                
    	CompletableFuture<Void> completableFuture = null;
    	try {
        	FloorService delegate = service;

        	completableFuture = delegate.delete( command );
    		LOGGER.log( Level.WARNING, "Successfully deleted Floor with key " + command.getFloorId() );
        }
        catch( Throwable exc ) {
        	LOGGER.log( Level.WARNING, exc.getMessage() );
        }
        
        return completableFuture;
	}        
	
    /**
     * Handles loading a Floor using a UUID
     * @param		UUID uuid 
     * @return		Floor
     */    
    @GetMapping("/load")
    public Floor load( @RequestParam(required=true) UUID uuid ) {    	
    	Floor entity = null;

    	try {  
    		entity = service.getFloor( new FloorFetchOneSummary( uuid ) );   
        }
        catch( Throwable exc ) {
            LOGGER.log( Level.WARNING, "failed to load Floor using Id " + uuid );
            return null;
        }

        return entity;
    }

    /**
     * Handles loading all Floor business objects
     * @return		Set<Floor>
     */
    @GetMapping("/")
    public List<Floor> loadAll() {                
    	List<Floor> floorList = null;
        
    	try {
            // load the Floor
            floorList = service.getAllFloor();
            
            if ( floorList != null )
                LOGGER.log( Level.INFO,  "successfully loaded all Floors" );
        }
        catch( Throwable exc ) {
            LOGGER.log( Level.WARNING,  "failed to load all Floors ", exc );
        	return null;
        }

        return floorList;
                            
    }

    /**
     * save Building on Floor
     * @param		command AssignBuildingToFloorCommand
     */     
	@PutMapping("/assignBuilding")
	public void assignBuilding( @RequestBody AssignBuildingToFloorCommand command ) {
		try {
			service.assignBuilding( command );   
		}
        catch( Throwable exc ) {
        	LOGGER.log( Level.WARNING, "Failed to assign Building", exc );
        }
	}

    /**
     * unassign Building on Floor
     * @param		 command UnAssignBuildingFromFloorCommand
     */     
	@PutMapping("/unAssignBuilding")
	public void unAssignBuilding( @RequestBody(required=true)  UnAssignBuildingFromFloorCommand command ) {
		try {
			service.unAssignBuilding( command );   
		}
		catch( Exception exc ) {
			LOGGER.log( Level.WARNING, "Failed to unassign Building", exc );
		}
	}
	

    /**
     * save Rooms on Floor
     * @param		command AssignRoomsToFloorCommand
     */     
	@PutMapping("/addToRooms")
	public void addToRooms( @RequestBody(required=true) AssignRoomsToFloorCommand command ) {
		try {
			service.addToRooms( command );   
		}
		catch( Exception exc ) {
			LOGGER.log( Level.WARNING, "Failed to add to Set Rooms", exc );
		}
	}

    /**
     * remove Rooms on Floor
     * @param		command RemoveRoomsFromFloorCommand
     */     	
	@PutMapping("/removeFromRooms")
	public void removeFromRooms( 	@RequestBody(required=true) RemoveRoomsFromFloorCommand command )
	{		
		try {
			service.removeFromRooms( command );
		}
		catch( Exception exc ) {
			LOGGER.log( Level.WARNING, "Failed to remove from Set Rooms", exc );
		}
	}




//************************************************************************    
// Attributes
//************************************************************************
    protected Floor floor = null;
	protected FloorService service = null;
	private static final Logger LOGGER = Logger.getLogger(FloorRestController.class.getName());
    
}
