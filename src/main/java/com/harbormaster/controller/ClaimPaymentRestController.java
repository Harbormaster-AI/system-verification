
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Insurance Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.controller;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;

import java.util.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import com.harbormaster.api.*;
import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;

/**
 * ClaimPaymentRestController as REST API
 *
 * <p>Exposes REST endpoints for the ClaimPayment entity.</p>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Spring Boot 3.5</td></tr>
 *          <tr><td>published</td><td>09/12/2026</td></tr>
 *          <tr><td>design pattern</td><td>ServiceLayer</td></tr>
 *          <tr><td>architecture style</td><td>Layered</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Insurance Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core insurance product, underwriting, policy, billing, claims, and reinsurance capabilities.</td></tr>
 *          <tr><td>industry</td><td>Insurance</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services Used</h3>
 *
 *  	ClaimPaymentService
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <h3>Commands Accepted</h3>
 *      <ul>
 *      <li>CreateClaimPaymentCommand</li>
 *      <li></li>UpdateClaimPaymentCommand</li>
 *      <liDeleteClaimPaymentCommand</li>
 *      </ul>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@CrossOrigin
@RestController
@RequestMapping("/ClaimPayment")
public class ClaimPaymentRestController extends BaseSpringRestController {

	public ClaimPaymentRestController( ClaimPaymentService service ) {
		this.service = service;
	}
    /**
     * Handles create a ClaimPayment.  if not key provided, calls create, otherwise calls save
     * @param		ClaimPayment	claimPayment
     * @return		ClaimPayment 
     */
	@PostMapping("/create")
    public ClaimPayment create( @RequestBody(required=true) CreateClaimPaymentCommand command ) {
    	ClaimPayment entity = null;

		entity = service.createClaimPayment( command );
		LOGGER.info( "Successfully created ClaimPayment with Id " + entity.getClaimPaymentId()  );

		return entity;
    }

    /**
     * Handles updating a ClaimPayment.  if no key provided, calls create, otherwise calls save
     * @param		ClaimPayment claimPayment
     * @return		ClaimPayment
     */
	@PutMapping("/update")
    public ClaimPayment update( @RequestBody(required=true) UpdateClaimPaymentCommand command ) {
		ClaimPayment entity = null;

		entity = service.updateClaimPayment(command);
		LOGGER.info( "Successfully updated ClaimPayment with Id " + command.getClaimPaymentId()  );

		return entity;
	}
 
    /**
     * Handles deleting a ClaimPayment entity
     * @param		command ${class.getDeleteCommandAlias()}
     */
    @DeleteMapping("/delete")    
    public void delete( @RequestBody(required=true) DeleteClaimPaymentCommand command ) {                
		service.delete( command );
		LOGGER.info( "Successfully deleted ClaimPayment with Id " + command.getClaimPaymentId()  );
	}
	
    /**
     * Handles loading a ClaimPayment using a UUID
     * @param		UUID uuid 
     * @return		ClaimPayment
     */    
    @GetMapping("/get")
    public ClaimPayment get( @RequestParam(required=true) UUID uuid ) {
    	ClaimPayment entity = service.getClaimPayment( new ClaimPaymentFetchOneSummary( uuid ) );

		LOGGER.info( "Successfully loaded ClaimPayment with Id " + uuid   );

        return entity;
    }

    /**
     * Handles loading all ClaimPayment business objects
     * @return		List<ClaimPayment>
     */
    @GetMapping("/")
    public List<ClaimPayment> getAll() {
    	List<ClaimPayment> claimPaymentList = null;

		claimPaymentList = service.getAllClaimPayment();
		LOGGER.info( "successfully loaded all ClaimPayments" );

        return claimPaymentList;
                            
    }

	/**
	 * Handles loading all Claim} business objects
	 * @return		List<ClaimPayment>

    @GetMapping("/Claim")
    public List<ClaimPayment> Claim(UUID parentId) {
		List<ClaimPayment> claimPaymentList = load(parentId).getClaim;
		return claimPaymentList;
	}
	 */
    /**
     * save Claim on ClaimPayment
     * @param		command AssignClaimToClaimPaymentCommand
     */     
	@PutMapping("/assignClaim")
	public void assignClaim( @RequestBody AssignClaimToClaimPaymentCommand command ) {
		service.assignClaim( command );
		LOGGER.info( "Successfully assigned Claim with Id " + command.getClaimPaymentId()  );
	}

    /**
     * unassign Claim on ClaimPayment
     * @param		 command UnAssignClaimFromClaimPaymentCommand
     */     
	@PutMapping("/unassignClaim")
	public void unAssignClaim( @RequestBody(required=true)  UnAssignClaimFromClaimPaymentCommand command ) {
		service.unAssignClaim( command );
		LOGGER.info( "Successfully unassigned Claim with Id " + command.getClaimPaymentId()  );
	}
	
	/**
	 * Handles loading all Exposure} business objects
	 * @return		List<ClaimPayment>

    @GetMapping("/Exposure")
    public List<ClaimPayment> Exposure(UUID parentId) {
		List<ClaimPayment> claimPaymentList = load(parentId).getExposure;
		return claimPaymentList;
	}
	 */
    /**
     * save Exposure on ClaimPayment
     * @param		command AssignExposureToClaimPaymentCommand
     */     
	@PutMapping("/assignExposure")
	public void assignExposure( @RequestBody AssignExposureToClaimPaymentCommand command ) {
		service.assignExposure( command );
		LOGGER.info( "Successfully assigned Exposure with Id " + command.getClaimPaymentId()  );
	}

    /**
     * unassign Exposure on ClaimPayment
     * @param		 command UnAssignExposureFromClaimPaymentCommand
     */     
	@PutMapping("/unassignExposure")
	public void unAssignExposure( @RequestBody(required=true)  UnAssignExposureFromClaimPaymentCommand command ) {
		service.unAssignExposure( command );
		LOGGER.info( "Successfully unassigned Exposure with Id " + command.getClaimPaymentId()  );
	}
	
	/**
	 * Handles loading all Beneficiary} business objects
	 * @return		List<ClaimPayment>

    @GetMapping("/Beneficiary")
    public List<ClaimPayment> Beneficiary(UUID parentId) {
		List<ClaimPayment> claimPaymentList = load(parentId).getBeneficiary;
		return claimPaymentList;
	}
	 */
    /**
     * save Beneficiary on ClaimPayment
     * @param		command AssignBeneficiaryToClaimPaymentCommand
     */     
	@PutMapping("/assignBeneficiary")
	public void assignBeneficiary( @RequestBody AssignBeneficiaryToClaimPaymentCommand command ) {
		service.assignBeneficiary( command );
		LOGGER.info( "Successfully assigned Beneficiary with Id " + command.getClaimPaymentId()  );
	}

    /**
     * unassign Beneficiary on ClaimPayment
     * @param		 command UnAssignBeneficiaryFromClaimPaymentCommand
     */     
	@PutMapping("/unassignBeneficiary")
	public void unAssignBeneficiary( @RequestBody(required=true)  UnAssignBeneficiaryFromClaimPaymentCommand command ) {
		service.unAssignBeneficiary( command );
		LOGGER.info( "Successfully unassigned Beneficiary with Id " + command.getClaimPaymentId()  );
	}
	
	/**
	 * Handles loading all ServiceProvider} business objects
	 * @return		List<ClaimPayment>

    @GetMapping("/ServiceProvider")
    public List<ClaimPayment> ServiceProvider(UUID parentId) {
		List<ClaimPayment> claimPaymentList = load(parentId).getServiceProvider;
		return claimPaymentList;
	}
	 */
    /**
     * save ServiceProvider on ClaimPayment
     * @param		command AssignServiceProviderToClaimPaymentCommand
     */     
	@PutMapping("/assignServiceProvider")
	public void assignServiceProvider( @RequestBody AssignServiceProviderToClaimPaymentCommand command ) {
		service.assignServiceProvider( command );
		LOGGER.info( "Successfully assigned ServiceProvider with Id " + command.getClaimPaymentId()  );
	}

    /**
     * unassign ServiceProvider on ClaimPayment
     * @param		 command UnAssignServiceProviderFromClaimPaymentCommand
     */     
	@PutMapping("/unassignServiceProvider")
	public void unAssignServiceProvider( @RequestBody(required=true)  UnAssignServiceProviderFromClaimPaymentCommand command ) {
		service.unAssignServiceProvider( command );
		LOGGER.info( "Successfully unassigned ServiceProvider with Id " + command.getClaimPaymentId()  );
	}
	
	/**
	 * Handles loading all Customer} business objects
	 * @return		List<ClaimPayment>

    @GetMapping("/Customer")
    public List<ClaimPayment> Customer(UUID parentId) {
		List<ClaimPayment> claimPaymentList = load(parentId).getCustomer;
		return claimPaymentList;
	}
	 */
    /**
     * save Customer on ClaimPayment
     * @param		command AssignCustomerToClaimPaymentCommand
     */     
	@PutMapping("/assignCustomer")
	public void assignCustomer( @RequestBody AssignCustomerToClaimPaymentCommand command ) {
		service.assignCustomer( command );
		LOGGER.info( "Successfully assigned Customer with Id " + command.getClaimPaymentId()  );
	}

    /**
     * unassign Customer on ClaimPayment
     * @param		 command UnAssignCustomerFromClaimPaymentCommand
     */     
	@PutMapping("/unassignCustomer")
	public void unAssignCustomer( @RequestBody(required=true)  UnAssignCustomerFromClaimPaymentCommand command ) {
		service.unAssignCustomer( command );
		LOGGER.info( "Successfully unassigned Customer with Id " + command.getClaimPaymentId()  );
	}
	




//************************************************************************    
// Attributes
//************************************************************************
    protected ClaimPayment claimPayment = null;
	protected ClaimPaymentService service = null;
    private static final Logger LOGGER = LoggerFactory.getLogger(ClaimPaymentRestController.class.getName());
    
}
