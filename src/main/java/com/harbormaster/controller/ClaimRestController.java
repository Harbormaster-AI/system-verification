
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Insurance Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.controller;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;

import java.util.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import com.harbormaster.api.*;
import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;

/**
 * ClaimRestController as REST API
 *
 * <p>Exposes REST endpoints for the Claim entity.</p>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Spring Boot 3.5</td></tr>
 *          <tr><td>published</td><td>09/12/2026</td></tr>
 *          <tr><td>design pattern</td><td>ServiceLayer</td></tr>
 *          <tr><td>architecture style</td><td>Layered</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Insurance Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core insurance product, underwriting, policy, billing, claims, and reinsurance capabilities.</td></tr>
 *          <tr><td>industry</td><td>Insurance</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services Used</h3>
 *
 *  	ClaimService
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <h3>Commands Accepted</h3>
 *      <ul>
 *      <li>CreateClaimCommand</li>
 *      <li></li>UpdateClaimCommand</li>
 *      <liDeleteClaimCommand</li>
 *      </ul>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@CrossOrigin
@RestController
@RequestMapping("/Claim")
public class ClaimRestController extends BaseSpringRestController {

	public ClaimRestController( ClaimService service ) {
		this.service = service;
	}
    /**
     * Handles create a Claim.  if not key provided, calls create, otherwise calls save
     * @param		Claim	claim
     * @return		Claim 
     */
	@PostMapping("/create")
    public Claim create( @RequestBody(required=true) CreateClaimCommand command ) {
    	Claim entity = null;

		entity = service.createClaim( command );
		LOGGER.info( "Successfully created Claim with Id " + entity.getClaimId()  );

		return entity;
    }

    /**
     * Handles updating a Claim.  if no key provided, calls create, otherwise calls save
     * @param		Claim claim
     * @return		Claim
     */
	@PutMapping("/update")
    public Claim update( @RequestBody(required=true) UpdateClaimCommand command ) {
		Claim entity = null;

		entity = service.updateClaim(command);
		LOGGER.info( "Successfully updated Claim with Id " + command.getClaimId()  );

		return entity;
	}
 
    /**
     * Handles deleting a Claim entity
     * @param		command ${class.getDeleteCommandAlias()}
     */
    @DeleteMapping("/delete")    
    public void delete( @RequestBody(required=true) DeleteClaimCommand command ) {                
		service.delete( command );
		LOGGER.info( "Successfully deleted Claim with Id " + command.getClaimId()  );
	}
	
    /**
     * Handles loading a Claim using a UUID
     * @param		UUID uuid 
     * @return		Claim
     */    
    @GetMapping("/get")
    public Claim get( @RequestParam(required=true) UUID uuid ) {
    	Claim entity = service.getClaim( new ClaimFetchOneSummary( uuid ) );

		LOGGER.info( "Successfully loaded Claim with Id " + uuid   );

        return entity;
    }

    /**
     * Handles loading all Claim business objects
     * @return		List<Claim>
     */
    @GetMapping("/")
    public List<Claim> getAll() {
    	List<Claim> claimList = null;

		claimList = service.getAllClaim();
		LOGGER.info( "successfully loaded all Claims" );

        return claimList;
                            
    }

	/**
	 * Handles loading all Policy} business objects
	 * @return		List<Claim>

    @GetMapping("/Policy")
    public List<Claim> Policy(UUID parentId) {
		List<Claim> claimList = load(parentId).getPolicy;
		return claimList;
	}
	 */
    /**
     * save Policy on Claim
     * @param		command AssignPolicyToClaimCommand
     */     
	@PutMapping("/assignPolicy")
	public void assignPolicy( @RequestBody AssignPolicyToClaimCommand command ) {
		service.assignPolicy( command );
		LOGGER.info( "Successfully assigned Policy with Id " + command.getClaimId()  );
	}

    /**
     * unassign Policy on Claim
     * @param		 command UnAssignPolicyFromClaimCommand
     */     
	@PutMapping("/unassignPolicy")
	public void unAssignPolicy( @RequestBody(required=true)  UnAssignPolicyFromClaimCommand command ) {
		service.unAssignPolicy( command );
		LOGGER.info( "Successfully unassigned Policy with Id " + command.getClaimId()  );
	}
	
	/**
	 * Handles loading all Customer} business objects
	 * @return		List<Claim>

    @GetMapping("/Customer")
    public List<Claim> Customer(UUID parentId) {
		List<Claim> claimList = load(parentId).getCustomer;
		return claimList;
	}
	 */
    /**
     * save Customer on Claim
     * @param		command AssignCustomerToClaimCommand
     */     
	@PutMapping("/assignCustomer")
	public void assignCustomer( @RequestBody AssignCustomerToClaimCommand command ) {
		service.assignCustomer( command );
		LOGGER.info( "Successfully assigned Customer with Id " + command.getClaimId()  );
	}

    /**
     * unassign Customer on Claim
     * @param		 command UnAssignCustomerFromClaimCommand
     */     
	@PutMapping("/unassignCustomer")
	public void unAssignCustomer( @RequestBody(required=true)  UnAssignCustomerFromClaimCommand command ) {
		service.unAssignCustomer( command );
		LOGGER.info( "Successfully unassigned Customer with Id " + command.getClaimId()  );
	}
	
	/**
	 * Handles loading all Adjuster} business objects
	 * @return		List<Claim>

    @GetMapping("/Adjuster")
    public List<Claim> Adjuster(UUID parentId) {
		List<Claim> claimList = load(parentId).getAdjuster;
		return claimList;
	}
	 */
    /**
     * save Adjuster on Claim
     * @param		command AssignAdjusterToClaimCommand
     */     
	@PutMapping("/assignAdjuster")
	public void assignAdjuster( @RequestBody AssignAdjusterToClaimCommand command ) {
		service.assignAdjuster( command );
		LOGGER.info( "Successfully assigned Adjuster with Id " + command.getClaimId()  );
	}

    /**
     * unassign Adjuster on Claim
     * @param		 command UnAssignAdjusterFromClaimCommand
     */     
	@PutMapping("/unassignAdjuster")
	public void unAssignAdjuster( @RequestBody(required=true)  UnAssignAdjusterFromClaimCommand command ) {
		service.unAssignAdjuster( command );
		LOGGER.info( "Successfully unassigned Adjuster with Id " + command.getClaimId()  );
	}
	
	/**
	 * Handles loading all Incident} business objects
	 * @return		List<Claim>

    @GetMapping("/Incident")
    public List<Claim> Incident(UUID parentId) {
		List<Claim> claimList = load(parentId).getIncident;
		return claimList;
	}
	 */
    /**
     * save Incident on Claim
     * @param		command AssignIncidentToClaimCommand
     */     
	@PutMapping("/assignIncident")
	public void assignIncident( @RequestBody AssignIncidentToClaimCommand command ) {
		service.assignIncident( command );
		LOGGER.info( "Successfully assigned Incident with Id " + command.getClaimId()  );
	}

    /**
     * unassign Incident on Claim
     * @param		 command UnAssignIncidentFromClaimCommand
     */     
	@PutMapping("/unassignIncident")
	public void unAssignIncident( @RequestBody(required=true)  UnAssignIncidentFromClaimCommand command ) {
		service.unAssignIncident( command );
		LOGGER.info( "Successfully unassigned Incident with Id " + command.getClaimId()  );
	}
	

	/**
	 * Handles loading all Exposures} business objects
	 * @return		List<Claim>

    @GetMapping("/Exposures")
    public List<Claim> Exposures(UUID parentId) {
		List<Claim> claimList = load(parentId).getExposures;
		return claimList;
	}

    /**
     * save Exposures on Claim
     * @param		command AssignExposuresToClaimCommand
     */     
	@PutMapping("/addToExposures")
	public void addToExposures( @RequestBody(required=true) AssignExposuresToClaimCommand command ) {
		service.addToExposures( command );
		LOGGER.info( "Successfully added Exposures with Id " + command.getClaimId()  );
	}

    /**
     * remove Exposures on Claim
     * @param		command RemoveExposuresFromClaimCommand
     */     	
	@PutMapping("/removeFromExposures")
	public void removeFromExposures( 	@RequestBody(required=true) RemoveExposuresFromClaimCommand command )
	{		
		service.removeFromExposures( command );
		LOGGER.info( "Successfully removed Exposures with Id " + command.getClaimId()  );
	}

	/**
	 * Handles loading all Reserves} business objects
	 * @return		List<Claim>

    @GetMapping("/Reserves")
    public List<Claim> Reserves(UUID parentId) {
		List<Claim> claimList = load(parentId).getReserves;
		return claimList;
	}

    /**
     * save Reserves on Claim
     * @param		command AssignReservesToClaimCommand
     */     
	@PutMapping("/addToReserves")
	public void addToReserves( @RequestBody(required=true) AssignReservesToClaimCommand command ) {
		service.addToReserves( command );
		LOGGER.info( "Successfully added Reserves with Id " + command.getClaimId()  );
	}

    /**
     * remove Reserves on Claim
     * @param		command RemoveReservesFromClaimCommand
     */     	
	@PutMapping("/removeFromReserves")
	public void removeFromReserves( 	@RequestBody(required=true) RemoveReservesFromClaimCommand command )
	{		
		service.removeFromReserves( command );
		LOGGER.info( "Successfully removed Reserves with Id " + command.getClaimId()  );
	}

	/**
	 * Handles loading all ClaimPayments} business objects
	 * @return		List<Claim>

    @GetMapping("/ClaimPayments")
    public List<Claim> ClaimPayments(UUID parentId) {
		List<Claim> claimList = load(parentId).getClaimPayments;
		return claimList;
	}

    /**
     * save ClaimPayments on Claim
     * @param		command AssignClaimPaymentsToClaimCommand
     */     
	@PutMapping("/addToClaimPayments")
	public void addToClaimPayments( @RequestBody(required=true) AssignClaimPaymentsToClaimCommand command ) {
		service.addToClaimPayments( command );
		LOGGER.info( "Successfully added ClaimPayments with Id " + command.getClaimId()  );
	}

    /**
     * remove ClaimPayments on Claim
     * @param		command RemoveClaimPaymentsFromClaimCommand
     */     	
	@PutMapping("/removeFromClaimPayments")
	public void removeFromClaimPayments( 	@RequestBody(required=true) RemoveClaimPaymentsFromClaimCommand command )
	{		
		service.removeFromClaimPayments( command );
		LOGGER.info( "Successfully removed ClaimPayments with Id " + command.getClaimId()  );
	}

	/**
	 * Handles loading all ServiceProviders} business objects
	 * @return		List<Claim>

    @GetMapping("/ServiceProviders")
    public List<Claim> ServiceProviders(UUID parentId) {
		List<Claim> claimList = load(parentId).getServiceProviders;
		return claimList;
	}

    /**
     * save ServiceProviders on Claim
     * @param		command AssignServiceProvidersToClaimCommand
     */     
	@PutMapping("/addToServiceProviders")
	public void addToServiceProviders( @RequestBody(required=true) AssignServiceProvidersToClaimCommand command ) {
		service.addToServiceProviders( command );
		LOGGER.info( "Successfully added ServiceProviders with Id " + command.getClaimId()  );
	}

    /**
     * remove ServiceProviders on Claim
     * @param		command RemoveServiceProvidersFromClaimCommand
     */     	
	@PutMapping("/removeFromServiceProviders")
	public void removeFromServiceProviders( 	@RequestBody(required=true) RemoveServiceProvidersFromClaimCommand command )
	{		
		service.removeFromServiceProviders( command );
		LOGGER.info( "Successfully removed ServiceProviders with Id " + command.getClaimId()  );
	}

	/**
	 * Handles loading all Subrogations} business objects
	 * @return		List<Claim>

    @GetMapping("/Subrogations")
    public List<Claim> Subrogations(UUID parentId) {
		List<Claim> claimList = load(parentId).getSubrogations;
		return claimList;
	}

    /**
     * save Subrogations on Claim
     * @param		command AssignSubrogationsToClaimCommand
     */     
	@PutMapping("/addToSubrogations")
	public void addToSubrogations( @RequestBody(required=true) AssignSubrogationsToClaimCommand command ) {
		service.addToSubrogations( command );
		LOGGER.info( "Successfully added Subrogations with Id " + command.getClaimId()  );
	}

    /**
     * remove Subrogations on Claim
     * @param		command RemoveSubrogationsFromClaimCommand
     */     	
	@PutMapping("/removeFromSubrogations")
	public void removeFromSubrogations( 	@RequestBody(required=true) RemoveSubrogationsFromClaimCommand command )
	{		
		service.removeFromSubrogations( command );
		LOGGER.info( "Successfully removed Subrogations with Id " + command.getClaimId()  );
	}




//************************************************************************    
// Attributes
//************************************************************************
    protected Claim claim = null;
	protected ClaimService service = null;
    private static final Logger LOGGER = LoggerFactory.getLogger(ClaimRestController.class.getName());
    
}
