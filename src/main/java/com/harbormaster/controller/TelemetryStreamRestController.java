
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.controller;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;

import java.util.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import com.harbormaster.api.*;
import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;

/**
 * TelemetryStreamRestController as REST API
 *
 * <p>Exposes REST endpoints for the TelemetryStream entity.</p>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Spring Boot 3.5</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>ServiceLayer</td></tr>
 *          <tr><td>architecture style</td><td>Layered</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services Used</h3>
 *
 *  	TelemetryStreamService
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <h3>Commands Accepted</h3>
 *      <ul>
 *      <li>CreateTelemetryStreamCommand</li>
 *      <li></li>UpdateTelemetryStreamCommand</li>
 *      <liDeleteTelemetryStreamCommand</li>
 *      </ul>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@CrossOrigin
@RestController
@RequestMapping("/TelemetryStream")
public class TelemetryStreamRestController extends BaseSpringRestController {

	public TelemetryStreamRestController( TelemetryStreamService service ) {
		this.service = service;
	}
    /**
     * Handles create a TelemetryStream.  if not key provided, calls create, otherwise calls save
     * @param		TelemetryStream	telemetryStream
     * @return		TelemetryStream 
     */
	@PostMapping("/create")
    public TelemetryStream create( @RequestBody(required=true) CreateTelemetryStreamCommand command ) {
    	TelemetryStream entity = null;

		entity = service.createTelemetryStream( command );
		LOGGER.info( "Successfully created TelemetryStream with Id " + entity.getTelemetryStreamId()  );

		return entity;
    }

    /**
     * Handles updating a TelemetryStream.  if no key provided, calls create, otherwise calls save
     * @param		TelemetryStream telemetryStream
     * @return		TelemetryStream
     */
	@PutMapping("/update")
    public TelemetryStream update( @RequestBody(required=true) UpdateTelemetryStreamCommand command ) {
		TelemetryStream entity = null;

		entity = service.updateTelemetryStream(command);
		LOGGER.info( "Successfully updated TelemetryStream with Id " + command.getTelemetryStreamId()  );

		return entity;
	}
 
    /**
     * Handles deleting a TelemetryStream entity
     * @param		command ${class.getDeleteCommandAlias()}
     */
    @DeleteMapping("/delete")    
    public void delete( @RequestBody(required=true) DeleteTelemetryStreamCommand command ) {                
		service.delete( command );
		LOGGER.info( "Successfully deleted TelemetryStream with Id " + command.getTelemetryStreamId()  );
	}
	
    /**
     * Handles loading a TelemetryStream using a UUID
     * @param		UUID uuid 
     * @return		TelemetryStream
     */    
    @GetMapping("/get")
    public TelemetryStream get( @RequestParam(required=true) UUID uuid ) {
    	TelemetryStream entity = service.getTelemetryStream( new TelemetryStreamFetchOneSummary( uuid ) );

		LOGGER.info( "Successfully loaded TelemetryStream with Id " + uuid   );

        return entity;
    }

    /**
     * Handles loading all TelemetryStream business objects
     * @return		List<TelemetryStream>
     */
    @GetMapping("/")
    public List<TelemetryStream> getAll() {
    	List<TelemetryStream> telemetryStreamList = null;

		telemetryStreamList = service.getAllTelemetryStream();
		LOGGER.info( "successfully loaded all TelemetryStreams" );

        return telemetryStreamList;
                            
    }

	/**
	 * Handles loading all Device} business objects
	 * @return		List<TelemetryStream>

    @GetMapping("/Device")
    public List<TelemetryStream> Device(UUID parentId) {
		List<TelemetryStream> telemetryStreamList = load(parentId).getDevice;
		return telemetryStreamList;
	}
	 */
    /**
     * save Device on TelemetryStream
     * @param		command AssignDeviceToTelemetryStreamCommand
     */     
	@PutMapping("/assignDevice")
	public void assignDevice( @RequestBody AssignDeviceToTelemetryStreamCommand command ) {
		service.assignDevice( command );
		LOGGER.info( "Successfully assigned Device with Id " + command.getTelemetryStreamId()  );
	}

    /**
     * unassign Device on TelemetryStream
     * @param		 command UnAssignDeviceFromTelemetryStreamCommand
     */     
	@PutMapping("/unassignDevice")
	public void unAssignDevice( @RequestBody(required=true)  UnAssignDeviceFromTelemetryStreamCommand command ) {
		service.unAssignDevice( command );
		LOGGER.info( "Successfully unassigned Device with Id " + command.getTelemetryStreamId()  );
	}
	
	/**
	 * Handles loading all Sensor} business objects
	 * @return		List<TelemetryStream>

    @GetMapping("/Sensor")
    public List<TelemetryStream> Sensor(UUID parentId) {
		List<TelemetryStream> telemetryStreamList = load(parentId).getSensor;
		return telemetryStreamList;
	}
	 */
    /**
     * save Sensor on TelemetryStream
     * @param		command AssignSensorToTelemetryStreamCommand
     */     
	@PutMapping("/assignSensor")
	public void assignSensor( @RequestBody AssignSensorToTelemetryStreamCommand command ) {
		service.assignSensor( command );
		LOGGER.info( "Successfully assigned Sensor with Id " + command.getTelemetryStreamId()  );
	}

    /**
     * unassign Sensor on TelemetryStream
     * @param		 command UnAssignSensorFromTelemetryStreamCommand
     */     
	@PutMapping("/unassignSensor")
	public void unAssignSensor( @RequestBody(required=true)  UnAssignSensorFromTelemetryStreamCommand command ) {
		service.unAssignSensor( command );
		LOGGER.info( "Successfully unassigned Sensor with Id " + command.getTelemetryStreamId()  );
	}
	
	/**
	 * Handles loading all Schema} business objects
	 * @return		List<TelemetryStream>

    @GetMapping("/Schema")
    public List<TelemetryStream> Schema(UUID parentId) {
		List<TelemetryStream> telemetryStreamList = load(parentId).getSchema;
		return telemetryStreamList;
	}
	 */
    /**
     * save Schema on TelemetryStream
     * @param		command AssignSchemaToTelemetryStreamCommand
     */     
	@PutMapping("/assignSchema")
	public void assignSchema( @RequestBody AssignSchemaToTelemetryStreamCommand command ) {
		service.assignSchema( command );
		LOGGER.info( "Successfully assigned Schema with Id " + command.getTelemetryStreamId()  );
	}

    /**
     * unassign Schema on TelemetryStream
     * @param		 command UnAssignSchemaFromTelemetryStreamCommand
     */     
	@PutMapping("/unassignSchema")
	public void unAssignSchema( @RequestBody(required=true)  UnAssignSchemaFromTelemetryStreamCommand command ) {
		service.unAssignSchema( command );
		LOGGER.info( "Successfully unassigned Schema with Id " + command.getTelemetryStreamId()  );
	}
	
	/**
	 * Handles loading all MessagingEndpoint} business objects
	 * @return		List<TelemetryStream>

    @GetMapping("/MessagingEndpoint")
    public List<TelemetryStream> MessagingEndpoint(UUID parentId) {
		List<TelemetryStream> telemetryStreamList = load(parentId).getMessagingEndpoint;
		return telemetryStreamList;
	}
	 */
    /**
     * save MessagingEndpoint on TelemetryStream
     * @param		command AssignMessagingEndpointToTelemetryStreamCommand
     */     
	@PutMapping("/assignMessagingEndpoint")
	public void assignMessagingEndpoint( @RequestBody AssignMessagingEndpointToTelemetryStreamCommand command ) {
		service.assignMessagingEndpoint( command );
		LOGGER.info( "Successfully assigned MessagingEndpoint with Id " + command.getTelemetryStreamId()  );
	}

    /**
     * unassign MessagingEndpoint on TelemetryStream
     * @param		 command UnAssignMessagingEndpointFromTelemetryStreamCommand
     */     
	@PutMapping("/unassignMessagingEndpoint")
	public void unAssignMessagingEndpoint( @RequestBody(required=true)  UnAssignMessagingEndpointFromTelemetryStreamCommand command ) {
		service.unAssignMessagingEndpoint( command );
		LOGGER.info( "Successfully unassigned MessagingEndpoint with Id " + command.getTelemetryStreamId()  );
	}
	
	/**
	 * Handles loading all RetentionPolicy} business objects
	 * @return		List<TelemetryStream>

    @GetMapping("/RetentionPolicy")
    public List<TelemetryStream> RetentionPolicy(UUID parentId) {
		List<TelemetryStream> telemetryStreamList = load(parentId).getRetentionPolicy;
		return telemetryStreamList;
	}
	 */
    /**
     * save RetentionPolicy on TelemetryStream
     * @param		command AssignRetentionPolicyToTelemetryStreamCommand
     */     
	@PutMapping("/assignRetentionPolicy")
	public void assignRetentionPolicy( @RequestBody AssignRetentionPolicyToTelemetryStreamCommand command ) {
		service.assignRetentionPolicy( command );
		LOGGER.info( "Successfully assigned RetentionPolicy with Id " + command.getTelemetryStreamId()  );
	}

    /**
     * unassign RetentionPolicy on TelemetryStream
     * @param		 command UnAssignRetentionPolicyFromTelemetryStreamCommand
     */     
	@PutMapping("/unassignRetentionPolicy")
	public void unAssignRetentionPolicy( @RequestBody(required=true)  UnAssignRetentionPolicyFromTelemetryStreamCommand command ) {
		service.unAssignRetentionPolicy( command );
		LOGGER.info( "Successfully unassigned RetentionPolicy with Id " + command.getTelemetryStreamId()  );
	}
	




//************************************************************************    
// Attributes
//************************************************************************
    protected TelemetryStream telemetryStream = null;
	protected TelemetryStreamService service = null;
    private static final Logger LOGGER = LoggerFactory.getLogger(TelemetryStreamRestController.class.getName());
    
}
