
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.controller;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;

import java.util.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import com.harbormaster.api.*;
import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;

/**
 * SimCardRestController as REST API
 *
 * <p>Exposes REST endpoints for the SimCard entity.</p>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Spring Boot 3.5</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>ServiceLayer</td></tr>
 *          <tr><td>architecture style</td><td>Layered</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services Used</h3>
 *
 *  	SimCardService
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <h3>Commands Accepted</h3>
 *      <ul>
 *      <li>CreateSimCardCommand</li>
 *      <li></li>UpdateSimCardCommand</li>
 *      <liDeleteSimCardCommand</li>
 *      </ul>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@CrossOrigin
@RestController
@RequestMapping("/SimCard")
public class SimCardRestController extends BaseSpringRestController {

	public SimCardRestController( SimCardService service ) {
		this.service = service;
	}
    /**
     * Handles create a SimCard.  if not key provided, calls create, otherwise calls save
     * @param		SimCard	simCard
     * @return		SimCard 
     */
	@PostMapping("/create")
    public SimCard create( @RequestBody(required=true) CreateSimCardCommand command ) {
    	SimCard entity = null;

		entity = service.createSimCard( command );
		LOGGER.info( "Successfully created SimCard with Id " + entity.getSimCardId()  );

		return entity;
    }

    /**
     * Handles updating a SimCard.  if no key provided, calls create, otherwise calls save
     * @param		SimCard simCard
     * @return		SimCard
     */
	@PutMapping("/update")
    public SimCard update( @RequestBody(required=true) UpdateSimCardCommand command ) {
		SimCard entity = null;

		entity = service.updateSimCard(command);
		LOGGER.info( "Successfully updated SimCard with Id " + command.getSimCardId()  );

		return entity;
	}
 
    /**
     * Handles deleting a SimCard entity
     * @param		command ${class.getDeleteCommandAlias()}
     */
    @DeleteMapping("/delete")    
    public void delete( @RequestBody(required=true) DeleteSimCardCommand command ) {                
		service.delete( command );
		LOGGER.info( "Successfully deleted SimCard with Id " + command.getSimCardId()  );
	}
	
    /**
     * Handles loading a SimCard using a UUID
     * @param		UUID uuid 
     * @return		SimCard
     */    
    @GetMapping("/get")
    public SimCard get( @RequestParam(required=true) UUID uuid ) {
    	SimCard entity = service.getSimCard( new SimCardFetchOneSummary( uuid ) );

		LOGGER.info( "Successfully loaded SimCard with Id " + uuid   );

        return entity;
    }

    /**
     * Handles loading all SimCard business objects
     * @return		List<SimCard>
     */
    @GetMapping("/")
    public List<SimCard> getAll() {
    	List<SimCard> simCardList = null;

		simCardList = service.getAllSimCard();
		LOGGER.info( "successfully loaded all SimCards" );

        return simCardList;
                            
    }

	/**
	 * Handles loading all Tenant} business objects
	 * @return		List<SimCard>

    @GetMapping("/Tenant")
    public List<SimCard> Tenant(UUID parentId) {
		List<SimCard> simCardList = load(parentId).getTenant;
		return simCardList;
	}
	 */
    /**
     * save Tenant on SimCard
     * @param		command AssignTenantToSimCardCommand
     */     
	@PutMapping("/assignTenant")
	public void assignTenant( @RequestBody AssignTenantToSimCardCommand command ) {
		service.assignTenant( command );
		LOGGER.info( "Successfully assigned Tenant with Id " + command.getSimCardId()  );
	}

    /**
     * unassign Tenant on SimCard
     * @param		 command UnAssignTenantFromSimCardCommand
     */     
	@PutMapping("/unassignTenant")
	public void unAssignTenant( @RequestBody(required=true)  UnAssignTenantFromSimCardCommand command ) {
		service.unAssignTenant( command );
		LOGGER.info( "Successfully unassigned Tenant with Id " + command.getSimCardId()  );
	}
	
	/**
	 * Handles loading all ConnectivityPlan} business objects
	 * @return		List<SimCard>

    @GetMapping("/ConnectivityPlan")
    public List<SimCard> ConnectivityPlan(UUID parentId) {
		List<SimCard> simCardList = load(parentId).getConnectivityPlan;
		return simCardList;
	}
	 */
    /**
     * save ConnectivityPlan on SimCard
     * @param		command AssignConnectivityPlanToSimCardCommand
     */     
	@PutMapping("/assignConnectivityPlan")
	public void assignConnectivityPlan( @RequestBody AssignConnectivityPlanToSimCardCommand command ) {
		service.assignConnectivityPlan( command );
		LOGGER.info( "Successfully assigned ConnectivityPlan with Id " + command.getSimCardId()  );
	}

    /**
     * unassign ConnectivityPlan on SimCard
     * @param		 command UnAssignConnectivityPlanFromSimCardCommand
     */     
	@PutMapping("/unassignConnectivityPlan")
	public void unAssignConnectivityPlan( @RequestBody(required=true)  UnAssignConnectivityPlanFromSimCardCommand command ) {
		service.unAssignConnectivityPlan( command );
		LOGGER.info( "Successfully unassigned ConnectivityPlan with Id " + command.getSimCardId()  );
	}
	

	/**
	 * Handles loading all NetworkProfiles} business objects
	 * @return		List<SimCard>

    @GetMapping("/NetworkProfiles")
    public List<SimCard> NetworkProfiles(UUID parentId) {
		List<SimCard> simCardList = load(parentId).getNetworkProfiles;
		return simCardList;
	}

    /**
     * save NetworkProfiles on SimCard
     * @param		command AssignNetworkProfilesToSimCardCommand
     */     
	@PutMapping("/addToNetworkProfiles")
	public void addToNetworkProfiles( @RequestBody(required=true) AssignNetworkProfilesToSimCardCommand command ) {
		service.addToNetworkProfiles( command );
		LOGGER.info( "Successfully added NetworkProfiles with Id " + command.getSimCardId()  );
	}

    /**
     * remove NetworkProfiles on SimCard
     * @param		command RemoveNetworkProfilesFromSimCardCommand
     */     	
	@PutMapping("/removeFromNetworkProfiles")
	public void removeFromNetworkProfiles( 	@RequestBody(required=true) RemoveNetworkProfilesFromSimCardCommand command )
	{		
		service.removeFromNetworkProfiles( command );
		LOGGER.info( "Successfully removed NetworkProfiles with Id " + command.getSimCardId()  );
	}




//************************************************************************    
// Attributes
//************************************************************************
    protected SimCard simCard = null;
	protected SimCardService service = null;
    private static final Logger LOGGER = LoggerFactory.getLogger(SimCardRestController.class.getName());
    
}
