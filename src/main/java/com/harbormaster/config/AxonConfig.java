/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.harbormaster.config;

import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.databind.SerializationFeature;
import com.fasterxml.jackson.datatype.jsr310.JavaTimeModule;
import org.axonframework.commandhandling.CommandBus;
import org.axonframework.common.caching.Cache;
import org.axonframework.common.caching.WeakReferenceCache;
import org.axonframework.config.EventProcessingConfigurer;
import org.axonframework.eventhandling.EventBus;
import org.axonframework.messaging.Message;
import org.axonframework.messaging.interceptors.LoggingInterceptor;
import org.axonframework.queryhandling.QueryBus;
import org.axonframework.serialization.Serializer;
import org.axonframework.serialization.json.JacksonSerializer;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Qualifier;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import springfox.documentation.builders.PathSelectors;
import springfox.documentation.builders.RequestHandlerSelectors;
import springfox.documentation.spi.DocumentationType;
import springfox.documentation.spring.web.plugins.Docket;
import springfox.documentation.swagger2.annotations.EnableSwagger2;

@Configuration
public class AxonConfig {

	@Bean
	public Docket api() {
		return new Docket(DocumentationType.SWAGGER_2)
		          .select()                                  
		          .apis(RequestHandlerSelectors.any())              
		          .paths(PathSelectors.any())                          
		          .build(); 
	}
	
    @Bean
    public LoggingInterceptor<Message<?>> loggingInterceptor() {
        return new LoggingInterceptor<>();
    }

    @Autowired
    public void configureLoggingInterceptorFor(CommandBus commandBus,
                                               LoggingInterceptor<Message<?>> loggingInterceptor) {
        commandBus.registerDispatchInterceptor(loggingInterceptor);
        commandBus.registerHandlerInterceptor(loggingInterceptor);
    }

    @Autowired
    public void configureLoggingInterceptorFor(EventBus eventBus, LoggingInterceptor<Message<?>> loggingInterceptor) {
        eventBus.registerDispatchInterceptor(loggingInterceptor);
    }

    @Autowired
    public void configureLoggingInterceptorFor(EventProcessingConfigurer eventProcessingConfigurer,
                                               LoggingInterceptor<Message<?>> loggingInterceptor) {
        eventProcessingConfigurer.registerDefaultHandlerInterceptor((config, processorName) -> loggingInterceptor);
    }

    @Autowired
    public void configureLoggingInterceptorFor(QueryBus queryBus, LoggingInterceptor<Message<?>> loggingInterceptor) {
        queryBus.registerDispatchInterceptor(loggingInterceptor);
        queryBus.registerHandlerInterceptor(loggingInterceptor);
    }

    // ------------------------------------------------
    // create a cache for each command type
    // ------------------------------------------------
    
    @Bean
    public Cache deviceVendorCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache hardwareModuleCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache deviceModelCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache firmwareReleaseCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache ioTDeviceCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache sensorInstanceCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache actuatorInstanceCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache telemetrySchemaCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache telemetryStreamCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache commandDefinitionCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache commandInvocationCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache alertRuleCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache alertCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache tenantCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache tenantUserCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache siteCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache buildingCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache floorCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache roomCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache gatewayCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache edgeApplicationCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache networkProfileCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache simCardCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache connectivityPlanCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache messagingEndpointCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache accessPolicyCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache apiKeyCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache deviceCertificateCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache provisioningRecordCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache digitalTwinCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache twinTemplateCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache twinChangeEventCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache maintenanceTicketCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache dataRetentionPolicyCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache softwareUpdateCampaignCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache softwareUpdateExecutionCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache deviceGroupCache() {    	
        return new WeakReferenceCache();
    }

    @Bean
    public Cache usageRecordCache() {    	
        return new WeakReferenceCache();
    }


}
