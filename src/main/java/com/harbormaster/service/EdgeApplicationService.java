/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */

package com.harbormaster.service;

import java.io.IOException;
import java.util.*;
import java.util.concurrent.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import org.springframework.stereotype.Service;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;


import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.repository.*;
import com.harbormaster.validator.*;
import com.harbormaster.projector.*;
import com.harbormaster.security.*;

/**
 *
 * <h2>EdgeApplication business delegate</h2>
 *
 * <p>
 * This class implements the Business Delegate design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of EdgeApplication related services in the case of a EdgeApplication business related service failing.</li>
 * <li>Exposes a simpler, uniform EdgeApplication interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill EdgeApplication business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Spring Boot 3.5</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>ServiceLayer</td></tr>
 *          <tr><td>architecture style</td><td>Layered</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@Service
public class EdgeApplicationService 
		extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
	/**
	 * Default Constructor
	 */
    public EdgeApplicationService(CurrentIdentity identity,
				ApplicationContext applicationContext)  {

		this.identity		= identity;
		this.projector 		= new EdgeApplicationEntityProjector( applicationContext.getBean(ProjectorRegistry.class),
										applicationContext.getBean(EdgeApplicationRepository.class) );
		this.validator		= applicationContext.getBean(EdgeApplicationValidator.class) ;
	}


	/**
	 * Creates the provided command.
	 *
	 * @param		command ${class.getCreateCommandAlias()}
	 * @return		EdgeApplication
	 */
		public EdgeApplication createEdgeApplication( CreateEdgeApplicationCommand command ) {

		EdgeApplication entity = new EdgeApplication();

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

            entity.setEdgeApplicationId( command.getEdgeApplicationId() );
            entity.setName( command.getName() );
            entity.setVersion( command.getVersion() );
            entity.setImage( command.getImage() );
            entity.setStatus( command.getStatus() );

		// ------------------------------------------
		// persist a new one
		// ------------------------------------------
		entity = projector.create(entity);

		LOGGER.info( "done creating of EdgeApplication {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Update the provided command.
	 * @param		command UpdateEdgeApplicationCommand
	 * @return		EdgeApplication
	 */
	public EdgeApplication updateEdgeApplication( UpdateEdgeApplicationCommand command ) {

		EdgeApplication entity = new EdgeApplication();

		// --------------------------------------
		// validate
		// --------------------------------------
		validator.validate( command );

            entity.setEdgeApplicationId( command.getEdgeApplicationId() );
            entity.setName( command.getName() );
            entity.setVersion( command.getVersion() );
            entity.setImage( command.getImage() );
            entity.setGateway( command.getGateway() );
            entity.setStatus( command.getStatus() );

		// ------------------------------------------
		// persist an existing one
		// ------------------------------------------
		entity = projector.update(entity);

		LOGGER.info( "done saving of EdgeApplication {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Deletes the associatied value object
	 * @param		command DeleteEdgeApplicationCommand
	 */
	public void delete( DeleteEdgeApplicationCommand command ) {
		UUID id = null;

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		id = command.getEdgeApplicationId();

		// ------------------------------------------
		// delete the entity
		// ------------------------------------------
		projector.delete(id);

		LOGGER.info( "done deleting of EdgeApplication {0} ", id );

	}

	/**
	 * Method to retrieve the EdgeApplication via EdgeApplicationFetchOneSummary
	 * @param 	summary EdgeApplicationFetchOneSummary
	 * @return 	EdgeApplicationFetchOneResponse
	 */
	public EdgeApplication getEdgeApplication( EdgeApplicationFetchOneSummary summary ) {

		if( summary == null )
			throw new IllegalStateException( "EdgeApplicationFetchOneSummary arg cannot be null" );

		EdgeApplication entity = null;
		UUID id = summary.getEdgeApplicationId();

		// --------------------------------------
		// validate the fetch one summary
		// --------------------------------------
		validator.validate( summary );

		// --------------------------------------
		// find a EdgeApplication using the provided id
		// --------------------------------------
		entity = projector.find( id );

		return entity;
	}


	/**
	 * Method to retrieve a collection of all EdgeApplications
	 *
	 * @return 	List<EdgeApplication>
	 */
    public List<EdgeApplication> getAllEdgeApplication() {
		List<EdgeApplication> list = projector.findAll( new FindAllEdgeApplicationQuery() );
		return list;
	}

	/**
	 * assign Gateway on EdgeApplication
	 * @param		command AssignGatewayToEdgeApplicationCommand
	 */
	public void assignGateway( AssignGatewayToEdgeApplicationCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignGateway(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Gateway on EdgeApplication
	 * @param		command UnAssignGatewayFromEdgeApplicationCommand
	 */
	public void unAssignGateway( UnAssignGatewayFromEdgeApplicationCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignGateway(command.getChildId());
	}
	




//************************************************************************
// Attributes
//************************************************************************
	@Autowired
    private final EdgeApplicationEntityProjector projector;
	private final EdgeApplicationValidator validator;
	private EdgeApplication edgeApplication 	= null;
	private CurrentIdentity identity			= null;
	private static final Logger LOGGER 			=  LoggerFactory.getLogger(EdgeApplicationService.class);
}
