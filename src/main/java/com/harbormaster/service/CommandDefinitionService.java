/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.harbormaster.service;

import java.io.IOException;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.*;
import java.util.concurrent.*;

import org.axonframework.commandhandling.gateway.CommandGateway;
import org.axonframework.queryhandling.QueryGateway;
import org.axonframework.queryhandling.QueryUpdateEmitter;
import org.axonframework.messaging.responsetypes.ResponseTypes;
import org.springframework.stereotype.Service;
import org.springframework.beans.BeansException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;

import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.validator.*;

/**
 *
 * <h2>CommandDefinition business service</h2>
 *
 * <p>
 * This class implements the Service design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of CommandDefinition related services in the case of a CommandDefinition business related service failing.</li>
 * <li>Exposes a simpler, uniform CommandDefinition interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill CommandDefinition business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@Service
public class CommandDefinitionService 
extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
    /** 
     * Default Constructor 
     */
    public CommandDefinitionService()  {
	}

	@Override
	public void setApplicationContext(ApplicationContext ctx) throws BeansException {
		super.setApplicationContext(ctx);

		queryGateway 		= ctx.getBean(QueryGateway.class);
		commandGateway 		= ctx.getBean(CommandGateway.class);
		queryUpdateEmitter  = ctx.getBean(QueryUpdateEmitter.class);
		validator			= ctx.getBean(CommandDefinitionValidator.class);
	}


   /**
	* CommandDefinition Business Delegate Factory Method
	*
	* All methods are expected to be self-sufficient.
	*
	* @return 	CommandDefinitionService
	*/
	public static CommandDefinitionService getCommandDefinitionInstance() {
		return( new CommandDefinitionService() );
	}
 
   /**
    * Creates the provided command.
    * 
    * @param		command ${class.getCreateCommandAlias()}
    * @exception    ProcessingException
    * @exception	IllegalArgumentException
    * @return		CompletableFuture<UUID>
    */
	public CompletableFuture<UUID> createCommandDefinition( CreateCommandDefinitionCommand command )
    throws ProcessingException, IllegalArgumentException {

		CompletableFuture<UUID> completableFuture = null;
				
		try {
			// --------------------------------------
        	// assign identity now if none
        	// -------------------------------------- 
			if ( command.getCommandDefinitionId() == null )
				command.setCommandDefinitionId( UUID.randomUUID() );
				
			// --------------------------------------
        	// validate the command
        	// --------------------------------------    	
        	validator.validate( command );    

    		// ---------------------------------------
    		// issue the CreateCommandDefinitionCommand - by convention the future return value for a create command
        	// that is handled by the constructor of an aggregate will return the UUID 
    		// ---------------------------------------
        	completableFuture = commandGateway.send( command );
        	
			LOGGER.log( Level.INFO, "return from Command Gateway for CreateCommandDefinitionCommand of CommandDefinition is " + command );
			
        }
        catch (Exception exc) {
            final String errMsg = "Unable to create CommandDefinition - " + exc;
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }        
        
        return completableFuture;
    }

   /**
    * Update the provided command.
    * @param		command UpdateCommandDefinitionCommand
    * @return		CompletableFuture<Void>
    * @exception    ProcessingException
    * @exception  	IllegalArgumentException
    */
    public CompletableFuture<Void> updateCommandDefinition( UpdateCommandDefinitionCommand command ) 
    throws ProcessingException, IllegalArgumentException {
    	CompletableFuture<Void> completableFuture = null;
    	
    	try {       

			// --------------------------------------
        	// validate 
        	// --------------------------------------    	
        	validator.validate( command );    

        	// --------------------------------------
        	// issue the UpdateCommandDefinitionCommand and return right away
        	// --------------------------------------    	
        	completableFuture = commandGateway.send( command );
    	}
        catch (Exception exc) {
            final String errMsg = "Unable to save CommandDefinition - " + exc;
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        
    	return completableFuture;
    }
   
   /**
    * Deletes the associatied value object
    * @param		command DeleteCommandDefinitionCommand
    * @return		CompletableFuture<Void>
    * @exception 	ProcessingException
    */
    public CompletableFuture<Void> delete( DeleteCommandDefinitionCommand command ) 
    throws ProcessingException, IllegalArgumentException {	
    	
    	CompletableFuture<Void> completableFuture = null;
    	
        try {  
			// --------------------------------------
        	// validate the command
        	// --------------------------------------    	
        	validator.validate( command );    
        	
        	// --------------------------------------
        	// issue the DeleteCommandDefinitionCommand and return right away
        	// --------------------------------------    	
        	completableFuture = commandGateway.send( command );
        }
        catch (Exception exc) {
            final String errMsg = "Unable to delete CommandDefinition using Id = "  + command.getCommandDefinitionId();
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }
        
        return completableFuture;
    }

    /**
     * Method to retrieve the CommandDefinition via CommandDefinitionFetchOneSummary
     * @param 	summary CommandDefinitionFetchOneSummary 
     * @return 	CommandDefinitionFetchOneResponse
     * @exception ProcessingException - Thrown if processing any related problems
     * @exception IllegalArgumentException 
     */
    public CommandDefinition getCommandDefinition( CommandDefinitionFetchOneSummary summary ) 
    throws ProcessingException, IllegalArgumentException {
    	
    	if( summary == null )
    		throw new IllegalArgumentException( "CommandDefinitionFetchOneSummary arg cannot be null" );
    	
    	CommandDefinition entity = null;
    	
        try {
        	// --------------------------------------
        	// validate the fetch one summary
        	// --------------------------------------    	
        	validator.validate( summary );    
        	
        	// --------------------------------------
        	// use queryGateway to send request to Find a CommandDefinition
        	// --------------------------------------
        	CompletableFuture<CommandDefinition> futureEntity = queryGateway.query(new FindCommandDefinitionQuery( new LoadCommandDefinitionFilter( summary.getCommandDefinitionId() ) ), ResponseTypes.instanceOf(CommandDefinition.class));
        	
        	entity = futureEntity.get();
        }
        catch( Exception exc ) {
            final String errMsg = "Unable to locate CommandDefinition with id " + summary.getCommandDefinitionId();
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }        
        
        return entity;
    }


    /**
     * Method to retrieve a collection of all CommandDefinitions
     *
     * @return 	List<CommandDefinition> 
     * @exception ProcessingException Thrown if any problems
     */
    public List<CommandDefinition> getAllCommandDefinition() 
    throws ProcessingException {
        List<CommandDefinition> list = null;

        try {
        	CompletableFuture<List<CommandDefinition>> futureList = queryGateway.query(new FindAllCommandDefinitionQuery(), ResponseTypes.multipleInstancesOf(CommandDefinition.class));
        	
        	list = futureList.get();
        }
        catch( Exception exc ) {
            String errMsg = "Failed to get all CommandDefinition";
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }        
        
        return list;
    }

    /**
     * assign DeviceModel on CommandDefinition
     * @param		command AssignDeviceModelToCommandDefinitionCommand	
     * @exception	ProcessingException
     */     
	public void assignDeviceModel( AssignDeviceModelToCommandDefinitionCommand command ) throws ProcessingException {

		// --------------------------------------------
		// load the parent
		// --------------------------------------------
		load( command.getCommandDefinitionId() );
		
		DeviceModelService childDelegate 	= DeviceModelService.getDeviceModelInstance();
		CommandDefinitionService parentDelegate = CommandDefinitionService.getCommandDefinitionInstance();			
		UUID childId = command.getAssignment().getDeviceModelId();
		DeviceModel child = null;
		
		try {
			// --------------------------------------
	    	// best to validate the command now
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );

		}
        catch( Throwable exc ) {
			final String msg = "Failed to get DeviceModel using id " + childId;
			LOGGER.log( Level.WARNING,  msg );
			throw new ProcessingException( msg, exc );
        }
	}

    /**
     * unAssign DeviceModel on CommandDefinition
     * @param		command UnAssignDeviceModelFromCommandDefinitionCommand
     * @exception	ProcessingException
     */     
	public void unAssignDeviceModel( UnAssignDeviceModelFromCommandDefinitionCommand command ) throws ProcessingException {

		try {
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    
	
	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );
		}
		catch( Exception exc ) {
			final String msg = "Failed to unassign DeviceModel on CommandDefinition";
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}
	

    /**
     * add ActuatorInstance to Actuators 
     * @param		command AssignActuatorsToCommandDefinitionCommand
     * @exception	ProcessingException
     */     
	public void addToActuators( AssignActuatorsToCommandDefinitionCommand command ) throws ProcessingException {
		
		
		// -------------------------------------------
		// load the parent
		// -------------------------------------------
		load( command.getCommandDefinitionId() );

		ActuatorInstanceService childDelegate 	= ActuatorInstanceService.getActuatorInstanceInstance();
		CommandDefinitionService parentDelegate = CommandDefinitionService.getCommandDefinitionInstance();		
		UUID childId = command.getAddTo().getActuatorInstanceId();
		
		try {		
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );			
		}
		catch( Exception exc ) {
			final String msg = "Failed to add a ActuatorInstance as Actuators to CommandDefinition" ; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}

	}

    /**
     * remove ActuatorInstance from Actuators
     * @param		command RemoveActuatorsFromCommandDefinitionCommand
     * @exception	ProcessingException
     */     	
	public void removeFromActuators( RemoveActuatorsFromCommandDefinitionCommand command ) throws ProcessingException {		
		
		ActuatorInstanceService childDelegate 	= ActuatorInstanceService.getActuatorInstanceInstance();
		UUID childId = command.getRemoveFrom().getActuatorInstanceId();

		try {
			
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );

		}
		catch( Exception exc ) {
			final String msg = "Failed to remove child using Id " + childId; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}

    /**
     * add CommandInvocation to CommandInvocations 
     * @param		command AssignCommandInvocationsToCommandDefinitionCommand
     * @exception	ProcessingException
     */     
	public void addToCommandInvocations( AssignCommandInvocationsToCommandDefinitionCommand command ) throws ProcessingException {
		
		
		// -------------------------------------------
		// load the parent
		// -------------------------------------------
		load( command.getCommandDefinitionId() );

		CommandInvocationService childDelegate 	= CommandInvocationService.getCommandInvocationInstance();
		CommandDefinitionService parentDelegate = CommandDefinitionService.getCommandDefinitionInstance();		
		UUID childId = command.getAddTo().getCommandInvocationId();
		
		try {		
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );			
		}
		catch( Exception exc ) {
			final String msg = "Failed to add a CommandInvocation as CommandInvocations to CommandDefinition" ; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}

	}

    /**
     * remove CommandInvocation from CommandInvocations
     * @param		command RemoveCommandInvocationsFromCommandDefinitionCommand
     * @exception	ProcessingException
     */     	
	public void removeFromCommandInvocations( RemoveCommandInvocationsFromCommandDefinitionCommand command ) throws ProcessingException {		
		
		CommandInvocationService childDelegate 	= CommandInvocationService.getCommandInvocationInstance();
		UUID childId = command.getRemoveFrom().getCommandInvocationId();

		try {
			
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );

		}
		catch( Exception exc ) {
			final String msg = "Failed to remove child using Id " + childId; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}



	/**
	 * Internal helper method to load the root 
	 * 
	 * @param		id	UUID
	 * @return		CommandDefinition
	 */
	protected CommandDefinition load( UUID id ) throws ProcessingException {
		commandDefinition = CommandDefinitionService.getCommandDefinitionInstance().getCommandDefinition( new CommandDefinitionFetchOneSummary(id) );	
		return commandDefinition;
	}


//************************************************************************
// Attributes
//************************************************************************
	private QueryGateway queryGateway;
	private CommandGateway commandGateway;
	private QueryUpdateEmitter queryUpdateEmitter;
	private CommandDefinitionValidator validator;
	private CommandDefinition commandDefinition 	= null;
    private static final Logger LOGGER 			= Logger.getLogger(CommandDefinitionService.class.getName());
    
}
