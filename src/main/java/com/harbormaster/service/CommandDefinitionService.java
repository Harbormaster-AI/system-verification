/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */

package com.harbormaster.service;

import java.io.IOException;
import java.util.*;
import java.util.concurrent.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import org.springframework.stereotype.Service;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;


import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.repository.*;
import com.harbormaster.validator.*;
import com.harbormaster.projector.*;
import com.harbormaster.security.*;

/**
 *
 * <h2>CommandDefinition business delegate</h2>
 *
 * <p>
 * This class implements the Business Delegate design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of CommandDefinition related services in the case of a CommandDefinition business related service failing.</li>
 * <li>Exposes a simpler, uniform CommandDefinition interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill CommandDefinition business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Spring Boot 3.5</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>ServiceLayer</td></tr>
 *          <tr><td>architecture style</td><td>Layered</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@Service
public class CommandDefinitionService 
		extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
	/**
	 * Default Constructor
	 */
    public CommandDefinitionService(CurrentIdentity identity,
				ApplicationContext applicationContext)  {

		this.identity		= identity;
		this.projector 		= new CommandDefinitionEntityProjector( applicationContext.getBean(ProjectorRegistry.class),
										applicationContext.getBean(CommandDefinitionRepository.class) );
		this.validator		= applicationContext.getBean(CommandDefinitionValidator.class) ;
	}


	/**
	 * Creates the provided command.
	 *
	 * @param		command ${class.getCreateCommandAlias()}
	 * @return		CommandDefinition
	 */
		public CommandDefinition createCommandDefinition( CreateCommandDefinitionCommand command ) {

		CommandDefinition entity = new CommandDefinition();

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

            entity.setCommandDefinitionId( command.getCommandDefinitionId() );
            entity.setName( command.getName() );
            entity.setRequestSchemaUri( command.getRequestSchemaUri() );
            entity.setResponseSchemaUri( command.getResponseSchemaUri() );
            entity.setTimeoutSeconds( command.getTimeoutSeconds() );

		// ------------------------------------------
		// persist a new one
		// ------------------------------------------
		entity = projector.create(entity);

		LOGGER.info( "done creating of CommandDefinition {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Update the provided command.
	 * @param		command UpdateCommandDefinitionCommand
	 * @return		CommandDefinition
	 */
	public CommandDefinition updateCommandDefinition( UpdateCommandDefinitionCommand command ) {

		CommandDefinition entity = new CommandDefinition();

		// --------------------------------------
		// validate
		// --------------------------------------
		validator.validate( command );

            entity.setCommandDefinitionId( command.getCommandDefinitionId() );
            entity.setName( command.getName() );
            entity.setRequestSchemaUri( command.getRequestSchemaUri() );
            entity.setResponseSchemaUri( command.getResponseSchemaUri() );
            entity.setTimeoutSeconds( command.getTimeoutSeconds() );
            entity.setDeviceModel( command.getDeviceModel() );
            entity.setActuators( command.getActuators() );
            entity.setCommandInvocations( command.getCommandInvocations() );

		// ------------------------------------------
		// persist an existing one
		// ------------------------------------------
		entity = projector.update(entity);

		LOGGER.info( "done saving of CommandDefinition {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Deletes the associatied value object
	 * @param		command DeleteCommandDefinitionCommand
	 */
	public void delete( DeleteCommandDefinitionCommand command ) {
		UUID id = null;

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		id = command.getCommandDefinitionId();

		// ------------------------------------------
		// delete the entity
		// ------------------------------------------
		projector.delete(id);

		LOGGER.info( "done deleting of CommandDefinition {0} ", id );

	}

	/**
	 * Method to retrieve the CommandDefinition via CommandDefinitionFetchOneSummary
	 * @param 	summary CommandDefinitionFetchOneSummary
	 * @return 	CommandDefinitionFetchOneResponse
	 */
	public CommandDefinition getCommandDefinition( CommandDefinitionFetchOneSummary summary ) {

		if( summary == null )
			throw new IllegalStateException( "CommandDefinitionFetchOneSummary arg cannot be null" );

		CommandDefinition entity = null;
		UUID id = summary.getCommandDefinitionId();

		// --------------------------------------
		// validate the fetch one summary
		// --------------------------------------
		validator.validate( summary );

		// --------------------------------------
		// find a CommandDefinition using the provided id
		// --------------------------------------
		entity = projector.find( id );

		return entity;
	}


	/**
	 * Method to retrieve a collection of all CommandDefinitions
	 *
	 * @return 	List<CommandDefinition>
	 */
    public List<CommandDefinition> getAllCommandDefinition() {
		List<CommandDefinition> list = projector.findAll( new FindAllCommandDefinitionQuery() );
		return list;
	}

	/**
	 * assign DeviceModel on CommandDefinition
	 * @param		command AssignDeviceModelToCommandDefinitionCommand
	 */
	public void assignDeviceModel( AssignDeviceModelToCommandDefinitionCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignDeviceModel(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign DeviceModel on CommandDefinition
	 * @param		command UnAssignDeviceModelFromCommandDefinitionCommand
	 */
	public void unAssignDeviceModel( UnAssignDeviceModelFromCommandDefinitionCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignDeviceModel(command.getChildId());
	}
	

	/**
	 * add ActuatorInstance to Actuators
	 * @param		command AssignActuatorsToCommandDefinitionCommand
	 */
	public void addToActuators( AssignActuatorsToCommandDefinitionCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToActuators(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove ActuatorInstance from Actuators
	 * @param		command RemoveActuatorsFromCommandDefinitionCommand
	 */
	public void removeFromActuators( RemoveActuatorsFromCommandDefinitionCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromActuators(command.getParentId(), command.getChildIds());
	}

	/**
	 * add CommandInvocation to CommandInvocations
	 * @param		command AssignCommandInvocationsToCommandDefinitionCommand
	 */
	public void addToCommandInvocations( AssignCommandInvocationsToCommandDefinitionCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToCommandInvocations(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove CommandInvocation from CommandInvocations
	 * @param		command RemoveCommandInvocationsFromCommandDefinitionCommand
	 */
	public void removeFromCommandInvocations( RemoveCommandInvocationsFromCommandDefinitionCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromCommandInvocations(command.getParentId(), command.getChildIds());
	}




//************************************************************************
// Attributes
//************************************************************************
	@Autowired
    private final CommandDefinitionEntityProjector projector;
	private final CommandDefinitionValidator validator;
	private CommandDefinition commandDefinition 	= null;
	private CurrentIdentity identity			= null;
	private static final Logger LOGGER 			=  LoggerFactory.getLogger(CommandDefinitionService.class);
}
