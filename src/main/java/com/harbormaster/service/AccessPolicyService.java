/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */

package com.harbormaster.service;

import java.io.IOException;
import java.util.*;
import java.util.concurrent.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import org.springframework.stereotype.Service;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;


import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.repository.*;
import com.harbormaster.validator.*;
import com.harbormaster.projector.*;
import com.harbormaster.security.*;

/**
 *
 * <h2>AccessPolicy business delegate</h2>
 *
 * <p>
 * This class implements the Business Delegate design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of AccessPolicy related services in the case of a AccessPolicy business related service failing.</li>
 * <li>Exposes a simpler, uniform AccessPolicy interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill AccessPolicy business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Spring Boot 3.5</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>ServiceLayer</td></tr>
 *          <tr><td>architecture style</td><td>Layered</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@Service
public class AccessPolicyService 
		extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
	/**
	 * Default Constructor
	 */
    public AccessPolicyService(CurrentIdentity identity,
				ApplicationContext applicationContext)  {

		this.identity		= identity;
		this.projector 		= new AccessPolicyEntityProjector( applicationContext.getBean(ProjectorRegistry.class),
										applicationContext.getBean(AccessPolicyRepository.class) );
		this.validator		= applicationContext.getBean(AccessPolicyValidator.class) ;
	}


	/**
	 * Creates the provided command.
	 *
	 * @param		command ${class.getCreateCommandAlias()}
	 * @return		AccessPolicy
	 */
		public AccessPolicy createAccessPolicy( CreateAccessPolicyCommand command ) {

		AccessPolicy entity = new AccessPolicy();

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

            entity.setAccessPolicyId( command.getAccessPolicyId() );
            entity.setName( command.getName() );
            entity.setScope( command.getScope() );
            entity.setExpiresAt( command.getExpiresAt() );

		// ------------------------------------------
		// persist a new one
		// ------------------------------------------
		entity = projector.create(entity);

		LOGGER.info( "done creating of AccessPolicy {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Update the provided command.
	 * @param		command UpdateAccessPolicyCommand
	 * @return		AccessPolicy
	 */
	public AccessPolicy updateAccessPolicy( UpdateAccessPolicyCommand command ) {

		AccessPolicy entity = new AccessPolicy();

		// --------------------------------------
		// validate
		// --------------------------------------
		validator.validate( command );

            entity.setAccessPolicyId( command.getAccessPolicyId() );
            entity.setName( command.getName() );
            entity.setScope( command.getScope() );
            entity.setExpiresAt( command.getExpiresAt() );
            entity.setTenant( command.getTenant() );
            entity.setApiKeys( command.getApiKeys() );
            entity.setUsers( command.getUsers() );

		// ------------------------------------------
		// persist an existing one
		// ------------------------------------------
		entity = projector.update(entity);

		LOGGER.info( "done saving of AccessPolicy {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Deletes the associatied value object
	 * @param		command DeleteAccessPolicyCommand
	 */
	public void delete( DeleteAccessPolicyCommand command ) {
		UUID id = null;

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		id = command.getAccessPolicyId();

		// ------------------------------------------
		// delete the entity
		// ------------------------------------------
		projector.delete(id);

		LOGGER.info( "done deleting of AccessPolicy {0} ", id );

	}

	/**
	 * Method to retrieve the AccessPolicy via AccessPolicyFetchOneSummary
	 * @param 	summary AccessPolicyFetchOneSummary
	 * @return 	AccessPolicyFetchOneResponse
	 */
	public AccessPolicy getAccessPolicy( AccessPolicyFetchOneSummary summary ) {

		if( summary == null )
			throw new IllegalStateException( "AccessPolicyFetchOneSummary arg cannot be null" );

		AccessPolicy entity = null;
		UUID id = summary.getAccessPolicyId();

		// --------------------------------------
		// validate the fetch one summary
		// --------------------------------------
		validator.validate( summary );

		// --------------------------------------
		// find a AccessPolicy using the provided id
		// --------------------------------------
		entity = projector.find( id );

		return entity;
	}


	/**
	 * Method to retrieve a collection of all AccessPolicys
	 *
	 * @return 	List<AccessPolicy>
	 */
    public List<AccessPolicy> getAllAccessPolicy() {
		List<AccessPolicy> list = projector.findAll( new FindAllAccessPolicyQuery() );
		return list;
	}

	/**
	 * assign Tenant on AccessPolicy
	 * @param		command AssignTenantToAccessPolicyCommand
	 */
	public void assignTenant( AssignTenantToAccessPolicyCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignTenant(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Tenant on AccessPolicy
	 * @param		command UnAssignTenantFromAccessPolicyCommand
	 */
	public void unAssignTenant( UnAssignTenantFromAccessPolicyCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignTenant(command.getChildId());
	}
	

	/**
	 * add ApiKey to ApiKeys
	 * @param		command AssignApiKeysToAccessPolicyCommand
	 */
	public void addToApiKeys( AssignApiKeysToAccessPolicyCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToApiKeys(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove ApiKey from ApiKeys
	 * @param		command RemoveApiKeysFromAccessPolicyCommand
	 */
	public void removeFromApiKeys( RemoveApiKeysFromAccessPolicyCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromApiKeys(command.getParentId(), command.getChildIds());
	}

	/**
	 * add TenantUser to Users
	 * @param		command AssignUsersToAccessPolicyCommand
	 */
	public void addToUsers( AssignUsersToAccessPolicyCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToUsers(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove TenantUser from Users
	 * @param		command RemoveUsersFromAccessPolicyCommand
	 */
	public void removeFromUsers( RemoveUsersFromAccessPolicyCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromUsers(command.getParentId(), command.getChildIds());
	}




//************************************************************************
// Attributes
//************************************************************************
	@Autowired
    private final AccessPolicyEntityProjector projector;
	private final AccessPolicyValidator validator;
	private AccessPolicy accessPolicy 	= null;
	private CurrentIdentity identity			= null;
	private static final Logger LOGGER 			=  LoggerFactory.getLogger(AccessPolicyService.class);
}
