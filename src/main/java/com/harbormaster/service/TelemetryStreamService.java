/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */

package com.harbormaster.service;

import java.io.IOException;
import java.util.*;
import java.util.concurrent.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import org.springframework.stereotype.Service;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;


import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.repository.*;
import com.harbormaster.validator.*;
import com.harbormaster.projector.*;
import com.harbormaster.security.*;

/**
 *
 * <h2>TelemetryStream business delegate</h2>
 *
 * <p>
 * This class implements the Business Delegate design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of TelemetryStream related services in the case of a TelemetryStream business related service failing.</li>
 * <li>Exposes a simpler, uniform TelemetryStream interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill TelemetryStream business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Spring Boot 3.5</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>ServiceLayer</td></tr>
 *          <tr><td>architecture style</td><td>Layered</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@Service
public class TelemetryStreamService 
		extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
	/**
	 * Default Constructor
	 */
    public TelemetryStreamService(CurrentIdentity identity,
				ApplicationContext applicationContext)  {

		this.identity		= identity;
		this.projector 		= new TelemetryStreamEntityProjector( applicationContext.getBean(ProjectorRegistry.class),
										applicationContext.getBean(TelemetryStreamRepository.class) );
		this.validator		= applicationContext.getBean(TelemetryStreamValidator.class) ;
	}


	/**
	 * Creates the provided command.
	 *
	 * @param		command ${class.getCreateCommandAlias()}
	 * @return		TelemetryStream
	 */
		public TelemetryStream createTelemetryStream( CreateTelemetryStreamCommand command ) {

		TelemetryStream entity = new TelemetryStream();

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

            entity.setTelemetryStreamId( command.getTelemetryStreamId() );
            entity.setStreamName( command.getStreamName() );
            entity.setRetentionDays( command.getRetentionDays() );
            entity.setQos( command.getQos() );

		// ------------------------------------------
		// persist a new one
		// ------------------------------------------
		entity = projector.create(entity);

		LOGGER.info( "done creating of TelemetryStream {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Update the provided command.
	 * @param		command UpdateTelemetryStreamCommand
	 * @return		TelemetryStream
	 */
	public TelemetryStream updateTelemetryStream( UpdateTelemetryStreamCommand command ) {

		TelemetryStream entity = new TelemetryStream();

		// --------------------------------------
		// validate
		// --------------------------------------
		validator.validate( command );

            entity.setTelemetryStreamId( command.getTelemetryStreamId() );
            entity.setStreamName( command.getStreamName() );
            entity.setRetentionDays( command.getRetentionDays() );
            entity.setDevice( command.getDevice() );
            entity.setSensor( command.getSensor() );
            entity.setSchema( command.getSchema() );
            entity.setMessagingEndpoint( command.getMessagingEndpoint() );
            entity.setRetentionPolicy( command.getRetentionPolicy() );
            entity.setQos( command.getQos() );

		// ------------------------------------------
		// persist an existing one
		// ------------------------------------------
		entity = projector.update(entity);

		LOGGER.info( "done saving of TelemetryStream {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Deletes the associatied value object
	 * @param		command DeleteTelemetryStreamCommand
	 */
	public void delete( DeleteTelemetryStreamCommand command ) {
		UUID id = null;

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		id = command.getTelemetryStreamId();

		// ------------------------------------------
		// delete the entity
		// ------------------------------------------
		projector.delete(id);

		LOGGER.info( "done deleting of TelemetryStream {0} ", id );

	}

	/**
	 * Method to retrieve the TelemetryStream via TelemetryStreamFetchOneSummary
	 * @param 	summary TelemetryStreamFetchOneSummary
	 * @return 	TelemetryStreamFetchOneResponse
	 */
	public TelemetryStream getTelemetryStream( TelemetryStreamFetchOneSummary summary ) {

		if( summary == null )
			throw new IllegalStateException( "TelemetryStreamFetchOneSummary arg cannot be null" );

		TelemetryStream entity = null;
		UUID id = summary.getTelemetryStreamId();

		// --------------------------------------
		// validate the fetch one summary
		// --------------------------------------
		validator.validate( summary );

		// --------------------------------------
		// find a TelemetryStream using the provided id
		// --------------------------------------
		entity = projector.find( id );

		return entity;
	}


	/**
	 * Method to retrieve a collection of all TelemetryStreams
	 *
	 * @return 	List<TelemetryStream>
	 */
    public List<TelemetryStream> getAllTelemetryStream() {
		List<TelemetryStream> list = projector.findAll( new FindAllTelemetryStreamQuery() );
		return list;
	}

	/**
	 * assign Device on TelemetryStream
	 * @param		command AssignDeviceToTelemetryStreamCommand
	 */
	public void assignDevice( AssignDeviceToTelemetryStreamCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignDevice(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Device on TelemetryStream
	 * @param		command UnAssignDeviceFromTelemetryStreamCommand
	 */
	public void unAssignDevice( UnAssignDeviceFromTelemetryStreamCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignDevice(command.getChildId());
	}
	
	/**
	 * assign Sensor on TelemetryStream
	 * @param		command AssignSensorToTelemetryStreamCommand
	 */
	public void assignSensor( AssignSensorToTelemetryStreamCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignSensor(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Sensor on TelemetryStream
	 * @param		command UnAssignSensorFromTelemetryStreamCommand
	 */
	public void unAssignSensor( UnAssignSensorFromTelemetryStreamCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignSensor(command.getChildId());
	}
	
	/**
	 * assign Schema on TelemetryStream
	 * @param		command AssignSchemaToTelemetryStreamCommand
	 */
	public void assignSchema( AssignSchemaToTelemetryStreamCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignSchema(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Schema on TelemetryStream
	 * @param		command UnAssignSchemaFromTelemetryStreamCommand
	 */
	public void unAssignSchema( UnAssignSchemaFromTelemetryStreamCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignSchema(command.getChildId());
	}
	
	/**
	 * assign MessagingEndpoint on TelemetryStream
	 * @param		command AssignMessagingEndpointToTelemetryStreamCommand
	 */
	public void assignMessagingEndpoint( AssignMessagingEndpointToTelemetryStreamCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignMessagingEndpoint(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign MessagingEndpoint on TelemetryStream
	 * @param		command UnAssignMessagingEndpointFromTelemetryStreamCommand
	 */
	public void unAssignMessagingEndpoint( UnAssignMessagingEndpointFromTelemetryStreamCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignMessagingEndpoint(command.getChildId());
	}
	
	/**
	 * assign RetentionPolicy on TelemetryStream
	 * @param		command AssignRetentionPolicyToTelemetryStreamCommand
	 */
	public void assignRetentionPolicy( AssignRetentionPolicyToTelemetryStreamCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignRetentionPolicy(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign RetentionPolicy on TelemetryStream
	 * @param		command UnAssignRetentionPolicyFromTelemetryStreamCommand
	 */
	public void unAssignRetentionPolicy( UnAssignRetentionPolicyFromTelemetryStreamCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignRetentionPolicy(command.getChildId());
	}
	




//************************************************************************
// Attributes
//************************************************************************
	@Autowired
    private final TelemetryStreamEntityProjector projector;
	private final TelemetryStreamValidator validator;
	private TelemetryStream telemetryStream 	= null;
	private CurrentIdentity identity			= null;
	private static final Logger LOGGER 			=  LoggerFactory.getLogger(TelemetryStreamService.class);
}
