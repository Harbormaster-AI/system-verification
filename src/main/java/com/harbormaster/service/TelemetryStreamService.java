/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.harbormaster.service;

import java.io.IOException;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.*;
import java.util.concurrent.*;

import org.axonframework.commandhandling.gateway.CommandGateway;
import org.axonframework.queryhandling.QueryGateway;
import org.axonframework.queryhandling.QueryUpdateEmitter;
import org.axonframework.messaging.responsetypes.ResponseTypes;
import org.springframework.stereotype.Service;
import org.springframework.beans.BeansException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;

import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.validator.*;

/**
 *
 * <h2>TelemetryStream business service</h2>
 *
 * <p>
 * This class implements the Service design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of TelemetryStream related services in the case of a TelemetryStream business related service failing.</li>
 * <li>Exposes a simpler, uniform TelemetryStream interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill TelemetryStream business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@Service
public class TelemetryStreamService 
extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
    /** 
     * Default Constructor 
     */
    public TelemetryStreamService()  {
	}

	@Override
	public void setApplicationContext(ApplicationContext ctx) throws BeansException {
		super.setApplicationContext(ctx);

		queryGateway 		= ctx.getBean(QueryGateway.class);
		commandGateway 		= ctx.getBean(CommandGateway.class);
		queryUpdateEmitter  = ctx.getBean(QueryUpdateEmitter.class);
		validator			= ctx.getBean(TelemetryStreamValidator.class);
	}


   /**
	* TelemetryStream Business Delegate Factory Method
	*
	* All methods are expected to be self-sufficient.
	*
	* @return 	TelemetryStreamService
	*/
	public static TelemetryStreamService getTelemetryStreamInstance() {
		return( new TelemetryStreamService() );
	}
 
   /**
    * Creates the provided command.
    * 
    * @param		command ${class.getCreateCommandAlias()}
    * @exception    ProcessingException
    * @exception	IllegalArgumentException
    * @return		CompletableFuture<UUID>
    */
	public CompletableFuture<UUID> createTelemetryStream( CreateTelemetryStreamCommand command )
    throws ProcessingException, IllegalArgumentException {

		CompletableFuture<UUID> completableFuture = null;
				
		try {
			// --------------------------------------
        	// assign identity now if none
        	// -------------------------------------- 
			if ( command.getTelemetryStreamId() == null )
				command.setTelemetryStreamId( UUID.randomUUID() );
				
			// --------------------------------------
        	// validate the command
        	// --------------------------------------    	
        	validator.validate( command );    

    		// ---------------------------------------
    		// issue the CreateTelemetryStreamCommand - by convention the future return value for a create command
        	// that is handled by the constructor of an aggregate will return the UUID 
    		// ---------------------------------------
        	completableFuture = commandGateway.send( command );
        	
			LOGGER.log( Level.INFO, "return from Command Gateway for CreateTelemetryStreamCommand of TelemetryStream is " + command );
			
        }
        catch (Exception exc) {
            final String errMsg = "Unable to create TelemetryStream - " + exc;
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }        
        
        return completableFuture;
    }

   /**
    * Update the provided command.
    * @param		command UpdateTelemetryStreamCommand
    * @return		CompletableFuture<Void>
    * @exception    ProcessingException
    * @exception  	IllegalArgumentException
    */
    public CompletableFuture<Void> updateTelemetryStream( UpdateTelemetryStreamCommand command ) 
    throws ProcessingException, IllegalArgumentException {
    	CompletableFuture<Void> completableFuture = null;
    	
    	try {       

			// --------------------------------------
        	// validate 
        	// --------------------------------------    	
        	validator.validate( command );    

        	// --------------------------------------
        	// issue the UpdateTelemetryStreamCommand and return right away
        	// --------------------------------------    	
        	completableFuture = commandGateway.send( command );
    	}
        catch (Exception exc) {
            final String errMsg = "Unable to save TelemetryStream - " + exc;
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        
    	return completableFuture;
    }
   
   /**
    * Deletes the associatied value object
    * @param		command DeleteTelemetryStreamCommand
    * @return		CompletableFuture<Void>
    * @exception 	ProcessingException
    */
    public CompletableFuture<Void> delete( DeleteTelemetryStreamCommand command ) 
    throws ProcessingException, IllegalArgumentException {	
    	
    	CompletableFuture<Void> completableFuture = null;
    	
        try {  
			// --------------------------------------
        	// validate the command
        	// --------------------------------------    	
        	validator.validate( command );    
        	
        	// --------------------------------------
        	// issue the DeleteTelemetryStreamCommand and return right away
        	// --------------------------------------    	
        	completableFuture = commandGateway.send( command );
        }
        catch (Exception exc) {
            final String errMsg = "Unable to delete TelemetryStream using Id = "  + command.getTelemetryStreamId();
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }
        
        return completableFuture;
    }

    /**
     * Method to retrieve the TelemetryStream via TelemetryStreamFetchOneSummary
     * @param 	summary TelemetryStreamFetchOneSummary 
     * @return 	TelemetryStreamFetchOneResponse
     * @exception ProcessingException - Thrown if processing any related problems
     * @exception IllegalArgumentException 
     */
    public TelemetryStream getTelemetryStream( TelemetryStreamFetchOneSummary summary ) 
    throws ProcessingException, IllegalArgumentException {
    	
    	if( summary == null )
    		throw new IllegalArgumentException( "TelemetryStreamFetchOneSummary arg cannot be null" );
    	
    	TelemetryStream entity = null;
    	
        try {
        	// --------------------------------------
        	// validate the fetch one summary
        	// --------------------------------------    	
        	validator.validate( summary );    
        	
        	// --------------------------------------
        	// use queryGateway to send request to Find a TelemetryStream
        	// --------------------------------------
        	CompletableFuture<TelemetryStream> futureEntity = queryGateway.query(new FindTelemetryStreamQuery( new LoadTelemetryStreamFilter( summary.getTelemetryStreamId() ) ), ResponseTypes.instanceOf(TelemetryStream.class));
        	
        	entity = futureEntity.get();
        }
        catch( Exception exc ) {
            final String errMsg = "Unable to locate TelemetryStream with id " + summary.getTelemetryStreamId();
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }        
        
        return entity;
    }


    /**
     * Method to retrieve a collection of all TelemetryStreams
     *
     * @return 	List<TelemetryStream> 
     * @exception ProcessingException Thrown if any problems
     */
    public List<TelemetryStream> getAllTelemetryStream() 
    throws ProcessingException {
        List<TelemetryStream> list = null;

        try {
        	CompletableFuture<List<TelemetryStream>> futureList = queryGateway.query(new FindAllTelemetryStreamQuery(), ResponseTypes.multipleInstancesOf(TelemetryStream.class));
        	
        	list = futureList.get();
        }
        catch( Exception exc ) {
            String errMsg = "Failed to get all TelemetryStream";
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }        
        
        return list;
    }

    /**
     * assign Device on TelemetryStream
     * @param		command AssignDeviceToTelemetryStreamCommand	
     * @exception	ProcessingException
     */     
	public void assignDevice( AssignDeviceToTelemetryStreamCommand command ) throws ProcessingException {

		// --------------------------------------------
		// load the parent
		// --------------------------------------------
		load( command.getTelemetryStreamId() );
		
		IoTDeviceService childDelegate 	= IoTDeviceService.getIoTDeviceInstance();
		TelemetryStreamService parentDelegate = TelemetryStreamService.getTelemetryStreamInstance();			
		UUID childId = command.getAssignment().getIoTDeviceId();
		IoTDevice child = null;
		
		try {
			// --------------------------------------
	    	// best to validate the command now
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );

		}
        catch( Throwable exc ) {
			final String msg = "Failed to get IoTDevice using id " + childId;
			LOGGER.log( Level.WARNING,  msg );
			throw new ProcessingException( msg, exc );
        }
	}

    /**
     * unAssign Device on TelemetryStream
     * @param		command UnAssignDeviceFromTelemetryStreamCommand
     * @exception	ProcessingException
     */     
	public void unAssignDevice( UnAssignDeviceFromTelemetryStreamCommand command ) throws ProcessingException {

		try {
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    
	
	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );
		}
		catch( Exception exc ) {
			final String msg = "Failed to unassign Device on TelemetryStream";
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}
	
    /**
     * assign Sensor on TelemetryStream
     * @param		command AssignSensorToTelemetryStreamCommand	
     * @exception	ProcessingException
     */     
	public void assignSensor( AssignSensorToTelemetryStreamCommand command ) throws ProcessingException {

		// --------------------------------------------
		// load the parent
		// --------------------------------------------
		load( command.getTelemetryStreamId() );
		
		SensorInstanceService childDelegate 	= SensorInstanceService.getSensorInstanceInstance();
		TelemetryStreamService parentDelegate = TelemetryStreamService.getTelemetryStreamInstance();			
		UUID childId = command.getAssignment().getSensorInstanceId();
		SensorInstance child = null;
		
		try {
			// --------------------------------------
	    	// best to validate the command now
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );

		}
        catch( Throwable exc ) {
			final String msg = "Failed to get SensorInstance using id " + childId;
			LOGGER.log( Level.WARNING,  msg );
			throw new ProcessingException( msg, exc );
        }
	}

    /**
     * unAssign Sensor on TelemetryStream
     * @param		command UnAssignSensorFromTelemetryStreamCommand
     * @exception	ProcessingException
     */     
	public void unAssignSensor( UnAssignSensorFromTelemetryStreamCommand command ) throws ProcessingException {

		try {
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    
	
	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );
		}
		catch( Exception exc ) {
			final String msg = "Failed to unassign Sensor on TelemetryStream";
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}
	
    /**
     * assign Schema on TelemetryStream
     * @param		command AssignSchemaToTelemetryStreamCommand	
     * @exception	ProcessingException
     */     
	public void assignSchema( AssignSchemaToTelemetryStreamCommand command ) throws ProcessingException {

		// --------------------------------------------
		// load the parent
		// --------------------------------------------
		load( command.getTelemetryStreamId() );
		
		TelemetrySchemaService childDelegate 	= TelemetrySchemaService.getTelemetrySchemaInstance();
		TelemetryStreamService parentDelegate = TelemetryStreamService.getTelemetryStreamInstance();			
		UUID childId = command.getAssignment().getTelemetrySchemaId();
		TelemetrySchema child = null;
		
		try {
			// --------------------------------------
	    	// best to validate the command now
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );

		}
        catch( Throwable exc ) {
			final String msg = "Failed to get TelemetrySchema using id " + childId;
			LOGGER.log( Level.WARNING,  msg );
			throw new ProcessingException( msg, exc );
        }
	}

    /**
     * unAssign Schema on TelemetryStream
     * @param		command UnAssignSchemaFromTelemetryStreamCommand
     * @exception	ProcessingException
     */     
	public void unAssignSchema( UnAssignSchemaFromTelemetryStreamCommand command ) throws ProcessingException {

		try {
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    
	
	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );
		}
		catch( Exception exc ) {
			final String msg = "Failed to unassign Schema on TelemetryStream";
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}
	
    /**
     * assign MessagingEndpoint on TelemetryStream
     * @param		command AssignMessagingEndpointToTelemetryStreamCommand	
     * @exception	ProcessingException
     */     
	public void assignMessagingEndpoint( AssignMessagingEndpointToTelemetryStreamCommand command ) throws ProcessingException {

		// --------------------------------------------
		// load the parent
		// --------------------------------------------
		load( command.getTelemetryStreamId() );
		
		MessagingEndpointService childDelegate 	= MessagingEndpointService.getMessagingEndpointInstance();
		TelemetryStreamService parentDelegate = TelemetryStreamService.getTelemetryStreamInstance();			
		UUID childId = command.getAssignment().getMessagingEndpointId();
		MessagingEndpoint child = null;
		
		try {
			// --------------------------------------
	    	// best to validate the command now
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );

		}
        catch( Throwable exc ) {
			final String msg = "Failed to get MessagingEndpoint using id " + childId;
			LOGGER.log( Level.WARNING,  msg );
			throw new ProcessingException( msg, exc );
        }
	}

    /**
     * unAssign MessagingEndpoint on TelemetryStream
     * @param		command UnAssignMessagingEndpointFromTelemetryStreamCommand
     * @exception	ProcessingException
     */     
	public void unAssignMessagingEndpoint( UnAssignMessagingEndpointFromTelemetryStreamCommand command ) throws ProcessingException {

		try {
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    
	
	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );
		}
		catch( Exception exc ) {
			final String msg = "Failed to unassign MessagingEndpoint on TelemetryStream";
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}
	
    /**
     * assign RetentionPolicy on TelemetryStream
     * @param		command AssignRetentionPolicyToTelemetryStreamCommand	
     * @exception	ProcessingException
     */     
	public void assignRetentionPolicy( AssignRetentionPolicyToTelemetryStreamCommand command ) throws ProcessingException {

		// --------------------------------------------
		// load the parent
		// --------------------------------------------
		load( command.getTelemetryStreamId() );
		
		DataRetentionPolicyService childDelegate 	= DataRetentionPolicyService.getDataRetentionPolicyInstance();
		TelemetryStreamService parentDelegate = TelemetryStreamService.getTelemetryStreamInstance();			
		UUID childId = command.getAssignment().getDataRetentionPolicyId();
		DataRetentionPolicy child = null;
		
		try {
			// --------------------------------------
	    	// best to validate the command now
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );

		}
        catch( Throwable exc ) {
			final String msg = "Failed to get DataRetentionPolicy using id " + childId;
			LOGGER.log( Level.WARNING,  msg );
			throw new ProcessingException( msg, exc );
        }
	}

    /**
     * unAssign RetentionPolicy on TelemetryStream
     * @param		command UnAssignRetentionPolicyFromTelemetryStreamCommand
     * @exception	ProcessingException
     */     
	public void unAssignRetentionPolicy( UnAssignRetentionPolicyFromTelemetryStreamCommand command ) throws ProcessingException {

		try {
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    
	
	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );
		}
		catch( Exception exc ) {
			final String msg = "Failed to unassign RetentionPolicy on TelemetryStream";
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}
	



	/**
	 * Internal helper method to load the root 
	 * 
	 * @param		id	UUID
	 * @return		TelemetryStream
	 */
	protected TelemetryStream load( UUID id ) throws ProcessingException {
		telemetryStream = TelemetryStreamService.getTelemetryStreamInstance().getTelemetryStream( new TelemetryStreamFetchOneSummary(id) );	
		return telemetryStream;
	}


//************************************************************************
// Attributes
//************************************************************************
	private QueryGateway queryGateway;
	private CommandGateway commandGateway;
	private QueryUpdateEmitter queryUpdateEmitter;
	private TelemetryStreamValidator validator;
	private TelemetryStream telemetryStream 	= null;
    private static final Logger LOGGER 			= Logger.getLogger(TelemetryStreamService.class.getName());
    
}
