/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Insurance Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.service;

import java.io.IOException;
import java.util.*;
import java.util.concurrent.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import org.springframework.stereotype.Service;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;


import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.repository.*;
import com.harbormaster.validator.*;
import com.harbormaster.projector.*;
import com.harbormaster.security.*;

/**
 *
 * <h2>Quote business delegate</h2>
 *
 * <p>
 * This class implements the Business Delegate design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of Quote related services in the case of a Quote business related service failing.</li>
 * <li>Exposes a simpler, uniform Quote interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill Quote business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Spring Boot 3.5</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>ServiceLayer</td></tr>
 *          <tr><td>architecture style</td><td>Layered</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Insurance Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core insurance product, underwriting, policy, billing, claims, and reinsurance capabilities.</td></tr>
 *          <tr><td>industry</td><td>Insurance</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@Service
public class QuoteService 
		extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
	/**
	 * Default Constructor
	 */
    public QuoteService(CurrentIdentity identity,
				ApplicationContext applicationContext)  {

		this.identity		= identity;
		this.projector 		= new QuoteEntityProjector( applicationContext.getBean(ProjectorRegistry.class),
										applicationContext.getBean(QuoteRepository.class) );
		this.validator		= applicationContext.getBean(QuoteValidator.class) ;
	}


	/**
	 * Creates the provided command.
	 *
	 * @param		command ${class.getCreateCommandAlias()}
	 * @return		Quote
	 */
		public Quote createQuote( CreateQuoteCommand command ) {

		Quote entity = new Quote();

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

            entity.setQuoteId( command.getQuoteId() );
            entity.setQuoteNumber( command.getQuoteNumber() );
            entity.setTotalPremium( command.getTotalPremium() );
            entity.setRatingDate( command.getRatingDate() );
            entity.setAsBound( command.getAsBound() );

		// ------------------------------------------
		// persist a new one
		// ------------------------------------------
		entity = projector.create(entity);

		LOGGER.info( "done creating of Quote {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Update the provided command.
	 * @param		command UpdateQuoteCommand
	 * @return		Quote
	 */
	public Quote updateQuote( UpdateQuoteCommand command ) {

		Quote entity = new Quote();

		// --------------------------------------
		// validate
		// --------------------------------------
		validator.validate( command );

            entity.setQuoteId( command.getQuoteId() );
            entity.setQuoteNumber( command.getQuoteNumber() );
            entity.setTotalPremium( command.getTotalPremium() );
            entity.setRatingDate( command.getRatingDate() );
            entity.setAsBound( command.getAsBound() );
            entity.setApplication( command.getApplication() );
            entity.setUnderwritingDecisions( command.getUnderwritingDecisions() );
            entity.setPolicy( command.getPolicy() );

		// ------------------------------------------
		// persist an existing one
		// ------------------------------------------
		entity = projector.update(entity);

		LOGGER.info( "done saving of Quote {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Deletes the associatied value object
	 * @param		command DeleteQuoteCommand
	 */
	public void delete( DeleteQuoteCommand command ) {
		UUID id = null;

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		id = command.getQuoteId();

		// ------------------------------------------
		// delete the entity
		// ------------------------------------------
		projector.delete(id);

		LOGGER.info( "done deleting of Quote {0} ", id );

	}

	/**
	 * Method to retrieve the Quote via QuoteFetchOneSummary
	 * @param 	summary QuoteFetchOneSummary
	 * @return 	QuoteFetchOneResponse
	 */
	public Quote getQuote( QuoteFetchOneSummary summary ) {

		if( summary == null )
			throw new IllegalStateException( "QuoteFetchOneSummary arg cannot be null" );

		Quote entity = null;
		UUID id = summary.getQuoteId();

		// --------------------------------------
		// validate the fetch one summary
		// --------------------------------------
		validator.validate( summary );

		// --------------------------------------
		// find a Quote using the provided id
		// --------------------------------------
		entity = projector.find( id );

		return entity;
	}


	/**
	 * Method to retrieve a collection of all Quotes
	 *
	 * @return 	List<Quote>
	 */
    public List<Quote> getAllQuote() {
		List<Quote> list = projector.findAll( new FindAllQuoteQuery() );
		return list;
	}

	/**
	 * assign Application on Quote
	 * @param		command AssignApplicationToQuoteCommand
	 */
	public void assignApplication( AssignApplicationToQuoteCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignApplication(command.getQuoteId(), command.getAssignment());

	}

	/**
	 * unAssign Application on Quote
	 * @param		command UnAssignApplicationFromQuoteCommand
	 */
	public void unAssignApplication( UnAssignApplicationFromQuoteCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignApplication(command.getQuoteId());
	}
	
	/**
	 * assign Policy on Quote
	 * @param		command AssignPolicyToQuoteCommand
	 */
	public void assignPolicy( AssignPolicyToQuoteCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignPolicy(command.getQuoteId(), command.getAssignment());

	}

	/**
	 * unAssign Policy on Quote
	 * @param		command UnAssignPolicyFromQuoteCommand
	 */
	public void unAssignPolicy( UnAssignPolicyFromQuoteCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignPolicy(command.getQuoteId());
	}
	

	/**
	 * add UnderwritingDecision to UnderwritingDecisions
	 * @param		command AssignUnderwritingDecisionsToQuoteCommand
	 */
	public void addToUnderwritingDecisions( AssignUnderwritingDecisionsToQuoteCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToUnderwritingDecisions(command.getQuoteId(), command.getAddTo());
	}

	/**
	 * remove UnderwritingDecision from UnderwritingDecisions
	 * @param		command RemoveUnderwritingDecisionsFromQuoteCommand
	 */
	public void removeFromUnderwritingDecisions( RemoveUnderwritingDecisionsFromQuoteCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromUnderwritingDecisions(command.getQuoteId(), command.getRemoveFrom());
	}




//************************************************************************
// Attributes
//************************************************************************
	@Autowired
    private final QuoteEntityProjector projector;
	private final QuoteValidator validator;
	private Quote quote 	= null;
	private CurrentIdentity identity			= null;
	private static final Logger LOGGER 			=  LoggerFactory.getLogger(QuoteService.class);
}
