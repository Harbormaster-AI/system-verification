/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Insurance Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.service;

import java.io.IOException;
import java.util.*;
import java.util.concurrent.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import org.springframework.stereotype.Service;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;


import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.repository.*;
import com.harbormaster.validator.*;
import com.harbormaster.projector.*;
import com.harbormaster.security.*;

/**
 *
 * <h2>Invoice business delegate</h2>
 *
 * <p>
 * This class implements the Business Delegate design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of Invoice related services in the case of a Invoice business related service failing.</li>
 * <li>Exposes a simpler, uniform Invoice interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill Invoice business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Spring Boot 3.5</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>ServiceLayer</td></tr>
 *          <tr><td>architecture style</td><td>Layered</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Insurance Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core insurance product, underwriting, policy, billing, claims, and reinsurance capabilities.</td></tr>
 *          <tr><td>industry</td><td>Insurance</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@Service
public class InvoiceService 
		extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
	/**
	 * Default Constructor
	 */
    public InvoiceService(CurrentIdentity identity,
				ApplicationContext applicationContext)  {

		this.identity		= identity;
		this.projector 		= new InvoiceEntityProjector( applicationContext.getBean(ProjectorRegistry.class),
										applicationContext.getBean(InvoiceRepository.class) );
		this.validator		= applicationContext.getBean(InvoiceValidator.class) ;
	}


	/**
	 * Creates the provided command.
	 *
	 * @param		command ${class.getCreateCommandAlias()}
	 * @return		Invoice
	 */
		public Invoice createInvoice( CreateInvoiceCommand command ) {

		Invoice entity = new Invoice();

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

            entity.setInvoiceId( command.getInvoiceId() );
            entity.setInvoiceNumber( command.getInvoiceNumber() );
            entity.setDueDate( command.getDueDate() );
            entity.setTotalDue( command.getTotalDue() );
            entity.setStatus( command.getStatus() );

		// ------------------------------------------
		// persist a new one
		// ------------------------------------------
		entity = projector.create(entity);

		LOGGER.info( "done creating of Invoice {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Update the provided command.
	 * @param		command UpdateInvoiceCommand
	 * @return		Invoice
	 */
	public Invoice updateInvoice( UpdateInvoiceCommand command ) {

		Invoice entity = new Invoice();

		// --------------------------------------
		// validate
		// --------------------------------------
		validator.validate( command );

            entity.setInvoiceId( command.getInvoiceId() );
            entity.setInvoiceNumber( command.getInvoiceNumber() );
            entity.setDueDate( command.getDueDate() );
            entity.setTotalDue( command.getTotalDue() );
            entity.setBillingAccount( command.getBillingAccount() );
            entity.setPolicy( command.getPolicy() );
            entity.setPayments( command.getPayments() );
            entity.setStatus( command.getStatus() );

		// ------------------------------------------
		// persist an existing one
		// ------------------------------------------
		entity = projector.update(entity);

		LOGGER.info( "done saving of Invoice {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Deletes the associatied value object
	 * @param		command DeleteInvoiceCommand
	 */
	public void delete( DeleteInvoiceCommand command ) {
		UUID id = null;

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		id = command.getInvoiceId();

		// ------------------------------------------
		// delete the entity
		// ------------------------------------------
		projector.delete(id);

		LOGGER.info( "done deleting of Invoice {0} ", id );

	}

	/**
	 * Method to retrieve the Invoice via InvoiceFetchOneSummary
	 * @param 	summary InvoiceFetchOneSummary
	 * @return 	InvoiceFetchOneResponse
	 */
	public Invoice getInvoice( InvoiceFetchOneSummary summary ) {

		if( summary == null )
			throw new IllegalStateException( "InvoiceFetchOneSummary arg cannot be null" );

		Invoice entity = null;
		UUID id = summary.getInvoiceId();

		// --------------------------------------
		// validate the fetch one summary
		// --------------------------------------
		validator.validate( summary );

		// --------------------------------------
		// find a Invoice using the provided id
		// --------------------------------------
		entity = projector.find( id );

		return entity;
	}


	/**
	 * Method to retrieve a collection of all Invoices
	 *
	 * @return 	List<Invoice>
	 */
    public List<Invoice> getAllInvoice() {
		List<Invoice> list = projector.findAll( new FindAllInvoiceQuery() );
		return list;
	}

	/**
	 * assign BillingAccount on Invoice
	 * @param		command AssignBillingAccountToInvoiceCommand
	 */
	public void assignBillingAccount( AssignBillingAccountToInvoiceCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignBillingAccount(command.getInvoiceId(), command.getAssignment());

	}

	/**
	 * unAssign BillingAccount on Invoice
	 * @param		command UnAssignBillingAccountFromInvoiceCommand
	 */
	public void unAssignBillingAccount( UnAssignBillingAccountFromInvoiceCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignBillingAccount(command.getInvoiceId());
	}
	
	/**
	 * assign Policy on Invoice
	 * @param		command AssignPolicyToInvoiceCommand
	 */
	public void assignPolicy( AssignPolicyToInvoiceCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignPolicy(command.getInvoiceId(), command.getAssignment());

	}

	/**
	 * unAssign Policy on Invoice
	 * @param		command UnAssignPolicyFromInvoiceCommand
	 */
	public void unAssignPolicy( UnAssignPolicyFromInvoiceCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignPolicy(command.getInvoiceId());
	}
	

	/**
	 * add Payment to Payments
	 * @param		command AssignPaymentsToInvoiceCommand
	 */
	public void addToPayments( AssignPaymentsToInvoiceCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToPayments(command.getInvoiceId(), command.getAddTo());
	}

	/**
	 * remove Payment from Payments
	 * @param		command RemovePaymentsFromInvoiceCommand
	 */
	public void removeFromPayments( RemovePaymentsFromInvoiceCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromPayments(command.getInvoiceId(), command.getRemoveFrom());
	}




//************************************************************************
// Attributes
//************************************************************************
	@Autowired
    private final InvoiceEntityProjector projector;
	private final InvoiceValidator validator;
	private Invoice invoice 	= null;
	private CurrentIdentity identity			= null;
	private static final Logger LOGGER 			=  LoggerFactory.getLogger(InvoiceService.class);
}
