/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.harbormaster.service;

import java.io.IOException;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.*;
import java.util.concurrent.*;

import org.axonframework.commandhandling.gateway.CommandGateway;
import org.axonframework.queryhandling.QueryGateway;
import org.axonframework.queryhandling.QueryUpdateEmitter;
import org.axonframework.messaging.responsetypes.ResponseTypes;
import org.springframework.stereotype.Service;
import org.springframework.beans.BeansException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;

import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.validator.*;

/**
 *
 * <h2>Gateway business service</h2>
 *
 * <p>
 * This class implements the Service design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of Gateway related services in the case of a Gateway business related service failing.</li>
 * <li>Exposes a simpler, uniform Gateway interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill Gateway business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@Service
public class GatewayService 
extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
    /** 
     * Default Constructor 
     */
    public GatewayService()  {
	}

	@Override
	public void setApplicationContext(ApplicationContext ctx) throws BeansException {
		super.setApplicationContext(ctx);

		queryGateway 		= ctx.getBean(QueryGateway.class);
		commandGateway 		= ctx.getBean(CommandGateway.class);
		queryUpdateEmitter  = ctx.getBean(QueryUpdateEmitter.class);
		validator			= ctx.getBean(GatewayValidator.class);
	}


   /**
	* Gateway Business Delegate Factory Method
	*
	* All methods are expected to be self-sufficient.
	*
	* @return 	GatewayService
	*/
	public static GatewayService getGatewayInstance() {
		return( new GatewayService() );
	}
 
   /**
    * Creates the provided command.
    * 
    * @param		command ${class.getCreateCommandAlias()}
    * @exception    ProcessingException
    * @exception	IllegalArgumentException
    * @return		CompletableFuture<UUID>
    */
	public CompletableFuture<UUID> createGateway( CreateGatewayCommand command )
    throws ProcessingException, IllegalArgumentException {

		CompletableFuture<UUID> completableFuture = null;
				
		try {
			// --------------------------------------
        	// assign identity now if none
        	// -------------------------------------- 
			if ( command.getGatewayId() == null )
				command.setGatewayId( UUID.randomUUID() );
				
			// --------------------------------------
        	// validate the command
        	// --------------------------------------    	
        	validator.validate( command );    

    		// ---------------------------------------
    		// issue the CreateGatewayCommand - by convention the future return value for a create command
        	// that is handled by the constructor of an aggregate will return the UUID 
    		// ---------------------------------------
        	completableFuture = commandGateway.send( command );
        	
			LOGGER.log( Level.INFO, "return from Command Gateway for CreateGatewayCommand of Gateway is " + command );
			
        }
        catch (Exception exc) {
            final String errMsg = "Unable to create Gateway - " + exc;
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }        
        
        return completableFuture;
    }

   /**
    * Update the provided command.
    * @param		command UpdateGatewayCommand
    * @return		CompletableFuture<Void>
    * @exception    ProcessingException
    * @exception  	IllegalArgumentException
    */
    public CompletableFuture<Void> updateGateway( UpdateGatewayCommand command ) 
    throws ProcessingException, IllegalArgumentException {
    	CompletableFuture<Void> completableFuture = null;
    	
    	try {       

			// --------------------------------------
        	// validate 
        	// --------------------------------------    	
        	validator.validate( command );    

        	// --------------------------------------
        	// issue the UpdateGatewayCommand and return right away
        	// --------------------------------------    	
        	completableFuture = commandGateway.send( command );
    	}
        catch (Exception exc) {
            final String errMsg = "Unable to save Gateway - " + exc;
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        
    	return completableFuture;
    }
   
   /**
    * Deletes the associatied value object
    * @param		command DeleteGatewayCommand
    * @return		CompletableFuture<Void>
    * @exception 	ProcessingException
    */
    public CompletableFuture<Void> delete( DeleteGatewayCommand command ) 
    throws ProcessingException, IllegalArgumentException {	
    	
    	CompletableFuture<Void> completableFuture = null;
    	
        try {  
			// --------------------------------------
        	// validate the command
        	// --------------------------------------    	
        	validator.validate( command );    
        	
        	// --------------------------------------
        	// issue the DeleteGatewayCommand and return right away
        	// --------------------------------------    	
        	completableFuture = commandGateway.send( command );
        }
        catch (Exception exc) {
            final String errMsg = "Unable to delete Gateway using Id = "  + command.getGatewayId();
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }
        
        return completableFuture;
    }

    /**
     * Method to retrieve the Gateway via GatewayFetchOneSummary
     * @param 	summary GatewayFetchOneSummary 
     * @return 	GatewayFetchOneResponse
     * @exception ProcessingException - Thrown if processing any related problems
     * @exception IllegalArgumentException 
     */
    public Gateway getGateway( GatewayFetchOneSummary summary ) 
    throws ProcessingException, IllegalArgumentException {
    	
    	if( summary == null )
    		throw new IllegalArgumentException( "GatewayFetchOneSummary arg cannot be null" );
    	
    	Gateway entity = null;
    	
        try {
        	// --------------------------------------
        	// validate the fetch one summary
        	// --------------------------------------    	
        	validator.validate( summary );    
        	
        	// --------------------------------------
        	// use queryGateway to send request to Find a Gateway
        	// --------------------------------------
        	CompletableFuture<Gateway> futureEntity = queryGateway.query(new FindGatewayQuery( new LoadGatewayFilter( summary.getGatewayId() ) ), ResponseTypes.instanceOf(Gateway.class));
        	
        	entity = futureEntity.get();
        }
        catch( Exception exc ) {
            final String errMsg = "Unable to locate Gateway with id " + summary.getGatewayId();
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }        
        
        return entity;
    }


    /**
     * Method to retrieve a collection of all Gateways
     *
     * @return 	List<Gateway> 
     * @exception ProcessingException Thrown if any problems
     */
    public List<Gateway> getAllGateway() 
    throws ProcessingException {
        List<Gateway> list = null;

        try {
        	CompletableFuture<List<Gateway>> futureList = queryGateway.query(new FindAllGatewayQuery(), ResponseTypes.multipleInstancesOf(Gateway.class));
        	
        	list = futureList.get();
        }
        catch( Exception exc ) {
            String errMsg = "Failed to get all Gateway";
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }        
        
        return list;
    }

    /**
     * assign Site on Gateway
     * @param		command AssignSiteToGatewayCommand	
     * @exception	ProcessingException
     */     
	public void assignSite( AssignSiteToGatewayCommand command ) throws ProcessingException {

		// --------------------------------------------
		// load the parent
		// --------------------------------------------
		load( command.getGatewayId() );
		
		SiteService childDelegate 	= SiteService.getSiteInstance();
		GatewayService parentDelegate = GatewayService.getGatewayInstance();			
		UUID childId = command.getAssignment().getSiteId();
		Site child = null;
		
		try {
			// --------------------------------------
	    	// best to validate the command now
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );

		}
        catch( Throwable exc ) {
			final String msg = "Failed to get Site using id " + childId;
			LOGGER.log( Level.WARNING,  msg );
			throw new ProcessingException( msg, exc );
        }
	}

    /**
     * unAssign Site on Gateway
     * @param		command UnAssignSiteFromGatewayCommand
     * @exception	ProcessingException
     */     
	public void unAssignSite( UnAssignSiteFromGatewayCommand command ) throws ProcessingException {

		try {
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    
	
	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );
		}
		catch( Exception exc ) {
			final String msg = "Failed to unassign Site on Gateway";
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}
	
    /**
     * assign Room on Gateway
     * @param		command AssignRoomToGatewayCommand	
     * @exception	ProcessingException
     */     
	public void assignRoom( AssignRoomToGatewayCommand command ) throws ProcessingException {

		// --------------------------------------------
		// load the parent
		// --------------------------------------------
		load( command.getGatewayId() );
		
		RoomService childDelegate 	= RoomService.getRoomInstance();
		GatewayService parentDelegate = GatewayService.getGatewayInstance();			
		UUID childId = command.getAssignment().getRoomId();
		Room child = null;
		
		try {
			// --------------------------------------
	    	// best to validate the command now
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );

		}
        catch( Throwable exc ) {
			final String msg = "Failed to get Room using id " + childId;
			LOGGER.log( Level.WARNING,  msg );
			throw new ProcessingException( msg, exc );
        }
	}

    /**
     * unAssign Room on Gateway
     * @param		command UnAssignRoomFromGatewayCommand
     * @exception	ProcessingException
     */     
	public void unAssignRoom( UnAssignRoomFromGatewayCommand command ) throws ProcessingException {

		try {
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    
	
	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );
		}
		catch( Exception exc ) {
			final String msg = "Failed to unassign Room on Gateway";
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}
	
    /**
     * assign DigitalTwin on Gateway
     * @param		command AssignDigitalTwinToGatewayCommand	
     * @exception	ProcessingException
     */     
	public void assignDigitalTwin( AssignDigitalTwinToGatewayCommand command ) throws ProcessingException {

		// --------------------------------------------
		// load the parent
		// --------------------------------------------
		load( command.getGatewayId() );
		
		DigitalTwinService childDelegate 	= DigitalTwinService.getDigitalTwinInstance();
		GatewayService parentDelegate = GatewayService.getGatewayInstance();			
		UUID childId = command.getAssignment().getDigitalTwinId();
		DigitalTwin child = null;
		
		try {
			// --------------------------------------
	    	// best to validate the command now
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );

		}
        catch( Throwable exc ) {
			final String msg = "Failed to get DigitalTwin using id " + childId;
			LOGGER.log( Level.WARNING,  msg );
			throw new ProcessingException( msg, exc );
        }
	}

    /**
     * unAssign DigitalTwin on Gateway
     * @param		command UnAssignDigitalTwinFromGatewayCommand
     * @exception	ProcessingException
     */     
	public void unAssignDigitalTwin( UnAssignDigitalTwinFromGatewayCommand command ) throws ProcessingException {

		try {
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    
	
	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );
		}
		catch( Exception exc ) {
			final String msg = "Failed to unassign DigitalTwin on Gateway";
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}
	

    /**
     * add IoTDevice to Devices 
     * @param		command AssignDevicesToGatewayCommand
     * @exception	ProcessingException
     */     
	public void addToDevices( AssignDevicesToGatewayCommand command ) throws ProcessingException {
		
		
		// -------------------------------------------
		// load the parent
		// -------------------------------------------
		load( command.getGatewayId() );

		IoTDeviceService childDelegate 	= IoTDeviceService.getIoTDeviceInstance();
		GatewayService parentDelegate = GatewayService.getGatewayInstance();		
		UUID childId = command.getAddTo().getIoTDeviceId();
		
		try {		
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );			
		}
		catch( Exception exc ) {
			final String msg = "Failed to add a IoTDevice as Devices to Gateway" ; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}

	}

    /**
     * remove IoTDevice from Devices
     * @param		command RemoveDevicesFromGatewayCommand
     * @exception	ProcessingException
     */     	
	public void removeFromDevices( RemoveDevicesFromGatewayCommand command ) throws ProcessingException {		
		
		IoTDeviceService childDelegate 	= IoTDeviceService.getIoTDeviceInstance();
		UUID childId = command.getRemoveFrom().getIoTDeviceId();

		try {
			
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );

		}
		catch( Exception exc ) {
			final String msg = "Failed to remove child using Id " + childId; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}

    /**
     * add EdgeApplication to EdgeApplications 
     * @param		command AssignEdgeApplicationsToGatewayCommand
     * @exception	ProcessingException
     */     
	public void addToEdgeApplications( AssignEdgeApplicationsToGatewayCommand command ) throws ProcessingException {
		
		
		// -------------------------------------------
		// load the parent
		// -------------------------------------------
		load( command.getGatewayId() );

		EdgeApplicationService childDelegate 	= EdgeApplicationService.getEdgeApplicationInstance();
		GatewayService parentDelegate = GatewayService.getGatewayInstance();		
		UUID childId = command.getAddTo().getEdgeApplicationId();
		
		try {		
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );			
		}
		catch( Exception exc ) {
			final String msg = "Failed to add a EdgeApplication as EdgeApplications to Gateway" ; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}

	}

    /**
     * remove EdgeApplication from EdgeApplications
     * @param		command RemoveEdgeApplicationsFromGatewayCommand
     * @exception	ProcessingException
     */     	
	public void removeFromEdgeApplications( RemoveEdgeApplicationsFromGatewayCommand command ) throws ProcessingException {		
		
		EdgeApplicationService childDelegate 	= EdgeApplicationService.getEdgeApplicationInstance();
		UUID childId = command.getRemoveFrom().getEdgeApplicationId();

		try {
			
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );

		}
		catch( Exception exc ) {
			final String msg = "Failed to remove child using Id " + childId; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}

    /**
     * add DeviceCertificate to Certificates 
     * @param		command AssignCertificatesToGatewayCommand
     * @exception	ProcessingException
     */     
	public void addToCertificates( AssignCertificatesToGatewayCommand command ) throws ProcessingException {
		
		
		// -------------------------------------------
		// load the parent
		// -------------------------------------------
		load( command.getGatewayId() );

		DeviceCertificateService childDelegate 	= DeviceCertificateService.getDeviceCertificateInstance();
		GatewayService parentDelegate = GatewayService.getGatewayInstance();		
		UUID childId = command.getAddTo().getDeviceCertificateId();
		
		try {		
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );			
		}
		catch( Exception exc ) {
			final String msg = "Failed to add a DeviceCertificate as Certificates to Gateway" ; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}

	}

    /**
     * remove DeviceCertificate from Certificates
     * @param		command RemoveCertificatesFromGatewayCommand
     * @exception	ProcessingException
     */     	
	public void removeFromCertificates( RemoveCertificatesFromGatewayCommand command ) throws ProcessingException {		
		
		DeviceCertificateService childDelegate 	= DeviceCertificateService.getDeviceCertificateInstance();
		UUID childId = command.getRemoveFrom().getDeviceCertificateId();

		try {
			
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );

		}
		catch( Exception exc ) {
			final String msg = "Failed to remove child using Id " + childId; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}

    /**
     * add NetworkProfile to NetworkProfiles 
     * @param		command AssignNetworkProfilesToGatewayCommand
     * @exception	ProcessingException
     */     
	public void addToNetworkProfiles( AssignNetworkProfilesToGatewayCommand command ) throws ProcessingException {
		
		
		// -------------------------------------------
		// load the parent
		// -------------------------------------------
		load( command.getGatewayId() );

		NetworkProfileService childDelegate 	= NetworkProfileService.getNetworkProfileInstance();
		GatewayService parentDelegate = GatewayService.getGatewayInstance();		
		UUID childId = command.getAddTo().getNetworkProfileId();
		
		try {		
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );			
		}
		catch( Exception exc ) {
			final String msg = "Failed to add a NetworkProfile as NetworkProfiles to Gateway" ; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}

	}

    /**
     * remove NetworkProfile from NetworkProfiles
     * @param		command RemoveNetworkProfilesFromGatewayCommand
     * @exception	ProcessingException
     */     	
	public void removeFromNetworkProfiles( RemoveNetworkProfilesFromGatewayCommand command ) throws ProcessingException {		
		
		NetworkProfileService childDelegate 	= NetworkProfileService.getNetworkProfileInstance();
		UUID childId = command.getRemoveFrom().getNetworkProfileId();

		try {
			
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );

		}
		catch( Exception exc ) {
			final String msg = "Failed to remove child using Id " + childId; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}



	/**
	 * Internal helper method to load the root 
	 * 
	 * @param		id	UUID
	 * @return		Gateway
	 */
	protected Gateway load( UUID id ) throws ProcessingException {
		gateway = GatewayService.getGatewayInstance().getGateway( new GatewayFetchOneSummary(id) );	
		return gateway;
	}


//************************************************************************
// Attributes
//************************************************************************
	private QueryGateway queryGateway;
	private CommandGateway commandGateway;
	private QueryUpdateEmitter queryUpdateEmitter;
	private GatewayValidator validator;
	private Gateway gateway 	= null;
    private static final Logger LOGGER 			= Logger.getLogger(GatewayService.class.getName());
    
}
