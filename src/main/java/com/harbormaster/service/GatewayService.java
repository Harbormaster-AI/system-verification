/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */

package com.harbormaster.service;

import java.io.IOException;
import java.util.*;
import java.util.concurrent.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import org.springframework.stereotype.Service;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;


import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.repository.*;
import com.harbormaster.validator.*;
import com.harbormaster.projector.*;
import com.harbormaster.security.*;

/**
 *
 * <h2>Gateway business delegate</h2>
 *
 * <p>
 * This class implements the Business Delegate design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of Gateway related services in the case of a Gateway business related service failing.</li>
 * <li>Exposes a simpler, uniform Gateway interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill Gateway business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Spring Boot 3.5</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>ServiceLayer</td></tr>
 *          <tr><td>architecture style</td><td>Layered</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@Service
public class GatewayService 
		extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
	/**
	 * Default Constructor
	 */
    public GatewayService(CurrentIdentity identity,
				ApplicationContext applicationContext)  {

		this.identity		= identity;
		this.projector 		= new GatewayEntityProjector( applicationContext.getBean(ProjectorRegistry.class),
										applicationContext.getBean(GatewayRepository.class) );
		this.validator		= applicationContext.getBean(GatewayValidator.class) ;
	}


	/**
	 * Creates the provided command.
	 *
	 * @param		command ${class.getCreateCommandAlias()}
	 * @return		Gateway
	 */
		public Gateway createGateway( CreateGatewayCommand command ) {

		Gateway entity = new Gateway();

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

            entity.setGatewayId( command.getGatewayId() );
            entity.setSoftwareVersion( command.getSoftwareVersion() );
            entity.setStatus( command.getStatus() );

		// ------------------------------------------
		// persist a new one
		// ------------------------------------------
		entity = projector.create(entity);

		LOGGER.info( "done creating of Gateway {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Update the provided command.
	 * @param		command UpdateGatewayCommand
	 * @return		Gateway
	 */
	public Gateway updateGateway( UpdateGatewayCommand command ) {

		Gateway entity = new Gateway();

		// --------------------------------------
		// validate
		// --------------------------------------
		validator.validate( command );

            entity.setGatewayId( command.getGatewayId() );
            entity.setSoftwareVersion( command.getSoftwareVersion() );
            entity.setSite( command.getSite() );
            entity.setRoom( command.getRoom() );
            entity.setDevices( command.getDevices() );
            entity.setEdgeApplications( command.getEdgeApplications() );
            entity.setCertificates( command.getCertificates() );
            entity.setDigitalTwin( command.getDigitalTwin() );
            entity.setNetworkProfiles( command.getNetworkProfiles() );
            entity.setStatus( command.getStatus() );

		// ------------------------------------------
		// persist an existing one
		// ------------------------------------------
		entity = projector.update(entity);

		LOGGER.info( "done saving of Gateway {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Deletes the associatied value object
	 * @param		command DeleteGatewayCommand
	 */
	public void delete( DeleteGatewayCommand command ) {
		UUID id = null;

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		id = command.getGatewayId();

		// ------------------------------------------
		// delete the entity
		// ------------------------------------------
		projector.delete(id);

		LOGGER.info( "done deleting of Gateway {0} ", id );

	}

	/**
	 * Method to retrieve the Gateway via GatewayFetchOneSummary
	 * @param 	summary GatewayFetchOneSummary
	 * @return 	GatewayFetchOneResponse
	 */
	public Gateway getGateway( GatewayFetchOneSummary summary ) {

		if( summary == null )
			throw new IllegalStateException( "GatewayFetchOneSummary arg cannot be null" );

		Gateway entity = null;
		UUID id = summary.getGatewayId();

		// --------------------------------------
		// validate the fetch one summary
		// --------------------------------------
		validator.validate( summary );

		// --------------------------------------
		// find a Gateway using the provided id
		// --------------------------------------
		entity = projector.find( id );

		return entity;
	}


	/**
	 * Method to retrieve a collection of all Gateways
	 *
	 * @return 	List<Gateway>
	 */
    public List<Gateway> getAllGateway() {
		List<Gateway> list = projector.findAll( new FindAllGatewayQuery() );
		return list;
	}

	/**
	 * assign Site on Gateway
	 * @param		command AssignSiteToGatewayCommand
	 */
	public void assignSite( AssignSiteToGatewayCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignSite(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Site on Gateway
	 * @param		command UnAssignSiteFromGatewayCommand
	 */
	public void unAssignSite( UnAssignSiteFromGatewayCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignSite(command.getChildId());
	}
	
	/**
	 * assign Room on Gateway
	 * @param		command AssignRoomToGatewayCommand
	 */
	public void assignRoom( AssignRoomToGatewayCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignRoom(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Room on Gateway
	 * @param		command UnAssignRoomFromGatewayCommand
	 */
	public void unAssignRoom( UnAssignRoomFromGatewayCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignRoom(command.getChildId());
	}
	
	/**
	 * assign DigitalTwin on Gateway
	 * @param		command AssignDigitalTwinToGatewayCommand
	 */
	public void assignDigitalTwin( AssignDigitalTwinToGatewayCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignDigitalTwin(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign DigitalTwin on Gateway
	 * @param		command UnAssignDigitalTwinFromGatewayCommand
	 */
	public void unAssignDigitalTwin( UnAssignDigitalTwinFromGatewayCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignDigitalTwin(command.getChildId());
	}
	

	/**
	 * add IoTDevice to Devices
	 * @param		command AssignDevicesToGatewayCommand
	 */
	public void addToDevices( AssignDevicesToGatewayCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToDevices(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove IoTDevice from Devices
	 * @param		command RemoveDevicesFromGatewayCommand
	 */
	public void removeFromDevices( RemoveDevicesFromGatewayCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromDevices(command.getParentId(), command.getChildIds());
	}

	/**
	 * add EdgeApplication to EdgeApplications
	 * @param		command AssignEdgeApplicationsToGatewayCommand
	 */
	public void addToEdgeApplications( AssignEdgeApplicationsToGatewayCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToEdgeApplications(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove EdgeApplication from EdgeApplications
	 * @param		command RemoveEdgeApplicationsFromGatewayCommand
	 */
	public void removeFromEdgeApplications( RemoveEdgeApplicationsFromGatewayCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromEdgeApplications(command.getParentId(), command.getChildIds());
	}

	/**
	 * add DeviceCertificate to Certificates
	 * @param		command AssignCertificatesToGatewayCommand
	 */
	public void addToCertificates( AssignCertificatesToGatewayCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToCertificates(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove DeviceCertificate from Certificates
	 * @param		command RemoveCertificatesFromGatewayCommand
	 */
	public void removeFromCertificates( RemoveCertificatesFromGatewayCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromCertificates(command.getParentId(), command.getChildIds());
	}

	/**
	 * add NetworkProfile to NetworkProfiles
	 * @param		command AssignNetworkProfilesToGatewayCommand
	 */
	public void addToNetworkProfiles( AssignNetworkProfilesToGatewayCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToNetworkProfiles(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove NetworkProfile from NetworkProfiles
	 * @param		command RemoveNetworkProfilesFromGatewayCommand
	 */
	public void removeFromNetworkProfiles( RemoveNetworkProfilesFromGatewayCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromNetworkProfiles(command.getParentId(), command.getChildIds());
	}




//************************************************************************
// Attributes
//************************************************************************
	@Autowired
    private final GatewayEntityProjector projector;
	private final GatewayValidator validator;
	private Gateway gateway 	= null;
	private CurrentIdentity identity			= null;
	private static final Logger LOGGER 			=  LoggerFactory.getLogger(GatewayService.class);
}
