/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */

package com.harbormaster.service;

import java.io.IOException;
import java.util.*;
import java.util.concurrent.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import org.springframework.stereotype.Service;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;


import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.repository.*;
import com.harbormaster.validator.*;
import com.harbormaster.projector.*;
import com.harbormaster.security.*;

/**
 *
 * <h2>DeviceModel business delegate</h2>
 *
 * <p>
 * This class implements the Business Delegate design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of DeviceModel related services in the case of a DeviceModel business related service failing.</li>
 * <li>Exposes a simpler, uniform DeviceModel interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill DeviceModel business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Spring Boot 3.5</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>ServiceLayer</td></tr>
 *          <tr><td>architecture style</td><td>Layered</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@Service
public class DeviceModelService 
		extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
	/**
	 * Default Constructor
	 */
    public DeviceModelService(CurrentIdentity identity,
				ApplicationContext applicationContext)  {

		this.identity		= identity;
		this.projector 		= new DeviceModelEntityProjector( applicationContext.getBean(ProjectorRegistry.class),
										applicationContext.getBean(DeviceModelRepository.class) );
		this.validator		= applicationContext.getBean(DeviceModelValidator.class) ;
	}


	/**
	 * Creates the provided command.
	 *
	 * @param		command ${class.getCreateCommandAlias()}
	 * @return		DeviceModel
	 */
		public DeviceModel createDeviceModel( CreateDeviceModelCommand command ) {

		DeviceModel entity = new DeviceModel();

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

            entity.setDeviceModelId( command.getDeviceModelId() );
            entity.setName( command.getName() );
            entity.setModelNumber( command.getModelNumber() );
            entity.setHardwareRevision( command.getHardwareRevision() );
            entity.setSupportedConnectivity( command.getSupportedConnectivity() );
            entity.setDefaultTelemetryEncoding( command.getDefaultTelemetryEncoding() );

		// ------------------------------------------
		// persist a new one
		// ------------------------------------------
		entity = projector.create(entity);

		LOGGER.info( "done creating of DeviceModel {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Update the provided command.
	 * @param		command UpdateDeviceModelCommand
	 * @return		DeviceModel
	 */
	public DeviceModel updateDeviceModel( UpdateDeviceModelCommand command ) {

		DeviceModel entity = new DeviceModel();

		// --------------------------------------
		// validate
		// --------------------------------------
		validator.validate( command );

            entity.setDeviceModelId( command.getDeviceModelId() );
            entity.setName( command.getName() );
            entity.setModelNumber( command.getModelNumber() );
            entity.setHardwareRevision( command.getHardwareRevision() );
            entity.setVendor( command.getVendor() );
            entity.setHardwareModules( command.getHardwareModules() );
            entity.setTwinTemplate( command.getTwinTemplate() );
            entity.setFirmwareReleases( command.getFirmwareReleases() );
            entity.setCommandDefinitions( command.getCommandDefinitions() );
            entity.setSupportedConnectivity( command.getSupportedConnectivity() );
            entity.setDefaultTelemetryEncoding( command.getDefaultTelemetryEncoding() );

		// ------------------------------------------
		// persist an existing one
		// ------------------------------------------
		entity = projector.update(entity);

		LOGGER.info( "done saving of DeviceModel {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Deletes the associatied value object
	 * @param		command DeleteDeviceModelCommand
	 */
	public void delete( DeleteDeviceModelCommand command ) {
		UUID id = null;

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		id = command.getDeviceModelId();

		// ------------------------------------------
		// delete the entity
		// ------------------------------------------
		projector.delete(id);

		LOGGER.info( "done deleting of DeviceModel {0} ", id );

	}

	/**
	 * Method to retrieve the DeviceModel via DeviceModelFetchOneSummary
	 * @param 	summary DeviceModelFetchOneSummary
	 * @return 	DeviceModelFetchOneResponse
	 */
	public DeviceModel getDeviceModel( DeviceModelFetchOneSummary summary ) {

		if( summary == null )
			throw new IllegalStateException( "DeviceModelFetchOneSummary arg cannot be null" );

		DeviceModel entity = null;
		UUID id = summary.getDeviceModelId();

		// --------------------------------------
		// validate the fetch one summary
		// --------------------------------------
		validator.validate( summary );

		// --------------------------------------
		// find a DeviceModel using the provided id
		// --------------------------------------
		entity = projector.find( id );

		return entity;
	}


	/**
	 * Method to retrieve a collection of all DeviceModels
	 *
	 * @return 	List<DeviceModel>
	 */
    public List<DeviceModel> getAllDeviceModel() {
		List<DeviceModel> list = projector.findAll( new FindAllDeviceModelQuery() );
		return list;
	}

	/**
	 * assign Vendor on DeviceModel
	 * @param		command AssignVendorToDeviceModelCommand
	 */
	public void assignVendor( AssignVendorToDeviceModelCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignVendor(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Vendor on DeviceModel
	 * @param		command UnAssignVendorFromDeviceModelCommand
	 */
	public void unAssignVendor( UnAssignVendorFromDeviceModelCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignVendor(command.getChildId());
	}
	
	/**
	 * assign TwinTemplate on DeviceModel
	 * @param		command AssignTwinTemplateToDeviceModelCommand
	 */
	public void assignTwinTemplate( AssignTwinTemplateToDeviceModelCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignTwinTemplate(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign TwinTemplate on DeviceModel
	 * @param		command UnAssignTwinTemplateFromDeviceModelCommand
	 */
	public void unAssignTwinTemplate( UnAssignTwinTemplateFromDeviceModelCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignTwinTemplate(command.getChildId());
	}
	

	/**
	 * add HardwareModule to HardwareModules
	 * @param		command AssignHardwareModulesToDeviceModelCommand
	 */
	public void addToHardwareModules( AssignHardwareModulesToDeviceModelCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToHardwareModules(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove HardwareModule from HardwareModules
	 * @param		command RemoveHardwareModulesFromDeviceModelCommand
	 */
	public void removeFromHardwareModules( RemoveHardwareModulesFromDeviceModelCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromHardwareModules(command.getParentId(), command.getChildIds());
	}

	/**
	 * add FirmwareRelease to FirmwareReleases
	 * @param		command AssignFirmwareReleasesToDeviceModelCommand
	 */
	public void addToFirmwareReleases( AssignFirmwareReleasesToDeviceModelCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToFirmwareReleases(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove FirmwareRelease from FirmwareReleases
	 * @param		command RemoveFirmwareReleasesFromDeviceModelCommand
	 */
	public void removeFromFirmwareReleases( RemoveFirmwareReleasesFromDeviceModelCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromFirmwareReleases(command.getParentId(), command.getChildIds());
	}

	/**
	 * add CommandDefinition to CommandDefinitions
	 * @param		command AssignCommandDefinitionsToDeviceModelCommand
	 */
	public void addToCommandDefinitions( AssignCommandDefinitionsToDeviceModelCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToCommandDefinitions(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove CommandDefinition from CommandDefinitions
	 * @param		command RemoveCommandDefinitionsFromDeviceModelCommand
	 */
	public void removeFromCommandDefinitions( RemoveCommandDefinitionsFromDeviceModelCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromCommandDefinitions(command.getParentId(), command.getChildIds());
	}




//************************************************************************
// Attributes
//************************************************************************
	@Autowired
    private final DeviceModelEntityProjector projector;
	private final DeviceModelValidator validator;
	private DeviceModel deviceModel 	= null;
	private CurrentIdentity identity			= null;
	private static final Logger LOGGER 			=  LoggerFactory.getLogger(DeviceModelService.class);
}
