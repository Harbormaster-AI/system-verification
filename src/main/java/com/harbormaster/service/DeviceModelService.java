/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.harbormaster.service;

import java.io.IOException;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.*;
import java.util.concurrent.*;

import org.axonframework.commandhandling.gateway.CommandGateway;
import org.axonframework.queryhandling.QueryGateway;
import org.axonframework.queryhandling.QueryUpdateEmitter;
import org.axonframework.messaging.responsetypes.ResponseTypes;
import org.springframework.stereotype.Service;
import org.springframework.beans.BeansException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;

import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.validator.*;

/**
 *
 * <h2>DeviceModel business service</h2>
 *
 * <p>
 * This class implements the Service design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of DeviceModel related services in the case of a DeviceModel business related service failing.</li>
 * <li>Exposes a simpler, uniform DeviceModel interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill DeviceModel business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@Service
public class DeviceModelService 
extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
    /** 
     * Default Constructor 
     */
    public DeviceModelService()  {
	}

	@Override
	public void setApplicationContext(ApplicationContext ctx) throws BeansException {
		super.setApplicationContext(ctx);

		queryGateway 		= ctx.getBean(QueryGateway.class);
		commandGateway 		= ctx.getBean(CommandGateway.class);
		queryUpdateEmitter  = ctx.getBean(QueryUpdateEmitter.class);
		validator			= ctx.getBean(DeviceModelValidator.class);
	}


   /**
	* DeviceModel Business Delegate Factory Method
	*
	* All methods are expected to be self-sufficient.
	*
	* @return 	DeviceModelService
	*/
	public static DeviceModelService getDeviceModelInstance() {
		return( new DeviceModelService() );
	}
 
   /**
    * Creates the provided command.
    * 
    * @param		command ${class.getCreateCommandAlias()}
    * @exception    ProcessingException
    * @exception	IllegalArgumentException
    * @return		CompletableFuture<UUID>
    */
	public CompletableFuture<UUID> createDeviceModel( CreateDeviceModelCommand command )
    throws ProcessingException, IllegalArgumentException {

		CompletableFuture<UUID> completableFuture = null;
				
		try {
			// --------------------------------------
        	// assign identity now if none
        	// -------------------------------------- 
			if ( command.getDeviceModelId() == null )
				command.setDeviceModelId( UUID.randomUUID() );
				
			// --------------------------------------
        	// validate the command
        	// --------------------------------------    	
        	validator.validate( command );    

    		// ---------------------------------------
    		// issue the CreateDeviceModelCommand - by convention the future return value for a create command
        	// that is handled by the constructor of an aggregate will return the UUID 
    		// ---------------------------------------
        	completableFuture = commandGateway.send( command );
        	
			LOGGER.log( Level.INFO, "return from Command Gateway for CreateDeviceModelCommand of DeviceModel is " + command );
			
        }
        catch (Exception exc) {
            final String errMsg = "Unable to create DeviceModel - " + exc;
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }        
        
        return completableFuture;
    }

   /**
    * Update the provided command.
    * @param		command UpdateDeviceModelCommand
    * @return		CompletableFuture<Void>
    * @exception    ProcessingException
    * @exception  	IllegalArgumentException
    */
    public CompletableFuture<Void> updateDeviceModel( UpdateDeviceModelCommand command ) 
    throws ProcessingException, IllegalArgumentException {
    	CompletableFuture<Void> completableFuture = null;
    	
    	try {       

			// --------------------------------------
        	// validate 
        	// --------------------------------------    	
        	validator.validate( command );    

        	// --------------------------------------
        	// issue the UpdateDeviceModelCommand and return right away
        	// --------------------------------------    	
        	completableFuture = commandGateway.send( command );
    	}
        catch (Exception exc) {
            final String errMsg = "Unable to save DeviceModel - " + exc;
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        
    	return completableFuture;
    }
   
   /**
    * Deletes the associatied value object
    * @param		command DeleteDeviceModelCommand
    * @return		CompletableFuture<Void>
    * @exception 	ProcessingException
    */
    public CompletableFuture<Void> delete( DeleteDeviceModelCommand command ) 
    throws ProcessingException, IllegalArgumentException {	
    	
    	CompletableFuture<Void> completableFuture = null;
    	
        try {  
			// --------------------------------------
        	// validate the command
        	// --------------------------------------    	
        	validator.validate( command );    
        	
        	// --------------------------------------
        	// issue the DeleteDeviceModelCommand and return right away
        	// --------------------------------------    	
        	completableFuture = commandGateway.send( command );
        }
        catch (Exception exc) {
            final String errMsg = "Unable to delete DeviceModel using Id = "  + command.getDeviceModelId();
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }
        
        return completableFuture;
    }

    /**
     * Method to retrieve the DeviceModel via DeviceModelFetchOneSummary
     * @param 	summary DeviceModelFetchOneSummary 
     * @return 	DeviceModelFetchOneResponse
     * @exception ProcessingException - Thrown if processing any related problems
     * @exception IllegalArgumentException 
     */
    public DeviceModel getDeviceModel( DeviceModelFetchOneSummary summary ) 
    throws ProcessingException, IllegalArgumentException {
    	
    	if( summary == null )
    		throw new IllegalArgumentException( "DeviceModelFetchOneSummary arg cannot be null" );
    	
    	DeviceModel entity = null;
    	
        try {
        	// --------------------------------------
        	// validate the fetch one summary
        	// --------------------------------------    	
        	validator.validate( summary );    
        	
        	// --------------------------------------
        	// use queryGateway to send request to Find a DeviceModel
        	// --------------------------------------
        	CompletableFuture<DeviceModel> futureEntity = queryGateway.query(new FindDeviceModelQuery( new LoadDeviceModelFilter( summary.getDeviceModelId() ) ), ResponseTypes.instanceOf(DeviceModel.class));
        	
        	entity = futureEntity.get();
        }
        catch( Exception exc ) {
            final String errMsg = "Unable to locate DeviceModel with id " + summary.getDeviceModelId();
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }        
        
        return entity;
    }


    /**
     * Method to retrieve a collection of all DeviceModels
     *
     * @return 	List<DeviceModel> 
     * @exception ProcessingException Thrown if any problems
     */
    public List<DeviceModel> getAllDeviceModel() 
    throws ProcessingException {
        List<DeviceModel> list = null;

        try {
        	CompletableFuture<List<DeviceModel>> futureList = queryGateway.query(new FindAllDeviceModelQuery(), ResponseTypes.multipleInstancesOf(DeviceModel.class));
        	
        	list = futureList.get();
        }
        catch( Exception exc ) {
            String errMsg = "Failed to get all DeviceModel";
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }        
        
        return list;
    }

    /**
     * assign Vendor on DeviceModel
     * @param		command AssignVendorToDeviceModelCommand	
     * @exception	ProcessingException
     */     
	public void assignVendor( AssignVendorToDeviceModelCommand command ) throws ProcessingException {

		// --------------------------------------------
		// load the parent
		// --------------------------------------------
		load( command.getDeviceModelId() );
		
		DeviceVendorService childDelegate 	= DeviceVendorService.getDeviceVendorInstance();
		DeviceModelService parentDelegate = DeviceModelService.getDeviceModelInstance();			
		UUID childId = command.getAssignment().getDeviceVendorId();
		DeviceVendor child = null;
		
		try {
			// --------------------------------------
	    	// best to validate the command now
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );

		}
        catch( Throwable exc ) {
			final String msg = "Failed to get DeviceVendor using id " + childId;
			LOGGER.log( Level.WARNING,  msg );
			throw new ProcessingException( msg, exc );
        }
	}

    /**
     * unAssign Vendor on DeviceModel
     * @param		command UnAssignVendorFromDeviceModelCommand
     * @exception	ProcessingException
     */     
	public void unAssignVendor( UnAssignVendorFromDeviceModelCommand command ) throws ProcessingException {

		try {
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    
	
	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );
		}
		catch( Exception exc ) {
			final String msg = "Failed to unassign Vendor on DeviceModel";
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}
	
    /**
     * assign TwinTemplate on DeviceModel
     * @param		command AssignTwinTemplateToDeviceModelCommand	
     * @exception	ProcessingException
     */     
	public void assignTwinTemplate( AssignTwinTemplateToDeviceModelCommand command ) throws ProcessingException {

		// --------------------------------------------
		// load the parent
		// --------------------------------------------
		load( command.getDeviceModelId() );
		
		TwinTemplateService childDelegate 	= TwinTemplateService.getTwinTemplateInstance();
		DeviceModelService parentDelegate = DeviceModelService.getDeviceModelInstance();			
		UUID childId = command.getAssignment().getTwinTemplateId();
		TwinTemplate child = null;
		
		try {
			// --------------------------------------
	    	// best to validate the command now
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );

		}
        catch( Throwable exc ) {
			final String msg = "Failed to get TwinTemplate using id " + childId;
			LOGGER.log( Level.WARNING,  msg );
			throw new ProcessingException( msg, exc );
        }
	}

    /**
     * unAssign TwinTemplate on DeviceModel
     * @param		command UnAssignTwinTemplateFromDeviceModelCommand
     * @exception	ProcessingException
     */     
	public void unAssignTwinTemplate( UnAssignTwinTemplateFromDeviceModelCommand command ) throws ProcessingException {

		try {
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    
	
	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );
		}
		catch( Exception exc ) {
			final String msg = "Failed to unassign TwinTemplate on DeviceModel";
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}
	

    /**
     * add HardwareModule to HardwareModules 
     * @param		command AssignHardwareModulesToDeviceModelCommand
     * @exception	ProcessingException
     */     
	public void addToHardwareModules( AssignHardwareModulesToDeviceModelCommand command ) throws ProcessingException {
		
		
		// -------------------------------------------
		// load the parent
		// -------------------------------------------
		load( command.getDeviceModelId() );

		HardwareModuleService childDelegate 	= HardwareModuleService.getHardwareModuleInstance();
		DeviceModelService parentDelegate = DeviceModelService.getDeviceModelInstance();		
		UUID childId = command.getAddTo().getHardwareModuleId();
		
		try {		
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );			
		}
		catch( Exception exc ) {
			final String msg = "Failed to add a HardwareModule as HardwareModules to DeviceModel" ; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}

	}

    /**
     * remove HardwareModule from HardwareModules
     * @param		command RemoveHardwareModulesFromDeviceModelCommand
     * @exception	ProcessingException
     */     	
	public void removeFromHardwareModules( RemoveHardwareModulesFromDeviceModelCommand command ) throws ProcessingException {		
		
		HardwareModuleService childDelegate 	= HardwareModuleService.getHardwareModuleInstance();
		UUID childId = command.getRemoveFrom().getHardwareModuleId();

		try {
			
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );

		}
		catch( Exception exc ) {
			final String msg = "Failed to remove child using Id " + childId; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}

    /**
     * add FirmwareRelease to FirmwareReleases 
     * @param		command AssignFirmwareReleasesToDeviceModelCommand
     * @exception	ProcessingException
     */     
	public void addToFirmwareReleases( AssignFirmwareReleasesToDeviceModelCommand command ) throws ProcessingException {
		
		
		// -------------------------------------------
		// load the parent
		// -------------------------------------------
		load( command.getDeviceModelId() );

		FirmwareReleaseService childDelegate 	= FirmwareReleaseService.getFirmwareReleaseInstance();
		DeviceModelService parentDelegate = DeviceModelService.getDeviceModelInstance();		
		UUID childId = command.getAddTo().getFirmwareReleaseId();
		
		try {		
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );			
		}
		catch( Exception exc ) {
			final String msg = "Failed to add a FirmwareRelease as FirmwareReleases to DeviceModel" ; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}

	}

    /**
     * remove FirmwareRelease from FirmwareReleases
     * @param		command RemoveFirmwareReleasesFromDeviceModelCommand
     * @exception	ProcessingException
     */     	
	public void removeFromFirmwareReleases( RemoveFirmwareReleasesFromDeviceModelCommand command ) throws ProcessingException {		
		
		FirmwareReleaseService childDelegate 	= FirmwareReleaseService.getFirmwareReleaseInstance();
		UUID childId = command.getRemoveFrom().getFirmwareReleaseId();

		try {
			
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );

		}
		catch( Exception exc ) {
			final String msg = "Failed to remove child using Id " + childId; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}

    /**
     * add CommandDefinition to CommandDefinitions 
     * @param		command AssignCommandDefinitionsToDeviceModelCommand
     * @exception	ProcessingException
     */     
	public void addToCommandDefinitions( AssignCommandDefinitionsToDeviceModelCommand command ) throws ProcessingException {
		
		
		// -------------------------------------------
		// load the parent
		// -------------------------------------------
		load( command.getDeviceModelId() );

		CommandDefinitionService childDelegate 	= CommandDefinitionService.getCommandDefinitionInstance();
		DeviceModelService parentDelegate = DeviceModelService.getDeviceModelInstance();		
		UUID childId = command.getAddTo().getCommandDefinitionId();
		
		try {		
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );			
		}
		catch( Exception exc ) {
			final String msg = "Failed to add a CommandDefinition as CommandDefinitions to DeviceModel" ; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}

	}

    /**
     * remove CommandDefinition from CommandDefinitions
     * @param		command RemoveCommandDefinitionsFromDeviceModelCommand
     * @exception	ProcessingException
     */     	
	public void removeFromCommandDefinitions( RemoveCommandDefinitionsFromDeviceModelCommand command ) throws ProcessingException {		
		
		CommandDefinitionService childDelegate 	= CommandDefinitionService.getCommandDefinitionInstance();
		UUID childId = command.getRemoveFrom().getCommandDefinitionId();

		try {
			
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );

		}
		catch( Exception exc ) {
			final String msg = "Failed to remove child using Id " + childId; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}



	/**
	 * Internal helper method to load the root 
	 * 
	 * @param		id	UUID
	 * @return		DeviceModel
	 */
	protected DeviceModel load( UUID id ) throws ProcessingException {
		deviceModel = DeviceModelService.getDeviceModelInstance().getDeviceModel( new DeviceModelFetchOneSummary(id) );	
		return deviceModel;
	}


//************************************************************************
// Attributes
//************************************************************************
	private QueryGateway queryGateway;
	private CommandGateway commandGateway;
	private QueryUpdateEmitter queryUpdateEmitter;
	private DeviceModelValidator validator;
	private DeviceModel deviceModel 	= null;
    private static final Logger LOGGER 			= Logger.getLogger(DeviceModelService.class.getName());
    
}
