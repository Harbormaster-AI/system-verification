/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Insurance Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */

package com.harbormaster.service;

import java.io.IOException;
import java.util.*;
import java.util.concurrent.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import org.springframework.stereotype.Service;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;


import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.repository.*;
import com.harbormaster.validator.*;
import com.harbormaster.projector.*;
import com.harbormaster.security.*;

/**
 *
 * <h2>Payment business delegate</h2>
 *
 * <p>
 * This class implements the Business Delegate design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of Payment related services in the case of a Payment business related service failing.</li>
 * <li>Exposes a simpler, uniform Payment interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill Payment business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Spring Boot 3.5</td></tr>
 *          <tr><td>published</td><td>09/12/2026</td></tr>
 *          <tr><td>design pattern</td><td>ServiceLayer</td></tr>
 *          <tr><td>architecture style</td><td>Layered</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Insurance Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core insurance product, underwriting, policy, billing, claims, and reinsurance capabilities.</td></tr>
 *          <tr><td>industry</td><td>Insurance</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@Service
public class PaymentService 
		extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
	/**
	 * Default Constructor
	 */
    public PaymentService(CurrentIdentity identity,
				ApplicationContext applicationContext)  {

		this.identity		= identity;
		this.projector 		= new PaymentEntityProjector( applicationContext.getBean(ProjectorRegistry.class),
										applicationContext.getBean(PaymentRepository.class) );
		this.validator		= applicationContext.getBean(PaymentValidator.class) ;
	}


	/**
	 * Creates the provided command.
	 *
	 * @param		command ${class.getCreateCommandAlias()}
	 * @return		Payment
	 */
		public Payment createPayment( CreatePaymentCommand command ) {

		Payment entity = new Payment();

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

            entity.setPaymentId( command.getPaymentId() );
            entity.setPaymentReference( command.getPaymentReference() );
            entity.setAmount( command.getAmount() );
            entity.setPaymentDate( command.getPaymentDate() );
            entity.setMethod( command.getMethod() );
            entity.setStatus( command.getStatus() );

		// ------------------------------------------
		// persist a new one
		// ------------------------------------------
		entity = projector.create(entity);

		LOGGER.info( "done creating of Payment {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Update the provided command.
	 * @param		command UpdatePaymentCommand
	 * @return		Payment
	 */
	public Payment updatePayment( UpdatePaymentCommand command ) {

		Payment entity = new Payment();

		// --------------------------------------
		// validate
		// --------------------------------------
		validator.validate( command );

            entity.setPaymentId( command.getPaymentId() );
            entity.setPaymentReference( command.getPaymentReference() );
            entity.setAmount( command.getAmount() );
            entity.setPaymentDate( command.getPaymentDate() );
            entity.setInvoice( command.getInvoice() );
            entity.setBillingAccount( command.getBillingAccount() );
            entity.setPolicy( command.getPolicy() );
            entity.setMethod( command.getMethod() );
            entity.setStatus( command.getStatus() );

		// ------------------------------------------
		// persist an existing one
		// ------------------------------------------
		entity = projector.update(entity);

		LOGGER.info( "done saving of Payment {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Deletes the associatied value object
	 * @param		command DeletePaymentCommand
	 */
	public void delete( DeletePaymentCommand command ) {
		UUID id = null;

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		id = command.getPaymentId();

		// ------------------------------------------
		// delete the entity
		// ------------------------------------------
		projector.delete(id);

		LOGGER.info( "done deleting of Payment {0} ", id );

	}

	/**
	 * Method to retrieve the Payment via PaymentFetchOneSummary
	 * @param 	summary PaymentFetchOneSummary
	 * @return 	PaymentFetchOneResponse
	 */
	public Payment getPayment( PaymentFetchOneSummary summary ) {

		if( summary == null )
			throw new IllegalStateException( "PaymentFetchOneSummary arg cannot be null" );

		Payment entity = null;
		UUID id = summary.getPaymentId();

		// --------------------------------------
		// validate the fetch one summary
		// --------------------------------------
		validator.validate( summary );

		// --------------------------------------
		// find a Payment using the provided id
		// --------------------------------------
		entity = projector.find( id );

		return entity;
	}


	/**
	 * Method to retrieve a collection of all Payments
	 *
	 * @return 	List<Payment>
	 */
    public List<Payment> getAllPayment() {
		List<Payment> list = projector.findAll( new FindAllPaymentQuery() );
		return list;
	}

	/**
	 * assign Invoice on Payment
	 * @param		command AssignInvoiceToPaymentCommand
	 */
	public void assignInvoice( AssignInvoiceToPaymentCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignInvoice(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Invoice on Payment
	 * @param		command UnAssignInvoiceFromPaymentCommand
	 */
	public void unAssignInvoice( UnAssignInvoiceFromPaymentCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignInvoice(command.getChildId());
	}
	
	/**
	 * assign BillingAccount on Payment
	 * @param		command AssignBillingAccountToPaymentCommand
	 */
	public void assignBillingAccount( AssignBillingAccountToPaymentCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignBillingAccount(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign BillingAccount on Payment
	 * @param		command UnAssignBillingAccountFromPaymentCommand
	 */
	public void unAssignBillingAccount( UnAssignBillingAccountFromPaymentCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignBillingAccount(command.getChildId());
	}
	
	/**
	 * assign Policy on Payment
	 * @param		command AssignPolicyToPaymentCommand
	 */
	public void assignPolicy( AssignPolicyToPaymentCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignPolicy(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Policy on Payment
	 * @param		command UnAssignPolicyFromPaymentCommand
	 */
	public void unAssignPolicy( UnAssignPolicyFromPaymentCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignPolicy(command.getChildId());
	}
	




//************************************************************************
// Attributes
//************************************************************************
	@Autowired
    private final PaymentEntityProjector projector;
	private final PaymentValidator validator;
	private Payment payment 	= null;
	private CurrentIdentity identity			= null;
	private static final Logger LOGGER 			=  LoggerFactory.getLogger(PaymentService.class);
}
