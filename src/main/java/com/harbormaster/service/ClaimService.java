/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Insurance Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.service;

import java.io.IOException;
import java.util.*;
import java.util.concurrent.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import org.springframework.stereotype.Service;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;


import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.repository.*;
import com.harbormaster.validator.*;
import com.harbormaster.projector.*;
import com.harbormaster.security.*;

/**
 *
 * <h2>Claim business delegate</h2>
 *
 * <p>
 * This class implements the Business Delegate design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of Claim related services in the case of a Claim business related service failing.</li>
 * <li>Exposes a simpler, uniform Claim interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill Claim business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Spring Boot 3.5</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>ServiceLayer</td></tr>
 *          <tr><td>architecture style</td><td>Layered</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Insurance Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core insurance product, underwriting, policy, billing, claims, and reinsurance capabilities.</td></tr>
 *          <tr><td>industry</td><td>Insurance</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@Service
public class ClaimService 
		extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
	/**
	 * Default Constructor
	 */
    public ClaimService(CurrentIdentity identity,
				ApplicationContext applicationContext)  {

		this.identity		= identity;
		this.projector 		= new ClaimEntityProjector( applicationContext.getBean(ProjectorRegistry.class),
										applicationContext.getBean(ClaimRepository.class) );
		this.validator		= applicationContext.getBean(ClaimValidator.class) ;
	}


	/**
	 * Creates the provided command.
	 *
	 * @param		command ${class.getCreateCommandAlias()}
	 * @return		Claim
	 */
		public Claim createClaim( CreateClaimCommand command ) {

		Claim entity = new Claim();

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

            entity.setClaimId( command.getClaimId() );
            entity.setClaimNumber( command.getClaimNumber() );
            entity.setNoticeDate( command.getNoticeDate() );
            entity.setLossDate( command.getLossDate() );
            entity.setReportedBy( command.getReportedBy() );
            entity.setStatus( command.getStatus() );
            entity.setLossCause( command.getLossCause() );

		// ------------------------------------------
		// persist a new one
		// ------------------------------------------
		entity = projector.create(entity);

		LOGGER.info( "done creating of Claim {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Update the provided command.
	 * @param		command UpdateClaimCommand
	 * @return		Claim
	 */
	public Claim updateClaim( UpdateClaimCommand command ) {

		Claim entity = new Claim();

		// --------------------------------------
		// validate
		// --------------------------------------
		validator.validate( command );

            entity.setClaimId( command.getClaimId() );
            entity.setClaimNumber( command.getClaimNumber() );
            entity.setNoticeDate( command.getNoticeDate() );
            entity.setLossDate( command.getLossDate() );
            entity.setReportedBy( command.getReportedBy() );
            entity.setPolicy( command.getPolicy() );
            entity.setCustomer( command.getCustomer() );
            entity.setAdjuster( command.getAdjuster() );
            entity.setIncident( command.getIncident() );
            entity.setExposures( command.getExposures() );
            entity.setReserves( command.getReserves() );
            entity.setClaimPayments( command.getClaimPayments() );
            entity.setServiceProviders( command.getServiceProviders() );
            entity.setSubrogations( command.getSubrogations() );
            entity.setStatus( command.getStatus() );
            entity.setLossCause( command.getLossCause() );

		// ------------------------------------------
		// persist an existing one
		// ------------------------------------------
		entity = projector.update(entity);

		LOGGER.info( "done saving of Claim {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Deletes the associatied value object
	 * @param		command DeleteClaimCommand
	 */
	public void delete( DeleteClaimCommand command ) {
		UUID id = null;

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		id = command.getClaimId();

		// ------------------------------------------
		// delete the entity
		// ------------------------------------------
		projector.delete(id);

		LOGGER.info( "done deleting of Claim {0} ", id );

	}

	/**
	 * Method to retrieve the Claim via ClaimFetchOneSummary
	 * @param 	summary ClaimFetchOneSummary
	 * @return 	ClaimFetchOneResponse
	 */
	public Claim getClaim( ClaimFetchOneSummary summary ) {

		if( summary == null )
			throw new IllegalStateException( "ClaimFetchOneSummary arg cannot be null" );

		Claim entity = null;
		UUID id = summary.getClaimId();

		// --------------------------------------
		// validate the fetch one summary
		// --------------------------------------
		validator.validate( summary );

		// --------------------------------------
		// find a Claim using the provided id
		// --------------------------------------
		entity = projector.find( id );

		return entity;
	}


	/**
	 * Method to retrieve a collection of all Claims
	 *
	 * @return 	List<Claim>
	 */
    public List<Claim> getAllClaim() {
		List<Claim> list = projector.findAll( new FindAllClaimQuery() );
		return list;
	}

	/**
	 * assign Policy on Claim
	 * @param		command AssignPolicyToClaimCommand
	 */
	public void assignPolicy( AssignPolicyToClaimCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignPolicy(command.getClaimId(), command.getAssignment());

	}

	/**
	 * unAssign Policy on Claim
	 * @param		command UnAssignPolicyFromClaimCommand
	 */
	public void unAssignPolicy( UnAssignPolicyFromClaimCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignPolicy(command.getClaimId());
	}
	
	/**
	 * assign Customer on Claim
	 * @param		command AssignCustomerToClaimCommand
	 */
	public void assignCustomer( AssignCustomerToClaimCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignCustomer(command.getClaimId(), command.getAssignment());

	}

	/**
	 * unAssign Customer on Claim
	 * @param		command UnAssignCustomerFromClaimCommand
	 */
	public void unAssignCustomer( UnAssignCustomerFromClaimCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignCustomer(command.getClaimId());
	}
	
	/**
	 * assign Adjuster on Claim
	 * @param		command AssignAdjusterToClaimCommand
	 */
	public void assignAdjuster( AssignAdjusterToClaimCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignAdjuster(command.getClaimId(), command.getAssignment());

	}

	/**
	 * unAssign Adjuster on Claim
	 * @param		command UnAssignAdjusterFromClaimCommand
	 */
	public void unAssignAdjuster( UnAssignAdjusterFromClaimCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignAdjuster(command.getClaimId());
	}
	
	/**
	 * assign Incident on Claim
	 * @param		command AssignIncidentToClaimCommand
	 */
	public void assignIncident( AssignIncidentToClaimCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignIncident(command.getClaimId(), command.getAssignment());

	}

	/**
	 * unAssign Incident on Claim
	 * @param		command UnAssignIncidentFromClaimCommand
	 */
	public void unAssignIncident( UnAssignIncidentFromClaimCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignIncident(command.getClaimId());
	}
	

	/**
	 * add Exposure to Exposures
	 * @param		command AssignExposuresToClaimCommand
	 */
	public void addToExposures( AssignExposuresToClaimCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToExposures(command.getClaimId(), command.getAddTo());
	}

	/**
	 * remove Exposure from Exposures
	 * @param		command RemoveExposuresFromClaimCommand
	 */
	public void removeFromExposures( RemoveExposuresFromClaimCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromExposures(command.getClaimId(), command.getRemoveFrom());
	}

	/**
	 * add ClaimReserve to Reserves
	 * @param		command AssignReservesToClaimCommand
	 */
	public void addToReserves( AssignReservesToClaimCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToReserves(command.getClaimId(), command.getAddTo());
	}

	/**
	 * remove ClaimReserve from Reserves
	 * @param		command RemoveReservesFromClaimCommand
	 */
	public void removeFromReserves( RemoveReservesFromClaimCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromReserves(command.getClaimId(), command.getRemoveFrom());
	}

	/**
	 * add ClaimPayment to ClaimPayments
	 * @param		command AssignClaimPaymentsToClaimCommand
	 */
	public void addToClaimPayments( AssignClaimPaymentsToClaimCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToClaimPayments(command.getClaimId(), command.getAddTo());
	}

	/**
	 * remove ClaimPayment from ClaimPayments
	 * @param		command RemoveClaimPaymentsFromClaimCommand
	 */
	public void removeFromClaimPayments( RemoveClaimPaymentsFromClaimCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromClaimPayments(command.getClaimId(), command.getRemoveFrom());
	}

	/**
	 * add ServiceProvider to ServiceProviders
	 * @param		command AssignServiceProvidersToClaimCommand
	 */
	public void addToServiceProviders( AssignServiceProvidersToClaimCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToServiceProviders(command.getClaimId(), command.getAddTo());
	}

	/**
	 * remove ServiceProvider from ServiceProviders
	 * @param		command RemoveServiceProvidersFromClaimCommand
	 */
	public void removeFromServiceProviders( RemoveServiceProvidersFromClaimCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromServiceProviders(command.getClaimId(), command.getRemoveFrom());
	}

	/**
	 * add SubrogationRecovery to Subrogations
	 * @param		command AssignSubrogationsToClaimCommand
	 */
	public void addToSubrogations( AssignSubrogationsToClaimCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToSubrogations(command.getClaimId(), command.getAddTo());
	}

	/**
	 * remove SubrogationRecovery from Subrogations
	 * @param		command RemoveSubrogationsFromClaimCommand
	 */
	public void removeFromSubrogations( RemoveSubrogationsFromClaimCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromSubrogations(command.getClaimId(), command.getRemoveFrom());
	}




//************************************************************************
// Attributes
//************************************************************************
	@Autowired
    private final ClaimEntityProjector projector;
	private final ClaimValidator validator;
	private Claim claim 	= null;
	private CurrentIdentity identity			= null;
	private static final Logger LOGGER 			=  LoggerFactory.getLogger(ClaimService.class);
}
