/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Insurance Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */

package com.harbormaster.service;

import java.io.IOException;
import java.util.*;
import java.util.concurrent.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import org.springframework.stereotype.Service;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;


import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.repository.*;
import com.harbormaster.validator.*;
import com.harbormaster.projector.*;
import com.harbormaster.security.*;

/**
 *
 * <h2>Policy business delegate</h2>
 *
 * <p>
 * This class implements the Business Delegate design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of Policy related services in the case of a Policy business related service failing.</li>
 * <li>Exposes a simpler, uniform Policy interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill Policy business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Spring Boot 3.5</td></tr>
 *          <tr><td>published</td><td>09/12/2026</td></tr>
 *          <tr><td>design pattern</td><td>ServiceLayer</td></tr>
 *          <tr><td>architecture style</td><td>Layered</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Insurance Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core insurance product, underwriting, policy, billing, claims, and reinsurance capabilities.</td></tr>
 *          <tr><td>industry</td><td>Insurance</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@Service
public class PolicyService 
		extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
	/**
	 * Default Constructor
	 */
    public PolicyService(CurrentIdentity identity,
				ApplicationContext applicationContext)  {

		this.identity		= identity;
		this.projector 		= new PolicyEntityProjector( applicationContext.getBean(ProjectorRegistry.class),
										applicationContext.getBean(PolicyRepository.class) );
		this.validator		= applicationContext.getBean(PolicyValidator.class) ;
	}


	/**
	 * Creates the provided command.
	 *
	 * @param		command ${class.getCreateCommandAlias()}
	 * @return		Policy
	 */
		public Policy createPolicy( CreatePolicyCommand command ) {

		Policy entity = new Policy();

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

            entity.setPolicyId( command.getPolicyId() );
            entity.setPolicyNumber( command.getPolicyNumber() );
            entity.setEffectivePeriod( command.getEffectivePeriod() );
            entity.setTotalPremium( command.getTotalPremium() );
            entity.setStatus( command.getStatus() );
            entity.setPaymentPlan( command.getPaymentPlan() );

		// ------------------------------------------
		// persist a new one
		// ------------------------------------------
		entity = projector.create(entity);

		LOGGER.info( "done creating of Policy {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Update the provided command.
	 * @param		command UpdatePolicyCommand
	 * @return		Policy
	 */
	public Policy updatePolicy( UpdatePolicyCommand command ) {

		Policy entity = new Policy();

		// --------------------------------------
		// validate
		// --------------------------------------
		validator.validate( command );

            entity.setPolicyId( command.getPolicyId() );
            entity.setPolicyNumber( command.getPolicyNumber() );
            entity.setEffectivePeriod( command.getEffectivePeriod() );
            entity.setTotalPremium( command.getTotalPremium() );
            entity.setInsurer( command.getInsurer() );
            entity.setCustomer( command.getCustomer() );
            entity.setProduct( command.getProduct() );
            entity.setAgent( command.getAgent() );
            entity.setCoverages( command.getCoverages() );
            entity.setInsuredObjects( command.getInsuredObjects() );
            entity.setEndorsements( command.getEndorsements() );
            entity.setBillingAccount( command.getBillingAccount() );
            entity.setBeneficiaries( command.getBeneficiaries() );
            entity.setClaims( command.getClaims() );
            entity.setReinsuranceAgreements( command.getReinsuranceAgreements() );
            entity.setStatus( command.getStatus() );
            entity.setPaymentPlan( command.getPaymentPlan() );

		// ------------------------------------------
		// persist an existing one
		// ------------------------------------------
		entity = projector.update(entity);

		LOGGER.info( "done saving of Policy {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Deletes the associatied value object
	 * @param		command DeletePolicyCommand
	 */
	public void delete( DeletePolicyCommand command ) {
		UUID id = null;

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		id = command.getPolicyId();

		// ------------------------------------------
		// delete the entity
		// ------------------------------------------
		projector.delete(id);

		LOGGER.info( "done deleting of Policy {0} ", id );

	}

	/**
	 * Method to retrieve the Policy via PolicyFetchOneSummary
	 * @param 	summary PolicyFetchOneSummary
	 * @return 	PolicyFetchOneResponse
	 */
	public Policy getPolicy( PolicyFetchOneSummary summary ) {

		if( summary == null )
			throw new IllegalStateException( "PolicyFetchOneSummary arg cannot be null" );

		Policy entity = null;
		UUID id = summary.getPolicyId();

		// --------------------------------------
		// validate the fetch one summary
		// --------------------------------------
		validator.validate( summary );

		// --------------------------------------
		// find a Policy using the provided id
		// --------------------------------------
		entity = projector.find( id );

		return entity;
	}


	/**
	 * Method to retrieve a collection of all Policys
	 *
	 * @return 	List<Policy>
	 */
    public List<Policy> getAllPolicy() {
		List<Policy> list = projector.findAll( new FindAllPolicyQuery() );
		return list;
	}

	/**
	 * assign Insurer on Policy
	 * @param		command AssignInsurerToPolicyCommand
	 */
	public void assignInsurer( AssignInsurerToPolicyCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignInsurer(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Insurer on Policy
	 * @param		command UnAssignInsurerFromPolicyCommand
	 */
	public void unAssignInsurer( UnAssignInsurerFromPolicyCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignInsurer(command.getChildId());
	}
	
	/**
	 * assign Customer on Policy
	 * @param		command AssignCustomerToPolicyCommand
	 */
	public void assignCustomer( AssignCustomerToPolicyCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignCustomer(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Customer on Policy
	 * @param		command UnAssignCustomerFromPolicyCommand
	 */
	public void unAssignCustomer( UnAssignCustomerFromPolicyCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignCustomer(command.getChildId());
	}
	
	/**
	 * assign Product on Policy
	 * @param		command AssignProductToPolicyCommand
	 */
	public void assignProduct( AssignProductToPolicyCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignProduct(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Product on Policy
	 * @param		command UnAssignProductFromPolicyCommand
	 */
	public void unAssignProduct( UnAssignProductFromPolicyCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignProduct(command.getChildId());
	}
	
	/**
	 * assign Agent on Policy
	 * @param		command AssignAgentToPolicyCommand
	 */
	public void assignAgent( AssignAgentToPolicyCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignAgent(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Agent on Policy
	 * @param		command UnAssignAgentFromPolicyCommand
	 */
	public void unAssignAgent( UnAssignAgentFromPolicyCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignAgent(command.getChildId());
	}
	
	/**
	 * assign BillingAccount on Policy
	 * @param		command AssignBillingAccountToPolicyCommand
	 */
	public void assignBillingAccount( AssignBillingAccountToPolicyCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignBillingAccount(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign BillingAccount on Policy
	 * @param		command UnAssignBillingAccountFromPolicyCommand
	 */
	public void unAssignBillingAccount( UnAssignBillingAccountFromPolicyCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignBillingAccount(command.getChildId());
	}
	

	/**
	 * add PolicyCoverage to Coverages
	 * @param		command AssignCoveragesToPolicyCommand
	 */
	public void addToCoverages( AssignCoveragesToPolicyCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToCoverages(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove PolicyCoverage from Coverages
	 * @param		command RemoveCoveragesFromPolicyCommand
	 */
	public void removeFromCoverages( RemoveCoveragesFromPolicyCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromCoverages(command.getParentId(), command.getChildIds());
	}

	/**
	 * add InsuredObject to InsuredObjects
	 * @param		command AssignInsuredObjectsToPolicyCommand
	 */
	public void addToInsuredObjects( AssignInsuredObjectsToPolicyCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToInsuredObjects(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove InsuredObject from InsuredObjects
	 * @param		command RemoveInsuredObjectsFromPolicyCommand
	 */
	public void removeFromInsuredObjects( RemoveInsuredObjectsFromPolicyCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromInsuredObjects(command.getParentId(), command.getChildIds());
	}

	/**
	 * add Endorsement to Endorsements
	 * @param		command AssignEndorsementsToPolicyCommand
	 */
	public void addToEndorsements( AssignEndorsementsToPolicyCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToEndorsements(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove Endorsement from Endorsements
	 * @param		command RemoveEndorsementsFromPolicyCommand
	 */
	public void removeFromEndorsements( RemoveEndorsementsFromPolicyCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromEndorsements(command.getParentId(), command.getChildIds());
	}

	/**
	 * add Beneficiary to Beneficiaries
	 * @param		command AssignBeneficiariesToPolicyCommand
	 */
	public void addToBeneficiaries( AssignBeneficiariesToPolicyCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToBeneficiaries(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove Beneficiary from Beneficiaries
	 * @param		command RemoveBeneficiariesFromPolicyCommand
	 */
	public void removeFromBeneficiaries( RemoveBeneficiariesFromPolicyCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromBeneficiaries(command.getParentId(), command.getChildIds());
	}

	/**
	 * add Claim to Claims
	 * @param		command AssignClaimsToPolicyCommand
	 */
	public void addToClaims( AssignClaimsToPolicyCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToClaims(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove Claim from Claims
	 * @param		command RemoveClaimsFromPolicyCommand
	 */
	public void removeFromClaims( RemoveClaimsFromPolicyCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromClaims(command.getParentId(), command.getChildIds());
	}

	/**
	 * add ReinsuranceAgreement to ReinsuranceAgreements
	 * @param		command AssignReinsuranceAgreementsToPolicyCommand
	 */
	public void addToReinsuranceAgreements( AssignReinsuranceAgreementsToPolicyCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToReinsuranceAgreements(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove ReinsuranceAgreement from ReinsuranceAgreements
	 * @param		command RemoveReinsuranceAgreementsFromPolicyCommand
	 */
	public void removeFromReinsuranceAgreements( RemoveReinsuranceAgreementsFromPolicyCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromReinsuranceAgreements(command.getParentId(), command.getChildIds());
	}




//************************************************************************
// Attributes
//************************************************************************
	@Autowired
    private final PolicyEntityProjector projector;
	private final PolicyValidator validator;
	private Policy policy 	= null;
	private CurrentIdentity identity			= null;
	private static final Logger LOGGER 			=  LoggerFactory.getLogger(PolicyService.class);
}
