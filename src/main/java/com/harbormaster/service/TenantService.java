/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */

package com.harbormaster.service;

import java.io.IOException;
import java.util.*;
import java.util.concurrent.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import org.springframework.stereotype.Service;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;


import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.repository.*;
import com.harbormaster.validator.*;
import com.harbormaster.projector.*;
import com.harbormaster.security.*;

/**
 *
 * <h2>Tenant business delegate</h2>
 *
 * <p>
 * This class implements the Business Delegate design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of Tenant related services in the case of a Tenant business related service failing.</li>
 * <li>Exposes a simpler, uniform Tenant interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill Tenant business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Spring Boot 3.5</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>ServiceLayer</td></tr>
 *          <tr><td>architecture style</td><td>Layered</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@Service
public class TenantService 
		extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
	/**
	 * Default Constructor
	 */
    public TenantService(CurrentIdentity identity,
				ApplicationContext applicationContext)  {

		this.identity		= identity;
		this.projector 		= new TenantEntityProjector( applicationContext.getBean(ProjectorRegistry.class),
										applicationContext.getBean(TenantRepository.class) );
		this.validator		= applicationContext.getBean(TenantValidator.class) ;
	}


	/**
	 * Creates the provided command.
	 *
	 * @param		command ${class.getCreateCommandAlias()}
	 * @return		Tenant
	 */
		public Tenant createTenant( CreateTenantCommand command ) {

		Tenant entity = new Tenant();

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

            entity.setTenantId( command.getTenantId() );
            entity.setName( command.getName() );
            entity.setTenantType( command.getTenantType() );

		// ------------------------------------------
		// persist a new one
		// ------------------------------------------
		entity = projector.create(entity);

		LOGGER.info( "done creating of Tenant {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Update the provided command.
	 * @param		command UpdateTenantCommand
	 * @return		Tenant
	 */
	public Tenant updateTenant( UpdateTenantCommand command ) {

		Tenant entity = new Tenant();

		// --------------------------------------
		// validate
		// --------------------------------------
		validator.validate( command );

            entity.setTenantId( command.getTenantId() );
            entity.setName( command.getName() );
            entity.setSites( command.getSites() );
            entity.setUsers( command.getUsers() );
            entity.setDevices( command.getDevices() );
            entity.setDataRetentionPolicies( command.getDataRetentionPolicies() );
            entity.setConnectivityPlans( command.getConnectivityPlans() );
            entity.setSimCards( command.getSimCards() );
            entity.setMessagingEndpoints( command.getMessagingEndpoints() );
            entity.setAccessPolicies( command.getAccessPolicies() );
            entity.setDeviceGroups( command.getDeviceGroups() );
            entity.setAlertRules( command.getAlertRules() );
            entity.setMaintenanceTickets( command.getMaintenanceTickets() );
            entity.setUsageRecords( command.getUsageRecords() );
            entity.setTenantType( command.getTenantType() );

		// ------------------------------------------
		// persist an existing one
		// ------------------------------------------
		entity = projector.update(entity);

		LOGGER.info( "done saving of Tenant {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Deletes the associatied value object
	 * @param		command DeleteTenantCommand
	 */
	public void delete( DeleteTenantCommand command ) {
		UUID id = null;

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		id = command.getTenantId();

		// ------------------------------------------
		// delete the entity
		// ------------------------------------------
		projector.delete(id);

		LOGGER.info( "done deleting of Tenant {0} ", id );

	}

	/**
	 * Method to retrieve the Tenant via TenantFetchOneSummary
	 * @param 	summary TenantFetchOneSummary
	 * @return 	TenantFetchOneResponse
	 */
	public Tenant getTenant( TenantFetchOneSummary summary ) {

		if( summary == null )
			throw new IllegalStateException( "TenantFetchOneSummary arg cannot be null" );

		Tenant entity = null;
		UUID id = summary.getTenantId();

		// --------------------------------------
		// validate the fetch one summary
		// --------------------------------------
		validator.validate( summary );

		// --------------------------------------
		// find a Tenant using the provided id
		// --------------------------------------
		entity = projector.find( id );

		return entity;
	}


	/**
	 * Method to retrieve a collection of all Tenants
	 *
	 * @return 	List<Tenant>
	 */
    public List<Tenant> getAllTenant() {
		List<Tenant> list = projector.findAll( new FindAllTenantQuery() );
		return list;
	}


	/**
	 * add Site to Sites
	 * @param		command AssignSitesToTenantCommand
	 */
	public void addToSites( AssignSitesToTenantCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToSites(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove Site from Sites
	 * @param		command RemoveSitesFromTenantCommand
	 */
	public void removeFromSites( RemoveSitesFromTenantCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromSites(command.getParentId(), command.getChildIds());
	}

	/**
	 * add TenantUser to Users
	 * @param		command AssignUsersToTenantCommand
	 */
	public void addToUsers( AssignUsersToTenantCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToUsers(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove TenantUser from Users
	 * @param		command RemoveUsersFromTenantCommand
	 */
	public void removeFromUsers( RemoveUsersFromTenantCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromUsers(command.getParentId(), command.getChildIds());
	}

	/**
	 * add IoTDevice to Devices
	 * @param		command AssignDevicesToTenantCommand
	 */
	public void addToDevices( AssignDevicesToTenantCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToDevices(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove IoTDevice from Devices
	 * @param		command RemoveDevicesFromTenantCommand
	 */
	public void removeFromDevices( RemoveDevicesFromTenantCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromDevices(command.getParentId(), command.getChildIds());
	}

	/**
	 * add DataRetentionPolicy to DataRetentionPolicies
	 * @param		command AssignDataRetentionPoliciesToTenantCommand
	 */
	public void addToDataRetentionPolicies( AssignDataRetentionPoliciesToTenantCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToDataRetentionPolicies(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove DataRetentionPolicy from DataRetentionPolicies
	 * @param		command RemoveDataRetentionPoliciesFromTenantCommand
	 */
	public void removeFromDataRetentionPolicies( RemoveDataRetentionPoliciesFromTenantCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromDataRetentionPolicies(command.getParentId(), command.getChildIds());
	}

	/**
	 * add ConnectivityPlan to ConnectivityPlans
	 * @param		command AssignConnectivityPlansToTenantCommand
	 */
	public void addToConnectivityPlans( AssignConnectivityPlansToTenantCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToConnectivityPlans(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove ConnectivityPlan from ConnectivityPlans
	 * @param		command RemoveConnectivityPlansFromTenantCommand
	 */
	public void removeFromConnectivityPlans( RemoveConnectivityPlansFromTenantCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromConnectivityPlans(command.getParentId(), command.getChildIds());
	}

	/**
	 * add SimCard to SimCards
	 * @param		command AssignSimCardsToTenantCommand
	 */
	public void addToSimCards( AssignSimCardsToTenantCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToSimCards(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove SimCard from SimCards
	 * @param		command RemoveSimCardsFromTenantCommand
	 */
	public void removeFromSimCards( RemoveSimCardsFromTenantCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromSimCards(command.getParentId(), command.getChildIds());
	}

	/**
	 * add MessagingEndpoint to MessagingEndpoints
	 * @param		command AssignMessagingEndpointsToTenantCommand
	 */
	public void addToMessagingEndpoints( AssignMessagingEndpointsToTenantCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToMessagingEndpoints(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove MessagingEndpoint from MessagingEndpoints
	 * @param		command RemoveMessagingEndpointsFromTenantCommand
	 */
	public void removeFromMessagingEndpoints( RemoveMessagingEndpointsFromTenantCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromMessagingEndpoints(command.getParentId(), command.getChildIds());
	}

	/**
	 * add AccessPolicy to AccessPolicies
	 * @param		command AssignAccessPoliciesToTenantCommand
	 */
	public void addToAccessPolicies( AssignAccessPoliciesToTenantCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToAccessPolicies(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove AccessPolicy from AccessPolicies
	 * @param		command RemoveAccessPoliciesFromTenantCommand
	 */
	public void removeFromAccessPolicies( RemoveAccessPoliciesFromTenantCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromAccessPolicies(command.getParentId(), command.getChildIds());
	}

	/**
	 * add DeviceGroup to DeviceGroups
	 * @param		command AssignDeviceGroupsToTenantCommand
	 */
	public void addToDeviceGroups( AssignDeviceGroupsToTenantCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToDeviceGroups(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove DeviceGroup from DeviceGroups
	 * @param		command RemoveDeviceGroupsFromTenantCommand
	 */
	public void removeFromDeviceGroups( RemoveDeviceGroupsFromTenantCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromDeviceGroups(command.getParentId(), command.getChildIds());
	}

	/**
	 * add AlertRule to AlertRules
	 * @param		command AssignAlertRulesToTenantCommand
	 */
	public void addToAlertRules( AssignAlertRulesToTenantCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToAlertRules(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove AlertRule from AlertRules
	 * @param		command RemoveAlertRulesFromTenantCommand
	 */
	public void removeFromAlertRules( RemoveAlertRulesFromTenantCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromAlertRules(command.getParentId(), command.getChildIds());
	}

	/**
	 * add MaintenanceTicket to MaintenanceTickets
	 * @param		command AssignMaintenanceTicketsToTenantCommand
	 */
	public void addToMaintenanceTickets( AssignMaintenanceTicketsToTenantCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToMaintenanceTickets(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove MaintenanceTicket from MaintenanceTickets
	 * @param		command RemoveMaintenanceTicketsFromTenantCommand
	 */
	public void removeFromMaintenanceTickets( RemoveMaintenanceTicketsFromTenantCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromMaintenanceTickets(command.getParentId(), command.getChildIds());
	}

	/**
	 * add UsageRecord to UsageRecords
	 * @param		command AssignUsageRecordsToTenantCommand
	 */
	public void addToUsageRecords( AssignUsageRecordsToTenantCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToUsageRecords(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove UsageRecord from UsageRecords
	 * @param		command RemoveUsageRecordsFromTenantCommand
	 */
	public void removeFromUsageRecords( RemoveUsageRecordsFromTenantCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromUsageRecords(command.getParentId(), command.getChildIds());
	}




//************************************************************************
// Attributes
//************************************************************************
	@Autowired
    private final TenantEntityProjector projector;
	private final TenantValidator validator;
	private Tenant tenant 	= null;
	private CurrentIdentity identity			= null;
	private static final Logger LOGGER 			=  LoggerFactory.getLogger(TenantService.class);
}
