/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */

package com.harbormaster.service;

import java.io.IOException;
import java.util.*;
import java.util.concurrent.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import org.springframework.stereotype.Service;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;


import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.repository.*;
import com.harbormaster.validator.*;
import com.harbormaster.projector.*;
import com.harbormaster.security.*;

/**
 *
 * <h2>NetworkProfile business delegate</h2>
 *
 * <p>
 * This class implements the Business Delegate design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of NetworkProfile related services in the case of a NetworkProfile business related service failing.</li>
 * <li>Exposes a simpler, uniform NetworkProfile interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill NetworkProfile business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Spring Boot 3.5</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>ServiceLayer</td></tr>
 *          <tr><td>architecture style</td><td>Layered</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@Service
public class NetworkProfileService 
		extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
	/**
	 * Default Constructor
	 */
    public NetworkProfileService(CurrentIdentity identity,
				ApplicationContext applicationContext)  {

		this.identity		= identity;
		this.projector 		= new NetworkProfileEntityProjector( applicationContext.getBean(ProjectorRegistry.class),
										applicationContext.getBean(NetworkProfileRepository.class) );
		this.validator		= applicationContext.getBean(NetworkProfileValidator.class) ;
	}


	/**
	 * Creates the provided command.
	 *
	 * @param		command ${class.getCreateCommandAlias()}
	 * @return		NetworkProfile
	 */
		public NetworkProfile createNetworkProfile( CreateNetworkProfileCommand command ) {

		NetworkProfile entity = new NetworkProfile();

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

            entity.setNetworkProfileId( command.getNetworkProfileId() );
            entity.setProfileName( command.getProfileName() );
            entity.setSsid( command.getSsid() );
            entity.setApn( command.getApn() );
            entity.setConnectivityType( command.getConnectivityType() );

		// ------------------------------------------
		// persist a new one
		// ------------------------------------------
		entity = projector.create(entity);

		LOGGER.info( "done creating of NetworkProfile {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Update the provided command.
	 * @param		command UpdateNetworkProfileCommand
	 * @return		NetworkProfile
	 */
	public NetworkProfile updateNetworkProfile( UpdateNetworkProfileCommand command ) {

		NetworkProfile entity = new NetworkProfile();

		// --------------------------------------
		// validate
		// --------------------------------------
		validator.validate( command );

            entity.setNetworkProfileId( command.getNetworkProfileId() );
            entity.setProfileName( command.getProfileName() );
            entity.setSsid( command.getSsid() );
            entity.setApn( command.getApn() );
            entity.setDevice( command.getDevice() );
            entity.setGateway( command.getGateway() );
            entity.setSimCard( command.getSimCard() );
            entity.setConnectivityType( command.getConnectivityType() );

		// ------------------------------------------
		// persist an existing one
		// ------------------------------------------
		entity = projector.update(entity);

		LOGGER.info( "done saving of NetworkProfile {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Deletes the associatied value object
	 * @param		command DeleteNetworkProfileCommand
	 */
	public void delete( DeleteNetworkProfileCommand command ) {
		UUID id = null;

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		id = command.getNetworkProfileId();

		// ------------------------------------------
		// delete the entity
		// ------------------------------------------
		projector.delete(id);

		LOGGER.info( "done deleting of NetworkProfile {0} ", id );

	}

	/**
	 * Method to retrieve the NetworkProfile via NetworkProfileFetchOneSummary
	 * @param 	summary NetworkProfileFetchOneSummary
	 * @return 	NetworkProfileFetchOneResponse
	 */
	public NetworkProfile getNetworkProfile( NetworkProfileFetchOneSummary summary ) {

		if( summary == null )
			throw new IllegalStateException( "NetworkProfileFetchOneSummary arg cannot be null" );

		NetworkProfile entity = null;
		UUID id = summary.getNetworkProfileId();

		// --------------------------------------
		// validate the fetch one summary
		// --------------------------------------
		validator.validate( summary );

		// --------------------------------------
		// find a NetworkProfile using the provided id
		// --------------------------------------
		entity = projector.find( id );

		return entity;
	}


	/**
	 * Method to retrieve a collection of all NetworkProfiles
	 *
	 * @return 	List<NetworkProfile>
	 */
    public List<NetworkProfile> getAllNetworkProfile() {
		List<NetworkProfile> list = projector.findAll( new FindAllNetworkProfileQuery() );
		return list;
	}

	/**
	 * assign Device on NetworkProfile
	 * @param		command AssignDeviceToNetworkProfileCommand
	 */
	public void assignDevice( AssignDeviceToNetworkProfileCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignDevice(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Device on NetworkProfile
	 * @param		command UnAssignDeviceFromNetworkProfileCommand
	 */
	public void unAssignDevice( UnAssignDeviceFromNetworkProfileCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignDevice(command.getChildId());
	}
	
	/**
	 * assign Gateway on NetworkProfile
	 * @param		command AssignGatewayToNetworkProfileCommand
	 */
	public void assignGateway( AssignGatewayToNetworkProfileCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignGateway(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Gateway on NetworkProfile
	 * @param		command UnAssignGatewayFromNetworkProfileCommand
	 */
	public void unAssignGateway( UnAssignGatewayFromNetworkProfileCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignGateway(command.getChildId());
	}
	
	/**
	 * assign SimCard on NetworkProfile
	 * @param		command AssignSimCardToNetworkProfileCommand
	 */
	public void assignSimCard( AssignSimCardToNetworkProfileCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignSimCard(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign SimCard on NetworkProfile
	 * @param		command UnAssignSimCardFromNetworkProfileCommand
	 */
	public void unAssignSimCard( UnAssignSimCardFromNetworkProfileCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignSimCard(command.getChildId());
	}
	




//************************************************************************
// Attributes
//************************************************************************
	@Autowired
    private final NetworkProfileEntityProjector projector;
	private final NetworkProfileValidator validator;
	private NetworkProfile networkProfile 	= null;
	private CurrentIdentity identity			= null;
	private static final Logger LOGGER 			=  LoggerFactory.getLogger(NetworkProfileService.class);
}
