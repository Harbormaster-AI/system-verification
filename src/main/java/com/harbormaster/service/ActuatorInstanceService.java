/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */

package com.harbormaster.service;

import java.io.IOException;
import java.util.*;
import java.util.concurrent.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import org.springframework.stereotype.Service;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;


import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.repository.*;
import com.harbormaster.validator.*;
import com.harbormaster.projector.*;
import com.harbormaster.security.*;

/**
 *
 * <h2>ActuatorInstance business delegate</h2>
 *
 * <p>
 * This class implements the Business Delegate design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of ActuatorInstance related services in the case of a ActuatorInstance business related service failing.</li>
 * <li>Exposes a simpler, uniform ActuatorInstance interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill ActuatorInstance business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Spring Boot 3.5</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>ServiceLayer</td></tr>
 *          <tr><td>architecture style</td><td>Layered</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@Service
public class ActuatorInstanceService 
		extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
	/**
	 * Default Constructor
	 */
    public ActuatorInstanceService(CurrentIdentity identity,
				ApplicationContext applicationContext)  {

		this.identity		= identity;
		this.projector 		= new ActuatorInstanceEntityProjector( applicationContext.getBean(ProjectorRegistry.class),
										applicationContext.getBean(ActuatorInstanceRepository.class) );
		this.validator		= applicationContext.getBean(ActuatorInstanceValidator.class) ;
	}


	/**
	 * Creates the provided command.
	 *
	 * @param		command ${class.getCreateCommandAlias()}
	 * @return		ActuatorInstance
	 */
		public ActuatorInstance createActuatorInstance( CreateActuatorInstanceCommand command ) {

		ActuatorInstance entity = new ActuatorInstance();

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

            entity.setActuatorInstanceId( command.getActuatorInstanceId() );
            entity.setName( command.getName() );
            entity.setCommandTopic( command.getCommandTopic() );
            entity.setActuatorType( command.getActuatorType() );

		// ------------------------------------------
		// persist a new one
		// ------------------------------------------
		entity = projector.create(entity);

		LOGGER.info( "done creating of ActuatorInstance {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Update the provided command.
	 * @param		command UpdateActuatorInstanceCommand
	 * @return		ActuatorInstance
	 */
	public ActuatorInstance updateActuatorInstance( UpdateActuatorInstanceCommand command ) {

		ActuatorInstance entity = new ActuatorInstance();

		// --------------------------------------
		// validate
		// --------------------------------------
		validator.validate( command );

            entity.setActuatorInstanceId( command.getActuatorInstanceId() );
            entity.setName( command.getName() );
            entity.setCommandTopic( command.getCommandTopic() );
            entity.setDevice( command.getDevice() );
            entity.setSupportedCommands( command.getSupportedCommands() );
            entity.setActuatorType( command.getActuatorType() );

		// ------------------------------------------
		// persist an existing one
		// ------------------------------------------
		entity = projector.update(entity);

		LOGGER.info( "done saving of ActuatorInstance {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Deletes the associatied value object
	 * @param		command DeleteActuatorInstanceCommand
	 */
	public void delete( DeleteActuatorInstanceCommand command ) {
		UUID id = null;

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		id = command.getActuatorInstanceId();

		// ------------------------------------------
		// delete the entity
		// ------------------------------------------
		projector.delete(id);

		LOGGER.info( "done deleting of ActuatorInstance {0} ", id );

	}

	/**
	 * Method to retrieve the ActuatorInstance via ActuatorInstanceFetchOneSummary
	 * @param 	summary ActuatorInstanceFetchOneSummary
	 * @return 	ActuatorInstanceFetchOneResponse
	 */
	public ActuatorInstance getActuatorInstance( ActuatorInstanceFetchOneSummary summary ) {

		if( summary == null )
			throw new IllegalStateException( "ActuatorInstanceFetchOneSummary arg cannot be null" );

		ActuatorInstance entity = null;
		UUID id = summary.getActuatorInstanceId();

		// --------------------------------------
		// validate the fetch one summary
		// --------------------------------------
		validator.validate( summary );

		// --------------------------------------
		// find a ActuatorInstance using the provided id
		// --------------------------------------
		entity = projector.find( id );

		return entity;
	}


	/**
	 * Method to retrieve a collection of all ActuatorInstances
	 *
	 * @return 	List<ActuatorInstance>
	 */
    public List<ActuatorInstance> getAllActuatorInstance() {
		List<ActuatorInstance> list = projector.findAll( new FindAllActuatorInstanceQuery() );
		return list;
	}

	/**
	 * assign Device on ActuatorInstance
	 * @param		command AssignDeviceToActuatorInstanceCommand
	 */
	public void assignDevice( AssignDeviceToActuatorInstanceCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignDevice(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Device on ActuatorInstance
	 * @param		command UnAssignDeviceFromActuatorInstanceCommand
	 */
	public void unAssignDevice( UnAssignDeviceFromActuatorInstanceCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignDevice(command.getChildId());
	}
	

	/**
	 * add CommandDefinition to SupportedCommands
	 * @param		command AssignSupportedCommandsToActuatorInstanceCommand
	 */
	public void addToSupportedCommands( AssignSupportedCommandsToActuatorInstanceCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToSupportedCommands(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove CommandDefinition from SupportedCommands
	 * @param		command RemoveSupportedCommandsFromActuatorInstanceCommand
	 */
	public void removeFromSupportedCommands( RemoveSupportedCommandsFromActuatorInstanceCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromSupportedCommands(command.getParentId(), command.getChildIds());
	}




//************************************************************************
// Attributes
//************************************************************************
	@Autowired
    private final ActuatorInstanceEntityProjector projector;
	private final ActuatorInstanceValidator validator;
	private ActuatorInstance actuatorInstance 	= null;
	private CurrentIdentity identity			= null;
	private static final Logger LOGGER 			=  LoggerFactory.getLogger(ActuatorInstanceService.class);
}
