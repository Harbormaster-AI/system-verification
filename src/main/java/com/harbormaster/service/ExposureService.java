/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Insurance Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */

package com.harbormaster.service;

import java.io.IOException;
import java.util.*;
import java.util.concurrent.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import org.springframework.stereotype.Service;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;


import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.repository.*;
import com.harbormaster.validator.*;
import com.harbormaster.projector.*;
import com.harbormaster.security.*;

/**
 *
 * <h2>Exposure business delegate</h2>
 *
 * <p>
 * This class implements the Business Delegate design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of Exposure related services in the case of a Exposure business related service failing.</li>
 * <li>Exposes a simpler, uniform Exposure interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill Exposure business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Spring Boot 3.5</td></tr>
 *          <tr><td>published</td><td>09/12/2026</td></tr>
 *          <tr><td>design pattern</td><td>ServiceLayer</td></tr>
 *          <tr><td>architecture style</td><td>Layered</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Insurance Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core insurance product, underwriting, policy, billing, claims, and reinsurance capabilities.</td></tr>
 *          <tr><td>industry</td><td>Insurance</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@Service
public class ExposureService 
		extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
	/**
	 * Default Constructor
	 */
    public ExposureService(CurrentIdentity identity,
				ApplicationContext applicationContext)  {

		this.identity		= identity;
		this.projector 		= new ExposureEntityProjector( applicationContext.getBean(ProjectorRegistry.class),
										applicationContext.getBean(ExposureRepository.class) );
		this.validator		= applicationContext.getBean(ExposureValidator.class) ;
	}


	/**
	 * Creates the provided command.
	 *
	 * @param		command ${class.getCreateCommandAlias()}
	 * @return		Exposure
	 */
		public Exposure createExposure( CreateExposureCommand command ) {

		Exposure entity = new Exposure();

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

            entity.setExposureId( command.getExposureId() );
            entity.setExposureType( command.getExposureType() );
            entity.setStatus( command.getStatus() );

		// ------------------------------------------
		// persist a new one
		// ------------------------------------------
		entity = projector.create(entity);

		LOGGER.info( "done creating of Exposure {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Update the provided command.
	 * @param		command UpdateExposureCommand
	 * @return		Exposure
	 */
	public Exposure updateExposure( UpdateExposureCommand command ) {

		Exposure entity = new Exposure();

		// --------------------------------------
		// validate
		// --------------------------------------
		validator.validate( command );

            entity.setExposureId( command.getExposureId() );
            entity.setClaim( command.getClaim() );
            entity.setPolicyCoverage( command.getPolicyCoverage() );
            entity.setInsuredObject( command.getInsuredObject() );
            entity.setReserves( command.getReserves() );
            entity.setPayments( command.getPayments() );
            entity.setExposureType( command.getExposureType() );
            entity.setStatus( command.getStatus() );

		// ------------------------------------------
		// persist an existing one
		// ------------------------------------------
		entity = projector.update(entity);

		LOGGER.info( "done saving of Exposure {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Deletes the associatied value object
	 * @param		command DeleteExposureCommand
	 */
	public void delete( DeleteExposureCommand command ) {
		UUID id = null;

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		id = command.getExposureId();

		// ------------------------------------------
		// delete the entity
		// ------------------------------------------
		projector.delete(id);

		LOGGER.info( "done deleting of Exposure {0} ", id );

	}

	/**
	 * Method to retrieve the Exposure via ExposureFetchOneSummary
	 * @param 	summary ExposureFetchOneSummary
	 * @return 	ExposureFetchOneResponse
	 */
	public Exposure getExposure( ExposureFetchOneSummary summary ) {

		if( summary == null )
			throw new IllegalStateException( "ExposureFetchOneSummary arg cannot be null" );

		Exposure entity = null;
		UUID id = summary.getExposureId();

		// --------------------------------------
		// validate the fetch one summary
		// --------------------------------------
		validator.validate( summary );

		// --------------------------------------
		// find a Exposure using the provided id
		// --------------------------------------
		entity = projector.find( id );

		return entity;
	}


	/**
	 * Method to retrieve a collection of all Exposures
	 *
	 * @return 	List<Exposure>
	 */
    public List<Exposure> getAllExposure() {
		List<Exposure> list = projector.findAll( new FindAllExposureQuery() );
		return list;
	}

	/**
	 * assign Claim on Exposure
	 * @param		command AssignClaimToExposureCommand
	 */
	public void assignClaim( AssignClaimToExposureCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignClaim(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Claim on Exposure
	 * @param		command UnAssignClaimFromExposureCommand
	 */
	public void unAssignClaim( UnAssignClaimFromExposureCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignClaim(command.getChildId());
	}
	
	/**
	 * assign PolicyCoverage on Exposure
	 * @param		command AssignPolicyCoverageToExposureCommand
	 */
	public void assignPolicyCoverage( AssignPolicyCoverageToExposureCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignPolicyCoverage(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign PolicyCoverage on Exposure
	 * @param		command UnAssignPolicyCoverageFromExposureCommand
	 */
	public void unAssignPolicyCoverage( UnAssignPolicyCoverageFromExposureCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignPolicyCoverage(command.getChildId());
	}
	
	/**
	 * assign InsuredObject on Exposure
	 * @param		command AssignInsuredObjectToExposureCommand
	 */
	public void assignInsuredObject( AssignInsuredObjectToExposureCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignInsuredObject(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign InsuredObject on Exposure
	 * @param		command UnAssignInsuredObjectFromExposureCommand
	 */
	public void unAssignInsuredObject( UnAssignInsuredObjectFromExposureCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignInsuredObject(command.getChildId());
	}
	

	/**
	 * add ClaimReserve to Reserves
	 * @param		command AssignReservesToExposureCommand
	 */
	public void addToReserves( AssignReservesToExposureCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToReserves(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove ClaimReserve from Reserves
	 * @param		command RemoveReservesFromExposureCommand
	 */
	public void removeFromReserves( RemoveReservesFromExposureCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromReserves(command.getParentId(), command.getChildIds());
	}

	/**
	 * add ClaimPayment to Payments
	 * @param		command AssignPaymentsToExposureCommand
	 */
	public void addToPayments( AssignPaymentsToExposureCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToPayments(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove ClaimPayment from Payments
	 * @param		command RemovePaymentsFromExposureCommand
	 */
	public void removeFromPayments( RemovePaymentsFromExposureCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromPayments(command.getParentId(), command.getChildIds());
	}




//************************************************************************
// Attributes
//************************************************************************
	@Autowired
    private final ExposureEntityProjector projector;
	private final ExposureValidator validator;
	private Exposure exposure 	= null;
	private CurrentIdentity identity			= null;
	private static final Logger LOGGER 			=  LoggerFactory.getLogger(ExposureService.class);
}
