/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */

package com.harbormaster.service;

import java.io.IOException;
import java.util.*;
import java.util.concurrent.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import org.springframework.stereotype.Service;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;


import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.repository.*;
import com.harbormaster.validator.*;
import com.harbormaster.projector.*;
import com.harbormaster.security.*;

/**
 *
 * <h2>DeviceVendor business delegate</h2>
 *
 * <p>
 * This class implements the Business Delegate design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of DeviceVendor related services in the case of a DeviceVendor business related service failing.</li>
 * <li>Exposes a simpler, uniform DeviceVendor interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill DeviceVendor business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Spring Boot 3.5</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>ServiceLayer</td></tr>
 *          <tr><td>architecture style</td><td>Layered</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@Service
public class DeviceVendorService 
		extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
	/**
	 * Default Constructor
	 */
    public DeviceVendorService(CurrentIdentity identity,
				ApplicationContext applicationContext)  {

		this.identity		= identity;
		this.projector 		= new DeviceVendorEntityProjector( applicationContext.getBean(ProjectorRegistry.class),
										applicationContext.getBean(DeviceVendorRepository.class) );
		this.validator		= applicationContext.getBean(DeviceVendorValidator.class) ;
	}


	/**
	 * Creates the provided command.
	 *
	 * @param		command ${class.getCreateCommandAlias()}
	 * @return		DeviceVendor
	 */
		public DeviceVendor createDeviceVendor( CreateDeviceVendorCommand command ) {

		DeviceVendor entity = new DeviceVendor();

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

            entity.setDeviceVendorId( command.getDeviceVendorId() );
            entity.setName( command.getName() );
            entity.setLegalName( command.getLegalName() );
            entity.setHeadquartersCountry( command.getHeadquartersCountry() );
            entity.setWebsite( command.getWebsite() );

		// ------------------------------------------
		// persist a new one
		// ------------------------------------------
		entity = projector.create(entity);

		LOGGER.info( "done creating of DeviceVendor {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Update the provided command.
	 * @param		command UpdateDeviceVendorCommand
	 * @return		DeviceVendor
	 */
	public DeviceVendor updateDeviceVendor( UpdateDeviceVendorCommand command ) {

		DeviceVendor entity = new DeviceVendor();

		// --------------------------------------
		// validate
		// --------------------------------------
		validator.validate( command );

            entity.setDeviceVendorId( command.getDeviceVendorId() );
            entity.setName( command.getName() );
            entity.setLegalName( command.getLegalName() );
            entity.setHeadquartersCountry( command.getHeadquartersCountry() );
            entity.setWebsite( command.getWebsite() );
            entity.setDeviceModels( command.getDeviceModels() );
            entity.setFirmwareReleases( command.getFirmwareReleases() );
            entity.setHardwareModules( command.getHardwareModules() );

		// ------------------------------------------
		// persist an existing one
		// ------------------------------------------
		entity = projector.update(entity);

		LOGGER.info( "done saving of DeviceVendor {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Deletes the associatied value object
	 * @param		command DeleteDeviceVendorCommand
	 */
	public void delete( DeleteDeviceVendorCommand command ) {
		UUID id = null;

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		id = command.getDeviceVendorId();

		// ------------------------------------------
		// delete the entity
		// ------------------------------------------
		projector.delete(id);

		LOGGER.info( "done deleting of DeviceVendor {0} ", id );

	}

	/**
	 * Method to retrieve the DeviceVendor via DeviceVendorFetchOneSummary
	 * @param 	summary DeviceVendorFetchOneSummary
	 * @return 	DeviceVendorFetchOneResponse
	 */
	public DeviceVendor getDeviceVendor( DeviceVendorFetchOneSummary summary ) {

		if( summary == null )
			throw new IllegalStateException( "DeviceVendorFetchOneSummary arg cannot be null" );

		DeviceVendor entity = null;
		UUID id = summary.getDeviceVendorId();

		// --------------------------------------
		// validate the fetch one summary
		// --------------------------------------
		validator.validate( summary );

		// --------------------------------------
		// find a DeviceVendor using the provided id
		// --------------------------------------
		entity = projector.find( id );

		return entity;
	}


	/**
	 * Method to retrieve a collection of all DeviceVendors
	 *
	 * @return 	List<DeviceVendor>
	 */
    public List<DeviceVendor> getAllDeviceVendor() {
		List<DeviceVendor> list = projector.findAll( new FindAllDeviceVendorQuery() );
		return list;
	}


	/**
	 * add DeviceModel to DeviceModels
	 * @param		command AssignDeviceModelsToDeviceVendorCommand
	 */
	public void addToDeviceModels( AssignDeviceModelsToDeviceVendorCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToDeviceModels(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove DeviceModel from DeviceModels
	 * @param		command RemoveDeviceModelsFromDeviceVendorCommand
	 */
	public void removeFromDeviceModels( RemoveDeviceModelsFromDeviceVendorCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromDeviceModels(command.getParentId(), command.getChildIds());
	}

	/**
	 * add FirmwareRelease to FirmwareReleases
	 * @param		command AssignFirmwareReleasesToDeviceVendorCommand
	 */
	public void addToFirmwareReleases( AssignFirmwareReleasesToDeviceVendorCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToFirmwareReleases(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove FirmwareRelease from FirmwareReleases
	 * @param		command RemoveFirmwareReleasesFromDeviceVendorCommand
	 */
	public void removeFromFirmwareReleases( RemoveFirmwareReleasesFromDeviceVendorCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromFirmwareReleases(command.getParentId(), command.getChildIds());
	}

	/**
	 * add HardwareModule to HardwareModules
	 * @param		command AssignHardwareModulesToDeviceVendorCommand
	 */
	public void addToHardwareModules( AssignHardwareModulesToDeviceVendorCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToHardwareModules(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove HardwareModule from HardwareModules
	 * @param		command RemoveHardwareModulesFromDeviceVendorCommand
	 */
	public void removeFromHardwareModules( RemoveHardwareModulesFromDeviceVendorCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromHardwareModules(command.getParentId(), command.getChildIds());
	}




//************************************************************************
// Attributes
//************************************************************************
	@Autowired
    private final DeviceVendorEntityProjector projector;
	private final DeviceVendorValidator validator;
	private DeviceVendor deviceVendor 	= null;
	private CurrentIdentity identity			= null;
	private static final Logger LOGGER 			=  LoggerFactory.getLogger(DeviceVendorService.class);
}
