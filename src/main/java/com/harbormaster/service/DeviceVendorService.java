/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.harbormaster.service;

import java.io.IOException;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.*;
import java.util.concurrent.*;

import org.axonframework.commandhandling.gateway.CommandGateway;
import org.axonframework.queryhandling.QueryGateway;
import org.axonframework.queryhandling.QueryUpdateEmitter;
import org.axonframework.messaging.responsetypes.ResponseTypes;
import org.springframework.stereotype.Service;
import org.springframework.beans.BeansException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;

import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.validator.*;

/**
 *
 * <h2>DeviceVendor business service</h2>
 *
 * <p>
 * This class implements the Service design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of DeviceVendor related services in the case of a DeviceVendor business related service failing.</li>
 * <li>Exposes a simpler, uniform DeviceVendor interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill DeviceVendor business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@Service
public class DeviceVendorService 
extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
    /** 
     * Default Constructor 
     */
    public DeviceVendorService()  {
	}

	@Override
	public void setApplicationContext(ApplicationContext ctx) throws BeansException {
		super.setApplicationContext(ctx);

		queryGateway 		= ctx.getBean(QueryGateway.class);
		commandGateway 		= ctx.getBean(CommandGateway.class);
		queryUpdateEmitter  = ctx.getBean(QueryUpdateEmitter.class);
		validator			= ctx.getBean(DeviceVendorValidator.class);
	}


   /**
	* DeviceVendor Business Delegate Factory Method
	*
	* All methods are expected to be self-sufficient.
	*
	* @return 	DeviceVendorService
	*/
	public static DeviceVendorService getDeviceVendorInstance() {
		return( new DeviceVendorService() );
	}
 
   /**
    * Creates the provided command.
    * 
    * @param		command ${class.getCreateCommandAlias()}
    * @exception    ProcessingException
    * @exception	IllegalArgumentException
    * @return		CompletableFuture<UUID>
    */
	public CompletableFuture<UUID> createDeviceVendor( CreateDeviceVendorCommand command )
    throws ProcessingException, IllegalArgumentException {

		CompletableFuture<UUID> completableFuture = null;
				
		try {
			// --------------------------------------
        	// assign identity now if none
        	// -------------------------------------- 
			if ( command.getDeviceVendorId() == null )
				command.setDeviceVendorId( UUID.randomUUID() );
				
			// --------------------------------------
        	// validate the command
        	// --------------------------------------    	
        	validator.validate( command );    

    		// ---------------------------------------
    		// issue the CreateDeviceVendorCommand - by convention the future return value for a create command
        	// that is handled by the constructor of an aggregate will return the UUID 
    		// ---------------------------------------
        	completableFuture = commandGateway.send( command );
        	
			LOGGER.log( Level.INFO, "return from Command Gateway for CreateDeviceVendorCommand of DeviceVendor is " + command );
			
        }
        catch (Exception exc) {
            final String errMsg = "Unable to create DeviceVendor - " + exc;
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }        
        
        return completableFuture;
    }

   /**
    * Update the provided command.
    * @param		command UpdateDeviceVendorCommand
    * @return		CompletableFuture<Void>
    * @exception    ProcessingException
    * @exception  	IllegalArgumentException
    */
    public CompletableFuture<Void> updateDeviceVendor( UpdateDeviceVendorCommand command ) 
    throws ProcessingException, IllegalArgumentException {
    	CompletableFuture<Void> completableFuture = null;
    	
    	try {       

			// --------------------------------------
        	// validate 
        	// --------------------------------------    	
        	validator.validate( command );    

        	// --------------------------------------
        	// issue the UpdateDeviceVendorCommand and return right away
        	// --------------------------------------    	
        	completableFuture = commandGateway.send( command );
    	}
        catch (Exception exc) {
            final String errMsg = "Unable to save DeviceVendor - " + exc;
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        
    	return completableFuture;
    }
   
   /**
    * Deletes the associatied value object
    * @param		command DeleteDeviceVendorCommand
    * @return		CompletableFuture<Void>
    * @exception 	ProcessingException
    */
    public CompletableFuture<Void> delete( DeleteDeviceVendorCommand command ) 
    throws ProcessingException, IllegalArgumentException {	
    	
    	CompletableFuture<Void> completableFuture = null;
    	
        try {  
			// --------------------------------------
        	// validate the command
        	// --------------------------------------    	
        	validator.validate( command );    
        	
        	// --------------------------------------
        	// issue the DeleteDeviceVendorCommand and return right away
        	// --------------------------------------    	
        	completableFuture = commandGateway.send( command );
        }
        catch (Exception exc) {
            final String errMsg = "Unable to delete DeviceVendor using Id = "  + command.getDeviceVendorId();
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }
        
        return completableFuture;
    }

    /**
     * Method to retrieve the DeviceVendor via DeviceVendorFetchOneSummary
     * @param 	summary DeviceVendorFetchOneSummary 
     * @return 	DeviceVendorFetchOneResponse
     * @exception ProcessingException - Thrown if processing any related problems
     * @exception IllegalArgumentException 
     */
    public DeviceVendor getDeviceVendor( DeviceVendorFetchOneSummary summary ) 
    throws ProcessingException, IllegalArgumentException {
    	
    	if( summary == null )
    		throw new IllegalArgumentException( "DeviceVendorFetchOneSummary arg cannot be null" );
    	
    	DeviceVendor entity = null;
    	
        try {
        	// --------------------------------------
        	// validate the fetch one summary
        	// --------------------------------------    	
        	validator.validate( summary );    
        	
        	// --------------------------------------
        	// use queryGateway to send request to Find a DeviceVendor
        	// --------------------------------------
        	CompletableFuture<DeviceVendor> futureEntity = queryGateway.query(new FindDeviceVendorQuery( new LoadDeviceVendorFilter( summary.getDeviceVendorId() ) ), ResponseTypes.instanceOf(DeviceVendor.class));
        	
        	entity = futureEntity.get();
        }
        catch( Exception exc ) {
            final String errMsg = "Unable to locate DeviceVendor with id " + summary.getDeviceVendorId();
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }        
        
        return entity;
    }


    /**
     * Method to retrieve a collection of all DeviceVendors
     *
     * @return 	List<DeviceVendor> 
     * @exception ProcessingException Thrown if any problems
     */
    public List<DeviceVendor> getAllDeviceVendor() 
    throws ProcessingException {
        List<DeviceVendor> list = null;

        try {
        	CompletableFuture<List<DeviceVendor>> futureList = queryGateway.query(new FindAllDeviceVendorQuery(), ResponseTypes.multipleInstancesOf(DeviceVendor.class));
        	
        	list = futureList.get();
        }
        catch( Exception exc ) {
            String errMsg = "Failed to get all DeviceVendor";
            LOGGER.log( Level.WARNING, errMsg, exc );
            throw new ProcessingException( errMsg, exc );
        }
        finally {
        }        
        
        return list;
    }


    /**
     * add DeviceModel to DeviceModels 
     * @param		command AssignDeviceModelsToDeviceVendorCommand
     * @exception	ProcessingException
     */     
	public void addToDeviceModels( AssignDeviceModelsToDeviceVendorCommand command ) throws ProcessingException {
		
		
		// -------------------------------------------
		// load the parent
		// -------------------------------------------
		load( command.getDeviceVendorId() );

		DeviceModelService childDelegate 	= DeviceModelService.getDeviceModelInstance();
		DeviceVendorService parentDelegate = DeviceVendorService.getDeviceVendorInstance();		
		UUID childId = command.getAddTo().getDeviceModelId();
		
		try {		
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );			
		}
		catch( Exception exc ) {
			final String msg = "Failed to add a DeviceModel as DeviceModels to DeviceVendor" ; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}

	}

    /**
     * remove DeviceModel from DeviceModels
     * @param		command RemoveDeviceModelsFromDeviceVendorCommand
     * @exception	ProcessingException
     */     	
	public void removeFromDeviceModels( RemoveDeviceModelsFromDeviceVendorCommand command ) throws ProcessingException {		
		
		DeviceModelService childDelegate 	= DeviceModelService.getDeviceModelInstance();
		UUID childId = command.getRemoveFrom().getDeviceModelId();

		try {
			
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );

		}
		catch( Exception exc ) {
			final String msg = "Failed to remove child using Id " + childId; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}

    /**
     * add FirmwareRelease to FirmwareReleases 
     * @param		command AssignFirmwareReleasesToDeviceVendorCommand
     * @exception	ProcessingException
     */     
	public void addToFirmwareReleases( AssignFirmwareReleasesToDeviceVendorCommand command ) throws ProcessingException {
		
		
		// -------------------------------------------
		// load the parent
		// -------------------------------------------
		load( command.getDeviceVendorId() );

		FirmwareReleaseService childDelegate 	= FirmwareReleaseService.getFirmwareReleaseInstance();
		DeviceVendorService parentDelegate = DeviceVendorService.getDeviceVendorInstance();		
		UUID childId = command.getAddTo().getFirmwareReleaseId();
		
		try {		
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );			
		}
		catch( Exception exc ) {
			final String msg = "Failed to add a FirmwareRelease as FirmwareReleases to DeviceVendor" ; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}

	}

    /**
     * remove FirmwareRelease from FirmwareReleases
     * @param		command RemoveFirmwareReleasesFromDeviceVendorCommand
     * @exception	ProcessingException
     */     	
	public void removeFromFirmwareReleases( RemoveFirmwareReleasesFromDeviceVendorCommand command ) throws ProcessingException {		
		
		FirmwareReleaseService childDelegate 	= FirmwareReleaseService.getFirmwareReleaseInstance();
		UUID childId = command.getRemoveFrom().getFirmwareReleaseId();

		try {
			
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );

		}
		catch( Exception exc ) {
			final String msg = "Failed to remove child using Id " + childId; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}

    /**
     * add HardwareModule to HardwareModules 
     * @param		command AssignHardwareModulesToDeviceVendorCommand
     * @exception	ProcessingException
     */     
	public void addToHardwareModules( AssignHardwareModulesToDeviceVendorCommand command ) throws ProcessingException {
		
		
		// -------------------------------------------
		// load the parent
		// -------------------------------------------
		load( command.getDeviceVendorId() );

		HardwareModuleService childDelegate 	= HardwareModuleService.getHardwareModuleInstance();
		DeviceVendorService parentDelegate = DeviceVendorService.getDeviceVendorInstance();		
		UUID childId = command.getAddTo().getHardwareModuleId();
		
		try {		
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
        	// issue the command
        	// --------------------------------------    	
    		commandGateway.sendAndWait( command );			
		}
		catch( Exception exc ) {
			final String msg = "Failed to add a HardwareModule as HardwareModules to DeviceVendor" ; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}

	}

    /**
     * remove HardwareModule from HardwareModules
     * @param		command RemoveHardwareModulesFromDeviceVendorCommand
     * @exception	ProcessingException
     */     	
	public void removeFromHardwareModules( RemoveHardwareModulesFromDeviceVendorCommand command ) throws ProcessingException {		
		
		HardwareModuleService childDelegate 	= HardwareModuleService.getHardwareModuleInstance();
		UUID childId = command.getRemoveFrom().getHardwareModuleId();

		try {
			
			// --------------------------------------
	    	// validate the command
	    	// --------------------------------------    
	    	validator.validate( command );    

	    	// --------------------------------------
	    	// issue the command
	    	// --------------------------------------    	
			commandGateway.sendAndWait( command );

		}
		catch( Exception exc ) {
			final String msg = "Failed to remove child using Id " + childId; 
			LOGGER.log( Level.WARNING, msg, exc );
			throw new ProcessingException( msg, exc );
		}
	}



	/**
	 * Internal helper method to load the root 
	 * 
	 * @param		id	UUID
	 * @return		DeviceVendor
	 */
	protected DeviceVendor load( UUID id ) throws ProcessingException {
		deviceVendor = DeviceVendorService.getDeviceVendorInstance().getDeviceVendor( new DeviceVendorFetchOneSummary(id) );	
		return deviceVendor;
	}


//************************************************************************
// Attributes
//************************************************************************
	private QueryGateway queryGateway;
	private CommandGateway commandGateway;
	private QueryUpdateEmitter queryUpdateEmitter;
	private DeviceVendorValidator validator;
	private DeviceVendor deviceVendor 	= null;
    private static final Logger LOGGER 			= Logger.getLogger(DeviceVendorService.class.getName());
    
}
