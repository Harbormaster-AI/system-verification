/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Insurance Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */

package com.harbormaster.service;

import java.io.IOException;
import java.util.*;
import java.util.concurrent.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import org.springframework.stereotype.Service;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;


import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.repository.*;
import com.harbormaster.validator.*;
import com.harbormaster.projector.*;
import com.harbormaster.security.*;

/**
 *
 * <h2>Customer business delegate</h2>
 *
 * <p>
 * This class implements the Business Delegate design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of Customer related services in the case of a Customer business related service failing.</li>
 * <li>Exposes a simpler, uniform Customer interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill Customer business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Spring Boot 3.5</td></tr>
 *          <tr><td>published</td><td>09/12/2026</td></tr>
 *          <tr><td>design pattern</td><td>ServiceLayer</td></tr>
 *          <tr><td>architecture style</td><td>Layered</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Insurance Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core insurance product, underwriting, policy, billing, claims, and reinsurance capabilities.</td></tr>
 *          <tr><td>industry</td><td>Insurance</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@Service
public class CustomerService 
		extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
	/**
	 * Default Constructor
	 */
    public CustomerService(CurrentIdentity identity,
				ApplicationContext applicationContext)  {

		this.identity		= identity;
		this.projector 		= new CustomerEntityProjector( applicationContext.getBean(ProjectorRegistry.class),
										applicationContext.getBean(CustomerRepository.class) );
		this.validator		= applicationContext.getBean(CustomerValidator.class) ;
	}


	/**
	 * Creates the provided command.
	 *
	 * @param		command ${class.getCreateCommandAlias()}
	 * @return		Customer
	 */
		public Customer createCustomer( CreateCustomerCommand command ) {

		Customer entity = new Customer();

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

            entity.setCustomerId( command.getCustomerId() );
            entity.setFirstName( command.getFirstName() );
            entity.setLastName( command.getLastName() );
            entity.setOrganizationName( command.getOrganizationName() );
            entity.setTaxId( command.getTaxId() );
            entity.setDateOfBirth( command.getDateOfBirth() );
            entity.setPrimaryAddress( command.getPrimaryAddress() );
            entity.setCustomerType( command.getCustomerType() );

		// ------------------------------------------
		// persist a new one
		// ------------------------------------------
		entity = projector.create(entity);

		LOGGER.info( "done creating of Customer {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Update the provided command.
	 * @param		command UpdateCustomerCommand
	 * @return		Customer
	 */
	public Customer updateCustomer( UpdateCustomerCommand command ) {

		Customer entity = new Customer();

		// --------------------------------------
		// validate
		// --------------------------------------
		validator.validate( command );

            entity.setCustomerId( command.getCustomerId() );
            entity.setFirstName( command.getFirstName() );
            entity.setLastName( command.getLastName() );
            entity.setOrganizationName( command.getOrganizationName() );
            entity.setTaxId( command.getTaxId() );
            entity.setDateOfBirth( command.getDateOfBirth() );
            entity.setPrimaryAddress( command.getPrimaryAddress() );
            entity.setApplications( command.getApplications() );
            entity.setPolicies( command.getPolicies() );
            entity.setClaims( command.getClaims() );
            entity.setAgents( command.getAgents() );
            entity.setBeneficiaries( command.getBeneficiaries() );
            entity.setCustomerType( command.getCustomerType() );

		// ------------------------------------------
		// persist an existing one
		// ------------------------------------------
		entity = projector.update(entity);

		LOGGER.info( "done saving of Customer {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Deletes the associatied value object
	 * @param		command DeleteCustomerCommand
	 */
	public void delete( DeleteCustomerCommand command ) {
		UUID id = null;

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		id = command.getCustomerId();

		// ------------------------------------------
		// delete the entity
		// ------------------------------------------
		projector.delete(id);

		LOGGER.info( "done deleting of Customer {0} ", id );

	}

	/**
	 * Method to retrieve the Customer via CustomerFetchOneSummary
	 * @param 	summary CustomerFetchOneSummary
	 * @return 	CustomerFetchOneResponse
	 */
	public Customer getCustomer( CustomerFetchOneSummary summary ) {

		if( summary == null )
			throw new IllegalStateException( "CustomerFetchOneSummary arg cannot be null" );

		Customer entity = null;
		UUID id = summary.getCustomerId();

		// --------------------------------------
		// validate the fetch one summary
		// --------------------------------------
		validator.validate( summary );

		// --------------------------------------
		// find a Customer using the provided id
		// --------------------------------------
		entity = projector.find( id );

		return entity;
	}


	/**
	 * Method to retrieve a collection of all Customers
	 *
	 * @return 	List<Customer>
	 */
    public List<Customer> getAllCustomer() {
		List<Customer> list = projector.findAll( new FindAllCustomerQuery() );
		return list;
	}


	/**
	 * add Application to Applications
	 * @param		command AssignApplicationsToCustomerCommand
	 */
	public void addToApplications( AssignApplicationsToCustomerCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToApplications(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove Application from Applications
	 * @param		command RemoveApplicationsFromCustomerCommand
	 */
	public void removeFromApplications( RemoveApplicationsFromCustomerCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromApplications(command.getParentId(), command.getChildIds());
	}

	/**
	 * add Policy to Policies
	 * @param		command AssignPoliciesToCustomerCommand
	 */
	public void addToPolicies( AssignPoliciesToCustomerCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToPolicies(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove Policy from Policies
	 * @param		command RemovePoliciesFromCustomerCommand
	 */
	public void removeFromPolicies( RemovePoliciesFromCustomerCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromPolicies(command.getParentId(), command.getChildIds());
	}

	/**
	 * add Claim to Claims
	 * @param		command AssignClaimsToCustomerCommand
	 */
	public void addToClaims( AssignClaimsToCustomerCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToClaims(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove Claim from Claims
	 * @param		command RemoveClaimsFromCustomerCommand
	 */
	public void removeFromClaims( RemoveClaimsFromCustomerCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromClaims(command.getParentId(), command.getChildIds());
	}

	/**
	 * add Agent to Agents
	 * @param		command AssignAgentsToCustomerCommand
	 */
	public void addToAgents( AssignAgentsToCustomerCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToAgents(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove Agent from Agents
	 * @param		command RemoveAgentsFromCustomerCommand
	 */
	public void removeFromAgents( RemoveAgentsFromCustomerCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromAgents(command.getParentId(), command.getChildIds());
	}

	/**
	 * add Beneficiary to Beneficiaries
	 * @param		command AssignBeneficiariesToCustomerCommand
	 */
	public void addToBeneficiaries( AssignBeneficiariesToCustomerCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToBeneficiaries(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove Beneficiary from Beneficiaries
	 * @param		command RemoveBeneficiariesFromCustomerCommand
	 */
	public void removeFromBeneficiaries( RemoveBeneficiariesFromCustomerCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromBeneficiaries(command.getParentId(), command.getChildIds());
	}




//************************************************************************
// Attributes
//************************************************************************
	@Autowired
    private final CustomerEntityProjector projector;
	private final CustomerValidator validator;
	private Customer customer 	= null;
	private CurrentIdentity identity			= null;
	private static final Logger LOGGER 			=  LoggerFactory.getLogger(CustomerService.class);
}
