/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */

package com.harbormaster.service;

import java.io.IOException;
import java.util.*;
import java.util.concurrent.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import org.springframework.stereotype.Service;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;


import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.repository.*;
import com.harbormaster.validator.*;
import com.harbormaster.projector.*;
import com.harbormaster.security.*;

/**
 *
 * <h2>Site business delegate</h2>
 *
 * <p>
 * This class implements the Business Delegate design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of Site related services in the case of a Site business related service failing.</li>
 * <li>Exposes a simpler, uniform Site interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill Site business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Spring Boot 3.5</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>ServiceLayer</td></tr>
 *          <tr><td>architecture style</td><td>Layered</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@Service
public class SiteService 
		extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
	/**
	 * Default Constructor
	 */
    public SiteService(CurrentIdentity identity,
				ApplicationContext applicationContext)  {

		this.identity		= identity;
		this.projector 		= new SiteEntityProjector( applicationContext.getBean(ProjectorRegistry.class),
										applicationContext.getBean(SiteRepository.class) );
		this.validator		= applicationContext.getBean(SiteValidator.class) ;
	}


	/**
	 * Creates the provided command.
	 *
	 * @param		command ${class.getCreateCommandAlias()}
	 * @return		Site
	 */
		public Site createSite( CreateSiteCommand command ) {

		Site entity = new Site();

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

            entity.setSiteId( command.getSiteId() );
            entity.setName( command.getName() );
            entity.setAddress( command.getAddress() );
            entity.setTimezone( command.getTimezone() );
            entity.setLatitude( command.getLatitude() );
            entity.setLongitude( command.getLongitude() );

		// ------------------------------------------
		// persist a new one
		// ------------------------------------------
		entity = projector.create(entity);

		LOGGER.info( "done creating of Site {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Update the provided command.
	 * @param		command UpdateSiteCommand
	 * @return		Site
	 */
	public Site updateSite( UpdateSiteCommand command ) {

		Site entity = new Site();

		// --------------------------------------
		// validate
		// --------------------------------------
		validator.validate( command );

            entity.setSiteId( command.getSiteId() );
            entity.setName( command.getName() );
            entity.setAddress( command.getAddress() );
            entity.setTimezone( command.getTimezone() );
            entity.setLatitude( command.getLatitude() );
            entity.setLongitude( command.getLongitude() );
            entity.setTenant( command.getTenant() );
            entity.setBuildings( command.getBuildings() );
            entity.setDevices( command.getDevices() );
            entity.setGateways( command.getGateways() );

		// ------------------------------------------
		// persist an existing one
		// ------------------------------------------
		entity = projector.update(entity);

		LOGGER.info( "done saving of Site {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Deletes the associatied value object
	 * @param		command DeleteSiteCommand
	 */
	public void delete( DeleteSiteCommand command ) {
		UUID id = null;

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		id = command.getSiteId();

		// ------------------------------------------
		// delete the entity
		// ------------------------------------------
		projector.delete(id);

		LOGGER.info( "done deleting of Site {0} ", id );

	}

	/**
	 * Method to retrieve the Site via SiteFetchOneSummary
	 * @param 	summary SiteFetchOneSummary
	 * @return 	SiteFetchOneResponse
	 */
	public Site getSite( SiteFetchOneSummary summary ) {

		if( summary == null )
			throw new IllegalStateException( "SiteFetchOneSummary arg cannot be null" );

		Site entity = null;
		UUID id = summary.getSiteId();

		// --------------------------------------
		// validate the fetch one summary
		// --------------------------------------
		validator.validate( summary );

		// --------------------------------------
		// find a Site using the provided id
		// --------------------------------------
		entity = projector.find( id );

		return entity;
	}


	/**
	 * Method to retrieve a collection of all Sites
	 *
	 * @return 	List<Site>
	 */
    public List<Site> getAllSite() {
		List<Site> list = projector.findAll( new FindAllSiteQuery() );
		return list;
	}

	/**
	 * assign Tenant on Site
	 * @param		command AssignTenantToSiteCommand
	 */
	public void assignTenant( AssignTenantToSiteCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignTenant(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Tenant on Site
	 * @param		command UnAssignTenantFromSiteCommand
	 */
	public void unAssignTenant( UnAssignTenantFromSiteCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignTenant(command.getChildId());
	}
	

	/**
	 * add Building to Buildings
	 * @param		command AssignBuildingsToSiteCommand
	 */
	public void addToBuildings( AssignBuildingsToSiteCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToBuildings(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove Building from Buildings
	 * @param		command RemoveBuildingsFromSiteCommand
	 */
	public void removeFromBuildings( RemoveBuildingsFromSiteCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromBuildings(command.getParentId(), command.getChildIds());
	}

	/**
	 * add IoTDevice to Devices
	 * @param		command AssignDevicesToSiteCommand
	 */
	public void addToDevices( AssignDevicesToSiteCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToDevices(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove IoTDevice from Devices
	 * @param		command RemoveDevicesFromSiteCommand
	 */
	public void removeFromDevices( RemoveDevicesFromSiteCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromDevices(command.getParentId(), command.getChildIds());
	}

	/**
	 * add Gateway to Gateways
	 * @param		command AssignGatewaysToSiteCommand
	 */
	public void addToGateways( AssignGatewaysToSiteCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToGateways(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove Gateway from Gateways
	 * @param		command RemoveGatewaysFromSiteCommand
	 */
	public void removeFromGateways( RemoveGatewaysFromSiteCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromGateways(command.getParentId(), command.getChildIds());
	}




//************************************************************************
// Attributes
//************************************************************************
	@Autowired
    private final SiteEntityProjector projector;
	private final SiteValidator validator;
	private Site site 	= null;
	private CurrentIdentity identity			= null;
	private static final Logger LOGGER 			=  LoggerFactory.getLogger(SiteService.class);
}
