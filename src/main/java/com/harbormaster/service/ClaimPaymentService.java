/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Insurance Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */

package com.harbormaster.service;

import java.io.IOException;
import java.util.*;
import java.util.concurrent.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import org.springframework.stereotype.Service;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;


import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.repository.*;
import com.harbormaster.validator.*;
import com.harbormaster.projector.*;
import com.harbormaster.security.*;

/**
 *
 * <h2>ClaimPayment business delegate</h2>
 *
 * <p>
 * This class implements the Business Delegate design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of ClaimPayment related services in the case of a ClaimPayment business related service failing.</li>
 * <li>Exposes a simpler, uniform ClaimPayment interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill ClaimPayment business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Spring Boot 3.5</td></tr>
 *          <tr><td>published</td><td>09/12/2026</td></tr>
 *          <tr><td>design pattern</td><td>ServiceLayer</td></tr>
 *          <tr><td>architecture style</td><td>Layered</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Insurance Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core insurance product, underwriting, policy, billing, claims, and reinsurance capabilities.</td></tr>
 *          <tr><td>industry</td><td>Insurance</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@Service
public class ClaimPaymentService 
		extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
	/**
	 * Default Constructor
	 */
    public ClaimPaymentService(CurrentIdentity identity,
				ApplicationContext applicationContext)  {

		this.identity		= identity;
		this.projector 		= new ClaimPaymentEntityProjector( applicationContext.getBean(ProjectorRegistry.class),
										applicationContext.getBean(ClaimPaymentRepository.class) );
		this.validator		= applicationContext.getBean(ClaimPaymentValidator.class) ;
	}


	/**
	 * Creates the provided command.
	 *
	 * @param		command ${class.getCreateCommandAlias()}
	 * @return		ClaimPayment
	 */
		public ClaimPayment createClaimPayment( CreateClaimPaymentCommand command ) {

		ClaimPayment entity = new ClaimPayment();

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

            entity.setClaimPaymentId( command.getClaimPaymentId() );
            entity.setPaymentNumber( command.getPaymentNumber() );
            entity.setAmount( command.getAmount() );
            entity.setPaymentDate( command.getPaymentDate() );
            entity.setPayeeType( command.getPayeeType() );
            entity.setMethod( command.getMethod() );
            entity.setStatus( command.getStatus() );

		// ------------------------------------------
		// persist a new one
		// ------------------------------------------
		entity = projector.create(entity);

		LOGGER.info( "done creating of ClaimPayment {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Update the provided command.
	 * @param		command UpdateClaimPaymentCommand
	 * @return		ClaimPayment
	 */
	public ClaimPayment updateClaimPayment( UpdateClaimPaymentCommand command ) {

		ClaimPayment entity = new ClaimPayment();

		// --------------------------------------
		// validate
		// --------------------------------------
		validator.validate( command );

            entity.setClaimPaymentId( command.getClaimPaymentId() );
            entity.setPaymentNumber( command.getPaymentNumber() );
            entity.setAmount( command.getAmount() );
            entity.setPaymentDate( command.getPaymentDate() );
            entity.setClaim( command.getClaim() );
            entity.setExposure( command.getExposure() );
            entity.setBeneficiary( command.getBeneficiary() );
            entity.setServiceProvider( command.getServiceProvider() );
            entity.setCustomer( command.getCustomer() );
            entity.setPayeeType( command.getPayeeType() );
            entity.setMethod( command.getMethod() );
            entity.setStatus( command.getStatus() );

		// ------------------------------------------
		// persist an existing one
		// ------------------------------------------
		entity = projector.update(entity);

		LOGGER.info( "done saving of ClaimPayment {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Deletes the associatied value object
	 * @param		command DeleteClaimPaymentCommand
	 */
	public void delete( DeleteClaimPaymentCommand command ) {
		UUID id = null;

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		id = command.getClaimPaymentId();

		// ------------------------------------------
		// delete the entity
		// ------------------------------------------
		projector.delete(id);

		LOGGER.info( "done deleting of ClaimPayment {0} ", id );

	}

	/**
	 * Method to retrieve the ClaimPayment via ClaimPaymentFetchOneSummary
	 * @param 	summary ClaimPaymentFetchOneSummary
	 * @return 	ClaimPaymentFetchOneResponse
	 */
	public ClaimPayment getClaimPayment( ClaimPaymentFetchOneSummary summary ) {

		if( summary == null )
			throw new IllegalStateException( "ClaimPaymentFetchOneSummary arg cannot be null" );

		ClaimPayment entity = null;
		UUID id = summary.getClaimPaymentId();

		// --------------------------------------
		// validate the fetch one summary
		// --------------------------------------
		validator.validate( summary );

		// --------------------------------------
		// find a ClaimPayment using the provided id
		// --------------------------------------
		entity = projector.find( id );

		return entity;
	}


	/**
	 * Method to retrieve a collection of all ClaimPayments
	 *
	 * @return 	List<ClaimPayment>
	 */
    public List<ClaimPayment> getAllClaimPayment() {
		List<ClaimPayment> list = projector.findAll( new FindAllClaimPaymentQuery() );
		return list;
	}

	/**
	 * assign Claim on ClaimPayment
	 * @param		command AssignClaimToClaimPaymentCommand
	 */
	public void assignClaim( AssignClaimToClaimPaymentCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignClaim(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Claim on ClaimPayment
	 * @param		command UnAssignClaimFromClaimPaymentCommand
	 */
	public void unAssignClaim( UnAssignClaimFromClaimPaymentCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignClaim(command.getChildId());
	}
	
	/**
	 * assign Exposure on ClaimPayment
	 * @param		command AssignExposureToClaimPaymentCommand
	 */
	public void assignExposure( AssignExposureToClaimPaymentCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignExposure(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Exposure on ClaimPayment
	 * @param		command UnAssignExposureFromClaimPaymentCommand
	 */
	public void unAssignExposure( UnAssignExposureFromClaimPaymentCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignExposure(command.getChildId());
	}
	
	/**
	 * assign Beneficiary on ClaimPayment
	 * @param		command AssignBeneficiaryToClaimPaymentCommand
	 */
	public void assignBeneficiary( AssignBeneficiaryToClaimPaymentCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignBeneficiary(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Beneficiary on ClaimPayment
	 * @param		command UnAssignBeneficiaryFromClaimPaymentCommand
	 */
	public void unAssignBeneficiary( UnAssignBeneficiaryFromClaimPaymentCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignBeneficiary(command.getChildId());
	}
	
	/**
	 * assign ServiceProvider on ClaimPayment
	 * @param		command AssignServiceProviderToClaimPaymentCommand
	 */
	public void assignServiceProvider( AssignServiceProviderToClaimPaymentCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignServiceProvider(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign ServiceProvider on ClaimPayment
	 * @param		command UnAssignServiceProviderFromClaimPaymentCommand
	 */
	public void unAssignServiceProvider( UnAssignServiceProviderFromClaimPaymentCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignServiceProvider(command.getChildId());
	}
	
	/**
	 * assign Customer on ClaimPayment
	 * @param		command AssignCustomerToClaimPaymentCommand
	 */
	public void assignCustomer( AssignCustomerToClaimPaymentCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignCustomer(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Customer on ClaimPayment
	 * @param		command UnAssignCustomerFromClaimPaymentCommand
	 */
	public void unAssignCustomer( UnAssignCustomerFromClaimPaymentCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignCustomer(command.getChildId());
	}
	




//************************************************************************
// Attributes
//************************************************************************
	@Autowired
    private final ClaimPaymentEntityProjector projector;
	private final ClaimPaymentValidator validator;
	private ClaimPayment claimPayment 	= null;
	private CurrentIdentity identity			= null;
	private static final Logger LOGGER 			=  LoggerFactory.getLogger(ClaimPaymentService.class);
}
