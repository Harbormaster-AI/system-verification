/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Insurance Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */

package com.harbormaster.service;

import java.io.IOException;
import java.util.*;
import java.util.concurrent.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import org.springframework.stereotype.Service;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;


import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.repository.*;
import com.harbormaster.validator.*;
import com.harbormaster.projector.*;
import com.harbormaster.security.*;

/**
 *
 * <h2>Document business delegate</h2>
 *
 * <p>
 * This class implements the Business Delegate design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of Document related services in the case of a Document business related service failing.</li>
 * <li>Exposes a simpler, uniform Document interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill Document business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Spring Boot 3.5</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>ServiceLayer</td></tr>
 *          <tr><td>architecture style</td><td>Layered</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Insurance Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core insurance product, underwriting, policy, billing, claims, and reinsurance capabilities.</td></tr>
 *          <tr><td>industry</td><td>Insurance</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@Service
public class DocumentService 
		extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
	/**
	 * Default Constructor
	 */
    public DocumentService(CurrentIdentity identity,
				ApplicationContext applicationContext)  {

		this.identity		= identity;
		this.projector 		= new DocumentEntityProjector( applicationContext.getBean(ProjectorRegistry.class),
										applicationContext.getBean(DocumentRepository.class) );
		this.validator		= applicationContext.getBean(DocumentValidator.class) ;
	}


	/**
	 * Creates the provided command.
	 *
	 * @param		command ${class.getCreateCommandAlias()}
	 * @return		Document
	 */
		public Document createDocument( CreateDocumentCommand command ) {

		Document entity = new Document();

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

            entity.setDocumentId( command.getDocumentId() );
            entity.setFileName( command.getFileName() );
            entity.setUploadedDate( command.getUploadedDate() );
            entity.setDocumentType( command.getDocumentType() );

		// ------------------------------------------
		// persist a new one
		// ------------------------------------------
		entity = projector.create(entity);

		LOGGER.info( "done creating of Document {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Update the provided command.
	 * @param		command UpdateDocumentCommand
	 * @return		Document
	 */
	public Document updateDocument( UpdateDocumentCommand command ) {

		Document entity = new Document();

		// --------------------------------------
		// validate
		// --------------------------------------
		validator.validate( command );

            entity.setDocumentId( command.getDocumentId() );
            entity.setFileName( command.getFileName() );
            entity.setUploadedDate( command.getUploadedDate() );
            entity.setPolicy( command.getPolicy() );
            entity.setClaim( command.getClaim() );
            entity.setApplication( command.getApplication() );
            entity.setCustomer( command.getCustomer() );
            entity.setDocumentType( command.getDocumentType() );

		// ------------------------------------------
		// persist an existing one
		// ------------------------------------------
		entity = projector.update(entity);

		LOGGER.info( "done saving of Document {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Deletes the associatied value object
	 * @param		command DeleteDocumentCommand
	 */
	public void delete( DeleteDocumentCommand command ) {
		UUID id = null;

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		id = command.getDocumentId();

		// ------------------------------------------
		// delete the entity
		// ------------------------------------------
		projector.delete(id);

		LOGGER.info( "done deleting of Document {0} ", id );

	}

	/**
	 * Method to retrieve the Document via DocumentFetchOneSummary
	 * @param 	summary DocumentFetchOneSummary
	 * @return 	DocumentFetchOneResponse
	 */
	public Document getDocument( DocumentFetchOneSummary summary ) {

		if( summary == null )
			throw new IllegalStateException( "DocumentFetchOneSummary arg cannot be null" );

		Document entity = null;
		UUID id = summary.getDocumentId();

		// --------------------------------------
		// validate the fetch one summary
		// --------------------------------------
		validator.validate( summary );

		// --------------------------------------
		// find a Document using the provided id
		// --------------------------------------
		entity = projector.find( id );

		return entity;
	}


	/**
	 * Method to retrieve a collection of all Documents
	 *
	 * @return 	List<Document>
	 */
    public List<Document> getAllDocument() {
		List<Document> list = projector.findAll( new FindAllDocumentQuery() );
		return list;
	}

	/**
	 * assign Policy on Document
	 * @param		command AssignPolicyToDocumentCommand
	 */
	public void assignPolicy( AssignPolicyToDocumentCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignPolicy(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Policy on Document
	 * @param		command UnAssignPolicyFromDocumentCommand
	 */
	public void unAssignPolicy( UnAssignPolicyFromDocumentCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignPolicy(command.getChildId());
	}
	
	/**
	 * assign Claim on Document
	 * @param		command AssignClaimToDocumentCommand
	 */
	public void assignClaim( AssignClaimToDocumentCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignClaim(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Claim on Document
	 * @param		command UnAssignClaimFromDocumentCommand
	 */
	public void unAssignClaim( UnAssignClaimFromDocumentCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignClaim(command.getChildId());
	}
	
	/**
	 * assign Application on Document
	 * @param		command AssignApplicationToDocumentCommand
	 */
	public void assignApplication( AssignApplicationToDocumentCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignApplication(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Application on Document
	 * @param		command UnAssignApplicationFromDocumentCommand
	 */
	public void unAssignApplication( UnAssignApplicationFromDocumentCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignApplication(command.getChildId());
	}
	
	/**
	 * assign Customer on Document
	 * @param		command AssignCustomerToDocumentCommand
	 */
	public void assignCustomer( AssignCustomerToDocumentCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignCustomer(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Customer on Document
	 * @param		command UnAssignCustomerFromDocumentCommand
	 */
	public void unAssignCustomer( UnAssignCustomerFromDocumentCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignCustomer(command.getChildId());
	}
	




//************************************************************************
// Attributes
//************************************************************************
	@Autowired
    private final DocumentEntityProjector projector;
	private final DocumentValidator validator;
	private Document document 	= null;
	private CurrentIdentity identity			= null;
	private static final Logger LOGGER 			=  LoggerFactory.getLogger(DocumentService.class);
}
