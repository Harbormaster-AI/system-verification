/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Insurance Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */

package com.harbormaster.service;

import java.io.IOException;
import java.util.*;
import java.util.concurrent.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import org.springframework.stereotype.Service;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;


import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.repository.*;
import com.harbormaster.validator.*;
import com.harbormaster.projector.*;
import com.harbormaster.security.*;

/**
 *
 * <h2>Application business delegate</h2>
 *
 * <p>
 * This class implements the Business Delegate design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of Application related services in the case of a Application business related service failing.</li>
 * <li>Exposes a simpler, uniform Application interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill Application business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Spring Boot 3.5</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>ServiceLayer</td></tr>
 *          <tr><td>architecture style</td><td>Layered</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>Insurance Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core insurance product, underwriting, policy, billing, claims, and reinsurance capabilities.</td></tr>
 *          <tr><td>industry</td><td>Insurance</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@Service
public class ApplicationService 
		extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
	/**
	 * Default Constructor
	 */
    public ApplicationService(CurrentIdentity identity,
				ApplicationContext applicationContext)  {

		this.identity		= identity;
		this.projector 		= new ApplicationEntityProjector( applicationContext.getBean(ProjectorRegistry.class),
										applicationContext.getBean(ApplicationRepository.class) );
		this.validator		= applicationContext.getBean(ApplicationValidator.class) ;
	}


	/**
	 * Creates the provided command.
	 *
	 * @param		command ${class.getCreateCommandAlias()}
	 * @return		Application
	 */
		public Application createApplication( CreateApplicationCommand command ) {

		Application entity = new Application();

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

            entity.setApplicationId( command.getApplicationId() );
            entity.setApplicationNumber( command.getApplicationNumber() );
            entity.setSubmissionDate( command.getSubmissionDate() );
            entity.setStatus( command.getStatus() );

		// ------------------------------------------
		// persist a new one
		// ------------------------------------------
		entity = projector.create(entity);

		LOGGER.info( "done creating of Application {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Update the provided command.
	 * @param		command UpdateApplicationCommand
	 * @return		Application
	 */
	public Application updateApplication( UpdateApplicationCommand command ) {

		Application entity = new Application();

		// --------------------------------------
		// validate
		// --------------------------------------
		validator.validate( command );

            entity.setApplicationId( command.getApplicationId() );
            entity.setApplicationNumber( command.getApplicationNumber() );
            entity.setSubmissionDate( command.getSubmissionDate() );
            entity.setCustomer( command.getCustomer() );
            entity.setProduct( command.getProduct() );
            entity.setDistributor( command.getDistributor() );
            entity.setQuotes( command.getQuotes() );
            entity.setSelectedQuote( command.getSelectedQuote() );
            entity.setStatus( command.getStatus() );

		// ------------------------------------------
		// persist an existing one
		// ------------------------------------------
		entity = projector.update(entity);

		LOGGER.info( "done saving of Application {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Deletes the associatied value object
	 * @param		command DeleteApplicationCommand
	 */
	public void delete( DeleteApplicationCommand command ) {
		UUID id = null;

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		id = command.getApplicationId();

		// ------------------------------------------
		// delete the entity
		// ------------------------------------------
		projector.delete(id);

		LOGGER.info( "done deleting of Application {0} ", id );

	}

	/**
	 * Method to retrieve the Application via ApplicationFetchOneSummary
	 * @param 	summary ApplicationFetchOneSummary
	 * @return 	ApplicationFetchOneResponse
	 */
	public Application getApplication( ApplicationFetchOneSummary summary ) {

		if( summary == null )
			throw new IllegalStateException( "ApplicationFetchOneSummary arg cannot be null" );

		Application entity = null;
		UUID id = summary.getApplicationId();

		// --------------------------------------
		// validate the fetch one summary
		// --------------------------------------
		validator.validate( summary );

		// --------------------------------------
		// find a Application using the provided id
		// --------------------------------------
		entity = projector.find( id );

		return entity;
	}


	/**
	 * Method to retrieve a collection of all Applications
	 *
	 * @return 	List<Application>
	 */
    public List<Application> getAllApplication() {
		List<Application> list = projector.findAll( new FindAllApplicationQuery() );
		return list;
	}

	/**
	 * assign Customer on Application
	 * @param		command AssignCustomerToApplicationCommand
	 */
	public void assignCustomer( AssignCustomerToApplicationCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignCustomer(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Customer on Application
	 * @param		command UnAssignCustomerFromApplicationCommand
	 */
	public void unAssignCustomer( UnAssignCustomerFromApplicationCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignCustomer(command.getChildId());
	}
	
	/**
	 * assign Product on Application
	 * @param		command AssignProductToApplicationCommand
	 */
	public void assignProduct( AssignProductToApplicationCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignProduct(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Product on Application
	 * @param		command UnAssignProductFromApplicationCommand
	 */
	public void unAssignProduct( UnAssignProductFromApplicationCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignProduct(command.getChildId());
	}
	
	/**
	 * assign Distributor on Application
	 * @param		command AssignDistributorToApplicationCommand
	 */
	public void assignDistributor( AssignDistributorToApplicationCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignDistributor(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Distributor on Application
	 * @param		command UnAssignDistributorFromApplicationCommand
	 */
	public void unAssignDistributor( UnAssignDistributorFromApplicationCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignDistributor(command.getChildId());
	}
	
	/**
	 * assign SelectedQuote on Application
	 * @param		command AssignSelectedQuoteToApplicationCommand
	 */
	public void assignSelectedQuote( AssignSelectedQuoteToApplicationCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignSelectedQuote(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign SelectedQuote on Application
	 * @param		command UnAssignSelectedQuoteFromApplicationCommand
	 */
	public void unAssignSelectedQuote( UnAssignSelectedQuoteFromApplicationCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignSelectedQuote(command.getChildId());
	}
	

	/**
	 * add Quote to Quotes
	 * @param		command AssignQuotesToApplicationCommand
	 */
	public void addToQuotes( AssignQuotesToApplicationCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToQuotes(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove Quote from Quotes
	 * @param		command RemoveQuotesFromApplicationCommand
	 */
	public void removeFromQuotes( RemoveQuotesFromApplicationCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromQuotes(command.getParentId(), command.getChildIds());
	}




//************************************************************************
// Attributes
//************************************************************************
	@Autowired
    private final ApplicationEntityProjector projector;
	private final ApplicationValidator validator;
	private Application application 	= null;
	private CurrentIdentity identity			= null;
	private static final Logger LOGGER 			=  LoggerFactory.getLogger(ApplicationService.class);
}
