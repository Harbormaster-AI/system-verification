/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */

package com.harbormaster.service;

import java.io.IOException;
import java.util.*;
import java.util.concurrent.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import org.springframework.stereotype.Service;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.ApplicationContext;


import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;
import com.harbormaster.repository.*;
import com.harbormaster.validator.*;
import com.harbormaster.projector.*;
import com.harbormaster.security.*;

/**
 *
 * <h2>DigitalTwin business delegate</h2>
 *
 * <p>
 * This class implements the Business Delegate design pattern for the purpose of:
 * </p>
 * <ol>
 * <li>Reducing coupling between the business tier and a client of the business tier by hiding all business-tier implementation details</li>
 * <li>Improving the available of DigitalTwin related services in the case of a DigitalTwin business related service failing.</li>
 * <li>Exposes a simpler, uniform DigitalTwin interface to the business tier, making it easy for clients to consume a simple Java object.</li>
 * <li>Hides the communication protocol that may be required to fulfill DigitalTwin business related services.</li>
 * </ol>
 *
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Spring Boot 3.5</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>ServiceLayer</td></tr>
 *          <tr><td>architecture style</td><td>Layered</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 *		<ul>
 * 		<li>QueryGateway</li>
 * 		<li>CommandGateway</li>
 *		<li>QueryUpdateEmitter</li>
 *		</ul>
 *
 * <h3>Produces</h3>
 *
 *      <p>application/json</p>
 *
 * <p>@author Harbormaster Dev Team</p>
 *
 */
@Service
public class DigitalTwinService 
		extends BaseService {
//************************************************************************
// Public Methods
//************************************************************************
	/**
	 * Default Constructor
	 */
    public DigitalTwinService(CurrentIdentity identity,
				ApplicationContext applicationContext)  {

		this.identity		= identity;
		this.projector 		= new DigitalTwinEntityProjector( applicationContext.getBean(ProjectorRegistry.class),
										applicationContext.getBean(DigitalTwinRepository.class) );
		this.validator		= applicationContext.getBean(DigitalTwinValidator.class) ;
	}


	/**
	 * Creates the provided command.
	 *
	 * @param		command ${class.getCreateCommandAlias()}
	 * @return		DigitalTwin
	 */
		public DigitalTwin createDigitalTwin( CreateDigitalTwinCommand command ) {

		DigitalTwin entity = new DigitalTwin();

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

            entity.setDigitalTwinId( command.getDigitalTwinId() );
            entity.setTwinId( command.getTwinId() );
            entity.setDesiredStateVersion( command.getDesiredStateVersion() );
            entity.setReportedStateVersion( command.getReportedStateVersion() );
            entity.setLastSyncAt( command.getLastSyncAt() );

		// ------------------------------------------
		// persist a new one
		// ------------------------------------------
		entity = projector.create(entity);

		LOGGER.info( "done creating of DigitalTwin {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Update the provided command.
	 * @param		command UpdateDigitalTwinCommand
	 * @return		DigitalTwin
	 */
	public DigitalTwin updateDigitalTwin( UpdateDigitalTwinCommand command ) {

		DigitalTwin entity = new DigitalTwin();

		// --------------------------------------
		// validate
		// --------------------------------------
		validator.validate( command );

            entity.setDigitalTwinId( command.getDigitalTwinId() );
            entity.setTwinId( command.getTwinId() );
            entity.setDesiredStateVersion( command.getDesiredStateVersion() );
            entity.setReportedStateVersion( command.getReportedStateVersion() );
            entity.setLastSyncAt( command.getLastSyncAt() );
            entity.setDevice( command.getDevice() );
            entity.setGateway( command.getGateway() );
            entity.setTemplate( command.getTemplate() );
            entity.setChangeEvents( command.getChangeEvents() );

		// ------------------------------------------
		// persist an existing one
		// ------------------------------------------
		entity = projector.update(entity);

		LOGGER.info( "done saving of DigitalTwin {0} ", entity.toString() );

		return entity;
	}

	/**
	 * Deletes the associatied value object
	 * @param		command DeleteDigitalTwinCommand
	 */
	public void delete( DeleteDigitalTwinCommand command ) {
		UUID id = null;

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		id = command.getDigitalTwinId();

		// ------------------------------------------
		// delete the entity
		// ------------------------------------------
		projector.delete(id);

		LOGGER.info( "done deleting of DigitalTwin {0} ", id );

	}

	/**
	 * Method to retrieve the DigitalTwin via DigitalTwinFetchOneSummary
	 * @param 	summary DigitalTwinFetchOneSummary
	 * @return 	DigitalTwinFetchOneResponse
	 */
	public DigitalTwin getDigitalTwin( DigitalTwinFetchOneSummary summary ) {

		if( summary == null )
			throw new IllegalStateException( "DigitalTwinFetchOneSummary arg cannot be null" );

		DigitalTwin entity = null;
		UUID id = summary.getDigitalTwinId();

		// --------------------------------------
		// validate the fetch one summary
		// --------------------------------------
		validator.validate( summary );

		// --------------------------------------
		// find a DigitalTwin using the provided id
		// --------------------------------------
		entity = projector.find( id );

		return entity;
	}


	/**
	 * Method to retrieve a collection of all DigitalTwins
	 *
	 * @return 	List<DigitalTwin>
	 */
    public List<DigitalTwin> getAllDigitalTwin() {
		List<DigitalTwin> list = projector.findAll( new FindAllDigitalTwinQuery() );
		return list;
	}

	/**
	 * assign Device on DigitalTwin
	 * @param		command AssignDeviceToDigitalTwinCommand
	 */
	public void assignDevice( AssignDeviceToDigitalTwinCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignDevice(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Device on DigitalTwin
	 * @param		command UnAssignDeviceFromDigitalTwinCommand
	 */
	public void unAssignDevice( UnAssignDeviceFromDigitalTwinCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignDevice(command.getChildId());
	}
	
	/**
	 * assign Gateway on DigitalTwin
	 * @param		command AssignGatewayToDigitalTwinCommand
	 */
	public void assignGateway( AssignGatewayToDigitalTwinCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignGateway(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Gateway on DigitalTwin
	 * @param		command UnAssignGatewayFromDigitalTwinCommand
	 */
	public void unAssignGateway( UnAssignGatewayFromDigitalTwinCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignGateway(command.getChildId());
	}
	
	/**
	 * assign Template on DigitalTwin
	 * @param		command AssignTemplateToDigitalTwinCommand
	 */
	public void assignTemplate( AssignTemplateToDigitalTwinCommand command ) {
		// --------------------------------------
		// best to validate the command now
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.assignTemplate(command.getChildId(), command.getAssignment());

	}

	/**
	 * unAssign Template on DigitalTwin
	 * @param		command UnAssignTemplateFromDigitalTwinCommand
	 */
	public void unAssignTemplate( UnAssignTemplateFromDigitalTwinCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.unAssignTemplate(command.getChildId());
	}
	

	/**
	 * add TwinChangeEvent to ChangeEvents
	 * @param		command AssignChangeEventsToDigitalTwinCommand
	 */
	public void addToChangeEvents( AssignChangeEventsToDigitalTwinCommand command ) {

		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.addToChangeEvents(command.getParentId(), command.getChildIds());
	}

	/**
	 * remove TwinChangeEvent from ChangeEvents
	 * @param		command RemoveChangeEventsFromDigitalTwinCommand
	 */
	public void removeFromChangeEvents( RemoveChangeEventsFromDigitalTwinCommand command )  {
		// --------------------------------------
		// validate the command
		// --------------------------------------
		validator.validate( command );

		// --------------------------------------
		// delegate to the projector
		// --------------------------------------
		projector.removeFromChangeEvents(command.getParentId(), command.getChildIds());
	}




//************************************************************************
// Attributes
//************************************************************************
	@Autowired
    private final DigitalTwinEntityProjector projector;
	private final DigitalTwinValidator validator;
	private DigitalTwin digitalTwin 	= null;
	private CurrentIdentity identity			= null;
	private static final Logger LOGGER 			=  LoggerFactory.getLogger(DigitalTwinService.class);
}
