
package com.harbormaster.aggregate;
import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;

import java.util.*;
import java.util.logging.Level;
import java.util.logging.Logger;

import org.axonframework.commandhandling.CommandHandler;
import org.axonframework.modelling.command.AggregateMember;
import org.axonframework.eventsourcing.EventSourcingHandler;
import org.axonframework.modelling.command.AggregateIdentifier;
import static org.axonframework.modelling.command.AggregateLifecycle.apply;
import org.axonframework.spring.stereotype.Aggregate;
import org.springframework.context.annotation.Profile;

/**
 * <h2>TwinChangeEventAggregate</h2>
 * <p>
 * The TwinChangeEvent aggregate is the authoritative consistency boundary for all
 * branch-related operations. It validates incoming commands, enforces
 * business rules, maintains the integrity of the TwinChangeEvent domain, and emits
 * immutable domain events representing state changes.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Commands Accepted</h3>
 *      <ul>
 *      <li>CreateTwinChangeEventCommand</li>
 *      <li>UpdateTwinChangeEventCommand</li>
 *      <li>DeleteTwinChangeEventCommand</li>
 *      </ul>
 *
 * <h3>Associations</h3>
 * <ul>
 *       <li>AssignTwinToTwinChangeEventCommand</li>
 *       <li>UnAssignTwinFromTwinChangeEventCommand</li>
 *  </ul>
 *  <ul>
 *
 *
 * <h3>Events Emitted</h3>
 *      <ul>
 *      <li>CreateTwinChangeEventEvent</li>
 *      <li>UpdateTwinChangeEventEvent</li>
 *      <li>DeleteTwinChangeEventEvent</li>
   *       <li>AssignTwinToTwinChangeEventEvent</li>
 *       <li>UnAssignTwinFromTwinChangeEventEvent</li>
   *        </ul>
 *
 * <p>@author Harbormaster Dev Team</p>
 */
@Aggregate
public class TwinChangeEventAggregate {  

	// -----------------------------------------
	// Axon requires an empty constructor
    // -----------------------------------------
    public TwinChangeEventAggregate() {
    }

	// ----------------------------------------------
	// intrinsic command handlers
	// ----------------------------------------------
    @CommandHandler
    public TwinChangeEventAggregate(CreateTwinChangeEventCommand command) throws Exception {
    	LOGGER.info( "Handling command CreateTwinChangeEventCommand" );
    	CreateTwinChangeEventEvent event = new CreateTwinChangeEventEvent(command.getTwinChangeEventId(), command.getEventId(), command.getOccurredAt(), command.getChangeType());
    	
        apply(event);
    }

    @CommandHandler
    public void handle(UpdateTwinChangeEventCommand command) throws Exception {
    	LOGGER.info( "handling command UpdateTwinChangeEventCommand" );
    	UpdateTwinChangeEventEvent event = new UpdateTwinChangeEventEvent(command.getTwinChangeEventId(), command.getEventId(), command.getOccurredAt(), command.getTwin(), command.getChangeType());        
    	
        apply(event);
    }

    @CommandHandler
    public void handle(DeleteTwinChangeEventCommand command) throws Exception {
    	LOGGER.info( "Handling command DeleteTwinChangeEventCommand" );
        apply(new DeleteTwinChangeEventEvent(command.getTwinChangeEventId()));
    }

	// ----------------------------------------------
	// association command handlers
	// ----------------------------------------------

    // single association commands
    @CommandHandler
    public void handle(AssignTwinToTwinChangeEventCommand command) throws Exception {
    	LOGGER.info( "Handling command AssignTwinToTwinChangeEventCommand" );
    	
    	if (  twin != null && twin.getDigitalTwinId() == command.getAssignment().getDigitalTwinId() )
    		throw new ProcessingException( "Twin already assigned with id " + command.getAssignment().getDigitalTwinId() );  
    		
        apply(new AssignTwinToTwinChangeEventEvent(command.getTwinChangeEventId(), command.getAssignment()));
    }

    @CommandHandler
    public void handle(UnAssignTwinFromTwinChangeEventCommand command) throws Exception {
    	LOGGER.info( "Handling command UnAssignTwinFromTwinChangeEventCommand" );

    	if (  twin == null )
    		throw new ProcessingException( "Twin already has nothing assigned." );  

    	apply(new UnAssignTwinFromTwinChangeEventEvent(command.getTwinChangeEventId()));
    }

    // multiple association commands

	// ----------------------------------------------
	// intrinsic event source handlers
	// ----------------------------------------------
    @EventSourcingHandler
    void on(CreateTwinChangeEventEvent event) {	
    	LOGGER.info( "Event sourcing CreateTwinChangeEventEvent" );
    	this.twinChangeEventId = event.getTwinChangeEventId();
        this.eventId = event.getEventId();
        this.occurredAt = event.getOccurredAt();
        this.changeType = event.getChangeType();
    }
    
    @EventSourcingHandler
    void on(UpdateTwinChangeEventEvent event) {
    	LOGGER.info( "Event sourcing classObject.getUpdateEventAlias()}" );
        this.eventId = event.getEventId();
        this.occurredAt = event.getOccurredAt();
        this.twin = event.getTwin();
        this.changeType = event.getChangeType();
    }   

	// ----------------------------------------------
	// association event source handlers
	// ----------------------------------------------
	// single associations
    @EventSourcingHandler
    void on(AssignTwinToTwinChangeEventEvent event ) {	
    	LOGGER.info( "Event sourcing AssignTwinToTwinChangeEventEvent" );
    	this.twin = event.getAssignment();
    }

	@EventSourcingHandler
	void on(UnAssignTwinFromTwinChangeEventEvent event ) {	
		LOGGER.info( "Event sourcing UnAssignTwinFromTwinChangeEventEvent" );
		this.twin = null;
	}


    // ------------------------------------------
    // attributes
    // ------------------------------------------
	
    @AggregateIdentifier
    private UUID twinChangeEventId;
    
    private String eventId;
    private  Date occurredAt;
    private TwinChangeType changeType;
    private DigitalTwin twin = null;

    private static final Logger LOGGER 	= Logger.getLogger(TwinChangeEventAggregate.class.getName());
}
