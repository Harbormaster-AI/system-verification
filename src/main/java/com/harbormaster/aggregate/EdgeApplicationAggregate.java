
package com.harbormaster.aggregate;
import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;

import java.util.*;
import java.util.logging.Level;
import java.util.logging.Logger;

import org.axonframework.commandhandling.CommandHandler;
import org.axonframework.modelling.command.AggregateMember;
import org.axonframework.eventsourcing.EventSourcingHandler;
import org.axonframework.modelling.command.AggregateIdentifier;
import static org.axonframework.modelling.command.AggregateLifecycle.apply;
import org.axonframework.spring.stereotype.Aggregate;
import org.springframework.context.annotation.Profile;

/**
 * <h2>EdgeApplicationAggregate</h2>
 * <p>
 * The EdgeApplication aggregate is the authoritative consistency boundary for all
 * branch-related operations. It validates incoming commands, enforces
 * business rules, maintains the integrity of the EdgeApplication domain, and emits
 * immutable domain events representing state changes.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Commands Accepted</h3>
 *      <ul>
 *      <li>CreateEdgeApplicationCommand</li>
 *      <li>UpdateEdgeApplicationCommand</li>
 *      <li>DeleteEdgeApplicationCommand</li>
 *      </ul>
 *
 * <h3>Associations</h3>
 * <ul>
 *       <li>AssignGatewayToEdgeApplicationCommand</li>
 *       <li>UnAssignGatewayFromEdgeApplicationCommand</li>
 *  </ul>
 *  <ul>
 *
 *
 * <h3>Events Emitted</h3>
 *      <ul>
 *      <li>CreateEdgeApplicationEvent</li>
 *      <li>UpdateEdgeApplicationEvent</li>
 *      <li>DeleteEdgeApplicationEvent</li>
   *       <li>AssignGatewayToEdgeApplicationEvent</li>
 *       <li>UnAssignGatewayFromEdgeApplicationEvent</li>
   *        </ul>
 *
 * <p>@author Harbormaster Dev Team</p>
 */
@Aggregate
public class EdgeApplicationAggregate {  

	// -----------------------------------------
	// Axon requires an empty constructor
    // -----------------------------------------
    public EdgeApplicationAggregate() {
    }

	// ----------------------------------------------
	// intrinsic command handlers
	// ----------------------------------------------
    @CommandHandler
    public EdgeApplicationAggregate(CreateEdgeApplicationCommand command) throws Exception {
    	LOGGER.info( "Handling command CreateEdgeApplicationCommand" );
    	CreateEdgeApplicationEvent event = new CreateEdgeApplicationEvent(command.getEdgeApplicationId(), command.getName(), command.getVersion(), command.getImage(), command.getStatus());
    	
        apply(event);
    }

    @CommandHandler
    public void handle(UpdateEdgeApplicationCommand command) throws Exception {
    	LOGGER.info( "handling command UpdateEdgeApplicationCommand" );
    	UpdateEdgeApplicationEvent event = new UpdateEdgeApplicationEvent(command.getEdgeApplicationId(), command.getName(), command.getVersion(), command.getImage(), command.getGateway(), command.getStatus());        
    	
        apply(event);
    }

    @CommandHandler
    public void handle(DeleteEdgeApplicationCommand command) throws Exception {
    	LOGGER.info( "Handling command DeleteEdgeApplicationCommand" );
        apply(new DeleteEdgeApplicationEvent(command.getEdgeApplicationId()));
    }

	// ----------------------------------------------
	// association command handlers
	// ----------------------------------------------

    // single association commands
    @CommandHandler
    public void handle(AssignGatewayToEdgeApplicationCommand command) throws Exception {
    	LOGGER.info( "Handling command AssignGatewayToEdgeApplicationCommand" );
    	
    	if (  gateway != null && gateway.getGatewayId() == command.getAssignment().getGatewayId() )
    		throw new ProcessingException( "Gateway already assigned with id " + command.getAssignment().getGatewayId() );  
    		
        apply(new AssignGatewayToEdgeApplicationEvent(command.getEdgeApplicationId(), command.getAssignment()));
    }

    @CommandHandler
    public void handle(UnAssignGatewayFromEdgeApplicationCommand command) throws Exception {
    	LOGGER.info( "Handling command UnAssignGatewayFromEdgeApplicationCommand" );

    	if (  gateway == null )
    		throw new ProcessingException( "Gateway already has nothing assigned." );  

    	apply(new UnAssignGatewayFromEdgeApplicationEvent(command.getEdgeApplicationId()));
    }

    // multiple association commands

	// ----------------------------------------------
	// intrinsic event source handlers
	// ----------------------------------------------
    @EventSourcingHandler
    void on(CreateEdgeApplicationEvent event) {	
    	LOGGER.info( "Event sourcing CreateEdgeApplicationEvent" );
    	this.edgeApplicationId = event.getEdgeApplicationId();
        this.name = event.getName();
        this.version = event.getVersion();
        this.image = event.getImage();
        this.status = event.getStatus();
    }
    
    @EventSourcingHandler
    void on(UpdateEdgeApplicationEvent event) {
    	LOGGER.info( "Event sourcing classObject.getUpdateEventAlias()}" );
        this.name = event.getName();
        this.version = event.getVersion();
        this.image = event.getImage();
        this.gateway = event.getGateway();
        this.status = event.getStatus();
    }   

	// ----------------------------------------------
	// association event source handlers
	// ----------------------------------------------
	// single associations
    @EventSourcingHandler
    void on(AssignGatewayToEdgeApplicationEvent event ) {	
    	LOGGER.info( "Event sourcing AssignGatewayToEdgeApplicationEvent" );
    	this.gateway = event.getAssignment();
    }

	@EventSourcingHandler
	void on(UnAssignGatewayFromEdgeApplicationEvent event ) {	
		LOGGER.info( "Event sourcing UnAssignGatewayFromEdgeApplicationEvent" );
		this.gateway = null;
	}


    // ------------------------------------------
    // attributes
    // ------------------------------------------
	
    @AggregateIdentifier
    private UUID edgeApplicationId;
    
    private String name;
    private String version;
    private String image;
    private DeploymentStatus status;
    private Gateway gateway = null;

    private static final Logger LOGGER 	= Logger.getLogger(EdgeApplicationAggregate.class.getName());
}
