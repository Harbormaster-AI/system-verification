
package com.harbormaster.aggregate;
import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;

import java.util.*;
import java.util.logging.Level;
import java.util.logging.Logger;

import org.axonframework.commandhandling.CommandHandler;
import org.axonframework.modelling.command.AggregateMember;
import org.axonframework.eventsourcing.EventSourcingHandler;
import org.axonframework.modelling.command.AggregateIdentifier;
import static org.axonframework.modelling.command.AggregateLifecycle.apply;
import org.axonframework.spring.stereotype.Aggregate;
import org.springframework.context.annotation.Profile;

/**
 * <h2>ApiKeyAggregate</h2>
 * <p>
 * The ApiKey aggregate is the authoritative consistency boundary for all
 * branch-related operations. It validates incoming commands, enforces
 * business rules, maintains the integrity of the ApiKey domain, and emits
 * immutable domain events representing state changes.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Commands Accepted</h3>
 *      <ul>
 *      <li>CreateApiKeyCommand</li>
 *      <li>UpdateApiKeyCommand</li>
 *      <li>DeleteApiKeyCommand</li>
 *      </ul>
 *
 * <h3>Associations</h3>
 * <ul>
 *       <li>AssignAccessPolicyToApiKeyCommand</li>
 *       <li>UnAssignAccessPolicyFromApiKeyCommand</li>
 *  </ul>
 *  <ul>
 *
 *
 * <h3>Events Emitted</h3>
 *      <ul>
 *      <li>CreateApiKeyEvent</li>
 *      <li>UpdateApiKeyEvent</li>
 *      <li>DeleteApiKeyEvent</li>
   *       <li>AssignAccessPolicyToApiKeyEvent</li>
 *       <li>UnAssignAccessPolicyFromApiKeyEvent</li>
   *        </ul>
 *
 * <p>@author Harbormaster Dev Team</p>
 */
@Aggregate
public class ApiKeyAggregate {  

	// -----------------------------------------
	// Axon requires an empty constructor
    // -----------------------------------------
    public ApiKeyAggregate() {
    }

	// ----------------------------------------------
	// intrinsic command handlers
	// ----------------------------------------------
    @CommandHandler
    public ApiKeyAggregate(CreateApiKeyCommand command) throws Exception {
    	LOGGER.info( "Handling command CreateApiKeyCommand" );
    	CreateApiKeyEvent event = new CreateApiKeyEvent(command.getApiKeyId(), command.getKeyId(), command.getHashedSecret(), command.getCreatedAt(), command.getLastUsedAt());
    	
        apply(event);
    }

    @CommandHandler
    public void handle(UpdateApiKeyCommand command) throws Exception {
    	LOGGER.info( "handling command UpdateApiKeyCommand" );
    	UpdateApiKeyEvent event = new UpdateApiKeyEvent(command.getApiKeyId(), command.getKeyId(), command.getHashedSecret(), command.getCreatedAt(), command.getLastUsedAt(), command.getAccessPolicy());        
    	
        apply(event);
    }

    @CommandHandler
    public void handle(DeleteApiKeyCommand command) throws Exception {
    	LOGGER.info( "Handling command DeleteApiKeyCommand" );
        apply(new DeleteApiKeyEvent(command.getApiKeyId()));
    }

	// ----------------------------------------------
	// association command handlers
	// ----------------------------------------------

    // single association commands
    @CommandHandler
    public void handle(AssignAccessPolicyToApiKeyCommand command) throws Exception {
    	LOGGER.info( "Handling command AssignAccessPolicyToApiKeyCommand" );
    	
    	if (  accessPolicy != null && accessPolicy.getAccessPolicyId() == command.getAssignment().getAccessPolicyId() )
    		throw new ProcessingException( "AccessPolicy already assigned with id " + command.getAssignment().getAccessPolicyId() );  
    		
        apply(new AssignAccessPolicyToApiKeyEvent(command.getApiKeyId(), command.getAssignment()));
    }

    @CommandHandler
    public void handle(UnAssignAccessPolicyFromApiKeyCommand command) throws Exception {
    	LOGGER.info( "Handling command UnAssignAccessPolicyFromApiKeyCommand" );

    	if (  accessPolicy == null )
    		throw new ProcessingException( "AccessPolicy already has nothing assigned." );  

    	apply(new UnAssignAccessPolicyFromApiKeyEvent(command.getApiKeyId()));
    }

    // multiple association commands

	// ----------------------------------------------
	// intrinsic event source handlers
	// ----------------------------------------------
    @EventSourcingHandler
    void on(CreateApiKeyEvent event) {	
    	LOGGER.info( "Event sourcing CreateApiKeyEvent" );
    	this.apiKeyId = event.getApiKeyId();
        this.keyId = event.getKeyId();
        this.hashedSecret = event.getHashedSecret();
        this.createdAt = event.getCreatedAt();
        this.lastUsedAt = event.getLastUsedAt();
    }
    
    @EventSourcingHandler
    void on(UpdateApiKeyEvent event) {
    	LOGGER.info( "Event sourcing classObject.getUpdateEventAlias()}" );
        this.keyId = event.getKeyId();
        this.hashedSecret = event.getHashedSecret();
        this.createdAt = event.getCreatedAt();
        this.lastUsedAt = event.getLastUsedAt();
        this.accessPolicy = event.getAccessPolicy();
    }   

	// ----------------------------------------------
	// association event source handlers
	// ----------------------------------------------
	// single associations
    @EventSourcingHandler
    void on(AssignAccessPolicyToApiKeyEvent event ) {	
    	LOGGER.info( "Event sourcing AssignAccessPolicyToApiKeyEvent" );
    	this.accessPolicy = event.getAssignment();
    }

	@EventSourcingHandler
	void on(UnAssignAccessPolicyFromApiKeyEvent event ) {	
		LOGGER.info( "Event sourcing UnAssignAccessPolicyFromApiKeyEvent" );
		this.accessPolicy = null;
	}


    // ------------------------------------------
    // attributes
    // ------------------------------------------
	
    @AggregateIdentifier
    private UUID apiKeyId;
    
    private String keyId;
    private String hashedSecret;
    private  Date createdAt;
    private  Date lastUsedAt;
    private AccessPolicy accessPolicy = null;

    private static final Logger LOGGER 	= Logger.getLogger(ApiKeyAggregate.class.getName());
}
