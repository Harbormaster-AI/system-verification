
package com.harbormaster.aggregate;
import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;

import java.util.*;
import java.util.logging.Level;
import java.util.logging.Logger;

import org.axonframework.commandhandling.CommandHandler;
import org.axonframework.modelling.command.AggregateMember;
import org.axonframework.eventsourcing.EventSourcingHandler;
import org.axonframework.modelling.command.AggregateIdentifier;
import static org.axonframework.modelling.command.AggregateLifecycle.apply;
import org.axonframework.spring.stereotype.Aggregate;
import org.springframework.context.annotation.Profile;

/**
 * <h2>TelemetrySchemaAggregate</h2>
 * <p>
 * The TelemetrySchema aggregate is the authoritative consistency boundary for all
 * branch-related operations. It validates incoming commands, enforces
 * business rules, maintains the integrity of the TelemetrySchema domain, and emits
 * immutable domain events representing state changes.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Commands Accepted</h3>
 *      <ul>
 *      <li>CreateTelemetrySchemaCommand</li>
 *      <li>UpdateTelemetrySchemaCommand</li>
 *      <li>DeleteTelemetrySchemaCommand</li>
 *      </ul>
 *
 * <h3>Associations</h3>
 * <ul>
 *  </ul>
 *  <ul>
 *       <li>AssignStreamsToTelemetrySchemaCommand</li>
 *       <li>RemoveStreamsFromTelemetrySchemaCommand</li>
 *   </ul>
 *
 * <p>
 * @author Harbormaster Dev Team
 * </p>
 *
 *
 * <h3>Events Emitted</h3>
 *      <ul>
 *      <li>CreateTelemetrySchemaEvent</li>
 *      <li>UpdateTelemetrySchemaEvent</li>
 *      <li>DeleteTelemetrySchemaEvent</li>
    *       <li>AssignStreamsToTelemetrySchemaEvent</li>
 *       <li>RemoveStreamsFromTelemetrySchemaEvent</li>
  *        </ul>
 *
 * <p>@author Harbormaster Dev Team</p>
 */
@Aggregate
public class TelemetrySchemaAggregate {  

	// -----------------------------------------
	// Axon requires an empty constructor
    // -----------------------------------------
    public TelemetrySchemaAggregate() {
    }

	// ----------------------------------------------
	// intrinsic command handlers
	// ----------------------------------------------
    @CommandHandler
    public TelemetrySchemaAggregate(CreateTelemetrySchemaCommand command) throws Exception {
    	LOGGER.info( "Handling command CreateTelemetrySchemaCommand" );
    	CreateTelemetrySchemaEvent event = new CreateTelemetrySchemaEvent(command.getTelemetrySchemaId(), command.getSchemaId(), command.getSchemaUri(), command.getEncoding());
    	
        apply(event);
    }

    @CommandHandler
    public void handle(UpdateTelemetrySchemaCommand command) throws Exception {
    	LOGGER.info( "handling command UpdateTelemetrySchemaCommand" );
    	UpdateTelemetrySchemaEvent event = new UpdateTelemetrySchemaEvent(command.getTelemetrySchemaId(), command.getSchemaId(), command.getSchemaUri(), command.getStreams(), command.getEncoding());        
    	
        apply(event);
    }

    @CommandHandler
    public void handle(DeleteTelemetrySchemaCommand command) throws Exception {
    	LOGGER.info( "Handling command DeleteTelemetrySchemaCommand" );
        apply(new DeleteTelemetrySchemaEvent(command.getTelemetrySchemaId()));
    }

	// ----------------------------------------------
	// association command handlers
	// ----------------------------------------------

    // single association commands

    // multiple association commands
    @CommandHandler
    public void handle(AssignStreamsToTelemetrySchemaCommand command) throws Exception {
    	LOGGER.info( "Handling command AssignStreamsToTelemetrySchemaCommand" );
    	
    	if ( streams.contains( command.getAddTo() ) )
    		throw new ProcessingException( "Streams already contains an entity with id " + command.getAddTo().getTelemetryStreamId() );

    	apply(new AssignStreamsToTelemetrySchemaEvent(command.getTelemetrySchemaId(), command.getAddTo()));
    }

    @CommandHandler
    public void handle(RemoveStreamsFromTelemetrySchemaCommand command) throws Exception {
    	LOGGER.info( "Handling command RemoveStreamsFromTelemetrySchemaCommand" );
    	
    	if ( !streams.contains( command.getRemoveFrom() ) )
    		throw new ProcessingException( "Streams does not contain an entity with id " + command.getRemoveFrom().getTelemetryStreamId() );

        apply(new RemoveStreamsFromTelemetrySchemaEvent(command.getTelemetrySchemaId(), command.getRemoveFrom()));
    }

	// ----------------------------------------------
	// intrinsic event source handlers
	// ----------------------------------------------
    @EventSourcingHandler
    void on(CreateTelemetrySchemaEvent event) {	
    	LOGGER.info( "Event sourcing CreateTelemetrySchemaEvent" );
    	this.telemetrySchemaId = event.getTelemetrySchemaId();
        this.schemaId = event.getSchemaId();
        this.schemaUri = event.getSchemaUri();
        this.encoding = event.getEncoding();
    }
    
    @EventSourcingHandler
    void on(UpdateTelemetrySchemaEvent event) {
    	LOGGER.info( "Event sourcing classObject.getUpdateEventAlias()}" );
        this.schemaId = event.getSchemaId();
        this.schemaUri = event.getSchemaUri();
        this.streams = event.getStreams();
        this.encoding = event.getEncoding();
    }   

	// ----------------------------------------------
	// association event source handlers
	// ----------------------------------------------

	// multiple associations
    @EventSourcingHandler
    void on(AssignStreamsToTelemetrySchemaEvent event ) {
    	LOGGER.info( "Event sourcing AssignStreamsToTelemetrySchemaEvent" );
    	this.streams.add( event.getAddTo() );
    }

	@EventSourcingHandler
	void on(RemoveStreamsFromTelemetrySchemaEvent event ) {	
		LOGGER.info( "Event sourcing RemoveStreamsFromTelemetrySchemaEvent" );
		this.streams.remove( event.getRemoveFrom() );
	}
	

    // ------------------------------------------
    // attributes
    // ------------------------------------------
	
    @AggregateIdentifier
    private UUID telemetrySchemaId;
    
    private String schemaId;
    private Uri schemaUri;
    private TelemetryEncoding encoding;
    private Set<TelemetryStream> streams = new HashSet<>();

    private static final Logger LOGGER 	= Logger.getLogger(TelemetrySchemaAggregate.class.getName());
}
