
package com.harbormaster.aggregate;
import com.harbormaster.api.*;
import com.harbormaster.entity.*;
import com.harbormaster.exception.*;

import java.util.*;
import java.util.logging.Level;
import java.util.logging.Logger;

import org.axonframework.commandhandling.CommandHandler;
import org.axonframework.modelling.command.AggregateMember;
import org.axonframework.eventsourcing.EventSourcingHandler;
import org.axonframework.modelling.command.AggregateIdentifier;
import static org.axonframework.modelling.command.AggregateLifecycle.apply;
import org.axonframework.spring.stereotype.Aggregate;
import org.springframework.context.annotation.Profile;

/**
 * <h2>FirmwareReleaseAggregate</h2>
 * <p>
 * The FirmwareRelease aggregate is the authoritative consistency boundary for all
 * branch-related operations. It validates incoming commands, enforces
 * business rules, maintains the integrity of the FirmwareRelease domain, and emits
 * immutable domain events representing state changes.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Commands Accepted</h3>
 *      <ul>
 *      <li>CreateFirmwareReleaseCommand</li>
 *      <li>UpdateFirmwareReleaseCommand</li>
 *      <li>DeleteFirmwareReleaseCommand</li>
 *      </ul>
 *
 * <h3>Associations</h3>
 * <ul>
 *       <li>AssignDeviceModelToFirmwareReleaseCommand</li>
 *       <li>UnAssignDeviceModelFromFirmwareReleaseCommand</li>
 *  </ul>
 *  <ul>
 *
 *
 * <h3>Events Emitted</h3>
 *      <ul>
 *      <li>CreateFirmwareReleaseEvent</li>
 *      <li>UpdateFirmwareReleaseEvent</li>
 *      <li>DeleteFirmwareReleaseEvent</li>
   *       <li>AssignDeviceModelToFirmwareReleaseEvent</li>
 *       <li>UnAssignDeviceModelFromFirmwareReleaseEvent</li>
   *        </ul>
 *
 * <p>@author Harbormaster Dev Team</p>
 */
@Aggregate
public class FirmwareReleaseAggregate {  

	// -----------------------------------------
	// Axon requires an empty constructor
    // -----------------------------------------
    public FirmwareReleaseAggregate() {
    }

	// ----------------------------------------------
	// intrinsic command handlers
	// ----------------------------------------------
    @CommandHandler
    public FirmwareReleaseAggregate(CreateFirmwareReleaseCommand command) throws Exception {
    	LOGGER.info( "Handling command CreateFirmwareReleaseCommand" );
    	CreateFirmwareReleaseEvent event = new CreateFirmwareReleaseEvent(command.getFirmwareReleaseId(), command.getVersion(), command.getReleaseDate(), command.getReleaseNotes(), command.getChecksum());
    	
        apply(event);
    }

    @CommandHandler
    public void handle(UpdateFirmwareReleaseCommand command) throws Exception {
    	LOGGER.info( "handling command UpdateFirmwareReleaseCommand" );
    	UpdateFirmwareReleaseEvent event = new UpdateFirmwareReleaseEvent(command.getFirmwareReleaseId(), command.getVersion(), command.getReleaseDate(), command.getReleaseNotes(), command.getChecksum(), command.getDeviceModel());        
    	
        apply(event);
    }

    @CommandHandler
    public void handle(DeleteFirmwareReleaseCommand command) throws Exception {
    	LOGGER.info( "Handling command DeleteFirmwareReleaseCommand" );
        apply(new DeleteFirmwareReleaseEvent(command.getFirmwareReleaseId()));
    }

	// ----------------------------------------------
	// association command handlers
	// ----------------------------------------------

    // single association commands
    @CommandHandler
    public void handle(AssignDeviceModelToFirmwareReleaseCommand command) throws Exception {
    	LOGGER.info( "Handling command AssignDeviceModelToFirmwareReleaseCommand" );
    	
    	if (  deviceModel != null && deviceModel.getDeviceModelId() == command.getAssignment().getDeviceModelId() )
    		throw new ProcessingException( "DeviceModel already assigned with id " + command.getAssignment().getDeviceModelId() );  
    		
        apply(new AssignDeviceModelToFirmwareReleaseEvent(command.getFirmwareReleaseId(), command.getAssignment()));
    }

    @CommandHandler
    public void handle(UnAssignDeviceModelFromFirmwareReleaseCommand command) throws Exception {
    	LOGGER.info( "Handling command UnAssignDeviceModelFromFirmwareReleaseCommand" );

    	if (  deviceModel == null )
    		throw new ProcessingException( "DeviceModel already has nothing assigned." );  

    	apply(new UnAssignDeviceModelFromFirmwareReleaseEvent(command.getFirmwareReleaseId()));
    }

    // multiple association commands

	// ----------------------------------------------
	// intrinsic event source handlers
	// ----------------------------------------------
    @EventSourcingHandler
    void on(CreateFirmwareReleaseEvent event) {	
    	LOGGER.info( "Event sourcing CreateFirmwareReleaseEvent" );
    	this.firmwareReleaseId = event.getFirmwareReleaseId();
        this.version = event.getVersion();
        this.releaseDate = event.getReleaseDate();
        this.releaseNotes = event.getReleaseNotes();
        this.checksum = event.getChecksum();
    }
    
    @EventSourcingHandler
    void on(UpdateFirmwareReleaseEvent event) {
    	LOGGER.info( "Event sourcing classObject.getUpdateEventAlias()}" );
        this.version = event.getVersion();
        this.releaseDate = event.getReleaseDate();
        this.releaseNotes = event.getReleaseNotes();
        this.checksum = event.getChecksum();
        this.deviceModel = event.getDeviceModel();
    }   

	// ----------------------------------------------
	// association event source handlers
	// ----------------------------------------------
	// single associations
    @EventSourcingHandler
    void on(AssignDeviceModelToFirmwareReleaseEvent event ) {	
    	LOGGER.info( "Event sourcing AssignDeviceModelToFirmwareReleaseEvent" );
    	this.deviceModel = event.getAssignment();
    }

	@EventSourcingHandler
	void on(UnAssignDeviceModelFromFirmwareReleaseEvent event ) {	
		LOGGER.info( "Event sourcing UnAssignDeviceModelFromFirmwareReleaseEvent" );
		this.deviceModel = null;
	}


    // ------------------------------------------
    // attributes
    // ------------------------------------------
	
    @AggregateIdentifier
    private UUID firmwareReleaseId;
    
    private FirmwareVersion version;
    private  Date releaseDate;
    private String releaseNotes;
    private Checksum checksum;
    private DeviceModel deviceModel = null;

    private static final Logger LOGGER 	= Logger.getLogger(FirmwareReleaseAggregate.class.getName());
}
