/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.springframework.util.Assert.state;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;
import com.harbormaster.subscriber.*;

/**
 * <h2>ActuatorInstanceTest</h2>
 * <p>
 * Junit test class for entity ActuatorInstance.  Handle the full lifecycle as it relates to persistence. Leverages
 * the ActuatorInstanceService and a ActuatorInstanceSubscriber to test everything under a MVC framework which includes the
 * ActuatorInstanceAggregate and related commands, queries, and events.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 * 		ActuatorInstanceService
 * 	    ActuatorInstanceSubscriber
 *
 * <h3>Methods</h3>
 *      <ul>
 *      <li>read</li>
 *      <li>update</li>
 *      <li>delete</li>
 *      </ul>
 *
 * <p>
 * @author Harbormaster Dev Team
 * </p>
 */
public class ActuatorInstanceTest {

    // ------------------------------------
	// default constructor
    // ------------------------------------
	public ActuatorInstanceTest() {
		subscriber = new ActuatorInstanceSubscriber();
	}

	// test methods
	@Test
	/*
	 * Initiate ActuatorInstanceTest.
	 */
	public void startTest() throws Throwable {
		try {
			LOGGER.info("**********************************************************");
			LOGGER.info("Beginning test on ActuatorInstance...");
			LOGGER.info("**********************************************************\n");
			
			// ---------------------------------------------
			// jumpstart process
			// ---------------------------------------------
			jumpStart();
			
		} catch (Throwable e) {
			throw e;
		} finally {
		}
	}

	/** 
	 * jumpstart the process by instantiating2 ActuatorInstance
	 */
	protected void jumpStart() throws Throwable {
		LOGGER.info( "\n======== create instances to get the ball rolling  ======== ");

		actuatorInstanceId = ActuatorInstanceService.getActuatorInstanceInstance()
		.createActuatorInstance( generateNewCommand() )
		.get();

		// ---------------------------------------------
		// set up query subscriptions after the 1st create
		// ---------------------------------------------
		testingStep = "create";
		setUpQuerySubscriptions();

		ActuatorInstanceService.getActuatorInstanceInstance()
				.createActuatorInstance( generateNewCommand() )
				.get();

	}

	/** 
	 * Set up query subscriptions
	 */
	protected void setUpQuerySubscriptions() throws Throwable {
		LOGGER.info( "\n======== Setting Up Query Subscriptions ======== ");
			
		try {            
			subscriber.actuatorInstanceSubscribe().updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetAll update received for ActuatorInstance : " + successValue.getActuatorInstanceId());
							  if (successValue.getActuatorInstanceId().equals(actuatorInstanceId)) {
								  if (testingStep.equals("create")) {
									  testingStep = "update";
									  update();
								  } else if (testingStep.equals("delete")) {
									  testingStep = "complete";
									  state( getAll().size() == sizeOfActuatorInstanceList - 1 , "value not deleted from list");
									  LOGGER.info("**********************************************************");
									  LOGGER.info("ActuatorInstance test completed successfully...");
									  LOGGER.info("**********************************************************\n");
								  }
							  }
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on actuatorInstance consumed")
					);
			subscriber.actuatorInstanceSubscribe( actuatorInstanceId ).updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetOne update received for ActuatorInstance : " + successValue.getActuatorInstanceId() + " in step " + testingStep);
							  testingStep = "delete";
							  sizeOfActuatorInstanceList = getAll().size();
							  delete();
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on actuatorInstance for actuatorInstanceId consumed")

					);
			

			}
			catch (Exception e) {
				LOGGER.warning( e.getMessage() );
				throw e;
			}
		}
		
		/** 
	 * read a ActuatorInstance. 
	 */
	protected ActuatorInstance read() throws Throwable {
		LOGGER.info( "\n======== READ ======== ");
		LOGGER.info( "-- Reading a previously created ActuatorInstance" );

		ActuatorInstance entity = null;
		StringBuilder msg = new StringBuilder( "-- Failed to read ActuatorInstance with primary key" );
		msg.append( actuatorInstanceId );
		
		ActuatorInstanceFetchOneSummary fetchOneSummary = new ActuatorInstanceFetchOneSummary( actuatorInstanceId );

		try {
			entity = ActuatorInstanceService.getActuatorInstanceInstance().getActuatorInstance( fetchOneSummary );

			assertNotNull( entity,msg.toString() );

			LOGGER.info( "-- Successfully found ActuatorInstance " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return entity;
	}

	/** 
	 * updating a ActuatorInstance.
	 */
	protected void update() throws Throwable {
		LOGGER.info( "\n======== UPDATE ======== ");
		LOGGER.info( "-- Attempting to update a ActuatorInstance." );

		StringBuilder msg = new StringBuilder( "Failed to update a ActuatorInstance : " );        
		ActuatorInstance entity = read();
		UpdateActuatorInstanceCommand command = generateUpdateCommand();
		command.setActuatorInstanceId(entity.getActuatorInstanceId());

		try {            
			assertNotNull( entity, msg.toString() );

			LOGGER.info( "-- Now updating the created ActuatorInstance." );

			// for use later on...
			actuatorInstanceId = entity.getActuatorInstanceId();

			ActuatorInstanceService proxy = ActuatorInstanceService.getActuatorInstanceInstance();  

			proxy.updateActuatorInstance( command ).get();

			LOGGER.info( "-- Successfully saved ActuatorInstance - " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : primarykey = " + actuatorInstanceId + " : command -" +  command + " : " + e );

			throw e;
		}
	}

	/** 
	 * delete a ActuatorInstance.
	 */
	protected void delete() throws Throwable {
		LOGGER.info( "\n======== DELETE ======== ");
		LOGGER.info( "-- Deleting a previously created ActuatorInstance." );

		ActuatorInstance entity = null;
		
		try{
		    entity = read(); 
			LOGGER.info( "-- Successfully read ActuatorInstance with id " + actuatorInstanceId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to read ActuatorInstance with id " + actuatorInstanceId );

			throw e;
		}

		try{
			ActuatorInstanceService.getActuatorInstanceInstance().delete( new DeleteActuatorInstanceCommand( entity.getActuatorInstanceId() ) ).get();
			LOGGER.info( "-- Successfully deleted ActuatorInstance with id " + actuatorInstanceId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to delete ActuatorInstance with id " + actuatorInstanceId );

			throw e;
		}
	}

	/**
	 * get all ActuatorInstances.
	 */
	protected List<ActuatorInstance> getAll() throws Throwable {    
		LOGGER.info( "======== GETALL ======== ");
		LOGGER.info( "-- Retrieving Collection of ActuatorInstances:" );

		StringBuilder msg = new StringBuilder( "-- Failed to get all ActuatorInstance : " );        
		List<ActuatorInstance> collection  = new ArrayList<>();

		try {
			// call the static get method on the ActuatorInstanceService
			collection = ActuatorInstanceService.getActuatorInstanceInstance().getAllActuatorInstance();
			assertNotNull( collection, "An Empty collection of ActuatorInstance was incorrectly returned.");
			
			// Now print out the values
			ActuatorInstance entity = null;            
			Iterator<ActuatorInstance> iter = collection.iterator();
			int index = 1;

			while( iter.hasNext() ) {
				// Retrieve the entity   
				entity = iter.next();

				assertNotNull( entity,"-- null entity in Collection." );
				assertNotNull( entity.getActuatorInstanceId(), "-- entity in Collection has a null primary key" );        

				LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
				index++;
			}
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return collection;			
	}

	/**
	 * Assigns a common log handler for each test class in the suite 
	 * in the event log output needs to go elsewhere
	 * 
	 * @param		handler	Handler
	 * @return		ActuatorInstanceTest
	 */
	protected ActuatorInstanceTest setHandler(Handler handler) {
		if ( handler != null )
			LOGGER.addHandler(handler); 
		return this;
	}

	/**
	 * Returns a new populated ActuatorInstance
	 * 
	 * @return CreateActuatorInstanceCommand alias
	 */
	protected CreateActuatorInstanceCommand generateNewCommand() {
        CreateActuatorInstanceCommand command = new CreateActuatorInstanceCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new TopicName(),  ActuatorType.values()[0] );
		
		return( command );
	}

		/**
		 * Returns a new populated ActuatorInstance
		 * 
		 * @return UpdateActuatorInstanceCommand alias
		 */
	protected UpdateActuatorInstanceCommand generateUpdateCommand() {
	        UpdateActuatorInstanceCommand command = new UpdateActuatorInstanceCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new TopicName(),  null,  new HashSet<>(),  ActuatorType.values()[0] );
			
			return( command );
		}
	//-----------------------------------------------------
	// attributes 
	//-----------------------------------------------------
	protected UUID actuatorInstanceId = null;
	protected ActuatorInstanceSubscriber subscriber = null;
	private final String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
	private final Logger LOGGER = Logger.getLogger(ActuatorInstanceTest.class.getName());
	private String testingStep = "";
	private Integer sizeOfActuatorInstanceList = 0;
}
