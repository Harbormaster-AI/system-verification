/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.springframework.util.Assert.state;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;
import com.harbormaster.subscriber.*;

/**
 * <h2>ConnectivityPlanTest</h2>
 * <p>
 * Junit test class for entity ConnectivityPlan.  Handle the full lifecycle as it relates to persistence. Leverages
 * the ConnectivityPlanService and a ConnectivityPlanSubscriber to test everything under a MVC framework which includes the
 * ConnectivityPlanAggregate and related commands, queries, and events.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 * 		ConnectivityPlanService
 * 	    ConnectivityPlanSubscriber
 *
 * <h3>Methods</h3>
 *      <ul>
 *      <li>read</li>
 *      <li>update</li>
 *      <li>delete</li>
 *      </ul>
 *
 * <p>
 * @author Harbormaster Dev Team
 * </p>
 */
public class ConnectivityPlanTest {

    // ------------------------------------
	// default constructor
    // ------------------------------------
	public ConnectivityPlanTest() {
		subscriber = new ConnectivityPlanSubscriber();
	}

	// test methods
	@Test
	/*
	 * Initiate ConnectivityPlanTest.
	 */
	public void startTest() throws Throwable {
		try {
			LOGGER.info("**********************************************************");
			LOGGER.info("Beginning test on ConnectivityPlan...");
			LOGGER.info("**********************************************************\n");
			
			// ---------------------------------------------
			// jumpstart process
			// ---------------------------------------------
			jumpStart();
			
		} catch (Throwable e) {
			throw e;
		} finally {
		}
	}

	/** 
	 * jumpstart the process by instantiating2 ConnectivityPlan
	 */
	protected void jumpStart() throws Throwable {
		LOGGER.info( "\n======== create instances to get the ball rolling  ======== ");

		connectivityPlanId = ConnectivityPlanService.getConnectivityPlanInstance()
		.createConnectivityPlan( generateNewCommand() )
		.get();

		// ---------------------------------------------
		// set up query subscriptions after the 1st create
		// ---------------------------------------------
		testingStep = "create";
		setUpQuerySubscriptions();

		ConnectivityPlanService.getConnectivityPlanInstance()
				.createConnectivityPlan( generateNewCommand() )
				.get();

	}

	/** 
	 * Set up query subscriptions
	 */
	protected void setUpQuerySubscriptions() throws Throwable {
		LOGGER.info( "\n======== Setting Up Query Subscriptions ======== ");
			
		try {            
			subscriber.connectivityPlanSubscribe().updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetAll update received for ConnectivityPlan : " + successValue.getConnectivityPlanId());
							  if (successValue.getConnectivityPlanId().equals(connectivityPlanId)) {
								  if (testingStep.equals("create")) {
									  testingStep = "update";
									  update();
								  } else if (testingStep.equals("delete")) {
									  testingStep = "complete";
									  state( getAll().size() == sizeOfConnectivityPlanList - 1 , "value not deleted from list");
									  LOGGER.info("**********************************************************");
									  LOGGER.info("ConnectivityPlan test completed successfully...");
									  LOGGER.info("**********************************************************\n");
								  }
							  }
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on connectivityPlan consumed")
					);
			subscriber.connectivityPlanSubscribe( connectivityPlanId ).updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetOne update received for ConnectivityPlan : " + successValue.getConnectivityPlanId() + " in step " + testingStep);
							  testingStep = "delete";
							  sizeOfConnectivityPlanList = getAll().size();
							  delete();
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on connectivityPlan for connectivityPlanId consumed")

					);
			

			}
			catch (Exception e) {
				LOGGER.warning( e.getMessage() );
				throw e;
			}
		}
		
		/** 
	 * read a ConnectivityPlan. 
	 */
	protected ConnectivityPlan read() throws Throwable {
		LOGGER.info( "\n======== READ ======== ");
		LOGGER.info( "-- Reading a previously created ConnectivityPlan" );

		ConnectivityPlan entity = null;
		StringBuilder msg = new StringBuilder( "-- Failed to read ConnectivityPlan with primary key" );
		msg.append( connectivityPlanId );
		
		ConnectivityPlanFetchOneSummary fetchOneSummary = new ConnectivityPlanFetchOneSummary( connectivityPlanId );

		try {
			entity = ConnectivityPlanService.getConnectivityPlanInstance().getConnectivityPlan( fetchOneSummary );

			assertNotNull( entity,msg.toString() );

			LOGGER.info( "-- Successfully found ConnectivityPlan " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return entity;
	}

	/** 
	 * updating a ConnectivityPlan.
	 */
	protected void update() throws Throwable {
		LOGGER.info( "\n======== UPDATE ======== ");
		LOGGER.info( "-- Attempting to update a ConnectivityPlan." );

		StringBuilder msg = new StringBuilder( "Failed to update a ConnectivityPlan : " );        
		ConnectivityPlan entity = read();
		UpdateConnectivityPlanCommand command = generateUpdateCommand();
		command.setConnectivityPlanId(entity.getConnectivityPlanId());

		try {            
			assertNotNull( entity, msg.toString() );

			LOGGER.info( "-- Now updating the created ConnectivityPlan." );

			// for use later on...
			connectivityPlanId = entity.getConnectivityPlanId();

			ConnectivityPlanService proxy = ConnectivityPlanService.getConnectivityPlanInstance();  

			proxy.updateConnectivityPlan( command ).get();

			LOGGER.info( "-- Successfully saved ConnectivityPlan - " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : primarykey = " + connectivityPlanId + " : command -" +  command + " : " + e );

			throw e;
		}
	}

	/** 
	 * delete a ConnectivityPlan.
	 */
	protected void delete() throws Throwable {
		LOGGER.info( "\n======== DELETE ======== ");
		LOGGER.info( "-- Deleting a previously created ConnectivityPlan." );

		ConnectivityPlan entity = null;
		
		try{
		    entity = read(); 
			LOGGER.info( "-- Successfully read ConnectivityPlan with id " + connectivityPlanId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to read ConnectivityPlan with id " + connectivityPlanId );

			throw e;
		}

		try{
			ConnectivityPlanService.getConnectivityPlanInstance().delete( new DeleteConnectivityPlanCommand( entity.getConnectivityPlanId() ) ).get();
			LOGGER.info( "-- Successfully deleted ConnectivityPlan with id " + connectivityPlanId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to delete ConnectivityPlan with id " + connectivityPlanId );

			throw e;
		}
	}

	/**
	 * get all ConnectivityPlans.
	 */
	protected List<ConnectivityPlan> getAll() throws Throwable {    
		LOGGER.info( "======== GETALL ======== ");
		LOGGER.info( "-- Retrieving Collection of ConnectivityPlans:" );

		StringBuilder msg = new StringBuilder( "-- Failed to get all ConnectivityPlan : " );        
		List<ConnectivityPlan> collection  = new ArrayList<>();

		try {
			// call the static get method on the ConnectivityPlanService
			collection = ConnectivityPlanService.getConnectivityPlanInstance().getAllConnectivityPlan();
			assertNotNull( collection, "An Empty collection of ConnectivityPlan was incorrectly returned.");
			
			// Now print out the values
			ConnectivityPlan entity = null;            
			Iterator<ConnectivityPlan> iter = collection.iterator();
			int index = 1;

			while( iter.hasNext() ) {
				// Retrieve the entity   
				entity = iter.next();

				assertNotNull( entity,"-- null entity in Collection." );
				assertNotNull( entity.getConnectivityPlanId(), "-- entity in Collection has a null primary key" );        

				LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
				index++;
			}
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return collection;			
	}

	/**
	 * Assigns a common log handler for each test class in the suite 
	 * in the event log output needs to go elsewhere
	 * 
	 * @param		handler	Handler
	 * @return		ConnectivityPlanTest
	 */
	protected ConnectivityPlanTest setHandler(Handler handler) {
		if ( handler != null )
			LOGGER.addHandler(handler); 
		return this;
	}

	/**
	 * Returns a new populated ConnectivityPlan
	 * 
	 * @return CreateConnectivityPlanCommand alias
	 */
	protected CreateConnectivityPlanCommand generateNewCommand() {
        CreateConnectivityPlanCommand command = new CreateConnectivityPlanCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  0,  0 );
		
		return( command );
	}

		/**
		 * Returns a new populated ConnectivityPlan
		 * 
		 * @return UpdateConnectivityPlanCommand alias
		 */
	protected UpdateConnectivityPlanCommand generateUpdateCommand() {
	        UpdateConnectivityPlanCommand command = new UpdateConnectivityPlanCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  0,  0,  new HashSet<>(),  null );
			
			return( command );
		}
	//-----------------------------------------------------
	// attributes 
	//-----------------------------------------------------
	protected UUID connectivityPlanId = null;
	protected ConnectivityPlanSubscriber subscriber = null;
	private final String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
	private final Logger LOGGER = Logger.getLogger(ConnectivityPlanTest.class.getName());
	private String testingStep = "";
	private Integer sizeOfConnectivityPlanList = 0;
}
