/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.springframework.util.Assert.state;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;
import com.harbormaster.subscriber.*;

/**
 * <h2>TelemetryStreamTest</h2>
 * <p>
 * Junit test class for entity TelemetryStream.  Handle the full lifecycle as it relates to persistence. Leverages
 * the TelemetryStreamService and a TelemetryStreamSubscriber to test everything under a MVC framework which includes the
 * TelemetryStreamAggregate and related commands, queries, and events.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 * 		TelemetryStreamService
 * 	    TelemetryStreamSubscriber
 *
 * <h3>Methods</h3>
 *      <ul>
 *      <li>read</li>
 *      <li>update</li>
 *      <li>delete</li>
 *      </ul>
 *
 * <p>
 * @author Harbormaster Dev Team
 * </p>
 */
public class TelemetryStreamTest {

    // ------------------------------------
	// default constructor
    // ------------------------------------
	public TelemetryStreamTest() {
		subscriber = new TelemetryStreamSubscriber();
	}

	// test methods
	@Test
	/*
	 * Initiate TelemetryStreamTest.
	 */
	public void startTest() throws Throwable {
		try {
			LOGGER.info("**********************************************************");
			LOGGER.info("Beginning test on TelemetryStream...");
			LOGGER.info("**********************************************************\n");
			
			// ---------------------------------------------
			// jumpstart process
			// ---------------------------------------------
			jumpStart();
			
		} catch (Throwable e) {
			throw e;
		} finally {
		}
	}

	/** 
	 * jumpstart the process by instantiating2 TelemetryStream
	 */
	protected void jumpStart() throws Throwable {
		LOGGER.info( "\n======== create instances to get the ball rolling  ======== ");

		telemetryStreamId = TelemetryStreamService.getTelemetryStreamInstance()
		.createTelemetryStream( generateNewCommand() )
		.get();

		// ---------------------------------------------
		// set up query subscriptions after the 1st create
		// ---------------------------------------------
		testingStep = "create";
		setUpQuerySubscriptions();

		TelemetryStreamService.getTelemetryStreamInstance()
				.createTelemetryStream( generateNewCommand() )
				.get();

	}

	/** 
	 * Set up query subscriptions
	 */
	protected void setUpQuerySubscriptions() throws Throwable {
		LOGGER.info( "\n======== Setting Up Query Subscriptions ======== ");
			
		try {            
			subscriber.telemetryStreamSubscribe().updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetAll update received for TelemetryStream : " + successValue.getTelemetryStreamId());
							  if (successValue.getTelemetryStreamId().equals(telemetryStreamId)) {
								  if (testingStep.equals("create")) {
									  testingStep = "update";
									  update();
								  } else if (testingStep.equals("delete")) {
									  testingStep = "complete";
									  state( getAll().size() == sizeOfTelemetryStreamList - 1 , "value not deleted from list");
									  LOGGER.info("**********************************************************");
									  LOGGER.info("TelemetryStream test completed successfully...");
									  LOGGER.info("**********************************************************\n");
								  }
							  }
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on telemetryStream consumed")
					);
			subscriber.telemetryStreamSubscribe( telemetryStreamId ).updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetOne update received for TelemetryStream : " + successValue.getTelemetryStreamId() + " in step " + testingStep);
							  testingStep = "delete";
							  sizeOfTelemetryStreamList = getAll().size();
							  delete();
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on telemetryStream for telemetryStreamId consumed")

					);
			

			}
			catch (Exception e) {
				LOGGER.warning( e.getMessage() );
				throw e;
			}
		}
		
		/** 
	 * read a TelemetryStream. 
	 */
	protected TelemetryStream read() throws Throwable {
		LOGGER.info( "\n======== READ ======== ");
		LOGGER.info( "-- Reading a previously created TelemetryStream" );

		TelemetryStream entity = null;
		StringBuilder msg = new StringBuilder( "-- Failed to read TelemetryStream with primary key" );
		msg.append( telemetryStreamId );
		
		TelemetryStreamFetchOneSummary fetchOneSummary = new TelemetryStreamFetchOneSummary( telemetryStreamId );

		try {
			entity = TelemetryStreamService.getTelemetryStreamInstance().getTelemetryStream( fetchOneSummary );

			assertNotNull( entity,msg.toString() );

			LOGGER.info( "-- Successfully found TelemetryStream " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return entity;
	}

	/** 
	 * updating a TelemetryStream.
	 */
	protected void update() throws Throwable {
		LOGGER.info( "\n======== UPDATE ======== ");
		LOGGER.info( "-- Attempting to update a TelemetryStream." );

		StringBuilder msg = new StringBuilder( "Failed to update a TelemetryStream : " );        
		TelemetryStream entity = read();
		UpdateTelemetryStreamCommand command = generateUpdateCommand();
		command.setTelemetryStreamId(entity.getTelemetryStreamId());

		try {            
			assertNotNull( entity, msg.toString() );

			LOGGER.info( "-- Now updating the created TelemetryStream." );

			// for use later on...
			telemetryStreamId = entity.getTelemetryStreamId();

			TelemetryStreamService proxy = TelemetryStreamService.getTelemetryStreamInstance();  

			proxy.updateTelemetryStream( command ).get();

			LOGGER.info( "-- Successfully saved TelemetryStream - " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : primarykey = " + telemetryStreamId + " : command -" +  command + " : " + e );

			throw e;
		}
	}

	/** 
	 * delete a TelemetryStream.
	 */
	protected void delete() throws Throwable {
		LOGGER.info( "\n======== DELETE ======== ");
		LOGGER.info( "-- Deleting a previously created TelemetryStream." );

		TelemetryStream entity = null;
		
		try{
		    entity = read(); 
			LOGGER.info( "-- Successfully read TelemetryStream with id " + telemetryStreamId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to read TelemetryStream with id " + telemetryStreamId );

			throw e;
		}

		try{
			TelemetryStreamService.getTelemetryStreamInstance().delete( new DeleteTelemetryStreamCommand( entity.getTelemetryStreamId() ) ).get();
			LOGGER.info( "-- Successfully deleted TelemetryStream with id " + telemetryStreamId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to delete TelemetryStream with id " + telemetryStreamId );

			throw e;
		}
	}

	/**
	 * get all TelemetryStreams.
	 */
	protected List<TelemetryStream> getAll() throws Throwable {    
		LOGGER.info( "======== GETALL ======== ");
		LOGGER.info( "-- Retrieving Collection of TelemetryStreams:" );

		StringBuilder msg = new StringBuilder( "-- Failed to get all TelemetryStream : " );        
		List<TelemetryStream> collection  = new ArrayList<>();

		try {
			// call the static get method on the TelemetryStreamService
			collection = TelemetryStreamService.getTelemetryStreamInstance().getAllTelemetryStream();
			assertNotNull( collection, "An Empty collection of TelemetryStream was incorrectly returned.");
			
			// Now print out the values
			TelemetryStream entity = null;            
			Iterator<TelemetryStream> iter = collection.iterator();
			int index = 1;

			while( iter.hasNext() ) {
				// Retrieve the entity   
				entity = iter.next();

				assertNotNull( entity,"-- null entity in Collection." );
				assertNotNull( entity.getTelemetryStreamId(), "-- entity in Collection has a null primary key" );        

				LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
				index++;
			}
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return collection;			
	}

	/**
	 * Assigns a common log handler for each test class in the suite 
	 * in the event log output needs to go elsewhere
	 * 
	 * @param		handler	Handler
	 * @return		TelemetryStreamTest
	 */
	protected TelemetryStreamTest setHandler(Handler handler) {
		if ( handler != null )
			LOGGER.addHandler(handler); 
		return this;
	}

	/**
	 * Returns a new populated TelemetryStream
	 * 
	 * @return CreateTelemetryStreamCommand alias
	 */
	protected CreateTelemetryStreamCommand generateNewCommand() {
        CreateTelemetryStreamCommand command = new CreateTelemetryStreamCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  0,  MessageQoS.values()[0] );
		
		return( command );
	}

		/**
		 * Returns a new populated TelemetryStream
		 * 
		 * @return UpdateTelemetryStreamCommand alias
		 */
	protected UpdateTelemetryStreamCommand generateUpdateCommand() {
	        UpdateTelemetryStreamCommand command = new UpdateTelemetryStreamCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  0,  null,  null,  null,  null,  null,  MessageQoS.values()[0] );
			
			return( command );
		}
	//-----------------------------------------------------
	// attributes 
	//-----------------------------------------------------
	protected UUID telemetryStreamId = null;
	protected TelemetryStreamSubscriber subscriber = null;
	private final String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
	private final Logger LOGGER = Logger.getLogger(TelemetryStreamTest.class.getName());
	private String testingStep = "";
	private Integer sizeOfTelemetryStreamList = 0;
}
