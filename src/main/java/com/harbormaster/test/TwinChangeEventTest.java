/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.springframework.util.Assert.state;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;
import com.harbormaster.subscriber.*;

/**
 * <h2>TwinChangeEventTest</h2>
 * <p>
 * Junit test class for entity TwinChangeEvent.  Handle the full lifecycle as it relates to persistence. Leverages
 * the TwinChangeEventService and a TwinChangeEventSubscriber to test everything under a MVC framework which includes the
 * TwinChangeEventAggregate and related commands, queries, and events.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 * 		TwinChangeEventService
 * 	    TwinChangeEventSubscriber
 *
 * <h3>Methods</h3>
 *      <ul>
 *      <li>read</li>
 *      <li>update</li>
 *      <li>delete</li>
 *      </ul>
 *
 * <p>
 * @author Harbormaster Dev Team
 * </p>
 */
public class TwinChangeEventTest {

    // ------------------------------------
	// default constructor
    // ------------------------------------
	public TwinChangeEventTest() {
		subscriber = new TwinChangeEventSubscriber();
	}

	// test methods
	@Test
	/*
	 * Initiate TwinChangeEventTest.
	 */
	public void startTest() throws Throwable {
		try {
			LOGGER.info("**********************************************************");
			LOGGER.info("Beginning test on TwinChangeEvent...");
			LOGGER.info("**********************************************************\n");
			
			// ---------------------------------------------
			// jumpstart process
			// ---------------------------------------------
			jumpStart();
			
		} catch (Throwable e) {
			throw e;
		} finally {
		}
	}

	/** 
	 * jumpstart the process by instantiating2 TwinChangeEvent
	 */
	protected void jumpStart() throws Throwable {
		LOGGER.info( "\n======== create instances to get the ball rolling  ======== ");

		twinChangeEventId = TwinChangeEventService.getTwinChangeEventInstance()
		.createTwinChangeEvent( generateNewCommand() )
		.get();

		// ---------------------------------------------
		// set up query subscriptions after the 1st create
		// ---------------------------------------------
		testingStep = "create";
		setUpQuerySubscriptions();

		TwinChangeEventService.getTwinChangeEventInstance()
				.createTwinChangeEvent( generateNewCommand() )
				.get();

	}

	/** 
	 * Set up query subscriptions
	 */
	protected void setUpQuerySubscriptions() throws Throwable {
		LOGGER.info( "\n======== Setting Up Query Subscriptions ======== ");
			
		try {            
			subscriber.twinChangeEventSubscribe().updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetAll update received for TwinChangeEvent : " + successValue.getTwinChangeEventId());
							  if (successValue.getTwinChangeEventId().equals(twinChangeEventId)) {
								  if (testingStep.equals("create")) {
									  testingStep = "update";
									  update();
								  } else if (testingStep.equals("delete")) {
									  testingStep = "complete";
									  state( getAll().size() == sizeOfTwinChangeEventList - 1 , "value not deleted from list");
									  LOGGER.info("**********************************************************");
									  LOGGER.info("TwinChangeEvent test completed successfully...");
									  LOGGER.info("**********************************************************\n");
								  }
							  }
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on twinChangeEvent consumed")
					);
			subscriber.twinChangeEventSubscribe( twinChangeEventId ).updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetOne update received for TwinChangeEvent : " + successValue.getTwinChangeEventId() + " in step " + testingStep);
							  testingStep = "delete";
							  sizeOfTwinChangeEventList = getAll().size();
							  delete();
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on twinChangeEvent for twinChangeEventId consumed")

					);
			

			}
			catch (Exception e) {
				LOGGER.warning( e.getMessage() );
				throw e;
			}
		}
		
		/** 
	 * read a TwinChangeEvent. 
	 */
	protected TwinChangeEvent read() throws Throwable {
		LOGGER.info( "\n======== READ ======== ");
		LOGGER.info( "-- Reading a previously created TwinChangeEvent" );

		TwinChangeEvent entity = null;
		StringBuilder msg = new StringBuilder( "-- Failed to read TwinChangeEvent with primary key" );
		msg.append( twinChangeEventId );
		
		TwinChangeEventFetchOneSummary fetchOneSummary = new TwinChangeEventFetchOneSummary( twinChangeEventId );

		try {
			entity = TwinChangeEventService.getTwinChangeEventInstance().getTwinChangeEvent( fetchOneSummary );

			assertNotNull( entity,msg.toString() );

			LOGGER.info( "-- Successfully found TwinChangeEvent " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return entity;
	}

	/** 
	 * updating a TwinChangeEvent.
	 */
	protected void update() throws Throwable {
		LOGGER.info( "\n======== UPDATE ======== ");
		LOGGER.info( "-- Attempting to update a TwinChangeEvent." );

		StringBuilder msg = new StringBuilder( "Failed to update a TwinChangeEvent : " );        
		TwinChangeEvent entity = read();
		UpdateTwinChangeEventCommand command = generateUpdateCommand();
		command.setTwinChangeEventId(entity.getTwinChangeEventId());

		try {            
			assertNotNull( entity, msg.toString() );

			LOGGER.info( "-- Now updating the created TwinChangeEvent." );

			// for use later on...
			twinChangeEventId = entity.getTwinChangeEventId();

			TwinChangeEventService proxy = TwinChangeEventService.getTwinChangeEventInstance();  

			proxy.updateTwinChangeEvent( command ).get();

			LOGGER.info( "-- Successfully saved TwinChangeEvent - " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : primarykey = " + twinChangeEventId + " : command -" +  command + " : " + e );

			throw e;
		}
	}

	/** 
	 * delete a TwinChangeEvent.
	 */
	protected void delete() throws Throwable {
		LOGGER.info( "\n======== DELETE ======== ");
		LOGGER.info( "-- Deleting a previously created TwinChangeEvent." );

		TwinChangeEvent entity = null;
		
		try{
		    entity = read(); 
			LOGGER.info( "-- Successfully read TwinChangeEvent with id " + twinChangeEventId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to read TwinChangeEvent with id " + twinChangeEventId );

			throw e;
		}

		try{
			TwinChangeEventService.getTwinChangeEventInstance().delete( new DeleteTwinChangeEventCommand( entity.getTwinChangeEventId() ) ).get();
			LOGGER.info( "-- Successfully deleted TwinChangeEvent with id " + twinChangeEventId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to delete TwinChangeEvent with id " + twinChangeEventId );

			throw e;
		}
	}

	/**
	 * get all TwinChangeEvents.
	 */
	protected List<TwinChangeEvent> getAll() throws Throwable {    
		LOGGER.info( "======== GETALL ======== ");
		LOGGER.info( "-- Retrieving Collection of TwinChangeEvents:" );

		StringBuilder msg = new StringBuilder( "-- Failed to get all TwinChangeEvent : " );        
		List<TwinChangeEvent> collection  = new ArrayList<>();

		try {
			// call the static get method on the TwinChangeEventService
			collection = TwinChangeEventService.getTwinChangeEventInstance().getAllTwinChangeEvent();
			assertNotNull( collection, "An Empty collection of TwinChangeEvent was incorrectly returned.");
			
			// Now print out the values
			TwinChangeEvent entity = null;            
			Iterator<TwinChangeEvent> iter = collection.iterator();
			int index = 1;

			while( iter.hasNext() ) {
				// Retrieve the entity   
				entity = iter.next();

				assertNotNull( entity,"-- null entity in Collection." );
				assertNotNull( entity.getTwinChangeEventId(), "-- entity in Collection has a null primary key" );        

				LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
				index++;
			}
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return collection;			
	}

	/**
	 * Assigns a common log handler for each test class in the suite 
	 * in the event log output needs to go elsewhere
	 * 
	 * @param		handler	Handler
	 * @return		TwinChangeEventTest
	 */
	protected TwinChangeEventTest setHandler(Handler handler) {
		if ( handler != null )
			LOGGER.addHandler(handler); 
		return this;
	}

	/**
	 * Returns a new populated TwinChangeEvent
	 * 
	 * @return CreateTwinChangeEventCommand alias
	 */
	protected CreateTwinChangeEventCommand generateNewCommand() {
        CreateTwinChangeEventCommand command = new CreateTwinChangeEventCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),   new Date(),  TwinChangeType.values()[0] );
		
		return( command );
	}

		/**
		 * Returns a new populated TwinChangeEvent
		 * 
		 * @return UpdateTwinChangeEventCommand alias
		 */
	protected UpdateTwinChangeEventCommand generateUpdateCommand() {
	        UpdateTwinChangeEventCommand command = new UpdateTwinChangeEventCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),   new Date(),  null,  TwinChangeType.values()[0] );
			
			return( command );
		}
	//-----------------------------------------------------
	// attributes 
	//-----------------------------------------------------
	protected UUID twinChangeEventId = null;
	protected TwinChangeEventSubscriber subscriber = null;
	private final String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
	private final Logger LOGGER = Logger.getLogger(TwinChangeEventTest.class.getName());
	private String testingStep = "";
	private Integer sizeOfTwinChangeEventList = 0;
}
