/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.springframework.util.Assert.state;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;
import com.harbormaster.subscriber.*;

/**
 * <h2>TwinTemplateTest</h2>
 * <p>
 * Junit test class for entity TwinTemplate.  Handle the full lifecycle as it relates to persistence. Leverages
 * the TwinTemplateService and a TwinTemplateSubscriber to test everything under a MVC framework which includes the
 * TwinTemplateAggregate and related commands, queries, and events.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 * 		TwinTemplateService
 * 	    TwinTemplateSubscriber
 *
 * <h3>Methods</h3>
 *      <ul>
 *      <li>read</li>
 *      <li>update</li>
 *      <li>delete</li>
 *      </ul>
 *
 * <p>
 * @author Harbormaster Dev Team
 * </p>
 */
public class TwinTemplateTest {

    // ------------------------------------
	// default constructor
    // ------------------------------------
	public TwinTemplateTest() {
		subscriber = new TwinTemplateSubscriber();
	}

	// test methods
	@Test
	/*
	 * Initiate TwinTemplateTest.
	 */
	public void startTest() throws Throwable {
		try {
			LOGGER.info("**********************************************************");
			LOGGER.info("Beginning test on TwinTemplate...");
			LOGGER.info("**********************************************************\n");
			
			// ---------------------------------------------
			// jumpstart process
			// ---------------------------------------------
			jumpStart();
			
		} catch (Throwable e) {
			throw e;
		} finally {
		}
	}

	/** 
	 * jumpstart the process by instantiating2 TwinTemplate
	 */
	protected void jumpStart() throws Throwable {
		LOGGER.info( "\n======== create instances to get the ball rolling  ======== ");

		twinTemplateId = TwinTemplateService.getTwinTemplateInstance()
		.createTwinTemplate( generateNewCommand() )
		.get();

		// ---------------------------------------------
		// set up query subscriptions after the 1st create
		// ---------------------------------------------
		testingStep = "create";
		setUpQuerySubscriptions();

		TwinTemplateService.getTwinTemplateInstance()
				.createTwinTemplate( generateNewCommand() )
				.get();

	}

	/** 
	 * Set up query subscriptions
	 */
	protected void setUpQuerySubscriptions() throws Throwable {
		LOGGER.info( "\n======== Setting Up Query Subscriptions ======== ");
			
		try {            
			subscriber.twinTemplateSubscribe().updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetAll update received for TwinTemplate : " + successValue.getTwinTemplateId());
							  if (successValue.getTwinTemplateId().equals(twinTemplateId)) {
								  if (testingStep.equals("create")) {
									  testingStep = "update";
									  update();
								  } else if (testingStep.equals("delete")) {
									  testingStep = "complete";
									  state( getAll().size() == sizeOfTwinTemplateList - 1 , "value not deleted from list");
									  LOGGER.info("**********************************************************");
									  LOGGER.info("TwinTemplate test completed successfully...");
									  LOGGER.info("**********************************************************\n");
								  }
							  }
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on twinTemplate consumed")
					);
			subscriber.twinTemplateSubscribe( twinTemplateId ).updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetOne update received for TwinTemplate : " + successValue.getTwinTemplateId() + " in step " + testingStep);
							  testingStep = "delete";
							  sizeOfTwinTemplateList = getAll().size();
							  delete();
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on twinTemplate for twinTemplateId consumed")

					);
			

			}
			catch (Exception e) {
				LOGGER.warning( e.getMessage() );
				throw e;
			}
		}
		
		/** 
	 * read a TwinTemplate. 
	 */
	protected TwinTemplate read() throws Throwable {
		LOGGER.info( "\n======== READ ======== ");
		LOGGER.info( "-- Reading a previously created TwinTemplate" );

		TwinTemplate entity = null;
		StringBuilder msg = new StringBuilder( "-- Failed to read TwinTemplate with primary key" );
		msg.append( twinTemplateId );
		
		TwinTemplateFetchOneSummary fetchOneSummary = new TwinTemplateFetchOneSummary( twinTemplateId );

		try {
			entity = TwinTemplateService.getTwinTemplateInstance().getTwinTemplate( fetchOneSummary );

			assertNotNull( entity,msg.toString() );

			LOGGER.info( "-- Successfully found TwinTemplate " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return entity;
	}

	/** 
	 * updating a TwinTemplate.
	 */
	protected void update() throws Throwable {
		LOGGER.info( "\n======== UPDATE ======== ");
		LOGGER.info( "-- Attempting to update a TwinTemplate." );

		StringBuilder msg = new StringBuilder( "Failed to update a TwinTemplate : " );        
		TwinTemplate entity = read();
		UpdateTwinTemplateCommand command = generateUpdateCommand();
		command.setTwinTemplateId(entity.getTwinTemplateId());

		try {            
			assertNotNull( entity, msg.toString() );

			LOGGER.info( "-- Now updating the created TwinTemplate." );

			// for use later on...
			twinTemplateId = entity.getTwinTemplateId();

			TwinTemplateService proxy = TwinTemplateService.getTwinTemplateInstance();  

			proxy.updateTwinTemplate( command ).get();

			LOGGER.info( "-- Successfully saved TwinTemplate - " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : primarykey = " + twinTemplateId + " : command -" +  command + " : " + e );

			throw e;
		}
	}

	/** 
	 * delete a TwinTemplate.
	 */
	protected void delete() throws Throwable {
		LOGGER.info( "\n======== DELETE ======== ");
		LOGGER.info( "-- Deleting a previously created TwinTemplate." );

		TwinTemplate entity = null;
		
		try{
		    entity = read(); 
			LOGGER.info( "-- Successfully read TwinTemplate with id " + twinTemplateId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to read TwinTemplate with id " + twinTemplateId );

			throw e;
		}

		try{
			TwinTemplateService.getTwinTemplateInstance().delete( new DeleteTwinTemplateCommand( entity.getTwinTemplateId() ) ).get();
			LOGGER.info( "-- Successfully deleted TwinTemplate with id " + twinTemplateId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to delete TwinTemplate with id " + twinTemplateId );

			throw e;
		}
	}

	/**
	 * get all TwinTemplates.
	 */
	protected List<TwinTemplate> getAll() throws Throwable {    
		LOGGER.info( "======== GETALL ======== ");
		LOGGER.info( "-- Retrieving Collection of TwinTemplates:" );

		StringBuilder msg = new StringBuilder( "-- Failed to get all TwinTemplate : " );        
		List<TwinTemplate> collection  = new ArrayList<>();

		try {
			// call the static get method on the TwinTemplateService
			collection = TwinTemplateService.getTwinTemplateInstance().getAllTwinTemplate();
			assertNotNull( collection, "An Empty collection of TwinTemplate was incorrectly returned.");
			
			// Now print out the values
			TwinTemplate entity = null;            
			Iterator<TwinTemplate> iter = collection.iterator();
			int index = 1;

			while( iter.hasNext() ) {
				// Retrieve the entity   
				entity = iter.next();

				assertNotNull( entity,"-- null entity in Collection." );
				assertNotNull( entity.getTwinTemplateId(), "-- entity in Collection has a null primary key" );        

				LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
				index++;
			}
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return collection;			
	}

	/**
	 * Assigns a common log handler for each test class in the suite 
	 * in the event log output needs to go elsewhere
	 * 
	 * @param		handler	Handler
	 * @return		TwinTemplateTest
	 */
	protected TwinTemplateTest setHandler(Handler handler) {
		if ( handler != null )
			LOGGER.addHandler(handler); 
		return this;
	}

	/**
	 * Returns a new populated TwinTemplate
	 * 
	 * @return CreateTwinTemplateCommand alias
	 */
	protected CreateTwinTemplateCommand generateNewCommand() {
        CreateTwinTemplateCommand command = new CreateTwinTemplateCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Uri(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16) );
		
		return( command );
	}

		/**
		 * Returns a new populated TwinTemplate
		 * 
		 * @return UpdateTwinTemplateCommand alias
		 */
	protected UpdateTwinTemplateCommand generateUpdateCommand() {
	        UpdateTwinTemplateCommand command = new UpdateTwinTemplateCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Uri(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new HashSet<>() );
			
			return( command );
		}
	//-----------------------------------------------------
	// attributes 
	//-----------------------------------------------------
	protected UUID twinTemplateId = null;
	protected TwinTemplateSubscriber subscriber = null;
	private final String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
	private final Logger LOGGER = Logger.getLogger(TwinTemplateTest.class.getName());
	private String testingStep = "";
	private Integer sizeOfTwinTemplateList = 0;
}
