/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.springframework.util.Assert.state;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;
import com.harbormaster.subscriber.*;

/**
 * <h2>DeviceVendorTest</h2>
 * <p>
 * Junit test class for entity DeviceVendor.  Handle the full lifecycle as it relates to persistence. Leverages
 * the DeviceVendorService and a DeviceVendorSubscriber to test everything under a MVC framework which includes the
 * DeviceVendorAggregate and related commands, queries, and events.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 * 		DeviceVendorService
 * 	    DeviceVendorSubscriber
 *
 * <h3>Methods</h3>
 *      <ul>
 *      <li>read</li>
 *      <li>update</li>
 *      <li>delete</li>
 *      </ul>
 *
 * <p>
 * @author Harbormaster Dev Team
 * </p>
 */
public class DeviceVendorTest {

    // ------------------------------------
	// default constructor
    // ------------------------------------
	public DeviceVendorTest() {
		subscriber = new DeviceVendorSubscriber();
	}

	// test methods
	@Test
	/*
	 * Initiate DeviceVendorTest.
	 */
	public void startTest() throws Throwable {
		try {
			LOGGER.info("**********************************************************");
			LOGGER.info("Beginning test on DeviceVendor...");
			LOGGER.info("**********************************************************\n");
			
			// ---------------------------------------------
			// jumpstart process
			// ---------------------------------------------
			jumpStart();
			
		} catch (Throwable e) {
			throw e;
		} finally {
		}
	}

	/** 
	 * jumpstart the process by instantiating2 DeviceVendor
	 */
	protected void jumpStart() throws Throwable {
		LOGGER.info( "\n======== create instances to get the ball rolling  ======== ");

		deviceVendorId = DeviceVendorService.getDeviceVendorInstance()
		.createDeviceVendor( generateNewCommand() )
		.get();

		// ---------------------------------------------
		// set up query subscriptions after the 1st create
		// ---------------------------------------------
		testingStep = "create";
		setUpQuerySubscriptions();

		DeviceVendorService.getDeviceVendorInstance()
				.createDeviceVendor( generateNewCommand() )
				.get();

	}

	/** 
	 * Set up query subscriptions
	 */
	protected void setUpQuerySubscriptions() throws Throwable {
		LOGGER.info( "\n======== Setting Up Query Subscriptions ======== ");
			
		try {            
			subscriber.deviceVendorSubscribe().updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetAll update received for DeviceVendor : " + successValue.getDeviceVendorId());
							  if (successValue.getDeviceVendorId().equals(deviceVendorId)) {
								  if (testingStep.equals("create")) {
									  testingStep = "update";
									  update();
								  } else if (testingStep.equals("delete")) {
									  testingStep = "complete";
									  state( getAll().size() == sizeOfDeviceVendorList - 1 , "value not deleted from list");
									  LOGGER.info("**********************************************************");
									  LOGGER.info("DeviceVendor test completed successfully...");
									  LOGGER.info("**********************************************************\n");
								  }
							  }
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on deviceVendor consumed")
					);
			subscriber.deviceVendorSubscribe( deviceVendorId ).updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetOne update received for DeviceVendor : " + successValue.getDeviceVendorId() + " in step " + testingStep);
							  testingStep = "delete";
							  sizeOfDeviceVendorList = getAll().size();
							  delete();
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on deviceVendor for deviceVendorId consumed")

					);
			

			}
			catch (Exception e) {
				LOGGER.warning( e.getMessage() );
				throw e;
			}
		}
		
		/** 
	 * read a DeviceVendor. 
	 */
	protected DeviceVendor read() throws Throwable {
		LOGGER.info( "\n======== READ ======== ");
		LOGGER.info( "-- Reading a previously created DeviceVendor" );

		DeviceVendor entity = null;
		StringBuilder msg = new StringBuilder( "-- Failed to read DeviceVendor with primary key" );
		msg.append( deviceVendorId );
		
		DeviceVendorFetchOneSummary fetchOneSummary = new DeviceVendorFetchOneSummary( deviceVendorId );

		try {
			entity = DeviceVendorService.getDeviceVendorInstance().getDeviceVendor( fetchOneSummary );

			assertNotNull( entity,msg.toString() );

			LOGGER.info( "-- Successfully found DeviceVendor " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return entity;
	}

	/** 
	 * updating a DeviceVendor.
	 */
	protected void update() throws Throwable {
		LOGGER.info( "\n======== UPDATE ======== ");
		LOGGER.info( "-- Attempting to update a DeviceVendor." );

		StringBuilder msg = new StringBuilder( "Failed to update a DeviceVendor : " );        
		DeviceVendor entity = read();
		UpdateDeviceVendorCommand command = generateUpdateCommand();
		command.setDeviceVendorId(entity.getDeviceVendorId());

		try {            
			assertNotNull( entity, msg.toString() );

			LOGGER.info( "-- Now updating the created DeviceVendor." );

			// for use later on...
			deviceVendorId = entity.getDeviceVendorId();

			DeviceVendorService proxy = DeviceVendorService.getDeviceVendorInstance();  

			proxy.updateDeviceVendor( command ).get();

			LOGGER.info( "-- Successfully saved DeviceVendor - " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : primarykey = " + deviceVendorId + " : command -" +  command + " : " + e );

			throw e;
		}
	}

	/** 
	 * delete a DeviceVendor.
	 */
	protected void delete() throws Throwable {
		LOGGER.info( "\n======== DELETE ======== ");
		LOGGER.info( "-- Deleting a previously created DeviceVendor." );

		DeviceVendor entity = null;
		
		try{
		    entity = read(); 
			LOGGER.info( "-- Successfully read DeviceVendor with id " + deviceVendorId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to read DeviceVendor with id " + deviceVendorId );

			throw e;
		}

		try{
			DeviceVendorService.getDeviceVendorInstance().delete( new DeleteDeviceVendorCommand( entity.getDeviceVendorId() ) ).get();
			LOGGER.info( "-- Successfully deleted DeviceVendor with id " + deviceVendorId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to delete DeviceVendor with id " + deviceVendorId );

			throw e;
		}
	}

	/**
	 * get all DeviceVendors.
	 */
	protected List<DeviceVendor> getAll() throws Throwable {    
		LOGGER.info( "======== GETALL ======== ");
		LOGGER.info( "-- Retrieving Collection of DeviceVendors:" );

		StringBuilder msg = new StringBuilder( "-- Failed to get all DeviceVendor : " );        
		List<DeviceVendor> collection  = new ArrayList<>();

		try {
			// call the static get method on the DeviceVendorService
			collection = DeviceVendorService.getDeviceVendorInstance().getAllDeviceVendor();
			assertNotNull( collection, "An Empty collection of DeviceVendor was incorrectly returned.");
			
			// Now print out the values
			DeviceVendor entity = null;            
			Iterator<DeviceVendor> iter = collection.iterator();
			int index = 1;

			while( iter.hasNext() ) {
				// Retrieve the entity   
				entity = iter.next();

				assertNotNull( entity,"-- null entity in Collection." );
				assertNotNull( entity.getDeviceVendorId(), "-- entity in Collection has a null primary key" );        

				LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
				index++;
			}
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return collection;			
	}

	/**
	 * Assigns a common log handler for each test class in the suite 
	 * in the event log output needs to go elsewhere
	 * 
	 * @param		handler	Handler
	 * @return		DeviceVendorTest
	 */
	protected DeviceVendorTest setHandler(Handler handler) {
		if ( handler != null )
			LOGGER.addHandler(handler); 
		return this;
	}

	/**
	 * Returns a new populated DeviceVendor
	 * 
	 * @return CreateDeviceVendorCommand alias
	 */
	protected CreateDeviceVendorCommand generateNewCommand() {
        CreateDeviceVendorCommand command = new CreateDeviceVendorCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16) );
		
		return( command );
	}

		/**
		 * Returns a new populated DeviceVendor
		 * 
		 * @return UpdateDeviceVendorCommand alias
		 */
	protected UpdateDeviceVendorCommand generateUpdateCommand() {
	        UpdateDeviceVendorCommand command = new UpdateDeviceVendorCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new HashSet<>(),  new HashSet<>(),  new HashSet<>() );
			
			return( command );
		}
	//-----------------------------------------------------
	// attributes 
	//-----------------------------------------------------
	protected UUID deviceVendorId = null;
	protected DeviceVendorSubscriber subscriber = null;
	private final String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
	private final Logger LOGGER = Logger.getLogger(DeviceVendorTest.class.getName());
	private String testingStep = "";
	private Integer sizeOfDeviceVendorList = 0;
}
