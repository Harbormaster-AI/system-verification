/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.springframework.util.Assert.state;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;
import com.harbormaster.subscriber.*;

/**
 * <h2>SoftwareUpdateCampaignTest</h2>
 * <p>
 * Junit test class for entity SoftwareUpdateCampaign.  Handle the full lifecycle as it relates to persistence. Leverages
 * the SoftwareUpdateCampaignService and a SoftwareUpdateCampaignSubscriber to test everything under a MVC framework which includes the
 * SoftwareUpdateCampaignAggregate and related commands, queries, and events.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 * 		SoftwareUpdateCampaignService
 * 	    SoftwareUpdateCampaignSubscriber
 *
 * <h3>Methods</h3>
 *      <ul>
 *      <li>read</li>
 *      <li>update</li>
 *      <li>delete</li>
 *      </ul>
 *
 * <p>
 * @author Harbormaster Dev Team
 * </p>
 */
public class SoftwareUpdateCampaignTest {

    // ------------------------------------
	// default constructor
    // ------------------------------------
	public SoftwareUpdateCampaignTest() {
		subscriber = new SoftwareUpdateCampaignSubscriber();
	}

	// test methods
	@Test
	/*
	 * Initiate SoftwareUpdateCampaignTest.
	 */
	public void startTest() throws Throwable {
		try {
			LOGGER.info("**********************************************************");
			LOGGER.info("Beginning test on SoftwareUpdateCampaign...");
			LOGGER.info("**********************************************************\n");
			
			// ---------------------------------------------
			// jumpstart process
			// ---------------------------------------------
			jumpStart();
			
		} catch (Throwable e) {
			throw e;
		} finally {
		}
	}

	/** 
	 * jumpstart the process by instantiating2 SoftwareUpdateCampaign
	 */
	protected void jumpStart() throws Throwable {
		LOGGER.info( "\n======== create instances to get the ball rolling  ======== ");

		softwareUpdateCampaignId = SoftwareUpdateCampaignService.getSoftwareUpdateCampaignInstance()
		.createSoftwareUpdateCampaign( generateNewCommand() )
		.get();

		// ---------------------------------------------
		// set up query subscriptions after the 1st create
		// ---------------------------------------------
		testingStep = "create";
		setUpQuerySubscriptions();

		SoftwareUpdateCampaignService.getSoftwareUpdateCampaignInstance()
				.createSoftwareUpdateCampaign( generateNewCommand() )
				.get();

	}

	/** 
	 * Set up query subscriptions
	 */
	protected void setUpQuerySubscriptions() throws Throwable {
		LOGGER.info( "\n======== Setting Up Query Subscriptions ======== ");
			
		try {            
			subscriber.softwareUpdateCampaignSubscribe().updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetAll update received for SoftwareUpdateCampaign : " + successValue.getSoftwareUpdateCampaignId());
							  if (successValue.getSoftwareUpdateCampaignId().equals(softwareUpdateCampaignId)) {
								  if (testingStep.equals("create")) {
									  testingStep = "update";
									  update();
								  } else if (testingStep.equals("delete")) {
									  testingStep = "complete";
									  state( getAll().size() == sizeOfSoftwareUpdateCampaignList - 1 , "value not deleted from list");
									  LOGGER.info("**********************************************************");
									  LOGGER.info("SoftwareUpdateCampaign test completed successfully...");
									  LOGGER.info("**********************************************************\n");
								  }
							  }
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on softwareUpdateCampaign consumed")
					);
			subscriber.softwareUpdateCampaignSubscribe( softwareUpdateCampaignId ).updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetOne update received for SoftwareUpdateCampaign : " + successValue.getSoftwareUpdateCampaignId() + " in step " + testingStep);
							  testingStep = "delete";
							  sizeOfSoftwareUpdateCampaignList = getAll().size();
							  delete();
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on softwareUpdateCampaign for softwareUpdateCampaignId consumed")

					);
			

			}
			catch (Exception e) {
				LOGGER.warning( e.getMessage() );
				throw e;
			}
		}
		
		/** 
	 * read a SoftwareUpdateCampaign. 
	 */
	protected SoftwareUpdateCampaign read() throws Throwable {
		LOGGER.info( "\n======== READ ======== ");
		LOGGER.info( "-- Reading a previously created SoftwareUpdateCampaign" );

		SoftwareUpdateCampaign entity = null;
		StringBuilder msg = new StringBuilder( "-- Failed to read SoftwareUpdateCampaign with primary key" );
		msg.append( softwareUpdateCampaignId );
		
		SoftwareUpdateCampaignFetchOneSummary fetchOneSummary = new SoftwareUpdateCampaignFetchOneSummary( softwareUpdateCampaignId );

		try {
			entity = SoftwareUpdateCampaignService.getSoftwareUpdateCampaignInstance().getSoftwareUpdateCampaign( fetchOneSummary );

			assertNotNull( entity,msg.toString() );

			LOGGER.info( "-- Successfully found SoftwareUpdateCampaign " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return entity;
	}

	/** 
	 * updating a SoftwareUpdateCampaign.
	 */
	protected void update() throws Throwable {
		LOGGER.info( "\n======== UPDATE ======== ");
		LOGGER.info( "-- Attempting to update a SoftwareUpdateCampaign." );

		StringBuilder msg = new StringBuilder( "Failed to update a SoftwareUpdateCampaign : " );        
		SoftwareUpdateCampaign entity = read();
		UpdateSoftwareUpdateCampaignCommand command = generateUpdateCommand();
		command.setSoftwareUpdateCampaignId(entity.getSoftwareUpdateCampaignId());

		try {            
			assertNotNull( entity, msg.toString() );

			LOGGER.info( "-- Now updating the created SoftwareUpdateCampaign." );

			// for use later on...
			softwareUpdateCampaignId = entity.getSoftwareUpdateCampaignId();

			SoftwareUpdateCampaignService proxy = SoftwareUpdateCampaignService.getSoftwareUpdateCampaignInstance();  

			proxy.updateSoftwareUpdateCampaign( command ).get();

			LOGGER.info( "-- Successfully saved SoftwareUpdateCampaign - " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : primarykey = " + softwareUpdateCampaignId + " : command -" +  command + " : " + e );

			throw e;
		}
	}

	/** 
	 * delete a SoftwareUpdateCampaign.
	 */
	protected void delete() throws Throwable {
		LOGGER.info( "\n======== DELETE ======== ");
		LOGGER.info( "-- Deleting a previously created SoftwareUpdateCampaign." );

		SoftwareUpdateCampaign entity = null;
		
		try{
		    entity = read(); 
			LOGGER.info( "-- Successfully read SoftwareUpdateCampaign with id " + softwareUpdateCampaignId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to read SoftwareUpdateCampaign with id " + softwareUpdateCampaignId );

			throw e;
		}

		try{
			SoftwareUpdateCampaignService.getSoftwareUpdateCampaignInstance().delete( new DeleteSoftwareUpdateCampaignCommand( entity.getSoftwareUpdateCampaignId() ) ).get();
			LOGGER.info( "-- Successfully deleted SoftwareUpdateCampaign with id " + softwareUpdateCampaignId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to delete SoftwareUpdateCampaign with id " + softwareUpdateCampaignId );

			throw e;
		}
	}

	/**
	 * get all SoftwareUpdateCampaigns.
	 */
	protected List<SoftwareUpdateCampaign> getAll() throws Throwable {    
		LOGGER.info( "======== GETALL ======== ");
		LOGGER.info( "-- Retrieving Collection of SoftwareUpdateCampaigns:" );

		StringBuilder msg = new StringBuilder( "-- Failed to get all SoftwareUpdateCampaign : " );        
		List<SoftwareUpdateCampaign> collection  = new ArrayList<>();

		try {
			// call the static get method on the SoftwareUpdateCampaignService
			collection = SoftwareUpdateCampaignService.getSoftwareUpdateCampaignInstance().getAllSoftwareUpdateCampaign();
			assertNotNull( collection, "An Empty collection of SoftwareUpdateCampaign was incorrectly returned.");
			
			// Now print out the values
			SoftwareUpdateCampaign entity = null;            
			Iterator<SoftwareUpdateCampaign> iter = collection.iterator();
			int index = 1;

			while( iter.hasNext() ) {
				// Retrieve the entity   
				entity = iter.next();

				assertNotNull( entity,"-- null entity in Collection." );
				assertNotNull( entity.getSoftwareUpdateCampaignId(), "-- entity in Collection has a null primary key" );        

				LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
				index++;
			}
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return collection;			
	}

	/**
	 * Assigns a common log handler for each test class in the suite 
	 * in the event log output needs to go elsewhere
	 * 
	 * @param		handler	Handler
	 * @return		SoftwareUpdateCampaignTest
	 */
	protected SoftwareUpdateCampaignTest setHandler(Handler handler) {
		if ( handler != null )
			LOGGER.addHandler(handler); 
		return this;
	}

	/**
	 * Returns a new populated SoftwareUpdateCampaign
	 * 
	 * @return CreateSoftwareUpdateCampaignCommand alias
	 */
	protected CreateSoftwareUpdateCampaignCommand generateNewCommand() {
        CreateSoftwareUpdateCampaignCommand command = new CreateSoftwareUpdateCampaignCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),   new Date(),   new Date(),  UpdateCampaignStatus.values()[0] );
		
		return( command );
	}

		/**
		 * Returns a new populated SoftwareUpdateCampaign
		 * 
		 * @return UpdateSoftwareUpdateCampaignCommand alias
		 */
	protected UpdateSoftwareUpdateCampaignCommand generateUpdateCommand() {
	        UpdateSoftwareUpdateCampaignCommand command = new UpdateSoftwareUpdateCampaignCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),   new Date(),   new Date(),  null,  null,  new HashSet<>(),  UpdateCampaignStatus.values()[0] );
			
			return( command );
		}
	//-----------------------------------------------------
	// attributes 
	//-----------------------------------------------------
	protected UUID softwareUpdateCampaignId = null;
	protected SoftwareUpdateCampaignSubscriber subscriber = null;
	private final String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
	private final Logger LOGGER = Logger.getLogger(SoftwareUpdateCampaignTest.class.getName());
	private String testingStep = "";
	private Integer sizeOfSoftwareUpdateCampaignList = 0;
}
