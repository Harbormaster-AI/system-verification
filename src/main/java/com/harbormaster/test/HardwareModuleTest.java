/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.springframework.util.Assert.state;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;
import com.harbormaster.subscriber.*;

/**
 * <h2>HardwareModuleTest</h2>
 * <p>
 * Junit test class for entity HardwareModule.  Handle the full lifecycle as it relates to persistence. Leverages
 * the HardwareModuleService and a HardwareModuleSubscriber to test everything under a MVC framework which includes the
 * HardwareModuleAggregate and related commands, queries, and events.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 * 		HardwareModuleService
 * 	    HardwareModuleSubscriber
 *
 * <h3>Methods</h3>
 *      <ul>
 *      <li>read</li>
 *      <li>update</li>
 *      <li>delete</li>
 *      </ul>
 *
 * <p>
 * @author Harbormaster Dev Team
 * </p>
 */
public class HardwareModuleTest {

    // ------------------------------------
	// default constructor
    // ------------------------------------
	public HardwareModuleTest() {
		subscriber = new HardwareModuleSubscriber();
	}

	// test methods
	@Test
	/*
	 * Initiate HardwareModuleTest.
	 */
	public void startTest() throws Throwable {
		try {
			LOGGER.info("**********************************************************");
			LOGGER.info("Beginning test on HardwareModule...");
			LOGGER.info("**********************************************************\n");
			
			// ---------------------------------------------
			// jumpstart process
			// ---------------------------------------------
			jumpStart();
			
		} catch (Throwable e) {
			throw e;
		} finally {
		}
	}

	/** 
	 * jumpstart the process by instantiating2 HardwareModule
	 */
	protected void jumpStart() throws Throwable {
		LOGGER.info( "\n======== create instances to get the ball rolling  ======== ");

		hardwareModuleId = HardwareModuleService.getHardwareModuleInstance()
		.createHardwareModule( generateNewCommand() )
		.get();

		// ---------------------------------------------
		// set up query subscriptions after the 1st create
		// ---------------------------------------------
		testingStep = "create";
		setUpQuerySubscriptions();

		HardwareModuleService.getHardwareModuleInstance()
				.createHardwareModule( generateNewCommand() )
				.get();

	}

	/** 
	 * Set up query subscriptions
	 */
	protected void setUpQuerySubscriptions() throws Throwable {
		LOGGER.info( "\n======== Setting Up Query Subscriptions ======== ");
			
		try {            
			subscriber.hardwareModuleSubscribe().updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetAll update received for HardwareModule : " + successValue.getHardwareModuleId());
							  if (successValue.getHardwareModuleId().equals(hardwareModuleId)) {
								  if (testingStep.equals("create")) {
									  testingStep = "update";
									  update();
								  } else if (testingStep.equals("delete")) {
									  testingStep = "complete";
									  state( getAll().size() == sizeOfHardwareModuleList - 1 , "value not deleted from list");
									  LOGGER.info("**********************************************************");
									  LOGGER.info("HardwareModule test completed successfully...");
									  LOGGER.info("**********************************************************\n");
								  }
							  }
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on hardwareModule consumed")
					);
			subscriber.hardwareModuleSubscribe( hardwareModuleId ).updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetOne update received for HardwareModule : " + successValue.getHardwareModuleId() + " in step " + testingStep);
							  testingStep = "delete";
							  sizeOfHardwareModuleList = getAll().size();
							  delete();
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on hardwareModule for hardwareModuleId consumed")

					);
			

			}
			catch (Exception e) {
				LOGGER.warning( e.getMessage() );
				throw e;
			}
		}
		
		/** 
	 * read a HardwareModule. 
	 */
	protected HardwareModule read() throws Throwable {
		LOGGER.info( "\n======== READ ======== ");
		LOGGER.info( "-- Reading a previously created HardwareModule" );

		HardwareModule entity = null;
		StringBuilder msg = new StringBuilder( "-- Failed to read HardwareModule with primary key" );
		msg.append( hardwareModuleId );
		
		HardwareModuleFetchOneSummary fetchOneSummary = new HardwareModuleFetchOneSummary( hardwareModuleId );

		try {
			entity = HardwareModuleService.getHardwareModuleInstance().getHardwareModule( fetchOneSummary );

			assertNotNull( entity,msg.toString() );

			LOGGER.info( "-- Successfully found HardwareModule " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return entity;
	}

	/** 
	 * updating a HardwareModule.
	 */
	protected void update() throws Throwable {
		LOGGER.info( "\n======== UPDATE ======== ");
		LOGGER.info( "-- Attempting to update a HardwareModule." );

		StringBuilder msg = new StringBuilder( "Failed to update a HardwareModule : " );        
		HardwareModule entity = read();
		UpdateHardwareModuleCommand command = generateUpdateCommand();
		command.setHardwareModuleId(entity.getHardwareModuleId());

		try {            
			assertNotNull( entity, msg.toString() );

			LOGGER.info( "-- Now updating the created HardwareModule." );

			// for use later on...
			hardwareModuleId = entity.getHardwareModuleId();

			HardwareModuleService proxy = HardwareModuleService.getHardwareModuleInstance();  

			proxy.updateHardwareModule( command ).get();

			LOGGER.info( "-- Successfully saved HardwareModule - " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : primarykey = " + hardwareModuleId + " : command -" +  command + " : " + e );

			throw e;
		}
	}

	/** 
	 * delete a HardwareModule.
	 */
	protected void delete() throws Throwable {
		LOGGER.info( "\n======== DELETE ======== ");
		LOGGER.info( "-- Deleting a previously created HardwareModule." );

		HardwareModule entity = null;
		
		try{
		    entity = read(); 
			LOGGER.info( "-- Successfully read HardwareModule with id " + hardwareModuleId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to read HardwareModule with id " + hardwareModuleId );

			throw e;
		}

		try{
			HardwareModuleService.getHardwareModuleInstance().delete( new DeleteHardwareModuleCommand( entity.getHardwareModuleId() ) ).get();
			LOGGER.info( "-- Successfully deleted HardwareModule with id " + hardwareModuleId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to delete HardwareModule with id " + hardwareModuleId );

			throw e;
		}
	}

	/**
	 * get all HardwareModules.
	 */
	protected List<HardwareModule> getAll() throws Throwable {    
		LOGGER.info( "======== GETALL ======== ");
		LOGGER.info( "-- Retrieving Collection of HardwareModules:" );

		StringBuilder msg = new StringBuilder( "-- Failed to get all HardwareModule : " );        
		List<HardwareModule> collection  = new ArrayList<>();

		try {
			// call the static get method on the HardwareModuleService
			collection = HardwareModuleService.getHardwareModuleInstance().getAllHardwareModule();
			assertNotNull( collection, "An Empty collection of HardwareModule was incorrectly returned.");
			
			// Now print out the values
			HardwareModule entity = null;            
			Iterator<HardwareModule> iter = collection.iterator();
			int index = 1;

			while( iter.hasNext() ) {
				// Retrieve the entity   
				entity = iter.next();

				assertNotNull( entity,"-- null entity in Collection." );
				assertNotNull( entity.getHardwareModuleId(), "-- entity in Collection has a null primary key" );        

				LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
				index++;
			}
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return collection;			
	}

	/**
	 * Assigns a common log handler for each test class in the suite 
	 * in the event log output needs to go elsewhere
	 * 
	 * @param		handler	Handler
	 * @return		HardwareModuleTest
	 */
	protected HardwareModuleTest setHandler(Handler handler) {
		if ( handler != null )
			LOGGER.addHandler(handler); 
		return this;
	}

	/**
	 * Returns a new populated HardwareModule
	 * 
	 * @return CreateHardwareModuleCommand alias
	 */
	protected CreateHardwareModuleCommand generateNewCommand() {
        CreateHardwareModuleCommand command = new CreateHardwareModuleCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Uri(),  ModuleType.values()[0] );
		
		return( command );
	}

		/**
		 * Returns a new populated HardwareModule
		 * 
		 * @return UpdateHardwareModuleCommand alias
		 */
	protected UpdateHardwareModuleCommand generateUpdateCommand() {
	        UpdateHardwareModuleCommand command = new UpdateHardwareModuleCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Uri(),  null,  ModuleType.values()[0] );
			
			return( command );
		}
	//-----------------------------------------------------
	// attributes 
	//-----------------------------------------------------
	protected UUID hardwareModuleId = null;
	protected HardwareModuleSubscriber subscriber = null;
	private final String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
	private final Logger LOGGER = Logger.getLogger(HardwareModuleTest.class.getName());
	private String testingStep = "";
	private Integer sizeOfHardwareModuleList = 0;
}
