/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.springframework.util.Assert.state;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;
import com.harbormaster.subscriber.*;

/**
 * <h2>TelemetrySchemaTest</h2>
 * <p>
 * Junit test class for entity TelemetrySchema.  Handle the full lifecycle as it relates to persistence. Leverages
 * the TelemetrySchemaService and a TelemetrySchemaSubscriber to test everything under a MVC framework which includes the
 * TelemetrySchemaAggregate and related commands, queries, and events.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 * 		TelemetrySchemaService
 * 	    TelemetrySchemaSubscriber
 *
 * <h3>Methods</h3>
 *      <ul>
 *      <li>read</li>
 *      <li>update</li>
 *      <li>delete</li>
 *      </ul>
 *
 * <p>
 * @author Harbormaster Dev Team
 * </p>
 */
public class TelemetrySchemaTest {

    // ------------------------------------
	// default constructor
    // ------------------------------------
	public TelemetrySchemaTest() {
		subscriber = new TelemetrySchemaSubscriber();
	}

	// test methods
	@Test
	/*
	 * Initiate TelemetrySchemaTest.
	 */
	public void startTest() throws Throwable {
		try {
			LOGGER.info("**********************************************************");
			LOGGER.info("Beginning test on TelemetrySchema...");
			LOGGER.info("**********************************************************\n");
			
			// ---------------------------------------------
			// jumpstart process
			// ---------------------------------------------
			jumpStart();
			
		} catch (Throwable e) {
			throw e;
		} finally {
		}
	}

	/** 
	 * jumpstart the process by instantiating2 TelemetrySchema
	 */
	protected void jumpStart() throws Throwable {
		LOGGER.info( "\n======== create instances to get the ball rolling  ======== ");

		telemetrySchemaId = TelemetrySchemaService.getTelemetrySchemaInstance()
		.createTelemetrySchema( generateNewCommand() )
		.get();

		// ---------------------------------------------
		// set up query subscriptions after the 1st create
		// ---------------------------------------------
		testingStep = "create";
		setUpQuerySubscriptions();

		TelemetrySchemaService.getTelemetrySchemaInstance()
				.createTelemetrySchema( generateNewCommand() )
				.get();

	}

	/** 
	 * Set up query subscriptions
	 */
	protected void setUpQuerySubscriptions() throws Throwable {
		LOGGER.info( "\n======== Setting Up Query Subscriptions ======== ");
			
		try {            
			subscriber.telemetrySchemaSubscribe().updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetAll update received for TelemetrySchema : " + successValue.getTelemetrySchemaId());
							  if (successValue.getTelemetrySchemaId().equals(telemetrySchemaId)) {
								  if (testingStep.equals("create")) {
									  testingStep = "update";
									  update();
								  } else if (testingStep.equals("delete")) {
									  testingStep = "complete";
									  state( getAll().size() == sizeOfTelemetrySchemaList - 1 , "value not deleted from list");
									  LOGGER.info("**********************************************************");
									  LOGGER.info("TelemetrySchema test completed successfully...");
									  LOGGER.info("**********************************************************\n");
								  }
							  }
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on telemetrySchema consumed")
					);
			subscriber.telemetrySchemaSubscribe( telemetrySchemaId ).updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetOne update received for TelemetrySchema : " + successValue.getTelemetrySchemaId() + " in step " + testingStep);
							  testingStep = "delete";
							  sizeOfTelemetrySchemaList = getAll().size();
							  delete();
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on telemetrySchema for telemetrySchemaId consumed")

					);
			

			}
			catch (Exception e) {
				LOGGER.warning( e.getMessage() );
				throw e;
			}
		}
		
		/** 
	 * read a TelemetrySchema. 
	 */
	protected TelemetrySchema read() throws Throwable {
		LOGGER.info( "\n======== READ ======== ");
		LOGGER.info( "-- Reading a previously created TelemetrySchema" );

		TelemetrySchema entity = null;
		StringBuilder msg = new StringBuilder( "-- Failed to read TelemetrySchema with primary key" );
		msg.append( telemetrySchemaId );
		
		TelemetrySchemaFetchOneSummary fetchOneSummary = new TelemetrySchemaFetchOneSummary( telemetrySchemaId );

		try {
			entity = TelemetrySchemaService.getTelemetrySchemaInstance().getTelemetrySchema( fetchOneSummary );

			assertNotNull( entity,msg.toString() );

			LOGGER.info( "-- Successfully found TelemetrySchema " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return entity;
	}

	/** 
	 * updating a TelemetrySchema.
	 */
	protected void update() throws Throwable {
		LOGGER.info( "\n======== UPDATE ======== ");
		LOGGER.info( "-- Attempting to update a TelemetrySchema." );

		StringBuilder msg = new StringBuilder( "Failed to update a TelemetrySchema : " );        
		TelemetrySchema entity = read();
		UpdateTelemetrySchemaCommand command = generateUpdateCommand();
		command.setTelemetrySchemaId(entity.getTelemetrySchemaId());

		try {            
			assertNotNull( entity, msg.toString() );

			LOGGER.info( "-- Now updating the created TelemetrySchema." );

			// for use later on...
			telemetrySchemaId = entity.getTelemetrySchemaId();

			TelemetrySchemaService proxy = TelemetrySchemaService.getTelemetrySchemaInstance();  

			proxy.updateTelemetrySchema( command ).get();

			LOGGER.info( "-- Successfully saved TelemetrySchema - " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : primarykey = " + telemetrySchemaId + " : command -" +  command + " : " + e );

			throw e;
		}
	}

	/** 
	 * delete a TelemetrySchema.
	 */
	protected void delete() throws Throwable {
		LOGGER.info( "\n======== DELETE ======== ");
		LOGGER.info( "-- Deleting a previously created TelemetrySchema." );

		TelemetrySchema entity = null;
		
		try{
		    entity = read(); 
			LOGGER.info( "-- Successfully read TelemetrySchema with id " + telemetrySchemaId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to read TelemetrySchema with id " + telemetrySchemaId );

			throw e;
		}

		try{
			TelemetrySchemaService.getTelemetrySchemaInstance().delete( new DeleteTelemetrySchemaCommand( entity.getTelemetrySchemaId() ) ).get();
			LOGGER.info( "-- Successfully deleted TelemetrySchema with id " + telemetrySchemaId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to delete TelemetrySchema with id " + telemetrySchemaId );

			throw e;
		}
	}

	/**
	 * get all TelemetrySchemas.
	 */
	protected List<TelemetrySchema> getAll() throws Throwable {    
		LOGGER.info( "======== GETALL ======== ");
		LOGGER.info( "-- Retrieving Collection of TelemetrySchemas:" );

		StringBuilder msg = new StringBuilder( "-- Failed to get all TelemetrySchema : " );        
		List<TelemetrySchema> collection  = new ArrayList<>();

		try {
			// call the static get method on the TelemetrySchemaService
			collection = TelemetrySchemaService.getTelemetrySchemaInstance().getAllTelemetrySchema();
			assertNotNull( collection, "An Empty collection of TelemetrySchema was incorrectly returned.");
			
			// Now print out the values
			TelemetrySchema entity = null;            
			Iterator<TelemetrySchema> iter = collection.iterator();
			int index = 1;

			while( iter.hasNext() ) {
				// Retrieve the entity   
				entity = iter.next();

				assertNotNull( entity,"-- null entity in Collection." );
				assertNotNull( entity.getTelemetrySchemaId(), "-- entity in Collection has a null primary key" );        

				LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
				index++;
			}
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return collection;			
	}

	/**
	 * Assigns a common log handler for each test class in the suite 
	 * in the event log output needs to go elsewhere
	 * 
	 * @param		handler	Handler
	 * @return		TelemetrySchemaTest
	 */
	protected TelemetrySchemaTest setHandler(Handler handler) {
		if ( handler != null )
			LOGGER.addHandler(handler); 
		return this;
	}

	/**
	 * Returns a new populated TelemetrySchema
	 * 
	 * @return CreateTelemetrySchemaCommand alias
	 */
	protected CreateTelemetrySchemaCommand generateNewCommand() {
        CreateTelemetrySchemaCommand command = new CreateTelemetrySchemaCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Uri(),  TelemetryEncoding.values()[0] );
		
		return( command );
	}

		/**
		 * Returns a new populated TelemetrySchema
		 * 
		 * @return UpdateTelemetrySchemaCommand alias
		 */
	protected UpdateTelemetrySchemaCommand generateUpdateCommand() {
	        UpdateTelemetrySchemaCommand command = new UpdateTelemetrySchemaCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Uri(),  new HashSet<>(),  TelemetryEncoding.values()[0] );
			
			return( command );
		}
	//-----------------------------------------------------
	// attributes 
	//-----------------------------------------------------
	protected UUID telemetrySchemaId = null;
	protected TelemetrySchemaSubscriber subscriber = null;
	private final String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
	private final Logger LOGGER = Logger.getLogger(TelemetrySchemaTest.class.getName());
	private String testingStep = "";
	private Integer sizeOfTelemetrySchemaList = 0;
}
