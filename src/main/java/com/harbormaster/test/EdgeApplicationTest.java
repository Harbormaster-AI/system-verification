/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.springframework.util.Assert.state;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;
import com.harbormaster.subscriber.*;

/**
 * <h2>EdgeApplicationTest</h2>
 * <p>
 * Junit test class for entity EdgeApplication.  Handle the full lifecycle as it relates to persistence. Leverages
 * the EdgeApplicationService and a EdgeApplicationSubscriber to test everything under a MVC framework which includes the
 * EdgeApplicationAggregate and related commands, queries, and events.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 * 		EdgeApplicationService
 * 	    EdgeApplicationSubscriber
 *
 * <h3>Methods</h3>
 *      <ul>
 *      <li>read</li>
 *      <li>update</li>
 *      <li>delete</li>
 *      </ul>
 *
 * <p>
 * @author Harbormaster Dev Team
 * </p>
 */
public class EdgeApplicationTest {

    // ------------------------------------
	// default constructor
    // ------------------------------------
	public EdgeApplicationTest() {
		subscriber = new EdgeApplicationSubscriber();
	}

	// test methods
	@Test
	/*
	 * Initiate EdgeApplicationTest.
	 */
	public void startTest() throws Throwable {
		try {
			LOGGER.info("**********************************************************");
			LOGGER.info("Beginning test on EdgeApplication...");
			LOGGER.info("**********************************************************\n");
			
			// ---------------------------------------------
			// jumpstart process
			// ---------------------------------------------
			jumpStart();
			
		} catch (Throwable e) {
			throw e;
		} finally {
		}
	}

	/** 
	 * jumpstart the process by instantiating2 EdgeApplication
	 */
	protected void jumpStart() throws Throwable {
		LOGGER.info( "\n======== create instances to get the ball rolling  ======== ");

		edgeApplicationId = EdgeApplicationService.getEdgeApplicationInstance()
		.createEdgeApplication( generateNewCommand() )
		.get();

		// ---------------------------------------------
		// set up query subscriptions after the 1st create
		// ---------------------------------------------
		testingStep = "create";
		setUpQuerySubscriptions();

		EdgeApplicationService.getEdgeApplicationInstance()
				.createEdgeApplication( generateNewCommand() )
				.get();

	}

	/** 
	 * Set up query subscriptions
	 */
	protected void setUpQuerySubscriptions() throws Throwable {
		LOGGER.info( "\n======== Setting Up Query Subscriptions ======== ");
			
		try {            
			subscriber.edgeApplicationSubscribe().updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetAll update received for EdgeApplication : " + successValue.getEdgeApplicationId());
							  if (successValue.getEdgeApplicationId().equals(edgeApplicationId)) {
								  if (testingStep.equals("create")) {
									  testingStep = "update";
									  update();
								  } else if (testingStep.equals("delete")) {
									  testingStep = "complete";
									  state( getAll().size() == sizeOfEdgeApplicationList - 1 , "value not deleted from list");
									  LOGGER.info("**********************************************************");
									  LOGGER.info("EdgeApplication test completed successfully...");
									  LOGGER.info("**********************************************************\n");
								  }
							  }
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on edgeApplication consumed")
					);
			subscriber.edgeApplicationSubscribe( edgeApplicationId ).updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetOne update received for EdgeApplication : " + successValue.getEdgeApplicationId() + " in step " + testingStep);
							  testingStep = "delete";
							  sizeOfEdgeApplicationList = getAll().size();
							  delete();
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on edgeApplication for edgeApplicationId consumed")

					);
			

			}
			catch (Exception e) {
				LOGGER.warning( e.getMessage() );
				throw e;
			}
		}
		
		/** 
	 * read a EdgeApplication. 
	 */
	protected EdgeApplication read() throws Throwable {
		LOGGER.info( "\n======== READ ======== ");
		LOGGER.info( "-- Reading a previously created EdgeApplication" );

		EdgeApplication entity = null;
		StringBuilder msg = new StringBuilder( "-- Failed to read EdgeApplication with primary key" );
		msg.append( edgeApplicationId );
		
		EdgeApplicationFetchOneSummary fetchOneSummary = new EdgeApplicationFetchOneSummary( edgeApplicationId );

		try {
			entity = EdgeApplicationService.getEdgeApplicationInstance().getEdgeApplication( fetchOneSummary );

			assertNotNull( entity,msg.toString() );

			LOGGER.info( "-- Successfully found EdgeApplication " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return entity;
	}

	/** 
	 * updating a EdgeApplication.
	 */
	protected void update() throws Throwable {
		LOGGER.info( "\n======== UPDATE ======== ");
		LOGGER.info( "-- Attempting to update a EdgeApplication." );

		StringBuilder msg = new StringBuilder( "Failed to update a EdgeApplication : " );        
		EdgeApplication entity = read();
		UpdateEdgeApplicationCommand command = generateUpdateCommand();
		command.setEdgeApplicationId(entity.getEdgeApplicationId());

		try {            
			assertNotNull( entity, msg.toString() );

			LOGGER.info( "-- Now updating the created EdgeApplication." );

			// for use later on...
			edgeApplicationId = entity.getEdgeApplicationId();

			EdgeApplicationService proxy = EdgeApplicationService.getEdgeApplicationInstance();  

			proxy.updateEdgeApplication( command ).get();

			LOGGER.info( "-- Successfully saved EdgeApplication - " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : primarykey = " + edgeApplicationId + " : command -" +  command + " : " + e );

			throw e;
		}
	}

	/** 
	 * delete a EdgeApplication.
	 */
	protected void delete() throws Throwable {
		LOGGER.info( "\n======== DELETE ======== ");
		LOGGER.info( "-- Deleting a previously created EdgeApplication." );

		EdgeApplication entity = null;
		
		try{
		    entity = read(); 
			LOGGER.info( "-- Successfully read EdgeApplication with id " + edgeApplicationId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to read EdgeApplication with id " + edgeApplicationId );

			throw e;
		}

		try{
			EdgeApplicationService.getEdgeApplicationInstance().delete( new DeleteEdgeApplicationCommand( entity.getEdgeApplicationId() ) ).get();
			LOGGER.info( "-- Successfully deleted EdgeApplication with id " + edgeApplicationId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to delete EdgeApplication with id " + edgeApplicationId );

			throw e;
		}
	}

	/**
	 * get all EdgeApplications.
	 */
	protected List<EdgeApplication> getAll() throws Throwable {    
		LOGGER.info( "======== GETALL ======== ");
		LOGGER.info( "-- Retrieving Collection of EdgeApplications:" );

		StringBuilder msg = new StringBuilder( "-- Failed to get all EdgeApplication : " );        
		List<EdgeApplication> collection  = new ArrayList<>();

		try {
			// call the static get method on the EdgeApplicationService
			collection = EdgeApplicationService.getEdgeApplicationInstance().getAllEdgeApplication();
			assertNotNull( collection, "An Empty collection of EdgeApplication was incorrectly returned.");
			
			// Now print out the values
			EdgeApplication entity = null;            
			Iterator<EdgeApplication> iter = collection.iterator();
			int index = 1;

			while( iter.hasNext() ) {
				// Retrieve the entity   
				entity = iter.next();

				assertNotNull( entity,"-- null entity in Collection." );
				assertNotNull( entity.getEdgeApplicationId(), "-- entity in Collection has a null primary key" );        

				LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
				index++;
			}
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return collection;			
	}

	/**
	 * Assigns a common log handler for each test class in the suite 
	 * in the event log output needs to go elsewhere
	 * 
	 * @param		handler	Handler
	 * @return		EdgeApplicationTest
	 */
	protected EdgeApplicationTest setHandler(Handler handler) {
		if ( handler != null )
			LOGGER.addHandler(handler); 
		return this;
	}

	/**
	 * Returns a new populated EdgeApplication
	 * 
	 * @return CreateEdgeApplicationCommand alias
	 */
	protected CreateEdgeApplicationCommand generateNewCommand() {
        CreateEdgeApplicationCommand command = new CreateEdgeApplicationCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  DeploymentStatus.values()[0] );
		
		return( command );
	}

		/**
		 * Returns a new populated EdgeApplication
		 * 
		 * @return UpdateEdgeApplicationCommand alias
		 */
	protected UpdateEdgeApplicationCommand generateUpdateCommand() {
	        UpdateEdgeApplicationCommand command = new UpdateEdgeApplicationCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  null,  DeploymentStatus.values()[0] );
			
			return( command );
		}
	//-----------------------------------------------------
	// attributes 
	//-----------------------------------------------------
	protected UUID edgeApplicationId = null;
	protected EdgeApplicationSubscriber subscriber = null;
	private final String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
	private final Logger LOGGER = Logger.getLogger(EdgeApplicationTest.class.getName());
	private String testingStep = "";
	private Integer sizeOfEdgeApplicationList = 0;
}
