/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.springframework.util.Assert.state;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;
import com.harbormaster.subscriber.*;

/**
 * <h2>TenantUserTest</h2>
 * <p>
 * Junit test class for entity TenantUser.  Handle the full lifecycle as it relates to persistence. Leverages
 * the TenantUserService and a TenantUserSubscriber to test everything under a MVC framework which includes the
 * TenantUserAggregate and related commands, queries, and events.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 * 		TenantUserService
 * 	    TenantUserSubscriber
 *
 * <h3>Methods</h3>
 *      <ul>
 *      <li>read</li>
 *      <li>update</li>
 *      <li>delete</li>
 *      </ul>
 *
 * <p>
 * @author Harbormaster Dev Team
 * </p>
 */
public class TenantUserTest {

    // ------------------------------------
	// default constructor
    // ------------------------------------
	public TenantUserTest() {
		subscriber = new TenantUserSubscriber();
	}

	// test methods
	@Test
	/*
	 * Initiate TenantUserTest.
	 */
	public void startTest() throws Throwable {
		try {
			LOGGER.info("**********************************************************");
			LOGGER.info("Beginning test on TenantUser...");
			LOGGER.info("**********************************************************\n");
			
			// ---------------------------------------------
			// jumpstart process
			// ---------------------------------------------
			jumpStart();
			
		} catch (Throwable e) {
			throw e;
		} finally {
		}
	}

	/** 
	 * jumpstart the process by instantiating2 TenantUser
	 */
	protected void jumpStart() throws Throwable {
		LOGGER.info( "\n======== create instances to get the ball rolling  ======== ");

		tenantUserId = TenantUserService.getTenantUserInstance()
		.createTenantUser( generateNewCommand() )
		.get();

		// ---------------------------------------------
		// set up query subscriptions after the 1st create
		// ---------------------------------------------
		testingStep = "create";
		setUpQuerySubscriptions();

		TenantUserService.getTenantUserInstance()
				.createTenantUser( generateNewCommand() )
				.get();

	}

	/** 
	 * Set up query subscriptions
	 */
	protected void setUpQuerySubscriptions() throws Throwable {
		LOGGER.info( "\n======== Setting Up Query Subscriptions ======== ");
			
		try {            
			subscriber.tenantUserSubscribe().updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetAll update received for TenantUser : " + successValue.getTenantUserId());
							  if (successValue.getTenantUserId().equals(tenantUserId)) {
								  if (testingStep.equals("create")) {
									  testingStep = "update";
									  update();
								  } else if (testingStep.equals("delete")) {
									  testingStep = "complete";
									  state( getAll().size() == sizeOfTenantUserList - 1 , "value not deleted from list");
									  LOGGER.info("**********************************************************");
									  LOGGER.info("TenantUser test completed successfully...");
									  LOGGER.info("**********************************************************\n");
								  }
							  }
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on tenantUser consumed")
					);
			subscriber.tenantUserSubscribe( tenantUserId ).updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetOne update received for TenantUser : " + successValue.getTenantUserId() + " in step " + testingStep);
							  testingStep = "delete";
							  sizeOfTenantUserList = getAll().size();
							  delete();
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on tenantUser for tenantUserId consumed")

					);
			

			}
			catch (Exception e) {
				LOGGER.warning( e.getMessage() );
				throw e;
			}
		}
		
		/** 
	 * read a TenantUser. 
	 */
	protected TenantUser read() throws Throwable {
		LOGGER.info( "\n======== READ ======== ");
		LOGGER.info( "-- Reading a previously created TenantUser" );

		TenantUser entity = null;
		StringBuilder msg = new StringBuilder( "-- Failed to read TenantUser with primary key" );
		msg.append( tenantUserId );
		
		TenantUserFetchOneSummary fetchOneSummary = new TenantUserFetchOneSummary( tenantUserId );

		try {
			entity = TenantUserService.getTenantUserInstance().getTenantUser( fetchOneSummary );

			assertNotNull( entity,msg.toString() );

			LOGGER.info( "-- Successfully found TenantUser " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return entity;
	}

	/** 
	 * updating a TenantUser.
	 */
	protected void update() throws Throwable {
		LOGGER.info( "\n======== UPDATE ======== ");
		LOGGER.info( "-- Attempting to update a TenantUser." );

		StringBuilder msg = new StringBuilder( "Failed to update a TenantUser : " );        
		TenantUser entity = read();
		UpdateTenantUserCommand command = generateUpdateCommand();
		command.setTenantUserId(entity.getTenantUserId());

		try {            
			assertNotNull( entity, msg.toString() );

			LOGGER.info( "-- Now updating the created TenantUser." );

			// for use later on...
			tenantUserId = entity.getTenantUserId();

			TenantUserService proxy = TenantUserService.getTenantUserInstance();  

			proxy.updateTenantUser( command ).get();

			LOGGER.info( "-- Successfully saved TenantUser - " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : primarykey = " + tenantUserId + " : command -" +  command + " : " + e );

			throw e;
		}
	}

	/** 
	 * delete a TenantUser.
	 */
	protected void delete() throws Throwable {
		LOGGER.info( "\n======== DELETE ======== ");
		LOGGER.info( "-- Deleting a previously created TenantUser." );

		TenantUser entity = null;
		
		try{
		    entity = read(); 
			LOGGER.info( "-- Successfully read TenantUser with id " + tenantUserId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to read TenantUser with id " + tenantUserId );

			throw e;
		}

		try{
			TenantUserService.getTenantUserInstance().delete( new DeleteTenantUserCommand( entity.getTenantUserId() ) ).get();
			LOGGER.info( "-- Successfully deleted TenantUser with id " + tenantUserId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to delete TenantUser with id " + tenantUserId );

			throw e;
		}
	}

	/**
	 * get all TenantUsers.
	 */
	protected List<TenantUser> getAll() throws Throwable {    
		LOGGER.info( "======== GETALL ======== ");
		LOGGER.info( "-- Retrieving Collection of TenantUsers:" );

		StringBuilder msg = new StringBuilder( "-- Failed to get all TenantUser : " );        
		List<TenantUser> collection  = new ArrayList<>();

		try {
			// call the static get method on the TenantUserService
			collection = TenantUserService.getTenantUserInstance().getAllTenantUser();
			assertNotNull( collection, "An Empty collection of TenantUser was incorrectly returned.");
			
			// Now print out the values
			TenantUser entity = null;            
			Iterator<TenantUser> iter = collection.iterator();
			int index = 1;

			while( iter.hasNext() ) {
				// Retrieve the entity   
				entity = iter.next();

				assertNotNull( entity,"-- null entity in Collection." );
				assertNotNull( entity.getTenantUserId(), "-- entity in Collection has a null primary key" );        

				LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
				index++;
			}
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return collection;			
	}

	/**
	 * Assigns a common log handler for each test class in the suite 
	 * in the event log output needs to go elsewhere
	 * 
	 * @param		handler	Handler
	 * @return		TenantUserTest
	 */
	protected TenantUserTest setHandler(Handler handler) {
		if ( handler != null )
			LOGGER.addHandler(handler); 
		return this;
	}

	/**
	 * Returns a new populated TenantUser
	 * 
	 * @return CreateTenantUserCommand alias
	 */
	protected CreateTenantUserCommand generateNewCommand() {
        CreateTenantUserCommand command = new CreateTenantUserCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  UserRole.values()[0] );
		
		return( command );
	}

		/**
		 * Returns a new populated TenantUser
		 * 
		 * @return UpdateTenantUserCommand alias
		 */
	protected UpdateTenantUserCommand generateUpdateCommand() {
	        UpdateTenantUserCommand command = new UpdateTenantUserCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  null,  new HashSet<>(),  UserRole.values()[0] );
			
			return( command );
		}
	//-----------------------------------------------------
	// attributes 
	//-----------------------------------------------------
	protected UUID tenantUserId = null;
	protected TenantUserSubscriber subscriber = null;
	private final String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
	private final Logger LOGGER = Logger.getLogger(TenantUserTest.class.getName());
	private String testingStep = "";
	private Integer sizeOfTenantUserList = 0;
}
