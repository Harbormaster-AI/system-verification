/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.springframework.util.Assert.state;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;
import com.harbormaster.subscriber.*;

/**
 * <h2>BuildingTest</h2>
 * <p>
 * Junit test class for entity Building.  Handle the full lifecycle as it relates to persistence. Leverages
 * the BuildingService and a BuildingSubscriber to test everything under a MVC framework which includes the
 * BuildingAggregate and related commands, queries, and events.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 * 		BuildingService
 * 	    BuildingSubscriber
 *
 * <h3>Methods</h3>
 *      <ul>
 *      <li>read</li>
 *      <li>update</li>
 *      <li>delete</li>
 *      </ul>
 *
 * <p>
 * @author Harbormaster Dev Team
 * </p>
 */
public class BuildingTest {

    // ------------------------------------
	// default constructor
    // ------------------------------------
	public BuildingTest() {
		subscriber = new BuildingSubscriber();
	}

	// test methods
	@Test
	/*
	 * Initiate BuildingTest.
	 */
	public void startTest() throws Throwable {
		try {
			LOGGER.info("**********************************************************");
			LOGGER.info("Beginning test on Building...");
			LOGGER.info("**********************************************************\n");
			
			// ---------------------------------------------
			// jumpstart process
			// ---------------------------------------------
			jumpStart();
			
		} catch (Throwable e) {
			throw e;
		} finally {
		}
	}

	/** 
	 * jumpstart the process by instantiating2 Building
	 */
	protected void jumpStart() throws Throwable {
		LOGGER.info( "\n======== create instances to get the ball rolling  ======== ");

		buildingId = BuildingService.getBuildingInstance()
		.createBuilding( generateNewCommand() )
		.get();

		// ---------------------------------------------
		// set up query subscriptions after the 1st create
		// ---------------------------------------------
		testingStep = "create";
		setUpQuerySubscriptions();

		BuildingService.getBuildingInstance()
				.createBuilding( generateNewCommand() )
				.get();

	}

	/** 
	 * Set up query subscriptions
	 */
	protected void setUpQuerySubscriptions() throws Throwable {
		LOGGER.info( "\n======== Setting Up Query Subscriptions ======== ");
			
		try {            
			subscriber.buildingSubscribe().updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetAll update received for Building : " + successValue.getBuildingId());
							  if (successValue.getBuildingId().equals(buildingId)) {
								  if (testingStep.equals("create")) {
									  testingStep = "update";
									  update();
								  } else if (testingStep.equals("delete")) {
									  testingStep = "complete";
									  state( getAll().size() == sizeOfBuildingList - 1 , "value not deleted from list");
									  LOGGER.info("**********************************************************");
									  LOGGER.info("Building test completed successfully...");
									  LOGGER.info("**********************************************************\n");
								  }
							  }
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on building consumed")
					);
			subscriber.buildingSubscribe( buildingId ).updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetOne update received for Building : " + successValue.getBuildingId() + " in step " + testingStep);
							  testingStep = "delete";
							  sizeOfBuildingList = getAll().size();
							  delete();
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on building for buildingId consumed")

					);
			

			}
			catch (Exception e) {
				LOGGER.warning( e.getMessage() );
				throw e;
			}
		}
		
		/** 
	 * read a Building. 
	 */
	protected Building read() throws Throwable {
		LOGGER.info( "\n======== READ ======== ");
		LOGGER.info( "-- Reading a previously created Building" );

		Building entity = null;
		StringBuilder msg = new StringBuilder( "-- Failed to read Building with primary key" );
		msg.append( buildingId );
		
		BuildingFetchOneSummary fetchOneSummary = new BuildingFetchOneSummary( buildingId );

		try {
			entity = BuildingService.getBuildingInstance().getBuilding( fetchOneSummary );

			assertNotNull( entity,msg.toString() );

			LOGGER.info( "-- Successfully found Building " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return entity;
	}

	/** 
	 * updating a Building.
	 */
	protected void update() throws Throwable {
		LOGGER.info( "\n======== UPDATE ======== ");
		LOGGER.info( "-- Attempting to update a Building." );

		StringBuilder msg = new StringBuilder( "Failed to update a Building : " );        
		Building entity = read();
		UpdateBuildingCommand command = generateUpdateCommand();
		command.setBuildingId(entity.getBuildingId());

		try {            
			assertNotNull( entity, msg.toString() );

			LOGGER.info( "-- Now updating the created Building." );

			// for use later on...
			buildingId = entity.getBuildingId();

			BuildingService proxy = BuildingService.getBuildingInstance();  

			proxy.updateBuilding( command ).get();

			LOGGER.info( "-- Successfully saved Building - " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : primarykey = " + buildingId + " : command -" +  command + " : " + e );

			throw e;
		}
	}

	/** 
	 * delete a Building.
	 */
	protected void delete() throws Throwable {
		LOGGER.info( "\n======== DELETE ======== ");
		LOGGER.info( "-- Deleting a previously created Building." );

		Building entity = null;
		
		try{
		    entity = read(); 
			LOGGER.info( "-- Successfully read Building with id " + buildingId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to read Building with id " + buildingId );

			throw e;
		}

		try{
			BuildingService.getBuildingInstance().delete( new DeleteBuildingCommand( entity.getBuildingId() ) ).get();
			LOGGER.info( "-- Successfully deleted Building with id " + buildingId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to delete Building with id " + buildingId );

			throw e;
		}
	}

	/**
	 * get all Buildings.
	 */
	protected List<Building> getAll() throws Throwable {    
		LOGGER.info( "======== GETALL ======== ");
		LOGGER.info( "-- Retrieving Collection of Buildings:" );

		StringBuilder msg = new StringBuilder( "-- Failed to get all Building : " );        
		List<Building> collection  = new ArrayList<>();

		try {
			// call the static get method on the BuildingService
			collection = BuildingService.getBuildingInstance().getAllBuilding();
			assertNotNull( collection, "An Empty collection of Building was incorrectly returned.");
			
			// Now print out the values
			Building entity = null;            
			Iterator<Building> iter = collection.iterator();
			int index = 1;

			while( iter.hasNext() ) {
				// Retrieve the entity   
				entity = iter.next();

				assertNotNull( entity,"-- null entity in Collection." );
				assertNotNull( entity.getBuildingId(), "-- entity in Collection has a null primary key" );        

				LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
				index++;
			}
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return collection;			
	}

	/**
	 * Assigns a common log handler for each test class in the suite 
	 * in the event log output needs to go elsewhere
	 * 
	 * @param		handler	Handler
	 * @return		BuildingTest
	 */
	protected BuildingTest setHandler(Handler handler) {
		if ( handler != null )
			LOGGER.addHandler(handler); 
		return this;
	}

	/**
	 * Returns a new populated Building
	 * 
	 * @return CreateBuildingCommand alias
	 */
	protected CreateBuildingCommand generateNewCommand() {
        CreateBuildingCommand command = new CreateBuildingCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16) );
		
		return( command );
	}

		/**
		 * Returns a new populated Building
		 * 
		 * @return UpdateBuildingCommand alias
		 */
	protected UpdateBuildingCommand generateUpdateCommand() {
	        UpdateBuildingCommand command = new UpdateBuildingCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  null,  new HashSet<>() );
			
			return( command );
		}
	//-----------------------------------------------------
	// attributes 
	//-----------------------------------------------------
	protected UUID buildingId = null;
	protected BuildingSubscriber subscriber = null;
	private final String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
	private final Logger LOGGER = Logger.getLogger(BuildingTest.class.getName());
	private String testingStep = "";
	private Integer sizeOfBuildingList = 0;
}
