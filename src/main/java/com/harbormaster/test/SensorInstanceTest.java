/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.springframework.util.Assert.state;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;
import com.harbormaster.subscriber.*;

/**
 * <h2>SensorInstanceTest</h2>
 * <p>
 * Junit test class for entity SensorInstance.  Handle the full lifecycle as it relates to persistence. Leverages
 * the SensorInstanceService and a SensorInstanceSubscriber to test everything under a MVC framework which includes the
 * SensorInstanceAggregate and related commands, queries, and events.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 * 		SensorInstanceService
 * 	    SensorInstanceSubscriber
 *
 * <h3>Methods</h3>
 *      <ul>
 *      <li>read</li>
 *      <li>update</li>
 *      <li>delete</li>
 *      </ul>
 *
 * <p>
 * @author Harbormaster Dev Team
 * </p>
 */
public class SensorInstanceTest {

    // ------------------------------------
	// default constructor
    // ------------------------------------
	public SensorInstanceTest() {
		subscriber = new SensorInstanceSubscriber();
	}

	// test methods
	@Test
	/*
	 * Initiate SensorInstanceTest.
	 */
	public void startTest() throws Throwable {
		try {
			LOGGER.info("**********************************************************");
			LOGGER.info("Beginning test on SensorInstance...");
			LOGGER.info("**********************************************************\n");
			
			// ---------------------------------------------
			// jumpstart process
			// ---------------------------------------------
			jumpStart();
			
		} catch (Throwable e) {
			throw e;
		} finally {
		}
	}

	/** 
	 * jumpstart the process by instantiating2 SensorInstance
	 */
	protected void jumpStart() throws Throwable {
		LOGGER.info( "\n======== create instances to get the ball rolling  ======== ");

		sensorInstanceId = SensorInstanceService.getSensorInstanceInstance()
		.createSensorInstance( generateNewCommand() )
		.get();

		// ---------------------------------------------
		// set up query subscriptions after the 1st create
		// ---------------------------------------------
		testingStep = "create";
		setUpQuerySubscriptions();

		SensorInstanceService.getSensorInstanceInstance()
				.createSensorInstance( generateNewCommand() )
				.get();

	}

	/** 
	 * Set up query subscriptions
	 */
	protected void setUpQuerySubscriptions() throws Throwable {
		LOGGER.info( "\n======== Setting Up Query Subscriptions ======== ");
			
		try {            
			subscriber.sensorInstanceSubscribe().updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetAll update received for SensorInstance : " + successValue.getSensorInstanceId());
							  if (successValue.getSensorInstanceId().equals(sensorInstanceId)) {
								  if (testingStep.equals("create")) {
									  testingStep = "update";
									  update();
								  } else if (testingStep.equals("delete")) {
									  testingStep = "complete";
									  state( getAll().size() == sizeOfSensorInstanceList - 1 , "value not deleted from list");
									  LOGGER.info("**********************************************************");
									  LOGGER.info("SensorInstance test completed successfully...");
									  LOGGER.info("**********************************************************\n");
								  }
							  }
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on sensorInstance consumed")
					);
			subscriber.sensorInstanceSubscribe( sensorInstanceId ).updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetOne update received for SensorInstance : " + successValue.getSensorInstanceId() + " in step " + testingStep);
							  testingStep = "delete";
							  sizeOfSensorInstanceList = getAll().size();
							  delete();
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on sensorInstance for sensorInstanceId consumed")

					);
			

			}
			catch (Exception e) {
				LOGGER.warning( e.getMessage() );
				throw e;
			}
		}
		
		/** 
	 * read a SensorInstance. 
	 */
	protected SensorInstance read() throws Throwable {
		LOGGER.info( "\n======== READ ======== ");
		LOGGER.info( "-- Reading a previously created SensorInstance" );

		SensorInstance entity = null;
		StringBuilder msg = new StringBuilder( "-- Failed to read SensorInstance with primary key" );
		msg.append( sensorInstanceId );
		
		SensorInstanceFetchOneSummary fetchOneSummary = new SensorInstanceFetchOneSummary( sensorInstanceId );

		try {
			entity = SensorInstanceService.getSensorInstanceInstance().getSensorInstance( fetchOneSummary );

			assertNotNull( entity,msg.toString() );

			LOGGER.info( "-- Successfully found SensorInstance " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return entity;
	}

	/** 
	 * updating a SensorInstance.
	 */
	protected void update() throws Throwable {
		LOGGER.info( "\n======== UPDATE ======== ");
		LOGGER.info( "-- Attempting to update a SensorInstance." );

		StringBuilder msg = new StringBuilder( "Failed to update a SensorInstance : " );        
		SensorInstance entity = read();
		UpdateSensorInstanceCommand command = generateUpdateCommand();
		command.setSensorInstanceId(entity.getSensorInstanceId());

		try {            
			assertNotNull( entity, msg.toString() );

			LOGGER.info( "-- Now updating the created SensorInstance." );

			// for use later on...
			sensorInstanceId = entity.getSensorInstanceId();

			SensorInstanceService proxy = SensorInstanceService.getSensorInstanceInstance();  

			proxy.updateSensorInstance( command ).get();

			LOGGER.info( "-- Successfully saved SensorInstance - " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : primarykey = " + sensorInstanceId + " : command -" +  command + " : " + e );

			throw e;
		}
	}

	/** 
	 * delete a SensorInstance.
	 */
	protected void delete() throws Throwable {
		LOGGER.info( "\n======== DELETE ======== ");
		LOGGER.info( "-- Deleting a previously created SensorInstance." );

		SensorInstance entity = null;
		
		try{
		    entity = read(); 
			LOGGER.info( "-- Successfully read SensorInstance with id " + sensorInstanceId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to read SensorInstance with id " + sensorInstanceId );

			throw e;
		}

		try{
			SensorInstanceService.getSensorInstanceInstance().delete( new DeleteSensorInstanceCommand( entity.getSensorInstanceId() ) ).get();
			LOGGER.info( "-- Successfully deleted SensorInstance with id " + sensorInstanceId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to delete SensorInstance with id " + sensorInstanceId );

			throw e;
		}
	}

	/**
	 * get all SensorInstances.
	 */
	protected List<SensorInstance> getAll() throws Throwable {    
		LOGGER.info( "======== GETALL ======== ");
		LOGGER.info( "-- Retrieving Collection of SensorInstances:" );

		StringBuilder msg = new StringBuilder( "-- Failed to get all SensorInstance : " );        
		List<SensorInstance> collection  = new ArrayList<>();

		try {
			// call the static get method on the SensorInstanceService
			collection = SensorInstanceService.getSensorInstanceInstance().getAllSensorInstance();
			assertNotNull( collection, "An Empty collection of SensorInstance was incorrectly returned.");
			
			// Now print out the values
			SensorInstance entity = null;            
			Iterator<SensorInstance> iter = collection.iterator();
			int index = 1;

			while( iter.hasNext() ) {
				// Retrieve the entity   
				entity = iter.next();

				assertNotNull( entity,"-- null entity in Collection." );
				assertNotNull( entity.getSensorInstanceId(), "-- entity in Collection has a null primary key" );        

				LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
				index++;
			}
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return collection;			
	}

	/**
	 * Assigns a common log handler for each test class in the suite 
	 * in the event log output needs to go elsewhere
	 * 
	 * @param		handler	Handler
	 * @return		SensorInstanceTest
	 */
	protected SensorInstanceTest setHandler(Handler handler) {
		if ( handler != null )
			LOGGER.addHandler(handler); 
		return this;
	}

	/**
	 * Returns a new populated SensorInstance
	 * 
	 * @return CreateSensorInstanceCommand alias
	 */
	protected CreateSensorInstanceCommand generateNewCommand() {
        CreateSensorInstanceCommand command = new CreateSensorInstanceCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  0,  SensorType.values()[0] );
		
		return( command );
	}

		/**
		 * Returns a new populated SensorInstance
		 * 
		 * @return UpdateSensorInstanceCommand alias
		 */
	protected UpdateSensorInstanceCommand generateUpdateCommand() {
	        UpdateSensorInstanceCommand command = new UpdateSensorInstanceCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  0,  null,  new HashSet<>(),  SensorType.values()[0] );
			
			return( command );
		}
	//-----------------------------------------------------
	// attributes 
	//-----------------------------------------------------
	protected UUID sensorInstanceId = null;
	protected SensorInstanceSubscriber subscriber = null;
	private final String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
	private final Logger LOGGER = Logger.getLogger(SensorInstanceTest.class.getName());
	private String testingStep = "";
	private Integer sizeOfSensorInstanceList = 0;
}
