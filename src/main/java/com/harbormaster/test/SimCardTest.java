/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.springframework.util.Assert.state;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;
import com.harbormaster.subscriber.*;

/**
 * <h2>SimCardTest</h2>
 * <p>
 * Junit test class for entity SimCard.  Handle the full lifecycle as it relates to persistence. Leverages
 * the SimCardService and a SimCardSubscriber to test everything under a MVC framework which includes the
 * SimCardAggregate and related commands, queries, and events.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 * 		SimCardService
 * 	    SimCardSubscriber
 *
 * <h3>Methods</h3>
 *      <ul>
 *      <li>read</li>
 *      <li>update</li>
 *      <li>delete</li>
 *      </ul>
 *
 * <p>
 * @author Harbormaster Dev Team
 * </p>
 */
public class SimCardTest {

    // ------------------------------------
	// default constructor
    // ------------------------------------
	public SimCardTest() {
		subscriber = new SimCardSubscriber();
	}

	// test methods
	@Test
	/*
	 * Initiate SimCardTest.
	 */
	public void startTest() throws Throwable {
		try {
			LOGGER.info("**********************************************************");
			LOGGER.info("Beginning test on SimCard...");
			LOGGER.info("**********************************************************\n");
			
			// ---------------------------------------------
			// jumpstart process
			// ---------------------------------------------
			jumpStart();
			
		} catch (Throwable e) {
			throw e;
		} finally {
		}
	}

	/** 
	 * jumpstart the process by instantiating2 SimCard
	 */
	protected void jumpStart() throws Throwable {
		LOGGER.info( "\n======== create instances to get the ball rolling  ======== ");

		simCardId = SimCardService.getSimCardInstance()
		.createSimCard( generateNewCommand() )
		.get();

		// ---------------------------------------------
		// set up query subscriptions after the 1st create
		// ---------------------------------------------
		testingStep = "create";
		setUpQuerySubscriptions();

		SimCardService.getSimCardInstance()
				.createSimCard( generateNewCommand() )
				.get();

	}

	/** 
	 * Set up query subscriptions
	 */
	protected void setUpQuerySubscriptions() throws Throwable {
		LOGGER.info( "\n======== Setting Up Query Subscriptions ======== ");
			
		try {            
			subscriber.simCardSubscribe().updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetAll update received for SimCard : " + successValue.getSimCardId());
							  if (successValue.getSimCardId().equals(simCardId)) {
								  if (testingStep.equals("create")) {
									  testingStep = "update";
									  update();
								  } else if (testingStep.equals("delete")) {
									  testingStep = "complete";
									  state( getAll().size() == sizeOfSimCardList - 1 , "value not deleted from list");
									  LOGGER.info("**********************************************************");
									  LOGGER.info("SimCard test completed successfully...");
									  LOGGER.info("**********************************************************\n");
								  }
							  }
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on simCard consumed")
					);
			subscriber.simCardSubscribe( simCardId ).updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetOne update received for SimCard : " + successValue.getSimCardId() + " in step " + testingStep);
							  testingStep = "delete";
							  sizeOfSimCardList = getAll().size();
							  delete();
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on simCard for simCardId consumed")

					);
			

			}
			catch (Exception e) {
				LOGGER.warning( e.getMessage() );
				throw e;
			}
		}
		
		/** 
	 * read a SimCard. 
	 */
	protected SimCard read() throws Throwable {
		LOGGER.info( "\n======== READ ======== ");
		LOGGER.info( "-- Reading a previously created SimCard" );

		SimCard entity = null;
		StringBuilder msg = new StringBuilder( "-- Failed to read SimCard with primary key" );
		msg.append( simCardId );
		
		SimCardFetchOneSummary fetchOneSummary = new SimCardFetchOneSummary( simCardId );

		try {
			entity = SimCardService.getSimCardInstance().getSimCard( fetchOneSummary );

			assertNotNull( entity,msg.toString() );

			LOGGER.info( "-- Successfully found SimCard " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return entity;
	}

	/** 
	 * updating a SimCard.
	 */
	protected void update() throws Throwable {
		LOGGER.info( "\n======== UPDATE ======== ");
		LOGGER.info( "-- Attempting to update a SimCard." );

		StringBuilder msg = new StringBuilder( "Failed to update a SimCard : " );        
		SimCard entity = read();
		UpdateSimCardCommand command = generateUpdateCommand();
		command.setSimCardId(entity.getSimCardId());

		try {            
			assertNotNull( entity, msg.toString() );

			LOGGER.info( "-- Now updating the created SimCard." );

			// for use later on...
			simCardId = entity.getSimCardId();

			SimCardService proxy = SimCardService.getSimCardInstance();  

			proxy.updateSimCard( command ).get();

			LOGGER.info( "-- Successfully saved SimCard - " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : primarykey = " + simCardId + " : command -" +  command + " : " + e );

			throw e;
		}
	}

	/** 
	 * delete a SimCard.
	 */
	protected void delete() throws Throwable {
		LOGGER.info( "\n======== DELETE ======== ");
		LOGGER.info( "-- Deleting a previously created SimCard." );

		SimCard entity = null;
		
		try{
		    entity = read(); 
			LOGGER.info( "-- Successfully read SimCard with id " + simCardId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to read SimCard with id " + simCardId );

			throw e;
		}

		try{
			SimCardService.getSimCardInstance().delete( new DeleteSimCardCommand( entity.getSimCardId() ) ).get();
			LOGGER.info( "-- Successfully deleted SimCard with id " + simCardId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to delete SimCard with id " + simCardId );

			throw e;
		}
	}

	/**
	 * get all SimCards.
	 */
	protected List<SimCard> getAll() throws Throwable {    
		LOGGER.info( "======== GETALL ======== ");
		LOGGER.info( "-- Retrieving Collection of SimCards:" );

		StringBuilder msg = new StringBuilder( "-- Failed to get all SimCard : " );        
		List<SimCard> collection  = new ArrayList<>();

		try {
			// call the static get method on the SimCardService
			collection = SimCardService.getSimCardInstance().getAllSimCard();
			assertNotNull( collection, "An Empty collection of SimCard was incorrectly returned.");
			
			// Now print out the values
			SimCard entity = null;            
			Iterator<SimCard> iter = collection.iterator();
			int index = 1;

			while( iter.hasNext() ) {
				// Retrieve the entity   
				entity = iter.next();

				assertNotNull( entity,"-- null entity in Collection." );
				assertNotNull( entity.getSimCardId(), "-- entity in Collection has a null primary key" );        

				LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
				index++;
			}
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return collection;			
	}

	/**
	 * Assigns a common log handler for each test class in the suite 
	 * in the event log output needs to go elsewhere
	 * 
	 * @param		handler	Handler
	 * @return		SimCardTest
	 */
	protected SimCardTest setHandler(Handler handler) {
		if ( handler != null )
			LOGGER.addHandler(handler); 
		return this;
	}

	/**
	 * Returns a new populated SimCard
	 * 
	 * @return CreateSimCardCommand alias
	 */
	protected CreateSimCardCommand generateNewCommand() {
        CreateSimCardCommand command = new CreateSimCardCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  SimStatus.values()[0] );
		
		return( command );
	}

		/**
		 * Returns a new populated SimCard
		 * 
		 * @return UpdateSimCardCommand alias
		 */
	protected UpdateSimCardCommand generateUpdateCommand() {
	        UpdateSimCardCommand command = new UpdateSimCardCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new HashSet<>(),  null,  null,  SimStatus.values()[0] );
			
			return( command );
		}
	//-----------------------------------------------------
	// attributes 
	//-----------------------------------------------------
	protected UUID simCardId = null;
	protected SimCardSubscriber subscriber = null;
	private final String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
	private final Logger LOGGER = Logger.getLogger(SimCardTest.class.getName());
	private String testingStep = "";
	private Integer sizeOfSimCardList = 0;
}
