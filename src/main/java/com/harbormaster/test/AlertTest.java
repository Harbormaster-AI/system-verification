/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Axon4-Framework-Server
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 *  Confidential
 *
 * 2026 
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *        - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.springframework.util.Assert.state;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;
import com.harbormaster.subscriber.*;

/**
 * <h2>AlertTest</h2>
 * <p>
 * Junit test class for entity Alert.  Handle the full lifecycle as it relates to persistence. Leverages
 * the AlertService and a AlertSubscriber to test everything under a MVC framework which includes the
 * AlertAggregate and related commands, queries, and events.
 * </p>
  * Generated By Harbormaster
 *
 * Generated From
 *
 *      <h3>Blueprint</h3>
 * 			<table>
 *          <tr><td>name</td><td>Axon4-Framework-Server</td></tr>
 *          <tr><td>published</td><td>09/14/2026</td></tr>
 *          <tr><td>design pattern</td><td>CQRS</td></tr>
 *          <tr><td>architecture style</td><td>EventDrivenArchitecture</td></tr>
 *          </table>
 *
 *      <h3>Domain Model</h3>
 *          <table>
 *          <tr><td>name</td><td>IoT Industry Domain Model</td></tr>
 *          <tr><td>description</td><td>HCIDL model capturing core IoT device, connectivity, telemetry, twin, command, security, and operations capabilities.</td></tr>
 *          <tr><td>industry</td><td>Internet of Things</td></tr>
 *          <tr><td>version</td><td>1.0.0</td></tr>
 *          <table>
 *
 * <h3>Services</h3>
 * 		AlertService
 * 	    AlertSubscriber
 *
 * <h3>Methods</h3>
 *      <ul>
 *      <li>read</li>
 *      <li>update</li>
 *      <li>delete</li>
 *      </ul>
 *
 * <p>
 * @author Harbormaster Dev Team
 * </p>
 */
public class AlertTest {

    // ------------------------------------
	// default constructor
    // ------------------------------------
	public AlertTest() {
		subscriber = new AlertSubscriber();
	}

	// test methods
	@Test
	/*
	 * Initiate AlertTest.
	 */
	public void startTest() throws Throwable {
		try {
			LOGGER.info("**********************************************************");
			LOGGER.info("Beginning test on Alert...");
			LOGGER.info("**********************************************************\n");
			
			// ---------------------------------------------
			// jumpstart process
			// ---------------------------------------------
			jumpStart();
			
		} catch (Throwable e) {
			throw e;
		} finally {
		}
	}

	/** 
	 * jumpstart the process by instantiating2 Alert
	 */
	protected void jumpStart() throws Throwable {
		LOGGER.info( "\n======== create instances to get the ball rolling  ======== ");

		alertId = AlertService.getAlertInstance()
		.createAlert( generateNewCommand() )
		.get();

		// ---------------------------------------------
		// set up query subscriptions after the 1st create
		// ---------------------------------------------
		testingStep = "create";
		setUpQuerySubscriptions();

		AlertService.getAlertInstance()
				.createAlert( generateNewCommand() )
				.get();

	}

	/** 
	 * Set up query subscriptions
	 */
	protected void setUpQuerySubscriptions() throws Throwable {
		LOGGER.info( "\n======== Setting Up Query Subscriptions ======== ");
			
		try {            
			subscriber.alertSubscribe().updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetAll update received for Alert : " + successValue.getAlertId());
							  if (successValue.getAlertId().equals(alertId)) {
								  if (testingStep.equals("create")) {
									  testingStep = "update";
									  update();
								  } else if (testingStep.equals("delete")) {
									  testingStep = "complete";
									  state( getAll().size() == sizeOfAlertList - 1 , "value not deleted from list");
									  LOGGER.info("**********************************************************");
									  LOGGER.info("Alert test completed successfully...");
									  LOGGER.info("**********************************************************\n");
								  }
							  }
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on alert consumed")
					);
			subscriber.alertSubscribe( alertId ).updates().subscribe(
					  successValue -> {
						  LOGGER.info(successValue.toString());
						  try {
							  LOGGER.info("GetOne update received for Alert : " + successValue.getAlertId() + " in step " + testingStep);
							  testingStep = "delete";
							  sizeOfAlertList = getAll().size();
							  delete();
						  } catch( Throwable exc ) {
							  LOGGER.warning( exc.getMessage() );
						  }
					  },
					  error -> LOGGER.warning(error.getMessage()),
					  () -> LOGGER.info("Subscription on alert for alertId consumed")

					);
			

			}
			catch (Exception e) {
				LOGGER.warning( e.getMessage() );
				throw e;
			}
		}
		
		/** 
	 * read a Alert. 
	 */
	protected Alert read() throws Throwable {
		LOGGER.info( "\n======== READ ======== ");
		LOGGER.info( "-- Reading a previously created Alert" );

		Alert entity = null;
		StringBuilder msg = new StringBuilder( "-- Failed to read Alert with primary key" );
		msg.append( alertId );
		
		AlertFetchOneSummary fetchOneSummary = new AlertFetchOneSummary( alertId );

		try {
			entity = AlertService.getAlertInstance().getAlert( fetchOneSummary );

			assertNotNull( entity,msg.toString() );

			LOGGER.info( "-- Successfully found Alert " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return entity;
	}

	/** 
	 * updating a Alert.
	 */
	protected void update() throws Throwable {
		LOGGER.info( "\n======== UPDATE ======== ");
		LOGGER.info( "-- Attempting to update a Alert." );

		StringBuilder msg = new StringBuilder( "Failed to update a Alert : " );        
		Alert entity = read();
		UpdateAlertCommand command = generateUpdateCommand();
		command.setAlertId(entity.getAlertId());

		try {            
			assertNotNull( entity, msg.toString() );

			LOGGER.info( "-- Now updating the created Alert." );

			// for use later on...
			alertId = entity.getAlertId();

			AlertService proxy = AlertService.getAlertInstance();  

			proxy.updateAlert( command ).get();

			LOGGER.info( "-- Successfully saved Alert - " + entity.toString() );
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : primarykey = " + alertId + " : command -" +  command + " : " + e );

			throw e;
		}
	}

	/** 
	 * delete a Alert.
	 */
	protected void delete() throws Throwable {
		LOGGER.info( "\n======== DELETE ======== ");
		LOGGER.info( "-- Deleting a previously created Alert." );

		Alert entity = null;
		
		try{
		    entity = read(); 
			LOGGER.info( "-- Successfully read Alert with id " + alertId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to read Alert with id " + alertId );

			throw e;
		}

		try{
			AlertService.getAlertInstance().delete( new DeleteAlertCommand( entity.getAlertId() ) ).get();
			LOGGER.info( "-- Successfully deleted Alert with id " + alertId );            
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( "-- Failed to delete Alert with id " + alertId );

			throw e;
		}
	}

	/**
	 * get all Alerts.
	 */
	protected List<Alert> getAll() throws Throwable {    
		LOGGER.info( "======== GETALL ======== ");
		LOGGER.info( "-- Retrieving Collection of Alerts:" );

		StringBuilder msg = new StringBuilder( "-- Failed to get all Alert : " );        
		List<Alert> collection  = new ArrayList<>();

		try {
			// call the static get method on the AlertService
			collection = AlertService.getAlertInstance().getAllAlert();
			assertNotNull( collection, "An Empty collection of Alert was incorrectly returned.");
			
			// Now print out the values
			Alert entity = null;            
			Iterator<Alert> iter = collection.iterator();
			int index = 1;

			while( iter.hasNext() ) {
				// Retrieve the entity   
				entity = iter.next();

				assertNotNull( entity,"-- null entity in Collection." );
				assertNotNull( entity.getAlertId(), "-- entity in Collection has a null primary key" );        

				LOGGER.info( " - " + String.valueOf(index) + ". " + entity.toString() );
				index++;
			}
		}
		catch ( Throwable e ) {
			LOGGER.warning( unexpectedErrorMsg );
			LOGGER.warning( msg.toString() + " : " + e );

			throw e;
		}
		
		return collection;			
	}

	/**
	 * Assigns a common log handler for each test class in the suite 
	 * in the event log output needs to go elsewhere
	 * 
	 * @param		handler	Handler
	 * @return		AlertTest
	 */
	protected AlertTest setHandler(Handler handler) {
		if ( handler != null )
			LOGGER.addHandler(handler); 
		return this;
	}

	/**
	 * Returns a new populated Alert
	 * 
	 * @return CreateAlertCommand alias
	 */
	protected CreateAlertCommand generateNewCommand() {
        CreateAlertCommand command = new CreateAlertCommand( null,   new Date(),   new Date(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  AlertStatus.values()[0] );
		
		return( command );
	}

		/**
		 * Returns a new populated Alert
		 * 
		 * @return UpdateAlertCommand alias
		 */
	protected UpdateAlertCommand generateUpdateCommand() {
	        UpdateAlertCommand command = new UpdateAlertCommand( null,   new Date(),   new Date(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  null,  null,  AlertStatus.values()[0] );
			
			return( command );
		}
	//-----------------------------------------------------
	// attributes 
	//-----------------------------------------------------
	protected UUID alertId = null;
	protected AlertSubscriber subscriber = null;
	private final String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
	private final Logger LOGGER = Logger.getLogger(AlertTest.class.getName());
	private String testingStep = "";
	private Integer sizeOfAlertList = 0;
}
