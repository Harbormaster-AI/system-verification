
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test MaintenanceTicket class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class MaintenanceTicketTest
{

// constructors

    public MaintenanceTicketTest(MaintenanceTicketService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a MaintenanceTicket, through a MaintenanceTicketTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on MaintenanceTicketTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on MaintenanceTicketTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new MaintenanceTicket.
     *
     * @return    MaintenanceTicket
     */
    public MaintenanceTicket testCreate() throws Throwable {
    MaintenanceTicket entity = null;

    LOGGER.info( "MaintenanceTicketTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a MaintenanceTicket");

    StringBuilder msg = new StringBuilder( "-- Failed to create a MaintenanceTicket" );

    try {
        entity = service.createMaintenanceTicket( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getMaintenanceTicketId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a MaintenanceTicket with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a MaintenanceTicket.
     *
     * @return    MaintenanceTicket
     */
    public MaintenanceTicket testRead() throws Throwable {
    LOGGER.info( "MaintenanceTicketTest:testRead()" );
    LOGGER.info( "-- Reading a previously created MaintenanceTicket" );

    MaintenanceTicket entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read MaintenanceTicket with primary key" );
    msg.append( theId );

    try {
        entity = service.getMaintenanceTicket( new MaintenanceTicketFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getMaintenanceTicketId();

        LOGGER.info( "-- Successfully found MaintenanceTicket " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a MaintenanceTicket.
     *
     * @return    MaintenanceTicket
     */
    public MaintenanceTicket testUpdate() throws Throwable {
    LOGGER.info( "MaintenanceTicketTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a MaintenanceTicket." );

    StringBuilder msg = new StringBuilder( "Failed to update a MaintenanceTicket : " );
    MaintenanceTicket entity = null;

    try {
        UpdateMaintenanceTicketCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setMaintenanceTicketId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created MaintenanceTicket." );

        entity = service.updateMaintenanceTicket( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved MaintenanceTicket - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a MaintenanceTicket.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "MaintenanceTicketTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created MaintenanceTicket." );

    try {
        DeleteMaintenanceTicketCommand deleteCommand = new DeleteMaintenanceTicketCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted MaintenanceTicket with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete MaintenanceTicket with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all MaintenanceTickets.
     *
     * @return    Collection
     */
    public List<MaintenanceTicket> testGetAll() throws Throwable {
    LOGGER.info( "MaintenanceTicketTest:testGetAll() - Retrieving Collection of MaintenanceTickets:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all MaintenanceTicket : " );
    List<MaintenanceTicket> collection  = null;

    try {
        // call the static get method on the MaintenanceTicketService
        collection = service.getAllMaintenanceTicket();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            MaintenanceTicket currEntity  = null;
            Iterator<MaintenanceTicket> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getMaintenanceTicketId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public MaintenanceTicketTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated MaintenanceTicket
     *
     * @return CreateMaintenanceTicketCommand alias
     */
	protected CreateMaintenanceTicketCommand generateNewCommand() {
    CreateMaintenanceTicketCommand command = new CreateMaintenanceTicketCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),   new Date(),   new Date(),  MaintenancePriority.values()[0],  MaintenanceStatus.values()[0] );

    return( command );
}

    /**
     * Returns a new populated MaintenanceTicket
     *
     * @return UpdateMaintenanceTicketCommand alias
     */
	protected UpdateMaintenanceTicketCommand generateUpdateCommand() {
    UpdateMaintenanceTicketCommand command = new UpdateMaintenanceTicketCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),   new Date(),   new Date(),  null,  null,  MaintenancePriority.values()[0],  MaintenanceStatus.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected MaintenanceTicketService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(MaintenanceTicket.class.getName());

}
