/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Insurance Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test Policy class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class PolicyTest
{

// constructors

    public PolicyTest(PolicyService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a Policy, through a PolicyTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on PolicyTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on PolicyTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new Policy.
     *
     * @return    Policy
     */
    public Policy testCreate() throws Throwable {
    Policy entity = null;

    LOGGER.info( "PolicyTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a Policy");

    StringBuilder msg = new StringBuilder( "-- Failed to create a Policy" );

    try {
        entity = service.createPolicy( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getPolicyId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a Policy with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a Policy.
     *
     * @return    Policy
     */
    public Policy testRead() throws Throwable {
    LOGGER.info( "PolicyTest:testRead()" );
    LOGGER.info( "-- Reading a previously created Policy" );

    Policy entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read Policy with primary key" );
    msg.append( theId );

    try {
        entity = service.getPolicy( new PolicyFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getPolicyId();

        LOGGER.info( "-- Successfully found Policy " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a Policy.
     *
     * @return    Policy
     */
    public Policy testUpdate() throws Throwable {
    LOGGER.info( "PolicyTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a Policy." );

    StringBuilder msg = new StringBuilder( "Failed to update a Policy : " );
    Policy entity = null;

    try {
        UpdatePolicyCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setPolicyId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created Policy." );

        entity = service.updatePolicy( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved Policy - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a Policy.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "PolicyTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created Policy." );

    try {
        DeletePolicyCommand deleteCommand = new DeletePolicyCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted Policy with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete Policy with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all Policys.
     *
     * @return    Collection
     */
    public List<Policy> testGetAll() throws Throwable {
    LOGGER.info( "PolicyTest:testGetAll() - Retrieving Collection of Policys:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all Policy : " );
    List<Policy> collection  = null;

    try {
        // call the static get method on the PolicyService
        collection = service.getAllPolicy();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            Policy currEntity  = null;
            Iterator<Policy> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getPolicyId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public PolicyTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated Policy
     *
     * @return CreatePolicyCommand alias
     */
	protected CreatePolicyCommand generateNewCommand() {
    CreatePolicyCommand command = new CreatePolicyCommand( null,  new PolicyNumber(),  new DateRange(),  new Money(),  PolicyStatus.values()[0],  PaymentPlanType.values()[0] );

    return( command );
}

    /**
     * Returns a new populated Policy
     *
     * @return UpdatePolicyCommand alias
     */
	protected UpdatePolicyCommand generateUpdateCommand() {
    UpdatePolicyCommand command = new UpdatePolicyCommand( null,  new PolicyNumber(),  new DateRange(),  new Money(),  null,  null,  null,  null,  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  null,  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  PolicyStatus.values()[0],  PaymentPlanType.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected PolicyService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(Policy.class.getName());

}
