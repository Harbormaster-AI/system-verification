/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Insurance Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test Agent class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class AgentTest
{

// constructors

    public AgentTest(AgentService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a Agent, through a AgentTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on AgentTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on AgentTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new Agent.
     *
     * @return    Agent
     */
    public Agent testCreate() throws Throwable {
    Agent entity = null;

    LOGGER.info( "AgentTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a Agent");

    StringBuilder msg = new StringBuilder( "-- Failed to create a Agent" );

    try {
        entity = service.createAgent( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getAgentId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a Agent with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a Agent.
     *
     * @return    Agent
     */
    public Agent testRead() throws Throwable {
    LOGGER.info( "AgentTest:testRead()" );
    LOGGER.info( "-- Reading a previously created Agent" );

    Agent entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read Agent with primary key" );
    msg.append( theId );

    try {
        entity = service.getAgent( new AgentFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getAgentId();

        LOGGER.info( "-- Successfully found Agent " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a Agent.
     *
     * @return    Agent
     */
    public Agent testUpdate() throws Throwable {
    LOGGER.info( "AgentTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a Agent." );

    StringBuilder msg = new StringBuilder( "Failed to update a Agent : " );
    Agent entity = null;

    try {
        UpdateAgentCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setAgentId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created Agent." );

        entity = service.updateAgent( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved Agent - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a Agent.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "AgentTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created Agent." );

    try {
        DeleteAgentCommand deleteCommand = new DeleteAgentCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted Agent with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete Agent with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all Agents.
     *
     * @return    Collection
     */
    public List<Agent> testGetAll() throws Throwable {
    LOGGER.info( "AgentTest:testGetAll() - Retrieving Collection of Agents:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all Agent : " );
    List<Agent> collection  = null;

    try {
        // call the static get method on the AgentService
        collection = service.getAllAgent();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            Agent currEntity  = null;
            Iterator<Agent> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getAgentId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public AgentTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated Agent
     *
     * @return CreateAgentCommand alias
     */
	protected CreateAgentCommand generateNewCommand() {
    CreateAgentCommand command = new CreateAgentCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  ProducerStatus.values()[0] );

    return( command );
}

    /**
     * Returns a new populated Agent
     *
     * @return UpdateAgentCommand alias
     */
	protected UpdateAgentCommand generateUpdateCommand() {
    UpdateAgentCommand command = new UpdateAgentCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  null,  new HashSet<>(),  new HashSet<>(),  ProducerStatus.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected AgentService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(Agent.class.getName());

}
