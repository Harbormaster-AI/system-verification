
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test CommandDefinition class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class CommandDefinitionTest
{

// constructors

    public CommandDefinitionTest(CommandDefinitionService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a CommandDefinition, through a CommandDefinitionTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on CommandDefinitionTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on CommandDefinitionTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new CommandDefinition.
     *
     * @return    CommandDefinition
     */
    public CommandDefinition testCreate() throws Throwable {
    CommandDefinition entity = null;

    LOGGER.info( "CommandDefinitionTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a CommandDefinition");

    StringBuilder msg = new StringBuilder( "-- Failed to create a CommandDefinition" );

    try {
        entity = service.createCommandDefinition( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getCommandDefinitionId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a CommandDefinition with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a CommandDefinition.
     *
     * @return    CommandDefinition
     */
    public CommandDefinition testRead() throws Throwable {
    LOGGER.info( "CommandDefinitionTest:testRead()" );
    LOGGER.info( "-- Reading a previously created CommandDefinition" );

    CommandDefinition entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read CommandDefinition with primary key" );
    msg.append( theId );

    try {
        entity = service.getCommandDefinition( new CommandDefinitionFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getCommandDefinitionId();

        LOGGER.info( "-- Successfully found CommandDefinition " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a CommandDefinition.
     *
     * @return    CommandDefinition
     */
    public CommandDefinition testUpdate() throws Throwable {
    LOGGER.info( "CommandDefinitionTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a CommandDefinition." );

    StringBuilder msg = new StringBuilder( "Failed to update a CommandDefinition : " );
    CommandDefinition entity = null;

    try {
        UpdateCommandDefinitionCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setCommandDefinitionId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created CommandDefinition." );

        entity = service.updateCommandDefinition( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved CommandDefinition - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a CommandDefinition.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "CommandDefinitionTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created CommandDefinition." );

    try {
        DeleteCommandDefinitionCommand deleteCommand = new DeleteCommandDefinitionCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted CommandDefinition with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete CommandDefinition with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all CommandDefinitions.
     *
     * @return    Collection
     */
    public List<CommandDefinition> testGetAll() throws Throwable {
    LOGGER.info( "CommandDefinitionTest:testGetAll() - Retrieving Collection of CommandDefinitions:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all CommandDefinition : " );
    List<CommandDefinition> collection  = null;

    try {
        // call the static get method on the CommandDefinitionService
        collection = service.getAllCommandDefinition();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            CommandDefinition currEntity  = null;
            Iterator<CommandDefinition> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getCommandDefinitionId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public CommandDefinitionTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated CommandDefinition
     *
     * @return CreateCommandDefinitionCommand alias
     */
	protected CreateCommandDefinitionCommand generateNewCommand() {
    CreateCommandDefinitionCommand command = new CreateCommandDefinitionCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Uri(),  new Uri(),  0 );

    return( command );
}

    /**
     * Returns a new populated CommandDefinition
     *
     * @return UpdateCommandDefinitionCommand alias
     */
	protected UpdateCommandDefinitionCommand generateUpdateCommand() {
    UpdateCommandDefinitionCommand command = new UpdateCommandDefinitionCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Uri(),  new Uri(),  0,  null,  new HashSet<>(),  new HashSet<>() );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected CommandDefinitionService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(CommandDefinition.class.getName());

}
