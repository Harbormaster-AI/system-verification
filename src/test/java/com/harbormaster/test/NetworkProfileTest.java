
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test NetworkProfile class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class NetworkProfileTest
{

// constructors

    public NetworkProfileTest(NetworkProfileService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a NetworkProfile, through a NetworkProfileTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on NetworkProfileTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on NetworkProfileTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new NetworkProfile.
     *
     * @return    NetworkProfile
     */
    public NetworkProfile testCreate() throws Throwable {
    NetworkProfile entity = null;

    LOGGER.info( "NetworkProfileTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a NetworkProfile");

    StringBuilder msg = new StringBuilder( "-- Failed to create a NetworkProfile" );

    try {
        entity = service.createNetworkProfile( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getNetworkProfileId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a NetworkProfile with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a NetworkProfile.
     *
     * @return    NetworkProfile
     */
    public NetworkProfile testRead() throws Throwable {
    LOGGER.info( "NetworkProfileTest:testRead()" );
    LOGGER.info( "-- Reading a previously created NetworkProfile" );

    NetworkProfile entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read NetworkProfile with primary key" );
    msg.append( theId );

    try {
        entity = service.getNetworkProfile( new NetworkProfileFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getNetworkProfileId();

        LOGGER.info( "-- Successfully found NetworkProfile " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a NetworkProfile.
     *
     * @return    NetworkProfile
     */
    public NetworkProfile testUpdate() throws Throwable {
    LOGGER.info( "NetworkProfileTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a NetworkProfile." );

    StringBuilder msg = new StringBuilder( "Failed to update a NetworkProfile : " );
    NetworkProfile entity = null;

    try {
        UpdateNetworkProfileCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setNetworkProfileId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created NetworkProfile." );

        entity = service.updateNetworkProfile( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved NetworkProfile - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a NetworkProfile.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "NetworkProfileTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created NetworkProfile." );

    try {
        DeleteNetworkProfileCommand deleteCommand = new DeleteNetworkProfileCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted NetworkProfile with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete NetworkProfile with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all NetworkProfiles.
     *
     * @return    Collection
     */
    public List<NetworkProfile> testGetAll() throws Throwable {
    LOGGER.info( "NetworkProfileTest:testGetAll() - Retrieving Collection of NetworkProfiles:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all NetworkProfile : " );
    List<NetworkProfile> collection  = null;

    try {
        // call the static get method on the NetworkProfileService
        collection = service.getAllNetworkProfile();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            NetworkProfile currEntity  = null;
            Iterator<NetworkProfile> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getNetworkProfileId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public NetworkProfileTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated NetworkProfile
     *
     * @return CreateNetworkProfileCommand alias
     */
	protected CreateNetworkProfileCommand generateNewCommand() {
    CreateNetworkProfileCommand command = new CreateNetworkProfileCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  ConnectivityType.values()[0] );

    return( command );
}

    /**
     * Returns a new populated NetworkProfile
     *
     * @return UpdateNetworkProfileCommand alias
     */
	protected UpdateNetworkProfileCommand generateUpdateCommand() {
    UpdateNetworkProfileCommand command = new UpdateNetworkProfileCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  null,  null,  null,  ConnectivityType.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected NetworkProfileService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(NetworkProfile.class.getName());

}
