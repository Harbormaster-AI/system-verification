/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Insurance Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test Incident class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class IncidentTest
{

// constructors

    public IncidentTest(IncidentService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a Incident, through a IncidentTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on IncidentTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on IncidentTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new Incident.
     *
     * @return    Incident
     */
    public Incident testCreate() throws Throwable {
    Incident entity = null;

    LOGGER.info( "IncidentTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a Incident");

    StringBuilder msg = new StringBuilder( "-- Failed to create a Incident" );

    try {
        entity = service.createIncident( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getIncidentId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a Incident with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a Incident.
     *
     * @return    Incident
     */
    public Incident testRead() throws Throwable {
    LOGGER.info( "IncidentTest:testRead()" );
    LOGGER.info( "-- Reading a previously created Incident" );

    Incident entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read Incident with primary key" );
    msg.append( theId );

    try {
        entity = service.getIncident( new IncidentFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getIncidentId();

        LOGGER.info( "-- Successfully found Incident " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a Incident.
     *
     * @return    Incident
     */
    public Incident testUpdate() throws Throwable {
    LOGGER.info( "IncidentTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a Incident." );

    StringBuilder msg = new StringBuilder( "Failed to update a Incident : " );
    Incident entity = null;

    try {
        UpdateIncidentCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setIncidentId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created Incident." );

        entity = service.updateIncident( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved Incident - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a Incident.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "IncidentTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created Incident." );

    try {
        DeleteIncidentCommand deleteCommand = new DeleteIncidentCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted Incident with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete Incident with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all Incidents.
     *
     * @return    Collection
     */
    public List<Incident> testGetAll() throws Throwable {
    LOGGER.info( "IncidentTest:testGetAll() - Retrieving Collection of Incidents:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all Incident : " );
    List<Incident> collection  = null;

    try {
        // call the static get method on the IncidentService
        collection = service.getAllIncident();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            Incident currEntity  = null;
            Iterator<Incident> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getIncidentId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public IncidentTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated Incident
     *
     * @return CreateIncidentCommand alias
     */
	protected CreateIncidentCommand generateNewCommand() {
    CreateIncidentCommand command = new CreateIncidentCommand( null,  new Address(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  PerilType.values()[0] );

    return( command );
}

    /**
     * Returns a new populated Incident
     *
     * @return UpdateIncidentCommand alias
     */
	protected UpdateIncidentCommand generateUpdateCommand() {
    UpdateIncidentCommand command = new UpdateIncidentCommand( null,  new Address(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  null,  new HashSet<>(),  PerilType.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected IncidentService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(Incident.class.getName());

}
