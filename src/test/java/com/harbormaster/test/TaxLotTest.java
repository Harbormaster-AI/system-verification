/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * ${blueprint.getModelMetadata().getName()}
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test TaxLot class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class TaxLotTest
{

// constructors

    public TaxLotTest(TaxLotService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a TaxLot, through a TaxLotTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on TaxLotTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on TaxLotTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new TaxLot.
     *
     * @return    TaxLot
     */
    public TaxLot testCreate() throws Throwable {
    TaxLot entity = null;

    LOGGER.info( "TaxLotTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a TaxLot");

    StringBuilder msg = new StringBuilder( "-- Failed to create a TaxLot" );

    try {
        entity = service.createTaxLot( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getTaxLotId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a TaxLot with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a TaxLot.
     *
     * @return    TaxLot
     */
    public TaxLot testRead() throws Throwable {
    LOGGER.info( "TaxLotTest:testRead()" );
    LOGGER.info( "-- Reading a previously created TaxLot" );

    TaxLot entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read TaxLot with primary key" );
    msg.append( theId );

    try {
        entity = service.getTaxLot( new TaxLotFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getTaxLotId();

        LOGGER.info( "-- Successfully found TaxLot " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a TaxLot.
     *
     * @return    TaxLot
     */
    public TaxLot testUpdate() throws Throwable {
    LOGGER.info( "TaxLotTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a TaxLot." );

    StringBuilder msg = new StringBuilder( "Failed to update a TaxLot : " );
    TaxLot entity = null;

    try {
        UpdateTaxLotCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setTaxLotId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created TaxLot." );

        entity = service.updateTaxLot( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved TaxLot - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a TaxLot.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "TaxLotTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created TaxLot." );

    try {
        DeleteTaxLotCommand deleteCommand = new DeleteTaxLotCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted TaxLot with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete TaxLot with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all TaxLots.
     *
     * @return    Collection
     */
    public List<TaxLot> testGetAll() throws Throwable {
    LOGGER.info( "TaxLotTest:testGetAll() - Retrieving Collection of TaxLots:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all TaxLot : " );
    List<TaxLot> collection  = null;

    try {
        // call the static get method on the TaxLotService
        collection = service.getAllTaxLot();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            TaxLot currEntity  = null;
            Iterator<TaxLot> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getTaxLotId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public TaxLotTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated TaxLot
     *
     * @return CreateTaxLotCommand alias
     */
	protected CreateTaxLotCommand generateNewCommand() {
    CreateTaxLotCommand command = new CreateTaxLotCommand( null,   new Date(),  java.math.BigDecimal.ZERO,  java.math.BigDecimal.ZERO );

    return( command );
}

    /**
     * Returns a new populated TaxLot
     *
     * @return UpdateTaxLotCommand alias
     */
	protected UpdateTaxLotCommand generateUpdateCommand() {
    UpdateTaxLotCommand command = new UpdateTaxLotCommand( null,   new Date(),  java.math.BigDecimal.ZERO,  java.math.BigDecimal.ZERO,  null );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected TaxLotService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(TaxLot.class.getName());

}
