
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test SensorInstance class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class SensorInstanceTest
{

// constructors

    public SensorInstanceTest(SensorInstanceService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a SensorInstance, through a SensorInstanceTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on SensorInstanceTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on SensorInstanceTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new SensorInstance.
     *
     * @return    SensorInstance
     */
    public SensorInstance testCreate() throws Throwable {
    SensorInstance entity = null;

    LOGGER.info( "SensorInstanceTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a SensorInstance");

    StringBuilder msg = new StringBuilder( "-- Failed to create a SensorInstance" );

    try {
        entity = service.createSensorInstance( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getSensorInstanceId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a SensorInstance with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a SensorInstance.
     *
     * @return    SensorInstance
     */
    public SensorInstance testRead() throws Throwable {
    LOGGER.info( "SensorInstanceTest:testRead()" );
    LOGGER.info( "-- Reading a previously created SensorInstance" );

    SensorInstance entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read SensorInstance with primary key" );
    msg.append( theId );

    try {
        entity = service.getSensorInstance( new SensorInstanceFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getSensorInstanceId();

        LOGGER.info( "-- Successfully found SensorInstance " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a SensorInstance.
     *
     * @return    SensorInstance
     */
    public SensorInstance testUpdate() throws Throwable {
    LOGGER.info( "SensorInstanceTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a SensorInstance." );

    StringBuilder msg = new StringBuilder( "Failed to update a SensorInstance : " );
    SensorInstance entity = null;

    try {
        UpdateSensorInstanceCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setSensorInstanceId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created SensorInstance." );

        entity = service.updateSensorInstance( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved SensorInstance - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a SensorInstance.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "SensorInstanceTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created SensorInstance." );

    try {
        DeleteSensorInstanceCommand deleteCommand = new DeleteSensorInstanceCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted SensorInstance with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete SensorInstance with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all SensorInstances.
     *
     * @return    Collection
     */
    public List<SensorInstance> testGetAll() throws Throwable {
    LOGGER.info( "SensorInstanceTest:testGetAll() - Retrieving Collection of SensorInstances:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all SensorInstance : " );
    List<SensorInstance> collection  = null;

    try {
        // call the static get method on the SensorInstanceService
        collection = service.getAllSensorInstance();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            SensorInstance currEntity  = null;
            Iterator<SensorInstance> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getSensorInstanceId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public SensorInstanceTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated SensorInstance
     *
     * @return CreateSensorInstanceCommand alias
     */
	protected CreateSensorInstanceCommand generateNewCommand() {
    CreateSensorInstanceCommand command = new CreateSensorInstanceCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  0,  SensorType.values()[0] );

    return( command );
}

    /**
     * Returns a new populated SensorInstance
     *
     * @return UpdateSensorInstanceCommand alias
     */
	protected UpdateSensorInstanceCommand generateUpdateCommand() {
    UpdateSensorInstanceCommand command = new UpdateSensorInstanceCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  0,  null,  new HashSet<>(),  SensorType.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected SensorInstanceService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(SensorInstance.class.getName());

}
