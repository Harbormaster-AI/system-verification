
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test ActuatorInstance class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class ActuatorInstanceTest
{

// constructors

    public ActuatorInstanceTest(ActuatorInstanceService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a ActuatorInstance, through a ActuatorInstanceTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on ActuatorInstanceTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on ActuatorInstanceTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new ActuatorInstance.
     *
     * @return    ActuatorInstance
     */
    public ActuatorInstance testCreate() throws Throwable {
    ActuatorInstance entity = null;

    LOGGER.info( "ActuatorInstanceTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a ActuatorInstance");

    StringBuilder msg = new StringBuilder( "-- Failed to create a ActuatorInstance" );

    try {
        entity = service.createActuatorInstance( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getActuatorInstanceId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a ActuatorInstance with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a ActuatorInstance.
     *
     * @return    ActuatorInstance
     */
    public ActuatorInstance testRead() throws Throwable {
    LOGGER.info( "ActuatorInstanceTest:testRead()" );
    LOGGER.info( "-- Reading a previously created ActuatorInstance" );

    ActuatorInstance entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read ActuatorInstance with primary key" );
    msg.append( theId );

    try {
        entity = service.getActuatorInstance( new ActuatorInstanceFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getActuatorInstanceId();

        LOGGER.info( "-- Successfully found ActuatorInstance " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a ActuatorInstance.
     *
     * @return    ActuatorInstance
     */
    public ActuatorInstance testUpdate() throws Throwable {
    LOGGER.info( "ActuatorInstanceTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a ActuatorInstance." );

    StringBuilder msg = new StringBuilder( "Failed to update a ActuatorInstance : " );
    ActuatorInstance entity = null;

    try {
        UpdateActuatorInstanceCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setActuatorInstanceId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created ActuatorInstance." );

        entity = service.updateActuatorInstance( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved ActuatorInstance - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a ActuatorInstance.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "ActuatorInstanceTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created ActuatorInstance." );

    try {
        DeleteActuatorInstanceCommand deleteCommand = new DeleteActuatorInstanceCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted ActuatorInstance with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete ActuatorInstance with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all ActuatorInstances.
     *
     * @return    Collection
     */
    public List<ActuatorInstance> testGetAll() throws Throwable {
    LOGGER.info( "ActuatorInstanceTest:testGetAll() - Retrieving Collection of ActuatorInstances:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all ActuatorInstance : " );
    List<ActuatorInstance> collection  = null;

    try {
        // call the static get method on the ActuatorInstanceService
        collection = service.getAllActuatorInstance();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            ActuatorInstance currEntity  = null;
            Iterator<ActuatorInstance> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getActuatorInstanceId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public ActuatorInstanceTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated ActuatorInstance
     *
     * @return CreateActuatorInstanceCommand alias
     */
	protected CreateActuatorInstanceCommand generateNewCommand() {
    CreateActuatorInstanceCommand command = new CreateActuatorInstanceCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new TopicName(),  ActuatorType.values()[0] );

    return( command );
}

    /**
     * Returns a new populated ActuatorInstance
     *
     * @return UpdateActuatorInstanceCommand alias
     */
	protected UpdateActuatorInstanceCommand generateUpdateCommand() {
    UpdateActuatorInstanceCommand command = new UpdateActuatorInstanceCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new TopicName(),  null,  new HashSet<>(),  ActuatorType.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected ActuatorInstanceService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(ActuatorInstance.class.getName());

}
