
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test EdgeApplication class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class EdgeApplicationTest
{

// constructors

    public EdgeApplicationTest(EdgeApplicationService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a EdgeApplication, through a EdgeApplicationTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on EdgeApplicationTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on EdgeApplicationTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new EdgeApplication.
     *
     * @return    EdgeApplication
     */
    public EdgeApplication testCreate() throws Throwable {
    EdgeApplication entity = null;

    LOGGER.info( "EdgeApplicationTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a EdgeApplication");

    StringBuilder msg = new StringBuilder( "-- Failed to create a EdgeApplication" );

    try {
        entity = service.createEdgeApplication( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getEdgeApplicationId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a EdgeApplication with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a EdgeApplication.
     *
     * @return    EdgeApplication
     */
    public EdgeApplication testRead() throws Throwable {
    LOGGER.info( "EdgeApplicationTest:testRead()" );
    LOGGER.info( "-- Reading a previously created EdgeApplication" );

    EdgeApplication entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read EdgeApplication with primary key" );
    msg.append( theId );

    try {
        entity = service.getEdgeApplication( new EdgeApplicationFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getEdgeApplicationId();

        LOGGER.info( "-- Successfully found EdgeApplication " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a EdgeApplication.
     *
     * @return    EdgeApplication
     */
    public EdgeApplication testUpdate() throws Throwable {
    LOGGER.info( "EdgeApplicationTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a EdgeApplication." );

    StringBuilder msg = new StringBuilder( "Failed to update a EdgeApplication : " );
    EdgeApplication entity = null;

    try {
        UpdateEdgeApplicationCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setEdgeApplicationId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created EdgeApplication." );

        entity = service.updateEdgeApplication( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved EdgeApplication - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a EdgeApplication.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "EdgeApplicationTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created EdgeApplication." );

    try {
        DeleteEdgeApplicationCommand deleteCommand = new DeleteEdgeApplicationCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted EdgeApplication with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete EdgeApplication with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all EdgeApplications.
     *
     * @return    Collection
     */
    public List<EdgeApplication> testGetAll() throws Throwable {
    LOGGER.info( "EdgeApplicationTest:testGetAll() - Retrieving Collection of EdgeApplications:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all EdgeApplication : " );
    List<EdgeApplication> collection  = null;

    try {
        // call the static get method on the EdgeApplicationService
        collection = service.getAllEdgeApplication();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            EdgeApplication currEntity  = null;
            Iterator<EdgeApplication> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getEdgeApplicationId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public EdgeApplicationTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated EdgeApplication
     *
     * @return CreateEdgeApplicationCommand alias
     */
	protected CreateEdgeApplicationCommand generateNewCommand() {
    CreateEdgeApplicationCommand command = new CreateEdgeApplicationCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  DeploymentStatus.values()[0] );

    return( command );
}

    /**
     * Returns a new populated EdgeApplication
     *
     * @return UpdateEdgeApplicationCommand alias
     */
	protected UpdateEdgeApplicationCommand generateUpdateCommand() {
    UpdateEdgeApplicationCommand command = new UpdateEdgeApplicationCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  null,  DeploymentStatus.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected EdgeApplicationService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(EdgeApplication.class.getName());

}
