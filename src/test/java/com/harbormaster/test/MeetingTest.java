/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * ${blueprint.getModelMetadata().getName()}
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test Meeting class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class MeetingTest
{

// constructors

    public MeetingTest(MeetingService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a Meeting, through a MeetingTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on MeetingTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on MeetingTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new Meeting.
     *
     * @return    Meeting
     */
    public Meeting testCreate() throws Throwable {
    Meeting entity = null;

    LOGGER.info( "MeetingTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a Meeting");

    StringBuilder msg = new StringBuilder( "-- Failed to create a Meeting" );

    try {
        entity = service.createMeeting( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getMeetingId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a Meeting with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a Meeting.
     *
     * @return    Meeting
     */
    public Meeting testRead() throws Throwable {
    LOGGER.info( "MeetingTest:testRead()" );
    LOGGER.info( "-- Reading a previously created Meeting" );

    Meeting entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read Meeting with primary key" );
    msg.append( theId );

    try {
        entity = service.getMeeting( new MeetingFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getMeetingId();

        LOGGER.info( "-- Successfully found Meeting " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a Meeting.
     *
     * @return    Meeting
     */
    public Meeting testUpdate() throws Throwable {
    LOGGER.info( "MeetingTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a Meeting." );

    StringBuilder msg = new StringBuilder( "Failed to update a Meeting : " );
    Meeting entity = null;

    try {
        UpdateMeetingCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setMeetingId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created Meeting." );

        entity = service.updateMeeting( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved Meeting - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a Meeting.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "MeetingTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created Meeting." );

    try {
        DeleteMeetingCommand deleteCommand = new DeleteMeetingCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted Meeting with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete Meeting with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all Meetings.
     *
     * @return    Collection
     */
    public List<Meeting> testGetAll() throws Throwable {
    LOGGER.info( "MeetingTest:testGetAll() - Retrieving Collection of Meetings:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all Meeting : " );
    List<Meeting> collection  = null;

    try {
        // call the static get method on the MeetingService
        collection = service.getAllMeeting();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            Meeting currEntity  = null;
            Iterator<Meeting> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getMeetingId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public MeetingTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated Meeting
     *
     * @return CreateMeetingCommand alias
     */
	protected CreateMeetingCommand generateNewCommand() {
    CreateMeetingCommand command = new CreateMeetingCommand( null,   new Date(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16) );

    return( command );
}

    /**
     * Returns a new populated Meeting
     *
     * @return UpdateMeetingCommand alias
     */
	protected UpdateMeetingCommand generateUpdateCommand() {
    UpdateMeetingCommand command = new UpdateMeetingCommand( null,   new Date(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  null,  null,  new HashSet<>() );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected MeetingService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(Meeting.class.getName());

}
