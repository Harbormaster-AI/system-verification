/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test FXTrade class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class FXTradeTest
{

// constructors

    public FXTradeTest(FXTradeService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a FXTrade, through a FXTradeTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on FXTradeTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on FXTradeTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new FXTrade.
     *
     * @return    FXTrade
     */
    public FXTrade testCreate() throws Throwable {
    FXTrade entity = null;

    LOGGER.info( "FXTradeTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a FXTrade");

    StringBuilder msg = new StringBuilder( "-- Failed to create a FXTrade" );

    try {
        entity = service.createFXTrade( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getFXTradeId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a FXTrade with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a FXTrade.
     *
     * @return    FXTrade
     */
    public FXTrade testRead() throws Throwable {
    LOGGER.info( "FXTradeTest:testRead()" );
    LOGGER.info( "-- Reading a previously created FXTrade" );

    FXTrade entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read FXTrade with primary key" );
    msg.append( theId );

    try {
        entity = service.getFXTrade( new FXTradeFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getFXTradeId();

        LOGGER.info( "-- Successfully found FXTrade " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a FXTrade.
     *
     * @return    FXTrade
     */
    public FXTrade testUpdate() throws Throwable {
    LOGGER.info( "FXTradeTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a FXTrade." );

    StringBuilder msg = new StringBuilder( "Failed to update a FXTrade : " );
    FXTrade entity = null;

    try {
        UpdateFXTradeCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setFXTradeId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created FXTrade." );

        entity = service.updateFXTrade( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved FXTrade - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a FXTrade.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "FXTradeTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created FXTrade." );

    try {
        DeleteFXTradeCommand deleteCommand = new DeleteFXTradeCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted FXTrade with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete FXTrade with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all FXTrades.
     *
     * @return    Collection
     */
    public List<FXTrade> testGetAll() throws Throwable {
    LOGGER.info( "FXTradeTest:testGetAll() - Retrieving Collection of FXTrades:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all FXTrade : " );
    List<FXTrade> collection  = null;

    try {
        // call the static get method on the FXTradeService
        collection = service.getAllFXTrade();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            FXTrade currEntity  = null;
            Iterator<FXTrade> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getFXTradeId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public FXTradeTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated FXTrade
     *
     * @return CreateFXTradeCommand alias
     */
	protected CreateFXTradeCommand generateNewCommand() {
    CreateFXTradeCommand command = new CreateFXTradeCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),   new Date(),   new Date(),  new Money(),  new Money(),  java.math.BigDecimal.ZERO,  TradeStatus.values()[0] );

    return( command );
}

    /**
     * Returns a new populated FXTrade
     *
     * @return UpdateFXTradeCommand alias
     */
	protected UpdateFXTradeCommand generateUpdateCommand() {
    UpdateFXTradeCommand command = new UpdateFXTradeCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),   new Date(),   new Date(),  new Money(),  new Money(),  java.math.BigDecimal.ZERO,  null,  null,  null,  null,  null,  null,  TradeStatus.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected FXTradeService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(FXTrade.class.getName());

}
