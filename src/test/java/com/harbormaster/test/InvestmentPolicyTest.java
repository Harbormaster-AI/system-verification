/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * ${blueprint.getModelMetadata().getName()}
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test InvestmentPolicy class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class InvestmentPolicyTest
{

// constructors

    public InvestmentPolicyTest(InvestmentPolicyService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a InvestmentPolicy, through a InvestmentPolicyTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on InvestmentPolicyTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on InvestmentPolicyTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new InvestmentPolicy.
     *
     * @return    InvestmentPolicy
     */
    public InvestmentPolicy testCreate() throws Throwable {
    InvestmentPolicy entity = null;

    LOGGER.info( "InvestmentPolicyTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a InvestmentPolicy");

    StringBuilder msg = new StringBuilder( "-- Failed to create a InvestmentPolicy" );

    try {
        entity = service.createInvestmentPolicy( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getInvestmentPolicyId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a InvestmentPolicy with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a InvestmentPolicy.
     *
     * @return    InvestmentPolicy
     */
    public InvestmentPolicy testRead() throws Throwable {
    LOGGER.info( "InvestmentPolicyTest:testRead()" );
    LOGGER.info( "-- Reading a previously created InvestmentPolicy" );

    InvestmentPolicy entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read InvestmentPolicy with primary key" );
    msg.append( theId );

    try {
        entity = service.getInvestmentPolicy( new InvestmentPolicyFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getInvestmentPolicyId();

        LOGGER.info( "-- Successfully found InvestmentPolicy " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a InvestmentPolicy.
     *
     * @return    InvestmentPolicy
     */
    public InvestmentPolicy testUpdate() throws Throwable {
    LOGGER.info( "InvestmentPolicyTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a InvestmentPolicy." );

    StringBuilder msg = new StringBuilder( "Failed to update a InvestmentPolicy : " );
    InvestmentPolicy entity = null;

    try {
        UpdateInvestmentPolicyCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setInvestmentPolicyId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created InvestmentPolicy." );

        entity = service.updateInvestmentPolicy( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved InvestmentPolicy - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a InvestmentPolicy.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "InvestmentPolicyTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created InvestmentPolicy." );

    try {
        DeleteInvestmentPolicyCommand deleteCommand = new DeleteInvestmentPolicyCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted InvestmentPolicy with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete InvestmentPolicy with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all InvestmentPolicys.
     *
     * @return    Collection
     */
    public List<InvestmentPolicy> testGetAll() throws Throwable {
    LOGGER.info( "InvestmentPolicyTest:testGetAll() - Retrieving Collection of InvestmentPolicys:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all InvestmentPolicy : " );
    List<InvestmentPolicy> collection  = null;

    try {
        // call the static get method on the InvestmentPolicyService
        collection = service.getAllInvestmentPolicy();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            InvestmentPolicy currEntity  = null;
            Iterator<InvestmentPolicy> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getInvestmentPolicyId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public InvestmentPolicyTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated InvestmentPolicy
     *
     * @return CreateInvestmentPolicyCommand alias
     */
	protected CreateInvestmentPolicyCommand generateNewCommand() {
    CreateInvestmentPolicyCommand command = new CreateInvestmentPolicyCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  SuitabilityStatus.values()[0] );

    return( command );
}

    /**
     * Returns a new populated InvestmentPolicy
     *
     * @return UpdateInvestmentPolicyCommand alias
     */
	protected UpdateInvestmentPolicyCommand generateUpdateCommand() {
    UpdateInvestmentPolicyCommand command = new UpdateInvestmentPolicyCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  null,  null,  new HashSet<>(),  SuitabilityStatus.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected InvestmentPolicyService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(InvestmentPolicy.class.getName());

}
