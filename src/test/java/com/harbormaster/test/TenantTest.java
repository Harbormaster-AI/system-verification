
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test Tenant class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class TenantTest
{

// constructors

    public TenantTest(TenantService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a Tenant, through a TenantTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on TenantTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on TenantTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new Tenant.
     *
     * @return    Tenant
     */
    public Tenant testCreate() throws Throwable {
    Tenant entity = null;

    LOGGER.info( "TenantTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a Tenant");

    StringBuilder msg = new StringBuilder( "-- Failed to create a Tenant" );

    try {
        entity = service.createTenant( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getTenantId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a Tenant with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a Tenant.
     *
     * @return    Tenant
     */
    public Tenant testRead() throws Throwable {
    LOGGER.info( "TenantTest:testRead()" );
    LOGGER.info( "-- Reading a previously created Tenant" );

    Tenant entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read Tenant with primary key" );
    msg.append( theId );

    try {
        entity = service.getTenant( new TenantFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getTenantId();

        LOGGER.info( "-- Successfully found Tenant " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a Tenant.
     *
     * @return    Tenant
     */
    public Tenant testUpdate() throws Throwable {
    LOGGER.info( "TenantTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a Tenant." );

    StringBuilder msg = new StringBuilder( "Failed to update a Tenant : " );
    Tenant entity = null;

    try {
        UpdateTenantCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setTenantId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created Tenant." );

        entity = service.updateTenant( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved Tenant - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a Tenant.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "TenantTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created Tenant." );

    try {
        DeleteTenantCommand deleteCommand = new DeleteTenantCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted Tenant with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete Tenant with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all Tenants.
     *
     * @return    Collection
     */
    public List<Tenant> testGetAll() throws Throwable {
    LOGGER.info( "TenantTest:testGetAll() - Retrieving Collection of Tenants:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all Tenant : " );
    List<Tenant> collection  = null;

    try {
        // call the static get method on the TenantService
        collection = service.getAllTenant();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            Tenant currEntity  = null;
            Iterator<Tenant> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getTenantId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public TenantTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated Tenant
     *
     * @return CreateTenantCommand alias
     */
	protected CreateTenantCommand generateNewCommand() {
    CreateTenantCommand command = new CreateTenantCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  TenantType.values()[0] );

    return( command );
}

    /**
     * Returns a new populated Tenant
     *
     * @return UpdateTenantCommand alias
     */
	protected UpdateTenantCommand generateUpdateCommand() {
    UpdateTenantCommand command = new UpdateTenantCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  TenantType.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected TenantService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(Tenant.class.getName());

}
