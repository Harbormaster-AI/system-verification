/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Insurance Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test UnderwritingDecision class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class UnderwritingDecisionTest
{

// constructors

    public UnderwritingDecisionTest(UnderwritingDecisionService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a UnderwritingDecision, through a UnderwritingDecisionTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on UnderwritingDecisionTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on UnderwritingDecisionTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new UnderwritingDecision.
     *
     * @return    UnderwritingDecision
     */
    public UnderwritingDecision testCreate() throws Throwable {
    UnderwritingDecision entity = null;

    LOGGER.info( "UnderwritingDecisionTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a UnderwritingDecision");

    StringBuilder msg = new StringBuilder( "-- Failed to create a UnderwritingDecision" );

    try {
        entity = service.createUnderwritingDecision( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getUnderwritingDecisionId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a UnderwritingDecision with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a UnderwritingDecision.
     *
     * @return    UnderwritingDecision
     */
    public UnderwritingDecision testRead() throws Throwable {
    LOGGER.info( "UnderwritingDecisionTest:testRead()" );
    LOGGER.info( "-- Reading a previously created UnderwritingDecision" );

    UnderwritingDecision entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read UnderwritingDecision with primary key" );
    msg.append( theId );

    try {
        entity = service.getUnderwritingDecision( new UnderwritingDecisionFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getUnderwritingDecisionId();

        LOGGER.info( "-- Successfully found UnderwritingDecision " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a UnderwritingDecision.
     *
     * @return    UnderwritingDecision
     */
    public UnderwritingDecision testUpdate() throws Throwable {
    LOGGER.info( "UnderwritingDecisionTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a UnderwritingDecision." );

    StringBuilder msg = new StringBuilder( "Failed to update a UnderwritingDecision : " );
    UnderwritingDecision entity = null;

    try {
        UpdateUnderwritingDecisionCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setUnderwritingDecisionId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created UnderwritingDecision." );

        entity = service.updateUnderwritingDecision( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved UnderwritingDecision - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a UnderwritingDecision.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "UnderwritingDecisionTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created UnderwritingDecision." );

    try {
        DeleteUnderwritingDecisionCommand deleteCommand = new DeleteUnderwritingDecisionCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted UnderwritingDecision with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete UnderwritingDecision with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all UnderwritingDecisions.
     *
     * @return    Collection
     */
    public List<UnderwritingDecision> testGetAll() throws Throwable {
    LOGGER.info( "UnderwritingDecisionTest:testGetAll() - Retrieving Collection of UnderwritingDecisions:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all UnderwritingDecision : " );
    List<UnderwritingDecision> collection  = null;

    try {
        // call the static get method on the UnderwritingDecisionService
        collection = service.getAllUnderwritingDecision();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            UnderwritingDecision currEntity  = null;
            Iterator<UnderwritingDecision> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getUnderwritingDecisionId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public UnderwritingDecisionTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated UnderwritingDecision
     *
     * @return CreateUnderwritingDecisionCommand alias
     */
	protected CreateUnderwritingDecisionCommand generateNewCommand() {
    CreateUnderwritingDecisionCommand command = new CreateUnderwritingDecisionCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),   new Date(),  UnderwritingDecisionType.values()[0] );

    return( command );
}

    /**
     * Returns a new populated UnderwritingDecision
     *
     * @return UpdateUnderwritingDecisionCommand alias
     */
	protected UpdateUnderwritingDecisionCommand generateUpdateCommand() {
    UpdateUnderwritingDecisionCommand command = new UpdateUnderwritingDecisionCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),   new Date(),  null,  null,  UnderwritingDecisionType.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected UnderwritingDecisionService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(UnderwritingDecision.class.getName());

}
