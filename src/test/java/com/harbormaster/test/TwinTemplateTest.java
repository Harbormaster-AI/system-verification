
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test TwinTemplate class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class TwinTemplateTest
{

// constructors

    public TwinTemplateTest(TwinTemplateService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a TwinTemplate, through a TwinTemplateTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on TwinTemplateTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on TwinTemplateTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new TwinTemplate.
     *
     * @return    TwinTemplate
     */
    public TwinTemplate testCreate() throws Throwable {
    TwinTemplate entity = null;

    LOGGER.info( "TwinTemplateTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a TwinTemplate");

    StringBuilder msg = new StringBuilder( "-- Failed to create a TwinTemplate" );

    try {
        entity = service.createTwinTemplate( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getTwinTemplateId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a TwinTemplate with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a TwinTemplate.
     *
     * @return    TwinTemplate
     */
    public TwinTemplate testRead() throws Throwable {
    LOGGER.info( "TwinTemplateTest:testRead()" );
    LOGGER.info( "-- Reading a previously created TwinTemplate" );

    TwinTemplate entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read TwinTemplate with primary key" );
    msg.append( theId );

    try {
        entity = service.getTwinTemplate( new TwinTemplateFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getTwinTemplateId();

        LOGGER.info( "-- Successfully found TwinTemplate " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a TwinTemplate.
     *
     * @return    TwinTemplate
     */
    public TwinTemplate testUpdate() throws Throwable {
    LOGGER.info( "TwinTemplateTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a TwinTemplate." );

    StringBuilder msg = new StringBuilder( "Failed to update a TwinTemplate : " );
    TwinTemplate entity = null;

    try {
        UpdateTwinTemplateCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setTwinTemplateId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created TwinTemplate." );

        entity = service.updateTwinTemplate( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved TwinTemplate - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a TwinTemplate.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "TwinTemplateTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created TwinTemplate." );

    try {
        DeleteTwinTemplateCommand deleteCommand = new DeleteTwinTemplateCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted TwinTemplate with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete TwinTemplate with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all TwinTemplates.
     *
     * @return    Collection
     */
    public List<TwinTemplate> testGetAll() throws Throwable {
    LOGGER.info( "TwinTemplateTest:testGetAll() - Retrieving Collection of TwinTemplates:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all TwinTemplate : " );
    List<TwinTemplate> collection  = null;

    try {
        // call the static get method on the TwinTemplateService
        collection = service.getAllTwinTemplate();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            TwinTemplate currEntity  = null;
            Iterator<TwinTemplate> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getTwinTemplateId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public TwinTemplateTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated TwinTemplate
     *
     * @return CreateTwinTemplateCommand alias
     */
	protected CreateTwinTemplateCommand generateNewCommand() {
    CreateTwinTemplateCommand command = new CreateTwinTemplateCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Uri(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16) );

    return( command );
}

    /**
     * Returns a new populated TwinTemplate
     *
     * @return UpdateTwinTemplateCommand alias
     */
	protected UpdateTwinTemplateCommand generateUpdateCommand() {
    UpdateTwinTemplateCommand command = new UpdateTwinTemplateCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Uri(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new HashSet<>() );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected TwinTemplateService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(TwinTemplate.class.getName());

}
