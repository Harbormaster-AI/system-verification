/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * ${blueprint.getModelMetadata().getName()}
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test Portfolio class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class PortfolioTest
{

// constructors

    public PortfolioTest(PortfolioService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a Portfolio, through a PortfolioTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on PortfolioTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on PortfolioTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new Portfolio.
     *
     * @return    Portfolio
     */
    public Portfolio testCreate() throws Throwable {
    Portfolio entity = null;

    LOGGER.info( "PortfolioTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a Portfolio");

    StringBuilder msg = new StringBuilder( "-- Failed to create a Portfolio" );

    try {
        entity = service.createPortfolio( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getPortfolioId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a Portfolio with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a Portfolio.
     *
     * @return    Portfolio
     */
    public Portfolio testRead() throws Throwable {
    LOGGER.info( "PortfolioTest:testRead()" );
    LOGGER.info( "-- Reading a previously created Portfolio" );

    Portfolio entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read Portfolio with primary key" );
    msg.append( theId );

    try {
        entity = service.getPortfolio( new PortfolioFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getPortfolioId();

        LOGGER.info( "-- Successfully found Portfolio " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a Portfolio.
     *
     * @return    Portfolio
     */
    public Portfolio testUpdate() throws Throwable {
    LOGGER.info( "PortfolioTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a Portfolio." );

    StringBuilder msg = new StringBuilder( "Failed to update a Portfolio : " );
    Portfolio entity = null;

    try {
        UpdatePortfolioCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setPortfolioId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created Portfolio." );

        entity = service.updatePortfolio( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved Portfolio - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a Portfolio.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "PortfolioTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created Portfolio." );

    try {
        DeletePortfolioCommand deleteCommand = new DeletePortfolioCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted Portfolio with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete Portfolio with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all Portfolios.
     *
     * @return    Collection
     */
    public List<Portfolio> testGetAll() throws Throwable {
    LOGGER.info( "PortfolioTest:testGetAll() - Retrieving Collection of Portfolios:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all Portfolio : " );
    List<Portfolio> collection  = null;

    try {
        // call the static get method on the PortfolioService
        collection = service.getAllPortfolio();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            Portfolio currEntity  = null;
            Iterator<Portfolio> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getPortfolioId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public PortfolioTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated Portfolio
     *
     * @return CreatePortfolioCommand alias
     */
	protected CreatePortfolioCommand generateNewCommand() {
    CreatePortfolioCommand command = new CreatePortfolioCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),   new Date(),  PortfolioType.values()[0],  RebalanceFrequency.values()[0] );

    return( command );
}

    /**
     * Returns a new populated Portfolio
     *
     * @return UpdatePortfolioCommand alias
     */
	protected UpdatePortfolioCommand generateUpdateCommand() {
    UpdatePortfolioCommand command = new UpdatePortfolioCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),   new Date(),  null,  null,  null,  null,  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  PortfolioType.values()[0],  RebalanceFrequency.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected PortfolioService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(Portfolio.class.getName());

}
