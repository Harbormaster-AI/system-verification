
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test AccessPolicy class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class AccessPolicyTest
{

// constructors

    public AccessPolicyTest(AccessPolicyService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a AccessPolicy, through a AccessPolicyTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on AccessPolicyTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on AccessPolicyTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new AccessPolicy.
     *
     * @return    AccessPolicy
     */
    public AccessPolicy testCreate() throws Throwable {
    AccessPolicy entity = null;

    LOGGER.info( "AccessPolicyTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a AccessPolicy");

    StringBuilder msg = new StringBuilder( "-- Failed to create a AccessPolicy" );

    try {
        entity = service.createAccessPolicy( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getAccessPolicyId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a AccessPolicy with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a AccessPolicy.
     *
     * @return    AccessPolicy
     */
    public AccessPolicy testRead() throws Throwable {
    LOGGER.info( "AccessPolicyTest:testRead()" );
    LOGGER.info( "-- Reading a previously created AccessPolicy" );

    AccessPolicy entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read AccessPolicy with primary key" );
    msg.append( theId );

    try {
        entity = service.getAccessPolicy( new AccessPolicyFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getAccessPolicyId();

        LOGGER.info( "-- Successfully found AccessPolicy " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a AccessPolicy.
     *
     * @return    AccessPolicy
     */
    public AccessPolicy testUpdate() throws Throwable {
    LOGGER.info( "AccessPolicyTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a AccessPolicy." );

    StringBuilder msg = new StringBuilder( "Failed to update a AccessPolicy : " );
    AccessPolicy entity = null;

    try {
        UpdateAccessPolicyCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setAccessPolicyId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created AccessPolicy." );

        entity = service.updateAccessPolicy( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved AccessPolicy - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a AccessPolicy.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "AccessPolicyTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created AccessPolicy." );

    try {
        DeleteAccessPolicyCommand deleteCommand = new DeleteAccessPolicyCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted AccessPolicy with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete AccessPolicy with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all AccessPolicys.
     *
     * @return    Collection
     */
    public List<AccessPolicy> testGetAll() throws Throwable {
    LOGGER.info( "AccessPolicyTest:testGetAll() - Retrieving Collection of AccessPolicys:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all AccessPolicy : " );
    List<AccessPolicy> collection  = null;

    try {
        // call the static get method on the AccessPolicyService
        collection = service.getAllAccessPolicy();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            AccessPolicy currEntity  = null;
            Iterator<AccessPolicy> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getAccessPolicyId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public AccessPolicyTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated AccessPolicy
     *
     * @return CreateAccessPolicyCommand alias
     */
	protected CreateAccessPolicyCommand generateNewCommand() {
    CreateAccessPolicyCommand command = new CreateAccessPolicyCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),   new Date() );

    return( command );
}

    /**
     * Returns a new populated AccessPolicy
     *
     * @return UpdateAccessPolicyCommand alias
     */
	protected UpdateAccessPolicyCommand generateUpdateCommand() {
    UpdateAccessPolicyCommand command = new UpdateAccessPolicyCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),   new Date(),  null,  new HashSet<>(),  new HashSet<>() );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected AccessPolicyService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(AccessPolicy.class.getName());

}
