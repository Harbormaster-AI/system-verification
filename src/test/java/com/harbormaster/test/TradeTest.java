/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * ${blueprint.getModelMetadata().getName()}
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test Trade class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class TradeTest
{

// constructors

    public TradeTest(TradeService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a Trade, through a TradeTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on TradeTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on TradeTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new Trade.
     *
     * @return    Trade
     */
    public Trade testCreate() throws Throwable {
    Trade entity = null;

    LOGGER.info( "TradeTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a Trade");

    StringBuilder msg = new StringBuilder( "-- Failed to create a Trade" );

    try {
        entity = service.createTrade( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getTradeId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a Trade with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a Trade.
     *
     * @return    Trade
     */
    public Trade testRead() throws Throwable {
    LOGGER.info( "TradeTest:testRead()" );
    LOGGER.info( "-- Reading a previously created Trade" );

    Trade entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read Trade with primary key" );
    msg.append( theId );

    try {
        entity = service.getTrade( new TradeFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getTradeId();

        LOGGER.info( "-- Successfully found Trade " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a Trade.
     *
     * @return    Trade
     */
    public Trade testUpdate() throws Throwable {
    LOGGER.info( "TradeTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a Trade." );

    StringBuilder msg = new StringBuilder( "Failed to update a Trade : " );
    Trade entity = null;

    try {
        UpdateTradeCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setTradeId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created Trade." );

        entity = service.updateTrade( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved Trade - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a Trade.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "TradeTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created Trade." );

    try {
        DeleteTradeCommand deleteCommand = new DeleteTradeCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted Trade with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete Trade with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all Trades.
     *
     * @return    Collection
     */
    public List<Trade> testGetAll() throws Throwable {
    LOGGER.info( "TradeTest:testGetAll() - Retrieving Collection of Trades:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all Trade : " );
    List<Trade> collection  = null;

    try {
        // call the static get method on the TradeService
        collection = service.getAllTrade();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            Trade currEntity  = null;
            Iterator<Trade> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getTradeId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public TradeTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated Trade
     *
     * @return CreateTradeCommand alias
     */
	protected CreateTradeCommand generateNewCommand() {
    CreateTradeCommand command = new CreateTradeCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Money(),  java.math.BigDecimal.ZERO,   new Date(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  TradeStatus.values()[0] );

    return( command );
}

    /**
     * Returns a new populated Trade
     *
     * @return UpdateTradeCommand alias
     */
	protected UpdateTradeCommand generateUpdateCommand() {
    UpdateTradeCommand command = new UpdateTradeCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Money(),  java.math.BigDecimal.ZERO,   new Date(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  null,  null,  null,  null,  TradeStatus.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected TradeService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(Trade.class.getName());

}
