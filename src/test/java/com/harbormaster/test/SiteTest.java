
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test Site class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class SiteTest
{

// constructors

    public SiteTest(SiteService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a Site, through a SiteTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on SiteTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on SiteTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new Site.
     *
     * @return    Site
     */
    public Site testCreate() throws Throwable {
    Site entity = null;

    LOGGER.info( "SiteTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a Site");

    StringBuilder msg = new StringBuilder( "-- Failed to create a Site" );

    try {
        entity = service.createSite( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getSiteId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a Site with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a Site.
     *
     * @return    Site
     */
    public Site testRead() throws Throwable {
    LOGGER.info( "SiteTest:testRead()" );
    LOGGER.info( "-- Reading a previously created Site" );

    Site entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read Site with primary key" );
    msg.append( theId );

    try {
        entity = service.getSite( new SiteFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getSiteId();

        LOGGER.info( "-- Successfully found Site " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a Site.
     *
     * @return    Site
     */
    public Site testUpdate() throws Throwable {
    LOGGER.info( "SiteTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a Site." );

    StringBuilder msg = new StringBuilder( "Failed to update a Site : " );
    Site entity = null;

    try {
        UpdateSiteCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setSiteId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created Site." );

        entity = service.updateSite( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved Site - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a Site.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "SiteTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created Site." );

    try {
        DeleteSiteCommand deleteCommand = new DeleteSiteCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted Site with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete Site with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all Sites.
     *
     * @return    Collection
     */
    public List<Site> testGetAll() throws Throwable {
    LOGGER.info( "SiteTest:testGetAll() - Retrieving Collection of Sites:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all Site : " );
    List<Site> collection  = null;

    try {
        // call the static get method on the SiteService
        collection = service.getAllSite();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            Site currEntity  = null;
            Iterator<Site> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getSiteId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public SiteTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated Site
     *
     * @return CreateSiteCommand alias
     */
	protected CreateSiteCommand generateNewCommand() {
    CreateSiteCommand command = new CreateSiteCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Address(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  java.math.BigDecimal.ZERO,  java.math.BigDecimal.ZERO );

    return( command );
}

    /**
     * Returns a new populated Site
     *
     * @return UpdateSiteCommand alias
     */
	protected UpdateSiteCommand generateUpdateCommand() {
    UpdateSiteCommand command = new UpdateSiteCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Address(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  java.math.BigDecimal.ZERO,  java.math.BigDecimal.ZERO,  null,  new HashSet<>(),  new HashSet<>(),  new HashSet<>() );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected SiteService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(Site.class.getName());

}
