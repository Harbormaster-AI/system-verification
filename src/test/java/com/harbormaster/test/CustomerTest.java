/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Insurance Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test Customer class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class CustomerTest
{

// constructors

    public CustomerTest(CustomerService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a Customer, through a CustomerTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on CustomerTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on CustomerTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new Customer.
     *
     * @return    Customer
     */
    public Customer testCreate() throws Throwable {
    Customer entity = null;

    LOGGER.info( "CustomerTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a Customer");

    StringBuilder msg = new StringBuilder( "-- Failed to create a Customer" );

    try {
        entity = service.createCustomer( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getCustomerId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a Customer with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a Customer.
     *
     * @return    Customer
     */
    public Customer testRead() throws Throwable {
    LOGGER.info( "CustomerTest:testRead()" );
    LOGGER.info( "-- Reading a previously created Customer" );

    Customer entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read Customer with primary key" );
    msg.append( theId );

    try {
        entity = service.getCustomer( new CustomerFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getCustomerId();

        LOGGER.info( "-- Successfully found Customer " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a Customer.
     *
     * @return    Customer
     */
    public Customer testUpdate() throws Throwable {
    LOGGER.info( "CustomerTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a Customer." );

    StringBuilder msg = new StringBuilder( "Failed to update a Customer : " );
    Customer entity = null;

    try {
        UpdateCustomerCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setCustomerId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created Customer." );

        entity = service.updateCustomer( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved Customer - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a Customer.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "CustomerTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created Customer." );

    try {
        DeleteCustomerCommand deleteCommand = new DeleteCustomerCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted Customer with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete Customer with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all Customers.
     *
     * @return    Collection
     */
    public List<Customer> testGetAll() throws Throwable {
    LOGGER.info( "CustomerTest:testGetAll() - Retrieving Collection of Customers:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all Customer : " );
    List<Customer> collection  = null;

    try {
        // call the static get method on the CustomerService
        collection = service.getAllCustomer();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            Customer currEntity  = null;
            Iterator<Customer> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getCustomerId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public CustomerTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated Customer
     *
     * @return CreateCustomerCommand alias
     */
	protected CreateCustomerCommand generateNewCommand() {
    CreateCustomerCommand command = new CreateCustomerCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),   new Date(),  new Address(),  CustomerType.values()[0] );

    return( command );
}

    /**
     * Returns a new populated Customer
     *
     * @return UpdateCustomerCommand alias
     */
	protected UpdateCustomerCommand generateUpdateCommand() {
    UpdateCustomerCommand command = new UpdateCustomerCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),   new Date(),  new Address(),  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  CustomerType.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected CustomerService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(Customer.class.getName());

}
