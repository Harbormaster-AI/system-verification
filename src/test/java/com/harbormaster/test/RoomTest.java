
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test Room class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class RoomTest
{

// constructors

    public RoomTest(RoomService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a Room, through a RoomTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on RoomTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on RoomTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new Room.
     *
     * @return    Room
     */
    public Room testCreate() throws Throwable {
    Room entity = null;

    LOGGER.info( "RoomTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a Room");

    StringBuilder msg = new StringBuilder( "-- Failed to create a Room" );

    try {
        entity = service.createRoom( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getRoomId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a Room with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a Room.
     *
     * @return    Room
     */
    public Room testRead() throws Throwable {
    LOGGER.info( "RoomTest:testRead()" );
    LOGGER.info( "-- Reading a previously created Room" );

    Room entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read Room with primary key" );
    msg.append( theId );

    try {
        entity = service.getRoom( new RoomFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getRoomId();

        LOGGER.info( "-- Successfully found Room " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a Room.
     *
     * @return    Room
     */
    public Room testUpdate() throws Throwable {
    LOGGER.info( "RoomTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a Room." );

    StringBuilder msg = new StringBuilder( "Failed to update a Room : " );
    Room entity = null;

    try {
        UpdateRoomCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setRoomId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created Room." );

        entity = service.updateRoom( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved Room - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a Room.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "RoomTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created Room." );

    try {
        DeleteRoomCommand deleteCommand = new DeleteRoomCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted Room with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete Room with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all Rooms.
     *
     * @return    Collection
     */
    public List<Room> testGetAll() throws Throwable {
    LOGGER.info( "RoomTest:testGetAll() - Retrieving Collection of Rooms:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all Room : " );
    List<Room> collection  = null;

    try {
        // call the static get method on the RoomService
        collection = service.getAllRoom();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            Room currEntity  = null;
            Iterator<Room> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getRoomId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public RoomTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated Room
     *
     * @return CreateRoomCommand alias
     */
	protected CreateRoomCommand generateNewCommand() {
    CreateRoomCommand command = new CreateRoomCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16) );

    return( command );
}

    /**
     * Returns a new populated Room
     *
     * @return UpdateRoomCommand alias
     */
	protected UpdateRoomCommand generateUpdateCommand() {
    UpdateRoomCommand command = new UpdateRoomCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  null,  new HashSet<>(),  new HashSet<>() );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected RoomService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(Room.class.getName());

}
