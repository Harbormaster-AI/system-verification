/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test KycProfile class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class KycProfileTest
{

// constructors

    public KycProfileTest(KycProfileService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a KycProfile, through a KycProfileTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on KycProfileTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on KycProfileTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new KycProfile.
     *
     * @return    KycProfile
     */
    public KycProfile testCreate() throws Throwable {
    KycProfile entity = null;

    LOGGER.info( "KycProfileTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a KycProfile");

    StringBuilder msg = new StringBuilder( "-- Failed to create a KycProfile" );

    try {
        entity = service.createKycProfile( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getKycProfileId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a KycProfile with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a KycProfile.
     *
     * @return    KycProfile
     */
    public KycProfile testRead() throws Throwable {
    LOGGER.info( "KycProfileTest:testRead()" );
    LOGGER.info( "-- Reading a previously created KycProfile" );

    KycProfile entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read KycProfile with primary key" );
    msg.append( theId );

    try {
        entity = service.getKycProfile( new KycProfileFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getKycProfileId();

        LOGGER.info( "-- Successfully found KycProfile " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a KycProfile.
     *
     * @return    KycProfile
     */
    public KycProfile testUpdate() throws Throwable {
    LOGGER.info( "KycProfileTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a KycProfile." );

    StringBuilder msg = new StringBuilder( "Failed to update a KycProfile : " );
    KycProfile entity = null;

    try {
        UpdateKycProfileCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setKycProfileId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created KycProfile." );

        entity = service.updateKycProfile( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved KycProfile - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a KycProfile.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "KycProfileTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created KycProfile." );

    try {
        DeleteKycProfileCommand deleteCommand = new DeleteKycProfileCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted KycProfile with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete KycProfile with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all KycProfiles.
     *
     * @return    Collection
     */
    public List<KycProfile> testGetAll() throws Throwable {
    LOGGER.info( "KycProfileTest:testGetAll() - Retrieving Collection of KycProfiles:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all KycProfile : " );
    List<KycProfile> collection  = null;

    try {
        // call the static get method on the KycProfileService
        collection = service.getAllKycProfile();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            KycProfile currEntity  = null;
            Iterator<KycProfile> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getKycProfileId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public KycProfileTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated KycProfile
     *
     * @return CreateKycProfileCommand alias
     */
	protected CreateKycProfileCommand generateNewCommand() {
    CreateKycProfileCommand command = new CreateKycProfileCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),   new Date(),  KycStatus.values()[0] );

    return( command );
}

    /**
     * Returns a new populated KycProfile
     *
     * @return UpdateKycProfileCommand alias
     */
	protected UpdateKycProfileCommand generateUpdateCommand() {
    UpdateKycProfileCommand command = new UpdateKycProfileCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),   new Date(),  null,  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  KycStatus.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected KycProfileService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(KycProfile.class.getName());

}
