
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test Floor class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class FloorTest
{

// constructors

    public FloorTest(FloorService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a Floor, through a FloorTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on FloorTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on FloorTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new Floor.
     *
     * @return    Floor
     */
    public Floor testCreate() throws Throwable {
    Floor entity = null;

    LOGGER.info( "FloorTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a Floor");

    StringBuilder msg = new StringBuilder( "-- Failed to create a Floor" );

    try {
        entity = service.createFloor( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getFloorId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a Floor with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a Floor.
     *
     * @return    Floor
     */
    public Floor testRead() throws Throwable {
    LOGGER.info( "FloorTest:testRead()" );
    LOGGER.info( "-- Reading a previously created Floor" );

    Floor entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read Floor with primary key" );
    msg.append( theId );

    try {
        entity = service.getFloor( new FloorFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getFloorId();

        LOGGER.info( "-- Successfully found Floor " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a Floor.
     *
     * @return    Floor
     */
    public Floor testUpdate() throws Throwable {
    LOGGER.info( "FloorTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a Floor." );

    StringBuilder msg = new StringBuilder( "Failed to update a Floor : " );
    Floor entity = null;

    try {
        UpdateFloorCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setFloorId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created Floor." );

        entity = service.updateFloor( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved Floor - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a Floor.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "FloorTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created Floor." );

    try {
        DeleteFloorCommand deleteCommand = new DeleteFloorCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted Floor with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete Floor with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all Floors.
     *
     * @return    Collection
     */
    public List<Floor> testGetAll() throws Throwable {
    LOGGER.info( "FloorTest:testGetAll() - Retrieving Collection of Floors:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all Floor : " );
    List<Floor> collection  = null;

    try {
        // call the static get method on the FloorService
        collection = service.getAllFloor();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            Floor currEntity  = null;
            Iterator<Floor> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getFloorId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public FloorTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated Floor
     *
     * @return CreateFloorCommand alias
     */
	protected CreateFloorCommand generateNewCommand() {
    CreateFloorCommand command = new CreateFloorCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  0 );

    return( command );
}

    /**
     * Returns a new populated Floor
     *
     * @return UpdateFloorCommand alias
     */
	protected UpdateFloorCommand generateUpdateCommand() {
    UpdateFloorCommand command = new UpdateFloorCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  0,  null,  new HashSet<>() );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected FloorService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(Floor.class.getName());

}
