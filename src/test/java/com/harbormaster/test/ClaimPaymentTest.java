/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Insurance Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test ClaimPayment class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class ClaimPaymentTest
{

// constructors

    public ClaimPaymentTest(ClaimPaymentService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a ClaimPayment, through a ClaimPaymentTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on ClaimPaymentTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on ClaimPaymentTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new ClaimPayment.
     *
     * @return    ClaimPayment
     */
    public ClaimPayment testCreate() throws Throwable {
    ClaimPayment entity = null;

    LOGGER.info( "ClaimPaymentTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a ClaimPayment");

    StringBuilder msg = new StringBuilder( "-- Failed to create a ClaimPayment" );

    try {
        entity = service.createClaimPayment( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getClaimPaymentId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a ClaimPayment with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a ClaimPayment.
     *
     * @return    ClaimPayment
     */
    public ClaimPayment testRead() throws Throwable {
    LOGGER.info( "ClaimPaymentTest:testRead()" );
    LOGGER.info( "-- Reading a previously created ClaimPayment" );

    ClaimPayment entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read ClaimPayment with primary key" );
    msg.append( theId );

    try {
        entity = service.getClaimPayment( new ClaimPaymentFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getClaimPaymentId();

        LOGGER.info( "-- Successfully found ClaimPayment " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a ClaimPayment.
     *
     * @return    ClaimPayment
     */
    public ClaimPayment testUpdate() throws Throwable {
    LOGGER.info( "ClaimPaymentTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a ClaimPayment." );

    StringBuilder msg = new StringBuilder( "Failed to update a ClaimPayment : " );
    ClaimPayment entity = null;

    try {
        UpdateClaimPaymentCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setClaimPaymentId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created ClaimPayment." );

        entity = service.updateClaimPayment( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved ClaimPayment - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a ClaimPayment.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "ClaimPaymentTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created ClaimPayment." );

    try {
        DeleteClaimPaymentCommand deleteCommand = new DeleteClaimPaymentCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted ClaimPayment with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete ClaimPayment with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all ClaimPayments.
     *
     * @return    Collection
     */
    public List<ClaimPayment> testGetAll() throws Throwable {
    LOGGER.info( "ClaimPaymentTest:testGetAll() - Retrieving Collection of ClaimPayments:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all ClaimPayment : " );
    List<ClaimPayment> collection  = null;

    try {
        // call the static get method on the ClaimPaymentService
        collection = service.getAllClaimPayment();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            ClaimPayment currEntity  = null;
            Iterator<ClaimPayment> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getClaimPaymentId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public ClaimPaymentTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated ClaimPayment
     *
     * @return CreateClaimPaymentCommand alias
     */
	protected CreateClaimPaymentCommand generateNewCommand() {
    CreateClaimPaymentCommand command = new CreateClaimPaymentCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Money(),   new Date(),  PayeeType.values()[0],  PaymentMethod.values()[0],  PaymentStatus.values()[0] );

    return( command );
}

    /**
     * Returns a new populated ClaimPayment
     *
     * @return UpdateClaimPaymentCommand alias
     */
	protected UpdateClaimPaymentCommand generateUpdateCommand() {
    UpdateClaimPaymentCommand command = new UpdateClaimPaymentCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Money(),   new Date(),  null,  null,  null,  null,  null,  PayeeType.values()[0],  PaymentMethod.values()[0],  PaymentStatus.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected ClaimPaymentService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(ClaimPayment.class.getName());

}
