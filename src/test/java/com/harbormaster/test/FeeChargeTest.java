/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test FeeCharge class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class FeeChargeTest
{

// constructors

    public FeeChargeTest(FeeChargeService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a FeeCharge, through a FeeChargeTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on FeeChargeTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on FeeChargeTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new FeeCharge.
     *
     * @return    FeeCharge
     */
    public FeeCharge testCreate() throws Throwable {
    FeeCharge entity = null;

    LOGGER.info( "FeeChargeTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a FeeCharge");

    StringBuilder msg = new StringBuilder( "-- Failed to create a FeeCharge" );

    try {
        entity = service.createFeeCharge( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getFeeChargeId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a FeeCharge with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a FeeCharge.
     *
     * @return    FeeCharge
     */
    public FeeCharge testRead() throws Throwable {
    LOGGER.info( "FeeChargeTest:testRead()" );
    LOGGER.info( "-- Reading a previously created FeeCharge" );

    FeeCharge entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read FeeCharge with primary key" );
    msg.append( theId );

    try {
        entity = service.getFeeCharge( new FeeChargeFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getFeeChargeId();

        LOGGER.info( "-- Successfully found FeeCharge " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a FeeCharge.
     *
     * @return    FeeCharge
     */
    public FeeCharge testUpdate() throws Throwable {
    LOGGER.info( "FeeChargeTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a FeeCharge." );

    StringBuilder msg = new StringBuilder( "Failed to update a FeeCharge : " );
    FeeCharge entity = null;

    try {
        UpdateFeeChargeCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setFeeChargeId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created FeeCharge." );

        entity = service.updateFeeCharge( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved FeeCharge - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a FeeCharge.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "FeeChargeTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created FeeCharge." );

    try {
        DeleteFeeChargeCommand deleteCommand = new DeleteFeeChargeCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted FeeCharge with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete FeeCharge with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all FeeCharges.
     *
     * @return    Collection
     */
    public List<FeeCharge> testGetAll() throws Throwable {
    LOGGER.info( "FeeChargeTest:testGetAll() - Retrieving Collection of FeeCharges:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all FeeCharge : " );
    List<FeeCharge> collection  = null;

    try {
        // call the static get method on the FeeChargeService
        collection = service.getAllFeeCharge();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            FeeCharge currEntity  = null;
            Iterator<FeeCharge> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getFeeChargeId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public FeeChargeTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated FeeCharge
     *
     * @return CreateFeeChargeCommand alias
     */
	protected CreateFeeChargeCommand generateNewCommand() {
    CreateFeeChargeCommand command = new CreateFeeChargeCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Money(),   new Date(),  FeeType.values()[0] );

    return( command );
}

    /**
     * Returns a new populated FeeCharge
     *
     * @return UpdateFeeChargeCommand alias
     */
	protected UpdateFeeChargeCommand generateUpdateCommand() {
    UpdateFeeChargeCommand command = new UpdateFeeChargeCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Money(),   new Date(),  null,  null,  FeeType.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected FeeChargeService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(FeeCharge.class.getName());

}
