/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test Branch class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class BranchTest
{

// constructors

    public BranchTest(BranchService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a Branch, through a BranchTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on BranchTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on BranchTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new Branch.
     *
     * @return    Branch
     */
    public Branch testCreate() throws Throwable {
    Branch entity = null;

    LOGGER.info( "BranchTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a Branch");

    StringBuilder msg = new StringBuilder( "-- Failed to create a Branch" );

    try {
        entity = service.createBranch( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getBranchId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a Branch with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a Branch.
     *
     * @return    Branch
     */
    public Branch testRead() throws Throwable {
    LOGGER.info( "BranchTest:testRead()" );
    LOGGER.info( "-- Reading a previously created Branch" );

    Branch entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read Branch with primary key" );
    msg.append( theId );

    try {
        entity = service.getBranch( new BranchFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getBranchId();

        LOGGER.info( "-- Successfully found Branch " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a Branch.
     *
     * @return    Branch
     */
    public Branch testUpdate() throws Throwable {
    LOGGER.info( "BranchTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a Branch." );

    StringBuilder msg = new StringBuilder( "Failed to update a Branch : " );
    Branch entity = null;

    try {
        UpdateBranchCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setBranchId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created Branch." );

        entity = service.updateBranch( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved Branch - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a Branch.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "BranchTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created Branch." );

    try {
        DeleteBranchCommand deleteCommand = new DeleteBranchCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted Branch with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete Branch with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all Branchs.
     *
     * @return    Collection
     */
    public List<Branch> testGetAll() throws Throwable {
    LOGGER.info( "BranchTest:testGetAll() - Retrieving Collection of Branchs:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all Branch : " );
    List<Branch> collection  = null;

    try {
        // call the static get method on the BranchService
        collection = service.getAllBranch();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            Branch currEntity  = null;
            Iterator<Branch> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getBranchId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public BranchTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated Branch
     *
     * @return CreateBranchCommand alias
     */
	protected CreateBranchCommand generateNewCommand() {
    CreateBranchCommand command = new CreateBranchCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Address(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16) );

    return( command );
}

    /**
     * Returns a new populated Branch
     *
     * @return UpdateBranchCommand alias
     */
	protected UpdateBranchCommand generateUpdateCommand() {
    UpdateBranchCommand command = new UpdateBranchCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Address(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  null,  new HashSet<>(),  new HashSet<>(),  new HashSet<>() );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected BranchService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(Branch.class.getName());

}
