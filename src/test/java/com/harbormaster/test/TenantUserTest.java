
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test TenantUser class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class TenantUserTest
{

// constructors

    public TenantUserTest(TenantUserService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a TenantUser, through a TenantUserTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on TenantUserTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on TenantUserTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new TenantUser.
     *
     * @return    TenantUser
     */
    public TenantUser testCreate() throws Throwable {
    TenantUser entity = null;

    LOGGER.info( "TenantUserTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a TenantUser");

    StringBuilder msg = new StringBuilder( "-- Failed to create a TenantUser" );

    try {
        entity = service.createTenantUser( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getTenantUserId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a TenantUser with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a TenantUser.
     *
     * @return    TenantUser
     */
    public TenantUser testRead() throws Throwable {
    LOGGER.info( "TenantUserTest:testRead()" );
    LOGGER.info( "-- Reading a previously created TenantUser" );

    TenantUser entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read TenantUser with primary key" );
    msg.append( theId );

    try {
        entity = service.getTenantUser( new TenantUserFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getTenantUserId();

        LOGGER.info( "-- Successfully found TenantUser " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a TenantUser.
     *
     * @return    TenantUser
     */
    public TenantUser testUpdate() throws Throwable {
    LOGGER.info( "TenantUserTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a TenantUser." );

    StringBuilder msg = new StringBuilder( "Failed to update a TenantUser : " );
    TenantUser entity = null;

    try {
        UpdateTenantUserCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setTenantUserId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created TenantUser." );

        entity = service.updateTenantUser( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved TenantUser - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a TenantUser.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "TenantUserTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created TenantUser." );

    try {
        DeleteTenantUserCommand deleteCommand = new DeleteTenantUserCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted TenantUser with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete TenantUser with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all TenantUsers.
     *
     * @return    Collection
     */
    public List<TenantUser> testGetAll() throws Throwable {
    LOGGER.info( "TenantUserTest:testGetAll() - Retrieving Collection of TenantUsers:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all TenantUser : " );
    List<TenantUser> collection  = null;

    try {
        // call the static get method on the TenantUserService
        collection = service.getAllTenantUser();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            TenantUser currEntity  = null;
            Iterator<TenantUser> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getTenantUserId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public TenantUserTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated TenantUser
     *
     * @return CreateTenantUserCommand alias
     */
	protected CreateTenantUserCommand generateNewCommand() {
    CreateTenantUserCommand command = new CreateTenantUserCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  UserRole.values()[0] );

    return( command );
}

    /**
     * Returns a new populated TenantUser
     *
     * @return UpdateTenantUserCommand alias
     */
	protected UpdateTenantUserCommand generateUpdateCommand() {
    UpdateTenantUserCommand command = new UpdateTenantUserCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  null,  new HashSet<>(),  UserRole.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected TenantUserService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(TenantUser.class.getName());

}
