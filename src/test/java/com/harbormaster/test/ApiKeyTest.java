
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test ApiKey class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class ApiKeyTest
{

// constructors

    public ApiKeyTest(ApiKeyService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a ApiKey, through a ApiKeyTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on ApiKeyTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on ApiKeyTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new ApiKey.
     *
     * @return    ApiKey
     */
    public ApiKey testCreate() throws Throwable {
    ApiKey entity = null;

    LOGGER.info( "ApiKeyTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a ApiKey");

    StringBuilder msg = new StringBuilder( "-- Failed to create a ApiKey" );

    try {
        entity = service.createApiKey( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getApiKeyId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a ApiKey with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a ApiKey.
     *
     * @return    ApiKey
     */
    public ApiKey testRead() throws Throwable {
    LOGGER.info( "ApiKeyTest:testRead()" );
    LOGGER.info( "-- Reading a previously created ApiKey" );

    ApiKey entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read ApiKey with primary key" );
    msg.append( theId );

    try {
        entity = service.getApiKey( new ApiKeyFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getApiKeyId();

        LOGGER.info( "-- Successfully found ApiKey " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a ApiKey.
     *
     * @return    ApiKey
     */
    public ApiKey testUpdate() throws Throwable {
    LOGGER.info( "ApiKeyTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a ApiKey." );

    StringBuilder msg = new StringBuilder( "Failed to update a ApiKey : " );
    ApiKey entity = null;

    try {
        UpdateApiKeyCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setApiKeyId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created ApiKey." );

        entity = service.updateApiKey( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved ApiKey - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a ApiKey.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "ApiKeyTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created ApiKey." );

    try {
        DeleteApiKeyCommand deleteCommand = new DeleteApiKeyCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted ApiKey with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete ApiKey with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all ApiKeys.
     *
     * @return    Collection
     */
    public List<ApiKey> testGetAll() throws Throwable {
    LOGGER.info( "ApiKeyTest:testGetAll() - Retrieving Collection of ApiKeys:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all ApiKey : " );
    List<ApiKey> collection  = null;

    try {
        // call the static get method on the ApiKeyService
        collection = service.getAllApiKey();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            ApiKey currEntity  = null;
            Iterator<ApiKey> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getApiKeyId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public ApiKeyTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated ApiKey
     *
     * @return CreateApiKeyCommand alias
     */
	protected CreateApiKeyCommand generateNewCommand() {
    CreateApiKeyCommand command = new CreateApiKeyCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),   new Date(),   new Date() );

    return( command );
}

    /**
     * Returns a new populated ApiKey
     *
     * @return UpdateApiKeyCommand alias
     */
	protected UpdateApiKeyCommand generateUpdateCommand() {
    UpdateApiKeyCommand command = new UpdateApiKeyCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),   new Date(),   new Date(),  null );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected ApiKeyService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(ApiKey.class.getName());

}
