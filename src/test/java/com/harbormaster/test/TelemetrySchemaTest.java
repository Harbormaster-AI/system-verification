
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test TelemetrySchema class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class TelemetrySchemaTest
{

// constructors

    public TelemetrySchemaTest(TelemetrySchemaService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a TelemetrySchema, through a TelemetrySchemaTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on TelemetrySchemaTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on TelemetrySchemaTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new TelemetrySchema.
     *
     * @return    TelemetrySchema
     */
    public TelemetrySchema testCreate() throws Throwable {
    TelemetrySchema entity = null;

    LOGGER.info( "TelemetrySchemaTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a TelemetrySchema");

    StringBuilder msg = new StringBuilder( "-- Failed to create a TelemetrySchema" );

    try {
        entity = service.createTelemetrySchema( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getTelemetrySchemaId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a TelemetrySchema with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a TelemetrySchema.
     *
     * @return    TelemetrySchema
     */
    public TelemetrySchema testRead() throws Throwable {
    LOGGER.info( "TelemetrySchemaTest:testRead()" );
    LOGGER.info( "-- Reading a previously created TelemetrySchema" );

    TelemetrySchema entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read TelemetrySchema with primary key" );
    msg.append( theId );

    try {
        entity = service.getTelemetrySchema( new TelemetrySchemaFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getTelemetrySchemaId();

        LOGGER.info( "-- Successfully found TelemetrySchema " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a TelemetrySchema.
     *
     * @return    TelemetrySchema
     */
    public TelemetrySchema testUpdate() throws Throwable {
    LOGGER.info( "TelemetrySchemaTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a TelemetrySchema." );

    StringBuilder msg = new StringBuilder( "Failed to update a TelemetrySchema : " );
    TelemetrySchema entity = null;

    try {
        UpdateTelemetrySchemaCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setTelemetrySchemaId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created TelemetrySchema." );

        entity = service.updateTelemetrySchema( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved TelemetrySchema - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a TelemetrySchema.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "TelemetrySchemaTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created TelemetrySchema." );

    try {
        DeleteTelemetrySchemaCommand deleteCommand = new DeleteTelemetrySchemaCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted TelemetrySchema with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete TelemetrySchema with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all TelemetrySchemas.
     *
     * @return    Collection
     */
    public List<TelemetrySchema> testGetAll() throws Throwable {
    LOGGER.info( "TelemetrySchemaTest:testGetAll() - Retrieving Collection of TelemetrySchemas:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all TelemetrySchema : " );
    List<TelemetrySchema> collection  = null;

    try {
        // call the static get method on the TelemetrySchemaService
        collection = service.getAllTelemetrySchema();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            TelemetrySchema currEntity  = null;
            Iterator<TelemetrySchema> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getTelemetrySchemaId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public TelemetrySchemaTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated TelemetrySchema
     *
     * @return CreateTelemetrySchemaCommand alias
     */
	protected CreateTelemetrySchemaCommand generateNewCommand() {
    CreateTelemetrySchemaCommand command = new CreateTelemetrySchemaCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Uri(),  TelemetryEncoding.values()[0] );

    return( command );
}

    /**
     * Returns a new populated TelemetrySchema
     *
     * @return UpdateTelemetrySchemaCommand alias
     */
	protected UpdateTelemetrySchemaCommand generateUpdateCommand() {
    UpdateTelemetrySchemaCommand command = new UpdateTelemetrySchemaCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Uri(),  new HashSet<>(),  TelemetryEncoding.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected TelemetrySchemaService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(TelemetrySchema.class.getName());

}
