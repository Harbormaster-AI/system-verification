/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * ${blueprint.getModelMetadata().getName()}
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test Dividend class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class DividendTest
{

// constructors

    public DividendTest(DividendService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a Dividend, through a DividendTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on DividendTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on DividendTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new Dividend.
     *
     * @return    Dividend
     */
    public Dividend testCreate() throws Throwable {
    Dividend entity = null;

    LOGGER.info( "DividendTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a Dividend");

    StringBuilder msg = new StringBuilder( "-- Failed to create a Dividend" );

    try {
        entity = service.createDividend( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getDividendId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a Dividend with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a Dividend.
     *
     * @return    Dividend
     */
    public Dividend testRead() throws Throwable {
    LOGGER.info( "DividendTest:testRead()" );
    LOGGER.info( "-- Reading a previously created Dividend" );

    Dividend entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read Dividend with primary key" );
    msg.append( theId );

    try {
        entity = service.getDividend( new DividendFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getDividendId();

        LOGGER.info( "-- Successfully found Dividend " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a Dividend.
     *
     * @return    Dividend
     */
    public Dividend testUpdate() throws Throwable {
    LOGGER.info( "DividendTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a Dividend." );

    StringBuilder msg = new StringBuilder( "Failed to update a Dividend : " );
    Dividend entity = null;

    try {
        UpdateDividendCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setDividendId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created Dividend." );

        entity = service.updateDividend( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved Dividend - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a Dividend.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "DividendTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created Dividend." );

    try {
        DeleteDividendCommand deleteCommand = new DeleteDividendCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted Dividend with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete Dividend with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all Dividends.
     *
     * @return    Collection
     */
    public List<Dividend> testGetAll() throws Throwable {
    LOGGER.info( "DividendTest:testGetAll() - Retrieving Collection of Dividends:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all Dividend : " );
    List<Dividend> collection  = null;

    try {
        // call the static get method on the DividendService
        collection = service.getAllDividend();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            Dividend currEntity  = null;
            Iterator<Dividend> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getDividendId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public DividendTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated Dividend
     *
     * @return CreateDividendCommand alias
     */
	protected CreateDividendCommand generateNewCommand() {
    CreateDividendCommand command = new CreateDividendCommand( null,  new Money(),  new Money() );

    return( command );
}

    /**
     * Returns a new populated Dividend
     *
     * @return UpdateDividendCommand alias
     */
	protected UpdateDividendCommand generateUpdateCommand() {
    UpdateDividendCommand command = new UpdateDividendCommand( null,  new Money(),  new Money(),  null );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected DividendService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(Dividend.class.getName());

}
