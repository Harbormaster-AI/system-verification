/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * ${blueprint.getModelMetadata().getName()}
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test Position class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class PositionTest
{

// constructors

    public PositionTest(PositionService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a Position, through a PositionTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on PositionTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on PositionTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new Position.
     *
     * @return    Position
     */
    public Position testCreate() throws Throwable {
    Position entity = null;

    LOGGER.info( "PositionTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a Position");

    StringBuilder msg = new StringBuilder( "-- Failed to create a Position" );

    try {
        entity = service.createPosition( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getPositionId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a Position with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a Position.
     *
     * @return    Position
     */
    public Position testRead() throws Throwable {
    LOGGER.info( "PositionTest:testRead()" );
    LOGGER.info( "-- Reading a previously created Position" );

    Position entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read Position with primary key" );
    msg.append( theId );

    try {
        entity = service.getPosition( new PositionFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getPositionId();

        LOGGER.info( "-- Successfully found Position " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a Position.
     *
     * @return    Position
     */
    public Position testUpdate() throws Throwable {
    LOGGER.info( "PositionTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a Position." );

    StringBuilder msg = new StringBuilder( "Failed to update a Position : " );
    Position entity = null;

    try {
        UpdatePositionCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setPositionId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created Position." );

        entity = service.updatePosition( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved Position - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a Position.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "PositionTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created Position." );

    try {
        DeletePositionCommand deleteCommand = new DeletePositionCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted Position with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete Position with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all Positions.
     *
     * @return    Collection
     */
    public List<Position> testGetAll() throws Throwable {
    LOGGER.info( "PositionTest:testGetAll() - Retrieving Collection of Positions:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all Position : " );
    List<Position> collection  = null;

    try {
        // call the static get method on the PositionService
        collection = service.getAllPosition();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            Position currEntity  = null;
            Iterator<Position> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getPositionId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public PositionTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated Position
     *
     * @return CreatePositionCommand alias
     */
	protected CreatePositionCommand generateNewCommand() {
    CreatePositionCommand command = new CreatePositionCommand( null,  java.math.BigDecimal.ZERO,  new Money(),  PositionType.values()[0],  TaxLotMethod.values()[0] );

    return( command );
}

    /**
     * Returns a new populated Position
     *
     * @return UpdatePositionCommand alias
     */
	protected UpdatePositionCommand generateUpdateCommand() {
    UpdatePositionCommand command = new UpdatePositionCommand( null,  java.math.BigDecimal.ZERO,  new Money(),  null,  null,  new HashSet<>(),  new HashSet<>(),  PositionType.values()[0],  TaxLotMethod.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected PositionService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(Position.class.getName());

}
