/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test Bank class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class BankTest
{

// constructors

    public BankTest(BankService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a Bank, through a BankTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on BankTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on BankTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new Bank.
     *
     * @return    Bank
     */
    public Bank testCreate() throws Throwable {
    Bank entity = null;

    LOGGER.info( "BankTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a Bank");

    StringBuilder msg = new StringBuilder( "-- Failed to create a Bank" );

    try {
        entity = service.createBank( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getBankId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a Bank with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a Bank.
     *
     * @return    Bank
     */
    public Bank testRead() throws Throwable {
    LOGGER.info( "BankTest:testRead()" );
    LOGGER.info( "-- Reading a previously created Bank" );

    Bank entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read Bank with primary key" );
    msg.append( theId );

    try {
        entity = service.getBank( new BankFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getBankId();

        LOGGER.info( "-- Successfully found Bank " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a Bank.
     *
     * @return    Bank
     */
    public Bank testUpdate() throws Throwable {
    LOGGER.info( "BankTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a Bank." );

    StringBuilder msg = new StringBuilder( "Failed to update a Bank : " );
    Bank entity = null;

    try {
        UpdateBankCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setBankId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created Bank." );

        entity = service.updateBank( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved Bank - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a Bank.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "BankTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created Bank." );

    try {
        DeleteBankCommand deleteCommand = new DeleteBankCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted Bank with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete Bank with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all Banks.
     *
     * @return    Collection
     */
    public List<Bank> testGetAll() throws Throwable {
    LOGGER.info( "BankTest:testGetAll() - Retrieving Collection of Banks:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all Bank : " );
    List<Bank> collection  = null;

    try {
        // call the static get method on the BankService
        collection = service.getAllBank();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            Bank currEntity  = null;
            Iterator<Bank> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getBankId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public BankTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated Bank
     *
     * @return CreateBankCommand alias
     */
	protected CreateBankCommand generateNewCommand() {
    CreateBankCommand command = new CreateBankCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new BIC(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16) );

    return( command );
}

    /**
     * Returns a new populated Bank
     *
     * @return UpdateBankCommand alias
     */
	protected UpdateBankCommand generateUpdateCommand() {
    UpdateBankCommand command = new UpdateBankCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new BIC(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  new HashSet<>() );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected BankService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(Bank.class.getName());

}
