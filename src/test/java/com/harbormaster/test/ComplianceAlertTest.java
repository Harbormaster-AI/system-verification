/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * ${blueprint.getModelMetadata().getName()}
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test ComplianceAlert class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class ComplianceAlertTest
{

// constructors

    public ComplianceAlertTest(ComplianceAlertService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a ComplianceAlert, through a ComplianceAlertTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on ComplianceAlertTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on ComplianceAlertTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new ComplianceAlert.
     *
     * @return    ComplianceAlert
     */
    public ComplianceAlert testCreate() throws Throwable {
    ComplianceAlert entity = null;

    LOGGER.info( "ComplianceAlertTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a ComplianceAlert");

    StringBuilder msg = new StringBuilder( "-- Failed to create a ComplianceAlert" );

    try {
        entity = service.createComplianceAlert( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getComplianceAlertId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a ComplianceAlert with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a ComplianceAlert.
     *
     * @return    ComplianceAlert
     */
    public ComplianceAlert testRead() throws Throwable {
    LOGGER.info( "ComplianceAlertTest:testRead()" );
    LOGGER.info( "-- Reading a previously created ComplianceAlert" );

    ComplianceAlert entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read ComplianceAlert with primary key" );
    msg.append( theId );

    try {
        entity = service.getComplianceAlert( new ComplianceAlertFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getComplianceAlertId();

        LOGGER.info( "-- Successfully found ComplianceAlert " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a ComplianceAlert.
     *
     * @return    ComplianceAlert
     */
    public ComplianceAlert testUpdate() throws Throwable {
    LOGGER.info( "ComplianceAlertTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a ComplianceAlert." );

    StringBuilder msg = new StringBuilder( "Failed to update a ComplianceAlert : " );
    ComplianceAlert entity = null;

    try {
        UpdateComplianceAlertCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setComplianceAlertId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created ComplianceAlert." );

        entity = service.updateComplianceAlert( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved ComplianceAlert - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a ComplianceAlert.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "ComplianceAlertTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created ComplianceAlert." );

    try {
        DeleteComplianceAlertCommand deleteCommand = new DeleteComplianceAlertCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted ComplianceAlert with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete ComplianceAlert with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all ComplianceAlerts.
     *
     * @return    Collection
     */
    public List<ComplianceAlert> testGetAll() throws Throwable {
    LOGGER.info( "ComplianceAlertTest:testGetAll() - Retrieving Collection of ComplianceAlerts:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all ComplianceAlert : " );
    List<ComplianceAlert> collection  = null;

    try {
        // call the static get method on the ComplianceAlertService
        collection = service.getAllComplianceAlert();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            ComplianceAlert currEntity  = null;
            Iterator<ComplianceAlert> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getComplianceAlertId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public ComplianceAlertTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated ComplianceAlert
     *
     * @return CreateComplianceAlertCommand alias
     */
	protected CreateComplianceAlertCommand generateNewCommand() {
    CreateComplianceAlertCommand command = new CreateComplianceAlertCommand( null,   new Date(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  ComplianceStatus.values()[0],  AlertSeverity.values()[0] );

    return( command );
}

    /**
     * Returns a new populated ComplianceAlert
     *
     * @return UpdateComplianceAlertCommand alias
     */
	protected UpdateComplianceAlertCommand generateUpdateCommand() {
    UpdateComplianceAlertCommand command = new UpdateComplianceAlertCommand( null,   new Date(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  null,  null,  null,  null,  ComplianceStatus.values()[0],  AlertSeverity.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected ComplianceAlertService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(ComplianceAlert.class.getName());

}
