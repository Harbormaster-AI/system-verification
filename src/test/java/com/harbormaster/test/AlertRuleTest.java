
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test AlertRule class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class AlertRuleTest
{

// constructors

    public AlertRuleTest(AlertRuleService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a AlertRule, through a AlertRuleTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on AlertRuleTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on AlertRuleTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new AlertRule.
     *
     * @return    AlertRule
     */
    public AlertRule testCreate() throws Throwable {
    AlertRule entity = null;

    LOGGER.info( "AlertRuleTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a AlertRule");

    StringBuilder msg = new StringBuilder( "-- Failed to create a AlertRule" );

    try {
        entity = service.createAlertRule( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getAlertRuleId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a AlertRule with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a AlertRule.
     *
     * @return    AlertRule
     */
    public AlertRule testRead() throws Throwable {
    LOGGER.info( "AlertRuleTest:testRead()" );
    LOGGER.info( "-- Reading a previously created AlertRule" );

    AlertRule entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read AlertRule with primary key" );
    msg.append( theId );

    try {
        entity = service.getAlertRule( new AlertRuleFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getAlertRuleId();

        LOGGER.info( "-- Successfully found AlertRule " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a AlertRule.
     *
     * @return    AlertRule
     */
    public AlertRule testUpdate() throws Throwable {
    LOGGER.info( "AlertRuleTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a AlertRule." );

    StringBuilder msg = new StringBuilder( "Failed to update a AlertRule : " );
    AlertRule entity = null;

    try {
        UpdateAlertRuleCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setAlertRuleId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created AlertRule." );

        entity = service.updateAlertRule( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved AlertRule - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a AlertRule.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "AlertRuleTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created AlertRule." );

    try {
        DeleteAlertRuleCommand deleteCommand = new DeleteAlertRuleCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted AlertRule with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete AlertRule with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all AlertRules.
     *
     * @return    Collection
     */
    public List<AlertRule> testGetAll() throws Throwable {
    LOGGER.info( "AlertRuleTest:testGetAll() - Retrieving Collection of AlertRules:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all AlertRule : " );
    List<AlertRule> collection  = null;

    try {
        // call the static get method on the AlertRuleService
        collection = service.getAllAlertRule();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            AlertRule currEntity  = null;
            Iterator<AlertRule> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getAlertRuleId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public AlertRuleTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated AlertRule
     *
     * @return CreateAlertRuleCommand alias
     */
	protected CreateAlertRuleCommand generateNewCommand() {
    CreateAlertRuleCommand command = new CreateAlertRuleCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  AlertSeverity.values()[0] );

    return( command );
}

    /**
     * Returns a new populated AlertRule
     *
     * @return UpdateAlertRuleCommand alias
     */
	protected UpdateAlertRuleCommand generateUpdateCommand() {
    UpdateAlertRuleCommand command = new UpdateAlertRuleCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  null,  new HashSet<>(),  new HashSet<>(),  AlertSeverity.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected AlertRuleService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(AlertRule.class.getName());

}
