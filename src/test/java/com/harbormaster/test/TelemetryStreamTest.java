
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test TelemetryStream class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class TelemetryStreamTest
{

// constructors

    public TelemetryStreamTest(TelemetryStreamService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a TelemetryStream, through a TelemetryStreamTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on TelemetryStreamTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on TelemetryStreamTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new TelemetryStream.
     *
     * @return    TelemetryStream
     */
    public TelemetryStream testCreate() throws Throwable {
    TelemetryStream entity = null;

    LOGGER.info( "TelemetryStreamTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a TelemetryStream");

    StringBuilder msg = new StringBuilder( "-- Failed to create a TelemetryStream" );

    try {
        entity = service.createTelemetryStream( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getTelemetryStreamId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a TelemetryStream with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a TelemetryStream.
     *
     * @return    TelemetryStream
     */
    public TelemetryStream testRead() throws Throwable {
    LOGGER.info( "TelemetryStreamTest:testRead()" );
    LOGGER.info( "-- Reading a previously created TelemetryStream" );

    TelemetryStream entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read TelemetryStream with primary key" );
    msg.append( theId );

    try {
        entity = service.getTelemetryStream( new TelemetryStreamFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getTelemetryStreamId();

        LOGGER.info( "-- Successfully found TelemetryStream " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a TelemetryStream.
     *
     * @return    TelemetryStream
     */
    public TelemetryStream testUpdate() throws Throwable {
    LOGGER.info( "TelemetryStreamTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a TelemetryStream." );

    StringBuilder msg = new StringBuilder( "Failed to update a TelemetryStream : " );
    TelemetryStream entity = null;

    try {
        UpdateTelemetryStreamCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setTelemetryStreamId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created TelemetryStream." );

        entity = service.updateTelemetryStream( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved TelemetryStream - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a TelemetryStream.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "TelemetryStreamTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created TelemetryStream." );

    try {
        DeleteTelemetryStreamCommand deleteCommand = new DeleteTelemetryStreamCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted TelemetryStream with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete TelemetryStream with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all TelemetryStreams.
     *
     * @return    Collection
     */
    public List<TelemetryStream> testGetAll() throws Throwable {
    LOGGER.info( "TelemetryStreamTest:testGetAll() - Retrieving Collection of TelemetryStreams:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all TelemetryStream : " );
    List<TelemetryStream> collection  = null;

    try {
        // call the static get method on the TelemetryStreamService
        collection = service.getAllTelemetryStream();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            TelemetryStream currEntity  = null;
            Iterator<TelemetryStream> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getTelemetryStreamId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public TelemetryStreamTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated TelemetryStream
     *
     * @return CreateTelemetryStreamCommand alias
     */
	protected CreateTelemetryStreamCommand generateNewCommand() {
    CreateTelemetryStreamCommand command = new CreateTelemetryStreamCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  0,  MessageQoS.values()[0] );

    return( command );
}

    /**
     * Returns a new populated TelemetryStream
     *
     * @return UpdateTelemetryStreamCommand alias
     */
	protected UpdateTelemetryStreamCommand generateUpdateCommand() {
    UpdateTelemetryStreamCommand command = new UpdateTelemetryStreamCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  0,  null,  null,  null,  null,  null,  MessageQoS.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected TelemetryStreamService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(TelemetryStream.class.getName());

}
