/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test Transaction class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class TransactionTest
{

// constructors

    public TransactionTest(TransactionService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a Transaction, through a TransactionTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on TransactionTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on TransactionTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new Transaction.
     *
     * @return    Transaction
     */
    public Transaction testCreate() throws Throwable {
    Transaction entity = null;

    LOGGER.info( "TransactionTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a Transaction");

    StringBuilder msg = new StringBuilder( "-- Failed to create a Transaction" );

    try {
        entity = service.createTransaction( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getTransactionId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a Transaction with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a Transaction.
     *
     * @return    Transaction
     */
    public Transaction testRead() throws Throwable {
    LOGGER.info( "TransactionTest:testRead()" );
    LOGGER.info( "-- Reading a previously created Transaction" );

    Transaction entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read Transaction with primary key" );
    msg.append( theId );

    try {
        entity = service.getTransaction( new TransactionFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getTransactionId();

        LOGGER.info( "-- Successfully found Transaction " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a Transaction.
     *
     * @return    Transaction
     */
    public Transaction testUpdate() throws Throwable {
    LOGGER.info( "TransactionTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a Transaction." );

    StringBuilder msg = new StringBuilder( "Failed to update a Transaction : " );
    Transaction entity = null;

    try {
        UpdateTransactionCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setTransactionId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created Transaction." );

        entity = service.updateTransaction( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved Transaction - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a Transaction.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "TransactionTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created Transaction." );

    try {
        DeleteTransactionCommand deleteCommand = new DeleteTransactionCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted Transaction with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete Transaction with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all Transactions.
     *
     * @return    Collection
     */
    public List<Transaction> testGetAll() throws Throwable {
    LOGGER.info( "TransactionTest:testGetAll() - Retrieving Collection of Transactions:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all Transaction : " );
    List<Transaction> collection  = null;

    try {
        // call the static get method on the TransactionService
        collection = service.getAllTransaction();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            Transaction currEntity  = null;
            Iterator<Transaction> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getTransactionId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public TransactionTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated Transaction
     *
     * @return CreateTransactionCommand alias
     */
	protected CreateTransactionCommand generateNewCommand() {
    CreateTransactionCommand command = new CreateTransactionCommand( null,   new Date(),   new Date(),  new Money(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  TransactionDirection.values()[0],  TransactionType.values()[0],  TransactionStatus.values()[0],  ChannelType.values()[0] );

    return( command );
}

    /**
     * Returns a new populated Transaction
     *
     * @return UpdateTransactionCommand alias
     */
	protected UpdateTransactionCommand generateUpdateCommand() {
    UpdateTransactionCommand command = new UpdateTransactionCommand( null,   new Date(),   new Date(),  new Money(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  null,  null,  null,  null,  null,  null,  TransactionDirection.values()[0],  TransactionType.values()[0],  TransactionStatus.values()[0],  ChannelType.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected TransactionService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(Transaction.class.getName());

}
