
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test TwinChangeEvent class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class TwinChangeEventTest
{

// constructors

    public TwinChangeEventTest(TwinChangeEventService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a TwinChangeEvent, through a TwinChangeEventTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on TwinChangeEventTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on TwinChangeEventTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new TwinChangeEvent.
     *
     * @return    TwinChangeEvent
     */
    public TwinChangeEvent testCreate() throws Throwable {
    TwinChangeEvent entity = null;

    LOGGER.info( "TwinChangeEventTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a TwinChangeEvent");

    StringBuilder msg = new StringBuilder( "-- Failed to create a TwinChangeEvent" );

    try {
        entity = service.createTwinChangeEvent( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getTwinChangeEventId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a TwinChangeEvent with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a TwinChangeEvent.
     *
     * @return    TwinChangeEvent
     */
    public TwinChangeEvent testRead() throws Throwable {
    LOGGER.info( "TwinChangeEventTest:testRead()" );
    LOGGER.info( "-- Reading a previously created TwinChangeEvent" );

    TwinChangeEvent entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read TwinChangeEvent with primary key" );
    msg.append( theId );

    try {
        entity = service.getTwinChangeEvent( new TwinChangeEventFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getTwinChangeEventId();

        LOGGER.info( "-- Successfully found TwinChangeEvent " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a TwinChangeEvent.
     *
     * @return    TwinChangeEvent
     */
    public TwinChangeEvent testUpdate() throws Throwable {
    LOGGER.info( "TwinChangeEventTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a TwinChangeEvent." );

    StringBuilder msg = new StringBuilder( "Failed to update a TwinChangeEvent : " );
    TwinChangeEvent entity = null;

    try {
        UpdateTwinChangeEventCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setTwinChangeEventId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created TwinChangeEvent." );

        entity = service.updateTwinChangeEvent( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved TwinChangeEvent - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a TwinChangeEvent.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "TwinChangeEventTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created TwinChangeEvent." );

    try {
        DeleteTwinChangeEventCommand deleteCommand = new DeleteTwinChangeEventCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted TwinChangeEvent with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete TwinChangeEvent with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all TwinChangeEvents.
     *
     * @return    Collection
     */
    public List<TwinChangeEvent> testGetAll() throws Throwable {
    LOGGER.info( "TwinChangeEventTest:testGetAll() - Retrieving Collection of TwinChangeEvents:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all TwinChangeEvent : " );
    List<TwinChangeEvent> collection  = null;

    try {
        // call the static get method on the TwinChangeEventService
        collection = service.getAllTwinChangeEvent();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            TwinChangeEvent currEntity  = null;
            Iterator<TwinChangeEvent> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getTwinChangeEventId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public TwinChangeEventTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated TwinChangeEvent
     *
     * @return CreateTwinChangeEventCommand alias
     */
	protected CreateTwinChangeEventCommand generateNewCommand() {
    CreateTwinChangeEventCommand command = new CreateTwinChangeEventCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),   new Date(),  TwinChangeType.values()[0] );

    return( command );
}

    /**
     * Returns a new populated TwinChangeEvent
     *
     * @return UpdateTwinChangeEventCommand alias
     */
	protected UpdateTwinChangeEventCommand generateUpdateCommand() {
    UpdateTwinChangeEventCommand command = new UpdateTwinChangeEventCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),   new Date(),  null,  TwinChangeType.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected TwinChangeEventService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(TwinChangeEvent.class.getName());

}
