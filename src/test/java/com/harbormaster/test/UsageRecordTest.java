
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test UsageRecord class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class UsageRecordTest
{

// constructors

    public UsageRecordTest(UsageRecordService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a UsageRecord, through a UsageRecordTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on UsageRecordTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on UsageRecordTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new UsageRecord.
     *
     * @return    UsageRecord
     */
    public UsageRecord testCreate() throws Throwable {
    UsageRecord entity = null;

    LOGGER.info( "UsageRecordTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a UsageRecord");

    StringBuilder msg = new StringBuilder( "-- Failed to create a UsageRecord" );

    try {
        entity = service.createUsageRecord( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getUsageRecordId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a UsageRecord with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a UsageRecord.
     *
     * @return    UsageRecord
     */
    public UsageRecord testRead() throws Throwable {
    LOGGER.info( "UsageRecordTest:testRead()" );
    LOGGER.info( "-- Reading a previously created UsageRecord" );

    UsageRecord entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read UsageRecord with primary key" );
    msg.append( theId );

    try {
        entity = service.getUsageRecord( new UsageRecordFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getUsageRecordId();

        LOGGER.info( "-- Successfully found UsageRecord " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a UsageRecord.
     *
     * @return    UsageRecord
     */
    public UsageRecord testUpdate() throws Throwable {
    LOGGER.info( "UsageRecordTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a UsageRecord." );

    StringBuilder msg = new StringBuilder( "Failed to update a UsageRecord : " );
    UsageRecord entity = null;

    try {
        UpdateUsageRecordCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setUsageRecordId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created UsageRecord." );

        entity = service.updateUsageRecord( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved UsageRecord - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a UsageRecord.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "UsageRecordTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created UsageRecord." );

    try {
        DeleteUsageRecordCommand deleteCommand = new DeleteUsageRecordCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted UsageRecord with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete UsageRecord with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all UsageRecords.
     *
     * @return    Collection
     */
    public List<UsageRecord> testGetAll() throws Throwable {
    LOGGER.info( "UsageRecordTest:testGetAll() - Retrieving Collection of UsageRecords:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all UsageRecord : " );
    List<UsageRecord> collection  = null;

    try {
        // call the static get method on the UsageRecordService
        collection = service.getAllUsageRecord();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            UsageRecord currEntity  = null;
            Iterator<UsageRecord> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getUsageRecordId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public UsageRecordTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated UsageRecord
     *
     * @return CreateUsageRecordCommand alias
     */
	protected CreateUsageRecordCommand generateNewCommand() {
    CreateUsageRecordCommand command = new CreateUsageRecordCommand( null,   new Date(),   new Date(),  0,  0 );

    return( command );
}

    /**
     * Returns a new populated UsageRecord
     *
     * @return UpdateUsageRecordCommand alias
     */
	protected UpdateUsageRecordCommand generateUpdateCommand() {
    UpdateUsageRecordCommand command = new UpdateUsageRecordCommand( null,   new Date(),   new Date(),  0,  0,  null,  null,  null );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected UsageRecordService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(UsageRecord.class.getName());

}
