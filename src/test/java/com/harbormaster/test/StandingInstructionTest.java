/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test StandingInstruction class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class StandingInstructionTest
{

// constructors

    public StandingInstructionTest(StandingInstructionService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a StandingInstruction, through a StandingInstructionTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on StandingInstructionTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on StandingInstructionTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new StandingInstruction.
     *
     * @return    StandingInstruction
     */
    public StandingInstruction testCreate() throws Throwable {
    StandingInstruction entity = null;

    LOGGER.info( "StandingInstructionTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a StandingInstruction");

    StringBuilder msg = new StringBuilder( "-- Failed to create a StandingInstruction" );

    try {
        entity = service.createStandingInstruction( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getStandingInstructionId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a StandingInstruction with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a StandingInstruction.
     *
     * @return    StandingInstruction
     */
    public StandingInstruction testRead() throws Throwable {
    LOGGER.info( "StandingInstructionTest:testRead()" );
    LOGGER.info( "-- Reading a previously created StandingInstruction" );

    StandingInstruction entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read StandingInstruction with primary key" );
    msg.append( theId );

    try {
        entity = service.getStandingInstruction( new StandingInstructionFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getStandingInstructionId();

        LOGGER.info( "-- Successfully found StandingInstruction " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a StandingInstruction.
     *
     * @return    StandingInstruction
     */
    public StandingInstruction testUpdate() throws Throwable {
    LOGGER.info( "StandingInstructionTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a StandingInstruction." );

    StringBuilder msg = new StringBuilder( "Failed to update a StandingInstruction : " );
    StandingInstruction entity = null;

    try {
        UpdateStandingInstructionCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setStandingInstructionId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created StandingInstruction." );

        entity = service.updateStandingInstruction( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved StandingInstruction - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a StandingInstruction.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "StandingInstructionTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created StandingInstruction." );

    try {
        DeleteStandingInstructionCommand deleteCommand = new DeleteStandingInstructionCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted StandingInstruction with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete StandingInstruction with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all StandingInstructions.
     *
     * @return    Collection
     */
    public List<StandingInstruction> testGetAll() throws Throwable {
    LOGGER.info( "StandingInstructionTest:testGetAll() - Retrieving Collection of StandingInstructions:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all StandingInstruction : " );
    List<StandingInstruction> collection  = null;

    try {
        // call the static get method on the StandingInstructionService
        collection = service.getAllStandingInstruction();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            StandingInstruction currEntity  = null;
            Iterator<StandingInstruction> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getStandingInstructionId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public StandingInstructionTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated StandingInstruction
     *
     * @return CreateStandingInstructionCommand alias
     */
	protected CreateStandingInstructionCommand generateNewCommand() {
    CreateStandingInstructionCommand command = new CreateStandingInstructionCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Money(),   new Date(),  StandingInstructionFrequency.values()[0],  StandingInstructionStatus.values()[0] );

    return( command );
}

    /**
     * Returns a new populated StandingInstruction
     *
     * @return UpdateStandingInstructionCommand alias
     */
	protected UpdateStandingInstructionCommand generateUpdateCommand() {
    UpdateStandingInstructionCommand command = new UpdateStandingInstructionCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Money(),   new Date(),  null,  null,  StandingInstructionFrequency.values()[0],  StandingInstructionStatus.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected StandingInstructionService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(StandingInstruction.class.getName());

}
