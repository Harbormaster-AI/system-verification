/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Insurance Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test Underwriter class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class UnderwriterTest
{

// constructors

    public UnderwriterTest(UnderwriterService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a Underwriter, through a UnderwriterTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on UnderwriterTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on UnderwriterTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new Underwriter.
     *
     * @return    Underwriter
     */
    public Underwriter testCreate() throws Throwable {
    Underwriter entity = null;

    LOGGER.info( "UnderwriterTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a Underwriter");

    StringBuilder msg = new StringBuilder( "-- Failed to create a Underwriter" );

    try {
        entity = service.createUnderwriter( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getUnderwriterId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a Underwriter with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a Underwriter.
     *
     * @return    Underwriter
     */
    public Underwriter testRead() throws Throwable {
    LOGGER.info( "UnderwriterTest:testRead()" );
    LOGGER.info( "-- Reading a previously created Underwriter" );

    Underwriter entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read Underwriter with primary key" );
    msg.append( theId );

    try {
        entity = service.getUnderwriter( new UnderwriterFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getUnderwriterId();

        LOGGER.info( "-- Successfully found Underwriter " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a Underwriter.
     *
     * @return    Underwriter
     */
    public Underwriter testUpdate() throws Throwable {
    LOGGER.info( "UnderwriterTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a Underwriter." );

    StringBuilder msg = new StringBuilder( "Failed to update a Underwriter : " );
    Underwriter entity = null;

    try {
        UpdateUnderwriterCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setUnderwriterId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created Underwriter." );

        entity = service.updateUnderwriter( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved Underwriter - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a Underwriter.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "UnderwriterTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created Underwriter." );

    try {
        DeleteUnderwriterCommand deleteCommand = new DeleteUnderwriterCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted Underwriter with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete Underwriter with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all Underwriters.
     *
     * @return    Collection
     */
    public List<Underwriter> testGetAll() throws Throwable {
    LOGGER.info( "UnderwriterTest:testGetAll() - Retrieving Collection of Underwriters:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all Underwriter : " );
    List<Underwriter> collection  = null;

    try {
        // call the static get method on the UnderwriterService
        collection = service.getAllUnderwriter();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            Underwriter currEntity  = null;
            Iterator<Underwriter> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getUnderwriterId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public UnderwriterTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated Underwriter
     *
     * @return CreateUnderwriterCommand alias
     */
	protected CreateUnderwriterCommand generateNewCommand() {
    CreateUnderwriterCommand command = new CreateUnderwriterCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Money() );

    return( command );
}

    /**
     * Returns a new populated Underwriter
     *
     * @return UpdateUnderwriterCommand alias
     */
	protected UpdateUnderwriterCommand generateUpdateCommand() {
    UpdateUnderwriterCommand command = new UpdateUnderwriterCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Money(),  new HashSet<>(),  null );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected UnderwriterService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(Underwriter.class.getName());

}
