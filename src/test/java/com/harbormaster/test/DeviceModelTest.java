
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test DeviceModel class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class DeviceModelTest
{

// constructors

    public DeviceModelTest(DeviceModelService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a DeviceModel, through a DeviceModelTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on DeviceModelTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on DeviceModelTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new DeviceModel.
     *
     * @return    DeviceModel
     */
    public DeviceModel testCreate() throws Throwable {
    DeviceModel entity = null;

    LOGGER.info( "DeviceModelTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a DeviceModel");

    StringBuilder msg = new StringBuilder( "-- Failed to create a DeviceModel" );

    try {
        entity = service.createDeviceModel( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getDeviceModelId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a DeviceModel with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a DeviceModel.
     *
     * @return    DeviceModel
     */
    public DeviceModel testRead() throws Throwable {
    LOGGER.info( "DeviceModelTest:testRead()" );
    LOGGER.info( "-- Reading a previously created DeviceModel" );

    DeviceModel entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read DeviceModel with primary key" );
    msg.append( theId );

    try {
        entity = service.getDeviceModel( new DeviceModelFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getDeviceModelId();

        LOGGER.info( "-- Successfully found DeviceModel " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a DeviceModel.
     *
     * @return    DeviceModel
     */
    public DeviceModel testUpdate() throws Throwable {
    LOGGER.info( "DeviceModelTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a DeviceModel." );

    StringBuilder msg = new StringBuilder( "Failed to update a DeviceModel : " );
    DeviceModel entity = null;

    try {
        UpdateDeviceModelCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setDeviceModelId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created DeviceModel." );

        entity = service.updateDeviceModel( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved DeviceModel - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a DeviceModel.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "DeviceModelTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created DeviceModel." );

    try {
        DeleteDeviceModelCommand deleteCommand = new DeleteDeviceModelCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted DeviceModel with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete DeviceModel with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all DeviceModels.
     *
     * @return    Collection
     */
    public List<DeviceModel> testGetAll() throws Throwable {
    LOGGER.info( "DeviceModelTest:testGetAll() - Retrieving Collection of DeviceModels:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all DeviceModel : " );
    List<DeviceModel> collection  = null;

    try {
        // call the static get method on the DeviceModelService
        collection = service.getAllDeviceModel();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            DeviceModel currEntity  = null;
            Iterator<DeviceModel> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getDeviceModelId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public DeviceModelTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated DeviceModel
     *
     * @return CreateDeviceModelCommand alias
     */
	protected CreateDeviceModelCommand generateNewCommand() {
    CreateDeviceModelCommand command = new CreateDeviceModelCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  ConnectivityType.values()[0],  TelemetryEncoding.values()[0] );

    return( command );
}

    /**
     * Returns a new populated DeviceModel
     *
     * @return UpdateDeviceModelCommand alias
     */
	protected UpdateDeviceModelCommand generateUpdateCommand() {
    UpdateDeviceModelCommand command = new UpdateDeviceModelCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  null,  new HashSet<>(),  null,  new HashSet<>(),  new HashSet<>(),  ConnectivityType.values()[0],  TelemetryEncoding.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected DeviceModelService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(DeviceModel.class.getName());

}
