/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Insurance Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test Adjuster class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class AdjusterTest
{

// constructors

    public AdjusterTest(AdjusterService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a Adjuster, through a AdjusterTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on AdjusterTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on AdjusterTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new Adjuster.
     *
     * @return    Adjuster
     */
    public Adjuster testCreate() throws Throwable {
    Adjuster entity = null;

    LOGGER.info( "AdjusterTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a Adjuster");

    StringBuilder msg = new StringBuilder( "-- Failed to create a Adjuster" );

    try {
        entity = service.createAdjuster( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getAdjusterId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a Adjuster with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a Adjuster.
     *
     * @return    Adjuster
     */
    public Adjuster testRead() throws Throwable {
    LOGGER.info( "AdjusterTest:testRead()" );
    LOGGER.info( "-- Reading a previously created Adjuster" );

    Adjuster entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read Adjuster with primary key" );
    msg.append( theId );

    try {
        entity = service.getAdjuster( new AdjusterFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getAdjusterId();

        LOGGER.info( "-- Successfully found Adjuster " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a Adjuster.
     *
     * @return    Adjuster
     */
    public Adjuster testUpdate() throws Throwable {
    LOGGER.info( "AdjusterTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a Adjuster." );

    StringBuilder msg = new StringBuilder( "Failed to update a Adjuster : " );
    Adjuster entity = null;

    try {
        UpdateAdjusterCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setAdjusterId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created Adjuster." );

        entity = service.updateAdjuster( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved Adjuster - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a Adjuster.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "AdjusterTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created Adjuster." );

    try {
        DeleteAdjusterCommand deleteCommand = new DeleteAdjusterCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted Adjuster with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete Adjuster with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all Adjusters.
     *
     * @return    Collection
     */
    public List<Adjuster> testGetAll() throws Throwable {
    LOGGER.info( "AdjusterTest:testGetAll() - Retrieving Collection of Adjusters:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all Adjuster : " );
    List<Adjuster> collection  = null;

    try {
        // call the static get method on the AdjusterService
        collection = service.getAllAdjuster();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            Adjuster currEntity  = null;
            Iterator<Adjuster> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getAdjusterId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public AdjusterTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated Adjuster
     *
     * @return CreateAdjusterCommand alias
     */
	protected CreateAdjusterCommand generateNewCommand() {
    CreateAdjusterCommand command = new CreateAdjusterCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  AdjusterType.values()[0] );

    return( command );
}

    /**
     * Returns a new populated Adjuster
     *
     * @return UpdateAdjusterCommand alias
     */
	protected UpdateAdjusterCommand generateUpdateCommand() {
    UpdateAdjusterCommand command = new UpdateAdjusterCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new HashSet<>(),  new HashSet<>(),  AdjusterType.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected AdjusterService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(Adjuster.class.getName());

}
