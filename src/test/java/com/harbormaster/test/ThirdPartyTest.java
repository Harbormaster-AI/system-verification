/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Insurance Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test ThirdParty class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class ThirdPartyTest
{

// constructors

    public ThirdPartyTest(ThirdPartyService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a ThirdParty, through a ThirdPartyTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on ThirdPartyTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on ThirdPartyTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new ThirdParty.
     *
     * @return    ThirdParty
     */
    public ThirdParty testCreate() throws Throwable {
    ThirdParty entity = null;

    LOGGER.info( "ThirdPartyTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a ThirdParty");

    StringBuilder msg = new StringBuilder( "-- Failed to create a ThirdParty" );

    try {
        entity = service.createThirdParty( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getThirdPartyId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a ThirdParty with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a ThirdParty.
     *
     * @return    ThirdParty
     */
    public ThirdParty testRead() throws Throwable {
    LOGGER.info( "ThirdPartyTest:testRead()" );
    LOGGER.info( "-- Reading a previously created ThirdParty" );

    ThirdParty entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read ThirdParty with primary key" );
    msg.append( theId );

    try {
        entity = service.getThirdParty( new ThirdPartyFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getThirdPartyId();

        LOGGER.info( "-- Successfully found ThirdParty " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a ThirdParty.
     *
     * @return    ThirdParty
     */
    public ThirdParty testUpdate() throws Throwable {
    LOGGER.info( "ThirdPartyTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a ThirdParty." );

    StringBuilder msg = new StringBuilder( "Failed to update a ThirdParty : " );
    ThirdParty entity = null;

    try {
        UpdateThirdPartyCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setThirdPartyId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created ThirdParty." );

        entity = service.updateThirdParty( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved ThirdParty - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a ThirdParty.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "ThirdPartyTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created ThirdParty." );

    try {
        DeleteThirdPartyCommand deleteCommand = new DeleteThirdPartyCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted ThirdParty with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete ThirdParty with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all ThirdPartys.
     *
     * @return    Collection
     */
    public List<ThirdParty> testGetAll() throws Throwable {
    LOGGER.info( "ThirdPartyTest:testGetAll() - Retrieving Collection of ThirdPartys:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all ThirdParty : " );
    List<ThirdParty> collection  = null;

    try {
        // call the static get method on the ThirdPartyService
        collection = service.getAllThirdParty();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            ThirdParty currEntity  = null;
            Iterator<ThirdParty> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getThirdPartyId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public ThirdPartyTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated ThirdParty
     *
     * @return CreateThirdPartyCommand alias
     */
	protected CreateThirdPartyCommand generateNewCommand() {
    CreateThirdPartyCommand command = new CreateThirdPartyCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Address(),  ThirdPartyType.values()[0] );

    return( command );
}

    /**
     * Returns a new populated ThirdParty
     *
     * @return UpdateThirdPartyCommand alias
     */
	protected UpdateThirdPartyCommand generateUpdateCommand() {
    UpdateThirdPartyCommand command = new UpdateThirdPartyCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Address(),  new HashSet<>(),  ThirdPartyType.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected ThirdPartyService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(ThirdParty.class.getName());

}
