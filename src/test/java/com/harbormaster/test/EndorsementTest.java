/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Insurance Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test Endorsement class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class EndorsementTest
{

// constructors

    public EndorsementTest(EndorsementService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a Endorsement, through a EndorsementTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on EndorsementTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on EndorsementTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new Endorsement.
     *
     * @return    Endorsement
     */
    public Endorsement testCreate() throws Throwable {
    Endorsement entity = null;

    LOGGER.info( "EndorsementTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a Endorsement");

    StringBuilder msg = new StringBuilder( "-- Failed to create a Endorsement" );

    try {
        entity = service.createEndorsement( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getEndorsementId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a Endorsement with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a Endorsement.
     *
     * @return    Endorsement
     */
    public Endorsement testRead() throws Throwable {
    LOGGER.info( "EndorsementTest:testRead()" );
    LOGGER.info( "-- Reading a previously created Endorsement" );

    Endorsement entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read Endorsement with primary key" );
    msg.append( theId );

    try {
        entity = service.getEndorsement( new EndorsementFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getEndorsementId();

        LOGGER.info( "-- Successfully found Endorsement " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a Endorsement.
     *
     * @return    Endorsement
     */
    public Endorsement testUpdate() throws Throwable {
    LOGGER.info( "EndorsementTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a Endorsement." );

    StringBuilder msg = new StringBuilder( "Failed to update a Endorsement : " );
    Endorsement entity = null;

    try {
        UpdateEndorsementCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setEndorsementId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created Endorsement." );

        entity = service.updateEndorsement( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved Endorsement - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a Endorsement.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "EndorsementTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created Endorsement." );

    try {
        DeleteEndorsementCommand deleteCommand = new DeleteEndorsementCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted Endorsement with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete Endorsement with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all Endorsements.
     *
     * @return    Collection
     */
    public List<Endorsement> testGetAll() throws Throwable {
    LOGGER.info( "EndorsementTest:testGetAll() - Retrieving Collection of Endorsements:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all Endorsement : " );
    List<Endorsement> collection  = null;

    try {
        // call the static get method on the EndorsementService
        collection = service.getAllEndorsement();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            Endorsement currEntity  = null;
            Iterator<Endorsement> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getEndorsementId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public EndorsementTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated Endorsement
     *
     * @return CreateEndorsementCommand alias
     */
	protected CreateEndorsementCommand generateNewCommand() {
    CreateEndorsementCommand command = new CreateEndorsementCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),   new Date(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16) );

    return( command );
}

    /**
     * Returns a new populated Endorsement
     *
     * @return UpdateEndorsementCommand alias
     */
	protected UpdateEndorsementCommand generateUpdateCommand() {
    UpdateEndorsementCommand command = new UpdateEndorsementCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),   new Date(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  null );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected EndorsementService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(Endorsement.class.getName());

}
