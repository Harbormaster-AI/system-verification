/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test ATM class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class ATMTest
{

// constructors

    public ATMTest(ATMService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a ATM, through a ATMTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on ATMTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on ATMTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new ATM.
     *
     * @return    ATM
     */
    public ATM testCreate() throws Throwable {
    ATM entity = null;

    LOGGER.info( "ATMTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a ATM");

    StringBuilder msg = new StringBuilder( "-- Failed to create a ATM" );

    try {
        entity = service.createATM( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getATMId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a ATM with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a ATM.
     *
     * @return    ATM
     */
    public ATM testRead() throws Throwable {
    LOGGER.info( "ATMTest:testRead()" );
    LOGGER.info( "-- Reading a previously created ATM" );

    ATM entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read ATM with primary key" );
    msg.append( theId );

    try {
        entity = service.getATM( new ATMFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getATMId();

        LOGGER.info( "-- Successfully found ATM " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a ATM.
     *
     * @return    ATM
     */
    public ATM testUpdate() throws Throwable {
    LOGGER.info( "ATMTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a ATM." );

    StringBuilder msg = new StringBuilder( "Failed to update a ATM : " );
    ATM entity = null;

    try {
        UpdateATMCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setATMId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created ATM." );

        entity = service.updateATM( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved ATM - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a ATM.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "ATMTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created ATM." );

    try {
        DeleteATMCommand deleteCommand = new DeleteATMCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted ATM with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete ATM with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all ATMs.
     *
     * @return    Collection
     */
    public List<ATM> testGetAll() throws Throwable {
    LOGGER.info( "ATMTest:testGetAll() - Retrieving Collection of ATMs:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all ATM : " );
    List<ATM> collection  = null;

    try {
        // call the static get method on the ATMService
        collection = service.getAllATM();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            ATM currEntity  = null;
            Iterator<ATM> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getATMId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public ATMTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated ATM
     *
     * @return CreateATMCommand alias
     */
	protected CreateATMCommand generateNewCommand() {
    CreateATMCommand command = new CreateATMCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Address(),  ATMStatus.values()[0] );

    return( command );
}

    /**
     * Returns a new populated ATM
     *
     * @return UpdateATMCommand alias
     */
	protected UpdateATMCommand generateUpdateCommand() {
    UpdateATMCommand command = new UpdateATMCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Address(),  null,  ATMStatus.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected ATMService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(ATM.class.getName());

}
