
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test Alert class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class AlertTest
{

// constructors

    public AlertTest(AlertService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a Alert, through a AlertTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on AlertTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on AlertTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new Alert.
     *
     * @return    Alert
     */
    public Alert testCreate() throws Throwable {
    Alert entity = null;

    LOGGER.info( "AlertTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a Alert");

    StringBuilder msg = new StringBuilder( "-- Failed to create a Alert" );

    try {
        entity = service.createAlert( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getAlertId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a Alert with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a Alert.
     *
     * @return    Alert
     */
    public Alert testRead() throws Throwable {
    LOGGER.info( "AlertTest:testRead()" );
    LOGGER.info( "-- Reading a previously created Alert" );

    Alert entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read Alert with primary key" );
    msg.append( theId );

    try {
        entity = service.getAlert( new AlertFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getAlertId();

        LOGGER.info( "-- Successfully found Alert " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a Alert.
     *
     * @return    Alert
     */
    public Alert testUpdate() throws Throwable {
    LOGGER.info( "AlertTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a Alert." );

    StringBuilder msg = new StringBuilder( "Failed to update a Alert : " );
    Alert entity = null;

    try {
        UpdateAlertCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setAlertId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created Alert." );

        entity = service.updateAlert( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved Alert - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a Alert.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "AlertTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created Alert." );

    try {
        DeleteAlertCommand deleteCommand = new DeleteAlertCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted Alert with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete Alert with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all Alerts.
     *
     * @return    Collection
     */
    public List<Alert> testGetAll() throws Throwable {
    LOGGER.info( "AlertTest:testGetAll() - Retrieving Collection of Alerts:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all Alert : " );
    List<Alert> collection  = null;

    try {
        // call the static get method on the AlertService
        collection = service.getAllAlert();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            Alert currEntity  = null;
            Iterator<Alert> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getAlertId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public AlertTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated Alert
     *
     * @return CreateAlertCommand alias
     */
	protected CreateAlertCommand generateNewCommand() {
    CreateAlertCommand command = new CreateAlertCommand( null,   new Date(),   new Date(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  AlertStatus.values()[0] );

    return( command );
}

    /**
     * Returns a new populated Alert
     *
     * @return UpdateAlertCommand alias
     */
	protected UpdateAlertCommand generateUpdateCommand() {
    UpdateAlertCommand command = new UpdateAlertCommand( null,   new Date(),   new Date(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  null,  null,  AlertStatus.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected AlertService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(Alert.class.getName());

}
