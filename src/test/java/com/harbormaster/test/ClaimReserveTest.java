/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Insurance Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test ClaimReserve class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class ClaimReserveTest
{

// constructors

    public ClaimReserveTest(ClaimReserveService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a ClaimReserve, through a ClaimReserveTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on ClaimReserveTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on ClaimReserveTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new ClaimReserve.
     *
     * @return    ClaimReserve
     */
    public ClaimReserve testCreate() throws Throwable {
    ClaimReserve entity = null;

    LOGGER.info( "ClaimReserveTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a ClaimReserve");

    StringBuilder msg = new StringBuilder( "-- Failed to create a ClaimReserve" );

    try {
        entity = service.createClaimReserve( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getClaimReserveId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a ClaimReserve with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a ClaimReserve.
     *
     * @return    ClaimReserve
     */
    public ClaimReserve testRead() throws Throwable {
    LOGGER.info( "ClaimReserveTest:testRead()" );
    LOGGER.info( "-- Reading a previously created ClaimReserve" );

    ClaimReserve entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read ClaimReserve with primary key" );
    msg.append( theId );

    try {
        entity = service.getClaimReserve( new ClaimReserveFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getClaimReserveId();

        LOGGER.info( "-- Successfully found ClaimReserve " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a ClaimReserve.
     *
     * @return    ClaimReserve
     */
    public ClaimReserve testUpdate() throws Throwable {
    LOGGER.info( "ClaimReserveTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a ClaimReserve." );

    StringBuilder msg = new StringBuilder( "Failed to update a ClaimReserve : " );
    ClaimReserve entity = null;

    try {
        UpdateClaimReserveCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setClaimReserveId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created ClaimReserve." );

        entity = service.updateClaimReserve( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved ClaimReserve - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a ClaimReserve.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "ClaimReserveTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created ClaimReserve." );

    try {
        DeleteClaimReserveCommand deleteCommand = new DeleteClaimReserveCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted ClaimReserve with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete ClaimReserve with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all ClaimReserves.
     *
     * @return    Collection
     */
    public List<ClaimReserve> testGetAll() throws Throwable {
    LOGGER.info( "ClaimReserveTest:testGetAll() - Retrieving Collection of ClaimReserves:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all ClaimReserve : " );
    List<ClaimReserve> collection  = null;

    try {
        // call the static get method on the ClaimReserveService
        collection = service.getAllClaimReserve();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            ClaimReserve currEntity  = null;
            Iterator<ClaimReserve> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getClaimReserveId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public ClaimReserveTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated ClaimReserve
     *
     * @return CreateClaimReserveCommand alias
     */
	protected CreateClaimReserveCommand generateNewCommand() {
    CreateClaimReserveCommand command = new CreateClaimReserveCommand( null,  new Money(),   new Date(),  ReserveType.values()[0],  ReserveStatus.values()[0] );

    return( command );
}

    /**
     * Returns a new populated ClaimReserve
     *
     * @return UpdateClaimReserveCommand alias
     */
	protected UpdateClaimReserveCommand generateUpdateCommand() {
    UpdateClaimReserveCommand command = new UpdateClaimReserveCommand( null,  new Money(),   new Date(),  null,  null,  ReserveType.values()[0],  ReserveStatus.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected ClaimReserveService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(ClaimReserve.class.getName());

}
