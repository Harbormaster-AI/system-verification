
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test Gateway class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class GatewayTest
{

// constructors

    public GatewayTest(GatewayService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a Gateway, through a GatewayTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on GatewayTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on GatewayTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new Gateway.
     *
     * @return    Gateway
     */
    public Gateway testCreate() throws Throwable {
    Gateway entity = null;

    LOGGER.info( "GatewayTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a Gateway");

    StringBuilder msg = new StringBuilder( "-- Failed to create a Gateway" );

    try {
        entity = service.createGateway( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getGatewayId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a Gateway with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a Gateway.
     *
     * @return    Gateway
     */
    public Gateway testRead() throws Throwable {
    LOGGER.info( "GatewayTest:testRead()" );
    LOGGER.info( "-- Reading a previously created Gateway" );

    Gateway entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read Gateway with primary key" );
    msg.append( theId );

    try {
        entity = service.getGateway( new GatewayFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getGatewayId();

        LOGGER.info( "-- Successfully found Gateway " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a Gateway.
     *
     * @return    Gateway
     */
    public Gateway testUpdate() throws Throwable {
    LOGGER.info( "GatewayTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a Gateway." );

    StringBuilder msg = new StringBuilder( "Failed to update a Gateway : " );
    Gateway entity = null;

    try {
        UpdateGatewayCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setGatewayId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created Gateway." );

        entity = service.updateGateway( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved Gateway - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a Gateway.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "GatewayTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created Gateway." );

    try {
        DeleteGatewayCommand deleteCommand = new DeleteGatewayCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted Gateway with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete Gateway with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all Gateways.
     *
     * @return    Collection
     */
    public List<Gateway> testGetAll() throws Throwable {
    LOGGER.info( "GatewayTest:testGetAll() - Retrieving Collection of Gateways:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all Gateway : " );
    List<Gateway> collection  = null;

    try {
        // call the static get method on the GatewayService
        collection = service.getAllGateway();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            Gateway currEntity  = null;
            Iterator<Gateway> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getGatewayId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public GatewayTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated Gateway
     *
     * @return CreateGatewayCommand alias
     */
	protected CreateGatewayCommand generateNewCommand() {
    CreateGatewayCommand command = new CreateGatewayCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  DeviceStatus.values()[0] );

    return( command );
}

    /**
     * Returns a new populated Gateway
     *
     * @return UpdateGatewayCommand alias
     */
	protected UpdateGatewayCommand generateUpdateCommand() {
    UpdateGatewayCommand command = new UpdateGatewayCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  null,  null,  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  null,  new HashSet<>(),  DeviceStatus.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected GatewayService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(Gateway.class.getName());

}
