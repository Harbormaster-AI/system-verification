/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Insurance Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test SubrogationRecovery class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class SubrogationRecoveryTest
{

// constructors

    public SubrogationRecoveryTest(SubrogationRecoveryService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a SubrogationRecovery, through a SubrogationRecoveryTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on SubrogationRecoveryTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on SubrogationRecoveryTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new SubrogationRecovery.
     *
     * @return    SubrogationRecovery
     */
    public SubrogationRecovery testCreate() throws Throwable {
    SubrogationRecovery entity = null;

    LOGGER.info( "SubrogationRecoveryTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a SubrogationRecovery");

    StringBuilder msg = new StringBuilder( "-- Failed to create a SubrogationRecovery" );

    try {
        entity = service.createSubrogationRecovery( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getSubrogationRecoveryId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a SubrogationRecovery with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a SubrogationRecovery.
     *
     * @return    SubrogationRecovery
     */
    public SubrogationRecovery testRead() throws Throwable {
    LOGGER.info( "SubrogationRecoveryTest:testRead()" );
    LOGGER.info( "-- Reading a previously created SubrogationRecovery" );

    SubrogationRecovery entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read SubrogationRecovery with primary key" );
    msg.append( theId );

    try {
        entity = service.getSubrogationRecovery( new SubrogationRecoveryFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getSubrogationRecoveryId();

        LOGGER.info( "-- Successfully found SubrogationRecovery " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a SubrogationRecovery.
     *
     * @return    SubrogationRecovery
     */
    public SubrogationRecovery testUpdate() throws Throwable {
    LOGGER.info( "SubrogationRecoveryTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a SubrogationRecovery." );

    StringBuilder msg = new StringBuilder( "Failed to update a SubrogationRecovery : " );
    SubrogationRecovery entity = null;

    try {
        UpdateSubrogationRecoveryCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setSubrogationRecoveryId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created SubrogationRecovery." );

        entity = service.updateSubrogationRecovery( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved SubrogationRecovery - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a SubrogationRecovery.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "SubrogationRecoveryTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created SubrogationRecovery." );

    try {
        DeleteSubrogationRecoveryCommand deleteCommand = new DeleteSubrogationRecoveryCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted SubrogationRecovery with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete SubrogationRecovery with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all SubrogationRecoverys.
     *
     * @return    Collection
     */
    public List<SubrogationRecovery> testGetAll() throws Throwable {
    LOGGER.info( "SubrogationRecoveryTest:testGetAll() - Retrieving Collection of SubrogationRecoverys:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all SubrogationRecovery : " );
    List<SubrogationRecovery> collection  = null;

    try {
        // call the static get method on the SubrogationRecoveryService
        collection = service.getAllSubrogationRecovery();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            SubrogationRecovery currEntity  = null;
            Iterator<SubrogationRecovery> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getSubrogationRecoveryId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public SubrogationRecoveryTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated SubrogationRecovery
     *
     * @return CreateSubrogationRecoveryCommand alias
     */
	protected CreateSubrogationRecoveryCommand generateNewCommand() {
    CreateSubrogationRecoveryCommand command = new CreateSubrogationRecoveryCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Money(),   new Date(),  SubrogationStatus.values()[0] );

    return( command );
}

    /**
     * Returns a new populated SubrogationRecovery
     *
     * @return UpdateSubrogationRecoveryCommand alias
     */
	protected UpdateSubrogationRecoveryCommand generateUpdateCommand() {
    UpdateSubrogationRecoveryCommand command = new UpdateSubrogationRecoveryCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Money(),   new Date(),  null,  null,  null,  SubrogationStatus.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected SubrogationRecoveryService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(SubrogationRecovery.class.getName());

}
