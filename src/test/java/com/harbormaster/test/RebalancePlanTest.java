/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * ${blueprint.getModelMetadata().getName()}
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test RebalancePlan class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class RebalancePlanTest
{

// constructors

    public RebalancePlanTest(RebalancePlanService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a RebalancePlan, through a RebalancePlanTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on RebalancePlanTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on RebalancePlanTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new RebalancePlan.
     *
     * @return    RebalancePlan
     */
    public RebalancePlan testCreate() throws Throwable {
    RebalancePlan entity = null;

    LOGGER.info( "RebalancePlanTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a RebalancePlan");

    StringBuilder msg = new StringBuilder( "-- Failed to create a RebalancePlan" );

    try {
        entity = service.createRebalancePlan( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getRebalancePlanId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a RebalancePlan with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a RebalancePlan.
     *
     * @return    RebalancePlan
     */
    public RebalancePlan testRead() throws Throwable {
    LOGGER.info( "RebalancePlanTest:testRead()" );
    LOGGER.info( "-- Reading a previously created RebalancePlan" );

    RebalancePlan entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read RebalancePlan with primary key" );
    msg.append( theId );

    try {
        entity = service.getRebalancePlan( new RebalancePlanFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getRebalancePlanId();

        LOGGER.info( "-- Successfully found RebalancePlan " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a RebalancePlan.
     *
     * @return    RebalancePlan
     */
    public RebalancePlan testUpdate() throws Throwable {
    LOGGER.info( "RebalancePlanTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a RebalancePlan." );

    StringBuilder msg = new StringBuilder( "Failed to update a RebalancePlan : " );
    RebalancePlan entity = null;

    try {
        UpdateRebalancePlanCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setRebalancePlanId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created RebalancePlan." );

        entity = service.updateRebalancePlan( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved RebalancePlan - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a RebalancePlan.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "RebalancePlanTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created RebalancePlan." );

    try {
        DeleteRebalancePlanCommand deleteCommand = new DeleteRebalancePlanCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted RebalancePlan with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete RebalancePlan with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all RebalancePlans.
     *
     * @return    Collection
     */
    public List<RebalancePlan> testGetAll() throws Throwable {
    LOGGER.info( "RebalancePlanTest:testGetAll() - Retrieving Collection of RebalancePlans:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all RebalancePlan : " );
    List<RebalancePlan> collection  = null;

    try {
        // call the static get method on the RebalancePlanService
        collection = service.getAllRebalancePlan();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            RebalancePlan currEntity  = null;
            Iterator<RebalancePlan> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getRebalancePlanId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public RebalancePlanTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated RebalancePlan
     *
     * @return CreateRebalancePlanCommand alias
     */
	protected CreateRebalancePlanCommand generateNewCommand() {
    CreateRebalancePlanCommand command = new CreateRebalancePlanCommand( null,   new Date(),  RebalanceStatus.values()[0],  RebalanceMethod.values()[0] );

    return( command );
}

    /**
     * Returns a new populated RebalancePlan
     *
     * @return UpdateRebalancePlanCommand alias
     */
	protected UpdateRebalancePlanCommand generateUpdateCommand() {
    UpdateRebalancePlanCommand command = new UpdateRebalancePlanCommand( null,   new Date(),  null,  new HashSet<>(),  null,  RebalanceStatus.values()[0],  RebalanceMethod.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected RebalancePlanService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(RebalancePlan.class.getName());

}
