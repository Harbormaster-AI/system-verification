
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test FirmwareRelease class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class FirmwareReleaseTest
{

// constructors

    public FirmwareReleaseTest(FirmwareReleaseService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a FirmwareRelease, through a FirmwareReleaseTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on FirmwareReleaseTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on FirmwareReleaseTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new FirmwareRelease.
     *
     * @return    FirmwareRelease
     */
    public FirmwareRelease testCreate() throws Throwable {
    FirmwareRelease entity = null;

    LOGGER.info( "FirmwareReleaseTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a FirmwareRelease");

    StringBuilder msg = new StringBuilder( "-- Failed to create a FirmwareRelease" );

    try {
        entity = service.createFirmwareRelease( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getFirmwareReleaseId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a FirmwareRelease with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a FirmwareRelease.
     *
     * @return    FirmwareRelease
     */
    public FirmwareRelease testRead() throws Throwable {
    LOGGER.info( "FirmwareReleaseTest:testRead()" );
    LOGGER.info( "-- Reading a previously created FirmwareRelease" );

    FirmwareRelease entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read FirmwareRelease with primary key" );
    msg.append( theId );

    try {
        entity = service.getFirmwareRelease( new FirmwareReleaseFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getFirmwareReleaseId();

        LOGGER.info( "-- Successfully found FirmwareRelease " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a FirmwareRelease.
     *
     * @return    FirmwareRelease
     */
    public FirmwareRelease testUpdate() throws Throwable {
    LOGGER.info( "FirmwareReleaseTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a FirmwareRelease." );

    StringBuilder msg = new StringBuilder( "Failed to update a FirmwareRelease : " );
    FirmwareRelease entity = null;

    try {
        UpdateFirmwareReleaseCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setFirmwareReleaseId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created FirmwareRelease." );

        entity = service.updateFirmwareRelease( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved FirmwareRelease - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a FirmwareRelease.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "FirmwareReleaseTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created FirmwareRelease." );

    try {
        DeleteFirmwareReleaseCommand deleteCommand = new DeleteFirmwareReleaseCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted FirmwareRelease with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete FirmwareRelease with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all FirmwareReleases.
     *
     * @return    Collection
     */
    public List<FirmwareRelease> testGetAll() throws Throwable {
    LOGGER.info( "FirmwareReleaseTest:testGetAll() - Retrieving Collection of FirmwareReleases:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all FirmwareRelease : " );
    List<FirmwareRelease> collection  = null;

    try {
        // call the static get method on the FirmwareReleaseService
        collection = service.getAllFirmwareRelease();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            FirmwareRelease currEntity  = null;
            Iterator<FirmwareRelease> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getFirmwareReleaseId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public FirmwareReleaseTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated FirmwareRelease
     *
     * @return CreateFirmwareReleaseCommand alias
     */
	protected CreateFirmwareReleaseCommand generateNewCommand() {
    CreateFirmwareReleaseCommand command = new CreateFirmwareReleaseCommand( null,  new FirmwareVersion(),   new Date(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Checksum() );

    return( command );
}

    /**
     * Returns a new populated FirmwareRelease
     *
     * @return UpdateFirmwareReleaseCommand alias
     */
	protected UpdateFirmwareReleaseCommand generateUpdateCommand() {
    UpdateFirmwareReleaseCommand command = new UpdateFirmwareReleaseCommand( null,  new FirmwareVersion(),   new Date(),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Checksum(),  null );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected FirmwareReleaseService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(FirmwareRelease.class.getName());

}
