/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Insurance Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test InsuranceProduct class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class InsuranceProductTest
{

// constructors

    public InsuranceProductTest(InsuranceProductService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a InsuranceProduct, through a InsuranceProductTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on InsuranceProductTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on InsuranceProductTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new InsuranceProduct.
     *
     * @return    InsuranceProduct
     */
    public InsuranceProduct testCreate() throws Throwable {
    InsuranceProduct entity = null;

    LOGGER.info( "InsuranceProductTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a InsuranceProduct");

    StringBuilder msg = new StringBuilder( "-- Failed to create a InsuranceProduct" );

    try {
        entity = service.createInsuranceProduct( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getInsuranceProductId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a InsuranceProduct with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a InsuranceProduct.
     *
     * @return    InsuranceProduct
     */
    public InsuranceProduct testRead() throws Throwable {
    LOGGER.info( "InsuranceProductTest:testRead()" );
    LOGGER.info( "-- Reading a previously created InsuranceProduct" );

    InsuranceProduct entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read InsuranceProduct with primary key" );
    msg.append( theId );

    try {
        entity = service.getInsuranceProduct( new InsuranceProductFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getInsuranceProductId();

        LOGGER.info( "-- Successfully found InsuranceProduct " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a InsuranceProduct.
     *
     * @return    InsuranceProduct
     */
    public InsuranceProduct testUpdate() throws Throwable {
    LOGGER.info( "InsuranceProductTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a InsuranceProduct." );

    StringBuilder msg = new StringBuilder( "Failed to update a InsuranceProduct : " );
    InsuranceProduct entity = null;

    try {
        UpdateInsuranceProductCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setInsuranceProductId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created InsuranceProduct." );

        entity = service.updateInsuranceProduct( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved InsuranceProduct - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a InsuranceProduct.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "InsuranceProductTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created InsuranceProduct." );

    try {
        DeleteInsuranceProductCommand deleteCommand = new DeleteInsuranceProductCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted InsuranceProduct with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete InsuranceProduct with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all InsuranceProducts.
     *
     * @return    Collection
     */
    public List<InsuranceProduct> testGetAll() throws Throwable {
    LOGGER.info( "InsuranceProductTest:testGetAll() - Retrieving Collection of InsuranceProducts:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all InsuranceProduct : " );
    List<InsuranceProduct> collection  = null;

    try {
        // call the static get method on the InsuranceProductService
        collection = service.getAllInsuranceProduct();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            InsuranceProduct currEntity  = null;
            Iterator<InsuranceProduct> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getInsuranceProductId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public InsuranceProductTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated InsuranceProduct
     *
     * @return CreateInsuranceProductCommand alias
     */
	protected CreateInsuranceProductCommand generateNewCommand() {
    CreateInsuranceProductCommand command = new CreateInsuranceProductCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  LineOfBusiness.values()[0] );

    return( command );
}

    /**
     * Returns a new populated InsuranceProduct
     *
     * @return UpdateInsuranceProductCommand alias
     */
	protected UpdateInsuranceProductCommand generateUpdateCommand() {
    UpdateInsuranceProductCommand command = new UpdateInsuranceProductCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  null,  new HashSet<>(),  LineOfBusiness.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected InsuranceProductService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(InsuranceProduct.class.getName());

}
