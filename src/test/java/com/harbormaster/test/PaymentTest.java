/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Insurance Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test Payment class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class PaymentTest
{

// constructors

    public PaymentTest(PaymentService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a Payment, through a PaymentTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on PaymentTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on PaymentTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new Payment.
     *
     * @return    Payment
     */
    public Payment testCreate() throws Throwable {
    Payment entity = null;

    LOGGER.info( "PaymentTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a Payment");

    StringBuilder msg = new StringBuilder( "-- Failed to create a Payment" );

    try {
        entity = service.createPayment( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getPaymentId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a Payment with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a Payment.
     *
     * @return    Payment
     */
    public Payment testRead() throws Throwable {
    LOGGER.info( "PaymentTest:testRead()" );
    LOGGER.info( "-- Reading a previously created Payment" );

    Payment entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read Payment with primary key" );
    msg.append( theId );

    try {
        entity = service.getPayment( new PaymentFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getPaymentId();

        LOGGER.info( "-- Successfully found Payment " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a Payment.
     *
     * @return    Payment
     */
    public Payment testUpdate() throws Throwable {
    LOGGER.info( "PaymentTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a Payment." );

    StringBuilder msg = new StringBuilder( "Failed to update a Payment : " );
    Payment entity = null;

    try {
        UpdatePaymentCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setPaymentId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created Payment." );

        entity = service.updatePayment( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved Payment - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a Payment.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "PaymentTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created Payment." );

    try {
        DeletePaymentCommand deleteCommand = new DeletePaymentCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted Payment with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete Payment with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all Payments.
     *
     * @return    Collection
     */
    public List<Payment> testGetAll() throws Throwable {
    LOGGER.info( "PaymentTest:testGetAll() - Retrieving Collection of Payments:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all Payment : " );
    List<Payment> collection  = null;

    try {
        // call the static get method on the PaymentService
        collection = service.getAllPayment();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            Payment currEntity  = null;
            Iterator<Payment> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getPaymentId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public PaymentTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated Payment
     *
     * @return CreatePaymentCommand alias
     */
	protected CreatePaymentCommand generateNewCommand() {
    CreatePaymentCommand command = new CreatePaymentCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Money(),   new Date(),  PaymentMethod.values()[0],  PaymentStatus.values()[0] );

    return( command );
}

    /**
     * Returns a new populated Payment
     *
     * @return UpdatePaymentCommand alias
     */
	protected UpdatePaymentCommand generateUpdateCommand() {
    UpdatePaymentCommand command = new UpdatePaymentCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Money(),   new Date(),  null,  null,  null,  PaymentMethod.values()[0],  PaymentStatus.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected PaymentService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(Payment.class.getName());

}
