
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test DeviceVendor class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class DeviceVendorTest
{

// constructors

    public DeviceVendorTest(DeviceVendorService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a DeviceVendor, through a DeviceVendorTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on DeviceVendorTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on DeviceVendorTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new DeviceVendor.
     *
     * @return    DeviceVendor
     */
    public DeviceVendor testCreate() throws Throwable {
    DeviceVendor entity = null;

    LOGGER.info( "DeviceVendorTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a DeviceVendor");

    StringBuilder msg = new StringBuilder( "-- Failed to create a DeviceVendor" );

    try {
        entity = service.createDeviceVendor( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getDeviceVendorId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a DeviceVendor with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a DeviceVendor.
     *
     * @return    DeviceVendor
     */
    public DeviceVendor testRead() throws Throwable {
    LOGGER.info( "DeviceVendorTest:testRead()" );
    LOGGER.info( "-- Reading a previously created DeviceVendor" );

    DeviceVendor entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read DeviceVendor with primary key" );
    msg.append( theId );

    try {
        entity = service.getDeviceVendor( new DeviceVendorFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getDeviceVendorId();

        LOGGER.info( "-- Successfully found DeviceVendor " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a DeviceVendor.
     *
     * @return    DeviceVendor
     */
    public DeviceVendor testUpdate() throws Throwable {
    LOGGER.info( "DeviceVendorTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a DeviceVendor." );

    StringBuilder msg = new StringBuilder( "Failed to update a DeviceVendor : " );
    DeviceVendor entity = null;

    try {
        UpdateDeviceVendorCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setDeviceVendorId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created DeviceVendor." );

        entity = service.updateDeviceVendor( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved DeviceVendor - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a DeviceVendor.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "DeviceVendorTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created DeviceVendor." );

    try {
        DeleteDeviceVendorCommand deleteCommand = new DeleteDeviceVendorCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted DeviceVendor with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete DeviceVendor with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all DeviceVendors.
     *
     * @return    Collection
     */
    public List<DeviceVendor> testGetAll() throws Throwable {
    LOGGER.info( "DeviceVendorTest:testGetAll() - Retrieving Collection of DeviceVendors:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all DeviceVendor : " );
    List<DeviceVendor> collection  = null;

    try {
        // call the static get method on the DeviceVendorService
        collection = service.getAllDeviceVendor();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            DeviceVendor currEntity  = null;
            Iterator<DeviceVendor> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getDeviceVendorId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public DeviceVendorTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated DeviceVendor
     *
     * @return CreateDeviceVendorCommand alias
     */
	protected CreateDeviceVendorCommand generateNewCommand() {
    CreateDeviceVendorCommand command = new CreateDeviceVendorCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16) );

    return( command );
}

    /**
     * Returns a new populated DeviceVendor
     *
     * @return UpdateDeviceVendorCommand alias
     */
	protected UpdateDeviceVendorCommand generateUpdateCommand() {
    UpdateDeviceVendorCommand command = new UpdateDeviceVendorCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new HashSet<>(),  new HashSet<>(),  new HashSet<>() );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected DeviceVendorService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(DeviceVendor.class.getName());

}
