
/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * IoT Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test SimCard class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class SimCardTest
{

// constructors

    public SimCardTest(SimCardService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a SimCard, through a SimCardTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on SimCardTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on SimCardTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new SimCard.
     *
     * @return    SimCard
     */
    public SimCard testCreate() throws Throwable {
    SimCard entity = null;

    LOGGER.info( "SimCardTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a SimCard");

    StringBuilder msg = new StringBuilder( "-- Failed to create a SimCard" );

    try {
        entity = service.createSimCard( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getSimCardId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a SimCard with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a SimCard.
     *
     * @return    SimCard
     */
    public SimCard testRead() throws Throwable {
    LOGGER.info( "SimCardTest:testRead()" );
    LOGGER.info( "-- Reading a previously created SimCard" );

    SimCard entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read SimCard with primary key" );
    msg.append( theId );

    try {
        entity = service.getSimCard( new SimCardFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getSimCardId();

        LOGGER.info( "-- Successfully found SimCard " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a SimCard.
     *
     * @return    SimCard
     */
    public SimCard testUpdate() throws Throwable {
    LOGGER.info( "SimCardTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a SimCard." );

    StringBuilder msg = new StringBuilder( "Failed to update a SimCard : " );
    SimCard entity = null;

    try {
        UpdateSimCardCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setSimCardId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created SimCard." );

        entity = service.updateSimCard( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved SimCard - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a SimCard.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "SimCardTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created SimCard." );

    try {
        DeleteSimCardCommand deleteCommand = new DeleteSimCardCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted SimCard with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete SimCard with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all SimCards.
     *
     * @return    Collection
     */
    public List<SimCard> testGetAll() throws Throwable {
    LOGGER.info( "SimCardTest:testGetAll() - Retrieving Collection of SimCards:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all SimCard : " );
    List<SimCard> collection  = null;

    try {
        // call the static get method on the SimCardService
        collection = service.getAllSimCard();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            SimCard currEntity  = null;
            Iterator<SimCard> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getSimCardId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public SimCardTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated SimCard
     *
     * @return CreateSimCardCommand alias
     */
	protected CreateSimCardCommand generateNewCommand() {
    CreateSimCardCommand command = new CreateSimCardCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  SimStatus.values()[0] );

    return( command );
}

    /**
     * Returns a new populated SimCard
     *
     * @return UpdateSimCardCommand alias
     */
	protected UpdateSimCardCommand generateUpdateCommand() {
    UpdateSimCardCommand command = new UpdateSimCardCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new HashSet<>(),  null,  null,  SimStatus.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected SimCardService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(SimCard.class.getName());

}
