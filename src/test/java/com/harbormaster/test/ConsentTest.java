/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Banking Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test Consent class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class ConsentTest
{

// constructors

    public ConsentTest(ConsentService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a Consent, through a ConsentTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on ConsentTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on ConsentTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new Consent.
     *
     * @return    Consent
     */
    public Consent testCreate() throws Throwable {
    Consent entity = null;

    LOGGER.info( "ConsentTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a Consent");

    StringBuilder msg = new StringBuilder( "-- Failed to create a Consent" );

    try {
        entity = service.createConsent( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getConsentId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a Consent with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a Consent.
     *
     * @return    Consent
     */
    public Consent testRead() throws Throwable {
    LOGGER.info( "ConsentTest:testRead()" );
    LOGGER.info( "-- Reading a previously created Consent" );

    Consent entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read Consent with primary key" );
    msg.append( theId );

    try {
        entity = service.getConsent( new ConsentFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getConsentId();

        LOGGER.info( "-- Successfully found Consent " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a Consent.
     *
     * @return    Consent
     */
    public Consent testUpdate() throws Throwable {
    LOGGER.info( "ConsentTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a Consent." );

    StringBuilder msg = new StringBuilder( "Failed to update a Consent : " );
    Consent entity = null;

    try {
        UpdateConsentCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setConsentId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created Consent." );

        entity = service.updateConsent( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved Consent - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a Consent.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "ConsentTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created Consent." );

    try {
        DeleteConsentCommand deleteCommand = new DeleteConsentCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted Consent with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete Consent with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all Consents.
     *
     * @return    Collection
     */
    public List<Consent> testGetAll() throws Throwable {
    LOGGER.info( "ConsentTest:testGetAll() - Retrieving Collection of Consents:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all Consent : " );
    List<Consent> collection  = null;

    try {
        // call the static get method on the ConsentService
        collection = service.getAllConsent();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            Consent currEntity  = null;
            Iterator<Consent> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getConsentId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public ConsentTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated Consent
     *
     * @return CreateConsentCommand alias
     */
	protected CreateConsentCommand generateNewCommand() {
    CreateConsentCommand command = new CreateConsentCommand( null,   new Date(),   new Date(),  ConsentType.values()[0],  ConsentStatus.values()[0] );

    return( command );
}

    /**
     * Returns a new populated Consent
     *
     * @return UpdateConsentCommand alias
     */
	protected UpdateConsentCommand generateUpdateCommand() {
    UpdateConsentCommand command = new UpdateConsentCommand( null,   new Date(),   new Date(),  null,  null,  new HashSet<>(),  null,  ConsentType.values()[0],  ConsentStatus.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected ConsentService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(Consent.class.getName());

}
