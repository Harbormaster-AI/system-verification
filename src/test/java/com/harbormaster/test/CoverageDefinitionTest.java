/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Insurance Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test CoverageDefinition class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class CoverageDefinitionTest
{

// constructors

    public CoverageDefinitionTest(CoverageDefinitionService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a CoverageDefinition, through a CoverageDefinitionTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on CoverageDefinitionTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on CoverageDefinitionTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new CoverageDefinition.
     *
     * @return    CoverageDefinition
     */
    public CoverageDefinition testCreate() throws Throwable {
    CoverageDefinition entity = null;

    LOGGER.info( "CoverageDefinitionTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a CoverageDefinition");

    StringBuilder msg = new StringBuilder( "-- Failed to create a CoverageDefinition" );

    try {
        entity = service.createCoverageDefinition( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getCoverageDefinitionId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a CoverageDefinition with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a CoverageDefinition.
     *
     * @return    CoverageDefinition
     */
    public CoverageDefinition testRead() throws Throwable {
    LOGGER.info( "CoverageDefinitionTest:testRead()" );
    LOGGER.info( "-- Reading a previously created CoverageDefinition" );

    CoverageDefinition entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read CoverageDefinition with primary key" );
    msg.append( theId );

    try {
        entity = service.getCoverageDefinition( new CoverageDefinitionFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getCoverageDefinitionId();

        LOGGER.info( "-- Successfully found CoverageDefinition " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a CoverageDefinition.
     *
     * @return    CoverageDefinition
     */
    public CoverageDefinition testUpdate() throws Throwable {
    LOGGER.info( "CoverageDefinitionTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a CoverageDefinition." );

    StringBuilder msg = new StringBuilder( "Failed to update a CoverageDefinition : " );
    CoverageDefinition entity = null;

    try {
        UpdateCoverageDefinitionCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setCoverageDefinitionId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created CoverageDefinition." );

        entity = service.updateCoverageDefinition( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved CoverageDefinition - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a CoverageDefinition.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "CoverageDefinitionTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created CoverageDefinition." );

    try {
        DeleteCoverageDefinitionCommand deleteCommand = new DeleteCoverageDefinitionCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted CoverageDefinition with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete CoverageDefinition with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all CoverageDefinitions.
     *
     * @return    Collection
     */
    public List<CoverageDefinition> testGetAll() throws Throwable {
    LOGGER.info( "CoverageDefinitionTest:testGetAll() - Retrieving Collection of CoverageDefinitions:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all CoverageDefinition : " );
    List<CoverageDefinition> collection  = null;

    try {
        // call the static get method on the CoverageDefinitionService
        collection = service.getAllCoverageDefinition();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            CoverageDefinition currEntity  = null;
            Iterator<CoverageDefinition> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getCoverageDefinitionId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public CoverageDefinitionTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated CoverageDefinition
     *
     * @return CreateCoverageDefinitionCommand alias
     */
	protected CreateCoverageDefinitionCommand generateNewCommand() {
    CreateCoverageDefinitionCommand command = new CreateCoverageDefinitionCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Money(),  new Money(),  new Boolean(true),  CoverageType.values()[0] );

    return( command );
}

    /**
     * Returns a new populated CoverageDefinition
     *
     * @return UpdateCoverageDefinitionCommand alias
     */
	protected UpdateCoverageDefinitionCommand generateUpdateCommand() {
    UpdateCoverageDefinitionCommand command = new UpdateCoverageDefinitionCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new Money(),  new Money(),  new Boolean(true),  null,  CoverageType.values()[0] );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected CoverageDefinitionService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(CoverageDefinition.class.getName());

}
