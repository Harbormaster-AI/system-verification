/*
 * Generated by Harbormaster
 *
 *
 * Blueprint:
 * Spring Boot 3.5
 *
 * Domain Model:
 * Insurance Industry Domain Model
 *
 * Do not edit manually.
 ********************************************************
 * Turnstone National Bank Confidential
 *
 * 2026 Turnstone National Bank
 * All Rights Reserved.
 *
 * This file is subject to the terms and conditions defined in
 * file 'license.txt', which is part of this source code package.
 *
 * Contributors :
 *       Turnstone National Bank - General Release
 */
package com.harbormaster.test;

import java.io.*;
import java.util.*;
import java.util.logging.*;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import org.springframework.stereotype.Component;

import com.harbormaster.service.*;
import com.harbormaster.entity.*;
import com.harbormaster.api.*;

/**
 * Test Insurer class.
 *
 * @author    Harbormaster Dev Team
 */
@Component
public class InsurerTest
{

// constructors

    public InsurerTest(InsurerService service)
    {
        this.service = service;
        LOGGER.setUseParentHandlers(false);	// only want to output to the provided LogHandler
    }

// test methods
    @Test
    /**
     * Full Create-Read-Update-Delete of a Insurer, through a InsurerTest.
     */
    public void testCRUD() throws Throwable {
    try {
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "Beginning full test on InsurerTest..." );

        testCreate();
        testRead();
        testUpdate();
        testGetAll();
        testDelete();

        LOGGER.info( "Successfully ran a full test on InsurerTest..." );
        LOGGER.info( "**********************************************************" );
        LOGGER.info( "" );
    }
    catch( Throwable e ) {
        throw e;
    }
    finally  {
        if ( handler != null ) {
            handler.flush();
            LOGGER.removeHandler(handler);
        }
    }
}

    /**
     * Tests creating a new Insurer.
     *
     * @return    Insurer
     */
    public Insurer testCreate() throws Throwable {
    Insurer entity = null;

    LOGGER.info( "InsurerTest:testCreate()" );
    LOGGER.info( "-- Attempting to create a Insurer");

    StringBuilder msg = new StringBuilder( "-- Failed to create a Insurer" );

    try {
        entity = service.createInsurer( generateNewCommand() );
        assertNotNull( entity, msg.toString() );

        theId = entity.getInsurerId();
        assertNotNull( theId, msg.toString() + " Contains a null primary key" );

        LOGGER.info( "-- Successfully created a Insurer with primary key" + theId );
    }
    catch (Exception e)  {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + entity );

        throw e;
    }
    return entity;
}

    /**
     * Tests reading a Insurer.
     *
     * @return    Insurer
     */
    public Insurer testRead() throws Throwable {
    LOGGER.info( "InsurerTest:testRead()" );
    LOGGER.info( "-- Reading a previously created Insurer" );

    Insurer entity = null;
    StringBuilder msg = new StringBuilder( "-- Failed to read Insurer with primary key" );
    msg.append( theId );

    try {
        entity = service.getInsurer( new InsurerFetchOneSummary(theId) );

        assertNotNull( entity,msg.toString() );

        // for use later
        theId = entity.getInsurerId();

        LOGGER.info( "-- Successfully found Insurer " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests updating a Insurer.
     *
     * @return    Insurer
     */
    public Insurer testUpdate() throws Throwable {
    LOGGER.info( "InsurerTest:testUpdate()" );
    LOGGER.info( "-- Attempting to update a Insurer." );

    StringBuilder msg = new StringBuilder( "Failed to update a Insurer : " );
    Insurer entity = null;

    try {
        UpdateInsurerCommand updateCommand = this.generateUpdateCommand();

        // apply the current id as to update the fields of the current entity
        updateCommand.setInsurerId( theId );

        assertNotNull( updateCommand, msg.toString() );

        LOGGER.info( "-- Now updating the created Insurer." );

        entity = service.updateInsurer( updateCommand );

        assertNotNull( entity, msg.toString()  );

        LOGGER.info( "-- Successfully saved Insurer - " + entity.toString() );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() + " : primarykey-" + theId + " : entity-" +  entity + " : " + e );

        throw e;
    }

    return( entity );
}

    /**
     * Tests deleting a Insurer.
     */
    public void testDelete() throws Throwable {
    LOGGER.info( "InsurerTest:testDelete()" );
    LOGGER.info( "-- Deleting a previously created Insurer." );

    try {
        DeleteInsurerCommand deleteCommand = new DeleteInsurerCommand( theId );

        service.delete( deleteCommand );

        LOGGER.info( "-- Successfully deleted Insurer with primary key " + theId );
    }
    catch ( Throwable e ) {
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( "-- Failed to delete Insurer with primary key " + theId );

        throw e;
    }
}

    /**
     * Tests getting all Insurers.
     *
     * @return    Collection
     */
    public List<Insurer> testGetAll() throws Throwable {
    LOGGER.info( "InsurerTest:testGetAll() - Retrieving Collection of Insurers:" );

    StringBuilder msg = new StringBuilder( "-- Failed to get all Insurer : " );
    List<Insurer> collection  = null;

    try {
        // call the static get method on the InsurerService
        collection = service.getAllInsurer();

        if ( collection == null || collection.size() == 0 ) {
            LOGGER.warning( unexpectedErrorMsg );
            LOGGER.warning( "-- " + msg.toString() + " Empty collection returned."  );
        }
        else {
            // Now print out the values
            Insurer currEntity  = null;
            Iterator<Insurer> iter = collection.iterator();

            while( iter.hasNext() ) {
                // Retrieve the entity
                currEntity = iter.next();

                assertNotNull( currEntity,"-- null value object in Collection." );
                assertNotNull( currEntity.getInsurerId(), "-- value object in Collection has a null primary key" );

                LOGGER.info( " - " + currEntity.toString() );
            }
        }
    }
    catch ( Throwable e ){
        LOGGER.warning( unexpectedErrorMsg );
        LOGGER.warning( msg.toString() );

        throw e;
    }

    return( collection );
}

    public InsurerTest setHandler( Handler handler ) {
    this.handler = handler;
    LOGGER.addHandler(handler);	// assign so the LOGGER can only output results to the Handler
    return this;
}


    /**
     * Returns a new populated Insurer
     *
     * @return CreateInsurerCommand alias
     */
	protected CreateInsurerCommand generateNewCommand() {
    CreateInsurerCommand command = new CreateInsurerCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16) );

    return( command );
}

    /**
     * Returns a new populated Insurer
     *
     * @return UpdateInsurerCommand alias
     */
	protected UpdateInsurerCommand generateUpdateCommand() {
    UpdateInsurerCommand command = new UpdateInsurerCommand( null,  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  org.apache.commons.lang3.RandomStringUtils.randomAlphanumeric(16),  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  new HashSet<>(),  new HashSet<>() );

    return( command );
}
    //----

// attributes

    protected UUID theId  = null;
    protected InsurerService service = null;
    private Handler handler = null;
    private String unexpectedErrorMsg = ":::::::::::::: Unexpected Error :::::::::::::::::";
    private final Logger LOGGER = Logger.getLogger(Insurer.class.getName());

}
